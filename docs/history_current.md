# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**회전 명부는 `digest.md` 의 `## 완료` 절 «아카이브 명부» 줄이 정본이다.**
여기 있던 스물한 회전의 서술(233줄)은 그 줄과 내용이 겹쳤고, 검사가 강제하는 명부도
그쪽 하나뿐이라(`tests/test_docs.py` 의 `ArchiveIndexTest`) **개발 9(반복 269)가 이
자리에서 지웠다** — 회전으로는 300줄 상한을 못 맞추던 세 반복(309 → 372 → 418줄)의
원인이 이 명부였다. **지운 것은 머리말이지 반복 기록이 아니다** — 항목은 여전히
append 전용이고 수정·삭제 금지다. 각 회전의 사유는 `digest.md` 의 같은 줄에, 원문은
`history_<NNN>.md` 에 그대로 있다.

## 2026-09-03 21:30 | hidden-passage | 리뷰 2 | 시도0

- 한 일: 패스 A(백지·diff 만) 뒤 패스 B(계획·설계·README·과거 코멘트). 지적 **2건**
  (승인 필요 1·자동감 1) · 버린 후보 **3** · 제품 `src/` **0줄** · **패스 B 첫 값**([R51-4]).
- **[R51-3] `p` 가지의 술어가 «열린 집합» 이라 숨김이 풀린다 — medium · 승인 필요.**
  개발 2 가 재사용한 `tag not in _INLINE_TAGS and tag not in _NON_BLOCK_TAGS` 는
  **여집합**이라 모르는 태그가 전부 `<p>` 를 닫는다 — 브라우저는 **닫힌 목록**만 닫고
  커스텀 요소는 phrasing 이라 안 닫는다. `<p hidden>숨은<my-widget>속</my-widget>또 숨은
  김치찌개` 가 스텝 1 뒤 `[]` → HEAD 는 숨은 텍스트를 낸다(`amp-img`·`slot`·`datalist`·
  `nobr` 도). **표준 태그 127개 전수**로 과잉 **19**·미달 **0**·커스텀 요소는 **전부 과잉
  (무한 집합)**. **실제 DB 로 3/4 유출**(대조군 `<div hidden>` 0/1)이라 완료 기준 1 「0/5」
  와 README 계약 문장이 거짓이다. 설계 3절 천장은 «덜 낸다» 방향이라 이 «더 낸다» 를 안
  덮는다 — [R51-1] 수리의 거울상.
- **[R51-4] `indexer.py:41~46` 의 성능 계수가 낡았다 — low.** 「최악 0.352ms/1000자 → 10건
  123ms(예산 25%)」가 HEAD 에선 **0.439 → 153.5ms · 배정치의 125% · 예산의 31%** 다.
- **성능 회귀는 «값을 한다» 로 판정했다.** 기사 11.31 → **14.40ms(+27.3%)** 는 재현되지만
  **실물 p95 는 1.51 → 1.54ms**(예산의 0.006%)이고 +29% 는 **캡 최악에서만** 난다 — 산 것은
  숨은 텍스트 **5/5 → 0/5**, 잃은 것 0, +29% 의 **87%가 스텝 1**(이 스텝은 +2.9%p).
- 결과: 단위 **588건 OK** rc 0(13.445 · 마감 13.495초 · 맨몸·단독) · `passage_eval` 통과 · 러너 3회 전부
  맨몸·단독 · 출력 조작 0 · PR 0 · RED 0. **다음: 개발 3.** 교훈은 리뷰 1·[T2-1]·[R51-3]
  이 **한 뿌리**라 `digest ## 반복 실패` 에 **한 항목(3회)** 으로 합쳤다.

## 2026-09-03 22:20 | hidden-passage | 개발 3 | 시도0

- 한 일: **리뷰 2 의 반려 2건을 닫았다.** [R51-3] 은 «열린 집합» 술어를 **지우고**
  `_IMPLIED_END["p"]` 에 파싱 알고리즘의 "close a p element" 목록(49개)을 그대로 옮겨
  닫힌 집합으로 만들었다 — `_implied_end` 스태틱메서드를 **통째로 삭제**해
  `_INLINE_TAGS`·`_NON_BLOCK_TAGS` 가 숨김 판정에 **입력으로 아예 안 들어간다**(제품
  순증 0줄에 가깝고 파일 1개). 리뷰가 준 ②(`if "-" in tag` 가드)를 안 쓴 이유는 그것이
  커스텀 요소만 깁고 **표준 태그 과잉 29 를 그대로 남기기** 때문이다.
- **TDD — RED 를 먼저 봤다**: `Ran 588 tests` → `FAILED (failures=6)`
  (extract 의 subTest 5개 + `test_hidden_text_never_becomes_the_passage`, 예:
  `AssertionError: '김치찌개 김치찌개' != '김치찌개는 배추로 만든다.'`), 구현 뒤
  `Ran 588 tests in 13.418s / OK` rc 0. 단언 자리는 **가장 깊은 모양**이다 —
  `indexer.passages()` 를 임시 DB 에서 부르는 행을 새로 넣었다.
- **전·후를 둘 다 제품 코드에서 쟀다.** 표준 태그 **135개 전수**(리뷰의 127 목록 + 폐기
  요소 8): 과잉 **29 → 0** · 미달 **0 → 0** · 커스텀 요소(`my-widget`·`amp-img`·
  `ion-icon`) **전부 과잉 → 없음**. 임시 DB 유출 **5/6 → 0/6**(대조군 `<div hidden>`
  전·후 0). `search` 는 리뷰가 과잉으로 셌지만 **현행 명세는 `<p>` 를 닫는 쪽**이라
  넣었다 — 빼면 브라우저가 보여 주는 텍스트를 지우는 «덜 낸다» 쪽 오류다(더 나쁜 방향).
- **[R51-4] 를 다시 쟀다** — 캡 35,000자 · 30회 중앙값 · 세 모양: 1,000자당 최악
  기점 0.323 · HEAD 0.409 · 이번 수정 **0.413**. 계수를 **0.44**(리뷰 2 실측 0.439 를
  **낮은 쪽으로 안 내렸다**)로, 최악을 **154ms · 예산의 31%** 로 적었다. **리뷰는 이것을
  «서술이라 스위트가 안 잡는다» 고 했는데 실제로는 `tests/test_indexer.py` 가
  `0.352`·`500*0.25` 를 박아 두고 있었다** — 서술만 고쳤으면 거짓 초록이 남는다. 배정치를
  **25% → 1/3** 로 다시 그었고(캡·`PASSAGE_LIMIT` 무변) 기록 자리는 `indexer.py:41~51`
  주석 · 같은 단언의 주석 · `docs/project.md`. 도로 25% 로 조이려면 캡을 28,000 으로
  내려야 하고 그것은 «긴 문서 뒷부분 근거» 를 자르는 별개 판단이라 범위 밖으로 적었다.
- **이미 초록이던 축을 옛 `extract.py` 를 임시 모듈로 띄워 나란히 재확인했다** — 숨은
  텍스트 **0/6** · 본문 **6/6** · 코퍼스 64문서 블록 **253 차이 0**(통짜 64블록도 0) ·
  불변식 깨진 문서 0 · `tests/` HTML 리터럴 **342개 중 달라진 6개는 전부 이번 fixture** ·
  음성 6종 6/6 · [R51-1] 형제 누출 4종 · 변이 ⑤/T2-1/T2-2 자리 그대로 죽는다.
- **변이 10개 중 8개 사망**: ①`p` 항목 통째 삭제(5) · ②**열린 집합 복원(6 — extract 5 +
  `passages` 1)** · ③div(1) · ④li(1) · ⑤`while`→`if`(3) · ⑥tr(1) · ⑦br 추가(1) ·
  ⑧slot 추가(1). **살아남음 ⑨caption 삭제 · ⑩search 삭제** — 49개 집합에서 원소 하나를
  빼는 변이라 전부 죽이려면 단언이 구현 리터럴을 그대로 되뇌게 된다(동어반복). 보상
  통제는 **다시 돌릴 수 있는 135태그 전수 대조**이고 8점 미만으로 남긴다.
- 결과: 단위 **588건 OK** rc 0(13.479초 · 맨몸·단독) · `passage_eval` 통과(블록 253 ·
  정확도 100.0% · p95 **1.51ms**) · 제품 파일 2개(`extract.py`·`indexer.py`) · 새 의존성 0 ·
  `e2e/`·`docs/specs/`·`data/crawl.db` 무변 · `.mutation-lock` 되돌림 True(`git diff` 확인) ·
  PR 무접촉. `README.md:66` 계약 문장은 **다시 참**이고 단위 588 · e2e 20 이라 README 무변.
- 다음: **테스트 3** — 스위트 밖 축(코퍼스·`quality_eval`)을 다시 재고, 살아남은 변이
  ⑨⑩ 이 가리키는 «집합 원소 하나» 자리에 갭 탐색을 건다.

## 2026-09-03 23:10 | hidden-passage | 테스트 3 | 시도0

- 한 일: **갭 탐색이 8점 이상 셋을 찾아 같은 반복에서 닫았다.** 제품 `src/` 2파일
  (`extract.py` **+29줄** · `indexer.py` 주석만) · 테스트 4건 추가 · 새 파일 0 · 새 의존성 0.
  단위 **588 → 592건 OK** rc 0(13.479초 · 맨몸·단독) · **e2e 20종 전수 rc 0**.
- **[T3-1] 깨진 marked section 하나가 그 실행의 색인을 통째로 날린다 (9점 · 신뢰 경계).**
  `_markupbase` 는 아는 키워드(`cdata`·`include`·`if`…)가 아닌 `<![foo]>` 를 만나면
  `error()` 로 보내고 `HTMLParser` 는 그것을 안 덮는다 — **`NotImplementedError` 가
  `feed()` 밖으로 나간다**(3.9.6. 3.13 은 `AssertionError`). 크롤 HTML 은 남이 쓴
  입력이고 `index_pages()` 는 문서마다 try 가 없이 `commit()` 이 루프 **끝**에 있어,
  **여덟 글자짜리 페이지 한 장이 그 실행이 넣던 색인을 통째로 지운다**. `/passages` 는
  500 이다. 기존 `test_broken_html_no_raise` 가 못 밟은 이유는 저기서 깨진 것이 **태그**고
  여기서 깨진 것은 **선언**이라 파서의 다른 가지로 가기 때문이다.
- **파서가 둘이라 수리도 둘이다 — 이것이 이 갭의 값이다.** 공통 부모 `_Parser` 하나를
  두고 `parse_marked_section` 만 덮어 명세대로 **bogus comment**(다음 `>` 까지 버린다)로
  떨어뜨렸다. `_TextParser` 와 `_MetaRobotsParser` 가 **둘 다** 상속한다 — 후자가 더 이른
  자리다(`index_pages()` 가 `is_noindex()` 를 **먼저** 부르므로 거기서 터지면 앞 수리는
  닿지도 못한다). 변이 ⑥(`_MetaRobotsParser` 만 안 고친다)이 **처음엔 초록으로 살아
  있었고** `TestIsNoindex` 에 단언을 세워 죽였다.
- **[T3-2] 암묵적 닫기가 이름표 스택(`_open`)을 안 걷었다 (8점).** 개발 2 가 넣은
  `_implied_end` 는 `_els` 만 pop 해서, `<p>첫 문단<div>둘째` 의 꼬리 텍스트가 **이미 끝난
  `<p>` 의 이름표를 달고** 나왔다(HEAD 실측 `[('p','첫 문단'),('div','둘째 문단'),
  ('p','꼬리 텍스트')]`). 기존 테스트가 못 밟은 이유는 그것이 **닫는 태그**로 같은 계약을
  재기 때문이다 — 종료 태그가 아예 없는 문서가 안 재지고 있었다. **값이 두 개다**:
  이름표는 아직 소비자가 0곳이지만(digest `[6]`) 갈림길 6 을 재는 축이고, **비용은
  실물이다** — 안 걷으면 `_open` 이 «안 닫힌 `<p>` 수» 만큼 쌓이고 닫기 관용구가 그
  길이에 붙어 깊이²다(캡 35,000자를 `<p>가<span>나</span>` 로 채운 실측 `_open` 최대
  **1250 → 1** · **0.422 → 0.304 ms/1,000자**).
- **넷 다 고치기 전 코드에서 빨간 것을 눈으로 봤다**(HEAD `extract.py` 를 제자리에 놓고
  `discover -s tests -k` 로 각각 · 끝나고 원복 True): 셋은 **예외**(`errors`), 이름표는
  **값 불일치**(`failures=1`). 작업 트리에서는 넷 다 OK.
- **첫 탐침이 거짓 RED 를 냈고 그것을 잡은 것이 이번 반복의 러너 교훈이다** — 저장소
  루트에서 `python3 -m unittest test_extract.<...>` 로 불렀더니 `tests/` 가 `sys.path` 에
  없어 **ImportError 가 `FAILED (errors=1)`** 로 나왔다. 넷이 나란히 «빨갛다» 고 나와서
  그럴듯했다. 판정 줄만 보고 적었으면 RED 근거가 통째로 가짜였다 — **러너를 바꾸면
  색이 바뀐다**(digest `[9]`·`[5]` 와 같은 뿌리). `discover -s tests -k` 로 고쳐 다시 쟀다.
- **변이 여섯을 심어 전부 눈으로 봤다**(매번 원복 · `python3 -B` · 끝나고 `git diff --
  src/` 확인 · 커밋된 변이 0): ①이름표 걷기 삭제 **실패 1** · ③오버라이드 삭제 **실패 6**
  · ⑤`parse_bogus_comment` → `i+2` **실패 3** · ⑥`_MetaRobotsParser` 안 고침 **실패 1**.
- **살아남은 둘은 «못 죽인 것» 이 아니라 각각 이유가 다르다.** ②(pop 가드를 빼고 늘
  pop)는 **등가 변이**다 — 열기 15 × 15 × 닫기 8 × 방아쇠 7 = **12,600 조합 전수에서
  갈라진 입력 0개**. 동어반복 단언을 세우는 대신 가드를 **남겼다**: 형제인
  `handle_endtag` 가 쓰는 관용구와 같은 모양이고, `option` 이 `_NON_BLOCK_TAGS` 로
  옮겨지는 것 같은 그럴듯한 편집에서 곧바로 갈린다. ④(`AssertionError` 가지 삭제)는
  **3.9.6 에서는 죽일 수 없다** — 그 가지는 3.13 이 예외 종류를 바꾼 것을 받는 이식성
  가지다. 신뢰 경계라 «못 죽이니 지운다» 로 안 갔고 주석에 사유를 적었다.
- **[R51-4] 의 계수를 오늘 다시 쟀다 — 참이다. 그런데 기록이 틀린 것을 붙들고 있었다.**
  기록된 모양(낱말마다 `<b>`/`<i>`)을 그대로 재니 **0.436 ms/1,000자**(기록 0.44)라
  여전히 참이고, 이번 반복의 두 수리가 그 모양에 붙인 비용은 **0** 이다. 그런데 계수가
  세 번 낡은(0.118 → 0.352 → 0.44) 뿌리는 «위키·CMS 출력» 이라는 **이름이 재현 불가능**
  하다는 것이었다 — 재는 사람마다 다른 점을 잰다. **축은 태그 밀도다**:
  `ms/1000자 ≈ 0.0025 × 태그/1000자`(스윕 0.436@169 · 0.502@200 · `<li>가</li>` 0.535 ·
  `<p>가</p>` 0.665). **캡 안 진짜 최악은 안 닫은 `<p>가` 반복의 0.901**(`PASSAGE_LIMIT`
  10건 **315ms · 예산의 63%**)로 기록된 154ms 의 **2.1배**다.
- **그래서 숫자를 또 올리는 대신 법칙과 천장을 적었다.** 초록을 유지하려고 0.47 을 고르면
  역산이고, 진짜 천장 0.901 을 단언에 넣으면 **RED** 인데 그 처방은 캡을 35,000 → 18,000
  으로 내리는 **계획 몫**이다(«긴 문서의 뒷부분 근거» 를 반으로 자른다). 대신 배정치
  **1/3 이 «여유» 가 아니라 그 모양 배수를 흡수하는 자리**임을 기록 자리 셋
  (`indexer.py:41~51` · `tests/test_indexer.py` 성능 단언 주석 · `docs/project.md`)에
  **같은 문장으로** 적었다 — 1/3 을 지키면 최악 모양이 500ms 안에 든다(154 × 2.1 = 323).
  단언 리터럴 `0.44`·`500/3` 은 **안 건드렸다**(둘 다 오늘 참이다). 남은 갭은 «단언이
  여전히 한 점을 붙든다» 이고 `digest` 에 `[7]` 로 등재했다.
- **스위트 밖 축을 전수로 다시 쟀다 — 기준선 그대로다.** `passage_eval` 블록 **253** ·
  정확도 **100.0%**(398/398) · 채택률 99.5% · p95 **1.52ms** · `quality_eval` ko **20/20** ·
  en **19/20** · 1위 **39** · 매치 평균 **14.0**(최소 11) · `perf_search` p95 **8.91ms** ·
  `search_api` p95 **2.11ms** · `perf_crawl` **10.22 / 10.24/s** · 문서집합 sha1
  `541d455a…c078654b` **무변** · `design_check` 4축 통과 · `data/crawl.db` 무변.
- **러너 규율 — 어긴 것도 적는다.** 판정 실행은 맨몸·단독이지만 **파이프 2회**를 썼다:
  회전 직후 아카이브 명부가 아직 안 맞아 났던 실패(`ArchiveIndexTest`)의 **이름을 찾느라**
  `| tail`·`| sed` 로 걸렀다. 진단용이었고 뒤에 맨몸으로 다시 돌려 판정을 냈지만 규율은
  «판정에만 맨몸» 이 아니라 «러너에 파이프를 안 붙인다» 라 **0회가 아니다.** 출력 조작 0회.
- 결과: 단위 **592건 OK** rc 0 · e2e **20/20 rc 0** · 새 단언 메서드 **4** ·
  `README.md` 단위 건수 **588 → 592** · **RED 0**(넷 다 같은 반복에서 초록) · 재시도 0 ·
  `e2e/`·`docs/specs/` 무변 · PR 무접촉.
- 다음: **리뷰 3.** 겨눌 자리 둘 — ① `_Parser` 가 `html.parser` 의 **다른 예외 가지**도
  덮어야 하는지(marked section 말고 `parse_html_declaration`·`parse_pi` 쪽), ② 성능 단언이
  아직 한 점을 붙드는 것(위 `[7]`). 살아남은 변이 ②④ 는 **사유를 적고 남긴 것**이라
  리뷰가 그 판단부터 본다.

## 2026-09-04 00:55 | hidden-passage | 리뷰 3 | 시도0

- 한 일: 패스 A(백지·diff 만) 뒤 패스 B(계획·설계·README·과거 반복). 지적 **2건**
  (승인 필요 1 · 자동 수정 1 적용) · 버린 후보 **3** · 제품 `src/` **0줄** · 심은 변이 0.
- **[R51-1]~[R51-4] 를 판정 줄로 직접 확인했다** — ① `<ul><li hidden>숨은<li>보이는</ul>`
  → `[('li','보이는 항목')]` ② 닫기가 제자리 역방향(리스트 생성 0)이고 캡 실측 재현
  (안 닫은 `<p>가` **0.900**/기록 0.901 · 위키·CMS 0.484@186태그 · 법칙 `0.0025 × 태그` 성립)
  ③ `<p hidden>숨은<my-widget>숨은` → `[]` / `<p hidden>숨은<div>보이는</div>` →
  `[('div','보이는 뒤')]` ④ `indexer.py:41~51` 의 세 숫자가 오늘 전부 재현.
- **[R51-5] 종료 태그 생략이 허용된 요소 여덟이 `_IMPLIED_END` 키에 없다 — medium ·
  승인 필요.** 집합 주석이 멤버십 규칙을 스스로 적어 뒀는데(「종료 태그 생략이 허용된
  요소」 — [R51-1] 수리의 근거) 키는 여덟뿐이고 `thead`·`tbody`·`tfoot`·`caption`·
  `colgroup`·`optgroup`·`rt`·`rp` 가 빠졌다. **명세가 허용하는 정상 HTML 여섯 모양에서
  보이는 문단이 통째로 사라진다**(`<table><thead hidden><tr><th>숨은<tbody><tr><td>보이는`
  → **`[]`**, `</thead>` 를 닫으면 정상 — 갈리는 것은 종료 태그 생략 하나다). 방향은
  계획 8절이 「제일 위험」이라 적은 **오탐**이다.
- **왜 세 반복이 못 봤나 — 전수 대조가 한 축에만 걸려 있었다.** 개발 3 의 「표준 태그
  135개 전수」는 **값 축**(어떤 시작 태그가 `<p>` 를 닫는가)이었고 **키 축**(어떤 요소가
  암묵적으로 닫히는가)은 한 번도 안 쟀다. 리뷰가 누출 방향을 **양축 전수**로 쟀다 —
  137 × 137 × 숨김 4모양 = **75,076 조합**에서 숨은 텍스트가 블록으로 나온 것은
  `_VOID_TAGS` 14개뿐이고 그것이 설계가 고른 정답이다(void 는 자식을 못 가져 브라우저에서도
  보인다). **누출 0 · 과잉 0**, 남은 것은 미달이다. 수리는 개발 4 몫이고 **키를 늘리는 것은
  과잉 방향**이라 수리 뒤 이 전수를 다시 돌리는 것이 단언보다 센 관문이라고 적었다.
- **[자동 수정] 설계 문서가 코드와 모순됐다 — `design_hidden-passage.md` 6절에 적었다.**
  6절이 「`_TextParser` 는 무변경」, 머리·7절이 「색인 경로 무변경 · 제품 파일 하나」인데
  테스트 3 이 `_TextParser`·`_MetaRobotsParser` 의 부모를 `_Parser` 로 바꿨다([T3-1]).
  룰 렌즈 1 이 「코드만 바꾸고 문서를 남겨두면 반려」라 예외를 적었고 **판단은 안 뒤집었다**
  (신뢰 경계 · 바뀐 것은 죽는 가지뿐 — 표본 343개에서 `extract_text` 본문·제목 차이 0).
- **겨눌 자리 둘은 실측으로 닫아 지적이 없다.** ① `HTMLParser` 는 `error()` 8곳을 가진
  `_markupbase.parse_declaration` 을 **한 번도 안 부른다**(소스에 주석 한 줄뿐) ·
  `parse_pi` 는 자기 것으로 덮여 `error()` 가 없다 · `_scan_name` 은 방금 덮인 자리
  아래에만 있다. 퍼즈로도 쟀다 — `<!`·`<![`·`<?`·`</` 조합 **입력 3,150개 × 함수 3개에서
  예외 0건.** ② `digest [7]` 로 이미 등재됐고 처방이 계획 몫이라는 판정도 유효하다.
- **버린 후보 3**: 안 닫힌 인라인이 얹힌 자리(**0점** — 개발 2 가 이미 천장으로 적었고
  깨진 입력이다. [R51-5] 와 갈리는 이유가 여기다) · `dict(attrs)` 중복 속성 순서(50점) ·
  `font-size:00`(30점).
- 결과: 단위 **592건 OK** rc 0(`Ran 592 tests in 13.579s`) · **e2e 20종 전수 rc 0**(수치
  전부 기준선: 블록 253 · 정확도 100.0% · p95 1.52ms · ko 20/20 · en 19/20 · `perf_search`
  8.92 · `search_api` 2.13) · 완료 기준 1~6 을 리뷰가 직접 돌려 대조(숨은 텍스트 **0/5** ·
  회귀 표본 343개 중 숨김 아닌 차이 **0** · `data/crawl.db` sha256 무변) · RED 0 · 재시도 0 ·
  PR 무접촉 · `data/`·`e2e/`·`docs/specs/` 무변.
- **러너 규율 — 어긴 것도 적는다.** e2e 20종을 `for` 루프 + 리다이렉션으로 돌려 rc 를
  한 번에 셌다(**리다이렉션 20회**) — 출력을 저장했을 뿐 조작은 0회고 수치는 로그 원문에서
  읽었지만 규율은 「러너에 파이프·리다이렉션을 안 붙인다」라 0회가 아니다. 단위는 파이프
  1회 뒤 맨몸 재확인.
- **집안일 — 이 항목을 붙인 뒤 세니 324줄이라 그 자리에서 회전했다**(서른 회전 연속 지각 0).
  개발 2(293) 한 항목 **56줄**만 `docs/history_050.md` 로 밀어 **268줄**. 경계를 「테스트 2
  바로 앞」에 그은 이유는 [R51-5] 의 대조 원본이 테스트 2·리뷰 2·개발 3·테스트 3 이기
  때문이다. `digest.md` 「아카이브 명부」에 이름을 더하고 회전 서술을 같은 줄에 이어 붙여
  **200줄 유지 · 새 색인 구멍 0**.
- 다음: **개발 4** — 룰 6절대로 승인 필요 1건이라 개발 phase 로 복귀. TDD 로 여섯 모양의
  RED 를 먼저 보고 키 여덟을 명세 삽입 모드에서 옮긴다.

## 2026-09-04 09:24 | hidden-passage | 개발 4 | 시도0

- 한 일: 리뷰 3 `[R51-5]` 를 TDD 로 닫았다. 제품 `src/websearch/extract.py` **한 파일
  +17/-2**(한도 20 안 · 계획 「건드릴 파일」 안) · `tests/test_extract.py` +38 ·
  `README.md` 숫자 1줄. 새 파일 0 · 새 의존성 0.
- **RED 를 먼저 봤다.** 리뷰가 실측한 여섯 모양을 그대로 테스트로 옮겨 돌리니
  `FAILED (failures=6)` — thead·tbody·caption·colgroup·optgroup 다섯은 `[]`, rt 는
  `['漢']`. **`rt` 만 기대값을 고쳤다**: 리뷰 표의 「브라우저」 칸(`漢 보이는`)을 베꼈는데
  `rt` 는 `_INLINE_TAGS` 라 경계에 공백이 안 들어간다(`Kim<b>chi</b>` 규칙). 실측으로
  `extract_text` 도 `漢보이는` 임을 확인하고 `["漢보이는"]` 로 정정했다 — **재는 것은
  띄어쓰기가 아니라 `보이는` 이 나오느냐다.**
- **GREEN — 키를 열여섯으로 채웠다.** 명세 「optional tags」 전부다. 표 쪽은 삽입 모드
  넷(in cell·in row·in table body·in caption)이 **같은 목록**을 쓰므로 `_TABLE_PARTS`
  하나를 두고 빼기로 적었다 — 빼는 것이 «형제가 아니라 자식» 이다(`<td>` 는 `<tr>` 을
  안 닫고, `<tr>` 은 `<tbody>` 를 안 닫고, `<col>` 은 `<colgroup>` 안이다).
- **값 축도 함께 넓혀야 했다 — 리뷰 제안이 키만 적은 자리다.** 암묵적 닫기는 스택
  꼭대기만 보므로 `<tbody hidden><tr><td>…<tfoot>` 에서 `tfoot` 이 `td`·`tr` 을 못 걷으면
  루프가 셀에서 멈춰 `tbody` 에 못 닿는다. `tr`·`td`·`th` 의 값에 구획 태그를 더했다.
- **관문은 단언이 아니라 전수였다**(리뷰가 지정한 그것). 태그 139 × 139 × 숨김 4모양
  **77,284 조합**을 HEAD 모듈과 나란히 돌렸다 — **누출 7,784 → 7,784(새 누출 0)** 이고
  그 7,784 는 `14 void × 139 × 4` 로 딱 떨어져 누출 집합이 여전히 `_VOID_TAGS` 뿐이다.
  **새로 사라진 것 0 · 수리된 것 248**(바깥 태그 열하나: caption·colgroup·optgroup·rp·
  rt·tbody·td·tfoot·th·thead·tr).
- 결과: 단위 **593건 OK** rc 0(`Ran 593 tests in 13.233s` · 맨몸·단독) · **e2e 20종 전수
  rc 0**(블록 **253** 무변 · 정확도 **100.0%**(398/398) · 채택률 99.5% · p95 **1.50ms** ·
  ko 20/20 · en 19/20 · `perf_search` 8.71 · `search_api` 2.08 · `design_check` 4축) ·
  회귀 표본 134개(`crawl.db`·`e2e/quality/corpus.json`·테스트 HTML 리터럴)에서
  **`extract_text` 차이 0** · `extract_blocks` 차이는 새 fixture 1건뿐 ·
  `data/crawl.db` sha256 `85c96744…75bda18` 무변 · `data/`·`e2e/`·`docs/specs/` 무변.
  `_IMPLIED_END` 를 읽는 곳은 `_BlockParser.handle_starttag` 하나(grep 실측)라 색인
  경로는 구조적으로 안 지나간다. RED 0 · 재시도 0 · PR 무접촉.
- **README 숫자가 즉시 울었다** — 테스트 +1 에 `test_readme` 가 592≠593 으로 FAILED.
  하루가 아니라 한 반복 안에서 값을 증명한 가드다.
- 러너 규율: 단위·e2e 전부 파이프·리다이렉션 0회(맨몸). 전수 스캔은 러너가 아니라
  스크래치패드 스크립트다.
- 다음: **테스트 4** — 변이로 새 키·새 값을 각각 재고, 특히 `_TABLE_PARTS` 뺄셈 세 줄이
  단언에 걸려 있는지 본다(빼기를 지우는 변이가 초록이면 자식 방향 가드가 비어 있다).
