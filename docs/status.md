---
signal: GREEN
phase: 설계
step: 1
attempt: 0
iteration: 222
updated: 2026-09-01
ctx: 62
night_iterations: 82
night_red: 0
night_retries: 0
plan: focus-ring-presence
---

# 현재 상태

**계획 44 `focus-ring-presence` 계획 1/1 완료 — 다음 반복은 설계다.**
계획서 `docs/plan_focus-ring-presence.md` · 브랜치 `loop/focus-ring-presence` ·
기점 `bc40ea9`. 제품 코드 **0줄** (이 반복은 문서만).

**사용자가 승인 게이트를 열었다.** 앞 반복이 *"개발 스텝을 여는 일이라 승인 필요"* 로
닫아 둔 후보 하나가 그것으로 열렸고, 그것이 이 계획이다.

## 방금 것 (2026-09-01 · 계획 1/1)

**탐색은 1~8순위 전수로 돌았고 착수 가능은 1건이다.** 전부 맨몸(파이프 0회).

- 1순위 **실패 테스트 0건** — `473건 OK 11.754초`
- 2순위 해당 없음(린터·타입체커 없음) · 3순위 **코드 TODO 0건**(`src` `tests` `e2e` `scripts`) ·
  4순위 `docs/candidates.md` **파일 없음** · 5순위 `digest ## 보류` **0건** ·
  8순위 **이슈 0건**(`gh issue list` 빈 출력)
- 6순위 열린 항목 **21건 → 착수 가능 1건**(포커스 링의 존재) ·
  7순위 `## 반복 실패` **3건 전부 판정**(계획서 5절)

**착수 탐침이 후보의 구멍을 다섯 배로 넓혔다.** 링을 죽이는 변이 **6종을 심어**
(심긴 것을 먼저 단언) `check_contrast` 를 돌리니 **여섯 전부 기준 위반 0 · 측정 불능 0**:
규칙 통째 삭제 · `outline:none` · `var(--focus)`→`var(--line)` ·
`:focus-visible`→`:focus` · `outline-offset` 제거 · `outline-offset:0`.
`digest` 항목은 이 중 **첫째 하나만** 쟀다.

**그중 둘은 후보가 몰랐던 갈래다.** `var(--focus)`→`var(--line)` 은 *재는 색과
그려지는 색을 가른다*(`--line` 은 라이트에서 1.08:1). `outline-offset` 을 건드리는
둘은 `NONTEXT_PAIRS` 주석이 명시한 **짝 선택의 근거 자체를 무효로 만든다** —
아래 ② 2번이 가정에서 실측 가능한 것으로 바뀐다.

## 다음 반복이 할 것 — 설계

`rules/design.md` 1절 **"대안이 2개 이상 갈림"** 에 걸린다. 갈림길이 둘이고,
**둘 다 탐침으로 확인한 것이지 추정이 아니다**(계획서 6절):

- **A. 종료 코드** — `digest` 처방은 *"없으면 종료 2"* 인데 **같은 파일의 형제
  관용구는 종료 1** 이다(`check_mobile` 의 `overflow-wrap:anywhere` 가 없을 때
  `fail=1 unm=0` — 실측). 계획 41 이 물려받은 처방이 *"한 칸 틀렸었다"* 던 자리와 같다.
- **B. 무엇을 자국으로 삼나** — 응답 CSS 실측 출현 수는 `var(--focus)` 1회 ·
  `:focus-visible` 3회 · `outline` 2회 · `outline-offset` 1회. 어디에 거느냐로
  잡는 변이 집합이 갈린다.

깰 가정(`rules/design.md` 3-2절): *"응답 바이트 안의 자국으로 '링이 보인다'를
대리할 수 있다"*. 거짓이면 개발로 가지 않는다.

## 기점 — 원격 갈라짐이 닫혔다 (2026-09-01 실측)

**앞 반복이 남긴 "PR #4 미병합" 전제는 낡았다.** 이번 반복에 다시 재니:

- `origin/main` 은 `494313b`(**PR #5 병합 커밋**)이고, PR #4 도 그 앞에 들어가 있다.
- `main` 에 계획 42·43 의 코드 산출물이 **둘 다 있다** —
  `tests/test_docs.py` 의 `CitationPatternTest` · `e2e/design_check.py` 의 `NO_PAIR`.
- `origin/main` 의 **트리는 `bc40ea9` 의 부모와 같다**(양쪽 tree 해시 `066c264…1584bf`).

**그래서 기점을 `bc40ea9` 로 잡았다.** `main` 기점으로 열면 반복 221 의 기록 넷이
반복 220 상태로 되돌아가 아래 ② 3번(기록 3파일 불일치)을 여섯 번째로 만든다.
사유는 계획서 머리에 적었다(`rules/git.md` 1절이 요구하는 기록).

**남은 것 (사람 결정)**: `bc40ea9`(반복 221 기록, 문서만) 와 이 반복의 커밋을
`main` 으로 보내는 PR. 무인 모드는 병합하지 않는다.
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로
해시가 새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 ② — 앞 반복이 넘긴 셋 (1·2 는 열려 있다)

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1, 입력창의 유일한 경계). `NO_PAIR`
   사유가 사실대로 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1** — `outline-offset:2px` 의 흰 틈 1겹이
   유일한 구분이다. **이번 탐침이 이 항목의 성격을 바꿨다** — 그 틈은 CSS 선언
   하나이고 지금 아무도 안 붙들고 있어, 계획 44 가 그것을 재기 시작하면 이 판단이
   가정이 아니라 실측 대상이 된다. 나머지 세 변이 3.56:1 이라 여전히 급하지는 않다.
3. **기록 3파일 불일치가 5회째 재발** — 처방은 `tests/test_docs.py` 에 검사 한 줄
   (`metrics.md` 의 누적 반복 수와 `status.md` 의 `iteration` 이 어긋나면 죽는다).
   **다음 계획의 1순위 후보로 남겼다**(계획서 5절) — 한 번에 한 계획만 활성이라
   이번엔 안 열었고, 포커스 링을 먼저 고른 근거는 *사양 문장이 실제로 깨지는 쪽*이
   그것이라는 점이다.

## 한도 (매 반복 확인)

- `main` 직접 커밋 금지 · `--no-verify` 금지 · 외부 네트워크 금지 ·
  `docs/specs/` 읽기만 · `data/crawl.db` 무변경(sha256 `85c96744…5bda18` 확인) ·
  의존성 추가 금지(stdlib 만 — 이 반복은 코드 0줄).
- **검증은 맨몸으로. 16회에서 멈춰 있고 이 반복도 러너 파이프 0회 — 세 반복 연속이다.**
  항목이 적어 둔 닫는 조건(*"한 반복만 더 0회면 접는다"*)을 **이 반복이 채웠다.**
  다음 반복이 확인하고 `digest` 에서 접는다.
  ※ 문서를 잘라 읽는 데는 `grep`·`sed` 를 썼다 — 항목의 정의는 *"러너를 파이프로
  감싼다"* 이고 러너가 아닌 것에는 판정 줄도 종료 코드도 없다. 세지 않되 적어는 둔다.
- `docs/digest.md` 200줄 상한 · 열린 항목 **49**(위 ① 이 계획이 됐으므로 다음
  DONE 에서 닫는다 — 지금 지우지 않는다) ·
  `history_current.md` 300줄 상한 — 지금 **119줄**로 여유가 넉넉하다.
