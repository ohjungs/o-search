---
signal: GREEN
phase: 리뷰
step: 2
attempt: 0
iteration: 294
updated: 2026-09-03
ctx: 62
night_iterations: 123
night_red: 1
night_retries: 0
plan: hidden-passage
---

# 현재 상태

**직전 반복이 겨누라고 적은 두 자리에서 변이가 살아 있었고, 같은 반복에서 닫았다.**
제품 `src/` **0줄** · 고친 파일 **1개**(`tests/test_extract.py` **+15줄**) · 새 파일 0 ·
새 의존성 0. 둘 다 **기존 표 주도 테스트의 행**이라 단언 메서드 수는 **588 유지**고
`README.md`·`tests/test_readme.py` 는 무변경이다. 단위 **588건 OK** rc 0(맨몸·단독) ·
**e2e 20종 전수 rc 0**.

## 갭 둘 — 둘 다 「588건 전부 초록」이던 변이다

| # | 변이(살아 있던 것) | 등재한 행 | 점수 |
|---|---|---|---|
| [T2-1] | `p` 가지의 `and tag not in _NON_BLOCK_TAGS` 절 삭제 | `<p hidden>숨은<br>여전히 숨은<img src=k.jpg>또 숨은 김치찌개` | 8 |
| [T2-2] | 암묵적 닫기의 `while` 을 **최대 2회**로 | `<table><tr hidden><th>숨은<p>여전히 숨은<tr><td>보이는</table>` | 8 |

**[T2-1]** `_implied_end` 의 `p` 가지는 술어 **둘**(`tag not in _INLINE_TAGS and tag not
in _NON_BLOCK_TAGS`)을 읽는데 기존 케이스가 `<b>` 하나뿐이라 **앞 절만 재지고 있었다**.
`<br>`·`<img>` 는 인라인이 아니지만 문단 경계도 아니라 `<p>` 를 **안 닫아야** 한다 —
닫으면 그 뒤가 숨김에서 풀려 **숨은 텍스트가 근거로 샌다**(완료 기준 1번 「0/5」의 반대
방향). 직전 반복이 「`p` 가지가 두 집합에 결합돼 있다」고 남긴 것은 결합이 아니라 **구멍**
이었다.

**[T2-2]** 개발 2 가 변이 ⑤(`while`→`if`)를 죽였는데 **한 칸 아래**가 다시 초록이다 —
새로 넣은 겹쳐 닫기 케이스가 마침 **두 번**짜리였다. 셋을 겹쳐 닫는 자리는 숨긴 행 안의
셀 안에 안 닫힌 `<p>` 가 있을 때(`p`→`th`→`tr`)이고 걸러진 표의 행은 실물의 기본형이다.
**같은 행이 `_IMPLIED_END` 의 `th` 값을 처음 밟는다** — 「`th` 줄 삭제」 변이도 588건
전부 초록이었다(값이 표에 있는데 아무 케이스도 안 썼다). 한 행이 변이 둘을 같이 죽였다.

## 변이 넷 — 새 단언 전·후를 둘 다 봤다

`.mutation-lock` 아래 제자리 · 매번 원복 · 러너 `python3 -B` · 끝나고 `git diff -- src/`
로 **원복 확인**(「초록이다」로 확인하지 않았다) · 커밋된 변이 **0**.

| 변이 | 새 단언 전 | 새 단언 후 |
|---|---|---|
| ⓐ `del self._els[i:]` → `[i:i+1]` | 실패 **1** | (그대로 1 — 아래 8점 미만) |
| ⓑ `p` 가지의 `_NON_BLOCK_TAGS` 절 삭제 | **초록 588** | 실패 **1** |
| ⓒ `while` 을 최대 2회로 | **초록 588** | 실패 **1** |
| ⓓ `_IMPLIED_END` 의 `th` 줄 삭제 | **초록 588** | 실패 **1** |

## 스위트 밖 축 — 전부 다시 쟀고 기준선 그대로다

- `passage_eval` 블록 **253** · 정확도 **100.0%**(398/398) · 채택률 **99.5%** ·
  p95 **1.52ms**(기준선 1.51 · 예산의 0.3%).
- `quality_eval` ko **20/20** · en **19/20** · 1위 **39** · 매치 평균 **14.0**(최소 11).
- `perf_search` p95 **8.78ms**(기준선 8.97) · `search_api` p95 **2.10ms**(2.11) ·
  `perf_crawl` **10.24 / 10.21**건/초.
- 문서집합 sha1 `541d455a…c078654b` **무변** · `data/crawl.db` sha256
  `85c96744…75bda18` **무변** · `e2e/`·`docs/specs/` 무변.
- 숨은 텍스트 **0/6** · 본문 **6/6** — 실제 임시 DB 로 `indexer.passages()`(탐침,
  `svg><title>` 포함).

## 성능 — 개발 2 의 표는 오늘도 참이고, 정상 문서 축을 새로 넣었다

캡(`MAX_PASSAGE_HTML` 35,000자) 안 · 5회 최소 · 세 판을 같은 입력으로.

| 입력(35,000자) | 기점 `d5367fa` | 고치기 전 `ef2794a` | **지금** | 개발 2 가 적은 값 |
|---|---|---|---|---|
| `<b>` 5,000중첩 | 14.7ms | 181.1ms | **17.7ms** | 18.1 |
| `<div>` 3,181중첩 | 18.2ms | 96.0ms | **21.8ms** | 21.8 |
| 위키/CMS 모양 | 13.2ms | 16.8ms | **16.5ms** | 16.2 |
| **정상 기사**(제목·문단·링크·목록·표) | 9.5ms | 11.3ms | **12.2ms** | — |
| **정상 문단만** | 5.1ms | 6.0ms | **6.0ms** | — |

정상 문서가 기점 대비 **+29% / +18%** 인데 **암묵적 닫기 몫은 그중 8%**(11.3 → 12.2)고
나머지는 숨김 판정이다. `PASSAGE_LIMIT` 10건이면 **122ms** 로 사양 성능 5 의 500ms 예산
안이고, 블록 수는 세 판이 **전부 같다**(1800 · 921) — 정상 문서에서 동작은 안 움직였다.
**이 축을 붙드는 것은 단언이 아니라 이 표다**(벽시계 단언은 스위트를 깨지기 쉽게 만든다).

## 남긴 것 — 8점 미만 하나 (`digest.md` 에 등재)

닫는 태그가 자손을 잘라내는 자리(`handle_endtag` 의 `del self._els[i:]`)를 붙드는 단언이
**하나뿐**이다 — 변이 ⓐ 는 죽지만 여유가 없다. 안 재지는 모양은
`<div hidden><span>속</div><p>보이는</p>`(`span` 은 `_IMPLIED_END` 에 없어 암묵적으로는
못 닫고 `</div>` 가 둘을 같이 자른다 · 실측 `[('p', '보이는')]`). **이미 죽는 변이**라
오늘 닫은 둘보다 급하지 않다.

## 러너 규율 — 이번 반복 **0회 위반** (누적 32회 · 연속 3회)

러너 호출 **30회 전부 맨몸·단독** — 전체 스위트 10회(초록 · 변이 전후 · 마감 검증 ·
그중 변이 8회는 `python3 -B`) + e2e **20종**. **러너 출력 조작 0회**(파이프·`grep`·
`head`·`tail`·`for`·`env`·`timeout`·`2>&1`·`2>/dev/null`·`> /dev/null` 전부 0).
러너 아닌 곳(`git`·`ls`·`wc`·`awk`·`python3` 탐침·회전 스크립트)은 **마감 시점 수치**로
적는다 — 파이프 **2회** · `;` **10회** · `&&` **3회** · 리다이렉션 **3회**(대조군
`extract.py` 두 판을 scratchpad 로 뽑은 것). 탐침·벤치(`bench_t2.py`·`probe_hidden.py`·
`rotate47.py`)는 저장소 밖 scratchpad 에 있고 커밋하지 않는다. **커밋 명령이 이 줄 뒤에
나가므로 이 절은 늘 낡는다** — 다음 반복이 정정한다.

## 문서 마감

`status.md`(이 문서 — `iteration` **294**) · `metrics.md`(`반복` **294** · `## phase 분포`
테스트 **35**) · `index.md`(51번 행에 테스트 2 결과) · `history_current.md` append + **회전** ·
`digest.md`(회전 압축 · 명부 · 8점 미만 갭 1건).
**`README.md`·`project.md` 는 무변경이다** — 단위 588 / e2e 20종이 그대로고, `project.md`
의 기준선 둘은 **날짜가 박힌 스냅샷**(2026-09-02 근거 문단 p95 1.38 · 2026-08-27
`perf_search` p95 9.11)이라 오늘 값(1.52 · 8.78)이 그 안이면 덮어쓸 것이 아니라 대조할
것이다. 오늘 값은 이 문서와 `index.md` 51번 행이 갖는다.
**회전**: 테스트 2 항목을 붙이니 **347줄**(상한 300) — 가장 오래된 둘(병합 287 · 계획 51
계획 1) **69줄**을 `history_047.md` 로 밀어 **278줄 / 6회**가 남았다. 계획 51 이 진행
중이라 경계를 「DONE」이 아니라 「설계 1 바로 앞」에 그었다 — 설계·개발은 리뷰가 대조할
원본이다. `digest.md` **197줄**(상한 200 안).

## 다음 — 리뷰 2

**갭 둘을 닫았고 스위트 밖 축이 전부 기준선이므로 리뷰로 간다.** 리뷰가 볼 것: ① 성능
축을 표로만 붙드는 판단이 두 반복째 옳은가 · ② 정상 문서 **+29%** 가 사양 안이라는
판정(예산의 24%)을 받아들일 것인가 · ③ 남긴 8점 미만(단언 하나짜리 ⓐ)을 지금 닫을
것인가. 그 뒤 e2e.

## 원격 — `origin/main` 은 계획 47 까지다

- 이 반복이 커밋·푸시 **뒤에** 다시 읽는다 — 결과는 커밋 메시지가 아니라 아래에 적는다.
  직전 확인은 `main` = `687a1598…`(계획 47) · `loop/merge-48-50` = `d5367fa…`(둘 다
  **무변**) · `loop/hidden-passage` = `cf53826`(개발 2 의 문서 정정 커밋).
- **개발 2 는 커밋이 둘로 갈렸다** — 자동 스냅샷(`280ed30`)이 먼저 담아 갔다. 되돌리려면
  `push --force` 가 필요하고 `rules/git.md` 가 그것을 deny 한다 — 합치지 않고 적어 둔다.
- **PR #7** — `loop/merge-48-50` → `main`, 계획 48·49·50 을 한 PR 로 담았다. **OPEN·미병합.**
  병합은 **사용자가 처리한다** — 이 반복도 PR 을 열지도 닫지도 않았고, 그 브랜치에
  커밋하지도 않았으며, `main` 으로 리베이스도 안 했다.
- **그래서 계획 51 의 기점은 `main` 이 아니다.** `origin/main` 에는 계획 48·49·50 이 없어
  `README.md` 의 단위 건수 단언(`tests/test_readme.py`)이 거기서는 RED 다.
  `d5367fa`(`loop/merge-48-50` HEAD)에서 `loop/hidden-passage` 를 땄다.
- 앞선 병합: PR #6 = 계획 44~47(`687a159`). 충돌 0.

## 사람이 정할 것 — 다섯 (PR #7 이 붙었다)

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
2. **`--focus` 는 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 이 0 일
   때만 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **반응형 360px 미검증** — 브라우저가 없어 이 저장소의 누구도 그 화면을 못 그린다.
4. **회전 규약·러너 규율의 저장소 밖 절반**과 **사양이 남긴 둘**(`docs/specs/concept.md` 의
   `## 사람이 정할 것` — 속도 제한 시점, 사양 숫자 90%·500ms·60회/분이 초안이라는 것).
5. **PR #7 병합** — 위 「원격」 절.
