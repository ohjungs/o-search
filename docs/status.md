---
signal: GREEN
phase: 테스트
step: 4
attempt: 0
iteration: 300
updated: 2026-09-04
ctx: 34
night_iterations: 129
night_red: 2
night_retries: 0
plan: hidden-passage
---

# 현재 상태

**테스트 4 — 개발 4 가 넓힌 `_IMPLIED_END` 를 변이로 전수 재고, 빠진 자리 셋만 메웠다.**
제품 `src/` **0줄** · `tests/test_extract.py` **+15**(전부 기존 두 테스트의 표에 붙인 줄이라
**테스트 건수는 593 그대로**이고 `README.md` 는 안 건드렸다). 새 파일 0 · 새 의존성 0 ·
커밋된 변이 0.

**신호는 GREEN** — 단위 **593건 OK** rc 0(맨몸·단독 13.548초) · **e2e 20종 전수 rc 0** ·
RED 0 · 재시도 0. 브랜치 `loop/hidden-passage`, 기점 `d5367fa`(**`main` 아님** — 아래 「원격」).

## 갭 탐색을 눈이 아니라 변이로 했다 — 146건 중 106건이 살아 있었다

룰 3절(갭 6개 카테고리)의 ⑥ 커버리지 축을 **자동으로** 훑었다. `_IMPLIED_END` 를
**메모리 안에서** 갈아끼우고 `test_extract`·`test_indexer`·`test_serve` 를 **진짜 러너로**
돌리는 탐침이다(스크래치패드 · 소스 무편집).

| 축 | 변이 | 생존 |
|---|---|---|
| ① 키 삭제 | 16 | 3 |
| ② 값에서 원소 하나 삭제 | 125 | 102 |
| ③ `_TABLE_PARTS` 의 뺄셈 제거 | 5 | **1** |
| 합 | **146** | **106** |

기준선 실패 **0** · 복귀 실패 **0**. **소스를 한 글자도 안 고쳐 `.mutation-lock` 이
필요 없었다** — `git diff -- src/` **0줄** · 미추적 0개. 변이 대상은 제품 코드가 읽는
바로 그 모듈 전역이라 「진짜 스위트에서 진짜 실패」라는 조건은 지킨다.

## 8점 셋만 이번 스텝에서 닫았다 (룰 4절)

1. **`키 삭제 tfoot`** — 개발 4 가 넣은 여덟 중 **`tfoot` 만 어느 방향으로도 안 밟혀**
   있었다. 못 닫으면 `<tfoot hidden>` 뒤의 `<tbody>` 가 숨김을 물려받아 **보이는 셀이
   통째로 사라진다**(오탐 — 계획서 8절의 「제일 위험」).
2. **`뺄셈 제거 tfoot`** — `- {td, th, tr}` 를 지워도 146건 중 아무것도 안 죽었다.
   직전 `status.md ## 다음` 이 지목한 그 자리다: **빼기를 지우는 변이가 초록이면 「자식
   방향」 가드가 비어 있고, 그 방향이 곧 누출이다.** 다섯 줄 중 넷(`tr`·`thead`·`tbody`·
   `colgroup`)은 이미 걸려 있었고 `tfoot` 하나만 비었다.
3. **`키 삭제 dd`** — `dt/dd` 케이스가 `dt` 키만 쓰고 `dd` 키는 안 쓴다(개발 4 이전부터
   비어 있던 자리). `<dd hidden>` 은 접히는 FAQ 의 실물 기본형이다.

**새 케이스 셋은 지금 코드에서 전부 초록이다** — 결함이 아니라 **커버리지** 갭이었다.
붙인 뒤 다시 재니 셋이 **사망**하고 덤으로 `값 dd -= dd`·`값 tfoot -= tbody` 도 죽는다.

## 8점 미만은 `digest` 로 보냈다

생존 101건은 거의 `p` 값 49개와 표 값들의 낱개 삭제(5~6점)와 `rp` 키(4점 — 인라인이라
새어도 괄호 한두 글자)다. **안 여는 이유는 룰 5절이다** — 값 하나마다 단언은 학술적
완전성이고, 표를 테스트에 그대로 베끼는 **거울 단언은 행동을 안 잰다**(변이가 테스트 쪽도
같이 고치면 안 죽는다). 여는 조건과 그때의 답(브라우저 오라클 하나로 표 전체 대조)까지
`digest ## 다음 계획 후보 (테스트 phase 갭, 8점 미만)` 에 `[5]` 로 적었다.

## 단언을 낮춘 자리 0 (룰 6절)

기존 표에 줄을 **더하기만** 했다. 기대값을 고친 것은 없고, 개발 4 가 정정했던 `rt` 의
`["漢보이는"]` 도 그대로 뒀다. 기존 테스트가 깨진 것 0.

## 관문 — 전수의 세 번째 축

리뷰 3 이 「전수를 돌렸다는 어느 축의 전수인가를 함께 적어야 증거다」를 굳혔고 개발 4 가
77,284 조합으로 그 관문을 통과했는데, 그 전수는 **«동작이 HEAD 와 어떻게 다른가»** 를 쟀지
**«그 동작을 무엇이 붙들고 있는가»** 는 안 쟀다. 이번 146건이 초록이던 자리가 정확히 그
자리다(`metrics.md` 에 한 문단으로 이어 붙였다).

## 한도

- 제품 `src/` **0줄** · 테스트 +15 · 새 파일 0 · 새 의존성 0 · 계획 「건드릴 파일」 안.
- `data/crawl.db`·`data/`·`e2e/`·`docs/specs/` 무변경. 스키마·마이그레이션 0.
- 커밋된 변이 0 — 변이는 전부 프로세스 메모리 안에서 나고 죽었다.

## 러너 규율

판정 실행은 **맨몸·단독**이다 — `PYTHONPATH=src python3 -m unittest discover -b -s tests`
와 e2e 20종 각각 `PYTHONPATH=src python3 e2e/<이름>.py`. **이번 반복도 파이프·리다이렉션
0회**다(`for` 루프에 리다이렉션을 안 붙이고 출력을 그대로 읽었다 — 두 반복 연속). 출력
조작 0회. 변이 탐침은 러너가 아니라 **저장소 밖 스크래치패드**에 있고 안 커밋한다.

## 문서

`status.md`(이 파일 · 반복 **300**) · `metrics.md`(반복 **300** · 테스트 **37** · 「전수의
축」 교훈에 세 번째 축을 이어 붙였다) · `index.md`(51번 줄에 테스트 4) ·
`history_current.md`(테스트 4 항목 · **붙인 뒤 세니 297줄로 상한 300 안이라 안 돌렸다** —
**리뷰 4 는 항목을 붙이는 즉시 넘으므로 그 반복이 회전한다**) · `digest.md`(**200줄 유지** —
새 `[5]` 한 줄을 넣느라 `history_009.md`·`history_010.md` 두 회전 서술을 한 줄로 합쳤다.
내용 손실 0 · **새 색인 구멍 0**) · `README.md` **무변**(테스트 건수 593 그대로).

## 다음 — 리뷰 4

백지로 본다: ① `_TABLE_PARTS` 뺄셈의 «자식» 논거가 삽입 모드와 실제로 맞는지 —
「빼는 쪽이 자식」은 `td⊂tr`·`tr⊂tbody`·`col⊂colgroup` 셋에는 맞지만 `caption` 이
뺄셈 없이 `_TABLE_PARTS` 전부를 갖는 것이 같은 논거로 설명되는지. ② `p` 목록 49개가
명세의 "close a p element" 와 같은지 diff 없이. ③ 개발 3 이 남긴 살아남은 변이 둘
(⑨caption 삭제 · ⑩search 삭제)이 이번 표 위에서 어떻게 판정되는지.

## 원격 — `origin/main` 은 계획 47 까지다

- **커밋·푸시 뒤에 `git ls-remote origin` 을 다시 읽었다**(digest 의 「원격을 바꾸는 동작을
  «명령을 보냈다» 로 기록하지 않는다」 항목). 아래 SHA 는 이 반복이 실제로 읽은 값이다.
- **`loop/hidden-passage` = `<커밋 뒤 채운다>`** — 푸시 뒤 `git ls-remote origin
  loop/hidden-passage` 로 다시 읽어 로컬 `HEAD` 와 같은지 확인한다.
  `main` = `687a159` 무변. `--no-verify`·`--force` 0회 · 훅 우회 0.
- **PR #7** — `loop/merge-48-50` → `main`, 계획 48·49·50. **OPEN·미병합.** 병합은
  **사용자가 처리한다** — 이 반복은 PR 을 열지도 닫지도 않았고 그 브랜치를 건드리지도,
  `main` 으로 리베이스하지도 않았다.
- **계획 51 의 기점은 `main` 이 아니다** — `origin/main`(`687a159`)에는 계획 48·49·50 이
  없어 `README.md` 의 건수 단언이 거기서는 RED 다. `d5367fa` 에서 브랜치를 땄다.
- 앞선 병합: PR #6 = 계획 44~47(`687a159`). 충돌 0.

## 사람이 정할 것 — 다섯 (PR #7 이 붙었다)

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
2. **`--focus` 는 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 이 0 일
   때만 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **반응형 360px 미검증** — 브라우저가 없어 이 저장소의 누구도 그 화면을 못 그린다.
4. **회전 규약·러너 규율의 저장소 밖 절반**과 **사양이 남긴 둘**(`docs/specs/concept.md` 의
   `## 사람이 정할 것` — 속도 제한 시점, 사양 숫자 90%·500ms·60회/분이 초안이라는 것).
5. **PR #7 병합** — 위 「원격」 절.
