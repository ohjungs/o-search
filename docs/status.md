---
signal: GREEN
phase: e2e
step: 0
attempt: 0
iteration: 226
updated: 2026-09-01
ctx: 72
night_iterations: 86
night_red: 0
night_retries: 0
plan: focus-ring-presence
---

# 현재 상태

**계획 44 `focus-ring-presence` 리뷰 1/1 완료 — 다음 반복은 e2e phase 다.**
설계서 `docs/design_focus-ring-presence.md` · 계획서 `docs/plan_focus-ring-presence.md` ·
브랜치 `loop/focus-ring-presence` · 기점 `bc40ea9`. 제품 코드 **0줄**(검사기와 단위만).

## 방금 것 (2026-09-01 · 리뷰 1/1)

**판정: 통과 (수정 1건).** 후보 6건 → 80점 미만으로 버림 3 → 보고 3 → 자동 수정 1 ·
`digest` 인계 2. 백지 패스(diff 만)가 먼저 돌았고, 그다음 계획·설계 대조 패스를 열었다.

**리뷰가 할 자리 셋을 전부 실측으로 답했다.**

1. **축소 변이 — 여섯을 심었고 여섯 다 죽는다. 계획 42 가 남긴 비대칭이 닫혔다.**
   `.mutation-lock` 을 걸고 검사기를 **좁히는** 쪽으로 비틀었다(전부 되돌렸다):
   `OUTLINE_RE` 를 축약형만으로 → **V8** 사망 · `RULE_RE` 가 at-rule 안쪽을 못 보게 →
   **V10** 사망 · `len(rules) != 1` 을 `> 1` 로 → **V7·V8·V10** 셋 사망 ·
   `:focus`·재는 토큰·offset>0 세 조건을 각각 무력화 → **V3·V5·V6·V11** 넷 사망.
   **다섯 조건과 두 정규식 어느 하나도 단언 없이 사라지지 못한다.**
2. **설계가 일부러 안 잡는 둘(V4·V9)은 지금도 살아 있고 사유도 여전히 참이다.**
   다만 **가드가 그 사유보다 넓다** — `:focus` 조건이 `":focus" in 셀렉터` 문자열이라
   `:not(:focus-visible)` 과 `:focus-within` 도 통과한다. 둘 다 키보드 포커스에 링을
   **안** 그리는데 검사기는 3.56:1 을 찍는다. 여기에 **at-rule 위치를 안 보는** 갈래
   둘(다크 블록 안으로 이동 · `@media print` 안)을 더해 **새 변이 넷**이 rc 0 이다.
3. **`history_003.md` 는 고아가 아니다 — 다만 색인에 구멍이 다섯이다.** 기점 시점엔
   지워진 `digest` 줄이 `docs/` 안의 유일한 003 포인터가 맞았지만, **같은 커밋
   (`2c85b96`)이 `history_current.md` 에 대체 포인터를 넣었다**(`git show` 확인).
   전수로 재니 아카이브 20개 중 **001·002·003·005·019** 다섯에 `digest` 압축 줄이
   없고, 대체 포인터 자신도 300줄로 회전하는 문서 안이다.

**고친 것 1건(낮음).** `tests/test_design_check.py` 의 docstring 이 *"착수 탐침에서
앞 여섯이 살아남았다"* 고 적었는데 **탐침의 여섯은 V1·V2·V3·V4·V5·V6** 이고 표의
앞 여섯은 V4 대신 **V7** 이다 — V7 은 설계가 따로 심어 본 셋이다. 한 문장을 고쳐
출처를 갈랐다. 코드·단언 무변경(단위 **475건 무변**).

**인계 2건(`digest`).** ⓐ 위 2번의 변이 넷 → `## 다음 계획 후보` 에 `[6]` 으로 ·
ⓑ 위 3번의 색인 구멍 → `## 반복 실패` 에 **5개**로. ⓑ 의 처방은 `tests/test_docs.py`
검사 한 줄이라 **아래 "기록 3파일 불일치" 와 같은 파일·같은 꼴이고 한 계획으로 묶인다.**
둘 다 이 계획의 범위 밖이라 안 고쳤다 — 설계서 `## 범위 밖` 이 V4·V9 를 명시로 뺐다.

**계약 9항은 전부 이행돼 있었다.** 7항의 `1.27:1` 은 다크 `--line`/`--bg-input` 을
손으로 재서 독립 검증했다(**1.2707**). 8항은 단언 6건이라 적었는데 실물은 9건 —
설계 본문의 대안 표가 *"9종(V4·V9 제외)"* 이라 **계약이 덜 센 것이고 테스트가 옳다.**

## 앞 반복 (2026-09-01 · 테스트 1/1)

**빠뜨린 것은 셋이었고, 셋 다 "설계가 잡는다고 적었지만 아무도 안 잰" 자리다.**
변이 표에 **V8**(뒤 규칙이 `outline-width:0` 으로 폭만 덮는다) · **V10**(다크 블록
안에서 덮는다) · **V11**(셀렉터가 `:hover` — 개발이 계약 밖에서 만든 다섯 번째
메시지를 처음 재는 단언)을 더했다. 단위 건수는 **475 무변**(같은 메서드의 subTest).
**이 반복은 서버 오류(502)로 기록 도중 끊겼고** 자동 스냅샷(`9951566`)이 작업을
보존했다. 러너 파이프 횟수는 확인 불가라 0 으로 세지 않았다.

## 다음 반복이 할 것 — e2e phase

1. **검사기가 진짜 프로세스에서 rc 를 낸다는 것을 잰다.** 단위는 `check_contrast`
   를 함수로 부르고 `unmeasurable` 리스트만 본다 — **`ValueError` → 종료 2** 라는
   설계 `## 갈림길 A` 의 결론은 아직 e2e 가 안 밟았다. 링을 죽인 CSS 로
   `e2e/design_check.py` 를 띄워 **rc 2 이고 3.56:1 이 화면에 없다**를 확인한다.
2. **회귀**: `e2e/*.py` 19종 rc 0 · 품질·성능 축 무변 · `data/crawl.db` sha256 무변경.
3. **집안일이 둘 밀려 있다** — 아래 한도의 `digest` 200줄과 `history_current` 회전.

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-01 확인)

`origin/main` 은 `494313b`(PR #5 병합)이고 그 트리는 `bc40ea9` 의 부모와 같다.
**남은 것 (사람 결정)**: `bc40ea9`(반복 221 기록) 와 이 계획의 커밋들을 `main` 으로
보내는 PR. 무인 모드는 병합하지 않는다.
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 ② — 셋 중 둘이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다 — 리뷰가 1.27 을 손으로 재확인했다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset`
   이 0 일 때만 링의 이웃이 되고, **검사기가 매 실행 offset > 0 을 확인한다**(제품 실측
   2px, 요약 줄에 찍힌다). 짝을 지금 늘리면 rc 1 이라 계획이 커진다 — **안 늘린다.**
   offset 이 0 이 되는 날 검사기가 rc 2 로 멈추고, 짝은 그때 열릴 계획의 일이다.
3. **기록 3파일 불일치가 5회째 재발** — 처방은 `tests/test_docs.py` 에 검사 한 줄
   (`metrics.md` 의 누적 반복 수와 `status.md` 의 `iteration` 이 어긋나면 죽는다).
   **`digest` 의 아카이브 색인 구멍(5개)과 같은 파일·같은 꼴이라 묶어서 한 계획이다** —
   다음 계획의 1순위 후보.

## 한도 (매 반복 확인)

- `main` 직접 커밋 금지 · `--no-verify` 금지 · 외부 네트워크 금지 ·
  `docs/specs/` 읽기만 · `data/crawl.db` 무변경 · 의존성 추가 금지(stdlib 만).
- **검증은 맨몸으로 — 이 반복이 또 깼다. 러너 파이프 1회, 연속 카운트는 0 이다.**
  변이 검증 열 몇 번은 전부 맨몸이었는데, **기록을 다 쓴 뒤 스위트를 재확인하며
  `... discover tests 2>/dev/null` 을 돌렸다** — 판정 줄이 삼켜져 `rc=0` 만 보였다.
  즉시 맨몸으로 다시 돌려 475건 OK 를 눈으로 봤다. **방아쇠가 앞 반복과 똑같다**:
  둘 다 *"이미 초록일 것 같은 재확인"* 이고, `digest` 가 최다 방아쇠로 적어 둔 바로
  그것이다(⑨). 그 항목의 처방(러너 호출을 별칭·`make test` 로 굳혀 손으로 조립하지
  않기)이 **규율로는 두 반복 연속 실패했다 — 다음 계획 후보로 격상할 자리다.**
- **변이 검증은 `.mutation-lock` 을 걸고 했고 끝나고 지웠다.** 되돌림 확인:
  작업 트리 깨끗 · 검사기 rc 0.
- 이번 반복 최종 실측(아래 셋은 전부 맨몸): `PYTHONPATH=src python3 -m unittest discover tests`
  **475건 OK 11.932초**(기록 반영 뒤 재실행) · `PYTHONPATH=src python3 e2e/design_check.py` **rc 0 · 4축 전부
  통과**(요약 줄 `포커스 링 규칙 1개 · outline var(--focus) · offset 2px` · `--focus`
  라이트 3.56:1 · 다크 11.14:1) · `PYTHONPATH=src python3 e2e/pagination_ui_e2e.py` **rc 0**.
- `docs/digest.md` 200줄 상한 · 열린 항목 **50** — 지금 **정확히 200줄, 상한에 닿았다.
  다음 반복이 회전을 돌린다**(가장 오래된 완료 항목 → `history_<NNN>.md`). `history_current.md` 300줄 상한 — 지금 **293줄로 여유가 7줄이다**.
