# e2e 105 `crawl-gap-ab` — 1초 하한을 누가 깨는가, 재는 자인가 크롤러인가

- 실행: 2026-09-18 반복 621 · 브랜치 `loop/crawl-gap-ab` · 기준 `5bc5ef5` · 트리 깨끗
- 앞 관문: 전수 **811 OK**(21.1s · 맨몸) — 린트·타입체크는 이 저장소에 **없다**(`project.md` 명령 절).
  CI 설정 파일이 없어 **CI 상태 관문은 해당 없음**(`project.md` 협업 절도 비어 있다 = 1인 기본값).
- 이 계획은 `src/` 를 **0줄** 고쳤다. 고친 것은 **재는 자리**다.

## ① `deadline_e2e` 5판 연속 — 전부 `rc=0`

계획의 완료 기준은 「시나리오 0·1 **둘 다** 1.00 이상」이다. **세 시나리오 전부** 적는다.
각 칸은 `도착 최소 / 응답시작 최소` (초). 단언이 붙은 쪽은 **도착**이다.

| 판 | 대조군(예산 없음) | 시나리오 1(CLI) | 시나리오 2(실시계) | rc |
|---|---|---|---|---|
| 1 | **1.029** / 0.989 | **1.006** / 0.999 | **1.011** / 0.988 | 0 |
| 2 | **1.008** / 0.952 | **1.012** / 1.003 | **1.025** / 1.014 | 0 |
| 3 | **1.031** / 0.950 | **1.123** / 1.106 | **1.027** / 1.009 | 0 |
| 4 | **1.028** / 0.974 | **1.152** / 1.096 | **1.043** / 0.981 | 0 |
| 5 | **1.014** / 0.986 | **1.056** / 1.035 | **1.049** / 1.020 | 0 |

**도착 15칸 최솟값 1.006 · 응답시작 15칸 최솟값 0.950.** 두 수가 같은 판에서 나온다 —
`e2e/deadline_e2e.py` 가 `PAGE_DELAY` 만큼 **자고 난 뒤** 찍던 옛 자리는 제 `time.sleep`
초과분을 간격에 섞는다. 판 3 대조군은 도착으로 1.031 인데 응답시작으로는 0.950 이다:
**같은 요청 쌍이 81ms 차로 다르게 읽힌다.**

**옛 못(`0.95`)이 오늘 어떻게 보였을지** — 판 3 의 0.950 은 **0.4ms 차로 겨우** 통과했을
값이고, 반복 616 에서 실제로 죽은 값은 0.948 이었다. 지터가 못을 스치고 있었다.

**남은 여유는 2~6ms 다.** 오늘 15칸 최솟값이 1.006, 스텝 2 검증 5판에서는 1.002 까지
내려갔다. 좁다 — 그러나 **자가 제 잠을 더는 안 섞으므로**, 앞으로 이 못이 걸리면
그것은 지터가 아니라 **신고**다. 이것이 못을 `1.0` 으로 올려도 되는 근거다.

## ② `crawl_delay_e2e` 3판 — 전부 `rc=0`

| 판 | `Crawl-delay:2` 최소 | 하한 도메인 최소 | rc |
|---|---|---|---|
| 1 | 2.108 | 1.086 | 0 |
| 2 | 2.136 | 1.044 | 0 |
| 3 | 2.019 | 1.119 | 0 |

합격선 `≥2.0` · `≥1.0` 둘 다 통과. 이 파일은 **원래부터 도착 시각에 찍고 있었고**
2도메인 순차라 4도메인 동시인 `deadline_e2e` 와 달리 한 번도 안 깨졌다 — 스텝 1 의 판정
②(재는 자리가 문제)와 같은 방향이다. 여기서 고친 것은 못 값(`0.95`→`1.0`)과 인쇄 자릿수뿐이다.

## ③ 음성 대조 — 자가 **안 물 때** 무엇이 보이나

`PYTHONDONTWRITEBYTECODE=1` 과 `PYTHONPYCACHEPREFIX=$(mktemp -d)` 를 함께 주고,
복원은 `cp` 사본(`/tmp/m105e/deadline.orig`)에서 했다. 둘 다 `diff` 로 원본 동일 확인.

| 변이 | e2e 결과 | 단위 가드(`tests.test_e2e_interval`) |
|---|---|---|
| **A** 못을 `0.5` 로 내린다 | `rc=0` — **그냥 통과한다** | **죽는다** (`1.0 != 0.5`) |
| **B** 간격 검사 호출을 지운다 | `rc=0` — **판정이 한 줄도 안 남는다** | **안 죽는다** |

A 는 계획이 노린 그림이다 — 못을 내리면 e2e 는 아무 말 없이 초록이 되고, **그 침묵을
무는 것이 단위 가드**다. 못 값은 이제 소스에 박혀 가드가 지킨다.

**B 가 새로 남긴 것(발견 후보)** — 가드는 `check_intervals` **함수의 몸**을 읽지,
그 함수가 **불리는지**는 안 본다. 호출 세 자리를 지우면 e2e 는 `rc=0` 이고 `[간격]` 줄이
사라지는데 **전수 811 은 그대로 초록**이다. 다만 구멍은 **e2e 판정에 한정된다** —
제품 쪽 1초 하한은 `tests/test_frontier.py:319`(윤리 하한)가 따로 물고 있어, 이 변이로
크롤러가 빨라지지는 않는다. 그래서 RED 가 아니라 **다음 계획 후보**로 남긴다:
「소스를 읽는 가드는 호출 자리도 세야 한다」.

## 품질 기준 축 (`docs/project.md`)

**건드린 파일에만 건다.** 이 계획은 `e2e/` 둘과 `tests/` 하나·문서뿐이고 `src/` 는 0줄이다.

| 축 | 판정 |
|---|---|
| **크롤 간격** (`e2e/crawl_delay_e2e.py`) | **통과** — 위 ②. 이 계획이 직접 고친 축이다 (컨셉 1순위 크롤 윤리) |
| **의존성 0** (`tests.test_deps`) | **통과** — 전수 811 안에서 돌았다 |
| 검색 품질 · 근거 문단 · 검색 지연 · 디자인 4축 · 서버 상주 메모리 | **해당 없음 — 미검증.** `src/` 를 안 건드렸으니 움직일 경로가 없다. **「통과」라고 쓰지 않는다** (계획 104 선례) |

## 판정

**계획 105 완료.** 기대 결과였던 「`deadline_e2e` 5판 연속 `rc=0` · 못이 `1.0` ·
실패 문구가 원인 중립」 셋 다 섰고, 9일 묵은 후보 `[6]` 의 정체는 **크롤러가 아니라 자**였다.

## 부록 — 스텝 1·2 실측 원본 (`status.md` 에서 옮겼다)

계획이 「10판 원본 수를 `status.md` 에 남긴다」를 완료 기준으로 걸었고 그대로 했다. 계획이 닫히면서 그 자리는 다음 계획의 것이 되므로 **수를 버리지 않고 이 문서로 옮긴다** — `status.md` 는 41줄 줄고, 원본은 계획 단위 기록에 남는다.

## 스텝 1 실측 — 탐침 10판 원본 (계획 105 · 반복 617)

`PYTHONPATH=src python3 e2e/deadline_e2e.py` 를 10판. 칸은 **도착 최소 / 응답시작 최소**(초).
`—` 는 그 판이 앞 시나리오에서 죽어 거기까지 못 간 것이다. 단언은 아직 응답시작 쪽에만 걸려 있다.

| 판 | 대조군 | 시나리오 1 | 시나리오 2 | rc |
|---|---|---|---|---|
| 1 | 1.052 / 1.015 | 1.054 / **0.944** | — | 1 |
| 2 | 1.026 / 0.994 | 1.054 / 1.016 | 1.032 / 1.032 | 0 |
| 3 | 1.030 / **0.907** | — | — | 1 |
| 4 | 1.007 / 1.002 | 1.051 / 1.017 | 1.008 / 1.001 | 0 |
| 5 | 1.010 / 0.969 | 1.054 / 1.030 | 1.055 / 1.048 | 0 |
| 6 | 1.018 / 0.984 | 1.145 / 1.016 | 1.022 / 1.010 | 0 |
| 7 | 1.048 / 0.994 | 1.054 / 0.998 | 1.051 / 0.981 | 0 |
| 8 | 1.008 / 1.001 | 1.054 / 1.023 | 1.008 / 1.013 | 0 |
| 9 | 1.030 / 1.000 | 1.056 / 0.993 | 1.019 / 0.989 | 0 |
| 10 | 1.014 / 0.981 | 1.054 / 1.040 | 1.050 / 1.040 | 0 |

- **도착 26칸 전부 ≥ 1.0** (최소 **1.007** · 판 4 대조군). **1.0 미만은 응답시작 쪽에만 9칸** (최소 **0.907**).
- rc=1 두 판(1·3)의 죽은 자리도 둘 다 응답시작이고, **같은 판 같은 도메인의 도착은 1.054·1.030 으로 멀쩡했다.**
- 판정 **②** — 계획서 「모르는 것」이 닫혔다. 10판 중 ①(발신 < 1.0)에 해당하는 판은 **0판**이다.

## 스텝 2 검증 — 5판 연속 rc=0 · 변이 넷 (계획 105 · 반복 618)

- **`deadline_e2e` 5판 전부 rc=0.** 도착 최소 15칸 1.002~1.146s — **전부 1.0 이상**.
  같은 판들의 응답시작은 **0.908s** 까지 내려갔다(판 2 시나리오 1) — **옛 못 `0.95` 였다면 오늘도 죽었다.**
- **`crawl_delay_e2e` 7판 rc=0** — `Crawl-delay:2` 최소 2.01~2.15s · 하한 도메인 최소 **1.003~1.142s**.
  이 파일은 원래 도착 시각으로 재고 있었다(핸들러가 자기 전에 찍는다). 인쇄를 2자리→3자리로 올렸다 —
  못이 `1.0` 인데 `1.00` 으로 찍으면 여유가 3ms 인지 0인지 안 보인다.
- **변이 넷** (`PYTHONDONTWRITEBYTECODE=1` · `PYTHONPYCACHEPREFIX` 임시 · 원복은 `cp` 사본):

| 변이 | 심은 것 | 결과 |
|---|---|---|
| M1 | 제품 `frontier.DOMAIN_INTERVAL` 1.0 → **0.5** | **죽음** — `대조군: 포트 54598 간격 0.602초 — 1초 하한이 깨졌다` |
| M2 | M1 + e2e 못을 0.5 로 내림 | **삼** (rc=0 · 도착 0.504s 를 통과시켰다) — 못의 **값**이 판정을 정한다 |
| M3 | M1 + 간격 단언만 삭제 | **삼** (rc=0) · 화면엔 `간격 최소 0.50s (하한 1초)` 가 찍힌다 — 사람 눈에만 보이는 거짓말 |
| M4 | `ARRIVAL_LOG.append` 삭제 (제품 원복) | **죽음** — `대조군: 도착 간격이 0칸뿐이다 — 응답시작은 12칸이다` |

- M2·M3 은 **살아야 맞는 음성 대조**다(계획서 e2e 시나리오 3). 무는 축은 M1·M4 둘이다.
- 전수 `PYTHONPATH=src python3 -m unittest discover -b tests` **809 OK** · 제품 `src/` **0줄**.
