---
signal: GREEN
phase: 테스트
step: 2/2
attempt: 0
iteration: 443
updated: 2026-09-07
ctx: 51
night_iterations: 10
night_red: 0
night_retries: 0
plan: history-cap-guard
---

## 현재 상태

**계획 76 개발 완료 (2/2) — 못이 살아 있는 `history_current.md` 에 물렸다.**
`HistoryCapTest` 2건: ① 실물이 상한 안이다(오늘 196줄·9회) ② 항목 축이 아무것도 안 재는
길을 `HISTORY_ENTRY_FLOOR = 1` 로 닫았다 — **오늘 값 9 에 안 붙였다**(회전 직후에는
항목이 몇 개든 정당하다). 머리 문구를 `## 반복` 로 드리프트시키는 변이가 하한 못에
잡혔다(0개 < 1).

**계획 스텝 2 가 요구한 회전 직전 재현을 눈으로 봤다** — `history_073.md`(회전으로
밀려난 247줄) + 실물 = **441줄·23회**, 판정 「줄 수 441 > 300 · 항목 수 23 > 20」.
반복 440 의 사고가 이 못 아래서는 다음 전수에 빨개진다. 전수 **672건 OK**
(README 건수 가드 함께 갱신). 다음은 테스트 phase.

## 닫힌 판단

- **`digest.md` 는 201줄이다** — 반복 440 의 회전 기록 한 줄이 상한 200 을 1줄 넘겼다.
  무인 모드는 삭제를 안 하므로 **무엇을 버릴지는 아침에 사람이 정한다**(야간 보고서).
  2026-09-07 의 삭제 위임은 그 한 건 한정이었고 `rules/docs.md` 3절 문장은 그대로다.
- **`.context-state.json` 이 낡았다**(반복 442 실측 22분) — statusLine 이 꺼진 것으로
  보고 `SKILL.md` 대로 **반복 상한에만 의존**한다. 마지막 값은 51/30/45 로 전부 여유.
