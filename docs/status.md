---
signal: GREEN
phase: 계획
step: 1
attempt: 0
iteration: 274
updated: 2026-09-02
ctx: 58
night_iterations: 116
night_red: 1
night_retries: 0
plan: null
---
# 현재 상태

**e2e 통과 — 계획 48 `passage-api` DONE.** 활성 계획 없음 · 다음은 계획 탐색이다.
브랜치 `loop/passage-api` · 기점 `6e99031`. 계획서는 `docs/plan_history_034.md`,
설계서는 `docs/design_history_034.md` 로 아카이브했고 결과는
`docs/e2e/passage-api/result.md` 다.
**단위 576건 전부 초록**(맨몸 2회 — 13.433 · 13.418초) · **e2e 20종 전수 rc 0** ·
제품 `src/` **0줄** · 새 파일 0(문서 제외) · 의존성 0 · 스키마·마이그레이션 없음 ·
`docs/specs/`·`data/crawl.db` 무변경(sha256 `85c96744…5bda18`) · `.mutation-lock` **0**.
`passage_eval` 정확도 **100.0%(398/398)** · 채택률 99.5% · p50 1.29ms · p95 **1.40ms**
(예산 500ms 의 0.3%).

## 이번 반복이 한 일 (e2e 1)

**새 e2e 파일은 안 만들고 `e2e/search_api_e2e.py` 에 갈래 둘을 얹었다**(+38 −2 · 제품 0줄).
계획 47 이 열어 둔 서버가 이미 **README 형태의 상대 경로**(`data/crawl.db`)로 떠 있어서,
경로를 하나 더 때리는 것으로 `/passages` 계약 전체가 재진다. 프로세스를 더 띄우면
재는 것은 같다(ponytail 2번).

**왜 그 자리가 비어 있었나.** 계획 48 이 만든 엔드포인트를 재는 자가 셋인데 셋 다 같은
자리를 안 본다 — `e2e/passage_eval.py` 는 **200 한 갈래뿐**(정확도·p95 전용)이고,
단위 576건은 `make_server()` 를 **스레드**로 부르며 `tempfile` **절대 경로**를 쓴다.
**진짜 CLI 프로세스 위에서 `/passages` 가 밟힌 적이 0회였다.**

| # | 시나리오 | 실측 |
|---|---|---|
| ⑧ | `GET /passages?q=김치` | **200** · `version` 1 · 문단마다 `position`(정수)·`title`·`url`·질의어를 담은 `text` |
| ⑧ | `q` 없음 / `page=2` / POST | **400** · **400** · **501** — 트레이스백 0 |
| ⑧ | 색인을 치우면 | **503**(`/search` 와 같은 값) · 되돌리면 200 |
| ⑨ | **손상된 DB** | `/search`·`/passages` **둘 다 500** · `version` 1 · 경로·트레이스백 유출 0 |

**⑨ 가 이 반복의 값이다.** 계획 47 의 result.md 5절이 *"500 은 한 번도 안 났다"* 로 닫혀
있었다 — 재시도해도 안 낫는 상태(500)와 색인을 다시 돌리면 낫는 상태(503)를 프로세스
밖에서 가른 자리가 **0개**였다. 이제 진짜 파일에 `not a sqlite database` 를 써서 두 경로가
갈라지고, 서버 로그가 **어느 문이 열렸는지**까지 적는 것도 확인했다.

## 대조군 — 변이 둘 전부 RED, 그런데 눈이 먼 것은 다른 자였다

| 변이 | e2e | 단위 |
|---|---|---|
| M1 `_send(500,…)` → `_send(503,…)` | rc 1 (`손상 DB 인데 /search → 503`) | **사망 8건** |
| M2 `"position": pos` → `str(pos)` | rc 1 (`position` 이 `'0'`) | **사망 2건** |

**둘 다 단위가 먼저 죽인다 — 이 e2e 는 눈먼 자리를 메운 것이 아니라 형태를 바꿔 잰다.**
그렇게 적는 것이 정직하다. **다만 `passage_eval` 은 M2 에 완전히 눈이 멀었다**
(rc 0 · 정확도 100.0% · p95 1.43ms) — 정확도를 재는 자는 `text` 만 읽고 계약의 **필드 타입**은
안 본다. 반복 267 의 눈금(«숫자가 안 움직이는 것은 그 축을 안 쟀다는 뜻일 수 있다»)이
또 맞았다: **품질을 재는 자와 계약을 재는 자는 다른 자다.**

원복은 초록이 아니라 **`git diff -- src/` 가 0줄인 것**으로 확인했고 `.mutation-lock` 은
심기 전에 걸고 되돌린 뒤 지웠다. **커밋된 변이 0.**

## 넘긴 것 — 갈림길 6 은 열린 채로 남는다

**닫는 조건은 갈림길 5 의 캡과 같다: 실물 크롤 코퍼스다.** `data/crawl.db` 는 2문서 × 66자,
e2e 코퍼스는 한 문장짜리 `<p>` 뿐이라 «보일러플레이트가 `<p>` 로 싸이는 빈도» 를 저장소가
못 잰다. 손잡이 셋도 겹친 둘도 19행을 다 못 맞춘다(11 · 12 · 15 · 13 · 15/19).
**설계서에 열린 채로 두고, 오늘의 선택과 그 이유를 거기 적었다** — 손잡이를 더 돌려 보는
것은 계획 48 이 아니라 다음 계획이다.

`digest.md ## 다음 계획 후보` 에 선 것: **`[6]`** 이름표를 읽는 소비자가 0곳(리뷰 7) ·
**`[9]`** `unittest -b`(아래 4번) · `[9]` `/passages` p95 의 대부분이 `/search` 몫.

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-02 확인)

`origin/main` 은 PR #6 으로 계획 44~47 까지 받았다. 계획 48 의 커밋들은 브랜치에만 있다.
**이번 반복도 `origin/loop/passage-api` 로 푸시했다** — 푸시는 루프의 정상 절차다
(강제 푸시만 금지).
**남은 것 (사람 결정)**: 계획 48 을 `main` 으로 보내는 PR. 계획이 DONE 이라 **지금이 그
자리**인데 무인 모드는 열지도 병합하지도 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 — 넷이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, `~/.claude/skills/loop-harness/rules/docs.md` 에는 그 예외가 없다.
   **저장소 밖이라 루프가 못 고친다.**
4. **«러너 호출을 다른 것과 섞지 않는다» 를 문장이 아니라 훅으로 옮기는 일.**
   **재발 27 → 28회 — 이번 반복이 밟았다.** 선행 관문의 전체 스위트를 맨몸으로 한 번
   돌려 **576건 OK** 를 읽은 뒤, *두 번째 측정만* 이라며 `2>&1 | tail -3` 을 붙였다.
   크롤 로그 세 줄이 `tail` 을 채워 `Ran/OK` 가 또 밀려났고, 즉시 맨몸으로 다시 돌려
   **13.418초**를 읽었다. **판정은 그 호출 위에서 안 내렸다.**
   **방아쇠·형태·자리가 전부 기존 칸이라 새로 보이는 것이 없다 — 그것이 이 표본의 값이다.**
   직전 반복(리뷰 7)이 «넷 만에 처음 안 밟았다, 한 번으로는 추세가 아니다» 라고 적었고
   **바로 다음 반복이 그 문장을 확인해 줬다.** 스물여덟 번을 문장으로 못 막았다.
   처방은 이미 판정해 뒀다: `unittest` 의 **`-b`** 가 방아쇠를 뿌리에서 없앤다
   (실측 약 90줄 → 5줄, 거른 테스트 0, 새 코드 0). `digest.md ## 다음 계획 후보` 에
   **`[9]`** 로 등재돼 있다. 단서 — `-b` 는 테스트가 직접 낸 stdout/stderr 만 버퍼링하므로
   하위 프로세스가 fd 로 직접 쓰는 로그는 샌다. 판정 명령을 단독 실행으로 강제하는
   PreToolUse 검사는 여전히 **저장소 밖 몫**이다.

**사양이 사람에게 남긴 것 셋** (`docs/specs/concept.md` 의 `## 사람이 정할 것`):
속도 제한 시점(가정 — IP 당 분당 60회) · JSON 검색과 근거 문단의 분할(가정 — 쪼갠다,
**계획 46 과 48 이 그 가정을 따랐고 둘 다 닫았다**) · 사양 숫자들이 초안이라는 것
(90% · 500ms · 60회/분). 첫째와 셋째는 아직 열려 있다.
