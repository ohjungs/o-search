---
signal: GREEN
phase: 테스트
step: 1
attempt: 0
iteration: 239
updated: 2026-09-02
ctx: 45
night_iterations: 95
night_red: 0
night_retries: 0
plan: json-contract
---

# 현재 상태

**계획 46 `json-contract` 개발 스텝 3/3 완료 — 세 스텝이 전부 끝났다.**
사양 기능 6(*"화면의 결과 순서와 JSON 순서가 완전히 일치한다"*)에 **처음으로 단언이 붙었다**.
제품 순증 **0줄**(설계 추정 그대로), 단위 **491 → 492건 OK**.
**다음 반복은 테스트 phase** — 새로 쓰는 곳이 아니라 **빠뜨린 것을 찾고 전체를 돌리는** 곳이다.

| 사양 항목 | 이전 | 지금 |
|---|---|---|
| 디자인 5 — 색인 없음은 503 | 500 (스텝 1 이전) | **503** · JSON·화면 두 경로 |
| 기능 9 — 응답 스키마에 버전 | 없음 | **`"version": 1`** (`_send` 한 곳) |
| **기능 6 — 화면 순서 == JSON 순서** | 구조상 참 · **재는 단언 0건** | **`TestScreenMatchesJson` 이 리스트로 잰다** |

## 방금 한 것 (2026-09-02 · 계획 46 개발 스텝 3)

- **`tests/test_serve.py` 에 `TestScreenMatchesJson` 하나.** 같은 질의·같은 페이지에서
  `/` HTML 의 `<div class="url">` 목록과 `/search` 의 `results[].url` 을 **리스트로**
  비교한다(집합이면 순서를 뒤집는 변이가 안 죽는다). 20문서 픽스처(`MANY_PAGES`)로
  **1·2페이지 둘 다** — 한 페이지만 재면 페이지를 어긋내는 변이가 안 죽는다.
  제품 `src/` **0줄** · 새 파일 0 · 의존성 0.
- **URL 을 `<div class="url">` 에서 뽑는다**(설계 5절 그대로). 링크(`<a href>`)로 뽑으면
  `_safe_href` 가 거른 URL 이 빠져 길이부터 달라진다. 값은 `html.escape` 를 지났으므로
  `html.unescape`(stdlib)로 되돌린다 — 새 파서 0.
- **RED 는 변이가 만들었다.** 재는 성질이 구조상 이미 참(두 경로가 `_page_hits` 한 벌을
  나눠 쓴다)이라 **실패를 볼 길이 변이뿐**이었다. `.mutation-lock` 을 두고 제자리에 심어
  둘 다 죽는 것을 봤다: **M13**(`_do_html` 만 `page + 1`) → 2건 · **M14**(화면 쪽만
  `reversed()`, 집합은 그대로) → 2건. 매번 복원 · 커밋된 변이 0 · 락 제거 확인.
- **M14 를 전 스위트에 걸어 커버리지가 0 이던 것을 실측했다** — 심은 채 492건을 돌리니
  운 단언은 **새 단언 둘 + README 카운트 가드**뿐이었다. 순서만 뒤집는(집합은 같은) 변이를
  잡는 테스트가 저장소에 **하나도 없었다** — 설계 변이 표의 *"오늘 0건"* 이 실물로 확인됐다.
- **빈 목록끼리는 무엇과도 같다** — `len(screen) == PAGE_SIZE` 를 먼저 못박았다. 잴 것이
  없는 상태에서 조용히 통과하는 길을 닫고, 덤으로 탐침 한 줄(11번째)이 화면에 새는 것도
  여기서 죽는다.
- **JSON 을 파싱하는 e2e 넷을 돌렸다** — `src` 0줄이라 깨질 자리가 없지만 단위 스위트가
  e2e 를 안 부르므로 값으로 확인한다: `search_api_e2e`·`pagination_ui_e2e`·`tokenizer_e2e`·
  `perf_search` 전부 rc=0. p95 **8.73ms**(예산 300ms 의 2.9%) — 기준선 8.86ms 그대로다.

## 한도 (매 반복 확인)

- `main` 직접 커밋 금지 · `--no-verify` 금지 · 외부 네트워크 금지 ·
  `docs/specs/` 읽기만 · `data/crawl.db` 무변경 · 의존성 추가 금지(stdlib 만).
- **러너 파이프 — 판정에는 0회.** 직전 반복이 `| tail -4` 로 `Ran/OK` 줄을 날린 뒤라
  RED·GREEN·변이·e2e 판정을 **전부 맨몸으로** 돌렸다. 파이프를 쓴 곳은 변이 한 번의
  실패 목록 추리기뿐이고 그 판정도 맨몸 재실행으로 확인했다.
- 이번 반복 실측(맨몸): `PYTHONPATH=src python3 -m unittest discover -s tests`
  **492건 OK**(12.124초) · e2e 넷 전부 rc=0. 제품 `src/` **+0줄** ·
  `data/crawl.db` **무변경**(테스트는 전부 임시 디렉터리) · 의존성 0 · 새 제품 파일 0.
- README 카운트 가드가 **세 반복 연속 울었다** — 단위 491 → 492 반영. 손으로 적는 숫자를
  세게 만든 판단이 매 반복 값을 내고 있다.
- 제자리 변이 0 — `.mutation-lock` **없음**(심는 동안만 두고 지웠다).
- **집안일 — 회전 미달.** `history_current.md` 290줄(상한 300) · 4항목(상한 20).
  **다음 반복이 붙이면 넘길 자리다** — 항목을 붙인 뒤에 세는 절차를 그대로 따를 것.
  아카이브 25개 그대로 · `digest.md` 명부 구멍 0.
- **설계서가 한 화면을 넘는 건(186줄)은 그대로 열려 있다.** 계획 phase 에서 갈림길 수를
  세는 것이 옳은 자리라고 반복 237 이 적어 뒀다.

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-01 확인)

`origin/main` 은 `494313b`(PR #5 병합). 계획 43·44·45 의 커밋들(`bc40ea9` 이후)이
아직 안 갔다. **남은 것 (사람 결정)**: 그것들을 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).
계획 46 의 기점은 `3d766be`(계획 45 DONE 커밋)이고 `main` 이 아닌 이유는 계획서 머리에
적어 뒀다 — `main` 기점은 `serve.py` 를 계획 44 이전으로 되돌리고 기록 4파일을 반복 221
로 되돌려 계획 45 가 세운 두 가드를 스스로 깬다.
**직전 반복이 원격 자동 스냅샷(`c3bd8c6`)을 `-s ours` 로 병합해 푸시를 풀었다** —
이번 반복은 푸시 전에 `git log --oneline -3` 과 fetch 를 확인했고 강제 푸시는 안 썼다.

## 사람이 정할 것 — 셋이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
   offset 이 0 이 되는 날 검사기가 멈추고, 짝은 그때 열릴 계획의 일이다.
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   이제 *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, 회전을 지시하는 `~/.claude/skills/loop-harness/rules/docs.md` 에는
   그 예외가 없다. **저장소 밖이라 루프가 못 고친다.** 실동작은 안전하다는 것을
   계획 45 e2e 가 실측했다(잊으면 RED · 완료 항목을 지워도 색인은 산다). 룰 문장을
   맞출지는 사람 몫.

**사양이 사람에게 남긴 것 셋은 계획 46 의 범위 밖이다** (`docs/specs/concept.md` 의
`## 사람이 정할 것`): 속도 제한 시점(가정 IP 당 분당 60회) · JSON 검색과 근거 문단의
분할(가정 쪼갠다 — 계획 46 이 그 가정을 따랐다) · 사양 숫자들이 초안이라는 것
(90% · 500ms · 60회/분). 첫째와 셋째는 아직 열려 있다.
