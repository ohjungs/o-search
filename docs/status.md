---

# 현재 상태

**32 `serve-500-log-cause` DONE**(짧은 경로). 열린 계획 없음 · 보류 패치 0건.
단위 **431건 OK** · e2e 17종(`src` 무변경이라 serve 쓰는 둘만 재실행, rc=0).
브랜치 `loop/readme-perf-audit`(기점 `main` `2bff526`) — 병합은 사용자 판단이다.

**설계는 건너뛰었다** — 새 모듈 없음 · `src` **0줄** · 파일 2개(테스트·README) ·
되돌리기 쉬움 · 보안 무관.

## 반복 151 — 32 `serve-500-log-cause`

**측정이 결론이었다.** `[6]`("`serve.do_GET` 의 넓은 `try`", 확신 낮음)을 고치기 전에
쟀다. `_page_hits` 에 `AttributeError`·`TypeError`·`MemoryError` 주입:

- JSON·HTML 둘 다 **500**, 본문은 고정 문구, `Traceback` 유출 **0**.
- `BaseException` 이 아닌 `Exception` 이라 `KeyboardInterrupt` 는 **안 삼킨다**.
- `try` 는 `_parse`+`_page_hits` 만 덮고 **200 렌더링은 `else`** — 이미 좁았다.

**그래서 `src` 를 0줄 고쳤다.** `[6]` 은 코드 문제가 아니었다 — `digest` 에서 닫았다.

**진짜 구멍은 테스트 쪽이었다.** 넓은 `try` 의 유일한 실위험은 "원인이 로그에만 있다"
인데 ① 화면 경로 500 은 로그 단언이 **0건** ② JSON 쪽은 `"search 실패"` 접두어만 봐서
`%r` 을 고정 문구로 바꿔도 초록이었다 → **고칠 수 없는 500**. 기존 두 클래스에
테스트 1 + 단언 1(새 파일·목 없음, DB 없는 기존 픽스처가 `FileNotFoundError` 를 낸다).

**단언 문자열을 `OperationalError` 로 잘못 적었고 테스트가 즉시 잡았다**(실제는
`FileNotFoundError`). 추측한 원인이 아니라 **찍히는 원인**을 못박았다.

**변이 3종 전부 잡힘 — 각각 새 단언 하나씩만 죽였다**(그 자리에 파수꾼이 없었다는 증거):
화면 `log_error` 삭제 · JSON `%r`→고정 · 화면 `%r`→고정. 심기 전 `count(old)==1` 로
**원문 존재를 먼저 단언**했고(`digest [8]` 거짓 초록), 치환·복원은 파이썬(BSD `sed -i` 안 씀).

**README `단위 430건` 이 또 그 자리에서 FAILED** — 28 이 만들고 30 이 이빨을 붙인 검사의
두 번째 실사용이고 두 번 다 정확했다. 431 로 고쳤다.

## 다음 계획 후보 (근거는 `digest.md`)

`[8]` **변이가 실제로 심어졌는지 먼저 단언한다**(BSD sed 거짓 초록 — 룰 쪽, 저장소 밖) ·
`[7]` 살아남은 변이가 등가인지 실측 · `[4]` `int_max_str_digits`(3.9.6 이라 도달 불가,
유지 판단). **`[6]`(넓은 `try`)은 닫혔다 — 재 보니 고칠 것이 없었다.**

**지시받은 것을 했다. 여기서 정지한다.** (night 지시 모드)

## 미결

`data/crawl.db` 는 URL 을 `pages.url` PK 로 쓴다 · CI 없음 · `docs/specs/` 는 동결이다.
