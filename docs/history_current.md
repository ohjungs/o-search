# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**회전 명부는 `digest.md` 의 `## 완료` 절 «아카이브 명부» 줄이 정본이다.**
여기 있던 스물한 회전의 서술(233줄)은 그 줄과 내용이 겹쳤고, 검사가 강제하는 명부도
그쪽 하나뿐이라(`tests/test_docs.py` 의 `ArchiveIndexTest`) **개발 9(반복 269)가 이
자리에서 지웠다** — 회전으로는 300줄 상한을 못 맞추던 세 반복(309 → 372 → 418줄)의
원인이 이 명부였다. **지운 것은 머리말이지 반복 기록이 아니다** — 항목은 여전히
append 전용이고 수정·삭제 금지다. 각 회전의 사유는 `digest.md` 의 같은 줄에, 원문은
`history_<NNN>.md` 에 그대로 있다.

## 2026-09-06 14:00 | (없음) | 계획 0/0 | 시도0
- 한 일: 8개 출처를 하나도 「해당 없음」으로 넘기지 않고 전수 실측했다 — 1순위 0(전수 `OK`) ·
  2순위 도구 0개(stdlib 전용, 설정 파일 부재로 확인) · 3순위 실행 코드 0(문 것은 fixture
  문자열 하나) · 4순위 대기열 파일 부재 · 5순위 보류 0(본문이 통째로 HTML 주석) ·
  6·7·8순위는 살아 있는 것이 전부 승인 게이트(재색인·스키마·보안 판정) 뒤다.
- 판정 ①(출처 편중): 계획 60~64 **다섯 연속**이 `tests/test_docs.py` 문서 하네스이고 제품
  `src/` 는 다섯 번 다 0줄이다. `SKILL.md` 4-1 의 문턱은 연속 3건이라 여섯째를 같은 광맥에서
  뽑는 것은 **정당하지 않다**로 판정하고, 다른 출처를 먼저 뒤진 결과가 위 8줄이다.
- 판정 ②(착수 탐침 둘 — 기록된 전제가 **둘 다 거짓**): (A) README 「잘하고 있나 재는 자」 표에
  실측치는 0개이고 담긴 일곱은 전부 합격선이다. 낡을 수 있는 숫자 둘(단위 622·e2e 21종)은
  `tests/test_readme.py` 가 이미 실제 개수와 대조한다. 다만 그 합격선 일곱이 `e2e/*.py`
  상수의 **무가드 복제**라는 것이 새로 드러났다 — 근거는 서지만 이것이야말로 여섯 번째
  문서 하네스라 판정 ①에 걸려 안 열었다. (B) 「품질 기준선 회귀는 사람 눈만 잡는다」도 거짓 —
  `.git` 없는 사본에서 `_fts_query` 의 `title_ng`·`body_ng` 를 한 열로 합치니 `quality_eval`
  단독은 ko 17/20(85%)에도 「합격선 80% — 통과」 rc 0 으로 **조용했지만**, 전수는
  `FAILED (failures=4)` rc 1 로 **울었다**(`KO_BASELINE`/`EN_BASELINE`). 계획 하나가 지워졌다.
- 검증: 전수 **맨몸**(리다이렉션·파이프 0) `Ran 622 tests in 15.867s` · `OK` · **rc 0**.
  변이는 저장소 밖 사본에서만 · 실물 `src/`·`e2e/`·`data/`·`docs/specs/` **0줄** ·
  `data/crawl.db` 무변경 · 재색인 0회 · PR #7 무접촉(`gh` 호출 0) · 러너 위반 0(누적 38).
- 다음: **YELLOW 정지 — 탐색 막힘 2건째**(`metrics ## 정지 사유`). 대화형이라 묻고 기다린다.
  사람이 셋 중 하나(여섯째 문서 하네스 허용 / `판단 필요` 항목 개방 / `docs/specs/` 방향)를
  열어 주면 그 지시가 곧 근거라 탐색 없이 바로 계획서를 쓴다.

## 2026-09-06 15:00 | readme-band-cover | 계획 0/1 | 시도0
- 한 일: 사람이 반복 376 의 물음 셋 중 **①(여섯 번째 문서 하네스 허용)만** 열어 주어
  YELLOW 를 풀고 **계획 65 `readme-band-cover`** 를 등재했다. ②(`판단 필요` 개방)와
  ③(`docs/specs/` 방향 기입)은 열리지 않았으므로 보안·재색인·스키마 게이트는 그대로
  닫힘이고, 지시 모드라 탐색은 다시 돌리지 않았다(`rules/discover.md` 0절).
- 닫을 것: `README.md` 「잘하고 있나 재는 자」 표의 **아홉 수치**(상위 10 · 80% · 90% ·
  0.3초 · 0.5초 · 초당 5장 · JS 50KB · 대비 4.5 · 비텍스트 3)가 `e2e/*.py` 상수의 복제인데
  **둘을 대조하는 단언이 0개**다. 오늘 아홉이 전부 일치하므로 **결함은 값이 아니라 대조의
  부재**다.
- 착수 탐침(변이 열셋 · 저장소 밖 사본 · 각각 전수): **반복 376 의 기록을 셋 정정했다**
  (`digest [7]` 「기록된 답을 실행 전에 다시 재라」의 다음 적용). ① 수치는 「일곱」이 아니라
  **아홉**이다 — 표는 일곱 행이지만 첫 행이 둘(상위 10 · 80%), 마지막 행이 둘(4.5 · 3)을
  담는다. ② **상수 축은 「무가드」가 아니다** — `THRESHOLD` 80→85 · `TOP_N` 10→5 ·
  `MIN_CONTRAST` 4.5→3.0 · `MIN_CONTRAST_NONTEXT` 3.0→2.0 이 각각 1·1·1·2건으로 죽는다
  (`test_quality_eval.TestVerdict`·`TestGuards` · `test_design_check.ContrastAxisTest`).
  생존은 다섯뿐 — `ACCURACY` 90→95 · `perf_search.BUDGET_MS` 300→400 ·
  `passage_eval.BUDGET_MS` 500→700 · `TARGET_RATE` 5.0→8.0 · `JS_BUDGET` 50→80KB.
  ③ **그러나 문서 축은 아홉이 다 열려 있다** — README 쪽 변이 셋(「80%」→「55%」 ·
  「0.3초」→「9.9초」 · 「50KB」→「500KB」)이 **3/3 생존**한다. 상수를 물고 있는 넷조차
  README 와는 안 묶여 있다는 뜻이고, **닫을 축은 「상수가 움직이나」가 아니라 「문서와
  상수가 같은가」다.** 이 판정이 계획 4절 완료 기준의 6번(문서 축)을 본 축으로 만들었다.
- 세 번째 사본: `e2e/*.py` 주석이 출처로 인용하는 `docs/specs/concept.md`(23·43·44·51·53·
  115·128·129행)까지 오늘 **셋이 전부 일치**하는 것을 눈으로 확인했다. 사양은 사람이 고치는
  읽기 전용 문서라 그 축은 계획 5절에서 안 연다.
- 설계 생략: 트리거 0(새 파일 0 · 인터페이스 무변 · 데이터 구조 0 · 파일 2개 · 커밋 하나로
  revert). 대안도 안 갈린다 — e2e 상수를 임포트해 읽는 길은 `tests/test_design_check.py`·
  `tests/test_quality_eval.py`·`tests/test_passage_eval.py` 가 이미 세 번 고른 관용구이고
  (사다리 2번), 소스를 정규식으로 파싱하는 안은 `50 * 1024` 같은 식을 다시 계산해야 해서
  더 크고 더 약하다. 다섯 모듈 동시 임포트는 **부작용 0 · 0.138초**로 쟀다.
- 중복 방지(지시 모드에서도 적용): 활성 계획 0 · `digest ## 보류 (승인 대기)` 0건 ·
  `docs/patches/`·`docs/candidates.md` 부재 · `digest` 의 `test_readme.py` 항목 `[5]` 는
  `RUNNER_LINE`·`BUFFERED` 의 **거짓 RED 방향 둘**이라 이 계획과 안 겹친다(5절에 못박음).
- 검증: 전수 **맨몸**(리다이렉션·파이프 0) `Ran 622 tests in 15.828s` · `OK` · **rc 0**.
  변이는 저장소 밖 사본에서만 — 실물 `src/`·`e2e/`·`README.md`·`docs/specs/`·`data/`
  **0줄** · `data/crawl.db` sha256 무변 · 재색인 0회 · PR #7 무접촉(`gh pr` 호출 0) ·
  러너 위반 0(누적 38). 기점 `535e8fc` 는 원격과 같다.
- 다음: **개발 phase 스텝 1/1** — `tests/test_readme.py` 에 `QualityBandTest`(아홉 줄 리터럴
  표 + 대조 단언). 제품 `src/`·`e2e/` 0줄. 메서드가 늘면 README 「단위 N건」을 같은 커밋에서
  함께 고친다.

## 2026-09-06 16:20 | readme-band-cover | 개발 1/1 | 시도0
- 한 일: `tests/test_readme.py` 에 `QualityBandTest` 를 세워 README 「잘하고 있나 재는 자」
  표의 **아홉 수치**를 `e2e/*.py` 상수와 묶었다. 파일 머리에 아홉 줄 리터럴 표
  `QUALITY_BAND`(이름 · README 에서 수치만 뽑는 정규식 · 모듈 · 상수 · 환산 배율)를 두고,
  메서드 하나가 `subTest` 로 아홉을 돈다. 제품 `src/` **0줄** · `e2e/` **0줄**.
- **문구가 아니라 수치를 잰다** (계획 7절 위험의 첫 항목 — 계획 62 e2e 가 잡은 「실물 첫 줄
  그대로」 거짓 주석이 그 자리다). 표에 `51200` 을 적으면 README 의 「50KB」가 바뀌어도 안
  울고, 문구를 통째로 적으면 문구를 다듬는 날 표도 같이 고쳐져 거울이 된다. 그래서 정규식은
  숫자만 잡고 환산(초→ms · KB→B)은 단언이 직접 곱한다. **곱셈은 `decimal.Decimal` 로 한다** —
  `0.3 * 1000` 이 부동소수라 `300` 과 안 같아지는 갈래를 아예 없앤다(계획 7절 둘째 위험).
- **정규식이 죽으면 대조가 빈손 위에서 조용히 통과한다.** 그래서 각 행이 「정확히 하나」를
  단언한다 — 0개(README 문구가 바뀜)도 2개 이상(어느 것을 잰 것인지 모름)도 실패다.
  「95번은 N초」는 두 행에 있어 행 머리(`| 빠른가 |` · `| 문단도 빠른가 |`)로 갈랐다.
- 새로 만든 구조 0 — `sys.path.insert` 로 e2e 모듈을 임포트하는 관용구는
  `test_design_check.py`·`test_quality_eval.py`·`test_passage_eval.py` 가 이미 세 번 썼다
  (사다리 2번). 다섯 모듈 임포트 비용 **0.116초** · 부작용 0.
- 완료 기준 10개 전부 통과(변이 열셋 · 저장소 밖 사본 · 각각 전수 623건):
  **① 본 축이 닫혔다 — R1·R2·R3 이 0/3 생존에서 3/3 사망으로 뒤집혔다.**
  「80%」→「55%」 · 「0.3초」→「9.9초」 · 「50KB」→「500KB」가 각각 1건씩 죽고, 실패
  메시지가 어느 행·어느 상수인지를 적는다(`band='검색 합격선 %'`).
  ② 상수 축 생존 다섯이 전부 죽었다 — M3 `ACCURACY` · M4 `perf_search.BUDGET_MS` ·
  M5 `passage_eval.BUDGET_MS` · M6 `TARGET_RATE` · M7 `JS_BUDGET` 각 1건.
  ③ **감지력 무회귀이자 증강** — 이미 물고 있던 넷이 1·1·1·2 → **2·2·2·3** 이 됐다
  (기존 `test_quality_eval`·`test_design_check` 단언은 그대로 죽고 새 단언이 겹쳐 문다).
  ④ 무변이 대조군 M0 는 사본에서도 `Ran 623` · `OK` · rc 0 — **오탐 0**.
- **README 「단위 622건」이 설계대로 즉시 울었다.** 메서드가 하나 늘자
  `test_verification_counts_match_reality` 가 `(622, 21) != (623, 21)` 로 빨개져,
  같은 커밋에서 623 으로 고쳤다. 문서 가드가 **자기가 늘린 수를 스스로 잡은 것**이다.
- 검증: 전수 **맨몸**(리다이렉션·파이프 0) `Ran 623 tests in 15.896s` · `OK` · **rc 0**.
  변이는 저장소 밖 사본에서만 · 실물 `src/`·`e2e/`·`docs/specs/`·`data/` **0줄**
  (`git diff --stat 535e8fc -- src/ e2e/ docs/specs/ data/` 빈손) ·
  `data/crawl.db` sha256 `85c96744…5bda18` 무변 · 재색인 0회 · PR #7 무접촉(`gh` 호출 0) ·
  러너 위반 0(누적 38). 고친 파일은 `tests/test_readme.py`(+57) · `README.md`(1줄)뿐이다.
- 다음: **테스트 phase**(갭 탐색). 계획 5절이 남긴 후보 하나 — `docs/specs/concept.md` 는
  세 번째 사본인데 읽기 전용이라 안 열었다(오늘 셋 일치 확인). `digest` 후보로 남긴다.

## 2026-09-06 17:30 | readme-band-cover | 테스트 1/1 | 시도0
- 한 일: **스위트에 쓴 줄은 0이다.** `rules/test.md` 3절 여섯 카테고리를 항목마다 대조하고
  (「해당 없음」으로 건너뛴 칸 없음), 개발이 세운 표 열셋을 저장소 밖 사본에서 **다시 쟀다.**
  거기에 이 계획 고유의 구멍을 겨눈 변이 스물넷을 더 돌렸다(P1~P9 · S1~S9 · V1~V6).
  변이는 전부 `.git` 없는 tar 사본에서만 돌았고 워킹트리는 도는 내내 무변경이었다.
- **① 재측정 열셋이 개발과 한 자도 안 어긋난다.** M0 무변이 대조군 0(오탐 0) ·
  M3~M7 각 1 · M1·M2·M8 각 2 · M9 3 · R1·R2·R3 각 1. 실패 메시지가 `band='검색 합격선 %'`
  처럼 범인 행을 이름으로 집는 것도 그대로다.
- **② 「정규식이 죽으면 조용한가」를 아홉 행 각각 쟀다(P1~P9) — 9/9 가 운다.**
  수치는 그대로 두고 문구만 갈아 패턴을 죽였다(「10개 중에」→「10건 가운데」 ·
  「80%」→「80퍼센트」 · 행 머리 `| 빠른가 |`→`| 검색이 빠른가 |` · 「JS 50KB 이하」→
  「JS 50KB 밑으로」 · 「은 3배)」→「은 3배 차이)」 등). 조용히 통과한 행 **0**.
  반대 방향(2개 이상)도 쟀다 — README 에 「10개 중에」를 한 줄 더 넣으면 `['10','10']` 로
  실패한다. 「0개도 2개도 실패」라는 개발의 주장이 양쪽에서 참이다.
- **③ 환산 경로 아홉을 전부 틀리게 했다(S1~S9) — 9/9 가 운다.** 초→ms 둘(1000→1) ·
  KB→B(1024→1000) · 배율 1인 여섯을 10·60·100 으로. `Decimal('0.3') != 300` ·
  `Decimal('50000') != 51200` 처럼 값이 갈린다 — **우연히 맞아떨어지는 자리 0.**
- **④ 값이 겹치는 수치에서 범인 행을 하나만 집는가(V1~V6) — 6/6.** 대비 4.5↔3 을 서로
  바꾸고 상위 10→80 · 「95번은 N초」 두 행을 0.3↔0.5 로 뒤집었다. 두 예산 행이 서로
  섞이지 않는다(V5·V6). 이름 오타는 `AttributeError`/`ModuleNotFoundError` 로 즉사한다.
- **⑤ 격리** — `test_readme.py` 단독도 `Ran 6` · `OK` · rc 0. 앞 모듈이 먼저 임포트해 준
  덕이 아니다. sleep·timeout·random·네트워크·서브프로세스 0 이라 불안정 패턴도 없다.
- 결과: **갭 셋을 찾았고 셋 다 8점 미만이라 4절대로 안 열었다.** [7] README 표에 열 번째
  행을 끼우면 `Ran 623 · OK · rc 0 · 죽은 단언 0` — `QUALITY_BAND` 가 리터럴 아홉 줄이라
  **새 합격선은 대조 없이 들어온다.** [5] 상수와 README 를 **함께** 4.5→4.6 으로 옮긴
  **일치 상태**에서 `Decimal('4.6') != 4.6` 으로 거짓 RED — 오늘 아홉은 전부 이진 정확이라
  잠복이다(처방 `Decimal(str(actual))` 한 줄). [4] 세 번째 사본 `docs/specs/concept.md` 는
  읽기만 했고 오늘 셋이 일치한다(비텍스트 3배만 사양에 없고, 그 상수 주석이 이미 적어 뒀다).
  셋을 `digest.md` 후보에 한 줄로 등재하며 **여는 조건**까지 적었다.
- 검증: 전수 **맨몸**(리다이렉션·파이프 0) `Ran 623 tests` · `OK` · **rc 0**.
  `tests/`·`e2e/`·`src/`·`README.md` **0줄**(단언을 낮춘 것도 없다 — 6절) ·
  `data/crawl.db` sha256 `85c96744…5bda18` 무변 · 재색인 0회 · `docs/specs/` 읽기만 ·
  PR #7 무접촉(`gh` 호출 0) · `main` 무접촉 · 러너 위반 0(누적 38).
- 회전했다: `history_current.md` 가 286줄이라 이 항목으로 상한 300 을 넘어, 계획 64
  `archive-scope-cover` 다섯 항목(반복 371~375)을 `docs/history_066.md` 로 옮기고
  `digest.md` **아카이브 명부에 등재**했다(명부 누락은 `ArchiveIndexTest` 가 문다).
  `digest.md` 200줄 정각을 지키려 가장 오래된 완료 항목(계획 59)을 뺐다 — `index.md` 에 남아 있다.
- 다음: **리뷰 phase.** 볼 것은 `QUALITY_BAND`·`QualityBandTest` 뿐이고, 갭 셋은 점수와
  여는 조건까지 적혀 있으니 다시 안 판다. e2e 는 그다음 반복 몫이다.

## 2026-09-06 18:20 | readme-band-cover | 리뷰 1/1 | 시도0
- 한 일: 백지 패스(diff 와 고친 파일만)를 먼저 끝내고 패스 B 로 갔다. 후보 **셋** →
  보고 2(둘 다 low · 자동 수정) · 버림 1(80점 미만). critical/high **0건**이라 반려 없음.
  변이는 전부 저장소 밖 tar 사본(`.git`·`data/` 없음)에서만 돌았다.
- **① 앞 phase 의 아홉을 오늘 다시 걸었다 — 아홉 줄이 한 자도 안 움직인다.**
  M0 대조군 0(오탐 0) · M3~M7 각 1 · R1~R3 각 1, 죽는 자리가 전부 `subTest` 라벨까지 같다.
  예산 두 행이 서로 안 섞이는 것(R2 는 `검색 예산 초`, M5 는 `문단 예산 초`)도 재현됐다.
  **수정 뒤 같은 아홉을 다시 돌려 아홉 다 그대로 죽는 것을 확인했다** — 단언을 낮추지
  않았다는 증거를 건수가 아니라 **라벨**로 붙였다.
- **[R65-1] (low · 95 · 자동 수정) 「맞는데도 빨개지는」 축이 있었다.** 비교의 오른쪽이
  float 상수라 이진 정확값이 아닌 합격선에서는 문서와 상수가 완전히 일치해도 RED 다.
  `decimal.Decimal(str(actual))` 한 줄로 양쪽을 십진으로 재웠다.
- **그 판정을 앞 phase 는 「잠복」으로 적었는데 틀렸다 — 도달 가능이다.** 테스트 phase 의
  4.5→4.6 판은 `ContrastAxisTest` 의 **4.5 못이 함께 죽는** 자리라 「일치 상태」가 아니었고,
  거짓 RED 가 못에 가려 보였다. 못이 없는 축에서 다시 걸었다 — `perf_crawl.TARGET_RATE`
  5.0→**5.1** 과 README 「1초에 **5.1**장」을 함께 옮긴 완전 일치 상태에서 수정 전 **RED
  1건이 유일한 실패**(`band='크롤 속도 장/초'`) · 수정 후 `Ran 623 · OK · rc 0` ·
  불일치(5.1 vs 5.0)는 여전히 RED. **합격선은 튜닝으로 움직이고 튜닝은 이진 정확값을
  골라 주지 않는다** — 「여는 조건을 기다린다」가 성립할 잠복이 아니었다. `digest [7]②` 닫음.
- **[R65-2] (low · 85 · 자동 수정) 이 diff 가 파일 docstring 을 거짓으로 만들었다.**
  「`find_spec` 은 임포트하지 않고 찾기만 한다」가 파일 전체의 비용 주장으로 읽히는데
  새 검사는 `e2e/*.py` 다섯을 **실제로 임포트**한다(그 아래로 `websearch.indexer`·`serve`).
  두 검사의 비용을 문장으로 갈랐다 — 임포트 시점에 상수만 서고 서버도 DB 도 안 연다는 것을
  다섯 모듈에서 직접 읽어 확인한 뒤 적었다.
- **버린 후보(신뢰도 60).** status 의 「`history_current.md` 는 회전 직후 128줄」이 실물
  168줄과 어긋나 계획 61 `[R61-1]` 의 재발로 보였으나, 그 문장은 **「회전 직후」로 한정**
  되어 참이고 회전 판단(상한 300)도 안 뒤집는다. 재발 아님.
- **[7]① 은 재현됐고 열어 둔다.** README 표에 열 번째 행을 끼운 판이 오늘도
  `Ran 623 · OK · rc 0 · 죽은 단언 0` 이다. `[7]③`(`concept.md` 축)도 그대로 열림.
- 검증: 저장소 안 전수 **맨몸**(리다이렉션·파이프 0) 두 판 — 수정 전/후 각각
  `Ran 623 tests` · `OK` · **rc 0**. 격리 `-p test_readme.py` `Ran 6` · `OK` · rc 0.
  제품 `src/`·`e2e/` **0줄** · `docs/specs/` `git diff --stat` **0줄** ·
  `data/crawl.db` sha256 `85c96744…5bda18` 무변 · 재색인 0회 ·
  PR #7 무접촉(`gh` 호출 0) · `main` 무접촉 · 러너 위반 0(누적 38).
- 배운 것: **「오늘은 안전하다」를 잰 판에 다른 못이 함께 서 있으면 그 판은 잠복 판정의
  근거가 못 된다.** 같은 결함을 «못이 없는 축»에서 한 번 더 재는 것이 잠복/도달의 갈림이다.
  반복 330 의 「오염이 남은 뒤를 재야 한다」와 같은 모양이고, 이번엔 **가린 것이 오염이
  아니라 우리 쪽 단언**이었다.
- 다음: **e2e phase.** 제품 0줄이라 `rules/e2e.md` 3절 면제를 근거 셋으로 다시 재는 것이
  첫 일이고, 그다음이 완료 기준 10/10 확인과 계획 65 닫기다.
