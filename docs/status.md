---
signal: GREEN
phase: e2e
step: 1
attempt: 0
iteration: 280
updated: 2026-09-03
ctx: 60
night_iterations: 116
night_red: 1
night_retries: 0
plan: focus-rule-scope
---

# 현재 상태

**계획 49 `focus-rule-scope` 의 리뷰 1 — 통과다. 반려 0.**
계획 전체(설계 1 · 개발 1 · 개발 2 · 테스트 1, `18d485f..6ac1a34`)를 두 패스로 봤다.
**보고 2건 · 자동 수정 1 · digest 등재 1.** 고친 줄은 docstring 한 줄뿐이고
`e2e/design_check.py` 는 **안 건드렸다**. 브랜치 `loop/focus-rule-scope`.

단위 **578건 OK**(맨몸 13.468초 실측) ·
`PYTHONPATH=src python3 e2e/design_check.py` **종료 0**(`라이트 --focus on --bg-page
3.56:1` 무변) · `data/crawl.db` 무변경 · `docs/specs/` 무변경 · 제품 `src/` **0줄**.

## 보고 2건

| # | 무엇 | 판정 |
|---|---|---|
| R1 | 조건 5 가 **결합자 뒤를 안 본다** — `a:focus-visible+.hint{outline:…}` 는 거짓 초록 | **계획 49 범위 밖** → digest 등재 |
| R2 | `tests/test_design_check.py:105` 의 *"아래 아홉 변이"* 가 낡았다(표가 19행) | 자동 수정 → *"열아홉"* |

## R1 — 링이 다른 상자에 그려져도 초록이다

셀렉터 **목록 전체**를 갈아 `focus_rule` 에 먹이는 탐침에서 넷이 살아남았다:
`a:focus-visible+.hint` · `a:focus-visible .hint` · `~` · `>` 가 전부 종료 0 이고
`--focus / 2px` 를 그대로 반환한다. 계획 49 가 죽인 ⓓ(`:focus-within`)·ⓕ(`:has`)와
**같은 고장**이다 — `:focus-visible` 이 낱말로 남았는데 링은 포커스받은 요소가 아니라
이웃·자손 상자에 그려지고, 그러면 설계서 6절이 적은 대로 `--focus on --bg-page`
라는 **짝 자체가 틀린 측정**이 된다.

**그래도 반려하지 않았다.** 조건 5 가 닫은 축은 «괄호 안의 극성» 이고 ⓔⓕⓖ 는 그 축을
읽는 정규식이 좁아 샌 것(새 코드의 결함)인데, 결합자는 **옛 가드도 새 가드도 한 번도
읽은 적 없는 다른 차원**이다. 닫으려면 셀렉터를 compound 로 쪼개야 하고, 그것은
계획서 5절·설계서 6절이 «CSS 파서를 안 만든다 · 천장을 올리려는 유혹» 으로 그어 둔 선
안쪽이다. 여는 조건과 처방(`re.split(r"[ >+~]", part)[-1]`)까지 적어
`digest ## 다음 계획 후보 (테스트 phase 갭, 8점 미만)` 에 등재했다. 제품 CSS 에는
결합자 뒤 링이 **0곳**이다.

## 탐침이 먼저 틀렸다 — 배선을 의심한 자리

첫 탐침은 열 케이스가 **전부 초록**이었다. 비틀 자리를 `RING_RULE` 의
`a:focus-visible{` 로 잡은 탓에 셀렉터 **목록**의 앞 둘(`.sb input:focus-visible` 등)이
남아 무엇을 심어도 통과했다. 자리를 목록 전체로 옮기니 4/10 만 살아남고
`:has` 안에만 포커스 · `:focus-within` · `:not(:is(.x,:focus-visible))` 은 정상 거절이었다.
digest `[6]` **탐침의 배선을 먼저 의심한다** 가 또 값을 냈다 — 첫 표를 그대로 보고했으면
없는 구멍 여섯을 신고할 뻔했다.

## 버린 후보 둘 · 확인한 개선 하나

- **설계서 표 5행 드리프트**(`:not(…)` 만 적혔는데 코드는 `:has`·`re.I` 도 한다) — 테스트 1
  이 늘린 것을 `history` 가 이미 적었고 설계서는 스냅샷이다. `severity.md` 4절 억제.
- **`token_maps` 는 주석을 안 지운다** — 이 diff 밖이고 원래 있던 갈래다.
- **확인한 개선**: 주석 처리한 링 규칙(`/* … outline … */`)이 전에는 규칙 1개로 세어져
  거짓 초록이었는데, 이 diff 의 `COMMENT_OR_STRING_RE` 덕에 이제 0개라 종료 2 다.
  계획서가 겨냥한 것이 아닌데 같이 닫혔다.

## 러너 규율 — 이번 반복 0회 (누적 30회)

단위 한 번과 e2e 한 번을 맨몸·단독으로 돌렸다. 파이프·리다이렉션·`;` 잇기 0회,
`> /dev/null` 0회. **네 반복 연속 0회다.** 탐침은 러너가 아니라 별도 스크립트라
스크래치패드에서 `python3 -B` 로 돌렸다(저장소 파일 무편집).

## 다음 — e2e phase

남은 완료 기준은 **5(e2e 20종 전수 종료 0)** 하나다. 6(새 단언이 각각 변이 하나씩만
죽인다)은 테스트 1 이, 1~4·7·8 은 개발 두 스텝이 닫았다.

## 기록·기점

`origin/main` 은 PR #6 까지만 받아 **계획 48 이 없다**. 그래서 `loop/focus-rule-scope` 는
**`18d485f` 에서 땄다**. 자동 스냅샷 `f9fc925` 를 열어 **커밋된 변이 0** 을 눈으로
확인했다(테스트 1 의 완성본만 들어 있다). e2e 는 20종 그대로, 단위는 578건 그대로다.
푸시는 정상 절차다(강제 푸시만 금지).
**PR 은 안 열었다 · 웹 UI 의 *Update branch* 도 안 눌렀다** — 무인 모드는 열지도
병합하지도 않고, rebase-merge 로 해시가 새로 쓰인 뒤의 그 버튼은 머지가 아니라
복제다(PR #3 을 깨뜨린 원인).
`night_*` 세 필드는 이번 호출이 야간 세션이라는 근거가 없어 **안 건드렸다**.

## 사람이 정할 것 — 넷 그대로다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
2. **`--focus` 는 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 이 0 일
   때만 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** — 후보 B 가 닫는 것은 저장소 안쪽 절반
   뿐이고, 룰 문장을 조이는 나머지 절반은 여전히 사람 몫이다.
4. **사양 확장이 남긴 것 둘** (`docs/specs/concept.md` 의 `## 사람이 정할 것`):
   속도 제한 시점(가정 — IP 당 분당 60회)과 사양 숫자들이 초안이라는 것(90% · 500ms ·
   60회/분). 분할 가정은 계획 46·48 이 따랐고 둘 다 닫혔다.
