---
signal: GREEN
phase: 테스트
step: 3
attempt: 0
iteration: 296
updated: 2026-09-03
ctx: 41
night_iterations: 125
night_red: 2
night_retries: 0
plan: hidden-passage
---

# 현재 상태

**개발 3 이 리뷰 2 의 반려 2건을 닫았다.** [R51-3](숨은 텍스트 유출)은 술어를 **닫힌
집합**으로 바꿔 닫았고, [R51-4](낡은 성능 계수)는 **다시 재서** 고쳤다. 제품은 두 파일
(`src/websearch/extract.py`·`src/websearch/indexer.py`)이고, 순증은 사실상 **0줄에
가깝다** — 새 표를 넣은 만큼 함수 하나를 통째로 지웠기 때문이다.

**신호는 GREEN** — 단위 **588건 OK** rc 0(맨몸·단독 **13.479초**) · `e2e/passage_eval.py`
통과(블록 **253** · 정확도 **100.0%** · p95 **1.51ms**). 브랜치 `loop/hidden-passage`,
기점 `d5367fa`(**`main` 아님** — 아래 「원격」 절).

## [R51-3] 을 어떻게 닫았나 — 여집합을 **지웠다**

`_TextParser._implied_end` 스태틱메서드를 **통째로 삭제**하고, 호출부(암묵적 닫기 루프)를
`_IMPLIED_END` 표만 읽게 인라인했다. `p` 키에는 파싱 알고리즘이 "close a p element" 를
부르는 시작 태그 **49개**를 그대로 적었다 — 종료 태그 생략 목록보다 넓어 `li`·`dd`·`dt`·
`summary`·`dialog` 를 포함하고, 표 문맥에서 셀·행을 닫으며 `<p>` 를 함께 걷는 표 구조
태그(`caption`·`colgroup`·`tbody`·`tfoot`·`thead`·`tr`·`td`·`th`)도 든다.

**핵심은 「무엇을 더했나」가 아니라 「무엇을 끊었나」다** — 이제 `_INLINE_TAGS` 와
`_NON_BLOCK_TAGS` 는 **숨김 범위 판정에 입력으로 들어가지 않는다.** 리뷰가 지목한 뿌리
(«선례를 옮기면 코드는 따라오지만 전제는 안 따라온다»)가 코드 수준에서 사라졌다.

**리뷰의 후보 ②(`if "-" in tag: return False`)를 안 골랐다** — 그것은 무한 집합(커스텀
요소)만 막고 **표준 태그 과잉 29개를 천장으로 남긴다.** 후보 ①(닫힌 집합)이 코드도 더
줄고 전수 대조에서 과잉·미달을 **둘 다 0** 으로 만든다.

## TDD — RED 를 먼저 봤다

단언을 먼저 넣고 돌린 실제 출력:

```
Ran 588 tests
FAILED (failures=6)
AssertionError: '김치찌개 김치찌개' != '김치찌개는 배추로 만든다.'
```

여섯은 `test_extract` 의 subTest **5개**(커스텀 요소 · 자기 닫는 커스텀 요소 · `slot` ·
`datalist` · `nobr`)와 `test_indexer.test_hidden_text_never_becomes_the_passage` **1개**다.
구현 뒤 `Ran 588 tests in 13.418s / OK` rc 0.

**단언이 가장 깊은 모양인지 확인한 방법** — 파서 단위가 아니라 **진짜 임시 DB 에
`indexer.passages()` 를 부르는 행**을 새로 넣었고, 변이 ②(열린 집합을 되살린다)를 심으니
그 행이 **함께 빨개졌다**(6실패 = extract 5 + `passages` 1). 파서만 붙들었다면 5였을
자리다. 단언 행 순증은 subTest 5행 + passages 1행이라 **메서드 건수 588 은 그대로**다.

## 전·후를 둘 다 제품 코드에서 쟀다

| 축 | 고치기 전(HEAD) | 고친 뒤 |
|---|---|---|
| 표준 태그 **135개** 전수 — 과잉 | **29** | **0** |
| 표준 태그 135개 전수 — 미달 | 0 | **0** |
| 커스텀 요소(`my-widget`·`amp-img`·`ion-icon`) | **전부 과잉**(무한 집합) | **없음** |
| 임시 DB `indexer.passages()` 유출 | **5/6** | **0/6** |
| 대조군 `<div hidden>` | 0/1 | 0/1 |

135 는 리뷰가 쓴 **127개 목록의 상위집합**이다(폐기 요소 8개를 더했다 — 폐기 요소일수록
파서가 이름을 모를 확률이 높아 여집합 술어가 새는 자리다). 리뷰는 과잉을 19로 셌고 나는
29로 셌다 — 목록이 8 넓고, 나머지 차이는 `search` 같은 경계 태그다.

**`search` 는 넣었다 — 리뷰는 과잉으로 셌지만 현행 명세는 `<p>` 를 닫는 쪽이다.** 판단은
방향으로 했다: 빼면 브라우저가 **보여 주는** 텍스트를 문단에서 지우는 «덜 낸다» 오류가
되고, 그것이 계획서 8절이 「가장 큰 위험」으로 적은 방향이다.

## [R51-4] 재측정 — 계수 0.44 · 최악 154ms · 배정치 25% → 1/3

캡 35,000자 · 30회 중앙값 · 세 모양을 **같은 판**에서 (1,000자당 ms):

| 모양 | 기점 `d5367fa` | HEAD | 이번 수정 |
|---|---|---|---|
| 기사(제목·문단·목록·표·중첩) | 11.28ms | — | 13.91ms |
| 문단만 | 5.30ms | — | 6.17ms |
| 위키/CMS(낱말마다 `<b>`) | 11.31ms | — | 14.46ms |
| **1,000자당 최악 계수** | **0.323** | **0.409** | **0.413** |

**적은 값은 0.44 다 — 리뷰 2 의 실측 0.439 를 낮은 쪽으로 안 내렸다.** 같은 모양을 다른
판에서 잰 두 값 중 큰 쪽을 쓰는 것이 성능 상한의 안전한 방향이다. 최악은
35,000자 × 0.44 × `PASSAGE_LIMIT` 10 = **154ms**, 500ms 예산의 **31%**.

**배정치를 25% → 1/3 로 다시 그었다.** 캡도 `PASSAGE_LIMIT` 도 안 움직였고 움직인 것은
계수뿐이다. 실물 코퍼스 p95 는 1.51 → **1.54ms**(예산의 0.006%). 도로 25% 로 조이려면 캡을
28,000 으로 내려야 하고, 그것은 «긴 문서의 뒷부분 근거» 를 더 자르는 **별개 판단**이라
이 스텝의 범위 밖으로 적었다.

**기록 자리 셋**: `src/websearch/indexer.py:41~51`(주석) · `tests/test_indexer.py` 의 성능
단언과 그 위 주석(측정 이력 4회를 남겼다) · `docs/project.md`.

**리뷰의 진단은 절반 틀렸다 — 이것이 이 스텝에서 나온 값이다.** 리뷰는 [R51-4] 를
«서술이라 스위트가 안 잡고» low 로 적었는데, `tests/test_indexer.py` 가 실제로는
`0.352` 와 `500 * 0.25` 를 **단언 안에 박고** 있었다. 주석만 고쳤으면 낡은 계수 위에 선
**거짓 초록**이 그대로 남는다. 「스위트가 안 잡는다」는 판정도 실제로 찾아서 확인할 것.

## 이미 초록이던 축 — 옛 `extract.py` 를 임시 모듈로 띄워 나란히 쟀다

- 숨은 텍스트 **0/6** · 본문 문단 **6/6**(둘 다 전·후 동일)
- 평가 코퍼스 64문서 — 문장을 `<p>` 로 싼 형태 **253블록 차이 0** · 통짜 형태 64블록 차이 0
- 불변식 `" ".join(blocks) == extract_text()[1]` 깨진 문서 **0**
- `tests/` 의 HTML 리터럴 **342개** 중 달라진 **6개는 전부 이번에 넣은 fixture**
  (`test_extract` 5 · `test_indexer` 1) — 기존 리터럴 336개 차이 **0**
- 음성 6종(`aria-hidden="false"`·`font-size:0.9em`·`font-size:10px`·`class="hidden-md"`·
  `display:block`·문단 안 인라인 숨김) **6/6 안 문다**
- [R51-1] 형제 누출 4종 그대로 · 변이 ⑤(`while`→`if`)·[T2-1]·[T2-2] 자리 전부 여전히 죽는다

## 변이 — 10개 중 8개 사망, 살아남은 둘은 의도적으로 남긴다

| 변이 | 결과 |
|---|---|
| ① `p` 항목 통째 삭제 | 죽음 (5실패) |
| ② **열린 집합(여집합) 복원** | 죽음 (**6실패 — extract 5 + `passages` 1**) |
| ③ `div` 삭제 | 죽음 (1) |
| ④ `li` 삭제 | 죽음 (1) |
| ⑤ `while` → `if` | 죽음 (3) |
| ⑥ `tr` 삭제(표 문맥) | 죽음 (1) |
| ⑦ `br` 추가(비블록) | 죽음 (1) |
| ⑧ `slot` 추가 | 죽음 (1) |
| ⑨ `caption` 삭제 | **살았다** (rc 0) |
| ⑩ `search` 삭제 | **살았다** (rc 0) |

⑨⑩ 은 **49개 집합에서 원소 하나를 빼는** 변이다. 둘을 죽이려면 원소마다 단언을 세워야
하고 그러면 단언이 구현 리터럴을 그대로 되뇌는 **동어반복**이 된다 — 표를 고치면 단언도
같이 고치게 되어 회귀를 못 잡는다. **8점 미만으로 남기고**, 보상 통제는 **다시 돌릴 수
있는 135태그 전수 대조**다(이번 반복이 전·후로 돌렸다). 다음 테스트 반복의 갭 탐색이
이 자리를 본다.

`.mutation-lock` 은 매번 원복했고 마지막에 `git diff` 로 확인했다 — **되돌림 True**,
변경 파일은 의도한 넷뿐이다. 탐침·벤치·변이 스크립트 넷(`probe_p.py`·`probe_regress.py`·
`bench_p.py`·`mutate.py`)은 **저장소 밖 스크래치패드**에 두었고 커밋하지 않는다.

## 완료 기준 대조

1. **숨은 텍스트 0/5 — 참이다.** 그리고 커스텀 요소를 더한 0/6 도 참이다(전에는 5/6 유출).
2. **본문 문단 5/5(6/6) — 참.** 코퍼스·리터럴·음성 축 전부 차이 0.
3. `README.md:66` 의 계약 문장 «`/passages` 는 화면에 안 보이는 텍스트를 근거로 내지
   않는다» 는 **다시 참**이다. 단위 **588** · e2e **20** 이 그대로라 `README.md` 는 무변.

## 저장소 불변

`e2e/` 무변 · `docs/specs/` 무변 · `data/crawl.db` 무변 · 새 의존성 0 · 새 파일 0(문서
아카이브 `history_048.md` 제외) · 스키마·마이그레이션 없음 · `VERSION` 1 · PR **무접촉**
(열지도 병합하지도 않았다 · `push --force` 0회). `_TextParser`·`_SKIP_TAGS`·`_INLINE_TAGS`·
`_NON_BLOCK_TAGS`·`_normalize` 는 **한 줄도 안 고쳤다**(`_TextParser` 는 그 안의 죽은
메서드 하나를 지운 것이 전부고 클래스·필드는 그대로다).

## 러너 규율 — 이번 반복 **0회 위반** (누적 33회 · 연속 5회)

전체 스위트는 `PYTHONPATH=src python3 -m unittest discover -s tests -b` 를 **맨몸·단독**
으로 불렀고(RED 확인 1 · GREEN 확인 1 · 마감 1), e2e 는
`PYTHONPATH=src python3 e2e/passage_eval.py` 1회다. **러너 출력 조작 0회** —
파이프·`grep`·`head`·`tail`·`for`·`env`·`timeout`·`2>&1`·`2>/dev/null`·`> /dev/null`
전부 0. 변이 10회는 `subprocess` 안에서 같은 명령을 돌렸고 rc 를 그대로 읽었다.
러너 아닌 곳(`git`·`wc`·`sed`·`python3` 탐침)의 파이프·리다이렉션은 규율 밖이다.
커밋 명령이 이 줄 **뒤에** 나가므로 이 절은 늘 낡는다 — 다음 반복이 정정한다.

## 문서 마감

`status.md`(이 문서 — `iteration` **296**) · `metrics.md`(`반복` **296** · `## phase 분포`
개발 **71** · `## 리뷰 정확도` 고친 것 **76**) · `index.md`(51번 행에 개발 3 결과) ·
`history_current.md`(**회전을 먼저 했다** — 300줄 상한에 정확히 닿아 있어 항목을 붙이기
전에 설계 1·개발 1 두 항목 68줄을 `history_048.md` 로 밀었고, 232줄에 개발 3 항목을 붙여
**274줄**이다. 스물여덟 회전 연속 지각 0) · `digest.md`(명부에 `history_048.md` 를 더하고
회전 사유를 기존 완료 줄에 이어 붙여 **199줄 유지 · 새 색인 구멍 0**) ·
`project.md`(성능 계수 절). `README.md` 는 안 고쳤다.

## 다음 — 테스트 3

반려 2건을 닫았으므로 **테스트**로 간다. 스위트 밖 축(코퍼스 블록 목록 · `quality_eval`
ko/en · `passage_eval` 정확도·p95)을 다시 재고, **갭 탐색은 살아남은 변이 ⑨⑩ 이 가리키는
자리** — `_IMPLIED_END["p"]` 의 원소 하나가 빠져도 스위트가 초록인 구간 — 에 건다.
동어반복이 아닌 방식(예: 명세 목록 대조를 검사로 승격)이 값이 되는지 판단한다.

## 원격 — `origin/main` 은 계획 47 까지다

- **커밋·푸시 뒤에 `git ls-remote origin` 을 다시 읽었다**(digest 의 「원격을 바꾸는 동작을
  «명령을 보냈다» 로 기록하지 않는다」 항목): `refs/heads/loop/hidden-passage` =
  **`03e78b9`**(개발 3 커밋, `3fcb921` → **fast-forward · force 아님**) ·
  `refs/heads/main` = **`687a159` 무변** · `refs/pull/7/head` = **`d5367fa` 무변** ·
  `refs/pull/7/merge` = `e3a70d0`(GitHub 이 만드는 것이고 내가 안 건드린다).
  이 절을 고치는 커밋이 하나 더 나가므로 그 커밋은 다음 반복이 확인한다.
- **PR #7** — `loop/merge-48-50` → `main`, 계획 48·49·50. **OPEN·미병합.** 병합은
  **사용자가 처리한다** — 이 반복은 PR 을 열지도 닫지도 않았고 그 브랜치를 건드리지도,
  `main` 으로 리베이스하지도 않았다.
- **계획 51 의 기점은 `main` 이 아니다** — `origin/main`(`687a159`)에는 계획 48·49·50 이
  없어 `README.md` 의 건수 단언이 거기서는 RED 다. `d5367fa` 에서 브랜치를 땄다.
- 앞선 병합: PR #6 = 계획 44~47(`687a159`). 충돌 0.

## 사람이 정할 것 — 다섯 (PR #7 이 붙었다)

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
2. **`--focus` 는 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 이 0 일
   때만 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **반응형 360px 미검증** — 브라우저가 없어 이 저장소의 누구도 그 화면을 못 그린다.
4. **회전 규약·러너 규율의 저장소 밖 절반**과 **사양이 남긴 둘**(`docs/specs/concept.md` 의
   `## 사람이 정할 것` — 속도 제한 시점, 사양 숫자 90%·500ms·60회/분이 초안이라는 것).
5. **PR #7 병합** — 위 「원격」 절.
