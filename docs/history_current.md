# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**반복 97~122 는 `history_006.md`, 123~128(계획 020 `deadline` 전체)은
`history_007.md`, 계획 21~24(짧은 경로 3건 + 보류 패치 소진, 2026-08-29 14:20~17:40)는
`history_008.md` 로 밀려났다** (2026-08-29 회전 3회).
**계획 25~29 는 `history_009.md`, 계획 30·31(짧은 경로) + 반복 152 탐색 +
계획 33 `clock-injection` 의 계획·설계·개발 1~4 는 `history_010.md` 로 밀려났다**
(2026-08-30 회전, 393줄 → 139줄. 상한 300줄을 넘겨 놓고 두 반복을 갔다).
**계획 33 의 마지막 네 반복(개발 5 · 테스트 1 · 리뷰 1 · e2e 1)은 `history_011.md`**
(2026-08-30 회전, 322줄 → 210줄. 상한을 넘긴 채 반복 두 번을 또 갔다 — 스텝 3 이
"미결(문서 위생)" 로 넘긴 것을 스텝 4 가 닫았다).
계획 33 의 전 phase 서술은 `plan_history_020.md` 의 `## 기록` 에 그대로 있다.

---

## 2026-08-30 05:5x | 34 graceful-interrupt | 계획 1 | 시도0

- 한 일: 직전 탐색이 고른 **중단 신호**를 계획서로 굳혔다(`docs/plan_graceful-interrupt.md`).
  착수 전 탐침 2개로 **최악 지연을 분해**했고, 그 결과가 `digest.md` `[4]`·`[6]` 을 정정했다.
- **실측 — `Crawl-delay: 30` + 응답 없는 서버, SIGINT 를 0.51초에: 종료 69.57초.**
  서버 수신 `t=0.05 / 30.07 / 60.07`(발신 간격 30.01초). 분해는
  **소켓 읽기 30초(`fetcher.py:39` 타임아웃 10초 × 3) + 재시도 앞 잠 40초(`crawl.py:74` × 2)**.
- **`digest.md` 가 적어 둔 셋이 틀렸다.** ① 최악은 90초가 아니라 **70초** —
  `before_send` 의 `remaining` 이 흘러간 타임아웃 10초를 이미 뺀다(90 은 중복 계산).
  ② `Event.wait` 는 70초 중 **40초만** 지운다. 느린 서버(재시도 없음) 재현 **7.56초는
  전부 소켓 읽기**라 **0초** 줄인다. ③ 잠만 깨우면 `Crawl-delay: 30` 서버에 10초 간격
  3발이 나간다 — **잠 깨우기와 재시도 취소는 하나의 변경**이고, 하나만 하면 RED다.
- **넷째 자리를 찾았다.** 플래그만 세우는 핸들러는 `crawl.py:184`
  `concurrent.futures.wait(timeout=20)` 을 **못 깨운다**(20.01초 다 잔다 · `time.sleep(5)`
  도 5.01초 · `Event.wait(20)` 만 0.51초). PEP 475 로 락 획득이 남은 시간으로 재시도되고,
  오늘 Ctrl-C 가 먹는 이유는 기본 핸들러가 **예외를 던져서**다 — 플래그로 갈아타면 잃는다.
  그래서 `[6]` 이 남긴 "주입 지점 한 곳만" 은 **이음매 이야기지 지연 이야기가 아니었다**.
- 결과: 코드 0줄. 계획서 4스텝·e2e 시나리오 5개 확정. 설계 트리거 3개(대안 갈림 ①②③ ·
  `crawl()` 시그니처 · 3개 파일)에 걸려 **설계 phase 로 넘긴다**.
- 다음: 설계 1스텝 — `docs/design_graceful-interrupt.md`. 네 자리를 몇 개의 기제로 덮을지,
  특히 `futures.wait` 를 어떻게 깨울지.

## 2026-08-30 06:0x | 34 graceful-interrupt | 설계 1 | 시도0

- 한 일: `docs/design_graceful-interrupt.md`. 계획서가 물은
  **"네 자리를 몇 개의 기제로 덮는가"** 의 답은 **둘**이다 — 소켓(축1)과
  `futures.wait`(축4)는 **덮지 않는 것이 옳다**.
- **결정: `stop` 이벤트 하나**를 `now`·`sleep` 옆 인자로(`stop=None` 기본).
  그 하나가 **깨우고**(`stop.wait`) **접는다**(`stop.is_set`). 대안 A(`sleep` 자리
  흡수)는 계획 탐침이 이미 거짓 판정한 것이고, C(모듈 전역)는 되돌리기 이점이
  B보다 크지 않은데 전역 상태 비용만 낸다 — **기본값 `None` 이 곧 꺼진 플래그**라
  B가 C의 이점을 이미 갖는다.
- **가장 위험한 가정을 탐침으로 깼다**(`design.md` 3-2, `gi_probe3.py`, 커밋 안 함).
  타임아웃 **30초**를 준 `futures.wait` 가 **[A] 잠든 워커 0.51초 · [B] 소켓에 갇힌
  워커 3.01초 · [C] 대조군(신호 없음) 2.01초**에 복귀했다. **가정은 참이다** —
  워커 완료가 `FIRST_COMPLETED` 로 메인을 깨우므로 축4 코드는 **0줄**이다.
- **계획 탐침의 "20.01초 다 잔다" 와 모순이 아니다.** 그때는 **워커가 계속 돌고
  있었다.** 같은 자리라도 *워커를 끝내면* 저절로 깬다 — 넷째 자리는 **독립 축이
  아니라 축1·2의 그림자**였다. 축을 셀 때 **누가 깨워 주는지까지 봐야 한다.**
- **축1도 0줄.** 재시도를 접으면 소켓이 10초×3 → **10초×1**, 목표 12초 안이다.
  `robots.txt` 왕복(10초·재시도 없음)은 뒤엣것이 취소되므로 합쳐 10초를 안 넘는다.
- **저장 모양이 갈림길이었다.** 중단된 시도를 `FetchResult(0,…)` 으로 흘리면
  `store.upsert(url, None, 0)` 이 **안 받은 페이지를 status 0 으로 박고** 다음 실행의
  `store.has()` 가 그 URL 을 영영 건너뛴다. **중단이 프런티어를 오염시키면 안 된다** →
  `result is None` 을 "저장할 응답 없음" 으로 쓰고 `_store_result` 에 가드 한 줄.
  예외로 흘리는 안(①)은 `요청이 예외로 끝났다` 를 in-flight 개수만큼 찍어
  **일부러 만든 상태를 오류로 보고**하게 되어 버렸다. `fetcher.py` 는 0줄(③ 버림).
- 결과: 코드 0줄. 계약 9항. rc는 **130**(오늘 관측값과 같은 값 — 0으로 바꾸면
  `crawl && indexer` 가 중단 뒤에도 다음 단계를 돈다).
- 다음: 개발 스텝 1 — 브랜치 `loop/graceful-interrupt` 를 파고 메인 루프 중단 가지.

## 2026-08-30 08:4x | 34 graceful-interrupt | 개발 스텝1 | 시도0

- 브랜치 `loop/graceful-interrupt` (`f316320`). `crawl(stop=None)` 하나 추가.
  신호가 서면 **새 URL 제출 0건**, 예산 소진 가지로 빠진다(떠 있는 결과는 줍고,
  사유를 찍고 `break`) — **새 종료 경로 0개**(설계 계약 6, ponytail 2번).
  메인 잠(축3)은 `wait_fn = sleep if stop is None else stop.wait`.
  `fetcher.py` 0줄 · 워커 0줄 · `_store_result` 0줄(계약 5는 스텝 2 다).
- **약한 빨강을 탐침으로 뚫었다.** 첫 RED 원문은 시그니처 에러 하나였다:
  `TypeError: crawl() got an unexpected keyword argument 'stop'`.
  그것만으로는 오늘 무엇이 일어나는지 모른다 → `stop=` 을 **받기만 하고 안 보는**
  탐침(`# 탐침: 받기만 하고 안 본다`)으로 같은 시나리오를 다시 돌렸다. 진짜 RED:
  `['http://a.com/', 'http://a.com/1', 'http://b.com/'] != [...a.com/, ...b.com/]`
  — 신호 뒤에 **한 건이 더 나갔다**(3건 수집). 그리고 `stop.waits == [] != [1.0]`
  — 메인은 주입된 `sleep` 에 잠들어 있어 **신호가 깨울 수 없었다.**
  계획 33 의 교훈("약한 빨강은 탐침으로 뚫는다")이 두 번째로 값을 했다.
- **가짜 시계 위에서 선 `Event` 는 테스트를 멈춘다 — 새 교훈.** 변이 M1(루프 꼭대기
  검사를 `False` 로) 이 실패가 아니라 **무한 정지**로 나왔다(120초 타임아웃). 원인:
  선 `Event.wait` 는 **즉시** 돌아오는데 가짜 시계는 그때 **안 흐른다** → 쿨다운이
  영영 안 차 메인이 같은 자리를 무한히 돈다. CI 90초를 통째로 태울 실패 모드다.
  고침: 테스트 더블 `FakeStop`(`wait` 가 **잔 만큼 시계를 흘리고** `is_set` 을
  돌려준다)으로 바꾸니 같은 변이가 **0.004초에 죽는다**. 스텝 2·3 도 이 더블을 쓴다.
- **변이 2종 모두 제 테스트만 물었다.** M1(꼭대기 검사 제거) →
  `test_signal_stops_new_submissions_and_reaps_inflight` 만 죽는다.
  M2(`wait_fn = sleep` 고정) → `test_main_loop_waits_on_the_signal_not_the_sleep`
  만 죽는다. 계약 2와 계약 6이 **서로 다른 테스트에 걸려 있다**는 뜻이다.
- 대조군 `test_no_signal_keeps_today` 를 같이 넣었다 — `stop` 을 안 주면 오늘 그대로.
  기본값 `None` 이 곧 꺼진 플래그라 되돌리기는 커밋 하나 revert.
- **자동 스냅샷 훅이 두 번 끼어들었다**(`06bc289`·`05a4808`, 변이가 든 중간 상태를
  커밋). `git reset --soft 2981dca` 로 접어 **커밋 하나**로 만들었다 — 설계
  "되돌리기" 절이 커밋 하나 revert 를 전제하므로 그 전제를 지켰다.
- 단위 **436건 OK**(신규 3건). README 의 단위 숫자를 433→436 으로 동기화했다
  (`test_readme.py` 가 잡아냈다 — 의도된 가드다).
- 다음: 개발 스텝 2 — `_fetch_one` 이 중단을 본다. **깨우기와 취소는 한 변경**이다.

## 2026-08-30 10:1x | 34 graceful-interrupt | 개발 1 | 시도0 (복구 · 재검증)

- 앞 반복이 스텝 1 을 `f316320` 으로 닫은 뒤 **커밋만 남기고 죽었다.** 이 반복은
  코드를 0줄 쓰고 **원격 갈라짐을 풀고 검증을 다시 받은 것**이다.
- **`git reset --soft` 가 늦었다 — 05:37 스냅샷은 그전에 이미 푸시돼 있었다.**
  로컬에서 접어 만든 `f316320` 과, 원격의 `06bc289` 가 둘 다 `2981dca` 를 부모로
  갖는 **갈라진 두 갈래**였다. 그리고 원격 쪽이 담은 것은 **변이 M1 을 심은 상태**다:

  ```
  -            interrupted = stop is not None and stop.is_set()
  +            interrupted = False  # M1
  ```

  **제자리 변이가 원격까지 나간 첫 사례다**(`0aac7b0` 은 로컬에서 끝났다).
  `git merge -s ours origin/loop/graceful-interrupt`(→ `d31560a`)로 트리를 그대로 둔 채
  갈라짐만 없앴다 — 강제 푸시 없이 푼다. 세 커밋의 트리가 전부 `76c531f` 로 같다.
- **교훈: `reset --soft` 는 푸시된 스냅샷을 못 되돌린다.** 스냅샷 훅이 끼어들었으면
  접기 전에 `git log origin/<브랜치>` 를 먼저 본다. 이번엔 `-s ours` 로 풀렸지만,
  원격 쪽이 상위집합이었으면 못 풀었다. **변이는 `.git` 없는 사본에서만 심는다** —
  이번 재검증은 전부 `scratchpad/red/`(사본, `.git` 없음)에서 쟀다.
- 재검증 결과 — 앞 반복의 수치를 독립적으로 다시 받았다:
  - 단위 **436건 OK** (3.0초, `discover tests` 전체)
  - **RED 재판정 2종, 둘 다 `AssertionError`** — `TypeError`/`KeyError` 가 아니라
    단언이 실제 동작을 쟀다. M1(`:163` 의 `interrupted or` 삭제) →
    `['a.com/', 'a.com/1', 'b.com/'] != ['a.com/', 'b.com/']`(신호 뒤 발신 1건).
    M2(`:155` 를 `wait_fn = sleep` 으로) → `stop.waits == [] != [1.0]`.
    각 변이는 **자기 테스트 하나만** 죽이고 대조군은 둘 다 살아남았다.
  - **e2e 17종 rc=0** (약 100초). 스텝 1 은 `stop=None` 기본값 뒤라 CLI 경로 무변경.
- 다음: 개발 스텝 2 — `_fetch_one` 이 중단을 본다. **깨우기와 취소는 한 변경**이다.

## 2026-08-30 12:2x | 34 graceful-interrupt | 개발 1 | 시도0 (독립 재검증 · 코드 0줄)

- **세 번째 세션이 같은 스텝에 붙었고, 붙는 동안 앞 세션이 살아서 끝냈다.** 착수 시점
  HEAD 는 `05a4808`(08:37 스냅샷)이었는데 검증을 돌리는 사이 브랜치가 `552ab46` 까지
  전진해 있었다 — `git status` 가 한 번 유령 `M` 3건을 보이고 다음 호출에 깨끗해진 것이
  그 순간이다(인덱스 갱신). **트리는 내가 잰 것과 바이트 단위로 같아** 검증이 그대로 산다.
  중복 커밋 0건 · 앞 세션 작업 손실 0건.
- 실측(전부 `.git` 없는 스크래치패드 전체 사본에서): 단위 **436건 OK rc=0** ·
  **e2e 17종 전수 rc=0**(크롤계 7종 84초 + 나머지 10종). 기준선 433 → 436 이 스텝 1 분.
- **약한 빨강을 독립으로 다시 확인했다.** `2981dca` 코드에 새 테스트를 얹으면 2건 다
  `TypeError: crawl() got an unexpected keyword argument 'stop'` 이다. `stop=None` 을
  **받기만 하는** 시그니처 한 줄을 얹자 같은 2건이 `AssertionError` 로 바뀌었다 —
  `['a.com/','a.com/1','b.com/'] != ['a.com/','b.com/']` 과 `[] != [1.0]`. 진짜 빨강이다.
- **변이 4종 전부 죽었다(rc=1).** M1·M2 는 앞 세션과 같은 값이고 **M3·M4 는 새로 심었다** —
  M3(종료 사유 구분 삭제)은 새 테스트와 **기존 `test_exhausted_budget_is_reported` 를 함께**
  죽였고, M4(중단이면 in-flight 를 버린다)는 "안 주우면 같은 URL 을 또 때린다" 는 설계
  문장이 실제로 단언돼 있음을 보였다. **계약 6 은 절반이 아니라 두 절반이 다 덮여 있다.**
- 곁가지 실물 고장 하나: `status.md` 프런트매터가 `step: 1` 인데 본문·계획서는 둘 다
  "스텝 1 DONE, 다음 2" 였다. **프런트매터만 읽는 세션은 스텝 1 을 다시 한다** — `step: 2`
  로 고쳤다. `metrics.md` 의 `반복 136` 은 반복 166 기준으로 낡았지만 추정으로 못 채운다.
- 다음: 개발 스텝 2 — `_fetch_one` 이 중단을 본다. **깨우기와 취소는 한 변경**이고
  따로 하면 `Crawl-delay: 30` 서버에 10초 간격 3발이라 RED다(계획 2절 3번).

## 2026-08-30 12:5x | 34 graceful-interrupt | 개발 2 | 시도0

- **워커가 중단을 본다.** `_fetch_one(stop=None)` — 진입 검사(뒤가 `robots.txt` 왕복),
  `before_send` 진입 검사, `if remaining > 0 and wait(remaining): raise _Interrupted`,
  `result = None`, `_store_result` 의 중단 가지, `submit` 배선. `fetcher.py` 0줄.
- **계획이 예상한 신규 2건이 5건이 됐다** — 늘어난 셋은 전부 실제로 살아남은 변이가
  근거다. 그중 **M6(`submit` 이 `stop` 을 안 넘긴다)이 교훈이다**: 단위 테스트가 워커
  함수를 직접 부르면 4건이 다 통과하는데 실제 크롤은 오늘 그대로 재시도한다.
  워커 단위 테스트를 쓸 때는 **배선을 재는 통합 1건**을 같이 둔다.
- **정상 동작이 옛 테스트의 타이밍 가정을 깼다.** 스텝 1 의 `test_signal_stops_new_
  submissions_and_reaps_inflight` 가 전체 스위트 부하에서 간헐 실패(1/4) — 진입 검사가
  생기면서 **아직 발신 안 한 워커가 신호를 보고 정당하게 접어** "떠 있던 요청의 결과"
  자체가 안 생겼다. 단언을 느슨하게 하는 대신 b.com 의 발신을 `Event` 로 기다린 뒤
  신호를 세우도록 **시나리오를 결정적으로** 만들었다. 제품 코드 0줄.
- 검증: 단위 **441건 OK**(5회 반복, 레이스 재현 없음) · **e2e 17종 전수 rc=0** ·
  **변이 6/6 사망**(`.git` 없는 스크래치패드 사본, 심기 전 `count(old) == 1` 단언).
  README 검증 숫자 436 → 441(`test_verification_counts_match_reality` 가 잡았다).
- 다음: 개발 스텝 3 — `main()` 이 SIGINT 를 신호로 바꾼다. 계약 7(`SIG_DFL` 먼저 →
  `stop.set()`, `finally` 로 핸들러 복원, 반환값 130). 계약 9(독스트링의 "최악 90초")도
  어느 스텝에도 안 배정돼 있으니 거기서 같이 고친다.

## 2026-08-30 13:5x | 34 graceful-interrupt | 개발 3 | 시도0

- **CLI 가 SIGINT 를 신호로 바꾼다.** `main()` 에 `stop = threading.Event()`,
  핸들러는 **`SIG_DFL` 먼저 → `stop.set()`** (두 번째 Ctrl-C 즉사), `finally` 로 원래
  핸들러 복원, `crawl(..., stop=stop)`, 반환값 **중단이면 130**. 계약 9(독스트링의
  "최악 90초")도 같이 실측(69.57초)으로 고쳤다. `crawl.py` 제품 변경 25줄·테스트 1파일.
- **핸들러 복원은 `signal.signal` 의 반환값으로 떴다** — 설계서가 적은 `getsignal` 과
  같은 객체인데 호출이 하나 줄고 등록·저장이 한 줄로 붙어 순서를 틀릴 자리가 없다.
- **테스트는 실물 신호를 안 쓴다.** `os.kill(getpid(), SIGINT)` 는 핸들러가 자기를
  SIG_DFL 로 내린 뒤라 **테스트 프로세스를 죽인다.** 설치된 핸들러를 직접 부르고,
  각 테스트는 센티널 핸들러를 깔았다 되돌리는 컨텍스트 매니저 안에서 돈다 —
  복원 계약을 재는 테스트가 곧 다른 테스트의 오염 방지다.
- 검증: 단위 **445건 OK**(+4) · **e2e 17종 전수 rc=0** · **변이 4/4 사망**
  (rc 130→0 · SIG_DFL 제거 · 복원 제거 · `stop=` 미전달. 스크래치패드에서 심고 복원).
  실물 탐침(로컬 서버 `Crawl-delay: 30`, 서브프로세스에 진짜 SIGINT):
  **rc 130 · 신호 뒤 0.01초 · 받아 둔 1페이지 유지 · 중단 중 새 요청 0**.
  README 검증 숫자 441 → 445.
- 다음: 개발 스텝 4 — `e2e/interrupt_e2e.py` 신규(계획 5절 시나리오, CLI 서브프로세스).
  위 탐침이 그 뼈대다. 변이 3종(스텝 1·2·3 각각 되돌리기)에서 전부 실패해야 한다.

## 2026-08-30 13:0x | 34 graceful-interrupt | 개발 4 | 시도0

- **중단 e2e 신규 — 개발 phase 끝.** `e2e/interrupt_e2e.py` 하나(제품 코드 0줄).
  계획 5절 시나리오 다섯을 그대로 잰다: [0] 대조군 3페이지 rc 0(잣대, `--control` 은
  페이지를 503 으로 막아 **불능 2**) · [1] 안 답하는 서버 + `Crawl-delay: 30` 에서
  **SIGINT 뒤 10.0초에 rc 130 · 페이지 요청 1건 · DB 0행** · [3] 그 간격이 `Crawl-delay`
  미만 아님 · [2] 느린 서버에서 **떠 있던 응답을 줍는다**(1행) · [4] 두 번째 Ctrl-C 는
  **rc -2 즉사**. 전체 18.3초.
- **고정 sleep 으로 신호 시점을 잡지 않는다 — 이 e2e 의 핵심 결정.** 탐침이 쓰던
  "0.5초 뒤 SIGINT" 는 어떤 날은 파이썬 부팅에도 못 미치고, 요청이 나가기 전에 신호가
  가면 **아무것도 안 재고 초록이 켜진다.** 서버가 그 요청을 받은 것을 보고(`REQUEST_LOG`
  폴링) 신호를 보낸다. 시각도 그 SIGINT 부터 잰다.
- **변이 3종 전부 사망**(`.git` 없이 원본을 `shutil.copy` 로 백업하고 심은 뒤 복원,
  앵커마다 `count(old) == 1` 단언):

  | 변이 | rc | 어디서 죽었나 |
  |---|---|---|
  | 메인 루프 중단 검사 제거(스텝 1) | 1 | 시나리오 2 — `SIGINT 뒤 30.0초` |
  | 재시도 취소 제거(스텝 2) | 1 | 시나리오 1 — `SIGINT 뒤 30.0초` |
  | SIGINT 핸들러 등록 제거(스텝 3) | 1 | 시나리오 1 — `40초 안에 안 끝났다` |

- **스텝 1 변이를 잡은 것은 시나리오 1 이 아니라 2 다.** 안 답하는 서버에서는 워커가
  접히고 프런티어가 비어 루프가 저절로 끝나 **메인 검사 없이도 10초에 끝난다.**
  링크가 있는(=프런티어가 안 비는) 느린 서버라야 그 검사가 유일한 종료 근거가 된다 —
  **시나리오 하나로 세 스텝을 다 덮으려 했으면 거짓 초록이었다.**
- **무한 대기를 e2e 가 같이 앓지 않게 했다.** `communicate(timeout=)` 의
  `TimeoutExpired` 를 잡아 죽이고 **위반(1)으로** 바꾼다 — 이 계획이 다루는 실패가
  바로 안 끝나는 것이라, 여기서 매달리면 판정이 아니라 행이다.
- **계획서의 "응답 8초" 를 5초로 낮췄다.** `fetcher.TIMEOUT` 10초와의 여유 2초는 부하가
  걸린 기계에서 타임아웃으로 새고, 재는 것(떠 있던 응답을 줍는가)은 그 값과 무관하다.
  상한 단언 8초는 그대로 뒀다.
- 검증: e2e **18종 rc=0** · `--control` rc=2 · 단위 **445건 OK**(제품 무변경) ·
  README·`docs/project.md` 의 e2e 종수 17 → 18(`test_readme.py` 가 강제한다).
  커밋 `6349a37`, 원격 푸시까지 확인.
- **문서 위생 미결을 닫았다** — `history_current.md` 322줄 → 210줄, 계획 33 의 마지막
  네 반복을 `history_011.md` 로 밀어냈다(스텝 3 이 남긴 항목).
- 다음: 테스트 phase. 개발 4스텝이 전부 닫혔다.

## 2026-08-30 19:2x | 34 graceful-interrupt | 테스트 스텝1 | 시도0 (제품 0줄)

- **앞 세션이 API 502 로 죽었고 그 결론을 안 물려받았다.** 남긴 것은 "살아남은 둘은
  진짜 구멍이고 서로를 가려준다" 한 줄뿐이고 **어느 변이인지 기록이 없었다.**
  스크래치패드 사본(`.git` 없음)에 계약 1~9 변이 14종을 다시 심어 재판정 →
  **12사망·2생존**. 생존은 **C3(깬 이유 안 구별)·C4b(`before_send` 진입 검사)** 로,
  앞 세션의 진술과 결과는 같았지만 **그 사실은 다시 재고 나서야 알았다.**
- **왜 서로를 가려주나.** 옛 테스트는 신호를 잠들기 **전에** 세운다 — 그러면 C3 을
  지워도 진입 검사가 잡고 C4b 를 지워도 `wait` 반환값이 잡는다. 한쪽씩 지우면 늘
  초록이라 **둘을 동시에 지워야 구멍이 보인다.** 새 테스트는 각각 상대가 못 걸리는
  상황을 만든다: `WokenStop`(잠든 **사이**에 서는 신호) · 간격이 **이미 지난** 재시도
  (앞 시도가 소켓 타임아웃 10초 > 간격 1초를 썼을 때. 잠이 없으니 `wait` 반환값도 없다).
- **셋째 구멍은 변이를 더 심어서 나왔다.** 14종이 다 죽은 뒤 순서·회수 축 3종을 더
  심었더니 **C7d(핸들러 순서 뒤집기)가 447건을 전부 통과했다.**
  `test_the_handler_disarms_itself_before_setting_the_signal` 은 이름과 독스트링이
  순서를 고정한다고 적어 두고 **핸들러가 돌아온 뒤의 상태**만 봤다 — 두 순서 다
  끝나고 보면 `SIG_DFL` 이고 신호도 서 있어 구별이 안 된다. `stop.set()` 이 불리는
  **그 순간**의 `getsignal(SIGINT)` 를 보도록 단언을 **더 세게** 고쳤다(제품 0줄).
  **일반화 — 테스트가 이름으로 약속한 것과 단언이 실제로 재는 것은 따로 확인한다.**
- 검증: 단위 **447건 OK**(445 → +2) · **e2e 18종 전수 rc=0**(각 `timeout 120`) ·
  **변이 17/17 사망**, 새 셋이 각각 제 변이만 물었다. 심기 전 `count(old)==1` 단언
  (BSD `sed` 거짓 초록 대응, digest `[8]`). README 445 → 447(`test_readme.py` 가 즉시 울었다).
- **한도 무손상**: 간격 기대값·`DOMAIN_INTERVAL`·1초 하한·robots·`Crawl-delay` 미변경.
  대조군과 변이 `Cx`(하한 제거→사망)가 그것을 지킨다. **낮춘 단언 0.**
- `metrics.md` 의 `반복` 은 `status.md` 프런트매터(권위 필드)를 읽어 136 → 171 로 맞췄다.
  **phase 분포 합은 여전히 낡았다** — 재구성은 추정이 되므로 안 채운다.
- 다음: 리뷰 phase. diff 는 `tests/test_crawl.py` 하나 + README 숫자 한 줄이다.

## 2026-08-30 15:0x | 34 graceful-interrupt | 리뷰 1 | 시도0

- 한 일: **계획 전체**(`git diff 310d91d..HEAD`)를 리뷰했다. 스텝별로 나눠 보면 놓치는 것이
  있어서다. **백지 패스 A 는 별도 세션에 넘겼다**(`rules/review.md` 0절) — 계획·설계·상태를
  안 주고 diff 와 수정 파일만 줬다. 판정: **승인 필요 0 · 자동 수정 3 · 기록만 4.**
- **자동 수정 3건**(제품은 주석뿐, 실행 코드 0줄): `crawl.py:130` 이 같은 독스트링 안에서
  자기와 모순(예산 절 "최악 90초" ↔ Ctrl-C 절 "그 90초는 오답, 실측 69.57초") — 설계 **계약 9**
  가 한 문단에만 적용됐다 · `frontier.py:68` "팝해 놓고 안 보내는 경로가 **둘**" 인데 이번
  변경이 **셋째**를 만들었다(쿨다운 계약의 범위를 세는 숫자다) · `docs/index.md` 34행이
  `(미개설) 0/4` 인데 개발 4/4·테스트 1/1 이 커밋돼 있었다(전례 `1abc058`). 셋 다 고쳤다.
- **digest `[5]` 는 절반만 닫혔다 — 재서 알았다.** "`Event.wait` 로 바꾸면 공회전이 같이
  사라진다" 였는데 바뀐 것은 기제가 아니라 **갈래**다(`sleep if stop is None else stop.wait`).
  `stop` 주면 **0회**(CPU 0.00초), `stop=None` 이면 **93만 회 그대로**(CPU 1.00초).
  `main()` 이 항상 넘기므로 실사용만 닫혔다. **"같이 사라진다" 류의 예언은 재기 전엔 절반이다.**
- 검증: 단위 **447건 OK**(수정 전·후) · 변이 **C7d 재심기**로 `test_the_handler_disarms_itself…`
  가 **혼자 죽는 것** 확인(스크래치패드 `.git` 없는 사본, `count(old)==1` 단언 후 치환) ·
  공회전 A/B 실측 · 백지 세션이 독립 실행한 `interrupt_e2e` rc 0 · `--control` rc 2 · 변이 10종.
  백지 세션의 지적 6건 중 **셋이 내 목록과 독립적으로 겹쳤다**(`:130` 모순 · `:68` 과장 · e2e 여유).
- 다음: e2e phase(마지막). `docs/e2e/graceful-interrupt/` 에 결과를 옮긴다 — 재는 것은 이미
  `e2e/interrupt_e2e.py` 에 다 있다. **이 파일이 정확히 상한 300줄이라 회전이 먼저다.**
