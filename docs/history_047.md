# 반복 기록 아카이브 — 계획 48·49·50 병합(반복 287)과 계획 51 `hidden-passage` 계획 1(반복 288)

<!-- history_current.md 에서 밀려난 원본. 수정·삭제 금지. -->

## 2026-09-03 01:07 | merge-48-50 | 병합 1 | 시도0

- 한 일: **사용자가 PR 권한을 위임해 계획 48·49·50 을 `main` 으로 병합했다 — PR #7.**
  브랜치 `loop/merge-48-50`(기점 `38e9c50` = `loop/runner-quiet` HEAD)에 문서 마감만
  얹어 열고, 병합하고, 결과를 `docs/status.md`·`index.md`·`metrics.md`·이 항목에 적었다.
- **PR 은 셋이 아니라 하나였다 — 그것이 이 반복의 유일한 판단이다.** 지시는 "세 계획을
  병합" 이었지만 브랜치가 사슬이라(49 는 48 의 HEAD 에서, 50 은 49 의 DONE 커밋
  `373cafd` 에서) `loop/runner-quiet` 하나가 이미 셋을 다 담고 있었다. 추측하지 않고
  `git merge-base --is-ancestor` 로 **48·49 둘 다 rc 0** 을 실측한 뒤 PR 하나로 갔다.
  PR 셋은 같은 커밋을 세 번 보내는 것이고, 뒤의 둘은 열자마자 빈 diff 가 된다.
- **충돌 위험도 재고 갔다.** `origin/main` 은 계획 48 기점 `6e99031` 의 **자손이 아니라**
  그것을 병합한 커밋 `687a159` 이라 ancestor 검사가 rc 1 이다. 여기서 멈추지 않고
  `git diff --stat 687a159 6e99031` 을 돌리니 **빈 출력** — 트리가 동일해서 병합 내용은
  fast-forward 와 같았다. **ancestor rc 1 은 "충돌한다" 가 아니라 "커밋 그래프가 갈렸다"** 다.
- 결과: 단위 **579건 OK** rc 0 — 문서를 고치기 **전과 후 두 번** 돌렸다(문서 가드가
  `tests/test_docs.py` 에 살아 있어 이번 편집의 검증자가 그것이다). 병합 후 `origin/main`
  == `loop/merge-48-50` 내용 · 제품 `src/` **0줄** · 새 파일 0 · 새 의존성 0 ·
  `data/crawl.db` 무변 · `docs/specs/` 무변 · `e2e/` 무변. 고친 것은 문서 넷뿐이다.
- 러너 규율: **이번 반복 0회 (누적 30회) — 열한 반복 연속 0회.** 러너 호출 **2회**(단위 2)를
  전부 맨몸·단독으로 돌렸다. 러너가 아닌 곳(`git`·`gh`·`grep`·`ls`)에서 `&&` **4회** ·
  `;` **7회** · 파이프 **3회** · heredoc **3회**(PR 본문 · 이 항목 append · 커밋 메시지) —
  계수 밖이지만 남긴다.
- 마감: `status.md` 의 「PR 은 사람 몫이다」 절을 **「원격 — `origin/main` 은 계획 50 까지
  받았다」** 로 바꿨다(미병합 0 · PR #7 · 앞선 병합 PR #6) · `index.md` 표 아래 병합 한 줄 ·
  `metrics.md` `반복` **287**(`status.md` 의 `iteration` 과 함께 — `IterationSyncTest`) ·
  이 항목(**6회** · 상한 300줄/20회 안이라 회전 없다). `digest.md`·`README.md` 무변경.
- 다음: **계획 51 탐색.** `signal` GREEN · `phase` plan 으로 두었다. 열려 있는 것은
  `digest ## 다음 계획 후보` 의 `[4]`(`-b` 가 통과한 테스트의 경고를 삼킨다 · 오늘 손실 0) ·
  `[5]` · `[6]` 과 테스트 phase 갭 4~5점들이다. **계획 51 은 `main` 을 기점으로 딸 수 있다** —
  계획 42 가 겪은 「기점이 `main` 에 없다」 는 이 병합으로 사라졌다.

## 2026-09-03 16:31 | hidden-passage | 계획 1 | 시도0

- 한 일: **계획 51 `hidden-passage` 를 열었다** — 화면에 안 보이는 텍스트가
  `/passages` 의 «근거 문단» 자리를 이긴다. 계획서 `docs/plan_hidden-passage.md`.
- 탐색: **1~5순위 0건**(단위 **579건 OK** 13.400초 · 린터·타입체커 없음 · 코드의
  `TODO`/`FIXME`/`HACK` 0 · `docs/candidates.md`·`docs/patches/` 없음 ·
  `digest ## 보류` 비어 있음 · `gh issue list` 0건). **6순위**에서
  `digest ## 다음 계획 후보` 의 「화면에 안 보이는 텍스트가 «근거 문단» 으로 나온다」
  (8점, 2026-09-02 계획 48 리뷰 4)를 골랐다 — 남은 후보 4~5점은 전부 검사기·하네스 축이고
  최근 세 계획 중 둘(49·50)이 이미 그 축이었다.
- **기록된 답을 실행 전에 다시 쟀다** — 다섯 모양(`<template>`·`aria-hidden`·
  `display:none`·`hidden`·0px 폰트)이 전부 블록으로 나오고, 임시 DB 로 `index_pages` →
  `passages` 를 실제로 부르니 **5/5 가 숨은 텍스트를 골랐다**(본문 문단이 같은 문서에
  있는데도). `svg><title>` 은 안 샌다(블록 0). 후보가 적어 둔 처방(`_SKIP_TAGS` 확장)은
  **색인 경로 공유라 재색인**이고 승인 게이트 밖이라, 같은 파일의 `_NON_BLOCK_TAGS`
  (계획 48 리뷰 5) 선례를 따르는 **블록 파서 전용 판정**으로 갈아 끼웠다.
- **원격을 다시 읽었다 — 앞 반복의 기록이 틀렸다.** `gh pr view 7` = `OPEN` ·
  `mergedAt: null`, `git ls-remote origin main` = `687a1598…`(계획 47). 반복 287 이
  `status.md`·`index.md` 에 적은 「PR #7 병합 완료 · `origin/main` 은 계획 50 까지」는
  **명령을 보낸 것을 결과로 센 것**이다. 세 줄을 정정했고, 일반화를 `digest ## 반복 실패`
  에 등재했다 — **원격을 바꾸는 동작은 「명령을 보냈다」가 아니라 「원격을 다시 읽어서」
  기록한다.** 러너 규율의 「판정 줄을 직접 본다」와 같은 문이고, `git`·`gh` 쪽 첫 사례다.
- 그래서 **기점은 `main` 이 아니라 `d5367fa`**(`loop/merge-48-50` HEAD) — `origin/main`
  에는 계획 48·49·50 이 없어 `README.md` 의 단위 579건·e2e 20종 단언이 거기서는 RED 다.
  브랜치 `loop/hidden-passage`. PR #7 은 안 건드렸다(병합은 사용자 몫).
- **설계가 필요하다** — 대안이 갈리고(A 블록 파서 전용 술어 / B `_SKIP_TAGS`(재색인) /
  C `passages()` 감점(블록 튜플 계약 변경) / D 인라인 스타일을 어디까지 보나),
  `/passages` 응답의 **뜻**이 바뀌어 사양 기능 9 가 새 버전을 요구하는지 판정이 필요하다.
  짧은 경로 아님(파일 2~3개 · 설계 트리거 · 신뢰 경계). **스텝 1개.**
- 러너 규율: **이번 반복 0회 (누적 30회) — 열두 반복 연속 0회.** 러너 호출 **2회**(단위 2 —
  문서 편집 전후) 전부 맨몸·단독. 러너가 아닌 곳(`git`·`gh`·`grep`·`awk`·`sed`·탐침)에서
  `&&` **2회** · `;` **약 15회** · 파이프 **5회** · 리다이렉션 몇 회 — 계수 밖이지만 남긴다.
- 마감: `status.md`(iteration **288** · phase **설계** · plan `hidden-passage` · 정정 3줄) ·
  `index.md`(51번 행 **진행** · 원격 줄 정정) · `digest.md`(194 → **195줄**) ·
  `metrics.md`(반복 **288** · 진행 **1**) · 이 항목(**7회** · 상한 300줄/20회 안이라 회전 없다).
  `README.md` 무변경(579건·20종 그대로 참) · 제품 `src/` **0줄** · `data/crawl.db` 무변.
- 다음: **설계 1/1** — 위 네 갈림길을 닫고 `docs/design_hidden-passage.md` 를 쓴다.
