---
signal: GREEN
phase: 개발
step: 6
attempt: 0
iteration: 262
updated: 2026-09-02
ctx: 59
night_iterations: 107
night_red: 0
night_retries: 0
plan: passage-api
---

# 현재 상태

**리뷰 3 이 계획 48 을 세 번째로 반려했다 — 이번엔 성능이 아니라 캡 되감기가 문서를
통째로 지우는 결함이다(승인 필요 2건). 개발 6 으로 돌아간다.**
활성 계획 `passage-api` · 설계서 `docs/design_passage-api.md` · 브랜치 `loop/passage-api` ·
기점 `6e99031`. **단위 558건 OK** · `e2e/*.py` **20종 전수 rc 0** ·
이번 반복의 제품 `src/` **0줄**(리뷰는 읽기만 한다 · 변이는 `.mutation-lock` 아래 전부 원복).

## 이번 반복이 한 것 (리뷰 3)

**백지 패스를 먼저 닫고 대조 패스로 갔다.** `indexer.passages` · `extract` · `serve` 를
읽어 후보 6건을 세우고 3건을 버린 뒤 3건을 보고했다. 승인 필요가 2건이라
`rules/review.md` 6절대로 **반려**다.

- **[승인 필요] 되감기가 문서를 통째로 지운다** — `src/websearch/indexer.py`.
  캡 안 마지막 `<` 가 인덱스 0 이면 `html[:0]` 이라 원문이 통째로 사라진다.
  실측: `"<p>" + "가"*40000` 이 **블록 0개**가 되어 그 문서가 `/passages` 에서 빠진다.
  잃는 원문은 최대 35,000자(캡 전부).
- **[승인 필요] 되감기가 캡 경로에만 걸려 있다** — 같은 파일. 캡보다 **짧은** 깨진 HTML
  (닫히지 않은 속성값 · 여는 태그 도중)은 되감기를 안 지나 그대로 응답에 실린다.
  실측(`probe_leak.py`): `'<p>김치찌개 <a href="xxx…'` 가 블록에 `<` 를 담아 나온다.
  **테스트 3 은 증상(자기가 만든 컷)만 닫고 형제 경로를 안 봤다.**
- **[딸린 주석]** `indexer.py` 의 «되감으면 잃는 것은 많아야 온전한 태그 하나» 가 위 둘과
  모순된다 — 코드를 고칠 때 함께 고친다.

**제안 수리를 실측으로 검증하고 원복했다**(`.mutation-lock` 아래 · `git diff -- src/` 0줄):

```python
html = row[0][:MAX_PASSAGE_HTML]
if html.rfind("<") > html.rfind(">"):
    html = html[:html.rfind("<")]
```

마지막 `<` 가 마지막 `>` 보다 뒤일 때만 «마크업이 안 닫혔다» 다. 정상 문서(`</html>` 로
끝남) · 태그 0개 · 텍스트의 날 `<` 는 되감지 않는다. 결과: **단위 558건 그대로 OK** —
즉 **기존 테스트는 두 결함에 눈이 멀어 있다**(깨진 동작을 붙드는 단언이 0개).
`probe_fix.py` 로 F1·F3 이 닫히고 대조군이 그대로임을 확인했다.

**성능 축은 닫았다 — 이번 반려는 «성능 초록불» 의 세 번째 재발이 아니다.** 프로세스
밖에서 `GET /passages` p95 를 쟀다(문서 12건 × 캡 35,000자): wiki 모양 **121.0ms(예산
500ms 의 24%)** · `<p>가</p>` 반복 **190.3ms(38%)**. 계수 0.489 축은 설계서 갈림길 5 가
근거와 닫는 조건까지 적어 둔 **공개된 판정**이라 `rules/severity.md` 4절의 억제 대상이고,
실측도 예산 안이다. 갈림길 5 에 이 실측을 적어 축을 «열린 채로, 다만 실측된» 상태로 뒀다.

## 넘긴 것 — 개발 6 이 할 일

1. 위 두 결함을 **한 줄로** 닫는다 — 제안 수리는 실측까지 끝나 있다.
2. **먼저 RED 를 본다.** 지금 558건이 둘 다 못 잡으므로, `"<p>"+"가"*40000` 이 문단
   1건을 내는 단언과 «캡 아래 깨진 속성값이 `<` 를 안 흘린다» 단언을 먼저 쓴다.
3. `indexer.py` 의 «많아야 온전한 태그 하나» 주석을 고친 코드에 맞춘다.
4. 열린 채로 두는 것: 갈림길 5(최악 입력의 축) — 파서 교체는 실물 코퍼스가 조건이다.
5. 회전은 **이번 반복이 이미 돌렸다** — 항목을 붙이니 302줄이라 개발 5 를
   `docs/history_037.md` 로 밀었다(282줄 · 열일곱 회전 연속 지각 0). 테스트 3 은 리뷰 3 이
   지목한 원본이라 남겼다. 다음 항목 하나는 여유가 있다.

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-02 확인)

`origin/main` 은 PR #6 으로 계획 44~47 까지 받았다. 계획 48 의 커밋들은 브랜치에만 있다.
**이번 반복이 `origin/loop/passage-api` 로 푸시했다** — 직전 반복이 «저장소 밖 외부
네트워크» 한도를 오독해 `c6d6430` 을 안 올린 채 뒀던 것을 함께 올렸다. 푸시는 루프의
정상 절차다(강제 푸시만 금지).
**남은 것 (사람 결정)**: 계획 48 이 끝난 뒤 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 — 넷이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, `~/.claude/skills/loop-harness/rules/docs.md` 에는 그 예외가 없다.
   **저장소 밖이라 루프가 못 고친다.**
4. **«러너 호출을 다른 것과 섞지 않는다» 를 문장이 아니라 훅으로 옮기는 일.**
   재발 **18회**. 저장소 쪽 자리(`docs/project.md ## 명령`)에 이미 적혀 있는데도 났고,
   남은 자리는 `~/.claude/skills/loop-harness/rules/` 라 **저장소 밖이라 루프가 못 고친다.**
   이번 반복도 4회를 0 으로 끝냈지만 그것이 항목을 닫지는 않는다 — 닫으려면
   **판정 명령을 단독 실행으로 강제하는 PreToolUse 검사**가 필요하다.

**사양이 사람에게 남긴 것 셋** (`docs/specs/concept.md` 의 `## 사람이 정할 것`):
속도 제한 시점(가정 — IP 당 분당 60회) · JSON 검색과 근거 문단의 분할(가정 — 쪼갠다,
**계획 46 이 그 가정을 따랐고 닫았다**) · 사양 숫자들이 초안이라는 것(90% · 500ms ·
60회/분). 첫째와 셋째는 아직 열려 있다.
