---
signal: GREEN
phase: 계획
step: 0/0
attempt: 0
iteration: 545
updated: 2026-09-11
ctx: 90
night_iterations: 1
night_red: 0
night_retries: 0
plan: null
---

## 이어받는 세션이 먼저 볼 것 (2026-09-11 · ctx 90% 로 정상 종료)

**크롤이 백그라운드에서 돌고 있다.** 끝나면 색인까지 자동으로 이어진다
(`--max 30000` · `--deadline 10800`). 진행은 이렇게 본다:

```bash
python3 -c "import sqlite3;c=sqlite3.connect('file:data/crawl.db?mode=ro',uri=True);\
print(c.execute('select count(*) from pages').fetchone()[0])"
pgrep -f websearch.crawl   # 비었으면 끝난 것
```

**규모를 막던 축은 전부 열렸다. 남은 것은 크롤 시간뿐이다.**
색인 **10,461** · pages **23,268** · 발견 큐 **321,913** · 1.05GB · 전수 **762 OK**.

| 축 | 상태 | 연 계획 |
|---|---|---|
| 저장 | 10만 환산 **3.7~5.4GB** | 77 |
| 색인 고정비 | 매 실행 **13초** | 78 |
| 처리량 | **8.84 문서/초**(12도메인) · 간격 무변 | 82·83 |
| 재개 | 실행을 넘어 **이어진다** | 88·91·92·95 |
| 질의 | `/search` p95 **38.5ms**(예산 12.8%) | 85 |

**병합은 손으로 조립하지 않는다** — `scripts/merge-to-main.sh` 를 쓴다
(전수가 초록일 때만 민다 · 계획 94 가 변이로 이빨을 쟀다).

## 2026-09-11 에 배운 것 — 같은 증상, 매번 다른 원인

**「큐에 일감이 있는데 크롤이 0장」이 세 번 났고 원인이 셋 다 달랐다:**

| 계획 | 원인 |
|---|---|
| 88 | 큐에 **못 들어옴** (부모가 신선해 링크 추출이 없다) |
| 92 | 큐가 **빔** (영속화 전에 마지막 크롤이 끝난 닭-달걀) |
| 95 | 큐를 **잘못 잘라 읽음** (범위 필터보다 상한이 먼저 걸렸다) |

**증상이 같다고 원인이 같지 않다.** 셋 다 단위 테스트로는 안 보였고 실물을 돌려야 나왔다.

**그리고 「고쳤다고 믿은 것」이 안 막고 있던 자리가 둘 더 있었다:**
- 계획 93 — 계획 90 이 막았다고 믿은 「테스트가 실물 코퍼스에 쓴다」가 **그대로 열려
  있었다**(파이썬 기본 인자가 정의 시점에 묶인다). 테스트 한 번에 실물 DB 1,001행,
  **3,580개 도메인에 실제 요청**. 90 은 증상(느려짐)만 고쳤다.
- 계획 94 — 병합을 손으로 조립하다 **전수가 빨간데 푸시됐다.**

**처방은 규율이 아니라 절차다.** 바깥과 만나는 축을 고쳤으면 **실물 한 번을 e2e 완료
기준에 넣고**, 반복하는 절차는 **파일로 옮겨 변이로 이빨을 잰다.**

## 2026-09-09~10 에 배운 것 (digest `[8]` 등재)

**「로컬 목·작은 표본은 초록, 실물은 틀림」이 이틀에 다섯 번 났다** (digest `[8]` 등재):

1. 저장 외삽 **4.4배** — 120장 표본이 꼬리를 못 봤다
2. 크롤 예의 — 위키미디어 6도메인이 **같은 IP** 라 한 서버에 초당 6건(429 3,686건)
3. 계획 86 이 실물에서 **아무 일도 안 함** — 부모가 신선해 자식이 큐에 못 들어온다
4. 벌점이 robots 칸에 **누출** — `set_delay` 가 `interval()` 을 읽었다
5. **테스트가 사용자 코퍼스에 썼다** — `crawl.main` 의 기본 DB 가 실물이었다

**②③④⑤ 는 전부 «둘 이상이 만나는 자리»** 이고 단위 테스트는 구조적으로 한 번에
하나만 본다. **처방은 규율이 아니라 절차다** — 바깥과 만나는 축을 고쳤으면
**실물 한 번을 e2e 완료 기준에 넣는다.**

## 사람 결정 대기 (셋 다 자율 위임 범위 밖 — 손대지 않았다)

- **`docs/patches/userinfo-leak.patch`** — URL 자격증명이 크롤·저장·렌더로 새는 건.
  야간이 구현·테스트까지 하고 초록 확인 후 패치로 뽑아 뒀다. **보안 경계**라 적용 보류.
- **`~/.claude/settings.json`** — 미커밋. **코어층 가드가 막았고 우회하지 않았다.**
  내 변경도 아니다(`model: opus → opus[1m]` + `allow` 자리 정렬 · 권한 상향 아님을
  diff 로 확인). 승인하면 `git commit --no-verify`.
- **`~/.claude/hooks/ojeong-guard.cjs`** — 카파시 4번 훅이 **매 턴 오탐**을 낸다.
  `RUNNER` 가 `^python3` 로 앞머리를 고정해 **환경변수로 시작하는 이 저장소의 정식
  테스트 명령을 한 번도 검증으로 안 센다**(전수 755건을 돌려도 0번으로 센다).
  고칠 내용은 `~/.claude/hooks/guard.test.js` 가 그대로 명세하고 회귀 검사에
  **skip 으로 사유를 남겼다.** 분류기가 그 파일 쓰기를 막아 내가 못 고친다 —
  나를 제약하는 가드를 내가 푸는 것은 권한 상향이라 우회하지 않는다.

## 직전 반복 (535) — 짧은 경로 `verdict-head` 완료

`verdict_gap` 이 「index.md 다섯째 칸이 e2e」라는 **자리 약속** 위에 서 있어, e2e
**앞에** 열을 끼우면 개수가 맞아 통과하고 엉뚱한 칸을 판정으로 읽었다. 자리 대신
**이름**을 확인하게 고쳤다(`VERDICT_HEAD`). 전수 755 OK.

**그 반복이 어젯밤 자기 판단을 뒤집었다** — 「탐색 8출처 전부 0건」이라 했는데
`digest` 의 **후보 절이 둘**이고 「테스트 phase 갭」 절을 안 봤다. 거기 착수 가능한
`[6]` 이 둘 있었다. **탐색할 때 후보 절이 둘이라는 것**이 그날 산 것이다.
