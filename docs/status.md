---
signal: GREEN
phase: 계획
step: 0/0
attempt: 0
iteration: 449
updated: 2026-09-07
ctx: 47
night_iterations: 0
night_red: 0
night_retries: 0
plan: null
---

## 현재 상태

**반복 448 의 야간이 「아침에 사람이 정할 것」으로 남긴 둘 중 하나를 닫았다.**
`digest.md` 201 → **198줄**. 전수 **672 OK rc 0**.

## 짧은 경로 — digest-cap-verdict

- 근거: 야간 448 이 `digest.md` 201줄(상한 200)을 사람 몫으로 남겼고, 계획 76 은
  그 판단을 **「무인이 `digest.md` 를 회전해도 되는가」라는 규약 질문**으로 적어 두고
  후보 `[7]` 의 digest 축을 그날까지 열어 뒀다. 오늘이 그날이다.
- 할 일: 답을 정하고 후보 `[7]` 을 닫는다. `docs/digest.md` 만 건드린다.
- 완료 기준: digest ≤ 200줄 · 후보 `[7]` 에 취소선과 닫는 근거 · 전수 초록.
- 이미 한 것: **전부.** 회전 기록 셋 삭제 + 후보 `[7]` 닫는 근거 · 672 OK.

## 정한 것 — 무인은 `digest.md` 를 회전하지 않는다

**그래서 digest 축에는 못을 세우지 않는다.** 세 가지 이유다:

1. 처방이 **삭제**라 못을 박으면 야간이 **스스로 못 푸는 RED** 가 된다 (계획 76 이
   이미 적었다). 오늘 실제로 201줄이었으니 그 못은 박는 순간 빨갛다.
2. 삭제 권한을 야간에 주면 야간이 **판단 재료**를 자기 판단으로 지운다. 원본이 git 에
   남아도 다음 탐색은 그것을 안 읽으므로 **실질은 되돌릴 수 없다.**
3. 못이 없어서 치른 값은 초과 **16·15·13·1줄** 넷뿐이고 **네 번 다 사람이 알아챘다.**
   history 축은 **83줄을 여섯 반복 동안 아무도 못 봤다** — 그래서 그쪽만 못을 세운
   계획 76 의 선택이 옳았다. **두 축은 같은 상한이지만 같은 위험이 아니다.**

**대신 자라는 원인을 줄였다.** `## 완료` 의 회전 기록은 회전 한 번에 한 줄씩 늘어
상한을 스스로 밀어내는 **트레드밀**이다. 아카이브 명부가 파일 이름을 영구 보관하고
어느 반복이 어디로 갔는지는 `index.md` 와 아카이브 파일 자체에 있으므로, 판단이 없는
셋(`history_070`·`071`·`072`)을 지웠다. 남긴 둘은 판단을 담은 것이다.

## 남은 「사람이 정할 것」

- `.context-state.json` 이 반복 448 시점에 56분 낡았다 — **정상이다.** 그 값은
  statusLine 이 매 렌더마다 쓰는데 밤에는 대화형 세션이 없다. 야간이 `SKILL.md` 대로
  반복 상한에만 의존한 것이 맞는 처리다. **고칠 것 없음.**
