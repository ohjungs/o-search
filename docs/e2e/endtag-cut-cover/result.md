# e2e 결과 — 계획 59 `endtag-cut-cover` (반복 349 · 2026-09-05)

**판정: 통과 — 21종 전수 `rc 0` · 기준선 회귀 0 · 완료 기준 6/6 → DONE.**

## 0. 면제 — 새 e2e 를 안 만든다 (`rules/e2e.md` 3절)

계획이 바꾼 것은 `tests/test_extract.py` 의 **단언 +12줄과 주석뿐**이고 제품 `src/` 는
**0줄**이다. 파서(`_BlockParser.handle_endtag`)의 동작이 한 글자도 안 바뀌었으므로
**프로세스 밖에서 달라지는 동작이 0** 이다. 새 단언이 지키는 실물 계약(숨김 컨테이너가
안 닫힌 자손 때문에 문서 끝까지 고정돼 뒤 문단이 사라지는 것)은 이미
`e2e/hidden_passage_e2e.py` 가 crawl→색인→HTTP 로 재고 있고 오늘도 **0/5 · 본문 5/5** 로
통과했다. e2e 가 낼 값은 「안 달라졌다」의 확인이라 시나리오를 새로 만들지 않는다.
(만든 e2e 파일 0개 · 고친 e2e 파일 0개 — 이 `result.md` 만 새로 쓴다.)

## 1. e2e 21종 — 전부 `rc 0`

전부 맨몸으로 돌렸다(파이프·리다이렉션 0).

| # | e2e | rc | 판정 줄 |
|---|---|---|---|
| 1 | `crawl_e2e.py` | 0 | 수집 15 · 차단 요청 0 · 최소 간격 1.005s |
| 2 | `indexer_e2e.py` | 0 | 3문서 색인 · 재실행 0문서(증분) · 상대 경로 질의 |
| 3 | `noindex_e2e.py` | 0 | 4페이지 중 2문서 · 뒤늦은 noindex 제거 |
| 4 | `search_api_e2e.py` | 0 | 10+5건 겹침 0 · 400/404/501/503/500 · p95 2.11ms |
| 5 | `crawl_delay_e2e.py` | 0 | Crawl-delay:2 최소 2.01s · 하한 도메인 1.00s |
| 6 | `non_ascii_e2e.py` | 0 | 한글 경로 3행 · 서로게이트 시드 1개 건너뜀 |
| 7 | `hidden_passage_e2e.py` | 0 | 숨은 텍스트 **0/5** · 본문 문단 5/5 · 오탐 4종 안 물림 |
| 8 | `design_check.py` | 0 | 4축 통과 · JS **0 B** · 최저 대비 4.87:1(포커스 3.56:1) |
| 9 | `tokenizer_e2e.py` | 0 | 화면 HTML 로 복합어·어순·굴절·AND · 옛 색인 503→복구 |
| 10 | `domain_key_e2e.py` | 0 | 같은 서버 4회 간격 2.01×3 · robots 1회 · 대조군 1.00s |
| 11 | `deadline_e2e.py` | 0 | 대조군 16 / CLI 8페이지 2.1s · 버려진 응답 0건 |
| 12 | `interrupt_e2e.py` | 0 | SIGINT 10.0s rc 130 · DB 0행 · 두 번째 Ctrl-C rc -2 |
| 13 | `indexer_interrupt_e2e.py` | 0 | 재구축 중단이 옛 2,000행 보존 · 재실행 복구 |
| 14 | `crawl_politeness_e2e.py` | 0 | 예외 뒤 2.01s · 대조군 1.00s · 재시도 1.00s |
| 15 | `pagination_ui_e2e.py` | 0 | 다음을 따라가 doc00 · 마지막에 다음 없음 |
| 16 | `retry_interval_e2e.py` | 0 | https 재시도 5.01s · 대조군 1.00s |
| 17 | `url_normalize_e2e.py` | 0 | 표기 10개가 문서 6개로 |
| 18 | `perf_crawl.py` | 0 | [열림] **10.24/s** · [차단] **10.24/s** · [예외] 간격 0.996s |
| 19 | `perf_search.py` | 0 | p50 1.33ms · **p95 8.80ms** (예산 300ms 의 2.9%) |
| 20 | `quality_eval.py` | 0 | ko **20/20** · en **19/20** · 매치 평균 14.0 · 최소 11 |
| 21 | `passage_eval.py` | 0 | 정확도 **100.0%** · 채택률 99.5% · **p95 1.51ms** |

## 2. 기준선 회귀 — **0건**

| 축 | 계획 58 e2e(반복 344) | 오늘(반복 349) | 판정 |
|---|---|---|---|
| 근거 문단 정확도 | 100.0% | **100.0%** | = |
| `/passages` p95 | 1.52ms | **1.51ms** | 예산 500ms 의 0.3% |
| `/search` p95 | 8.81ms | **8.80ms** | 기록 9.11ms 아래 |
| 검색 품질 ko/en | 20/20 · 19/20 | **20/20 · 19/20** | = (미검출은 그 `loaf` 1건) |
| 매치 수 평균/최소/최대 | 14.0 / 11 / 28 | **14.0 / 11 / 28** | = (G2 여유 1건도 그대로) |
| 크롤 처리량 [열림]/[차단] | 10.24 / 10.21 | **10.24 / 10.24** | 기준 5.0 · 기준선 9.0 위 |
| 디자인 4축 | JS 0 B · 4.87:1 | **JS 0 B · 4.87:1** | = |
| 숨은 텍스트 유출 | 0/5 | **0/5** | = |

**움직인 기준선이 없어 `docs/project.md` 의 수치는 한 줄도 안 갱신했다.** 계획이 제품
코드를 0줄 고쳤으니 기대한 결과 그대로다 — 이 표의 값은 판정이 아니라 **그 기대가 참임을
보인 증거**다.

## 3. 완료 기준 6/6 — 오늘 전부 다시 쟀다

| # | 기준 | 실측 | 판정 |
|---|---|---|---|
| 1 | 전수 `OK` · rc 0 · `Ran 605` | `Ran 605 tests in 13.789s` · `OK` · rc 0 (건수 무변 — subTest 라 메서드가 안 는다) | 충족 |
| 2 | 변이 M-a 의 `failures` 가 2 이상 · 늘어난 것이 새 subTest | `failures=2` — `shape='안 닫힌 li'` + **`shape='안 닫힌 span'`**(착수 탐침은 1) | 충족 |
| 3 | 새 subTest 가 원본에서 초록(오탐 0) | 무변이 대조군 `run=605 failures=0` · 2절 마지막 `반대 방향` 줄(`<section><span>가</section>` → `['가','나']`)도 함께 초록 | 충족 |
| 4 | 새 단언이 `_IMPLIED_END` 표에 안 의존 | 표에서 `li` 줄을 지우는 변이 → `failures=2` 인데 **둘 다 `test_an_optional_end_tag_does_not_hide_the_next_sibling`**(`li` · `li 안의 안 닫힌 p`)이고 `안 닫힌 span` 은 초록 | 충족 |
| 5 | 고친 파일 `tests/test_extract.py` 하나 · `src/` 0줄 | 계획 59 의 커밋은 `7396117`(+12줄)과 `97c3057`(주석만 +6/-4) 둘 다 이 파일 하나 · `git status --short` 빈손 · `README.md`·`docs/specs/` 무접촉 | 충족 |
| 6 | e2e 21종 전수 rc 0 · 기준선 회귀 0 | 위 1·2절 | 충족 |

**기준 4 가 계획의 값을 그대로 보여준다.** 표를 건드리는 변이는 *다른* 테스트만 죽이고
새 subTest 는 안 움직인다 — 옛 유일한 칸(`안 닫힌 li`)이 얹혀 있던 축과 **다른 축**에서
자르기를 붙든다는 뜻이다. 자르기 자체를 죽이는 M-a 에서는 둘이 함께 죽는다.

## 4. 러너 규율 · 저장소 상태

- 전수·e2e·변이 **전부 맨몸** — `2>&1`·`2>/dev/null`·`>/dev/null` 0회(누적 위반 37 유지).
- 변이 셋은 **메모리에서만** 걸었다(`mock.patch.object` 로 `_BlockParser`·`_IMPLIED_END`
  교체 · 하네스는 스크래치패드 `e2e59_mut.py`). 저장소 파일 무변경이고 변이 실행마다
  `PYTHONDONTWRITEBYTECODE=1` + `PYTHONPYCACHEPREFIX=$(mktemp -d)` 를 함께 줬다
  (`docs/project.md` 변이 검사 항목 — 이 기계는 캐시가 저장소 밖에 쌓인다).
- `data/crawl.db` sha256 `85c96744…5bda18` **무변** · 재색인 0 · 스키마 0 · 새 의존성 0.
- 원격 `loop/passage-cost-band` = `96e8d47`(반복 348)이고 오늘 커밋을 그 위에 쌓는다.
  `origin/main` 무접촉 · PR 무접촉(만들지도 조회하지도 않았다).
