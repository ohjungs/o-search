---
signal: DONE
phase: e2e
step: 2
attempt: 0
iteration: 179
updated: 2026-08-30
ctx: 45
night_iterations: 52
night_red: 0
night_retries: 0
---

# 현재 상태

**계획 35 `deadline-stop` DONE.** 계획서는 `docs/plan_history_022.md` 로 아카이브했고
e2e 결과는 `docs/e2e/deadline-stop/result.md` 에 있다. 브랜치 `loop/deadline-stop`
(`loop/graceful-interrupt` `eef1b50` 에서 팠다) — **`main` 병합은 사람이 정한다.**
계획 34 도 DONE·아카이브 완료. **열린 계획 0.**

## 이번 계획이 한 일

**`--deadline` 의 계약이 워커 안에서 깨져 있었다.** "예산을 다하면 새 요청을 안 던지고
지금까지 저장한 것을 정상 반환한다"(`crawl.py:126`)가 문서로만 참이었다 — 착수 탐침
(`--deadline 2` · 안 답하는 서버 · `Crawl-delay: 30`)에서 서버 수신이
`t=0.05 / 30.05 / 60.06` **3건**이고 종료가 **70.08초**였다. 만료(t=2) 뒤에 두 발이
더 나갔다. 계획 34 가 만든 `stop` 을 예산 축에 이어 **1건 · 10.1초 · rc 0** 으로 닫았다.

**제품 11줄(실행 7줄)이고 공개 표면 변화는 0이다** — 예산 만료의 rc `0` · stdout
`수집 0 페이지` · stderr `예산 N초 소진` 을 **유지하는 것이 완료 기준**이었고, SIGINT 는
rc 130 그대로다(rc 판정을 `stop.is_set()` → 신호 전용 `signaled.is_set()` 으로 갈랐다).

**본체는 코드가 아니라 그것을 재는 단언이다.** 고치기 전 447건·e2e 18종이 사본에서
전부 초록이었다 — 새 단언 없이 넣었으면 검증되지 않은 변경이고 되돌려도 조용하다.

## e2e phase 실측 (2026-08-30)

- 전체 검증 선통과: 단위 **451건 OK**(3.598초) · 린트·타입체크 없음(`docs/project.md`) ·
  CI 없음(`.github` 부재).
- **e2e 18종 전부 rc=0**(합계 164초). `deadline_e2e` 9 → **19초** — 늘어난 10초가 곧
  시나리오 3 이 실제로 태우는 소켓 타임아웃이다.
- **시나리오 3**: 안 답하는 서버가 받은 페이지 요청 **1건** · **10.1초** · rc **0** ·
  `수집 0 페이지` · `예산 2초 소진` · DB **0행**. 착수 탐침은 3건 · 70.08초였다.
- **`--control` rc 2**: 페이지를 503 으로 없애면 `안 답하는 서버가 받은 페이지 요청
  표본이 0개다` 로 **측정 불능**이다(파일 전체로는 시나리오 0 이 먼저 죽으므로 3만 따로).
- 예절 무회귀: `deadline_e2e` 간격 최소 **1.00s** · `crawl_delay_e2e` `Crawl-delay:2`
  **2.01s** / 하한 **1.01s** · `perf_crawl` 1.004 / 1.001 / 1.001s · `interrupt_e2e` 위반 0.
- SIGINT 축 무회귀: `interrupt_e2e` SIGINT 뒤 **10.0s rc 130** · 두 번째 Ctrl-C **rc -2**.
- 품질 4축·처리량 **전부 기준선 그대로**: recall@10 ko **100%** / en **95%** · 오탐 평균
  **14.0** · p95 **9.29ms** · JS **0 B** · 최저 명암비 **4.87:1** · 처리량 **10.26 / 10.29 docs/s**.
  무예산 경로가 `if left is not None` 가드 안이라 바이트 무변경이고, ±0.01/s 가 그 증거다.

## 이번에 배운 것

**"깨우기와 접기는 하나다"가 신호 축(계획 34)에 이어 예산 축에서 되풀이됐다.**
`stop.set()` 만 넣으면 70.08초, `futures.wait` 자르기만 넣으면 70.07초, **둘 다일 때만
10.05초**다 — 한 줄씩은 **0초를 회수한다**. digest `[8]` 에 2회로 올렸다. 짝으로 보면
digest `[7]`("보류 항목의 진단은 옳아도 처방은 그때의 추정이다")의 네 번째 사례이기도
하다 — `[5]` 가 적어 둔 "몇 줄이 전부" 를 그대로 믿었으면 0초를 회수하고 초록으로
닫을 뻔했다.

## 다음

**새 계획 탐색**(`rules/discover.md`). 열린 계획 0 · 보류 0.
남은 후보는 `digest.md ## 다음 계획 후보` 의 `[6]`(예산+SIGINT 동시 rc 130 무측정) ·
`[5]`(`_fetch_one` 위치 인자 6개 무단언) · `[4]`(`deadline=None` 의 `timeout=None` 무측정)
셋이고 전부 이번 계획이 남긴 것이다.
**3순위(사양 미구현)는 `recrawl` 하나뿐이고 스키마 변경이라 루프가 못 연다** —
아래 한도와 `plan_history_022.md`·`history_current.md ## 사용자 판단` 참조.

**집안일 하나가 밀려 있다 — `digest.md` 가 220줄로 상한 200 을 넘었다**(이 phase 전에
이미 217줄이었다). 룰의 처방은 "오래된 완료 항목부터 지운다" 인데 그 항목들을 `index.md`
와 `plan_history_*.md` 가 참조하고 있어 **지우기 전에 참조를 먼저 확인해야 한다** —
이 phase 의 범위 밖이라 탐색 반복이 처리한다. `history_current.md` 는 248줄로 여유가 있다.

## 한도 (넘으면 RED)

- 도메인당 요청 간격 1초 이상 · robots.txt `Crawl-delay` 준수. **예산 만료 중에도 그렇다.**
- `data/crawl.db` 실물·스키마를 안 건드린다. e2e·탐침은 임시 디렉터리에서만.
- 외부 네트워크 — 로컬 테스트 서버만.
- 기존 단언을 낮추지 않는다. 시간 상한을 올려 초록을 만드는 것은 실패다.
- `docs/specs/` 는 사용자 소유(읽기만) · `--no-verify` 금지 · `main` 직접 커밋 금지.
