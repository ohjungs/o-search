---
signal: GREEN
phase: 개발
step: 10
attempt: 0
iteration: 270
updated: 2026-09-02
ctx: 62
night_iterations: 113
night_red: 1
night_retries: 0
plan: passage-api
---
# 현재 상태

**갈림길 6 은 안 닫혔다 — 리뷰 6 이 반려했다(계획 48 의 여섯 번째).**
활성 계획 `passage-api` · 설계서 `docs/design_passage-api.md` · 브랜치 `loop/passage-api` ·
기점 `6e99031`. **단위 571건 전부 초록**(맨몸 단독 실행 확인, 13.4초) · 제품 `src/` 두 파일
(`extract.py`·`indexer.py`) · 새 파일 0 · 의존성 0 · 스키마·마이그레이션 없음 ·
`docs/specs/`·`data/crawl.db` 무변경 · `.mutation-lock` **0**.
**신호는 GREEN 이다** — 반려는 러너를 빨갛게 만들지 않는다(`rules/review.md`). 떨어지는 것은
`indexer.py` 의 **한 줄**이고 개발 10 이 그것만 만진다.

## 이번 반복이 한 일 (리뷰 6)

**리뷰 5 가 못박은 재측 둘만 했다. 새 축은 안 열었다.** 패스 A(백지)를 먼저 끝내고
계획·설계를 그 뒤에 열었다. 제품 `src/` **0줄** · 테스트 0건 · 새 파일 0.

**재측 ① PASS — 태그 집합은 색인 경로에 안 닿는다.** 개발 9 가 잰 2,496건 **밖**에서
**8,844건**을 기점 `6e99031` 과 문자 단위로 댔다: 태그 31 × 모양 10(자기닫힘·대문자·감쌈·
`data-y="a > b"`·안 닫힘·짝 없는 닫는 태그·블록을 감쌈·자기 중첩) × 틀 10 = **3,100건** ·
코퍼스 8문서 얹기 **744건** · 무작위 **5,000건**(seed 48). `extract_text`·`is_noindex`
불일치 **0건**. 샘플보다 강한 근거가 따로 있다 — `_TextParser._separate` 의 본문은
`target.append(" ")` 두 줄이라 **`block` 인자를 문법적으로 안 읽는다.**

**재측 ② FAIL — 동점 키가 새 편향을 만든다. [R6-1] high.**
11행 표는 **정답이 늘 `<p>` 이고 경쟁자는 한 번도 `<p>` 가 아니다** — `<p>` 대 `<p>` 동점
행이 **0개**다. 즉 개발 9 는 «앞/뒤»·«길다/짧다» 는 짝지었지만 **새 규칙이 읽는 축은
안 짝지었다.** 그 축을 짝지은 8행(정답이 `<div>`·`<li>`·`<td>`·`<section>`·`<blockquote>`
이거나 보일러플레이트가 `<p>` 인 것)에서 채택안은 **1/8**, 합친 19행에서 **12/19** 로
**철회된 「길이」(15/19) 밑**이다. 개발 9 의 왼쪽 열 넷(6/11 · 8/11 · 11/11 · 5/11)은
내 탐침에서 글자 그대로 재현됐다 — **저울이 아니라 자를 바꾼 것이 맞다.**
뿌리는 이름표가 **최내곽 태그**라는 것이다: `<footer><p>ⓒ…</p></footer>` 도
`<aside><p>인기글</p></aside>` 도 이름표가 `p` 다(실측). **이름표는 컨테이너를 못 본다.**
게다가 `<p>` 대 `<p>` 동점은 `test_a_tie_between_two_paragraphs_goes_to_the_earlier_one`
이 «앞이 이긴다» 로 계약으로 굳혀 뒀고, 그 자리가 곧 본문 앞의 광고 문구·빵부스러기다.
**원인이 리뷰 5 와 글자 그대로 같아 medium → high**(`severity.md` 5절).

**[R6-2] low — 이름표가 새는 자리 둘**(백지 패스). `title` 은 부모가 안 끊고 early-return
하므로 `<p>김치찌개 하나<title>제목</title>` 이 `[('title', '김치찌개 하나')]` 다. 그리고
`_tag` 가 닫는 태그에서 복원되지 않아 `<article><p>본문</p>꼬리 텍스트</article>` 의 꼬리가
`('p', '꼬리 텍스트')` 다 — **동점을 이기는 방향의 거짓양성**이다. 지금은 오답을 못 만들지만
개발 10 이 같은 자리를 만지므로 함께 고친다.

## 넘긴 것 — 개발 10 의 범위는 둘로 못박혔다

**어느 후보도 19행을 다 못 맞춘다(최고 15/19).** 그래서 결론이 «다른 손잡이» 가 아니다 —
이름표는 컨테이너를 못 보고, 길이·순서는 방향이 하나뿐이고, 셋을 겹쳐도 15/19 다.
**19행 표도 내가 만든 자라 정답표가 아니다.**

㉠ 동점 키를 **「길이」로 되돌리고**(15/19) 못 맞추는 행을 표에 «못 맞춤» 으로 **분리해**
남긴 뒤 갈림길 6 을 실물까지 열어 둔다 · ㉡ 동점 키를 **아예 안 쓴다**(9/19).
**어느 쪽이든 새 손잡이는 안 만든다.** `_NON_BLOCK_TAGS` 와 `(태그, 텍스트)` 계약은
**남긴다** — 재측 ①·인라인 7모양 표가 각각 값을 증명했다. 떨어지는 것은 **한 줄**이다.

**닫는 조건은 갈림길 5 의 캡과 같다: 실물 데이터다.** `data/crawl.db` 는 2문서 × 66자,
e2e 코퍼스는 한 문장짜리 `<p>` 뿐이라 «보일러플레이트가 `<p>` 로 싸이는 빈도» 를 저장소가
못 잰다. **범위는 그대로 못박혀 있다** — 인라인 연접(`김치찌개된장찌개`) · 갈림길 5 의 캡 ·
`_SKIP_TAGS` 를 색인 경로에서 빼는 것은 **계획 48 에서 다시 열지 않는다**(`digest` 후보).
개발 10 이 닫히면 **e2e 20종 전수로 보내 DONE 으로 닫는다.**

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-02 확인)

`origin/main` 은 PR #6 으로 계획 44~47 까지 받았다. 계획 48 의 커밋들은 브랜치에만 있다.
**이번 반복도 `origin/loop/passage-api` 로 푸시했다** — 푸시는 루프의 정상 절차다
(강제 푸시만 금지).
**남은 것 (사람 결정)**: 계획 48 이 끝난 뒤 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 — 넷이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, `~/.claude/skills/loop-harness/rules/docs.md` 에는 그 예외가 없다.
   **저장소 밖이라 루프가 못 고친다.** 이번 반복이 저장소 안쪽만 일원화했다(아래 5번 참조).
4. **«러너 호출을 다른 것과 섞지 않는다» 를 문장이 아니라 훅으로 옮기는 일.**
   재발 **25회에서 안 늘었다 — 이번 반복(리뷰 6)은 0건**이다(직전 두 반복이 각각 셋이었다).
   판정 호출 2회 다 맨몸·단독·전체 스위트다. 남은 자리는
   `~/.claude/skills/loop-harness/rules/` 라 **저장소 밖이라 루프가 못 고친다.**
   **처방 후보를 리뷰의 눈으로 판정했다 — 개발 9 의 제안보다 위칸이 있다.** 개발 9 가 낸
   «판정 줄을 마지막에 다시 찍는 얇은 래퍼» 는 진단이 옳다(방아쇠가 세 번 연속 «출력이
   길다»). 그런데 그 래퍼는 잡음을 **안 줄이고** 판정만 한 번 더 찍는다. `unittest` 에
   **이미 있는 `-b`** 가 방아쇠를 뿌리에서 없앤다 — 실측으로 같은 571건이 **약 90줄 → 5줄**,
   `Ran 571 tests / OK` 그대로, 거른 테스트 0, **새 파일 0 · 새 코드 0 · 명령에 두 글자**다.
   `digest.md ## 다음 계획 후보` 에 **`[9]`** 로 등재했다(이번 스텝에서 만들지 않는다 —
   범위 밖이고 `project.md`·README 의 명령 줄을 건드리는 일이다). 단서도 함께 적었다:
   `-b` 는 테스트가 직접 낸 stdout/stderr 만 버퍼링하므로 하위 프로세스·다른 스레드가
   fd 로 직접 쓰는 로그는 샌다. 판정 명령을 단독 실행으로 강제하는 PreToolUse 검사는
   여전히 저장소 밖 몫이다.

**5번이 닫혔다 — `history_current.md` 는 418 → 204줄이다.** 세 반복 연속 «회전으로는 300줄
아래로 못 내린다» 였던 원인은 항목이 아니라 **머리에 쌓인 회전 명부 233줄**이었다.
그 명부는 `digest.md` 의 `## 완료` «아카이브 명부» 줄과 내용이 겹치고 **검사
(`ArchiveIndexTest`)가 강제하는 것도 그쪽 하나뿐**이라, 이번 반복이 저장소 안에서
일원화했다 — **지운 것은 머리말이지 반복 기록이 아니다**(항목은 그대로 append 전용·수정
삭제 금지고, 그 규약을 두 파일 머리에 적었다). **저장소 밖 룰 파일은 안 건드렸다** —
그 갈림은 위 3번으로 남아 있다.

**곁가지 — `digest.md` 도 상한 위에 있었다(207 → 200줄).** 리뷰 6 이 붙이고 세니 207
이었는데 `git show HEAD:docs/digest.md` 가 **204** 라 내 편집 전에 이미 넘어 있었다.
룰대로 오래된 완료 항목 **일곱 줄**을 지웠고, 일곱 다 `history_` 이름을 안 담은 것으로 골라
**아카이브 명부와 `ArchiveIndexTest` 는 무손상**이다(원본은 각 `history_<NNN>.md` 와
`index.md` 계획 명부에 그대로 있다). 보류·반복 실패·명부 줄은 안 건드렸다.

**사양이 사람에게 남긴 것 셋** (`docs/specs/concept.md` 의 `## 사람이 정할 것`):
속도 제한 시점(가정 — IP 당 분당 60회) · JSON 검색과 근거 문단의 분할(가정 — 쪼갠다,
**계획 46 이 그 가정을 따랐고 닫았다**) · 사양 숫자들이 초안이라는 것(90% · 500ms ·
60회/분). 첫째와 셋째는 아직 열려 있다.
