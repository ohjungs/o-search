# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**회전 명부는 `digest.md` 의 `## 완료` 절 «아카이브 명부» 줄이 정본이다.**
여기 있던 스물한 회전의 서술(233줄)은 그 줄과 내용이 겹쳤고, 검사가 강제하는 명부도
그쪽 하나뿐이라(`tests/test_docs.py` 의 `ArchiveIndexTest`) **개발 9(반복 269)가 이
자리에서 지웠다** — 회전으로는 300줄 상한을 못 맞추던 세 반복(309 → 372 → 418줄)의
원인이 이 명부였다. **지운 것은 머리말이지 반복 기록이 아니다** — 항목은 여전히
append 전용이고 수정·삭제 금지다. 각 회전의 사유는 `digest.md` 의 같은 줄에, 원문은
`history_<NNN>.md` 에 그대로 있다.

## 2026-09-06 19:30 | archive-scope-cover | 계획 0/1 | 시도0
- 한 일: 계획 64 `archive-scope-cover` 를 탐색·등재했다. 산출물은 계획서
  `docs/plan_archive-scope-cover.md` · `docs/index.md` 64번 행 · `docs/status.md`
  (GREEN · 개발 · 0/1 · 반복 371) · `docs/metrics.md`(반복 371 · 진행 1) ·
  `docs/digest.md` 의 `[5]` 항목 정정(줄 수 200 정각 유지 — 새 줄 없이 덧붙였다).
- 결과: **1~5순위 실측 0건** — 전수 맨몸 `Ran 620 tests in 15.877s` · `OK` · rc 0 ·
  린터/타입체커 설정 파일 0개 · 코드 `TODO` 0(1건은 `tests/test_indexer.py:759` 의 파서
  입력 문자열 안) · `docs/candidates.md`·`docs/patches/` 없음 · `digest ## 보류` 0건 ·
  `gh issue list --state open` 0건 rc 0 · 활성 계획 0. **6순위 `[5]`** 를 열었고 더 높은
  점수는 전부 여는 조건 미도래다.
- 결과: **착수 탐침이 기록을 뒤집었다**(`digest [7]` 열다섯 번째 적용). 저장소 밖
  `mock.patch.object` 하네스로 전수를 변이마다 다시 돌렸다 — `ARCHIVE` 축은
  M3(`$` 제거)·M4(`re.I`)·M5(`[0-9]*`)에 더해 **기록에 없던 M11(`design_history` 이름
  빼기)까지 넷이 `Ran 620 · 죽은 단언 0`** 으로 살고, 양성 대조(`^ZZZ_[0-9]+\.md$`)가
  `DocCitationTest` 1건을 죽여 배선을 증명했다. M11 은 `design_history_*.md` 가 오늘
  `docs/` 에 **33개** 실재해 도달성이 항목의 서술보다 훨씬 높다.
- 결과: 반대로 항목이 값을 뒀던 **①(`APPEND_TARGETS`)은 하네스 인공물**이었다 —
  `CITATION` 이 임포트 시점에 그 상수에서 파생되므로 소스 편집과 같게 둘을 함께 갈면
  M1 **1건** · M2 **2건** · M8(이름 오타) **3건**이 죽는다. 살아남는 M7(넓히기)은 검사를
  더 조일 뿐이고 그 처방은 거울 단언이라 행동을 안 잰다. **축 하나를 버리고 `ARCHIVE`
  에만 서기로 계획서 5절에 못박았고, 다음 탐색이 다시 열지 않도록 digest 에 적었다.**
- 결과: 처방 리터럴 아홉이 갈래를 실제로 가르는 것도 미리 쟀다 — 실물은 `CAUGHT` 3/3
  통과 · `NOT_CAUGHT` 6/6 거절(**오탐 0**)이고 M3·M4·M5·M10·M11 이 각각 다른 이름에서
  물린다. **설계 생략 — 트리거 0**(새 모듈 0 · 제품 인터페이스 무변 · 파일 2개 ·
  되돌리기가 커밋 하나 revert · 대안은 저장소가 네 번 고른 관용구라 안 갈린다).
- 결과: 범위 무접촉 — `src/`·`e2e/`·`docs/specs/`·`data/` 무변경 · `data/crawl.db`
  sha256 `85c96744…5bda18` 무변 · `README.md` 무접촉(건수는 개발 커밋에서 움직인다).
  변이는 전부 메모리라 워킹트리가 열두 판 내내 `git status --porcelain` 빈손이었다.
  러너 위반 0(누적 38 유지) · `night_iterations` 는 대화형이라 187 그대로 뒀다.
- 다음: **개발 phase 1/1** — `ArchivePatternTest` 를 `ArchiveMatchTest` 옆에 세운다
  (리터럴 표 한 벌 + 메서드 둘 · 제품 `src/` 0줄 · `README.md` 건수 줄 동반 수정).

## 2026-09-06 17:35 | archive-scope-cover | 개발 1/1 | 시도0

- 한 일: `tests/test_docs.py` **한 파일 39줄 추가**(`README.md` +1 −1). `ArchivePatternTest`
  를 `ArchiveMatchTest` 바로 위에 세웠다 — `CAUGHT` 셋(`history_001.md`·
  `plan_history_049.md`·`design_history_046.md`) · `NOT_CAUGHT` 여섯
  (`history_current.md`·`history_001.md.bak.md`·`history_.md`·`HISTORY_001.MD`·
  `digest.md`·`index.md`) · `subTest` 로 도는 메서드 둘. 실물 파일 목록이 아니라 **모양**을
  고정해서 실물 아카이브가 늘거나 줄어도 아홉은 안 움직인다.
- 검증: `rules/dev.md` 0절대로 **RED 를 이 반복에서 직접 봤다** — 테스트를 먼저 넣고 돌리니
  `AssertionError: Tuples differ: (620, 21) != (622, 21)` 로 `test_readme` 가 즉시 울었고,
  같은 커밋에서 `README.md:104` 를 620 → **622** 로 고쳤다. **계획 63 이 무접촉이라 못 본
  가드가 이번엔 설계대로 울었다.**
- 검증: 완료 기준 **9/9**. 저장소 밖 `mock.patch.object` 하네스로 변이마다 전수를 다시 돌려
  ① M3(`$` 제거) → `history_001.md.bak.md` 1건 ② M4(`re.I`) → `HISTORY_001.MD` 1건
  ③ M5(`[0-9]*`) → `history_.md` 1건 ④ M11(`design_history` 제거) → `design_history_046.md`
  1건. **어제 넷 다 생존하던 자리가 넷 다 죽는다.**
- 검증: 감지력 무회귀(기준 5)를 **추정하지 않고 쟀다** — M10(접두 확대)은 `DocCitationTest`
  1건을 그대로 죽이고 새 단언 셋을 더 죽여 **4**, M1·M2·M8(`APPEND_TARGETS`+`CITATION` 을
  함께 간 소스 편향 변이)은 어제와 같은 **1·2·3**, 계획 63 이 세운 앵커 변이 다섯
  (A1a·A1b `STEP_LINE` · A2 `PLAN_SLUG` · A3 `ITER_ROW` · A4 `STEP_ROW`)도 **각 1건** 그대로.
  M0 무변이 대조군 **0**(오탐 0) · 양성 대조 `^ZZZ_[0-9]+\.md$` 는 `CAUGHT` 셋 +
  `DocCitationTest` 로 **4**.
- 검증: 전수 맨몸 `Ran 622 tests in 15.461s` · `OK` · **rc 0**. 범위 무접촉 —
  `git diff --stat ba53783 HEAD -- src/ e2e/ docs/specs/ data/` 빈손 · `data/crawl.db`
  sha256 `85c96744…5bda18` 무변 · 재색인 0 · 제품 `src/` **0줄**. 러너 리다이렉션·파이프
  위반 0(누적 38 유지) · `night_iterations` 는 대화형이라 187 그대로.
- 다음: **테스트 phase 1/1.** 남은 자리 둘을 status 에 이름으로 적어 뒀다 — ① `ARCHIVE` 는
  `.match()` 로만 불려 `^` 가 잉여라 **M6 은 여전히 등가 변이**(재는 쪽이 아니라 지우는 쪽이
  답이고 계획 밖) ② `APPEND_TARGETS` 축은 계획서 5절이 이유를 적어 닫아 뒀다 — 다시 안 연다.

## 2026-09-06 18:10 | archive-scope-cover | 테스트 1/1 시도0

- 일: `rules/test.md` 3절 일곱 카테고리를 이번 변경(`ArchivePatternTest` + `ARCHIVE` 축)에
  대조했다. **새로 쓴 테스트 0줄** — 이 phase 의 몫은 「빠뜨린 것을 찾고 전체를 돌리는 것」
  이라 갭 탐색을 말이 아니라 **전수 25판**으로 했다(저장소 밖 `mock.patch.object` ·
  워킹트리 `git status --porcelain` 빈손 · `PYTHONDONTWRITEBYTECODE=1` +
  `PYTHONPYCACHEPREFIX=$(mktemp -d)` 동반). 고친 것은 문서 넷뿐이다.
- 결과 ①: **개발 반복(372)의 판정을 독립 재측했고 하나 빼고 그대로다** — M0 **0**(오탐 0) ·
  M3·M4·M5·M11 **각 1** · M10 **4** · M1·M2·M8 **1·2·3** · M7·M9 **0·0** ·
  계획 63 앵커 다섯(A1a·A1b·A2·A3·A4) **각 1**(무회귀) · P 양성 대조 **4**.
  **어긋난 하나는 하네스가 계획서보다 넓게 간 것이다** — M10 을 `^.*\.md$` 로 갈면 **6**
  이지만(`NOT_CAUGHT` 여섯 중 `HISTORY_001.MD` 만 `\.md$` 를 피해 산다) 계획서가 적은 M10 은
  접두를 남긴 `_[0-9]+`→`.*` 이고 그것이 **4** 다. 감지력이 는 것이 아니라 변이 정의가
  달랐고, `digest [6]` 「탐침의 배선을 먼저 의심한다」가 건수 하나로 다시 걸린 자리다.
- 결과 ②(갭 탐색): **`ARCHIVE` 를 조각마다 하나씩 갈았다**(3절 ② 경계값). 대안 `history`
  제거 **2** · 대안 `plan_history` 제거 **2** · `_` 제거 **4** · `md`→`md.*` **1** —
  **행동을 바꾸는 조각은 남김없이 물린다.** 생존은 셋인데 전부 **진짜 등가**이고 근거를 각각
  댔다: `ARCHIVE` 의 제품 소비자가 `tests/test_docs.py:238` `ARCHIVE.match(path.name)`
  **하나뿐**이라(`grep -rn ARCHIVE tests/ src/ e2e/` 확인) ① **M6 `^` 제거** 는 `re.match` 가
  이미 위치 0에 앵커해 잉여이고, 새 단언이 `search` 의미로 재도 `NOT_CAUGHT` 여섯 중
  `…_<숫자>.md` 로 **끝나는** 이름이 0개라 구분자가 없다 ② **G3 `\.`→`.`** 는 순회가
  같은 줄 237 의 `DOCS.glob("*.md")` 라 `path.name` 이 언제나 `.md` 로 끝나 **도달 불가**다
  ③ **G4 대안 순서 뒤집기** 는 `^` 뒤에서 세 대안이 서로 배타라 결과를 못 바꾼다.
  셋 다 답은 재는 것이 아니라 **지우는 것**이고 그 편집은 계획 64 밖이다(계획서 5절).
  **8점 이상 갭 0건** — 메울 것이 없어 후보로도 안 남기고 `digest [5]` 를 닫았다.
- 결과 ③: 나머지 카테고리는 해당 없음이다 — ① 부정 경로는 `NOT_CAUGHT` 여섯이 그것이고
  실제로 M3·M4·M5·M10 이 거기서 죽는다 · ③ 격리는 파일·시계·네트워크를 0회 만지는 순수
  리터럴 시험 · ④ flaky 는 `sleep`·랜덤·순서 단언 0 · ⑤ 보안은 신뢰 경계 밖의 문서 검사
  상수 · ⑥ 커버리지는 새 public 함수 0 이고 `ARCHIVE` 자체가 안 바뀌어 「옛 동작만 덮는」
  분기가 0 · ⑦ 동시 실행은 서버·큐·공유 상태 무관.
- 검증: 전수 **맨몸**(리다이렉션·파이프 0) `Ran 622 tests in 13.781s` · `OK` · **rc 0**.
  제품 `src/` **0줄** · `data/crawl.db` 무변경 · 재색인 0 · `docs/specs/` 읽기만 ·
  PR #7 무접촉(`gh` 호출 0). 러너 위반 0(누적 38 유지) · `night_iterations` 187 그대로.
- 다음: **리뷰 phase 1/1.** 볼 자리 둘을 status 에 이름으로 적어 뒀다 —
  ① `ArchivePatternTest` 가 `assertRegex`(=`search`)로 재는데 제품은 `.match()` 다.
  시험이 제품보다 **엄격한** 쪽이라 오늘 거짓 초록은 없지만 술어를 맞출지는 리뷰 판단이다
  ② 등가 셋(`^`·`\.`·대안 순서)을 **지우는** 편집을 열지 말지도 리뷰가 정한다.

## 2026-09-06 18:55 | archive-scope-cover | 리뷰 1/1 | 시도0

- 일: 백지 패스(diff 만) → 대조 패스 순으로 봤고, 앞 phase 가 「했다」고 적은 표를
  믿지 않고 **변이 열 판을 직접 다시 걸었다**(저장소 밖 `mock.patch.object`).
- 지적 **1건 · low · 신뢰도 100 · 자동 수정**: 새 클래스 docstring 이 `DocCitationTest` 를
  「**아래**」로 가리키는데 실제로는 **위**다(`tests/test_docs.py:233` vs `522`). 바로 밑의
  `ArchiveMatchTest` 가 같은 관용구를 「**위** 검사는」으로 쓴다 — **방향만 뒤집힌 복사**다.
  한 글자를 고쳤고 단언·판정은 **0줄** 무변. critical/high **0건**.
- 재측: 여덟 줄이 **한 건도 안 움직였다** — M0 **0** · M3·M4·M5·M11 **각 1** · M10 **4** ·
  P 양성 대조 **4** · M6·G3·G4 **0·0·0**. 등가 셋은 건수가 아니라 **논거**를 다시 걸었고
  셋 다 성립한다(소비자가 `ARCHIVE.match()` 하나뿐 → `^` 잉여 / `glob("*.md")` 라 `md` 앞은
  언제나 점 / `^` 뒤 세 대안이 접두어로 배타).
- 앞 phase 가 넘긴 판단 둘, **둘 다 「안 연다」**: ① `assertRegex`(=`search`)를 제품
  `.match()` 에 맞추지 않는다 — `^` 가 붙어 있는 동안 둘은 같은 함수고, 갈리는 유일한
  경우가 M6(제품 등가)라 **맞춰도 새로 죽는 변이가 0** 이다. 다만 status 의 「시험이 제품보다
  엄격하다」는 **절반만 맞았다** — `assertNotRegex` 는 엄격하지만 `assertRegex` 는 느슨한
  쪽이고, 값이 같은 이유는 엄격도가 아니라 `^` 다. ② 등가 셋을 지우는 편집은 직교 편집이라
  계획 밖이다.
- 검증: 전수 **맨몸** `Ran 622 tests in 15.828s` · `OK` · **rc 0** · `git status --porcelain`
  커밋 전까지 이번 수정과 문서 셋뿐. 제품 `src/` **0줄** · `data/crawl.db` 무변경 · 재색인 0 ·
  `docs/specs/` 읽기만 · PR #7 무접촉(`gh` 호출 0) · 러너 위반 0(누적 38) · 야간 예산 미사용.
- 다음: **e2e phase 1/1.** 새 e2e 파일은 안 만든다(계획서 5절) — 21종 전수 rc 0 과
  완료 기준 대조가 몫이다.

## 2026-09-06 20:10 | archive-scope-cover | e2e 1/1 | 시도0

- 한 일: 계획 64 `archive-scope-cover` 를 e2e 로 닫았다. e2e 21종을 **하나씩 맨몸으로**
  다시 돌렸고(파이프·리다이렉션 0회), 전수 검증을 먼저 통과시킨 뒤 완료 기준 아홉을
  메모리 하네스 열여덟 판으로 재측했다. 만진 것은 `docs/` 뿐 —
  `src/`·`tests/`·`e2e/`·`README.md`·`docs/specs/`·`data/crawl.db` 무접촉.
- 결과 ①: **21종 전부 `rc 0`.** 기준선 8축 회귀 **0** — 정확도 100.0% · `/passages` p95
  1.51ms(직전 1.52) · `/search` p95 8.95ms(직전 9.01) · ko 20/20 · en 19/20 · 매치
  14.0/11/28 · 크롤 10.25·10.26/s · JS 0 B · 최저 대비 4.87:1 · 숨은 문단 0/5.
  움직인 기준선이 없어 `docs/project.md` 는 한 줄도 안 갱신했다.
- 결과 ②: **새 e2e 파일 0개** — 「해당 없음」이 아니라 근거 셋을 실측했다(`rules/e2e.md`
  3절). 프로세스 밖 diff 빈손 · `src/` 가 `tests` 를 import 하는 줄 0건 · 새로 만들면
  전수 명령과 겹쳐 「1회만」이 깨진다.
- 결과 ③: **완료 기준 9/9.** M0 0(오탐 0) · M3·M4·M5 각 1(`test_pattern_leaves_live_docs`) ·
  M11 1(`test_pattern_catches_archive_names`) · M10 4 · M1·M2·M8 1·2·3 · 앵커 일곱 각 1 ·
  M6(등가) 0 인데 **같은 판의 양성 대조는 4** — 하네스가 살아 있는데도 0 이라는 것까지 쟀다.
- 결과 ④: **사용자 관점 — 같은 인용 한 줄을 일곱 자리에 넣었다.** 아카이브 셋
  (`history_065.md`·`design_history_099.md`·`plan_history_099.md`)은 조용하고, 아카이브처럼
  생겼지만 아닌 둘(`history_001.md.bak.md`·`history_.md`)과 살아 있는 `index.md` 는 운다.
  **양방향이라 한쪽만 재는 검사가 아니다.** 대조군이 처음 빨갰던 것은 회전 산출물이
  명부에 들기 전이어서고, `ArchiveIndexTest` 가 **이번 회전 자체를 문** 실물 발화다.
- 검증: 전수 **맨몸**(리다이렉션·파이프 0) `Ran 622 tests in 15.941s` · `OK` · **rc 0**.
  `ls e2e/*.py` 21개 ↔ README 「21종」 · README 「단위 622건」 ↔ 실제 622.
  범위 무접촉 `git diff --stat ba53783 HEAD -- src/ e2e/ docs/specs/ data/` 빈손 ·
  `data/crawl.db` sha256 무변 · PR #7 무접촉(`gh` 호출 0) · 러너 위반 0(누적 38).
- 마감: 계획서를 `plan_history_050.md` 로 아카이브(`git mv` · 내용 무변경) ·
  `index.md` 64번 `진행`→`완료` · `digest ## 완료` 한 줄(가장 오래된 완료 항목 계획 58 을
  지워 **200줄 정각** 유지) · 명부에 `history_065.md` 추가 · `metrics.md` 반복 375 ·
  계획 50/0/0(진행 0) · e2e 41.
- 회전: 이 기록을 붙이면 상한 300 을 넘어 계획 63 의 반복 기록 다섯(반복 366~370)을
  `history_065.md` 로 밀었다. 남은 것은 항목 5.
- 다음: **계획 64 DONE · 활성 계획 0.** 다음 반복은 계획 phase 다. 후보 탐색은 이 반복에서
  하지 않았다.

## 2026-09-06 14:00 | (없음) | 계획 0/0 | 시도0
- 한 일: 8개 출처를 하나도 「해당 없음」으로 넘기지 않고 전수 실측했다 — 1순위 0(전수 `OK`) ·
  2순위 도구 0개(stdlib 전용, 설정 파일 부재로 확인) · 3순위 실행 코드 0(문 것은 fixture
  문자열 하나) · 4순위 대기열 파일 부재 · 5순위 보류 0(본문이 통째로 HTML 주석) ·
  6·7·8순위는 살아 있는 것이 전부 승인 게이트(재색인·스키마·보안 판정) 뒤다.
- 판정 ①(출처 편중): 계획 60~64 **다섯 연속**이 `tests/test_docs.py` 문서 하네스이고 제품
  `src/` 는 다섯 번 다 0줄이다. `SKILL.md` 4-1 의 문턱은 연속 3건이라 여섯째를 같은 광맥에서
  뽑는 것은 **정당하지 않다**로 판정하고, 다른 출처를 먼저 뒤진 결과가 위 8줄이다.
- 판정 ②(착수 탐침 둘 — 기록된 전제가 **둘 다 거짓**): (A) README 「잘하고 있나 재는 자」 표에
  실측치는 0개이고 담긴 일곱은 전부 합격선이다. 낡을 수 있는 숫자 둘(단위 622·e2e 21종)은
  `tests/test_readme.py` 가 이미 실제 개수와 대조한다. 다만 그 합격선 일곱이 `e2e/*.py`
  상수의 **무가드 복제**라는 것이 새로 드러났다 — 근거는 서지만 이것이야말로 여섯 번째
  문서 하네스라 판정 ①에 걸려 안 열었다. (B) 「품질 기준선 회귀는 사람 눈만 잡는다」도 거짓 —
  `.git` 없는 사본에서 `_fts_query` 의 `title_ng`·`body_ng` 를 한 열로 합치니 `quality_eval`
  단독은 ko 17/20(85%)에도 「합격선 80% — 통과」 rc 0 으로 **조용했지만**, 전수는
  `FAILED (failures=4)` rc 1 로 **울었다**(`KO_BASELINE`/`EN_BASELINE`). 계획 하나가 지워졌다.
- 검증: 전수 **맨몸**(리다이렉션·파이프 0) `Ran 622 tests in 15.867s` · `OK` · **rc 0**.
  변이는 저장소 밖 사본에서만 · 실물 `src/`·`e2e/`·`data/`·`docs/specs/` **0줄** ·
  `data/crawl.db` 무변경 · 재색인 0회 · PR #7 무접촉(`gh` 호출 0) · 러너 위반 0(누적 38).
- 다음: **YELLOW 정지 — 탐색 막힘 2건째**(`metrics ## 정지 사유`). 대화형이라 묻고 기다린다.
  사람이 셋 중 하나(여섯째 문서 하네스 허용 / `판단 필요` 항목 개방 / `docs/specs/` 방향)를
  열어 주면 그 지시가 곧 근거라 탐색 없이 바로 계획서를 쓴다.
