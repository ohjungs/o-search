# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**회전 명부는 `digest.md` 의 `## 완료` 절 «아카이브 명부» 줄이 정본이다.**
여기 있던 스물한 회전의 서술(233줄)은 그 줄과 내용이 겹쳤고, 검사가 강제하는 명부도
그쪽 하나뿐이라(`tests/test_docs.py` 의 `ArchiveIndexTest`) **개발 9(반복 269)가 이
자리에서 지웠다** — 회전으로는 300줄 상한을 못 맞추던 세 반복(309 → 372 → 418줄)의
원인이 이 명부였다. **지운 것은 머리말이지 반복 기록이 아니다** — 항목은 여전히
append 전용이고 수정·삭제 금지다. 각 회전의 사유는 `digest.md` 의 같은 줄에, 원문은
`history_<NNN>.md` 에 그대로 있다.

## 반복 404 — 계획 70 `digest-strike-sync` (계획 phase · 스텝 0/1 · 시도 0)

- **판정 GREEN — 계획 70 을 열었다.** 브랜치 `loop/digest-strike-sync`(기점 `1e17a20`
  = `origin/main`) · 계획서 `docs/plan_digest-strike-sync.md` · 스텝 **1개** · 설계 없음.
  이번 반복이 만진 것은 계획서와 기록 문서뿐 — `src/`·`tests/`·`e2e/`·`README.md` **0줄**.
- **탐색 1~5순위 빈손**: 실패 테스트 0(전수 `Ran 632 tests in 15.842s` `OK` rc 0 · 맨몸) ·
  린터/타입체커 설정 **0개** · 코드 `TODO`/`FIXME`/`HACK` 1건은 파서 **입력 문자열 안**
  (`tests/test_indexer.py:779`) · `docs/candidates.md`·`docs/patches/` **부재** ·
  `digest ## 보류 (승인 대기)` **0건**(주석 한 줄) · 활성 계획 0 · `gh` 호출 0.
- **6순위 목록을 읽다가 목록 «자신» 의 부기 구멍을 골랐다.** `## 다음 계획 후보` 로
  시작하는 두 절의 목록 줄 **74개** 중 「계획 NN `slug` … 로 열었다」 포인터를 단 줄은
  **8개**, 그 여덟 중 **일곱은 취소선이 있고 하나는 없다** — `[6]`「`concept.md:<N>` 인용
  18건 중 11건은 «주소가 빈 줄이 아니다» 말고 아무도 안 잰다」 항목은 계획 68
  `spec-citation-anchor` 로 열려 **어제 `완료` 로 닫혔는데**(`index.md` 상태 칸) 아직 열린
  줄이다. **이것을 재는 단언이 0개**라는 것이 이 계획이 닫을 것이다.
- **낡은 줄이 하나 더 있고, 그것은 자가 못 문다** — `[4]`「`is_noindex` 사전 필터와 제거
  질의가 엔티티 인코딩한 `meta` 를 놓친다」 항목은 어제 계획 69 가 제품 2줄로 닫았지만
  **사람의 지시로 열려 포인터 문구가 없다**(포인터 정규식 0건). 손으로 긋고 천장을 계획서
  7절에 적었다 — 「가드가 봤다」로 적지 않는다.
- **처방을 등재 전에 네 판 돌렸다**(문자열만 조작 · 저장소 무접촉 · 워킹트리 무변경):
  **M1** 오늘 실물 → **위반 1건**(`spec-citation-anchor`) · **M2** 그 줄에 `~~` → **0** ·
  **M3** 다른 취소선 줄에서 `~~` 제거 → **1건**(재발이 다시 운다) · **M4** 그 계획 상태를
  `완료`→`진행` → **0**(진행 중은 안 문다). `rules/plan.md` 의 「기록된 답을 실행 전에 다시
  재라」를 따랐고, 처방이 실제로 재는 축과 안 재는 축이 같은 판에서 갈렸다.
- **처방 문서 인용을 이름 인용으로 적었다** — 초안이 `digest` 를 줄번호로 가리켰는데
  `DocCitationTest` 가 금지하는 표기다(append 한 번에 남의 항목을 가리킨다). 계획서·상태
  문서 전부를 절 제목 + 항목 머리 인용으로 바꿨다. **계획 42 가 세운 자에 계획 70 이 걸린
  첫 사례다.**
- **스텝 1/1 예정**: `tests/test_docs.py` 에 `candidate_lines`·`strike_gap`(`iter_gap`·
  `step_gap` 과 같은 자리) + `StrikeSyncTest`(실물)·`StrikeGapTest`(합성 갈래 넷). 실물과
  합성을 쪼개는 이유는 저장소가 두 번 산 것이다 — 계획 60 착수 6/6 생존, 계획 61 착수
  4/4 생존.
- **설계 생략** — 트리거는 「파일 3개 이상」 하나뿐이고(가드 1 + `digest` 두 줄 + `README`
  건수 줄) 갈릴 뻔한 자리 하나(닫힘을 포인터로 판정하나, 완료 슬러그 역탐색으로 판정하나)는
  착수 탐침이 실측으로 이미 골랐다.
- **안 연 것**: 후보 줄의 내용·점수(열린 58줄 — `[9]`1 `[8]`5 `[7]`7 `[6]`12 `[5]`16
  `[4]`17) · 포인터 문구를 강제하는 규약(74줄 중 66줄이 RED 가 된다 · YAGNI) ·
  `~~` 가 닫히는 위치(거울이 된다) · digest 의 다른 네 절.
- **결과**: 전수 `Ran 632 tests in 15.842s` `OK` rc 0(맨몸 1회) · `src/`·`tests/`·`e2e/`·
  `README.md` **0줄** · `docs/specs/` 무접촉 · `data/crawl.db` **무개봉** · 새 의존성·
  스키마·마이그레이션·재색인 **0** · 바깥 네트워크 접속 0 · `main` 직접 커밋 0 ·
  PR #7 무접촉.
- **다음**: 개발 phase 1/1.

## 반복 405 — 계획 70 `digest-strike-sync` (개발 phase · 스텝 1/1 · 시도 0)

- **한 일**: `tests/test_docs.py` 에 부기 가드를 심었다 — `STRIKE_POINTER`·`PLAN_ROW`·
  `CANDIDATE_HEAD`·`STRIKE_POINTER_FLOOR` 상수, `candidate_pointers`·`strike_gap` 헬퍼
  (`iter_gap`·`step_gap` 옆), `StrikeSyncTest` 2개 · `StrikeGapTest` 4개. 대조하는 두 쪽은
  `digest` 후보 줄의 취소선 ↔ `index.md` 계획 행의 **상태 칸**이다. 제품 `src/` **0줄**.
- **RED 를 먼저 봤다**: 취소선을 긋기 전 `tests.test_docs` 만 돌려
  `FAILED (failures=1)` · 메시지가 계획 그대로 «index.md `plan_spec-citation-anchor` 는
  `완료` 인데 digest 후보 줄이 `- ~~` 로 시작하지 않는다». **완료 기준 1 충족.**
- **계획서 2절의 숫자를 하나 정정했다**: 포인터 줄은 8이 아니라 **9**다(취소선 8 + 없음 1).
  후보 두 절의 `열었다` 11자리 중 둘은 「계획 49 범위 밖이라 **안** 열었다」 부정문이고,
  **백틱 슬러그를 포인터 앞에 요구하는 것**이 그 둘을 가른다. 조사는 실물에 셋 다 있다
  (`로`·`으로`·`를`). RED 건수(1)와 그 자리는 계획서와 같다.
- **취소선은 두 줄**: `[6]` 인용 앵커(계획 68) · `[4]` 엔티티 noindex(계획 69). 뒤엣것은
  **포인터 자체가 없어 가드에 안 보이던 자리**라 「→ … 로 열었다」를 함께 채웠다 —
  계획서 7절이 적어 둔 천장(「여덟이 일곱으로 주는 것은 못 막는다」)의 실물 표본이다.
- **오탐 축**: `StrikeGapTest.test_running_plan_is_not_bitten` — 상태 칸이 `진행` 이면
  안 문다. 실물에 표본이 없어(오늘 활성 계획은 자기 자신뿐) 합성에서만 밟힌다.
- **결과**: 전수 `Ran 638 tests` `OK` rc 0(맨몸 1회) · 632 → **638**(새 메서드 6개와 정확히
  같다) · `README.md:104` 632→638 · `src/`·`e2e/` **0줄** · `docs/specs/` 무접촉 ·
  `data/crawl.db` **무개봉** · 새 의존성·스키마 0 · `main` 직접 커밋 0 · PR #7 무접촉.
- **다음**: 테스트 phase 1/1 — 변이로 판정을 무력화해 본다(포인터 정규식 사망 ·
  상태 칸 비교 삭제 · 취소선 검사 삭제). 하한 못(`STRIKE_POINTER_FLOOR`)이 첫째를
  실제로 죽이는지가 초점이다.


## 반복 406 — 계획 70 `digest-strike-sync` (테스트 phase · 스텝 1/1 · 시도 0)

- **한 일**: 변이 9판을 저장소 밖 `mktemp -d` 사본에서 돌렸다(워킹트리 무변경). 계획서가
  지목한 여섯 — 취소선 떼기(M1) · 절 제목 오타(M2) · 포인터 정규식 `열었다`→`열림`(M3) ·
  상태 칸 비교 삭제(M4) · 취소선 검사 삭제(M5) · 미등재 검사 삭제(M6) — 이 **6/6 사망**.
  완료 기준 3(M1 이 `StrikeSyncTest` 를 죽인다)과 5(M2·M3 가 하한 못을 울린다)를 실물로 봤다.
- **갭 둘을 찾았고 원인이 하나였다**: 추출기의 **범위를 넓히는** 변이 둘 — 절 자르기 제거
  (M7 `in_section = True`) · 목록 줄 요구 제거(M8) — 이 **638건을 통째로 통과**했다.
  하한 못 `STRIKE_POINTER_FLOOR` 는 범위가 **좁아지는** 쪽만 막는다. 넓어지면 남의 절
  (`## 완료`)과 후보 절의 **산문 줄**이 후보로 세어져 **거짓 RED** 가 된다 — 조용한 초록의
  **반대쪽** 실패이고, 실물은 늘 맞춰져 있어 표본이 없다. 중요도 8(거짓 RED 는 루프를
  `recovery` 로 보낸다).
- **합성 단언 하나로 둘 다 물게 했다**: `StrikeGapTest.test_only_candidate_list_lines_are_counted`
  — `## 완료` 절의 포인터 목록 줄 + 후보 절의 산문 포인터 줄 + 진짜 후보 줄 하나를 넣고
  `candidate_pointers` 가 **셋 중 하나만** 세는지 본다. 재확인: M7·M8 각각 이 하나만 죽인다.
  M9(하한 못 0 + 절 제목 오타)에서도 함께 운다.
- **README 건수 가드가 즉시 울었다** — 639건이 됐는데 `README.md:104` 가 638이라 전수가
  `FAILED (failures=1)` · `test_verification_counts_match_reality`. 계획 62 가 세운 자가
  같은 반복 안에서 값을 했다. 104줄을 639로 고치고 재실행.
- **결과**: 전수 `Ran 639 tests in 15.644s` `OK` rc 0(맨몸) · 638 → **639**(새 메서드 1개와
  정확히 같다) · `src/`·`e2e/` **0줄** · `docs/specs/` 무접촉 · `data/crawl.db` **무개봉** ·
  새 의존성·스키마·마이그레이션·재색인 **0** · 바깥 네트워크 0 · `main` 직접 커밋 0 ·
  PR #7 무접촉.
- **다음**: 리뷰 phase 1/1.

## 반복 407 — 계획 70 `digest-strike-sync` (리뷰 phase · 스텝 1/1 · 시도 0)

- **리뷰: 2건 (informational 2) — 자동 수정 1 · 버림 1.** 개발 phase 복귀 없음.
- **패스 A 를 구조로 돌렸다** — `claude -p` 별도 세션에 **diff 와 수정 파일만** 주고
  계획·설계·상태 문서는 주지 않았다(`rules/review.md` 0절). 받은 지적 1건: 「`## ` 만
  절을 끊어 `###`·H1 이 `in_section` 을 안 끈다」. **80점 미만으로 버렸다** — 실물
  `digest.md` 에 `###` **0개**, `###` 는 후보 절의 **하위** 절이라 계속 세는 것이 맞고,
  후보 절 뒤 H1 재출현은 `DocHeadTest` 가 존재하는 이유인 구조 붕괴다. 형제
  `done_section` 도 `## ` 로만 끊는다 — 바꾸면 두 헬퍼가 갈린다.
- **[R70-1] 자동 수정 (낡은 주석)**: `STRIKE_POINTER_FLOOR` 주석이 「오늘 실물의 포인터
  수는 9다」인데 **실측 10**이다 — 개발 phase 가 `[4]` 줄에 포인터를 채우면서 하나
  늘었고 주석은 그 전 숫자였다. **하한 상수는 9로 두었다**(단언 무변): 값 축에 붙이면
  문구가 달라지는 날도 물지만 후보 절을 정리하는 날 거짓 RED 다.
- **주석의 수치를 말로 낮추는 대신 실측으로 샀다** — 「M7·M8 이 638건을 통째로
  통과했다」는 실제로는 `tests.test_docs` 37건만 돌린 근거였다. 가드를 심기 **전**
  버전(`HEAD~1`)을 사본에 넣고 두 변이를 각각 **전수**로 돌려 `Ran 638 · OK` 를 봤다.
  **`metrics.md` 가 다섯 번 적어 온 「적힌 수치가 실측인가」를 자기 diff 에 적용한 자리다.**
- **등재 1건 (처방 실측 동행 · 반복 331 규칙)**: 계획 7절의 천장(어미가 달라지면 여덟이
  일곱으로 주는 것은 못 막는다)을 `digest` 후보에 적었다. **처방 A**(백틱 슬러그를 통째로
  세어 엄격 포인터와 대조)는 오늘 실물에서 엄격 10 대 느슨 **31**, **오탐 21건** 이라
  그대로는 못 쓴다 — 「닫혔다 — 계획 55 `slug`」 꼬리와 서술 안 계획 인용이 다 물린다.
  **처방 B**(하한을 실측치에 붙인다)가 답이고 여는 조건까지 적었다.
- **결과**: 전수 `Ran 639 tests in 15.858s` `OK` rc 0(맨몸) · 건수 **639 무변**(주석·문서만) ·
  `src/`·`e2e/` **0줄** · `docs/specs/` 무접촉 · `data/crawl.db` **무개봉** · 새 의존성·
  스키마 0 · `main` 직접 커밋 0 · PR #7 무접촉.
- **다음**: e2e phase 1/1 (계획의 마지막 phase).

## 반복 408 — 계획 70 `digest-strike-sync` (e2e phase · 스텝 1/1 · 시도 0)

- **한 일**: 새 e2e 파일 **0개**(`rules/e2e.md` 3절 면제 — 제품 `src/` 0줄). 이 가드의
  소비자는 루프 자신이라 **전수 러너를 파이프라인으로 삼아** 저장소 밖 사본에서 4막을 밟았다.
  **A** 대조군 `Ran 639 · OK` · **B** 후보를 열고 `index.md` 를 `완료` 로 넘기며 취소선을
  잊는 **실제 편집 순서** → **`failures=1`** 이 `plan_digest-strike-sync` 를 이름으로 대고
  나머지 638은 무동요 · **C** 취소선을 그으니 `OK` · **D** 어미만 `로 연다` 로 바꾸니
  닫힌 채 **조용한 초록**.
- **부분 실행이 아니라 전수 러너로 물은 것이 이 phase 의 값이다** — 부분 실행은 「가드가
  존재한다」만 보이고 「루프가 매 반복 돌리는 명령에서 **도달한다**」는 안 보인다.
- **천장(D)을 실행으로 확인했다** — 계획 7절이 예고한 자리고 실패가 아니다. 처방 A 는
  오탐 21건이라 못 쓰고 B(하한을 실측치에 붙인다)가 `digest` 에 여는 조건과 함께 있다.
- **결과**: 전수 `Ran 639 tests in 15.912s` `OK` rc 0(맨몸) · **e2e 21종 전부 rc 0** ·
  **기준선 회귀 0**(`project.md` 수치 무갱신) · CI 설정 부재·린트 없음은 기록만 ·
  `src/`·`e2e/` **0줄** · `docs/specs/` 무접촉 · `data/crawl.db` **무개봉** · 워킹트리 무변경 ·
  `main` 직접 커밋 0 · PR #7 무접촉. **완료 기준 7/7 → DONE.**
- **다음**: 계획 아카이브(`plan_history_056.md`) 완료 · 활성 계획 0 · 계획 탐색.

## 반복 409 — 계획 71 `index-e2e-verdict` (계획 phase · 스텝 0/1 · 시도 0)

- **한 일**: 야간 시작. 탐색 **1~5순위 빈손**을 명령으로 확인(전수 639 `OK` · 린트 없음 ·
  코드 `TODO` 1건은 `tests/test_indexer.py` 의 HTML 픽스처 **문자열 안** · `candidates.md`
  없음 · `digest ## 보류` 빈 절). **6순위 상위는 전부 여는 조건 미충족**이라(실물 코퍼스 ·
  재색인 = 야간 금지 · *"사람이 정할 갈림길"*) 직전 반복이 `## 다음` 에 남긴 입력을 골랐다 —
  `index.md` 의 `plan_noindex-entity-prefilter` 행이 **e2e 칸에 판정 대신 날짜**를 담았고
  그 칸을 재는 자가 0개다(스텝 칸은 계획 60, 상태 칸은 계획 70 이 이미 잰다).
- **착수 탐침이 설계를 골랐다**: 계획 행 47개 파싱 → 날짜뿐 **1** · `통과` 없는 행 **7**.
  뒤의 여섯은 `없음(…)`·`**새 e2e 0개**(…)` 로 전부 정당해 **「어휘 요구」안은 오탐 6건**이다.
  남는 안은 «날짜를 거절한다» 하나 — 저울질이 아니라 측정이 골라서 **설계 생략**.
- **계획서가 자기 저장소의 자에 걸렸다**: 초안이 `index.md` 를 줄번호로 **5자리** 인용해
  `DocCitationTest` RED(계획 70 에 이은 두 번째). 이름 인용으로 바꾸고, 가드가 못 무는
  산문 속 `57행`·`26~31행` 도 슬러그로 갈았다 — 행이 늘면 썩는 같은 결함이라서다.
- **결과**: 계획서 `docs/plan_index-e2e-verdict.md` · `index.md` 등재 1행(`진행` `0/1`) ·
  브랜치 `loop/index-e2e-verdict`(기점 `main`) · 전수 `Ran 639` `OK` rc 0 ·
  `src/`·`e2e/` **0줄** · `docs/specs/` 무접촉 · `data/crawl.db` **무개봉**.
- **회전**: 이 append 로 `history_current.md` 가 **315줄**이 되어 상한 300 을 넘겨 반복
  400~403 을 `docs/history_071.md` 로 밀어냈다(남은 192줄 · 명부에 이름 등재).
  **`digest.md` 200줄 상한은 안 맞췄다(214줄)** — 지울 수 있는 완료 항목이 여덟 줄뿐이라
  산술이 안 되고, 무인 모드는 데이터 삭제를 안 한다. 사람 몫으로 보고서에 올린다.
- **다음**: 개발 phase 스텝 1/1 (TDD — 먼저 빨갛게).

### 반복 410 — 개발 71 `index-e2e-verdict` 스텝 1/1 (GREEN)

- **먼저 빨갛게 봤다**: 검사만 심은 전수에서 `VerdictSyncTest` 가 실물 `index.md` 의
  `plan_noindex-entity-prefilter` 를 **슬러그 이름으로 대고** 실패했다(M1). 칸을
  `docs/e2e/noindex-entity-prefilter/result.md` 의 판정으로 고치니 조용해졌다(M2).
- **M4(오탐 축)를 실물이 증명했다**: `없음(…)`·`**새 e2e 0개**(…)` 여섯 행이 그대로인데
  성한 트리가 초록이다 — 어휘를 요구하지 않는 자라는 것이 여기서 사졌다.
- **계획서에 없던 판정 하나를 더했다**: 날짜만 보면 **열이 사라진 행을 정규식이 조용히
  건너뛴다**. 계획서 8절이 「침묵 말고 신고」라 적어 둔 답을 행 머리 세기 3줄로 심었다.
- **계획서에 없던 파일 하나**: `README.md` 건수 639→647. 테스트를 더하면 따라오는 부기이고
  선례가 셋이다(계획 68·69·70 커밋 전부 같은 줄을 동반 갱신).
- **결과**: 전수 `Ran 647` `OK` rc 0(639 + 새 검사 8) · `src/`·`e2e/` **0줄** ·
  `docs/specs/` 무접촉 · `data/crawl.db` **무개봉**.
- **다음**: 테스트 phase — 계획서 4절 M3·M5·M6 + 범위를 넓히는 변이.

### 반복 411 — 테스트 71 `index-e2e-verdict` 스텝 1/1 (GREEN)

- **변이 6판을 저장소 밖 사본에서 팠다**: V0 대조군 `Ran 647` `OK` · M3 재발 죽음(다른 행에
  날짜를 심으니 그 슬러그를 댄다) · M5 판정 무력화 죽음(2) · M6 추출기 무력화 죽음(10 —
  하한 못과 열 모양 신고가 함께 문다) · 넓힘 ② `fullmatch`→`search` 죽음.
- **넓힘 ① 이 살아남았다**: `완료` 행 가드를 지워도 647건이 그대로 통과했다. 합성 표의
  진행 행 e2e 칸이 `—` 라 **날짜가 아니어서 안 물렸고**, 그 단언은 아무것도 재지 않고
  있었다(`test.md` 3절 ⑥). 계획 70 이 밟은 「넓어지는 변이는 조용히 산다」와 같은 자리다.
- **단언 하나로 닫았다**: 진행 행의 e2e 칸을 `2026-09-07` 로 바꿨더니 같은 변이가 죽는다.
  **판정 로직은 한 줄도 안 고쳤다** — 단언만 조였다.
- **갭 탐색 6종 훑음, 새로 연 것 0개**. 빈 칸·`미정` 은 계획서 8절이 천장으로 등재했고
  여는 조건(날짜 아닌 비판정이 실물에 나타나는 날)이 안 찼다.
- **결과**: 전수 `Ran 647` `OK` rc 0 · `src/`·`e2e/` **0줄** · `data/crawl.db` **무개봉**.
- **다음**: 리뷰 phase.

### 반복 412 — 리뷰 71 `index-e2e-verdict` 스텝 1/1 (GREEN)

- **백지 패스를 별도 세션에 넘겼다**(`review.md` 0절 구조). diff 와 수정 파일만 주고
  계획서·설계·상태 문서는 주지 않았다. 셋을 주웠고 **전부 실측으로 재확인**했다.
- **[R71-1] 자동 수정**: 합성 오탐 축이 `—` 를 「정당한 판정」으로 못박았다. `—` 는
  계획서 8절이 **천장**으로 등재한 「못 무는 것」이라, 그 목록에 있으면 천장을 옳다고
  새기는 것이 된다. 케이스 하나 삭제 · **판정 로직 무변**.
- **[R71-2] 자동 수정**: 칸 구분 공백을 하나만 더 쳐도 「표의 열 모양이 바뀌었다」가
  뜬다(실측). 원인을 단정하지 않는 문구로 갈았다 · 단언 무변.
- **[R71-3] 등재**: e2e **앞에** 열을 끼우면 개수가 맞아 통과하고 새 칸을 판정으로
  읽는다(실측). 조용한 초록이 아니라 **시끄러운 오귀속**이라 8점 미만 — 처방(헤더
  다섯째 이름 단언 한 줄)과 여는 조건(표에 열을 더하는 날)을 `digest` 후보에 적었다.
- **결과**: 전수 `Ran 647` `OK` rc 0 · `src/`·`e2e/` **0줄** · `data/crawl.db` **무개봉**.
- **다음**: e2e phase — 계획서 7절, 전수 러너를 파이프라인으로 삼는다.


### 반복 413 — e2e 71 `index-e2e-verdict` 스텝 1/1 (DONE)

- **계획서 7절을 저장소 밖 사본에서 그대로 밟았다**: ① 이 계획 행을 `진행`→`완료` 로
  넘기며 e2e 칸에 판정 대신 날짜를 적으니 **`failures=1`** · 메시지가 슬러그와 처방을
  대고 나머지 646은 무동요 · ② 판정을 옮겨 적으니 `Ran 647` `OK` rc 0 · ③ `없음(…)`·
  `**새 e2e 0개**(…)` 여섯 행은 두 경우 다 안 물렸다. **새 e2e 파일 0개**(3절 면제 —
  제품 `src/` 0줄이라 관통시킬 동작이 없어 **전수 러너**를 파이프라인으로 삼았다).
- **사건을 낸 자리에서 사건을 되샀다** — ① 은 합성이 아니라 계획 1절이 실측한 위반
  (`plan_noindex-entity-prefilter`)이 생긴 편집 그대로이고, 오늘은 이 계획 자신의 행이다.
- **전체 검증**: 전수 `Ran 647 tests in 15.955s` `OK` rc 0(맨몸) · e2e 스크립트 **21/21
  rc 0** · 기준선 다섯 축 회귀 0(ko 20/20 · en 19/20 · 문단 100.0% p95 1.52ms · 검색 p95
  8.83ms · 크롤 10.22/s · JS 0 B 최저 대비 4.87:1) · `project.md` 수치 무갱신.
- **완료 기준 M0~M6 충족** — M5 `failures=2` · M6 `failures=10`(하한 못 45 와 열 모양
  신고가 함께 문다). 실물 계획 행 **48** · `완료` 47 · 날짜뿐인 칸 **0** · `verdict_gap` `None`.
- **천장은 안 열었다**(계획 8절 · [R71-3]) — 여는 조건(날짜 아닌 비판정의 출현 / 표에 열을
  더하는 날)이 둘 다 안 찼다.
- **결과**: `src/`·`e2e/` **0줄** · `docs/specs/` 무접촉 · `data/crawl.db` **무개봉** ·
  바깥 네트워크 0 · `main` 직접 커밋 0.
- **다음**: 계획 71 **DONE**. 아카이브 후 새 계획 탐색.
- **사람 몫**: `digest.md` **216줄**(상한 200) — 지울 항목이 여덟 줄뿐이라 산술이 안 맞고
  무인 모드는 삭제하지 않는다. 야간 보고서에 올렸다.

### 반복 414 — 계획 탐색 (YELLOW · 근거 0)

- **8개 출처를 전부 확인했고 열 수 있는 근거가 0 이다**: 실패 테스트 0(전수 647 OK ·
  e2e 21/21) · 린트/타입 수단 없음 · 코드 `TODO` 0(잡힌 1건은 `tests/test_indexer.py:779`
  의 **fixture 문자열**) · `candidates.md` 부재 · 보류 0 · 반복 실패 잔여 1건은 룰 문장으로
  닫힌 절차 항목 · `gh issue list` 빈손.
- **후보는 있지만 전부 여는 조건이 안 찼다** — `[9]` p95 배분(실물 분포가 먼저) · `[7]`
  비ASCII robots(도달 불가) · `[7]` 커서 페이지네이션(**공개 API 갈림길 — 사람 몫**) ·
  `[6]` [R71-3](표에 열을 더하는 날) · `[5]` `_IMPLIED_END` 101건(표를 고치는 날) ·
  `[5]` 예산 500 리터럴(사양 숫자가 움직이는 날). `[8]` 숨은 텍스트는 계획 51 이 닫았다.
- **`discover.md` 2절대로 계획을 만들지 않았다** — 조건이 안 찬 후보를 여는 것은 근거가
  아니라 취향이고, 3절이 밤에 금지한 부류다. 무인 모드라 질문 없이 **보류 후 정지**.
- **결과**: 커밋한 것은 문서뿐 · `src/`·`e2e/` **0줄** · `data/crawl.db` **무개봉** ·
  `main` 직접 커밋 0.
- **다음**: 야간 종료. 사람 몫 셋 — `digest.md` 216줄 정리 · 다음 계획 방향 ·
  `loop/index-e2e-verdict` 병합 여부. 보고서 `docs/reports/night_2026-09-07.md`.

### 반복 415 — 계획 72 `digest-rotate-guard` 수립 (GREEN · 사용자 지시)

- **근거는 측정값**: `digest.md` 216줄 > 상한 200(`rules/docs.md` 3절). 반복 414 가
  「사람 결정 대기」로 올린 첫째를 사용자가 결정했다 — 닫힌 기록에 한한 삭제 위임.
- **처방 넷 중 셋이 실측으로 떨어졌다.** A(느슨한 백틱 세기)는 계획 70 이 이미 반증.
  **B(하한을 실측치에 붙인다)는 오늘 반증** — 후보 포인터 **10개가 10개 다 취소선**이라
  삭제 정책이 모집단을 영구히 0 으로 만든다. 「실측치」가 0 이면 하한 0 이고, 그것은
  그 못이 막으라고 세워진 조용한 초록 자체다. B 는 후보 절이 append-only 라는 전제
  위에 있었고 그 전제가 오늘 틀렸다. **C(역방향 — `index.md` 완료 슬러그가 후보 절에
  없어야)는 오늘 처음 재서 반증** — 회전 뒤에도 오탐 4건이고 그중 `indexer` 는 제품
  모듈 이름과 계획 슬러그가 같은 글자다.
- **시뮬레이션이 사이 반복의 빨간 저장소를 미리 보여줬다**: 취소선 18줄을 지운 사본에서
  전수 647 중 **정확히 1건**(`test_pointer_extractor_still_bites`, `0 not >= 9`)만 실패.
  다른 축은 전부 조용해 회전이 밟는 자리가 하나임이 확정됐다. 사본은 `/tmp` 라
  `docs/digest.md` 는 216줄 그대로 원복했다.
- **덤**: 후보 절의 「화면에 안 보이는 텍스트가 «근거 문단» 으로」 항목이 취소선 없이
  살아 있는데 `plan_hidden-passage` 는 `완료` 다 — 문구가 「…가 앞 절반을 연다」라
  엄격 포인터가 안 문다. 계획 70 이 잠복이라 등재한 드리프트의 **실물 표본**이다.
  다만 뒷 절반은 진짜 열려 있어 닫힌 기록이 아니라 이 계획은 손대지 않는다.
- **다음**: 설계 phase — 남은 처방 D 의 **값**을 가른다. 58 에 붙이는 것은 오늘 사고의
  반복이라 그 자체가 갈림길이다.
