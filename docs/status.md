---
signal: GREEN
phase: 개발
step: 1
attempt: 0
iteration: 217
updated: 2026-09-01
ctx: 60
night_iterations: 77
night_red: 0
night_retries: 0
plan: docs/plan_focus-contrast.md
---

# 현재 상태

**계획 43 `focus-contrast` 개발 1/1 완료 — 커밋 `fe2b994` 푸시됨. 다음은 테스트 phase.**
브랜치 `loop/focus-contrast` · 설계서 `docs/design_focus-contrast.md` ·
계획서 `docs/plan_focus-contrast.md`.
이 스텝의 diff 는 계약대로 **세 파일뿐**이다(`+42 -26`).

## 방금 것 (2026-09-01 · 개발 1/1)

**설계 `## 계약` 절을 그대로 구현했다.** 한 커밋 안에서 ①→② 순서로 갔다.

### ① 축과 뒤집은 강제를 먼저 넣어 RED 를 눈으로 봤다

`MIN_CONTRAST_NONTEXT = 3.0`(WCAG 2.1 SC 1.4.11) · `NONTEXT_PAIRS` ·
`NO_PAIR` 를 넣고 `startswith("--fg-")` 두 줄을 `paired`/`missing` 로 교체했다.
그 자리에서 **rc 1 · `라이트 --focus on --bg-page 2.80:1 / 기준 3.0 **미달**`** 이 떴다.
**검사기가 낸 이 RED 가 이 스텝의 TDD 다** — 가르면 첫 커밋이 RED 로 남으므로 안 갈랐다.

### ② 그 다음 색 한 값

라이트 `--focus` `#f97316` → `#ea580c`. `design_check` **rc 0**, `[3]` 절에 비텍스트
**2행**(라이트 **3.56** · 다크 **11.14**) — 설계가 예측한 숫자와 소수점까지 같다.
두 자가 한 절에 섞이므로 출력 행에 `/ 기준 4.5`·`/ 기준 3.0` 을 적었다.

### 변이 4종 — 셋은 설계대로, **M4 는 설계 예측이 틀렸고 계약이 옳았다**

사본(스크래치패드)에서 돌렸고 심기 전에 원문 존재를 먼저 단언했다(`digest [8]`).

| | 심은 것 | rc | |
|---|---|---|---|
| M1 | 라이트 `--focus` 를 `#f97316` 로 되돌린다 | **1** | 이 스텝이 존재하는 이유 |
| M2 | `NONTEXT_PAIRS` 를 비운다 | **2** | `--focus` 가 `missing` 으로 떨어진다 |
| M3 | 다크 `--focus` 만 미달로 | **1** | 두 맵을 다 본다(계획 13 의 그 구멍) |
| M4 | `NO_PAIR` 에 `--focus` **만** 적는다 | **1** | 설계는 **rc 0** 을 예상했다 |
| M4b | 짝에서 빼고 **+** `NO_PAIR` 에 사유 | **0** | 진짜 탈출구는 **편집 두 번** |

**M4 의 어긋남은 계약이 이긴 자리다.** 설계 `## 스텝` 절은 "`NO_PAIR` 에 `--focus` 를
넣으면 축이 사라지고 rc 0" 이라고 적었지만, 같은 문서의 `## 계약` 절대로 짜면
`NO_PAIR` 는 **커버리지 강제에서만 빼 주고 재는 루프는 안 건드린다**. 그래서 짝에
남아 있는 한 계속 잰다. **계약이 자기 검증 예측보다 강했고, 결과는 설계 의도보다 세다** —
축을 지우려면 두 곳을 고쳐야 하고 그중 하나가 사유 문자열을 강제한다. 문은 더 무겁다.

## 다음

- **테스트 phase.** 개발이 끝났고 단위는 계약대로 **0건 추가**(469건 무변)다 —
  테스트 phase 가 그 결정을 다시 볼 자리다.
- `--line` 을 UI 컴포넌트 경계로 볼지(1.34:1)는 **범위 밖**이고 `digest` 후보로 넘기는
  것은 **DONE 반복이 한다**. 지금 넣으면 열린 항목 48·200줄 상한을 근거 없이 흔든다.

## 판단 필요 — PR 은 여전히 안 연다

**계획 42·43 둘 다 `origin/main` 기점이 아니다.** PR #4(계획 38~41)가 열려 있고
그 위에 42 가, 42 위에 43 이 쌓인다. 지금 PR 을 열면 PR #3 과 같은 사고가 난다.

**사람이 할 일: PR #4 병합 → 계획 42 체리픽 PR → 계획 43 체리픽 PR.**

## 한도 (매 반복 확인)

- `main` 직접 커밋 금지 · `--no-verify` 금지 · 외부 네트워크 금지 ·
  `docs/specs/` 읽기만 · `data/crawl.db` 무변경(sha256 `85c96744…5bda18` 확인) ·
  의존성 추가 금지(stdlib 만 — 이 반복도 stdlib 만 썼다).
- **검증은 맨몸으로.** (`digest ## 반복 실패` 의 해당 항목 **14회** — **이 반복이 ⑭이고
  네 반복 연속이다**: 단위 확인 첫 실행에 `2>&1 | tail -5` 를 썼다. 즉시 `export
  PYTHONPATH=src` 를 별도 명령으로 준 뒤 맨몸으로 재실행해 **469건 OK 11.694초**를
  받았고, 커밋 전 확인은 그 맨몸 실행이다. `design_check`·`pagination_ui_e2e` 는
  맨몸이었다.) **네 반복 연속이면 이것은 규율 문제가 아니라 룰 배치 문제다** —
  "맨몸으로" 가 `한도` 절에만 있고 검증을 적는 자리에는 없다.
- `docs/digest.md` 200줄 상한 · 열린 항목 **48**(이 반복은 안 건드렸다) ·
  `history_current.md` 300줄 상한 — 지금 **165줄**이라 여유 있다.
