# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**반복 97~122 는 `history_006.md`, 123~128(계획 020 `deadline` 전체)은
`history_007.md`, 계획 21~24(짧은 경로 3건 + 보류 패치 소진, 2026-08-29 14:20~17:40)는
`history_008.md` 로 밀려났다** (2026-08-29 회전 3회).
**계획 25~29 는 `history_009.md`, 계획 30·31(짧은 경로) + 반복 152 탐색 +
계획 33 `clock-injection` 의 계획·설계·개발 1~4 는 `history_010.md` 로 밀려났다**
(2026-08-30 회전, 393줄 → 139줄. 상한 300줄을 넘겨 놓고 두 반복을 갔다).
**계획 33 의 마지막 네 반복(개발 5 · 테스트 1 · 리뷰 1 · e2e 1)은 `history_011.md`**
(2026-08-30 회전, 322줄 → 210줄. 상한을 넘긴 채 반복 두 번을 또 갔다 — 스텝 3 이
"미결(문서 위생)" 로 넘긴 것을 스텝 4 가 닫았다).
계획 33 의 전 phase 서술은 `plan_history_020.md` 의 `## 기록` 에 그대로 있다.
**계획 34 `graceful-interrupt` 의 앞 여덟 반복(계획 1 · 설계 1 · 개발 1 세 번 · 2 · 3 · 4)은
`history_012.md`** (2026-08-30 회전, 300줄 → 85줄. **상한에 정확히 닿아 넘기기 전에 돌렸다** —
앞 두 번은 넘긴 채 반복을 더 갔다).
**계획 34 의 마지막 세 반복(테스트 1 · 리뷰 1 · e2e 1)은 `history_013.md`**
(2026-08-31 회전, 307줄 → 244줄. 상한 300 을 넘긴 그 반복에서 바로 돌렸다 —
`digest ## 반복 실패` 가 지적한 "회전이 두 반복 늦었다" 를 안 되풀이한다).
**계획 35 `deadline-stop` 전체 여섯 반복(계획 · 개발 1 · 테스트 2 · 리뷰 · e2e)은
`history_014.md`** (2026-08-31 회전, 336줄 → 188줄. 상한 300 을 넘긴 채 테스트 반복
하나를 더 갔고 **리뷰가 그것을 잡았다** — 회전은 phase 룰이 아니라 문서 위생이라
`status.md` 의 "집안일 미결" 로만 있었다. 완료된 계획 하나를 통째로 미는 경계로 잘랐다).

**계획 36 `signal-budget-cover` 전체 두 반복과 계획 37 `indexer-interrupt` 전체 여섯 반복은
`history_015.md`** (2026-08-31 회전, 318줄 → 96줄. **계획 38 리뷰가 잡아서 돌렸다** —
테스트 스텝이 305줄인 것을 알고 `status.md` 에 적어 둔 채 그대로 갔다. 393·322·336·318,
네 번째 지각이다. 완료된 계획 둘을 통째로 미는 경계로 잘랐다).

**계획 38 `digest-rotate` 전체 다섯 반복과 계획 39 `indexer-lock` 전체 다섯 반복은
`history_016.md`** (2026-09-01 회전, 299줄 → 145줄. **계획 40 리뷰가 제때 돌렸다** —
테스트 반복이 "다음 반복이 회전한다" 고 적어 둔 그대로다. 393·322·336·318 은 지각이었고
이번은 아니다. 완료된 계획 둘을 통째로 미는 경계로 잘랐다 — 계획 40 을 여는 반복 198 부터 남는다).

**계획 40 `exit-code-contract` 전체 여섯 반복(198~203)은 `history_017.md`** (2026-09-01
회전, 291줄 → 새 항목을 넣기 전에 돌렸다. **테스트 phase 가 자기 손으로 돌린 첫 회전이다** —
205 가 "다음 반복이 회전 자리다" 라고 적어 둔 그대로다. 완료된 계획 하나를 통째로 미는 경계).

**계획 41 `crawl-db-guard` 전체 다섯 반복(204~208)은 `history_018.md`** (2026-09-01
회전, 364줄 → 221줄. **e2e phase 가 마감 작업으로 돌렸다** — 300줄 상한을 64줄 넘긴
채로 개발 2/2 가 지나갔다. 완료된 계획 하나를 통째로 미는 경계. 이 회전이
`DocCitationTest` 의 아카이브 제외 규칙을 **실물로 시험한 첫 사례**다 — 계획 42
7절 반증 신호 ②가 예고한 그 자리이고, 새 이름이 규칙과 맞아 스위트는 초록이다).

**계획 42 `docs-citation-guard` 전체 여섯 반복(209~214)은 `history_019.md`** (2026-09-01
회전, 292줄 → **설계 반복이 자기 항목을 넣기 전에 돌렸다** — 넣었으면 300줄을 넘겼다.
계획 41 회전이 64줄 지각한 바로 다음 회전이고, 이번엔 앞에서 잘랐다. 완료된 계획 하나를
통째로 미는 경계).

**계획 43 `focus-contrast` 의 앞 여섯 반복(215~220)은 `history_020.md`** (2026-09-01
회전, 293줄 → 70줄. **e2e 반복이 자기 항목을 넣기 전에 돌렸다** — 넣었으면 311줄이다.
계획 42 회전과 같은 자리에서 같은 순서로 돌았고, 두 번 연속 지각이 없다. 완료된 계획
하나를 통째로 미는 경계).

**계획 43 의 마지막 반복(e2e 1/1 · 반복 221)은 `history_021.md`** (2026-09-01 회전,
293줄 → 269줄. **계획 44 e2e 반복이 자기 항목을 넣기 전에 돌렸다** — 넣었으면 300줄에
닿는다. 완료된 계획 하나를 통째로 미는 경계이고, 계획 43 은 이 회전으로 다 밀렸다).

---

**계획 44 `focus-ring-presence` 전체 여섯 반복(계획 · 설계 · 개발 · 테스트 · 리뷰 · e2e)은
`history_022.md`** (2026-09-01 회전, 306줄 → 107줄. **상한 300 을 넘긴 그 반복에서
바로 돌렸다** — 계획 phase 가 자기 손으로 돌린 첫 회전이다. `digest.md` 압축 줄은 200줄
상한 때문에 새 줄을 더하지 않고 **기존 회전 줄에 이어 붙였다**(`## 완료` 의 2026-09-01
`history_021.md` 줄) — 완료 항목은 하나도 안 지웠으므로 **새 색인 구멍 0** 이다).

**계획 45 `docs-record-guard` 의 앞 네 반복(계획 · 설계 · 개발 1/2 · 개발 2/2)은
`history_023.md`** (2026-09-02 회전, 318줄 → 140줄. **테스트 반복이 자기 항목을 넣고
상한을 넘긴 것을 보고 그 자리에서 돌렸다.** 진행 중인 계획을 가르는 경계라 완료 계획을
통째로 미는 평소 경계와 다르다 — 계획 33·34 회전과 같은 꼴이고, 나머지 서술은
`plan_docs-record-guard.md` 와 이 계획이 DONE 될 때 남길 아카이브가 갖는다.
**이번 회전은 새 의무를 하나 지고 돈 첫 회전이다** — 계획 45 자신이 만든
`ArchiveIndexTest` 때문에 `digest.md` 의 아카이브 명부에 `history_023.md` 를 더하지
않으면 스위트가 RED 다. 규약이 문서에만 있던 어제와 달리 오늘은 검사가 강제한다).

**계획 45 `docs-record-guard` 의 마지막 세 반복(테스트 · 리뷰 · e2e)은
`history_024.md`** (2026-09-02 회전, 303줄 → 155줄. **직전 계획 반복이 "회전 미달"
이라 적었는데 실측은 303줄로 이미 상한을 넘겨 있었다** — 그 반복이 자기 항목을 붙인 뒤
다시 안 셌다. 다음부터 회전 판정은 **항목을 붙인 뒤** `wc -l` 로 센다. 이 회전으로
계획 45 는 통째로 밀렸고(앞 넷은 `history_023.md`), 완료된 계획 하나를 통째로 미는
평소 경계로 돌아왔다. 명부에 `history_024.md` 를 더했다 — `ArchiveIndexTest` 가
강제하는 두 번째 회전이다. `digest.md` 는 200줄 상한이라 새 줄을 안 만들고
`## 완료` 의 2026-09-01 회전 줄에 이어 붙였다 — **완료 항목을 안 지웠으므로 새 색인 구멍 0**).
**계획 46 `json-contract` 의 계획 phase 반복은 `history_025.md`** (2026-09-02 회전,
317줄 → 257줄. **직전 회전이 적어 둔 "항목을 붙인 뒤 센다" 를 처음 그대로 따랐다** —
개발 2/3 항목을 append 한 뒤 `wc -l` 로 재서 317줄을 보고 그 자리에서 돌렸다.
진행 중인 계획을 가르는 경계라 설계·개발 항목은 남는다. 명부에 이름을 더했고
`digest.md` 는 같은 회전 줄에 이어 붙였다 — **새 색인 구멍 0**).


## 2026-09-02 02:05 | json-contract | 설계 1/1 | 시도0

- 한 일: **설계서 `docs/design_json-contract.md` 작성** — 계획이 넘긴 갈림길 넷을 전부
  닫았다. 대안은 `rules/design.md` 3-1 대로 **서로 다른 출발점**에서 하나씩 냈다.
  제품 `src/` **0줄** · `tests/` **0줄** (설계 phase 는 코드를 안 쓴다).
- **탐침이 갈림길 D 의 답을 실측으로 줬다.** `indexer.search()` 를 여섯 DB 상태에 직접
  걸었다(임시 디렉터리, `data/crawl.db` 는 읽지도 않았다). **오늘의 `200 + []` 는 네
  상태를 덮는다** — 크롤만 함 · 빈 DB · 색인 0건 · 무매치. 앞 둘은 "색인이 없다"이고 뒤
  둘은 정상이다. 가르는 술어는 `docs` 테이블의 **존재**이고 `indexer.search` 는 그것을
  `indexer.py:183-185` 에서 **이미 판별하고서 `[]` 로 합친다.**
- **D 는 200 유지로 닫았다.** 사양 디자인 5 는 *"503 = 색인이 **없다**"* 라 적었지
  *"색인 결과가 0건"* 이라 안 적었고, crawl → index → serve 순서상 미색인 창은 고장이
  아니라 정상 상태다. 여는 세 안이 전부 비쌌다 — 정공법은 `indexer.py` 와 **모든**
  호출부를 건드려 계획 40 의 CLI 계약을 다시 열고, 되돌리기 우선안은 같은 판정을 두
  자리에 두는 것이라 `serve.py:37` 주석(*"막는 자리가 둘이면 한쪽만 고쳐진다"*)이
  정확히 금지한 패턴이다. 대신 **네 상태를 덮는다는 사실을 `TestUnindexedDb` 독스트링에
  못박고** 열 조건(계획 47 이 `indexer` 를 어차피 건드릴 때)을 설계 4절에 적었다.
- **두 번째 탐침이 계획이 못 본 구멍을 찾았다.** 오늘 `serve` 의 500 을 재는 단언은
  `FileNotFoundError` **하나**에만 붙어 있다. 드리프트를 HTTP 로 재는 단언은 0건이고
  (`grep -rn "StaleIndexError" tests/test_serve.py` → 0건), 프로그래밍 오류가 500 이라는
  단언도 0건이다(`test_hostile_query_strings_do_not_500` 은 이름과 달리 **200 을 못박는**
  단언이라 `except Exception` 절에 아예 안 닿는다). **스텝 1 이 `TestMissingDb` 를 503 으로
  뒤집으면 JSON 경로의 500 커버리지가 0건이 된다** — 계획 42~45 의 교훈 ①(*"데이터가
  초록일 때 자기를 못 잰다"*)의 이번 판 형태다. 새 503 이 초록이라 사라진 500 이 안 보인다.
- 갈림길 결론 — **A** 본문 최상위 `"version": 1`(정수)를 `_send` **한 곳**에서(다섯 응답이
  전부 그곳을 지난다 · 화면은 `_send_html` 이라 *"안 붙인다"* 가 단언이 아니라 **구조로**
  성립) · **B** `except (FileNotFoundError, StaleIndexError)` 절 한 줄을 `except Exception`
  **앞**에(순서가 계약이다) · **C** **화면도 503**(500 은 재시도 안 하는 값, 503 은 하는 값
  — 색인을 다시 돌리면 낫는 상태에 500 은 틀린 신호다. 상태 코드는 사람이 아니라 인프라가
  읽는다) · **D** 200 유지.
- **기각한 안의 값도 실측했다.** A 의 `/v1/search` 안은 `/search` 직접 호출이 **e2e 4개
  파일 14곳**이고 그중 셋이 `serve.main` 시작 줄을 `line.split("/search")[0]` 로 파싱해
  경로를 바꾸면 통째로 깨진다 — 계획서 리스크 절이 예고한 그대로다. B 의 정공법 안은
  `FileNotFoundError` 가 **stdlib 예외라 상속 계층을 못 바꾼다**는 데서 막힌다.
- **변이 표 14줄** — 교훈 ②(*"넓어지는 변이는 잡히고 좁아지는 변이는 안 잡히는 비대칭"*)를
  반영해 **좁히는 변이를 절반 넣었다**. 핵심 셋(M3 드리프트 누락 · M5 `Exception`→503 확대 ·
  M11 화면에도 버전)은 **오늘 재는 것이 0건**이다. 안 잡을 둘(M15 `ORDER BY` 흔들기 —
  두 경로가 같이 움직여 안 죽는다, 랭킹은 `quality_eval.py` 몫 / M16 탐침 행 누출 — 이미
  `test_count_does_not_leak_the_probe_row` 가 잡는다)은 교훈 ③대로 **`## 범위 밖`** 에
  사유와 함께 적었다.
- **성능 계약은 새 스크립트를 안 만든다** — `e2e/perf_search.py` 가 이미 `BUDGET_MS = 300`
  으로 재고 초과에 assert 로 죽는다. 판정 ①(자동) p95 ≥ 300ms → RED, 판정 ②(눈)
  `project.md` 기준선 p95 9.11ms 의 **2배를 넘으면 설계가 틀린 것**이라 리뷰로 되돌린다.
  질의 경로가 무수정이고 느는 것은 `"version":1` 약 14바이트뿐이다.
- **보안(CSO)이 자리 하나를 잡았다** — 503 본문에 `str(exc)` 를 실으면 그것이 곧
  **DB 경로**다(`FileNotFoundError(db_path)`). 고정 문구로 못박고, `TestMissingDb` ·
  `TestHtmlPathFailsSafely` 의 경로 누출 단언을 503 으로 **옮겨서 살린다**고 계약에 적었다.
- **계획서 정정 한 곳**: `README.md:39` 가 *"요청마다 500 을 낸다"* 라 적어 스텝 1 에서
  거짓이 된다. 계획서는 README 를 스텝 2 에 걸어 뒀는데 실제로 걸리는 것은 버전이 아니라
  503 이다(`grep -rn "/search" README.md` → 0건 — API 스키마 설명 자체가 없다).
  스텝 2 → **스텝 1** 로 옮기고 6절 표도 고쳤다. **스텝 경계 자체는 그대로다** — 순증
  추정이 스텝 1 약 12줄 · 스텝 2 약 4줄 · 스텝 3 **0줄**로 셋 다 40줄 아래다.
- **설계서가 한 화면을 넘는다 — 줄이고도 186줄이다.** `rules/design.md` 7절은 *"한 화면을
  넘기지 않는다. 넘어가면 계획이 너무 큰 것이니 쪼갠다"* 이고 이 저장소의 기존 설계서는
  114~142줄이다. 초안 237줄을 두 번 압축해 186줄까지 내렸다(중복 문단 병합 · 기각 사유
  합치기). 남는 이유는 **갈림길이 넷이라 3-1 의 «출발점 셋» 이 네 번 곱해지고** 거기에
  변이 표 14줄이 붙어서다 — 7절이 말하는 *"쪼갠다"* 는 계획을 쪼개라는 뜻인데 계획 46 은
  이미 3스텝이고 갈림길 넷이 그 셋에 흩어져 있어 설계 단계에서 다시 못 쪼갠다.
  **다음 계획이 갈림길을 셋 이하로 들고 오면 이 문제가 사라진다** — 계획 phase 에서
  갈림길 수를 세어 보는 것이 옳은 자리다.
- **러너 파이프를 한 번 붙였다 — 연속 카운트가 0 으로 되돌아간다.** 설계서를 줄인 뒤
  *"이미 초록일 것 같은 실행"* 에 `| tail -5` 를 붙였고, 예상대로 결과 줄이 잘려나가
  **아무것도 못 봤다**. 직전 반복이 같은 실수를 적어 뒀는데 같은 이유로 또 했다.
  곧바로 맨몸으로 다시 돌려 482건 OK 를 눈으로 확인했다. 기록에만 남기고 넘어가지 않는다 —
  카운트를 0 으로 되돌린다.
- 결과: 맨몸 실측 `PYTHONPATH=src python3 -m unittest discover -s tests` → **482건 OK**
  (11.738초 — 기록을 다 쓴 뒤 마지막 실행). `data/crawl.db` sha256 `85c96744…` **무변경**
  (탐침은 임시 디렉터리에서 돌았다) · 의존성 0 · 새 제품 파일 0 · `.mutation-lock` 없음.
- 기록 갱신: **회전 돌렸다** — 계획 45 의 마지막 세 반복 → `history_024.md`
  (303줄 → 155줄). 직전 계획 반복이 *"회전 미달"* 이라 적었는데 실측은 이미 303줄이었다
  (자기 항목을 붙인 뒤 다시 안 셌다). `digest.md` 명부에 `history_024.md` 추가 ·
  `## 완료` 의 기존 회전 줄에 이어 붙임(새 줄 0 · 완료 항목 삭제 0 → **새 색인 구멍 0**).
  `status.md`(반복 236 · `phase: 개발`) · `metrics.md`(반복 236 · 설계 phase +1) ·
  `index.md`(계획 46 행에 설계 **있음** 반영).
- 다음: **개발 phase 스텝 1** — "색인이 없다" 를 503 으로 가른다. 설계 5절 계약과
  6절 변이 표(M1~M7)가 그 스텝의 것이다. **M5(프로그래밍 오류 → 500) 단언을 먼저 심는다**
  — 안 심으면 스텝 1 이 500 커버리지를 0 으로 만든다.

## 2026-09-02 02:34 | json-contract | 개발 1/3 | 시도0

- 한 일: **스텝 1 — "색인이 없다"를 503 으로 갈랐다.** 설계 갈림길 B 대로
  `except (FileNotFoundError, indexer.StaleIndexError)` 한 줄을 `except Exception`
  **앞**에 뒀다. `src/websearch/serve.py` 의 **두 경로 모두** — `do_GET`(JSON) 과
  `_do_html`(화면, 갈림길 C). 본문·문구는 고정(*"색인이 아직 준비되지 않았다"*)이고
  원인은 `log_error("색인 없음: %r")` 로 stderr 에만 남는다. 제품 **순증 10줄**
  (설계 추정 12줄) · 새 파일 0 · 의존성 0.
- **TDD 를 순서대로 밟았다.** 테스트를 먼저 쓰고 **RED 7건을 눈으로 봤다**
  (`TestMissingDb` 2 · `TestHtmlPathFailsSafely` 2 · `TestDriftedIndex` 2 ·
  README 카운트 가드 1). 구현 뒤 **486건 OK**.
- **M5 단언을 먼저 심었다** — 설계가 예고한 대로 503 절이 생기는 순간 두 경로의 500 을
  재는 단언이 0건이 된다. `TestProgrammingErrorStays500` 이 `serve._page_hits` 를
  `AttributeError` 로 갈아끼워 **JSON·화면 둘 다 500 인 것과 그 로그**를 못박는다.
  이 클래스는 구현 전부터 초록이다 — 재는 것이 새 동작이 아니라 **안 변한 동작**이라서다.
- **M3 픽스처가 새로 생겼다** — `TestDriftedIndex` 가 `docs` 를 옛 fts5 정의로 갈아끼워
  `StaleIndexError` 를 HTTP 로 처음 잰다. 그전까지 `serve` 는 그 이름을 **0번** 불렀다.
- **e2e 하나가 이 변경으로 거짓이 됐고 같이 고쳤다** — `e2e/tokenizer_e2e.py:149` 의
  ⑥ 시나리오가 옛 색인 화면을 `== 500` 으로 못박고 있었다(계획서 8절이 예고한 자리).
  503 으로 고치고 꼬리말 문장도 맞췄다. **실제로 돌려서 통과를 봤다** — 단위 스위트는
  e2e 를 안 부르므로 안 돌렸으면 다음 phase 까지 몰랐다.
- **러너 파이프를 또 붙였다 — 이번 반복 5회다.** RED 확인·GREEN 확인·e2e·최종 확인에
  전부 `| tail` 또는 `| grep` 을 달았다. 직전 두 반복이 같은 실수를 적어 뒀는데 **세 번째로**
  했다. 이번엔 잘려서 못 본 것은 없었지만(실패 목록과 `Ran/OK` 줄이 tail 안에 들어왔다)
  그것은 운이다 — 실패가 길었으면 또 못 봤다. **연속 카운트를 다시 0 으로 되돌린다.**
  기록에 남는 수치는 마지막 **맨몸** 실행의 것이다.
- 결과: 맨몸 실측 `PYTHONPATH=src python3 -m unittest discover -s tests` → **486건 OK**
  (12.108초). `PYTHONPATH=src python3 e2e/tokenizer_e2e.py` → 통과.
  `data/crawl.db` sha256 `85c96744…` **무변경** · `.mutation-lock` 없음(변이는 테스트
  phase 몫이다) · 새 제품 파일 0.
- 기록 갱신: `README.md` **두 줄** — rc 표 아래 *"요청마다 500 을 낸다"* 를 503/500 을
  가르는 문장으로(계획서 정정분), 검증 절의 단위 카운트 482 → **486**(가드가 즉시 울었다).
  `status.md`(반복 237 · 개발 스텝 2) · `metrics.md`(반복 237 · 개발 phase 51→52) ·
  `index.md`(계획 46 행 0/3 → **1/3**). 회전 미달 — 아카이브 24개 그대로.
- 다음: **개발 스텝 2** — 응답 스키마에 버전. `VERSION = 1` 상수 하나를 `_send` 한 곳에서
  `{"version": VERSION, **payload}` 로 낸다(갈림길 A). 화면에는 안 붙는다(M11 부정 단언).

## 2026-09-02 02:44 | json-contract | 개발 2/3 | 시도0

- 한 일: **스텝 2 — 응답 스키마에 버전을 붙였다.** 설계 갈림길 A 대로 `VERSION = 1`
  상수 하나를 `src/websearch/serve.py` 에 두고 `_send` **한 곳**에서
  `{"version": VERSION, **payload}` 로 낸다. 제품 **순증 7줄**(설계 추정 4 — 넘은 셋은
  전부 주석이다) · 새 파일 0 · 의존성 0.
- **다섯 응답이 전부 갖는 것을 실측했다** — 200·400·404·500·503. 앞 셋은 그대로 때리고
  500·503 은 `serve._page_hits` 를 `AttributeError`/`FileNotFoundError` 로 갈아끼워
  만든다(스텝 1 이 세운 픽스처를 그대로 쓴다 — `pages = None` 클래스를 새로 안 만들었다).
- **TDD 순서를 밟았고 RED 4건을 눈으로 봤다**(`TestSchemaVersion` 5건 중 4).
  다섯째(화면 부정 단언)는 **구현 전부터 초록**이다 — 재는 것이 새 동작이 아니라
  «화면에는 안 붙는다» 라는 **안 변한 동작**이라서다. 스텝 1 의 M5 와 같은 종류의 단언이다.
- **설계 변이 표의 다섯을 제자리에서 실제로 심어 전부 죽는 것을 봤다**(임시 스크립트,
  매번 원본 복원 · `.mutation-lock` 안 씀 · 커밋된 변이 0):
  M8 버전 삭제 → 4건 죽음 · **M9 200 에만 붙인다(좁힘) → 2건** · M10 `1`→`2` → 3건 ·
  **M12 `1`→`"1"`(좁힘) → 4건** · **M11 화면에도 버전(넓힘) → 새 부정 단언 2건**.
  M11 은 **오늘 재는 것이 0건이던 자리**고, 그 단언이 없으면 "일관성" 을 이유로 화면에
  붙여도 491건이 전부 초록이다.
- **버전을 올리는 규칙을 `VERSION` 상수 주석에 적었다** — *필드를 빼거나 뜻을 바꿀 때만
  +1, 필드를 더하는 변경은 1 그대로*. 정수라 소비자가 `>=` 로 비교한다(그래서 M12 가
  치명적이다 — `"1"` 이면 그 비교가 조용히 문자열 비교가 된다). 설계 2절이 근거다.
- **JSON 을 파싱하는 e2e 넷을 전부 돌렸다** — 최상위 키가 하나 늘었으므로 깨질 자리다.
  `search_api_e2e`·`pagination_ui_e2e`·`tokenizer_e2e`·`perf_search` 전부 rc=0.
  설계가 *"최상위 키 집합 단언 0건"* 이라 예측한 그대로 **고칠 e2e 는 없었다**(스텝 1 은
  하나가 깨졌다). p95 **8.80ms**(예산 300ms 의 2.9%) — 기준선 8.86ms 그대로다.
- **러너 파이프 1회.** 최종 확인에 `| tail -4` 를 달았다가 stdout 이 섞여
  `Ran/OK` 줄이 **잘려 나갔다** — 직전 세 반복이 "운이 좋았다" 고 적어 둔 그 경우가
  실제로 났다. 바로 맨몸으로 다시 돌렸고 아래 수치는 그 실행의 것이다.
- 결과: 맨몸 실측 `PYTHONPATH=src python3 -m unittest discover -s tests` → **491건 OK**
  (12.105초). `data/crawl.db` sha256 `85c96744…` **무변경**(테스트는 전부 임시 디렉터리) ·
  의존성 0 · 새 제품 파일 0 · `.mutation-lock` 없음.
- 기록 갱신: `README.md` 검증 절의 단위 카운트 486 → **491**(가드가 즉시 울었다 — 이번이
  두 반복 연속이다). `status.md`(반복 238 · 개발 스텝 3) · `metrics.md`(반복 238 ·
  개발 phase 52→53) · `index.md`(계획 46 행 1/3 → **2/3**). 회전 미달 — 아카이브 24개 그대로.
- **계획서 스텝 상태 두 줄을 고쳤다** — 스텝 1·2 가 `대기` 로 남아 있었다(스텝 1 반복이 자기 줄을 안 고쳤다). `status.md`·`index.md` 는 완료라고 적고 있어 계획서만 거짓이었다.
- 다음: **개발 스텝 3** — 화면 순서 == JSON 순서(사양 기능 6). `src` **0줄** 예상이고
  `tests/test_serve.py` 만 는다. 같은 페이지의 `/` HTML 에서 `<div class="url">` 을 뽑아
  `/search` 의 `results[].url` 과 **리스트로** 비교한다(집합이면 M14 를 놓친다).

## 2026-09-02 02:58 | json-contract | 개발 3/3 | 시도0

- 한 일: `tests/test_serve.py` 에 `TestScreenMatchesJson` 하나(사양 기능 6). 같은 질의·같은
  페이지에서 `/` HTML 의 `<div class="url">` 목록과 `/search` 의 `results[].url` 을
  **리스트로** 비교한다 — 1·2페이지 둘 다, 20문서 픽스처(`MANY_PAGES`) 재사용.
  제품 `src/` **0줄**(설계 추정 그대로) · 새 파일 0 · 의존성 0.
- **RED 를 변이로 만들었다.** 재는 성질이 구조상 이미 참(두 경로가 `_page_hits` 한 벌을
  나눠 쓴다)이라 실패를 볼 길이 변이뿐이었다 — `.mutation-lock` 아래 제자리에 심어
  **M13**(`_do_html` 만 `page + 1`) 2건 · **M14**(화면 쪽만 `reversed()`) 2건이 죽는 것을
  봤다. 매번 원본 복원 · 커밋된 변이 0 · 락 제거 확인.
- **M14 를 전 스위트에 걸어 «오늘 0건» 을 실물로 확인했다** — 심은 채 492건을 돌리니 운 것이
  새 단언 둘과 README 카운트 가드뿐이었다. **순서만 뒤집는(집합은 같은) 변이를 잡는 테스트가
  저장소에 하나도 없었다.** 설계 변이 표가 예측한 그대로다.
- **집합이 아니라 리스트인 이유가 여기서 증명됐다** — M14 는 집합 비교였다면 살아남는다.
  그리고 빈 목록끼리는 무엇과도 같으므로 `len(screen) == PAGE_SIZE` 를 먼저 못박았다
  (탐침 한 줄이 화면에 새는 것도 같은 단언에서 죽는다).
- URL 을 링크가 아니라 `<div class="url">` 에서 뽑는다 — `_safe_href` 가 거른 URL 이 빠지면
  길이부터 달라진다. 되돌리기는 `html.unescape`(stdlib), 새 파서 0.
- 결과: 맨몸 실측 `PYTHONPATH=src python3 -m unittest discover -s tests` → **492건 OK**
  (12.124초). JSON 을 파싱하는 e2e 넷(`search_api`·`pagination_ui`·`tokenizer`·`perf_search`)
  전부 rc=0 · p95 **8.73ms**(기준선 8.86ms). `data/crawl.db` 무변경 · 의존성 0.
- 기록 갱신: `README.md` 카운트 491 → **492**(가드가 세 반복 연속 울었다) ·
  `status.md`(반복 239 · phase 테스트) · `metrics.md`(반복 239 · 개발 phase 53→54) ·
  `index.md`(계획 46 행 2/3 → **3/3**) · 계획서 스텝 3 상태 완료. 회전 미달(4항목).
- 다음: **테스트 phase** — 개발 세 스텝이 전부 끝났다. 새로 쓰는 곳이 아니라 빠뜨린 것을
  찾고 전체를 돌리는 곳이다. `history_current.md` 가 상한에 닿았으니 다음 반복이 항목을
  붙인 **뒤에** 세어 회전한다.
