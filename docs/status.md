---
signal: GREEN
phase: 개발
step: 1/1
attempt: 0
iteration: 394
updated: 2026-09-06
ctx: 58
night_iterations: 191
night_red: 2
night_retries: 4
plan: spec-citation-anchor 계획 68 (개발 phase 스텝 1/1 완료 · 다음은 테스트 phase)
---

## 현재 상태

**계획 68 `spec-citation-anchor` 개발 1/1 을 닫았다 — 인용마다 앵커를 의무로 만들었다.**
`tests/test_docs.py` 의 `SpecCitationTest` 에 `test_every_citation_carries_an_anchor`
한 벌을 심어 **RED 12건**을 기록하고, 그 열두 자리에 사양 문구 조각을 동행시켰다.
전수 **`Ran 628` `OK` rc 0** · 브랜치 `loop/passage-cost-band`.

## 이번 스텝이 한 일

**RED 는 11 이 아니라 12 였다.** 계획서 2절이 「앵커 있음 7 · 없음 11」로 적었지만
그 절이 이름으로 나열한 자리는 **열둘**이고(`tests/test_serve.py` 다섯 ·
`e2e/design_check.py` 둘 · `e2e/perf_crawl.py` 넷 · `e2e/quality_eval.py` 하나),
심은 단언이 문 자리도 **그 열둘과 글자 그대로 같았다**. 「7 · 11」은
`e2e/quality_eval.py:34`(`TOP_N` — 문구 `"상위 10건"` 과 값 `10` 을 **둘 다** 가진 줄)를
문구 축과 값 축에서 한 번씩 세어 생긴 오산이다. 실제는 **앵커 있음 6 · 없음 12** 이고
`6 + 12 = 18` 로 전체와 맞는다. **계획서 완료 기준 1 의 숫자만 틀렸고 목록은 맞았다.**

**가드**: `_anchors(line, pos)` 헬퍼 하나를 뽑아 **대조 축과 의무 축이 같은 추출기를 본다** —
갈라 놓으면 한쪽 추출기가 죽는 날 다른 쪽이 조용히 통과한다(계획 67 리뷰 `[R67-1]` 이
하한 하나로 물었던 자리와 같은 종류). `test_spec_quotes_match_cited_lines` 는 그 헬퍼를
쓰도록 바꿨을 뿐 단언은 무변이다.

**정정 12자리** — 문구 조각은 「그 줄에만 있는 최소 길이」로 골랐다: `:50-54` → `"화면은
둘뿐"` · `:51` → `"JS 번들 50KB"` · `:50` → `"검색 홈(검색창 하나)"`·`"결과 페이지(검색창 +
결과 리스트)"` · `:53` → `"키보드만으로 검색·결과 이동"` · `:59` → `"크롤 윤리(robots·간격)"` ·
`:44` → `"초당 5문서 이상 지속"` · `:22-23` → `"한국어 질의 20개·영어 질의 20개"`.
**문자열 안 3자리는 계획 2절대로 바깥 따옴표를 작은따옴표로 돌렸다**
(`tests/test_serve.py:918` · `e2e/design_check.py:444` · `e2e/perf_crawl.py:275`).

**줄 수를 보존했다.** 처음에 네 자리를 두 줄로 쪼갰다가 전부 한 줄로 되돌렸다 —
파일이 한 줄이라도 밀리면 `docs/` 와 계획서가 대는 `e2e/design_check.py:33` 같은
**소스 주소가 통째로 썩기** 때문이다. 79자 상한은 이 저장소에 없다(기존 최장 101자,
`tests/test_serve.py:1032`). 여섯 파일 전부 삽입 = 삭제다.

**변이 3판 + 무변이 대조군**(저장소 밖 `rsync` 사본 · 워킹트리 무변경 · `tests.test_docs`
31건만 · 매 판 `PYTHONPYCACHEPREFIX=$(mktemp -d)`):
**V0** 무변이 `Ran 31` `OK` — **오탐 0**(완료 기준 5) ·
**V1** `e2e/perf_crawl.py` 1행 docstring `:44`→`:45` **`failures=1`** — 계획 착수 탐침 M1 이
`OK` 로 생존하던 바로 그 밀림이 **이제 죽는다**(완료 기준 3, 그리고 4의 docstring 표본) ·
**V2** `tests/test_serve.py` 주석 `:53`→`:54` **와** 문자열 안 `:51`→`:52` 를 한 판에
**`failures=2`** — 표본 셋(주석·docstring·문자열 안)이 자리마다 1건씩 죽었다(완료 기준 4) ·
**V3** `PHRASE` 를 작은따옴표 꼴로 바꿔 문구 추출기를 죽임 **`failures=14`** — 새 단언 13 +
`MIN_CHECKS` 하한 1(완료 기준 6).

**`MIN_CHECKS` 판정 — 6 그대로 둔다.** 대조 건수는 5 → **19**(문구 14 · 값 5)로 뛰지만
하한을 올리면 손으로 관리할 숫자가 하나 더 는다. V3 이 그 판정을 실측으로 샀다:
추출기가 죽는 같은 편집에서 **새 단언이 13번, 하한이 1번** 운다 — 새 단언이 하한의
상위 호환이라는 계획 5절 판정이 «먼저 그리고 더 크게 운다» 로 확인됐다.
**0건 수집이 초록이 되는 길도 막혀 있다** — `MIN_HITS 14` 가 그 앞에 서 있고,
새 단언은 수집된 인용마다 도는 것이라 수집이 0이면 `_citations()` 가 먼저 죽는다.

**범위**: `src/` **0줄** · `docs/specs/` **무변** · `data/crawl.db` 무변 · 재색인 0 ·
스키마 0 · 새 의존성 0(stdlib) · PR #7 무접촉(`gh` 0회) · 전수 **2회** · 변이 **3판** ·
`--no-verify`·`--force`·`--amend`·`rebase` 0회 · `main` 직접 커밋 0회.
바꾼 것: `tests/test_docs.py`(가드) · `tests/test_serve.py` 5줄 ·
`e2e/design_check.py` 2줄 · `e2e/perf_crawl.py` 4줄 · `e2e/quality_eval.py` 1줄 ·
`README.md:104` 건수 줄(627→628).

**완료 기준 7/7.** 1 은 숫자를 12 로 정정해 충족(자리 목록은 계획서 그대로) ·
2 `Ran 628 OK` rc 0 · 3 V1 · 4 V1+V2 · 5 V0 · 6 V3 · 7 `src/`·사양·DB 무변.

## 다음

**테스트 phase.** 새 단언이 무엇을 아직 못 무는지가 입력이다 — 앵커가 **있기만 하면**
통과하므로 조각이 짧아 이웃 줄에서도 찾아지는 자리는 여전히 조용하다(계획 7절이 적은
유일한 진짜 위험이고, 오늘은 자리마다 실측 대신 표본 셋으로 샀다).
