---
signal: GREEN
phase: 개발
step: 1/2
attempt: 0
iteration: 388
updated: 2026-09-06
ctx: 38
night_iterations: 190
night_red: 2
night_retries: 4
plan: spec-citation-address — 계획 67 (개발 1/2 완료 · 다음 스텝 2/2 인용문 축)
---

## 현재 상태

**계획 67 `spec-citation-address` 의 스텝 1/2(주소 축)를 닫았다.**
`tests/test_docs.py` 에 `SpecCitationTest` 를 심고, 인용 쪽 다섯 자리를 고쳤다.
전수 **626건 · `OK` · rc 0**.

## 이번 스텝이 한 일

**가드.** `src`·`tests`·`e2e` 의 `*.py` 를 훑어 `concept\.md:(\d+)(?:-(\d+))?` 를 전수
수집하고, `subTest` 라벨을 **파일:줄**로 달아 두 가지를 단언한다.

1. 대상 줄(범위면 끝 줄)이 **사양 길이 안**이다 — 없는 주소를 대면 죽는다.
2. 범위의 **첫 줄이 빈 줄이 아니다** — 밀린 주소가 죽는다.
3. 수집 건수 **≥ 14** — 정규식이 좁아져 0건을 수집하고 조용히 초록이 되는 자리를 막는다.

정규식 리터럴 안의 `\.` 때문에 **가드 자신은 자기 수집에 안 물린다**.

**TDD 판정 줄.** 가드만 심은 판이 **정확히 5건 RED** 였다 — `tests/test_serve.py` 세 자리,
`e2e/design_check.py` 두 자리. 계획서 2절 P1 이 정적 판독으로 예고한 다섯과 **파일·줄까지
같다.** 맞는 아홉(`:44` ×3 · `:53` ×2 · `:54` · `:59` ×2 · `:22-23`)은 한 건도 안 빨개졌다
— **오탐 0** 이고, 「전부 빨갛다/전부 초록이다」라는 배선 오류 신호가 아니다.

**정정.** `49` → `50` · `49-54` → `50-54` 다섯 자리. 사양 49행이 빈 줄이고 디자인 축은
50~54행이라 인용이 통째로 한 칸 밀려 있었다. **사양은 한 글자도 안 고쳤다.**
정정은 줄마다 **앵커가 정확히 1개인지 먼저 단언**하고 바꿨다.

**건수 못이 실제로 물었다.** 첫 전수에서 `test_readme.py` 의
`test_verification_counts_match_reality` 가 `(625, 21) != (626, 21)` 로 잡았고,
`README.md` 의 「단위 625건」을 626 으로 같은 커밋에서 고쳐 초록으로 갔다.

## 예산 준수

전수 **2판**(가드 단독 RED 1 · 최종 1) · **변이 0판** · 러너 리다이렉션 0회.
재려던 것이 「주소가 실재하나」라 읽으면 끝나는 일이었다 — 앞선 정지 사례 2회(48분·39분 ·
산출물 0)가 남긴 「정적으로 읽으면 끝나는 것에 변이를 걸지 마라」를 그대로 지켰다.

## 범위

`src/` 0줄 · `docs/specs/` 무변 · `data/crawl.db` 무변 · 재색인 0 · 스키마 0 ·
새 의존성 0(stdlib) · PR #7 무접촉(`gh` 0회) ·
`--no-verify`·`--force`·`--amend`·`rebase` 0회 · `main` 직접 커밋 0회.
바꾼 것은 `tests/test_docs.py`(+43) · `tests/test_serve.py` 3줄 ·
`e2e/design_check.py` 2줄 · `README.md` 1줄.

## 다음

**스텝 2/2 — 인용문 축.** 같은 수집기 위에, 인용과 같은 줄에 따옴표로 옮겨 적은 문구나
바로 앞 상수의 값 표기가 있으면 그 문자열이 대상 줄 범위 안에 실재하는지 단언한다.
그 뒤 네 자리를 고친다(`:50` → `:51` ×2 · `:22` → `:23` ×2).
완료 기준 C4·C5 가 그 스텝의 못이고, C6·C7 은 테스트 phase 가 변이로 잰다.
`docs/history_current.md` 는 **257줄**(상한 300) — 다음 회전 대상은 `docs/history_068.md`.
