# e2e 104 `read-budget` — 필수 읽기 예산을 재는 자

- 실행: 2026-09-18 반복 614 · 기준 트리 `0041aba` · **깨끗한 worktree**(`/tmp/rb_e2e`)에서 돌렸다
- 앞 관문: 전수 **809 OK**(21.0s) — 기록을 전부 붙인 **뒤에** 쟀다(반복 613 [R104-1] 의 교훈)

## 왜 worktree 인가

내 작업 트리에서만 초록인 자는 의미가 없다. `git worktree add` 로 `HEAD` 를 따로 펴서
거기서 쟀다 — 작업 중 파일·캐시·미커밋 변경이 판정에 안 섞인다.

## ① 자가 옳게 세나 — 독립 재측

`wc -l` 과 자가 인쇄하는 수를 대 봤다. **네 파일 전부 일치**.

| 파일 | `wc -l` | 자가 인쇄한 수 | 그 파일의 상한 |
|---|---|---|---|
| `status.md` | 180 | 180 | 50 |
| `project.md` | 63 | 63 | 60 |
| `history_current.md` | 272 | 272 | 300 |
| `plan_read-budget.md` | 79 | 79 | 80 |
| **합계** | **594** | **594** | **600** |

**이 대조가 이 계획의 요점이다** — 손으로 적은 수가 두 번 틀렸고(「375」·「525/600」),
세 번째로 손으로 적는 대신 자를 세웠으니 **그 자가 옳게 세는지를 손이 아닌 다른 자로 봐야 한다.**

## ② 사용자가 하는 그대로 — 밤 시뮬레이션

루프가 밤마다 하는 일은 `history_current.md` 에 기록을 append 하는 것이다. SKILL.md 규격인
**5줄짜리** 기록을 붙여 가며 언제 무는지 쟀다.

- **밤 2 째에 물었다** — 합계 **604 > 600**.
- 메시지가 파일별 실측/상한을 전부 인쇄했고 `status.md 180/50` 이 맨 앞에 선다.
- 되돌리니 **gap `None` · 합계 594** 로 복귀. 되돌릴 수 있는 판정이다.

**여유가 6줄이다.** 규격을 지켜도 두 밤을 못 간다는 뜻이고, 이것이 사람에게 넘길 수다.
(다만 **마감이 계획서 79줄을 아카이브하면 515 로 떨어진다** — 계획이 도는 동안이 가장 좁다.)

## ③ 전수 안에서도 같은 판정인가

시뮬레이션 상태로 `tests.test_docs` 를 돌리니 **3건 실패**:

| 실패 | 진짜인가 |
|---|---|
| `ReadBudgetTest.test_live_required_reading_is_within_the_budget` | **진짜** — 604 > 600 |
| `HistoryCapTest.test_live_history_is_within_the_caps` | 픽스처 부산물 — 합성 기록 둘이 항목을 **19 → 21**(상한 20)로 올렸다 |
| `IterationSyncTest.test_metrics_and_status_agree` | 픽스처 부산물 — 합성 기록의 반복 번호(`700`·`701`)가 `status.md` 와 안 맞는다 |

뒤 둘은 **내 합성 픽스처가 만든 것**이지 자의 오탐이 아니다. 숨기지 않고 적는다.
**덤으로 나온 사실**: 항목이 오늘 **19/20** 이라 회전이 한두 밤 앞이다.

## ④ 변이 — 9종 전부 죽는다

| # | 변이 | 죽는 자리 |
|---|---|---|
| M1 | 상한 600 → 700 | `test_one_line_over_the_cap_bites` |
| M2 | 경계 `<=` → `<` | `test_exactly_the_cap_is_silent` 외 2 |
| M3 | 메시지에서 파일별 내역 제거 | `test_the_message_prints_every_file_with_its_own_cap` |
| M4 | 파일별 상한을 단언으로 승격 | `test_a_file_over_its_own_cap_is_silent_while_the_sum_fits` |
| M5 | 계획을 파일 없어도 센다 | `test_an_active_plan_without_a_file_is_not_counted` |
| M6 | 계획을 아예 안 센다 | `test_an_active_plan_with_a_file_is_counted` |
| M7 | 명부에서 `status.md` 제거 | `ReadBudgetRosterTest` |
| M8 | 명부의 `status.md` 상한 50 → 500 | `ReadBudgetRosterTest` |
| M9 | 계획 상한 80 → 800 | `ReadBudgetRosterTest` |

**M5·M6 은 처음에 살았다**(반복 610) — `read_budget_sizes()` 가 살아 있는 트리만 읽어
갈래가 하나뿐이었다. **M7·M8·M9 도 처음에 살았다**(반복 612) — 명부 자체에 하한이 없어
**자가 제 입력을 줄여 초록이 될 수 있었다**. 둘 다 못을 박고 다시 쟀다.

**변이 스크립트가 한 번 나를 속였다** — 못을 박자 앵커가 2개가 돼 `assert` 가 조용히 터졌고,
변이가 안 걸린 실행이 「OK」로 찍혀 M7·M8 이 산 것처럼 보였다. 앵커 실패를 화면에 내게
고치고 다시 쟀다. **맨몸 규율이 경고하는 「실패가 통과로 보인다」가 검사 도구 쪽에서 났다.**

## 품질 기준 — 돌린 축과 안 돌린 축

제품 `src/` **0줄**이라 `e2e.md` 1-3절의 「이 계획이 건드린 파일에만 건다」가 적용된다.

- **돌린 것**: 의존성 0(`tests.test_deps` — 전수 안에서 돈다) · 전수 809
- **안 돌린 것**(해당 없음 · **통과라고 쓰지 않는다**): 검색 품질 · 근거 문단 ·
  검색 지연 · 디자인 4축 · 서버 상주 RSS · 크롤 간격 — 전부 `src/` 를 재는 축이고 이번 diff 는
  `tests/test_docs.py`·`README.md`·`docs/` 만 건드렸다

## 남긴 것

**계획 97 이 적어 둔 예측이 이번에는 조용하지 않았다.** 「앞 둘이 되밀린 것은 밤마다 쓰는
파일이라서다」 — 그 되밀림이 실제로 이 밤에 일어났고(반복 612 가 기록을 붙이자 603),
전수 797건이 조용했던 자리에서 **809건이 소리를 냈다.** 자가 제 첫 밤에 제 목적의 회귀를,
그것도 루프 자신의 기록에서 잡았다.

**사람 몫으로 남는 것**: `status.md` 180 은 제 상한 **50** 의 3.6배다. 자는 인쇄만 하고 안 문다 —
무엇을 남길지는 판단이고, 거기엔 사람 결정 대기 큐(게이트 ⑥⑦⑨)가 산다.
