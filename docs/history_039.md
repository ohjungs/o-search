# 반복 기록 아카이브 — 계획 48 `passage-api` 의 리뷰 3·개발 6 (반복 262~263)

<!-- history_current.md 에서 밀려난 원본. 수정·삭제 금지. -->

## 2026-09-02 16:10 | passage-api | 리뷰 3 | 시도0

**반려 — 승인 필요 2건.** 백지 패스로 후보 6건을 세워 3건을 버리고 3건을 보고했다
(`rules/review.md` 6절: 승인 필요가 하나라도 있으면 개발 phase 로 복귀).

- **[승인 필요] 캡 되감기가 문서를 통째로 지운다** (`indexer.passages`). 캡 안 마지막
  `<` 가 인덱스 0 이면 `html[:0]` 이다. 실측 `"<p>"+"가"*40000` → **블록 0개**, 그 문서가
  `/passages` 에서 사라진다. 잃는 원문 최대 35,000자(캡 전부).
- **[승인 필요] 되감기가 캡 경로에만 걸려 있다.** 캡보다 **짧은** 깨진 HTML(닫히지 않은
  속성값 · 여는 태그 도중)은 되감기를 안 지나 `<` 를 그대로 응답에 흘린다(`probe_leak.py`).
  **테스트 3 은 자기가 만든 컷(증상)만 닫고 형제 경로를 안 봤다** — 새 재발 유형이다.
- **[딸린 주석]** «잃는 것은 많아야 온전한 태그 하나» 가 위 둘과 모순된다.

**제안 수리를 실측하고 원복했다**(`.mutation-lock` · `git diff -- src/` 0줄):
되감기 조건을 `len(raw) > CAP and "<" in cut` 에서 **`cut.rfind("<") > cut.rfind(">")`** 로
바꾼다 — 마크업이 안 닫혔을 때만 되감으므로 정상 문서·태그 0개·날 `<` 는 안 건드린다.
**단위 558건이 그대로 OK** 였다 — 즉 **기존 테스트가 두 결함에 눈이 멀어 있다**(붙드는
단언 0개). `probe_fix.py` 에서 F1·F3 이 닫히고 대조군은 그대로였다.

**성능 축은 «세 번째 재발»이 아니다.** 프로세스 밖 `GET /passages` p95(12건 × 35,000자):
wiki **121.0ms(예산의 24%)** · `<p>가</p>` 반복 **190.3ms(38%)**. 계수 0.489 는 설계서
갈림길 5 가 근거와 닫는 조건까지 적어 둔 공개 판정이라 `severity.md` 4절 억제 대상이고,
실측도 예산 안이다. 갈림길 5 에 이 실측을 적어 축을 «열린 채로, 다만 실측된» 상태로 뒀다.

보안 패스(프로세스 밖 `probe_sec.py`): `/passages` 200·400(q없음·page=2·q초과)·404 가
전부 `application/json; charset=utf-8` + `nosniff` · `version: 1` · 트레이스백·경로 누출 **0**.
`/search` 계약(`['has_next','page','query','results','version']`)과 화면(CSP 있음 ·
`<script` 0개 · passages 언급 0 · 화면↔JSON 문서 수 일치) **무변경**.
검증: 단위 **558 OK** · `e2e/*.py` **20종 rc 0** · `passage_eval` 정확도 **100.0%(398/398)** ·
제품 `src/` **0줄** · 의존성 0(stdlib) · `data/crawl.db`·`docs/specs/` 무변경.

## 2026-09-02 16:35 | passage-api | 개발 6 | 시도0

- 한 일: **회전 먼저**(282줄 — 이 항목을 붙이면 넘는다). 테스트 3 을 `docs/history_038.md`
  로 밀고 명부·`digest.md` 를 갱신했다(열여덟 회전 연속 지각 0 · 새 색인 구멍 0).
  그다음 리뷰 3 의 승인 필요 2건을 **한 줄로** 닫았다. 사용자가 승인 게이트를 위임으로
  열어 뒀다(닫힌 것 — 스키마·마이그레이션·의존성·`data/crawl.db` — 은 하나도 안 건드렸다).
- **RED 를 먼저 눈으로 봤다.** 새 단언 3건을 먼저 쓰고 돌려 **실패 2건**을 확인했다:
  `test_a_cut_..._keeps_the_document` → `Lists differ: [] != ['http://a.test/']`(문서가
  통째로 사라진다) · `test_markup_broken_in_the_source_leaks_even_under_the_cap` →
  `'<' unexpectedly found in '김치찌개 <a href="xxx…'`(캡 아래 형제 경로의 마크업 누출).
- **처방은 한 줄** — 되감기 조건을 `len(raw) > CAP and "<" in cut` 에서
  **`cut.rfind("<") > cut.rfind(">")`** 로 바꿨다(`src/websearch/indexer.py`).
  「`<` 가 있나」가 아니라 **「그 `<` 가 안 닫혔나」**를 묻는다. 두 결함이 한 질문의
  두 얼굴이었다 — ① 문서 전체가 문단 하나면 마지막 `<` 가 인덱스 0 이라 `html[:0]` 이고
  ② 원인이 «캡» 이 아니라 «미완성 마크업» 이므로 캡보다 짧은 깨진 원문도 같은 자리를
  지나야 한다. **뿌리 하나에 가드 하나**라 형제 호출부가 안 남는다
  (`extract_blocks` 호출부는 `passages()` 이 하나뿐이다 — 재확인).
- **모순된 주석도 같이 고쳤다**(리뷰 3 의 딸린 지적). «잃는 것은 많아야 온전한 태그
  하나» 는 새 조건 아래에서 참이 됐고, 엔티티에 대한 과장(`A&am` 도 함께 사라진다)은
  **조건부로 적었다** — 안 닫힌 `<` 뒤에 있을 때만 사라지고, 아니면 평문이라 마크업으로
  안 샌다. 실제 동작보다 넓게 적힌 주석을 좁힌 것이다.
- **변이 3건 실측**(`.mutation-lock` 아래 · 전부 원복 · `git diff -- src/` 로 확인):
  X1 되감기 삭제(`if False`) → **RED 2**(캡 컷 · 원문 깨짐 둘 다) · X2 옛 조건으로 되돌리기
  → **RED 2**(백지 · 누출, 즉 리뷰 3 이 지목한 그 둘) · X3 `>` 를 `>=` 로 → **RED 1**
  (태그 0개 문서에서 둘 다 `-1` 이라 `html[:-1]`, 마지막 글자가 조용히 사라진다).
  **입력의 «모양» 축으로 셋을 갈랐다**: `<` 의 위치(맨앞 · 중간 · 없음) × 깨진 형태
  (속성값 도중) × 캡 초과 여부. 죽는 변이가 없는 단언은 안 넣었다 — 태그명 도중·엔티티
  도중은 속성값 도중과 **같은 변이에서 죽어** 새로 안 세웠다.
- **예산 재확인 — 되감기를 고치면 파싱 입력 길이가 달라지므로 프로세스 밖에서 다시 쟀다**
  (`GET /passages`, `--port 0`, 문서 12건 × 캡 35,000자 × 30회, 임시 DB · `data/crawl.db`
  는 열지 않는다): wiki 모양 **p95 123.5ms(예산 500ms 의 25%)** · `<p>가</p>` 반복
  **p95 196.3ms(39%)**. 리뷰 3 실측(121.0 · 190.3)과 **오차 안에서 같다** — 한 줄이
  길이 예산을 안 움직였다. 계획이 성능 축에서 두 번 넘어졌으므로 산수가 아니라 실측으로
  닫았다(설계서 갈림길 5 에 이 재측을 적었다).
- 결과: **단위 561건 OK(13.396초)** · **`e2e/*.py` 20종 전수 rc 0**. 사양 숫자 무변동 —
  `passage_eval` 정확도 **100.0%(398/398)** · 채택률 99.5% · p95 1.40ms, `perf_search`
  p95 8.86ms, `search_api_e2e` p95 2.13ms, `quality_eval` ko 100% · en 95%,
  `perf_crawl` 10.22/s. 러너 호출 **25회 전부 맨몸·단독**(파이프·`grep`·리다이렉션·
  명령 잇기 0회 · 일곱 반복 연속).
- 실측: 제품 `src/` **동작 1줄 + 주석 10줄**(`indexer.py`) · 테스트 +32줄
  (`tests/test_indexer.py` 3건) · 새 파일 1(`docs/history_038.md`) · 의존성 0(stdlib) ·
  스키마·마이그레이션 없음 · `data/crawl.db` **안 열었다** · `docs/specs/` 무변경 ·
  단위 558 → **561건**이라 README 카운트를 갱신했다(`test_readme.py` 가 실제와 대조한다).
- 다음: **테스트 4.** 볼 곳 — ① 리뷰 3 항목(위)을 대조로 읽고 두 승인 필요가 실제로
  닫혔는지 ② 이번에 안 세운 모양(태그명 도중 · 엔티티 도중 · 주석 `<!--` 도중)이
  «같은 변이에서 죽는다» 는 판정이 맞는지 갭 탐색으로 한 번 깨 보기 ③ 열린 축 0.489 를
  계획 48 안에서 닫을지 다음 계획으로 넘길지(리뷰 3 이 «열린 채로, 다만 실측된» 으로 뒀다).
