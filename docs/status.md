---
signal: GREEN
phase: DONE
step: 1
attempt: 0
iteration: 202
updated: 2026-09-01
ctx: 62
night_iterations: 68
night_red: 0
night_retries: 0
---

# 현재 상태

**계획 40 `exit-code-contract` 리뷰 1/1 완료 — 지적 4건 · 제품 동작 지적 0건 · 자동 수정 4.**
계획서 `docs/plan_exit-code-contract.md` · 설계서 `docs/design_exit-code-contract.md` ·
브랜치 `loop/exit-code-contract`(기점 `064e8a5`). **남은 것은 DONE 마감 한 반복뿐이다**
(계획 7절 판정: **새 e2e 0개**). 계획 34~37 은 PR #2 로 `main` 병합됨(`main` 최신 `e0890c8`)
— 38·39·40 의 `main` 병합은 사람이 정한다.

## 방금 한 것 (2026-09-01 · 리뷰 1)

**백지 패스(diff 만)를 먼저 회수하고 대조 패스를 열었다.** 대상은 `74bf266..HEAD`
(설계 `20b011c` · 개발 `426eb90` · 테스트 `1d607dd`).

### 패스 A — 백지 (지적 2건, 둘 다 자동 수정)

**[A1 low] `indexer.py:243` 의 주석이 문장 중간에 끊겨 있었다** — *"…계약표는 README"*
로 끝난다. 이 계약의 근거를 적은 세 줄짜리 주석이고 그 마지막 문장이 잘렸다. → 완성.

**[A2 low·같은 실패 모양] `README` rc 1 행의 `serve` 칸이 여전히 실제보다 좁았다** —
*"포트 점유"* 라고만 적혀 있는데 **특권 포트도 rc 1** 이다(실측 `serve … --port 80` →
`rc 1 · [Errno 13] Permission denied`). `serve.py:329` 주석은 EADDRINUSE·EACCES 를 둘 다
적어 뒀고 설계서 칸은 *"`serve`(bind)"* 였다. **테스트 스텝이 방금 고친 `[8]` 과 같은
자리·같은 모양**이다(설계 → README 로 옮기며 칸이 좁아진다). 바로 아래 산문은
*"포트를 못 열었을 때뿐"* 이라 이미 옳아, 표와 산문이 서로 다른 말을 하고 있었다.
→ 칸을 `포트를 못 연다(점유·권한)` 로.

**무지적 둘 — 무엇을 어떻게 봤는지**(백지 패스는 근거를 적는다):

- **rc 표를 다시 실측했다.** 세 CLI 를 진짜 프로세스로 **17가지 상황**에 돌렸다
  (스크래치패드 임시 디렉터리 · `crawl` 은 사례마다 새 디렉터리 · 시드는 `127.0.0.1`
  닫힌 포트 · 외부 네트워크 0). **A2 를 빼면 표의 모든 칸이 참이다** — `serve <없는 DB>`·
  `<손상 DB>` 는 **뜬 채 HTTP 500** 이고 Ctrl-C 에 rc 0(그래서 130 행이 `crawl`·`indexer`
  뿐인 것도 참이다). `crawl`: 손상 DB·쓸 수 없는 `data/` → 1(트레이스백) · 가져올 수 없는
  시드·usage → 2 · 닿지 않는 시드 → 0.
- **열거형 완전성 — diff 밖을 전수로 읽었다.** rc 2→1 이동 뒤 `tests/`·`e2e/` 에 남은
  `main(...) == 2` 단언은 **전부 인자 오류**다(`test_indexer` 2 · `test_crawl` 7 ·
  `test_serve` 8). 환경 rc 를 재는 e2e 는 두 곳(`tokenizer_e2e.py:153` ·
  `indexer_interrupt_e2e.py:281`)뿐이고 둘 다 옮겨졌다. **남은 낡은 자리 0곳.**

### 패스 B — 대조 (지적 2건, 둘 다 자동 수정)

**[B1 medium] 계획서 7절이 세 반복째 "미정 · 설계가 정한다" 로 남아 있었다** — 계획서가
스스로 *"설계가 이 기준을 적용한 결과를 계획서 7절에 되받아 적는다"* 고 요구해 놓고
설계·개발·테스트가 아무도 안 적었다. 판정은 이미 나 있었다(대안 A → 새 e2e 0개).
→ 7절에 되받아 적었다. **rc 1 은 안 잡힌 예외와 값이 같아 값만으로는 못 가른다** —
그 대가를 갚는 것이 두 e2e 의 `Traceback` 부재 단언이라는 것도 함께 적었다.

**[B2 low] 계획서 8절 한도의 `digest` 열린 항목이 50 인데 실제는 51 이다** — 테스트
스텝이 항목을 하나 더해 숫자가 움직였는데 **한도 줄만 안 따라왔다.** 다음 스텝이 그
줄을 보면 근거 없이 RED 를 낸다. → 51(`[7]` 닫으면 50)로 정정 + 왜 움직였는지 한 줄.

**설계서는 안 고쳤다** — 검증 절의 `50항목` 도 같은 이유로 낡았지만, 설계는 *그때 고른
근거*의 기록이고 현재 값이 사는 곳은 계획서 한도와 `status.md` 다.

### 역할 패스 — CEO/제품 · CSO · QA · SRE

- **제품**: 호출자가 `0/1/2/130` 으로 할 일을 가를 수 있게 됐고 그 표가 `README` 에 있다.
- **CSO**: 값만 바뀌었다. 신뢰 경계·비밀정보 무관이고, `serve` 는 DB 오류에서도 500 만
  내고 경로를 안 흘린다(실측 · `TestMissingDb` 가 붙들고 있다).
- **QA**: 이 계약의 유일한 구조적 대가는 **rc 1 이 안 잡힌 예외와 겹치는 것**이다.
  방어는 `Traceback` 부재 단언이고, 그것이 없는 자리는 **`crawl` 의 환경 오류 하나**뿐인데
  `digest [6]` 이 이미 열어 뒀다(아래).
- **SRE**: 되돌리기는 커밋 하나 revert(설계 명시) · 마이그레이션 없음 ·
  `crawl && indexer` 는 0/비0 만 보므로 무영향.

### 리뷰가 보라고 지목받은 셋

① **`README` 표** — 위 A2 하나를 빼고 실측과 일치(17행 재측정).
② **`digest [6]`(crawl 트레이스백) 을 이 계획에서 열까** — **안 연다.** 제품 diff 한도가
`indexer.py` 한 파일이라 `crawl.py` 를 건드리면 RED 이고, 이 계획의 축은 *값*이지
*보고 방식*이 아니다(계획 5절). 항목으로 남긴 것이 옳다.
③ **집안일 — `history_current.md` 를 제때 회전했다.** 299 → **145줄**
(계획 38 다섯 반복 + 계획 39 다섯 반복 → **`docs/history_016.md` 162줄**). 경계는
지난 네 번과 같다: 완료된 계획 둘을 통째로, 계획 40 을 여는 반복 198 부터 남겼다.
**`digest.md` 는 회전하지 않았다** — 200줄로 상한 *이내*이고, 열린 항목 51 을 줄이지 않고
줄일 여지가 없다(계획 38 이 취소선·완료 줄을 이미 다 걷어 갔다. 한 줄이 2,810자인
항목들이라 다음 회전은 삭제가 아니라 **압축**이고, 그것은 이 스텝의 크기가 아니다).
→ **다음 계획 후보**: `digest.md` 장문 항목 압축(방아쇠 — 항목을 하나라도 더할 때).

### 검증 (자동 수정 4건 뒤에 다시 돌렸다)

단위 **462건 OK**(11.954초) · e2e `indexer_e2e`·`indexer_interrupt_e2e`·`tokenizer_e2e`
전부 통과 · `data/crawl.db` sha256 **무변경** · `digest.md` 열린 항목 **51**·200줄 그대로.

**이 반복이 한도를 하나 밟았다** — 문서 수정 뒤 재검증에서 러너를 `| tail -4` 로 감쌌다.
바로 맨몸으로 다시 돌려 462 OK 를 확인했고, `digest ## 반복 실패` 의 그 항목을
**2회 → 3회**로 올렸다(항목을 압축해 200줄은 유지). **한도를 이 파일에 자기 손으로 적어
놓고 밟았다** — 읽는 것과 지키는 것은 다른 일이라는 것이 이 항목이 세 번째로 적는 내용이다.

## 다음 (DONE 마감 한 반복)

계획 40 은 개발·테스트·리뷰가 끝났고 **e2e 는 새로 붙일 것이 없다**(7절).
마감 반복이 할 것: `git mv docs/plan_exit-code-contract.md docs/plan_history_026.md` ·
`docs/index.md` 를 **완료**로 · `digest.md` `[7]` 을 닫아 **열린 항목 50** ·
`design_exit-code-contract.md` 도 같은 규칙으로 이름을 정한다(설계 파일 관례 확인).
그 뒤 `main` 병합은 사람이 정한다(38·39·40 이 브랜치에 쌓여 있다).

## 한도 (넘으면 RED)

- `data/crawl.db` 실물·스키마 무변경 (sha256 `85c96744…5bda18` — **확인함**).
- 단위 **462건**이 하나라도 줄면 RED · e2e **19종**이 줄면 RED.
- `docs/digest.md` 열린 항목 **51**(`grep -c '^- \['`)이 줄면 RED · **200줄(상한 200)**.
  `[7]` 은 계획 40 이 DONE 될 때만 닫는다(그때 50).
- 제품 diff 는 **`src/websearch/indexer.py` 한 파일**이다. `crawl.py`·`serve.py`·`store.py`
  가 바뀌면 RED. 이 리뷰 스텝의 `src/` diff 는 **주석 한 줄**이다.
- 변이는 `.git` 없는 스크래치패드 사본에서만 심는다(`## 반복 실패` 3회 항목).
- **러너를 파이프로 감싸지 않는다**(`## 반복 실패` 2회 항목).
- 의존성 추가 금지(stdlib 만) · `docs/specs/` 읽기만 · `--no-verify` 금지 ·
  `main` 직접 커밋 금지 · 외부 네트워크 금지 · 도메인당 요청 간격 1초 이상 · robots 준수.
