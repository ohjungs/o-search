---
signal: GREEN
phase: 개발
step: 1
attempt: 0
iteration: 188
updated: 2026-08-31
ctx: 55
night_iterations: 59
night_red: 0
night_retries: 0
---

# 현재 상태

**계획 38 `digest-rotate` 를 열었다 — 계획서 `docs/plan_digest-rotate.md`.**
브랜치 `loop/digest-rotate`(기점 `c2a115e`, `loop/indexer-interrupt` 에서 팠다).
계획 37 까지 전부 DONE·아카이브 완료. **활성 계획 1개, 다음은 개발 스텝 1.**
계획 34~37 은 PR #2 로 `main` 에 병합됐다(`main` 최신 `e0890c8`) — 이번 계획도
`main` 병합은 사람이 정한다.

## 이번 계획이 하려는 일

**탐색이 매번 통째로 읽는 `docs/digest.md` 가 상한을 넘겼고, 한 곳은 깨져 있다.**
① **232줄 · 90,568바이트**(머리말이 정한 상한 200) — `status.md` 가 두 반복 연속으로
"밀린다" 고 적은 집안일이다. ② **머리 두 줄이 깨져 H1 `# 아카이브 요약` 이 리스트 항목
안에 들어가 있다**(`b11fd40`, 2026-08-27 이후 25커밋·4일). ③ 그 사고로 닫힘 표시가
엉뚱한 자리에 붙어, **018 이 이미 닫은 `[5] 호스트 대소문자` 항목이 오늘도 열린 후보로
보인다** — 이번 탐색이 실제로 그것을 열린 후보로 읽었다.

2스텝이다: **스텝 1** 머리 복구 + 닫힘 표시를 `docs/digest.md:135` 제자리로,
**스텝 2** 취소선 항목·`[원문]` 하위줄·2026-08-25·26 완료 7줄 삭제. 순서가 있다
(스텝 1 이 만든 취소선이 스텝 2 의 삭제 대상이다). **제품 코드 0줄 · 설계 생략 · e2e 없음.**

## 탐색 phase 가 한 일 (2026-08-31)

- **discover 1~8순위를 전부 실측하고 나서 집안일을 골랐다.** 단위 456건 OK(rc 0) ·
  린터 없음 · 코드 `TODO`/`FIXME`/`HACK` **0건** · `candidates.md` 없음 · 보류는 승인
  대기(재시도 금지) · 후보는 전부 "도달 불가·천장 수용·사람이 정할 갈림길" · 이슈 없음.
- **값이 가장 큰 후보를 일부러 안 열었다** — `digest.md:178` 의 `[7]`(페이지 넘김에
  중복 1건·누락 1건)은 `?page=N` 공개 API 를 커서로 바꾸는 갈림길이라 **항목 자신이
  "사람이 정할 갈림길" 이라고 적었다.** 승인 항목으로 그대로 둔다.
- **회전을 두 반복 막고 있던 선행 조건을 실측으로 해소했다** — `digest.md:<줄번호>`
  인용 8곳 중 **6곳이 오늘 빈 줄이나 다른 항목**을 가리킨다(인용 당시에는 맞았다:
  `git show 1abc058:docs/digest.md` 156줄). **줄번호 인용은 회전 전에 이미 죽어 있다.**
- 회전 시뮬레이션(사본): 취소선만 지우면 **194줄**(여유 6줄뿐이라 두 반복이면 재발),
  2026-08-25·26 완료 7줄까지 지우면 **187줄 · 57,241바이트**(−36%).

## 검증 (이번 반복에 직접 돌렸다)

- 단위 **456건 OK** — `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest
  discover -s tests`.
- `PYTHONPATH=src python3 e2e/indexer_interrupt_e2e.py` rc **0**.
- 탐침은 전부 스크래치패드 임시 디렉터리 사본에서 돌렸고 저장소는 무변경으로 유지했다
  (`git status --short` 빈 상태에서 계획서만 새로 썼다).
- `data/crawl.db` sha256 `85c96744…5bda18` **무변경**.

## 남은 것 (다음 반복이 읽을 것)

- **개발 스텝 1 부터** — `docs/plan_digest-rotate.md` 4절. 완료 기준이 전부 실행 명령이다.
- **보류 2건은 그대로 열려 있다**(`plan_history_023.md` 12-2) — ① `commit()` 뒤 도착한
  중단이 "색인은 바뀌지 않았다" 를 거짓으로 만든다 ② `--query` 도중 중단도 같은 안내를
  낸다. 둘 다 사용자 표면이라 자기 RED 가 필요하다.
- **후보 2건** — ① `sqlite3.connect` timeout 불일치(`indexer.py:78·136·181` 5초 대
  `store.py:22` 30초) ② 죽은 `digest.md:<줄번호>` 인용 6곳(계획 38 의 "하지 않을 것").
- `docs/history_current.md` **283줄**(상한 300) — 다음 회전 대상이 예약돼 있다.

## 한도 (넘으면 RED)

- `data/crawl.db` 실물·스키마를 안 건드린다. 탐침은 임시 디렉터리에서만.
- **열린 후보 항목이 하나라도 사라지면 RED**(`grep -c '^- \[' docs/digest.md` = 47).
- `docs/digest.md` 의 `## 반복 실패`(3항목) · `## 보류` 항목 수 변경 금지.
- 기존 단언을 낮추지 않는다 · 단위 456건이 줄면 RED.
- 도메인당 요청 간격 1초 이상 · robots.txt 준수.
- 외부 네트워크 금지 · `docs/specs/` 읽기만 · `--no-verify` 금지 · `main` 직접 커밋 금지.
