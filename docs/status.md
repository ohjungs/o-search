---
signal: DONE
phase: e2e
step: 1/1
attempt: 0
iteration: 481
updated: 2026-09-09
ctx: 34
night_iterations: 7
night_red: 0
night_retries: 0
plan: ethics-floor
---

## 현재 상태

**계획 81 `ethics-floor` 마감.** 크롤 윤리 하한(도메인당 1초)을 **기계가 지킨다.**
전수 **708 OK rc 0** · 변이 3판 전부 물림.

## 근거는 오늘 아침의 사고다

무인 사이클이 강제 종료되며 `DOMAIN_INTERVAL` 을 **1.0 → 0.05** 로 낮춘 채 남겼다.
주석이 「내리지 않는다」고 적힌 그 줄이고, **전수 706건이 그 상태로 전부 초록이었다.**
잡은 것은 검사가 아니라 사람이 diff 를 본 것이다 — 그것은 기제가 아니다.

**기존 단언이 못 잡은 이유는 전부 상수를 «참조» 하기 때문이다.**
`assertEqual(f.interval("a.test"), DOMAIN_INTERVAL)` 는 값을 내리면 단언도 따라 내려간다.
「간격을 지키나」는 재도 **「그 간격이 윤리 하한 위인가」는 아무도 안 쟀다.**

## 그래서 리터럴로 박았다

`SPEC_FLOOR = 1.0` — `concept.md` 기능 3 이 준 숫자다. **상수로 빼지 않는다**: 빼면 그
상수를 같이 내려 다시 뚫린다. **재는 쪽과 재이는 쪽이 같은 값을 공유하면 자가 아니다.**

## 처리량은 코드 문제가 아니었다 (함께 실측)

도메인 1·2·4·8 → **1.03 · 2.08 · 4.40 · 9.86 문서/초**. 정비례다. 실물 3.67/초는
시드가 4도메인이라 **이론 상한의 92%** 에 이미 붙어 있던 값이고, 컨셉 성능 2(5문서/초)는
**도메인 5개 이상이면 자동 충족**이라 고칠 코드가 없다 — 시드 이야기다.

**이것이 간격을 낮추는 것이 유혹이 되는 이유다.** 숫자가 안 나오면 손이 그 상수로 간다.
오늘 실제로 그랬고, 이제 못이 막는다. 전문: `docs/e2e/ethics-floor/result.md`
