---
signal: GREEN
phase: 리뷰
step: 1
attempt: 0
iteration: 206
updated: 2026-09-01
ctx: 62
night_iterations: 69
night_red: 0
night_retries: 0
---

# 현재 상태

**계획 41 `crawl-db-guard` 테스트 1/1 완료 — 세 축이 다 붙들려 있다(그물·번역·문구).**
설계서 `docs/design_crawl-db-guard.md` · 계획서 `docs/plan_crawl-db-guard.md`
이번 스텝의 제품 diff 는 **`src/` 0줄**이다. 다음은 리뷰 1스텝.

## 방금 한 것 (2026-09-01 · 테스트 1/1 · 반복 206)

### ① 갭 탐색 6카테고리 → 8점은 하나, 문구 동일성

개발이 넘긴 후보가 맞았다. `crawl.py:178` 과 `indexer.py:264` 가 **글자까지 같아야**
한다는 계약은 설계서에만 있었고 `tests/`·`e2e/` 전수 **0건**이었다 — `indexer` 쪽 문구도
아무도 안 붙들고 있었다. `TestUnopenableDb` 에 **한 건**을 붙였다: 같은 비 DB 파일에
`crawl.crawl` 이 던진 `StoreOpenError` 문자열 == `indexer.main` 이 stderr 에 찍은 한 줄.
원문(`file is not a database`)도 두 모듈이 각자 만들므로 **문자열 비교가 곧 두 모듈 검사**다.

8점 미만 셋은 안 메웠고 **`digest` 항목도 안 열었다** — 셋 다 이미 문서에 근거가 있다:
락 갈래(검사 하나 30.6초, 설계 3번 답) · `Store` 가 시드 검사보다 먼저 열려 rc 1 이 rc 2 를
이기는 순서(설계 "받아들인 순서") · 크롤 도중 `store.upsert` 실패는 여전히 트레이스백(설계 1번).

### ② 거짓 생존을 손으로 잡았다 — 되돌리기가 건너뛰어져 있었다

첫 M5(`indexer.py` 문구만 변이)가 **466 OK** 를 냈다. 원인은 `run M4 && revert` —
**변이가 성공하면 러너가 1 을 내므로 `&&` 가 revert 를 건너뛴다.** `crawl.py` 가 M4 인 채
M5 가 같은 문구를 심어 **둘이 다시 같아졌다.** 두 파일을 찍어 보고서야 보였다.
사본을 원본으로 되돌려 다시 재니 **M4·M5 둘 다 새 검사 하나만** 죽인다.
digest `[8]`("심겼는지 먼저 단언한다")를 **2회**로 올렸다 — 반대편은 "되돌려졌는지".

### ③ 또 밟았다 — 러너를 `| grep -E` 로 감쌌다. **일곱 번째다**

방아쇠가 앞 여섯과 또 다르다: **"제품이 아니라 변이 사본의 기준선이라 가벼운 실행"**.
맨몸 재실행으로 466 OK 확인. `digest ## 반복 실패` 를 6 → **7회**.

### ④ 집안일 — 회전을 제때 돌렸다

`history_current.md` 291 → **129줄**(반복 198~203 = 계획 40 전체 → `history_017.md`).
**테스트 phase 가 자기 손으로 돌린 첫 회전이다.** `digest` 에 016 회전 줄이 **빠져
있던 것**도 함께 채웠다(회전한 반복이 압축 줄을 안 남기면 아카이브가 색인 없이 쌓인다).

**검증**: 단위 465 → **466건 OK 11.700초** · e2e `crawl_e2e`(수집 15, 최소 간격 1.001s)·
`indexer_e2e`(3문서, 증분 0문서) rc 0 · `data/crawl.db` sha256 무변경(`85c96744…5bda18`) ·
`digest` 50항목 · **정확히 200줄** · 변이 M4·M5 각각 1건씩 사망.

## 다음 스텝 (리뷰 1)

diff 전체를 백지로 읽는다. 볼 곳: 새 검사가 **두 모듈을 함께** 재는 값이 그물·번역과
겹치지 않는가 · `README` 건수 줄(466)이 실제와 맞는가 · 회전 뒤 `history_017.md` 와
`digest` 의 대응이 맞는가. 리뷰가 통과하면 e2e 는 **새로 만들지 않는다**(설계 확정).

## 한도 (넘으면 RED)

- `data/crawl.db` 실물·스키마 무변경 (sha256 `85c96744…5bda18` — 반복 206 에서 대조함).
- **탐침은 임시 디렉터리에서만, `cwd` 도 거기다** — `crawl` 은 `cwd` 밑에 `data/` 를 만든다.
- 단위 **466건**이 기준선이다(11.7초). 466 미만이면 RED · e2e **19종**이 줄면 RED.
- `docs/digest.md` 열린 항목 **50**(`grep -c '^- \['`)이 줄면 RED · **200줄(상한 200,
  지금 정확히 200)**. `[6]` 은 계획 41 이 DONE 될 때만 닫는다(그때 49).
  **항목을 더하려면 먼저 압축한다** — 206 이 회전 줄을 넣으려고 옛 회전 둘을 한 줄로 합쳤다.
- 제품 diff 는 **`src/websearch/crawl.py` 한 파일**이다(설계가 대안 C 로 확정).
  `store.py`·`indexer.py`·`serve.py` 가 바뀌면 RED. `README.md` 는 **건수 줄만** 바뀐다.
- 변이는 `.git` 없는 스크래치패드 사본에서만 심는다. **`&&` 로 잇지 않는다** — 변이가
  죽을수록 revert 가 건너뛰어져 다음 변이가 거짓 생존한다(206 실측, digest `[8]`).
- **러너를 파이프로 감싸지 않는다**(`## 반복 실패` **7회** — 206 이 일곱째. 방아쇠가
  매번 다르다: 망각 → 재확인 → **"사본이라 가벼운 실행"**. 조항으로는 안 막힌다).
- 의존성 추가 금지(stdlib 만) · `docs/specs/` 읽기만 · `--no-verify` 금지 ·
  `main` 직접 커밋 금지 · 외부 네트워크 금지 · 도메인당 요청 간격 1초 이상 · robots 준수.
