---
signal: DONE
mode: night
plan: tokenizer
phase: e2e
step: 3/3
attempt: 0
iteration: 81
night_iterations: 5
night_red: 0
night_retries: 0
night_self_amendments: 0
updated: 2026-08-27 (반복 81)
ctx: 41% / 200k
rules: rules/e2e.md
---

# 현재 상태

**`tokenizer`(010) DONE.** 브랜치 `loop/tokenizer` (기점 `f8d921b`) — 병합하지 않았다.
증거 `docs/e2e/tokenizer/result.md` · 아카이브 `plan_history_010.md`·`design_history_010.md`.

## 앞 세션 복구 (이번 반복에서 먼저 한 일)

앞 세션이 e2e phase 도중 **API 502 로 죽었다.** 작업 트리에 커밋 안 된
`e2e/tokenizer_e2e.py` 가 남아 있었고 문서는 `phase: e2e / 반복 80` 에 멈춰 있었다.

- 파일은 **끝까지 쓰여 있었고 종료 0 이었다.** 그러나 통과했다는 사실만으로는 근거가
  아니라서 **변이 6종으로 다시 세웠다** — 2-gram 분기 제거 · 어절 이어붙이기(반복 80
  회귀 재현) · 스니펫 `-1` 되돌리기 · `porter` 제거 · 드리프트 감지 무력화 ×2.
  **6/6 잡혔다.** 한 줄도 고치지 않고 그대로 커밋했다.
- 변이는 `src`·`e2e` 를 scratchpad 로 복사해서 했다 — 반복 78 사고의 교훈대로
  작업 트리를 되돌리는 방식은 쓰지 않았다.

## 이번 반복에 확인한 숫자 (전부 실제로 돌렸다)

| 축 | 실측 | 기준 |
|---|---|---|
| 테스트 전체 | **260/260 OK** | — |
| e2e 8종 | **전부 rc=0** (`tokenizer_e2e` 6/6 포함) | — |
| 포함률 | ko **20/20** · en **19/20** · 미검출 **1**(기준선 5) | 80% |
| **오탐** 매치 수 | 평균 **14.0** · 최소 11 · 최대 28 | 기준선 13.8 +1 이내 |
| `perf_search` p95 | **9.05ms** | 6.71ms 의 2배 이내 · 예산 300ms |
| `perf_crawl` | **10.25/s** · 도메인 간격 전부 0.95s 이상 | 5.0/s |
| `design_check` | JS gzip **0 B** · 최저 대비 **4.87:1** | 50KB · 4.5:1 |

**짝으로 갈렸다** — 미검출이 5→1 로 줄면서 오탐(매치 수)이 13.8→14.0 으로 늘지 않았다.
한쪽만 좋아졌으면 DONE 이 아니었다.

## 보류였던 `porter` 를 닫았다 — 유지

반복 80 이 "승인이 필요한 것" 으로 올린 항목이다. **루프가 정해도 되는 결정이었다**
(`rules/design.md` 4절의 넷 중 어디에도 안 걸린다 — 데이터 형태 아님·공개 계약 무손상·
의존성 없음·보안 아님. 되돌리기는 `SCHEMA` 한 낱말 + 자동 재구축).

앞 세션은 **손실만 448개 어휘로 재고 이득은 fixture 질의 1건으로 쟀다.** 앞 세션이
손실 쪽에서 정확히 짚은 눈멂(영어 질의 20개가 전부 완전한 낱말)은 **이득 쪽에도 걸린다**
— 그 20개는 거의 전부 기본형이다. 같은 어휘로 양쪽을 다시 쟀다:

| | en 포함률 | 굴절형 질의 (898건) | 접두 검색 깨짐 (448개) |
|---|---|---|---|
| **`porter unicode61` 유지** | 19/20 | **865 (96.3%)** | 50 (11.2%) |
| `unicode61` 제거 | 18/20 | 9 (1.0%) | 0 |

**천장으로 명시한 것**: 어휘 11.2% 가 부분 접두 검색을 잃는다. `y→i` 규칙 때문에 짧은
접두도 끊긴다(`any*` → `anything` 0건). 우회는 낱말을 끝까지 치는 것. `quality_eval` 은
fixture 가 동결이라 이 손실을 앞으로도 못 잰다 — 실물 로그가 생기면 다시 볼 값(digest).

## 다음 — 쿨다운 태우기 (digest `[high]`)

`crawl-throughput`(008) 리뷰가 보류한 1건. 보류 사유가 "프런티어 계약 변경 = 설계 승인"
인데, 프런티어는 **내부 계약**이라 `design.md` 4절에 안 걸린다 — 설계가 스스로 정하고
근거를 남기면 된다. **다만 설계에서 먼저 닫을 구멍이 기록돼 있다**: 순진한 수정은
워커 예외 경로에서 `mark_sent` 가 빠져 **간격 위반 구멍**이 열린다.
**도메인당 1초와 robots 준수는 어떤 경우에도 낮추지 않는다 — 어기는 설계는 RED.**

착수하려면 계획 phase 부터다 (`rules/plan.md`, 중복 방지는 `discover.md` 5절).

## 열지 않는 것 (승인 대기 — 야간 금지)

`recrawl`(`fetched_at` 스키마 변경) · `X-Robots-Tag`(스키마) · `loop/*` 브랜치 병합.

## digest 후보 (이번 밤 — 앞 세션 5개에 하나 더한다)

6. **거래를 재려면 양쪽을 같은 자로 재라.** 손실은 448개 어휘로, 이득은 fixture 질의
   1건으로 재고 고르면 **잰 쪽이 이긴다.** 앞 세션은 fixture 의 구조적 눈멂을 손실
   쪽에서 정확히 진단해 놓고 이득 쪽에 같은 진단을 적용하지 않았다 — 그래서 뒤집혔다
   (`porter`: "사는 것은 한 건" → 실제로는 굴절 질의 96.3% vs 1.0%)
