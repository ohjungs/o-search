---
signal: YELLOW
phase: 계획
step: 0/0
attempt: 0
plan: null
iteration: 561
updated: 2026-09-13
mode: night
night_iterations: 1
night_red: 0
night_retries: 0
night_self_amendments: 0
ctx: 측정 불가 — 공유 파일이 **43분 낡았고 `session_id` 도 남의 것**(게이트 ⑦ 다섯 번째
  재현)이라 두 검사 모두 탈락. 반복 상한에만 의존했고, 탐색 0건이라 어느 쪽으로 읽어도 정지다
note: 열 번째 빈손. 핀이 두 밤 연속 맞았다(첫 줄 빔 · 이슈 0 → 전수만 1회 763 OK).
  잰 것은 게이트 ⑥ 이다 — **상한 200 의 처방이 승인으로도 안 풀린다.** 「완료 항목 삭제」가
  닿는 줄이 넷뿐이라 다 지워도 207 이고, 자라는 175줄은 규칙이 남기라 한 절들이다
---

# 현재 상태

계획 **96 `docs-delete-rowid` 완료** 이후 새 계획이 없다. **탐색 0건 — 열 세션 연속**이다
(552~555 는 여덟 출처를 네 번 따로 전수, 556~561 은 아래 핀).

## 빈손 대조 핀 — 여덟 출처를 다시 안 재도 되는 조건

기준 트리는 **`ca032f7`** (559·560·561 이 전수로 확인한 것: 763 OK · 코드 `TODO` 0 · `candidates.md`
0건 · 이슈 0 · 패치는 `userinfo-leak` 하나 · 활성 `plan_*.md` 은 보류분 하나). 1~7순위는
**트리의 함수**라 트리가 같으면 결과가 같다. **8순위(이슈)만 트리 밖이라 둘째 줄을 «항상» 돌린다.**

```bash
git diff --stat ca032f7..HEAD -- src tests e2e scripts docs/digest.md docs/candidates.md docs/specs
gh issue list --state open --limit 20     # 트리 밖 — 항상 돌린다
```

**핀은 스냅샷이라 낡는다 — 전수를 새로 돌려 빔을 확인했으면 기준을 HEAD 로 옮긴다.**
**560·561 두 밤 연속 맞았다** — 첫 줄이 비어서 5·6·7순위를 안 열고 전수 한 번으로 끝냈다.
556 이 만들고 557 이 썼고 558 이 회전으로 깨뜨렸고 559 가 기준을 옮겨 560·561 이 이득을 봤다.
**핀의 수명은 회전까지다.** 핀을 깨는 유일한 것이 `digest.md` 이고, 그것을 건드리는 유일한
것이 `history_current.md` 300줄 초과 회전이다(회전이 digest 에 명부·기록 한 줄씩 쓴다).
지금 **193/300** 이고 빈손 한 밤이 실측 **21~28줄**(559 +21 · 560 +28 · 561 +25)이니
**다음 회전까지 약 4밤**이다 — 그 밤은 핀이 오경보를 내므로 전수를 돌리고 기준을 HEAD 로 옮긴다.

**핀은 «코드»의 함수지 «환경»의 함수가 아니다** — 파이썬·OS 가 움직이면 같은 트리가 다른
결과를 낸다. 그래서 전수는 싸니(**763 OK** · 18.7s · Python 3.9.6) 밤마다 돌린다.

## 사람 결정 대기 — 하나를 열어야 이어진다

근거·재현·처방은 갖춰졌고 막는 것은 판단뿐이다. 밤에 새로 막힌 것은 **여기 먼저 적는다**
(야간 보고서가 이 절을 그대로 퍼 간다).

- **① 동시 색인이 만드는 중복 `docs` 행** — 반복 550 리뷰 보류. 고치는 자리는 `index_pages`
  의 트랜잭션 경계(`BEGIN IMMEDIATE`)이고 재구축 갈래의 `BEGIN`(`indexer.py:217`)과 중첩을
  함께 풀어야 한다. **자기 계획 크기**이고 레이스 컨디션이라 밤이 안 연다. 승인하면 바로 계획.
- **② `perf_crawl.py` 기준선 여유 0.5%**(digest 후보 8점) — ①재기준선 ②[열림] 대비 비율
  ③참고용 강등 **셋 중 택1**. 밤이 못 고르는 이유는 셋 다 「통과시키려고 시나리오를 낮추는
  것」과 겉모양이 같아서다.
- **③ `docs/patches/userinfo-leak-refuse-credentials.patch`** — URL 자격증명이 크롤·저장·렌더로
  새는 건. 적용 상태에서 초록 확인 후 뽑아 뒀다. **보안 경계**라 밤이 영구히 못 연다.
- **④ `~/.claude/settings.json` 미커밋** — 내 변경이 아니고(`model: opus → opus[1m]` + `allow`
  자리 정렬 · 권한 상향 아님을 diff 로 확인) 코어층 가드가 막았다. 승인하면 `--no-verify`.
- **⑤ `~/.claude/hooks/ojeong-guard.cjs`** — `RUNNER` 가 `^python3` 로 앞머리를 고정해 **환경변수로
  시작하는 이 저장소의 정식 테스트 명령을 검증으로 한 번도 안 센다.** 고칠 내용은
  `~/.claude/hooks/guard.test.js` 가 명세한다. 분류기가 그 파일 쓰기를 막아 내가 못 고친다 —
  나를 제약하는 가드를 내가 푸는 것은 권한 상향이라 우회하지 않는다.
- **⑥ `docs/digest.md` 211줄**(상한 200) — **561 이 처방을 재 보니 승인으로도 안 풀린다.**
  상한 문구의 유일한 레버는 「오래된 완료 항목부터 지운다」인데, `## 완료` 절에서 그 레버가
  닿는 줄은 아카이브 명부를 뺀 **네 줄**(`digest.md` 21~24행)뿐이라 **넷을 다 지워도 207** 이다.
  남는 175줄은 규칙이 남기라 한 절(보류 6 · 반복 실패 59)과 **탐색이 입력으로 읽는** 후보
  절(43 + 47) · 사람 대기열(판단 필요 26)이고, 빈손 밤마다 자라는 쪽이 정확히 이쪽이다.
  이 상한은 `tests/test_docs.py:420-424` 가 **일부러 안 재는** 축이다(처방이 삭제라 못을
  박으면 야간이 스스로 못 푸는 RED 가 된다 — `history_current.md` 300 은 처방이 밀어내기라
  못을 박았다). **아침 처방이 바뀐다** — 「지우게 해 달라」가 아니라 「상한 200 이 이 파일
  모양에 안 맞으니 레버나 숫자를 고쳐라」다. 561 은 digest 를 한 줄도 안 건드렸다.
- **⑦ 정지 조건이 `session_id` 를 안 본다** (557 최초 실측 · 558·559·560·**561 재현**) —
  `~/.claude/.context-state.json` 은 **모든 세션이 공유하는 파일 하나**고 statusLine 이 매 렌더
  덮어쓴다. 신선도 검사는 `updated_unix` 만 보고 누가 썼는지를 안 본다. **559 는 앞의 둘과
  모양이 다르다** — 같은 남의 세션(`402da0af…`)이 쓴 값이 20분째(560 은 28분째) 안 자라
  **신선도 검사가 이 둘은 잡는다.** 셋을 다 잡는 검사는 신선도가 아니라 **소유**다. **위험한 쪽은 반대
  방향이다** — 내가 90%인데 남이 20%로 덮으면 정지선을 안 밟는다(그때는 신선하므로 못 잡는다).
  처방 한 줄: 읽은 `session_id` 가 내 것이 아니면 **statusLine 이 꺼진 경우와 똑같이** 취급.
  고칠 자리가 SKILL.md 의 «나를 멈추는 룰»이라 밤이 안 연다 — ⑤ 와 같은 이유다.
- **⑧ `metrics.jsonl` 의 `iterations` 가 밤마다 다른 것을 센다** (560 최초 실측). 스크립트는
  `status.md` 의 `night_iterations` 를 그대로 적는데(`metrics-record.js:48`), SKILL.md:226 의
  「야간 시작 시 0 으로 리셋」이 **세션 시작인지 그 밤의 첫 세션인지를 안 정한다.** 그래서
  08-24~08-29 세션들은 안 리셋해 **그 밤의 누적**을 적었고(08-29 가 `[12,14,16,18,25,27,29,30,33,35]`
  로 단조증가), 09-08 이후는 세션별로 적었다(`[4,1,1,1,1]`). **합이 626 인데 누적 반복은 559 다**
  — 넘을 수 없는 값을 넘었으니 적어도 한쪽은 제가 말하는 것을 안 센다. 같이 드러난 것:
  `metrics.md` 손목록은 **36줄인데 jsonl 은 52줄**이라 손목록이 세션을 놓쳤고, 그 목록으로 낸
  「평균 반복 4.7」도 그만큼 근거가 없다. **표의 용도가 「임계값을 고칠 근거」라 이게 곧 그
  근거의 품질이다.** 처방: 리셋 단위를 세션으로 못박고(최근 관례) 줄에 세션 식별자를 넣는다
  — 빈손 밤에는 변하는 칸이 ctx 뿐이고 그 ctx 가 ⑦ 로 굳어 **두 세션이 바이트까지 같은 줄로
  납작해진다**(09-13 의 두 줄이 실제로 그렇다). 고칠 자리가 공유 스크립트·SKILL.md 라 ⑤·⑦ 과
  같은 이유로 밤이 안 연다. **560 은 제 줄만 세션 단위로 적고 손목록 숫자만 맞췄다.**
  **같은 표의 「정지 사유」도 같이 깨져 있다** — jsonl 53줄이 **스키마 두 가지**(32줄은 `stop`
  칸이 없고 21줄만 싣는다)라 표가 아는 것은 세션의 **40%** 고, 아는 구간에서도 `계획 소진`
  표 3/jsonl 7 · `컨텍스트 상한` 표 6/jsonl 2 로 **4건이 두 칸 사이에서 뒤바뀌어 있다.**
  그 두 칸이 아침에 반대 처방을 낸다(「게이트를 열어라」 vs 「그냥 다시 불러라」).

병합은 손으로 조립하지 않는다 — `scripts/merge-to-main.sh` (전수가 초록일 때만 민다).

## 규모 축 (2026-09-13)

`pages` **52,172** · `docs` **40,347** · 2.48GB · 전수 **763 OK**.
필수 읽기 **549줄**/600 (`project.md` 249 + `status.md` 107 + `history_current.md` 193) —
회전분은 `history_083.md`(반복 532~551). `project.md` 는 제 예산 60 의 4배지만 「거의 안 바뀌는 사실」이라 안 건드렸다.
