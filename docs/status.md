---
signal: GREEN
phase: 테스트
step: 1/1
attempt: 0
iteration: 328
updated: 2026-09-05
ctx: 52
night_iterations: 150
night_red: 2
night_retries: 0
plan: loader-isolation # 계획 56 — 개발 1/1 완료 · 다음은 테스트 phase
---

# 현재 상태

**계획 56 개발 1/1 을 닫았다 — `-k` 아래에서 죽던 자가 산다.**
`tests/test_readme.py:83` 의 `unittest.defaultTestLoader.discover(...)` 를
`unittest.TestLoader().discover(...)` 로 바꿨다. 제품 `src/` **0줄**, 저장소 코드
파일 **1개**, 실질 한 줄(+ 왜인지 적는 주석 넉 줄).

## 오늘 다시 쟀다 — 계획서의 값이 그대로였다

착수 전에 두 값을 맨몸으로 다시 봤고 계획서와 한 글자도 안 달랐다.

| 잰 것 | 값 |
|---|---|
| `-k Readme` (고치기 전) | `AssertionError: (605, 21) != (5, 21)` · `Ran 5` · **rc 1** |
| 전수 (고치기 전) | `Ran 605 tests` · `OK` · **rc 0** · 13.713초 |
| `-k Readme` (고친 뒤) | `Ran 5 tests` · `OK` · **rc 0** |
| 전수 (고친 뒤) | `Ran 605 tests` · `OK` · **rc 0** · 13.579초 |

건수는 605 에서 한 칸도 안 움직였다(완료 기준 2). 기전도 직접 재서 확인했다 —
전역에 `testNamePatterns = ["*Readme*"]` 를 손으로 심으면 `defaultTestLoader` 는
**5**, `TestLoader()` 는 **605**. 호출처는 저장소 전체에 **한 곳뿐**임을 다시 셌다
(`grep -rn "defaultTestLoader\|TestLoader\|testNamePatterns" --include=*.py` → 1건).

## 완료 기준 3 — 변이 셋으로 쟀다 (스크래치패드 사본)

「고쳤는데 아무것도 안 막는다」가 이 계획의 최대 위험이라 세 방향에서 죽여 봤다.

| 변이 | 무엇 | 결과 |
|---|---|---|
| A | 검사 안에서 전역을 손으로 오염시킨다 | `OK` · rc 0 — **고친 줄이 막아 냈다** |
| B | A 위에서 `TestLoader()` 를 `defaultTestLoader` 로 되돌린다 | **`FAILED` · rc 1** — 확인이 살아 있다 |
| C | `README.md` 의 `단위 605건` → `604건` | **`FAILED` · rc 1** — 자가 안 멀었다 |

**C 가 함정이 사라진 것도 같이 보여 준다** — 실패 메시지가 이제 `-k` 아래에서도
「실제는 **(605, 21)**」라고 말한다. 고치기 전엔 같은 자리에서 「실제는 (5, 21)」이라
말해 README 를 틀리게 고치도록 유도했다. 변이는 전부 저장소 밖 사본에서 돌았고
저장소 파일은 한 글자도 안 건드렸다.

## 설계 — 건너뛴 사유가 오늘도 맞다 (확인함)

계획서 6절의 사유를 다시 봤고 유효하다. 대안이 갈리지 않는다 — 「전역 저장·복원」보다
「새 인스턴스」가 엄격히 작고(한 낱말 vs 두 줄), 전역을 아예 안 만지므로 실패 경로에서
복원을 빠뜨릴 자리도 없다. 파일 1개 · 한 줄 · 제품 0줄 · 보안 무관이라 `design.md`
1절 트리거 0. 변이 A 가 그 주장을 실측으로 뒷받침한다.

## 자동 스냅샷이 이 스텝을 갈랐다 — 되돌리지 않았다

**3시간짜리 launchd 잡(`com.ohjungs.osearch-autocommit`, `StartInterval 10800`)이
04:50 에 작업 중간을 덮쳐 코드 변경을 `bfaa3d8 자동 스냅샷 … 미커밋 작업 보존` 으로
커밋하고 원격에 밀어 버렸다.** `git ls-remote origin loop/loader-isolation` 이
`bfaa3d8` 이라 되돌리려면 `push --force` 가 필요한데 **한도가 금지**한다. 그래서
**고치지 않고 그대로 뒀다** — 브랜치에 계획대로의 코드가 있고 값은 같다. 다만
**스텝 커밋이 둘로 갈렸다**: 코드는 `bfaa3d8`(잡이 만든 메시지), 기록은 이번 커밋.

**막는 법은 이미 저장소가 안다** — `.mutation-lock` 관례(계획 45)가 그 잡을 세우려고
있던 것이다. 지금은 변이가 아니라 **정상 작업 중간**을 덮친 것이라 관례의 범위 밖이었다.
사람 결정 4번으로 올린다.

## 다음 행동

**테스트 phase.** 개발이 이미 변이 셋으로 재고 왔으므로 테스트 phase 가 볼 것은
「그 셋이 충분한 축인가」다. 안 재 본 축 하나가 보인다 — **`-k` 말고 다른 경로로도
전역이 오염되는가**(`unittest -k` 외에 `defaultTestLoader` 를 건드리는 진입점).
오늘 호출처는 셌지만 **오염원**은 `unittest.main` 하나만 확인했다.

## 러너 규율 — **이번 반복 0회 (누적 35회)**

러너를 여섯 번 돌렸고(전수 2 · `-k` 2 · 변이 사본 3 중 러너 3) **전부 맨몸·단독**이다.
파이프 왼쪽에 둔 적 0회 · `2>&1`·`2>/dev/null`·`>/dev/null` 0회 · 백그라운드 0회.
`; echo "rc=$?"` 는 러너가 끝난 뒤의 별개 명령이라 종료 코드를 안 가린다.
앞 반복이 새로 적어 둔 방아쇠(「한 번에 여러 번 돌린다」)가 이번에 실제로 걸릴
자리였다 — 변이가 셋이라 루프를 쓰고 싶었고, **쓰지 않고 세 번 따로 돌렸다.**

## 한도

제품 `src/` **0줄** · `e2e/` 0줄 · `data/crawl.db` **무변**(sha256 `85c96744…5bda18`
대조 통과, 열지 않았다) · `docs/specs/` 무변 · 새 의존성 0 · 스키마·마이그레이션·재색인 0 ·
`pgrep -f websearch.serve` **0건** · `--no-verify`·`--force` 0 · `main` 직접 커밋 0 ·
**PR 무접촉(조회·생성·병합 0회)** · 브랜치 병합·삭제 0 · `README.md` 무변(완료 기준 4) ·
변이는 스크래치패드 사본에서만 돌았다.

## 사람 결정 대기

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1).
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 0 일 때만.
3. **반응형 360px 미검증** — 브라우저가 없어 저장소의 아무도 안 그려 본다.
4. **3시간 자동 스냅샷 잡을 루프 작업 중에도 세울 것인가**(위 「자동 스냅샷」 절).
   오늘은 스텝 커밋을 둘로 갈랐을 뿐이지만, 다음에 RED 중간을 덮치면 **깨진 상태가
   원격에 올라간다.** 루프가 도는 동안 `.mutation-lock` 을 항상 켜 두는 안이 있다.

## 정지 사유

없음 — 계획 56 테스트 phase 로 이어간다.
