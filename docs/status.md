---
signal: GREEN
phase: 계획
step: 1
attempt: 0
iteration: 228
updated: 2026-09-01
ctx: 62
night_iterations: 88
night_red: 0
night_retries: 0
plan: docs-record-guard
---

# 현재 상태

**계획 45 `docs-record-guard` 를 열었다 — 계획서 `docs/plan_docs-record-guard.md` ·
브랜치 `loop/docs-record-guard`(기점 `79952ee`). 코드 0줄.**
**다음 반복은 설계 phase다** — `design.md` 1절 *"대안이 2개 이상 갈림"* 에 걸린다(계획서 5절).

## 방금 한 것 (2026-09-01 · 계획 1/1)

기록 문서의 불변량 둘을 `tests/test_docs.py` 가 붙들게 하는 계획이다. 탐색 **7순위**
(`digest ## 반복 실패`)에서 잡았고, 두 항목이 스스로 *"같은 파일·같은 꼴이라 한 계획으로
묶인다"* 고 적어 둔 것이다: **`metrics.md` 의 `반복` ↔ `status.md` 의 `iteration`**(4회 재발)와
**아카이브 색인 구멍**.

**착수 탐침이 근거를 세 군데 정정·확대했다** (전부 메모리 사본 · 저장소 무수정):

1. **신고문이 신고 대상을 초록으로 만든다.** `digest` 항목의 처방(*"아카이브 이름이
   `digest.md` 어딘가에 나오는가"*)을 글자 그대로 구현하면 구멍이 **4개**인데 항목이 적은
   수는 **5개**다. 차이 `history_001.md` 가 나오는 자리는 색인 구멍 항목 본문 —
   **구멍을 신고하는 그 문장 자신**뿐이다. `## 완료` 절로 범위를 자르면 정직하게 5개다.
2. **진짜 고아가 둘이다.** 살아있는 문서 6개 전수 대조에서 `history_002.md`·`history_005.md`
   는 어디에도 없었다(항목은 003 만 봤다). 003·019 는 **회전하는** `history_current.md` 에만 있다.
3. **순진한 substring 은 `plan_`/`design_` 접두에 가려진다.** `history_020.md` 는 단독 2회·
   접두 2회라, 미래 압축이 단독 언급을 지우면 순진한 검사는 **통과**하고 경계 검사만 RED 다.

**곁가지 — 계획서를 쓰자 2번이 스스로 지워졌다.** 고아 둘을 계획서와 기록에 적은 직후
다시 재니 **고아 0** 이다(포인터가 계획서와 `history_current.md` 로 생겼고 **둘 다 임시**다).
1번이 실시간으로 재현된 것이고, 계획서 갈림길 A 의 대안 3 을 실측으로 죽였다.

## 다음 반복이 할 것 — 설계 phase

계획서 5절의 갈림길 **둘**을 고른다. ① 검사의 범위(`digest` 전체 / `## 완료` 절만 /
살아있는 문서 어디든) ② 색인이 사는 자리 — **`digest.md` 는 정확히 200줄(상한)이라
색인 완전성과 압축 상한이 정면으로 부딪히는 것이 본체다.**
경계 검사를 쓸지는 갈림길이 아니다(3번이 실측으로 정했다).

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-01 확인)

`origin/main` 은 `494313b`(PR #5 병합). 계획 43·44 의 커밋들(`bc40ea9`~`79952ee`)이
아직 안 갔다. **남은 것 (사람 결정)**: 그것들을 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 — 둘이 열려 있다 (하나가 닫혔다)

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
   offset 이 0 이 되는 날 검사기가 멈추고, 짝은 그때 열릴 계획의 일이다.
3. ~~기록 3파일 불일치~~ — **계획 45 가 열었다.** *"개발 스텝을 여는 일이라 승인 필요"* 로
   접어 뒀던 판정은 **틀렸다** — 사용자가 저장소 작업 전권을 위임했고, 하네스 정지 조건
   (스키마·마이그레이션·배포·삭제·비밀정보·과금·의존성)에 이 일은 하나도 안 걸린다.

## 한도 (매 반복 확인)

- `main` 직접 커밋 금지 · `--no-verify` 금지 · 외부 네트워크 금지 ·
  `docs/specs/` 읽기만 · `data/crawl.db` 무변경(sha256 확인) · 의존성 추가 금지(stdlib 만).
- **러너 파이프 1회 — 연속 카운트 0 으로 리셋. 접기 관찰은 무산됐다(⑬).**
  계획서를 다 쓰고 재확인하며 `... discover tests 2>&1 | tail -3` 을 돌렸고
  `tail -3` 이 크롤 로그만 잡아와 판정 줄도 종료 코드도 안 보였다. 맨몸 재실행은
  **FAILED (failures=1)** — **앞의 열둘은 전부 초록을 가렸는데 이번은 빨강을 가렸다.**
  파이프를 안 걷었으면 RED 를 커밋했다. 방아쇠는 ⑥⑨⑪⑫ 와 같은 "이미 초록일 것 같은
  재확인" 이고, `digest` 처방(러너 호출을 별칭·`make test` 로 굳히기)의 근거가 세졌다.
  ※ 문서를 잘라 읽는 데는 `grep`·`sed` 를, 실측에는 `python3 - <<EOF` 를 썼다 —
  둘 다 러너가 아니라 판정 줄도 종료 코드도 없다. 세지 않되 적어는 둔다.
- **변이는 전부 메모리 사본에서 했다** — 저장소 파일을 한 번도 안 고쳤으므로
  `.mutation-lock` 이 필요 없었고 제자리 변이 0건이다.
- **계획 42 의 검사가 실사용에서 발화했다.** 그 RED 는 `DocCitationTest` 였고,
  **이 계획서가 `digest.md`·`index.md` 를 줄번호로 인용한 두 곳**을 잡았다 — append 전용
  문서를 줄번호로 가리키면 회전 한 번에 죽는다는, 계획 42 가 세운 바로 그 규칙이다.
  이름 인용으로 고쳤고, 정규식이 못 잡았지만 같은 죄인 두 곳도 함께 고쳤다.
- 이번 반복 최종 실측(맨몸): `PYTHONPATH=src python3 -m unittest discover tests`
  **475건 OK 11.963초** · `src/` **0줄** · 의존성 0 · `data/crawl.db` 무변경.
- **집안일을 이 반복이 닫았다.** `history_current.md` 306 → **107줄** 회전
  (`history_022.md` 신설 — 계획 44 전체 여섯 반복). **상한 300 을 넘긴 그 자리에서 바로
  돌렸고 계획 phase 가 자기 손으로 돌린 첫 회전이다.** `digest.md` **200줄 유지** —
  새 줄을 안 만들고 기존 `history_021.md` 회전 줄에 이어 붙여 **완료 항목을 하나도
  안 지웠다**(새 색인 구멍 **0**).
  회전 직후 107줄이었고 이 반복 기록을 다 붙인 지금 **132줄** · `digest.md` **200줄** ·
  아카이브 **22개** · 색인 구멍 **5개**(001·002·003·005·019 — 계획 45 가 닫을 것).
