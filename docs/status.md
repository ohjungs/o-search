---
signal: DONE
phase: e2e
step: 1
attempt: 0
iteration: 281
updated: 2026-09-03
ctx: 60
night_iterations: 116
night_red: 1
night_retries: 0
plan: null
---

# 현재 상태

**계획 49 `focus-rule-scope` — e2e 1/1 통과. DONE.**
남아 있던 완료 기준은 5(e2e 20종 전수 종료 0) 하나였고 그것을 밟았다.
증거는 `docs/e2e/focus-rule-scope/result.md`. 브랜치 `loop/focus-rule-scope`.

단위 **578건 OK**(맨몸 13.587초) · **e2e 20종 전수 rc 0** ·
`PYTHONPATH=src python3 e2e/design_check.py` **종료 0**(진단 줄
`라이트 --focus on --bg-page 3.56:1 / 기준 3.0 OK` 과 머리말 «링 규칙 1개 · offset 2px» 무변) ·
제품 `src/` **0줄** · 새 e2e 파일 **0개** · 새 의존성 0 ·
`docs/specs/` 무변경 · `data/crawl.db` sha256 무변경.

## 이 phase 가 판단한 것 하나

**8단계 중 7번(반응형)을 «통과» 가 아니라 «미검증» 으로 적었다.** Playwright 가 없어
360px 을 실제로 그려 볼 자가 저장소에 없는데, 하필 그 화면이 이번 설계의 근거다 —
설계서는 `@media (min-width:20em)` 안의 링 거절을 «360px 에 링이 없으니 정탐» 으로
정당화했다. **검사기는 그 화면을 재는 것이 아니라 거절함으로써 못 재는 것을 막는다.**
룰 1절 3항(*"명령이 없는 축은 검증되지 않았다고 기록한다"*)을 그대로 따랐다.

## 품질·성능 축 — 전부 기준선 그대로

ko **20/20** · en **19/20** · 순위 1위 39 · 매치 평균 14.0(최소 11) ·
문단 정확도 **100.0%** 채택률 99.5% p95 **1.46ms** · `perf_search` p95 **8.89ms** ·
`search_api` p95 2.10ms · 처리량 [열림]·[차단] **10.28/s** ·
문서집합 sha1 `541d455a…c078654b` · 디자인 4축(외부 0 · JS 0 B · 텍스트 4.87:1 · 링 3.56:1).

## 러너 규율 — 이번 반복 0회 (누적 30회)

단위 1회와 e2e 20회를 전부 맨몸·단독으로 돌렸다. 파이프·리다이렉션·`;` 잇기 0회,
`> /dev/null` 0회. **다섯 반복 연속 0회다.**

## 문서 마감

계획·설계를 `plan_history_035.md`·`design_history_035.md` 로 아카이브했고
`index.md` 49번 행이 완료(2/2 · 통과)다. `digest ## 완료` 에 계획 49 를 등재하며
**201줄이라 가장 오래된 완료 항목(plan_deadline 020)을 밀어 191줄**이 됐다 —
원본은 `history_007.md` 에 그대로 있고 명부 줄은 안 건드려 **새 색인 구멍 0**.
`history_current.md` 는 297줄·8회로 **회전 미달**이지만 다음 항목을 붙이면 넘을 자리다.

## 다음 — 계획 phase(탐색)가 이어받을 것 넷

1. **결합자 뒤의 링**(`a:focus-visible+.hint`) — 리뷰 1 이 범위 밖으로 판정하고
   여는 조건과 처방(`re.split(r"[ >+~]", part)[-1]`)까지 적어
   `digest ## 다음 계획 후보 (테스트 phase 갭, 8점 미만)` 에 등재해 뒀다.
   제품 CSS 에 결합자 뒤 링은 **0곳**이라 오늘의 3.56:1 은 참이다.
2. **괄호 두 겹 중첩**(`:not(:is(.x, :has(:focus-visible)))`) — 조건 5 의 알려진 천장.
3. `digest [6]` **이름표 `<p>` 소비자 0곳** · `[9]` `unittest -b` — 계획 48 이 남긴 것.
4. **`main` 으로 보내는 PR 은 사람 몫이다** — `origin/main` 은 PR #6 까지만 받아
   **계획 48·49 가 둘 다 미병합**이다. 무인 모드는 PR 을 열지도 병합하지도 않는다.
   기점이 `main` 이 아니라 `18d485f` 인 이유도 그것이다.

## 사람이 정할 것 — 넷 그대로다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
2. **`--focus` 는 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 이 0 일
   때만 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** — 룰 문장을 조이는 절반은 사람 몫이다.
4. **사양 확장이 남긴 것 둘**(`docs/specs/concept.md` 의 `## 사람이 정할 것`):
   속도 제한 시점(가정 — IP 당 분당 60회)과 사양 숫자들이 초안이라는 것(90% · 500ms · 60회/분).
