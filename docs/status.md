---
signal: GREEN
phase: 테스트
step: 1
attempt: 0
iteration: 210
updated: 2026-09-01
ctx: 46
night_iterations: 73
night_red: 0
night_retries: 0
---

# 현재 상태

**계획 42 `docs-citation-guard` 개발 1/1 완료 — 줄번호 인용을 기계가 센다.**
계획서 `docs/plan_docs-citation-guard.md`. 설계 없음(생략 사유는 계획서 6-1절).
제품 diff **0줄** · `tests/test_docs.py` 에 검사 **1건**(+15줄). 다음은 테스트 1스텝.

## 방금 한 것 (2026-09-01 · 개발 1/1 · 반복 210)

### ① RED 는 3건이 아니라 **13건**이었다 — 계획 실측이 이미 낡아 있었다

계획서 1절 표는 살아 있는 인용을 **3곳**(`index.md` 22·357 · `digest.md` 80)으로
적었다. 검사를 심고 맨몸으로 돌리니 **13곳**이 떴다.

| 파일 | 줄 | 무엇 |
|---|---|---|
| `docs/index.md` | 22 · 358 | 진짜 인용 — **틀린 항목을 가리키고 있었다** |
| `docs/digest.md` | 80 | 진짜 인용 — 오늘은 우연히 맞았다 |
| `docs/index.md` | 30 | 반복 209 계획 phase 가 쓴 **인용 형태 인용** |
| `docs/history_current.md` | 223 | 〃 |
| `docs/status.md` | 56 · 59 | 〃 |
| `docs/plan_docs-citation-guard.md` | 27 · 28 · 29 · 123 · 125 · 145 | 계획서 자신이 금지 형태를 6번 그대로 적었다 |

**차이의 원인이 곧 이 계획의 논지다.** 사람이 손으로 센 전수는 다음 편집까지만
참이고, 그 편집을 한 것이 **계획 phase 자신**이었다. 계획서를 쓰면서 10곳을
새로 만들어 놓고 3곳이라고 적었다. 검사를 심기 전에는 아무도 못 봤다.

### ② 고친 3줄이 무엇을 잘못 가리키고 있었나

- `docs/index.md` **22행** · **358행**: `` 근거 `digest.md:<156>` `[6]` `` →
  오늘 그 줄은 `[7] 살아남은 변이가 형제 구멍` 이다. **남의 항목이다.**
  `` `digest ## 판단 필요` 의 `[6]`(가짜 시계 10곳) `` 으로 고쳤다.
- `docs/digest.md` **80행**: `` `index.md:<24>` 가 `0/2` `` → 오늘은 우연히 맞았다.
  `` `index.md` 의 `plan_indexer-interrupt` 행 `` 으로 고쳤다. **200줄 유지.**

나머지 10곳은 **형태를 말하는 자리**라 내용이 아니라 표기를 바꿨다 — 실제 줄을
가리키는 것이 아니므로 `digest.md:<156>` · `digest.md` 156행 처럼 자리표시자를
쓴다. `digest [6]` 이 자기 항목에서 이미 쓰던 표기(`digest.md:<줄번호>`)와 같다.

### ③ 계획에 없던 파일 하나 — `README.md` (기존 검사가 강제했다)

검사가 1건 늘자 `tests/test_readme.py` 의 `test_verification_counts_match_reality`
가 같이 죽었다(`(466, 19) != (467, 19)`). README 의 단위 건수 주석 **한 줄**을
467 로 고쳤다. 계획서 5절 표에 없는 파일이지만 **기존 테스트가 요구한 수정**이라
YELLOW 로 올리지 않았다. 계획서 4절이 "README 는 검사 대상 밖"이라고 적은 것은
새 검사의 *대상*을 말한 것이고, README 자신의 건수 계약은 별개다.

### ④ 변이 3종 전부 죽었다 (심은 것을 먼저 단언하고, revert 를 `&&` 로 잇지 않았다)

- **M1** `index.md` 22행을 줄번호 인용으로 되돌림 → 새 검사만 FAIL(1건). 되돌림.
- **M2** 아카이브 제외 삭제 → FAIL. 계획서는 아카이브 **5개**를 예상했는데 실제로는
  **8개 파일 27줄**이다(`history_010`·`011`·`015`·`016` · `plan_history_020`·`021`·
  `023`·`024`). 제외 규칙이 실제로 일하고 있다. 되돌림.
- **M3** 금지 정규식의 숫자 요구 삭제(`:` 만 봄) → FAIL. 걸린 것이 **자리표시자
  표기**(`digest.md:<줄번호>` 꼴) 15줄이다 — 숫자를 요구하는 것이 형태를 넓히지
  않는다는 증거이자, ②의 표기 선택이 우연이 아니라는 증거다. 되돌림.

**검증**: 단위 **467건 OK 11.8초**(맨몸 실행, 파이프 0) · `data/crawl.db` sha256
**무변경**(`85c96744…5bda18`) · `docs/digest.md` **200줄 · 49항목**(무변) ·
`git diff --stat` 은 `tests/test_docs.py` · `README.md` · `docs/index.md` ·
`docs/digest.md` + 마감 문서.

## 다음 스텝 (테스트 1/1)

1. 새 검사의 단언이 실제로 무엇을 잡는지 되짚는다 — 특히 **아카이브 이름꼴**이
   회전이 만드는 실제 이름(`history_018.md` 꼴)과 맞는지.
2. 계획서 7절 반증 신호 ①: `docs/` 에 예상 못 한 이름의 `.md` 가 생겼을 때
   검사가 죽는지 — 죽으면 단위가 아니라 루프 운영 계약이다.
3. 회귀: 단위 467건 · e2e 19종 rc 0.

## 한도 (넘으면 RED)

- `data/crawl.db` 실물·스키마 무변경 (sha256 `85c96744…5bda18`).
- 단위 **467건**이 기준선(11.8초). 그 아래면 RED · e2e **19종**이 줄면 RED.
- `docs/digest.md` 열린 항목 **49**(`grep -c '^- \['`) · **200줄 상한**.
- **러너를 파이프로 감싸지 않는다**(`## 반복 실패` **8회**). 이번 반복은
  탐색 grep 과 러너를 갈라 썼고 러너는 3회 전부 맨몸이다.
- 의존성 추가 금지(stdlib 만) · `docs/specs/` 읽기만 · `--no-verify` 금지 ·
  `main` 직접 커밋 금지 · 외부 네트워크 금지.
- **`src/` 는 이 계획에서 0줄이다.** 건드리면 YELLOW.
