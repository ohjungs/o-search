---
signal: GREEN
phase: 개발
step: 2/2
attempt: 0
iteration: 418
updated: 2026-09-07
ctx: 77
night_iterations: 4
night_red: 0
night_retries: 0
plan: digest-rotate-guard
---

## 현재 상태

**개발 2/2 완료 — 회전했다. `digest.md` 216 → 193줄, 상한 200 아래다.**
지운 것은 닫힌 기록뿐이다: 후보 두 절의 취소선 **18줄** + `## 완료` 의 계획별 요약
**7줄**(계획 63~69). 더한 것은 회전 기록 1줄 + 새 후보 1줄. 아카이브 명부·회전 기록·
보류·반복 실패·**열린 후보 58줄**은 한 줄도 안 건드렸다. 전수 **648 OK rc 0**.
다음 스텝은 **테스트 phase** 다.

**회전이 실물 갈래를 하나 열었다.** 계획 70 이 등재한 후보 줄에 규약대로 포인터를
달았더니(`계획 72 `digest-rotate-guard` 로 열었다`) 실물 포인터가 **1개**가 됐고,
그 계획이 `진행` 이라 `strike_gap` 의 **「진행 중이면 안 문다」 갈래가 실물에서 처음
밟혔다**(지금까지 합성에만 표본이 있던 오탐 축이다). 결과 `None` — 조용하다.

**위임 범위를 문서에 정확히 적었다.** 「닫힌 후보를 지운다」는 항구적 정책이 아니라
이 건 한 번의 허가다. `rules/docs.md` 3절 문장은 안 바뀌었고 다음 회전은 다시 사람에게
묻는다. 회전 기록의 첫 문구가 이것을 「정책이 넓어졌다」로 과장해 같은 반복에서 고쳤다.

## 새 못을 다섯 판으로 밟았다 (커밋 안 함 · 전부 원복)

| 판 | 무엇 | 결과 |
|---|---|---|
| ① | 실물 **첫** 후보 절 머리 드리프트 | **RED** `test_candidate_heads_still_found` |
| ② | 실물 **둘째** 후보 절 머리 드리프트 | **RED** 같은 자리 |
| ③ | **회전 시뮬레이션**(취소선 18줄 삭제) | **OK** — 거짓 RED 없음. 이 계획의 목적이다 |
| ④ | 변이 `startswith(CANDIDATE_HEAD)` → `startswith("## ")` | **RED** 합성 `test_heads_do_not_count_other_sections` |
| ⑤ | 변이 하한 `2` → `0` + 드리프트 | **OK — 생존** |

**⑤ 는 살아남았다.** 하한 상수를 0 으로 내리면 못이 이빨을 잃고 아무도 안 운다.
형제 못들(`VERDICT_ROW_FLOOR`)이 똑같이 갖는 성질이고, 닫으려면 «못을 지키는 못» 이
되어 순환한다. **천장으로 적고 후보에 등재한다** — 스텝 2 가 digest 를 여는 김에 넣는다.

## 설계 결정 (반복 416)

**D5: 못을 후보 절 «머리 개수» 위에 세우고 하한 2**(`docs/design_digest-rotate-guard.md`).
가른 것은 탐침 둘 — 코드 상수 변이는 합성 넷이 물지만(4건 RED), **실물 문서 드리프트는
옛 못 하나만 물었다**(1건 RED). 상수와 실물 문서를 잇는 자가 그 못뿐이라 D3(지운다)이
반증됐다. 버린 것: A(오탐 21) · B(회전 뒤 실측치 0) · C(오탐 4) · D1(값 58 — 오늘 사고의
반복) · D2(값 1 — 머리 하나 드리프트를 못 문다).

**설계 6절에서 한 가지를 덜어냈다** — 거기 적힌 `candidate_lines` 분리는 소비자가
`candidate_pointers` 하나뿐이라 안 만들었다. D5 의 두 번째 소비자는 줄이 아니라 머리를
센다. 구현 하나짜리 추상은 다음 편집이 갈라 놓을 짐이다.

## 앞 반복 요약 (반복 415, 계획 수립)

근거는 측정값이다 — `docs/digest.md` 가 **216줄**로 `rules/docs.md` 3절의 상한 **200** 을
넘었다. 출처는 `discover.md` 0절(사용자 지시)이고, 그 지시가 반복 414 가 「사람 결정
대기」로 올린 셋 중 첫째를 결정했다(닫힌 기록에 한한 삭제 위임 포함).

## 계획 phase 가 잰 것 — 처방 넷 중 셋이 실측으로 떨어졌다

| # | 잰 것 | 값 |
|---|---|---|
| 1 | 후보 두 절의 엄격 포인터 | **10개, 10개 전부 취소선**. 열린 포인터 **0** |
| 2 | 후보 절의 취소선 목록 줄 | **18줄** (포인터를 가진 것은 그중 10줄) |
| 3 | 18줄을 지운 뒤 줄 수 | 216 → **198줄** |
| 4 | 18줄을 지운 뒤 전수 | `Ran 647` **failures=1** — `test_pointer_extractor_still_bites` 만 |
| 5 | 회전 뒤 후보 목록 줄 | 76 → **58줄**. 후보 절 머리는 **2개**로 불변 |
| 6 | `## 완료` 의 지울 수 있는 계획별 요약 | **7줄** (아카이브 명부·회전 기록 3줄은 룰이 남기라 한 줄) |

**등재된 처방 B(하한을 실측치에 붙인다)는 오늘 반증됐다.** B 는 포인터 모집단이
append-only 라고 가정했는데 실측 1 이 **10/10 전부 취소선**임을 보인다 — 삭제 정책은
그 모집단을 **영구히 0** 으로 만들고, 하한 0 은 그 못이 막으라고 세워진 「조용한
초록」 자체다. 처방 A(느슨한 백틱 세기)는 계획 70 이 이미 반증했고(오탐 21),
오늘 처음 잰 처방 C(역방향 — `index.md` 완료 슬러그가 후보 절에 없어야)도 회전 뒤
**오탐 4건**으로 떨어졌다(`indexer` 는 제품 모듈 이름과 계획 슬러그가 같은 글자다).
남은 것은 D(못을 회전이 살리는 모집단 위로 옮긴다)이고 **값이 아직 안 갈렸다** —
오늘의 교훈이 「모집단 크기에 못을 붙이면 회전하는 날 거짓 RED」라 58 에 붙이는 것은
같은 실수의 반복이다. 그 갈림이 설계 트리거(「대안이 2개 이상 갈림」)다.

## 덤 — 문구 드리프트 구멍의 실물 표본이 이미 하나 있다

후보 절의 「화면에 안 보이는 텍스트가 «근거 문단» 으로 나온다」 항목은 취소선이 없는데
`index.md` 의 `plan_hidden-passage` 행은 `완료` 다. 그 줄이 「계획 51 `hidden-passage`
**가 앞 절반을 연다**」라고 적혀 있어 엄격 포인터가 안 문다. 다만 **뒷 절반은 진짜로
열려 있어** 닫힌 기록이 아니다 — 이 계획은 손대지 않는다(계획서 「하지 않을 것」).

## 검증 (맨몸 · 리다이렉션 0)

```
PYTHONDONTWRITEBYTECODE=1 PYTHONPYCACHEPREFIX=$(mktemp -d) PYTHONPATH=src python3 -m unittest discover -b -s tests
Ran 647 tests in 16.017s · OK · rc 0
```

시뮬레이션(취소선 18줄 삭제)은 `/tmp` 사본으로 돌리고 **`docs/digest.md` 는 원복했다** —
216줄 그대로다. `src/`·`e2e/` **0줄** · `docs/specs/` 무접촉 · `data/crawl.db` **무개봉** ·
바깥 네트워크 0 · `main` 직접 커밋 0 · 브랜치 `loop/digest-rotate-guard`.
