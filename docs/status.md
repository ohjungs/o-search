---
signal: GREEN
phase: 개발
step: 2
attempt: 0
iteration: 231
updated: 2026-09-02
ctx: 62
night_iterations: 90
night_red: 0
night_retries: 0
plan: docs-record-guard
---

# 현재 상태

**계획 45 `docs-record-guard` 개발 스텝 2/2 완료 — 개발 phase 가 끝났다.**
`tests/test_docs.py` 에 `ArchiveIndexTest` 34줄 + `digest.md ## 완료` 절 머리의
아카이브 명부 한 줄. **오늘 열려 있던 색인 구멍 5개가 닫혔다.**
제품 `src/` 0줄 · 단위 476 → **477건 OK** · `digest.md` **200줄 유지**.
**다음 반복은 테스트 phase.**

## 방금 한 것 (2026-09-02 · 개발 2/2)

**RED 를 먼저 봤다.** 검사만 넣고 돌리니 설계 기준선 그대로 구멍 5개
(`history_001.md`·`002`·`003`·`005`·`019`)가 실패 메시지에 이름으로 찍혔다. 그 뒤
명부를 넣어 초록.

**검사**: 아카이브 `sorted(DOCS.glob("history_[0-9]*.md"))` 전부가 `digest.md` 의
`## 완료` 절에 **경계 매칭 `(?<![A-Za-z_])` + `re.escape(이름)`** 으로 있는가. 절은
`## 완료` 줄부터 다음 `^## ` 직전까지. 눈먼 자리 셋(파일 없음 · 절 없음 · glob 빈손)은
전부 통과가 아니라 실패다.

**200줄 상한은 순증 0 으로 지켰다** — 명부 1줄이 가장 오래된 완료 항목
(`plan_cooldown-burn` (011))의 자리를 그대로 차지한다. 그 항목은 아카이브 이름을
하나도 안 갖고 있어 새 구멍을 안 만들고, 내용은 `index.md` 의 `cooldown-burn` 행이
갖고 있다. 명부를 회전에서 지키는 규약도 머리 주석 줄을 **제자리에서 고쳐** 넣었다
(설계가 대안 4 를 기각하며 요구한 자족성을, 줄을 안 늘리고).

| 변이 | 무엇 | 실물 메시지 |
|---|---|---|
| V5 | 명부에서 `history_002.md` 삭제 | `[] != ['history_002.md']` |
| V6 | 절 이름을 `## 완료된 것` 으로 | `'## 완료' not found in [...]` (눈먼 자리 b) |
| V7 | 명부의 `history_019.md` → `plan_history_019.md` | **RED — 순진 매칭은 산다** |
| V8 | `DOCS` 를 없는 경로로 | `[] is not true : 아카이브를 못 찾았다` |

**V7 이 이 설계가 산 물건이고, 그 하나가 값을 냈다** — 같은 변이에서 순진한 substring
은 구멍 0 으로 살아남는 것을 같은 파일 안에서 대조로 실측했다.

**거짓 초록을 하나 더 밟았고, 그 자리가 하필 변이 되돌림 검증 자체였다.** V8 을 되돌린
뒤에도 스위트가 빨갰다 — 소스는 `docs` 인데 런타임 `DOCS` 는 `nope` 였다. 이 맥의 시스템
`python3`(3.9)이 `sys.pycache_prefix` 를 `~/Library/Caches/com.apple.python` 으로 잡아
**저장소 안에 `__pycache__` 가 아예 없고**, 변이·되돌림이 같은 초에 나며 `"docs"`↔`"nope"`
가 같은 바이트 수라 pyc 무효화 조건(mtime 초 + 크기)이 둘 다 안 걸렸다. **sha256 이
원본과 일치하는데 실행은 변이본이었다** — 파일 해시로 되돌림을 증명하는 기존 절차가
못 잡는 자리다. 처방: 테스트 파일 제자리 변이는 **`python3 -B`** 로 돌린다. 재실측을
그렇게 다시 해서 넷 다 RED·복귀 OK 를 확인했다. **`digest ## 반복 실패` 의 "제자리 변이"
항목이 적어 둔 처방(`.mutation-lock`)이 절반만 맞았다는 뜻이라, 다음 phase 가 그 항목을
넓힐지 판정한다** — 이번 반복은 200줄 상한 때문에 `digest` 를 더 안 건드렸다.

**README 카운트 가드가 이틀 연속 울었다** — `(476, 19) != (477, 19)`, 477 로 정정.

실측(맨몸): **476건 OK → 477건 OK 11.790초** · `src/` **0줄** · 의존성 0 ·
`data/crawl.db` sha256 `85c96744…` 무변경 · diff 는 `tests/test_docs.py` 34줄 ·
`docs/digest.md` 2줄 · `README.md` 1줄.

## 앞 반복 (2026-09-02 · 개발 1/2)

`IterationSyncTest` 24줄. `metrics.md` 의 `반복` 과 `status.md` 의 `iteration` 이
어긋나면(4회 재발) 스위트가 안 통과한다. **데이터가 이미 초록이라 변이 넷으로 RED 를
먼저 만들었다** — V1·V2 는 값 불일치 양방향, **V3·V4 는 `assertIsNotNone` 이 막는
`None == None` 조용한 통과**. `.mutation-lock` 걸고 되돌림까지 실측했고, README 카운트
가드가 `(475, 19) != (476, 19)` 로 즉시 울어 476 으로 정정했다. 단위 475 → 476건 OK.

## 다음 반복이 할 것 — 테스트 phase

개발 두 스텝이 끝났다. **설계 5절 변이표(V1~V8)는 소진했으니 표 밖을 찾는다** —
이름이 명부 아닌 곳(회전 줄 · `## 반복 실패`)에만 있을 때 · `## 완료` 가 두 번 나올 때 ·
회전이 새 아카이브를 만들고 명부를 안 고칠 때 · `metrics.md` 표에 `| 반복 |` 이 두 행일 때.
**변이는 `.mutation-lock` + `python3 -B` 로 돌린다**(위 거짓 초록).

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-01 확인)

`origin/main` 은 `494313b`(PR #5 병합). 계획 43·44 의 커밋들(`bc40ea9`~`79952ee`)이
아직 안 갔다. **남은 것 (사람 결정)**: 그것들을 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 — 둘이 열려 있다 (하나가 닫혔다)

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
   offset 이 0 이 되는 날 검사기가 멈추고, 짝은 그때 열릴 계획의 일이다.
3. ~~기록 3파일 불일치~~ — **계획 45 가 열었다** (설계까지 왔다).
## 한도 (매 반복 확인)

- `main` 직접 커밋 금지 · `--no-verify` 금지 · 외부 네트워크 금지 ·
  `docs/specs/` 읽기만 · `data/crawl.db` 무변경(sha256 확인) · 의존성 추가 금지(stdlib 만).
- **러너 파이프 — 연속 카운트가 2 에서 끊겼다. 이번 반복은 썼다.** 변이 넷의 판정 줄만
  뽑으려고 `| tail -1`·`| grep -E` 를 썼고, 한 번은 `| tail -3` 이 **판정 줄 대신 테스트가
  찍은 표준출력을 보여줘** 실제로 빨강/초록을 못 봤다(즉시 맨몸·`grep '^(Ran|OK|FAILED)'`
  로 다시 봤다). `digest` 가 적어 둔 그 방아쇠 그대로다. **최종 확인 두 번은 맨몸**이고
  결론(477건 OK)은 맨몸 실행에서 읽었다.
- **변이는 제자리 편집이었다** — `.mutation-lock` 을 걸고 했고 되돌린 뒤 지웠다.
  되돌리기를 `&&` 로 잇지 않았다. **되돌림 증명은 sha256 만으로 부족했다**(위 거짓 초록) —
  `python3 -B` 로 재실측해 복귀 OK 를 확인했다. 작업 트리에 `.mutation-lock` 없음.
- **계획 42 의 검사가 이 반복의 기록도 통과시켰다** — `digest.md`·`index.md`·
  `history_current.md` 를 항목 이름으로만 가리켰다(`cooldown-burn` 행 · `## 완료` 절).
- 이번 반복 최종 실측(맨몸): `PYTHONPATH=src python3 -m unittest discover tests`
  **477건 OK 11.790초** · `src/` **0줄** · 의존성 0 ·
  `data/crawl.db` sha256 `85c96744…` 무변경.
- **집안일 없음** — `history_current.md` 는 이 기록을 붙여 **262줄**(상한 300) ·
  `digest.md` **200줄**(상한 200 유지 — 명부가 완료 항목 1줄의 자리를 그대로 썼다) ·
  아카이브 **22개** · **색인 구멍 0개**(스위트가 붙든다).
