# e2e 결과 — 계획 57 `passage-cost-axis` (2026-09-05 · 반복 338)

브랜치 `loop/passage-cost-axis` · 기점 `e66968c` · 직전 커밋 `7be48c2` · **새로 만든 e2e 파일 0개**.
러너 명령은 전부 맨몸이다 — 파이프·리다이렉션 **0회**(`rules/e2e.md` 7절).

## 1. 선행 관문 셋 (`rules/e2e.md` 1절)

| 관문 | 명령 | 결과 |
|---|---|---|
| 전체 검증 | `PYTHONPATH=src python3 -m unittest discover -b -s tests` | **`Ran 605 tests` · `OK` · 13.837초 · rc 0** |
| 린트·타입체크 | `docs/project.md` 에 **없음** | 해당 없음 — "통과" 라고 쓰지 않는다 |
| CI | `.github` 부재 | 해당 없음 (1절 2항) |
| 품질 기준 | 아래 3절 | 전 축 통과 · 기준선 회귀 0 |

## 2. 새 e2e 파일 면제가 여전히 옳은가 — **옳다**

계획 5절이 새 e2e 파일을 배제한 근거는 «프로세스 경계에서 잴 것이 없다» 였다.
이 계획이 만진 것은 `tests/test_indexer.py` 의 **단언 셋과 주석**, `src/websearch/indexer.py`
의 **주석**, 문서뿐이다 — 실행 경로 0줄. 프로세스 밖에서 달라질 동작이 없다.
반대로 계획이 여는 **안쪽 상한**(캡 최악 모양의 재파싱 비용)은 이미
`e2e/passage_eval.py` 가 실물 코퍼스로 `/passages` p95 를 재고 있어, 새 시나리오를
더하면 같은 것을 두 번 재는 자리가 된다(ponytail 1번). **면제 유지.**

## 3. 기존 21종 전수 — 전부 `rc 0`

| # | 시나리오 | rc | 본 것 |
|---|---|---|---|
| 1 | `crawl_e2e` | 0 | 수집 15 · 차단 0 · 최소 간격 1.005s |
| 2 | `indexer_e2e` | 0 | 3문서 색인 · 한/영 정답 매치 · 증분 0문서 |
| 3 | `noindex_e2e` | 0 | 4페이지 중 2문서 · 뒤늦은 noindex 제거 |
| 4 | `search_api_e2e` | 0 | 200/400/404/501/503 · DB 고장 500 |
| 5 | `crawl_delay_e2e` | 0 | `Crawl-delay: 2` 준수 |
| 6 | `non_ascii_e2e` | 0 | 퍼센트 인코딩 경로 왕복 |
| 7 | `tokenizer_e2e` | 0 | crawl→색인→CLI·HTML · AND 질의 |
| 8 | `domain_key_e2e` | 0 | 같은 서버 4회 간격 2.01s · robots 1회 |
| 9 | `deadline_e2e` | 0 | 예산 2초에서 8페이지 · 버려진 응답 0건 |
| 10 | `interrupt_e2e` | 0 | SIGINT rc 130 · DB 무손상 |
| 11 | `indexer_interrupt_e2e` | 0 | SIGINT rc 130 · 색인 0 |
| 12 | `hidden_passage_e2e` | 0 | 숨김 문단 0/5 · 오탐 대조군 4종 무반응 |
| 13 | `crawl_politeness_e2e` | 0 | 예외 뒤 최소 2.01s · 대조군 1.00s |
| 14 | `retry_interval_e2e` | 0 | https 재시도 최소 5.01s |
| 15 | `url_normalize_e2e` | 0 | 표기 10개 → 문서 6개 |
| 16 | `pagination_ui_e2e` | 0 | 페이지네이션 화면 |
| 17 | `design_check` | 0 | 4축 전부 통과 (아래) |
| 18 | `passage_eval` | 0 | 정확도 100.0% · p95 1.51ms |
| 19 | `quality_eval` | 0 | ko 20/20 · en 19/20 |
| 20 | `perf_search` | 0 | p95 8.96ms |
| 21 | `perf_crawl` | 0 | 10.23/s(열림) · 10.24/s(차단) |

### 기준선 대조 (계획 56 e2e) — **회귀 0**

| 축 | 계획 56 | 오늘 | 판정 |
|---|---|---|---|
| 근거 문단 정확도 | 100.0% | **100.0%** (398/398) | 동일 |
| 채택률 | — | 99.5% (기록만) | 사양에 선 없음 |
| `/passages` p95 | 1.53ms | **1.51ms** | 동일 범위(예산의 0.3%) |
| `perf_search` p95 | 8.95ms | **8.96ms** | +0.01ms — 노이즈 |
| 품질 ko | 20/20 | **20/20** | 동일 |
| 품질 en | 19/20 | **19/20** | 동일 (`loaf` 미검출 그대로) |
| 크롤 처리량 | 10.30/s | **10.23/s** | −0.7% · 기준 5.0 대비 여유 2배 |
| 디자인 4축 | 통과 | **통과** | LCP 대리 1443·1755 · JS 0B · 대비 전 쌍 OK · 360px 고정폭 0건 |

**8단계 체크리스트(4절)는 Playwright 웹 UI 용이라 이 저장소에 해당 항목이 없다.**
브라우저를 안 들이는 대신 서 있는 대리가 둘이다 — `pagination_ui_e2e`(화면 HTML)와
`design_check`(4축·대비·고정폭). **7번 반응형만 여전히 «실제로 그려 본 사람 0»** 이고,
그것은 이 계획이 만든 빚이 아니라 `status.md` 의 사람 결정 대기 3번이다.

## 4. 오늘 다시 잰 것 — 계수와 변이 넷

**변이·실측은 저장소 밖 스크래치패드 사본에서만 돌리고 지웠다**(`src`·`tests` 사본 ·
매 실행 `PYTHONPYCACHEPREFIX` 새 임시 디렉터리 · 끝에 `diff -r` 로 원복 확인 후 삭제).

- **계수 재측정** — 캡을 채운 최악 모양(`<p>` 반복 · 태그 333개/1000자), idle
  min-of-3 × 5 를 두 벌: `0.973 0.974 0.941 0.946 0.950` / `0.942 0.931 0.961 0.964 0.943`.
  **표본 0.931~0.974.** 어제 기록(0.917~0.970)과 합쳐 20표본 **0.917~0.974**.
- **변이 넷** — 대상 `TestPassages.test_cap_and_passage_limit_together_stay_inside_the_budget`

| 변이 | 결과 | 어느 단언이 잡았나 |
|---|---|---|
| M1 `extract_blocks` 앞 1.0ms/1000자 지연(≈2배) | `FAILED` | ② 계수 상한 — `2.154 > 1.60` |
| M2 `MAX_PASSAGE_HTML` 100,000 | `FAILED` | ① 산술 — `1000.0 > 500` |
| M3 `PASSAGE_LIMIT` 100 | `FAILED` | ① 산술 — `3500.0 > 500` |
| M4 `MAX_PASSAGE_HTML` 50,000 | `FAILED` | **①-b 상수 동결** — `500000 > 350000` (① 은 정확히 500.0 이라 초록) |

M4 가 **①-b 가 왜 있는지**를 오늘도 재현했다 — 계수를 1.00 으로 올려 ① 이 허용하는
`캡 × PASSAGE_LIMIT` 이 오늘의 **+42.9%** 로 좁아졌는데도, 캡 50,000 은 ① 의 부등호에
정확히 걸터앉아 통과한다. 원복 뒤 대상 테스트 다시 `OK` · rc 0.

## 5. 리뷰 findings 처리 (반복 337)

- **[R57-1]** — 세 자리의 `0.95` 는 «한가할 때 0.939~0.970» 을 덮는 상한이라고 적었는데
  오늘 표본이 그것을 넘었다(최대 0.974). 리뷰가 제안한 **0.97 도 오늘 실측에 깨진다.**
  → **1.00 으로 올렸다**(표본 위로 올림 · 「낮은 쪽으로 안 내린다」 정책 유지).
  ① 은 **350ms · 예산의 70%** 가 되고 여전히 초록이며, 상수 변이에는 **더 조여진다**.
- **[R57-2]** — `tests/test_indexer.py` 의 주석에서 이미 사라진 «배정치 1/3» 과 현재형
  `0.44` 서술을 걷어내고 오늘의 세계로 다시 썼다(한도 500 정각 · 계수의 세 번 낡음은
  이력으로만 남김).
- **[R57-3]** — `docs/design_passage-cost-axis.md` 2절에 **①-b 를 계약에 추가**하고,
  「② 가 ① 의 리터럴을 붙든다」를 **「1.60 위에서만 붙든다 · 1.00~1.60 은 열린 창」**
  으로 정정했다. 24행의 67%/1.5배도 70%/1.4배로 맞췄다.

## 6. 완료 기준 대조 (계획 4절 · 오늘 다시 재서)

| # | 기준 | 오늘 실측 | 판정 |
|---|---|---|---|
| 1 | 전수 `OK` · rc 0 | `Ran 605 tests` · `OK` · rc 0 · 13.837초 | **충족** |
| 2 | M1 에서 빨개진다 | `FAILED` (2.154 > 1.60) | **충족** |
| 3 | M2·M3 에서 빨개진다 | `FAILED` (1000.0 / 3500.0 > 500) | **충족** |
| 4 | 스위트 증가 < 1.0초 | 착수 전 13.655s → 오늘 13.837s (+0.18s) | **충족** |
| 5 | `README.md` 단위 건수 일치 | `test_verification_counts_match_reality` `OK`(테스트 수 변동 0) | **충족** |
| 6 | 세 기록 자리 숫자 동일 | `indexer.py:44,47` · `project.md:186,187` · `test_indexer.py:800,806` 전부 **1.00 / 350ms / 70%** · 옛 숫자(0.95·332.5·67%) 잔여 0건 | **충족** |
| 7 | DB·`docs/specs/` 무변 · 의존성 0 | `data/crawl.db` sha256 `85c96744…5bda18`(앞뒤 동일) · `docs/specs/` 무접촉 · 새 의존성 0 · 스키마·재색인 0 | **충족** |

**7/7 충족 → `status.md` signal `DONE`.**

## 7. 새로 만든 파일

`docs/e2e/passage-cost-axis/result.md` (이 문서) 하나. e2e 시나리오 파일 0개.
