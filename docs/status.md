---
signal: GREEN
phase: 리뷰
step: 6
attempt: 0
iteration: 269
updated: 2026-09-02
ctx: 62
night_iterations: 113
night_red: 1
night_retries: 0
plan: passage-api
---
# 현재 상태

**갈림길 6 이 닫혔다 — 리뷰 5 가 못박은 둘만 했다.**
활성 계획 `passage-api` · 설계서 `docs/design_passage-api.md` · 브랜치 `loop/passage-api` ·
기점 `6e99031`. **단위 571건 전부 초록**(맨몸 단독 실행 확인, 13.4초) · 제품 `src/` 두 파일
(`extract.py`·`indexer.py`) · 새 파일 0 · 의존성 0 · 스키마·마이그레이션 없음 ·
`docs/specs/`·`data/crawl.db` 무변경 · `.mutation-lock` **0**.

## 이번 반복이 한 일 (개발 9)

**RED 를 먼저 봤다.** 리뷰 5 의 두 표를 `_SHAPES` 에 심자 **3행이 즉시 빨갛고**,
인라인 7모양 표는 **7 중 6이 빨갛다**. 고치기 전에 눈으로 읽었다.

**[R5-1] `br` 리터럴 → `_NON_BLOCK_TAGS` 집합.** `_SKIP_TAGS` 셋 + `_INLINE_TAGS` 밖
phrasing 태그(`img`·`del`/`ins`·`label`·`font`·폼 컨트롤·`iframe`·`svg` 등 26개)를
«문단 경계가 아닌 태그» 로 묶었다. `_separate(block=...)` 의 인자로만 흐르므로 색인 경로는
그대로다 — 기점 대조 **2,496건**에서 `extract_text`·`is_noindex` 불일치 **0건**,
불변식이 갈린 128건은 **문서마다 정확히 둘**이고 둘 다 docstring 의 예외 ①(EOF 잔여 마크업)이다.

**[R5-2] 표를 대칭으로 만들자 개발 8 이 고른 손잡이가 떨어졌다.** `_SHAPES` 8 → **11행**
(뒤·긴 푸터 / 앞·긴 사이드바 / 긴 내비 + 문단 안 `<script>`). 그 위에서 다시 잰 값:
개발 8 그대로 **8/11** · [R5-1] 만 6/11 · **[R5-1]+길이(안 1) 8/11** ·
[R5-1]+뒤집은 이름표 5/11 · **[R5-1]+`tag == "p"` 11/11 채택**.
여덟 행에서 6/8 을 받던 「길이」가 방향을 짝지으니 8/11 이다 — **이겼던 것이 아니라 한쪽만
재고 있었다.** 길이도 순서도 방향이 하나뿐이라 네 방향(앞·짧다/앞·길다/뒤·짧다/뒤·길다)을
동시에 못 진다. 그것을 가르는 이름표를 **HTML 이 이미 갖고 있다**: 「이 블록이 문단이었나」.
그래서 `extract_blocks()` 가 `(태그, 텍스트)` 를 내고 동점 키가 `(점수, 태그가 p 인가)` 다.
**안 1(길이)은 철회했다.**

**변이 여섯 심어 여섯 다 죽었다** — N1 집합을 `{"br"}` 로 되돌리기 7건 · N2 집합에 `p` 섞기
**23건** · N3 동점 규칙 삭제 6건 · N4 이름표 뒤집기 7건 · N5 `>` → `>=` **1건** ·
N6 이름표를 부모 호출 **앞에서** 갈아 끼우기 **1건**. N5·N6 이 1건인 것은 그 자리만 재는
단언이 각각 하나씩이기 때문이다(개발 8 의 Z2 관측과 같은 결).

## 넘긴 것 — 리뷰 6 이 볼 것은 둘뿐이다

1. **태그 집합이 색인 경로에 안 닿는가** — 2,496건 밖의 반례.
2. **이름표 동점 규칙이 11행 표 밖에서 새 편향을 만드는가** — 예: `<p>` 로 감싼 광고 문구가
   진짜 문단을 이기는 자리(리뷰 5 가 「길이」에서 잡아낸 것과 같은 형태의 되물음).

**범위는 그대로 못박혀 있다.** 인라인 연접(`김치찌개된장찌개`) · 갈림길 5 의 캡 ·
`_SKIP_TAGS` 를 색인 경로에서 빼는 것은 **계획 48 에서 다시 열지 않는다**(`digest` 후보).
닫히면 **e2e 20종 전수로 보내 DONE 으로 닫는다.**

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-02 확인)

`origin/main` 은 PR #6 으로 계획 44~47 까지 받았다. 계획 48 의 커밋들은 브랜치에만 있다.
**이번 반복도 `origin/loop/passage-api` 로 푸시했다** — 푸시는 루프의 정상 절차다
(강제 푸시만 금지).
**남은 것 (사람 결정)**: 계획 48 이 끝난 뒤 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 — 넷이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, `~/.claude/skills/loop-harness/rules/docs.md` 에는 그 예외가 없다.
   **저장소 밖이라 루프가 못 고친다.** 이번 반복이 저장소 안쪽만 일원화했다(아래 5번 참조).
4. **«러너 호출을 다른 것과 섞지 않는다» 를 문장이 아니라 훅으로 옮기는 일.**
   재발 **25회 — 이번 반복(개발 9)에 셋을 밟았다. 두 반복 연속으로 한 반복에 셋씩이다.**
   ① 변이 국면 진입 전 `2>/dev/null | tail -5` · ② 변이 N3 아래 `2>&1 | tail -40` ·
   ③ 후보 측정에서 `python3 -m unittest tests.test_indexer` 로 **모듈 하나만**(스위트 거르기).
   셋 다 맨몸·단독으로 다시 돌려 판정 줄을 눈으로 읽었고, ② 는 **변이를 다시 심어** 같은
   6건 실패를 재확인했다. **방아쇠가 셋 다 «출력이 길다» 로 같다** — 이 스위트는 크롤 로그가
   화면을 덮는 것이 상수다. 저장소 쪽 자리(`docs/project.md ## 명령`)에 적혀 있는데도 났고,
   남은 자리는 `~/.claude/skills/loop-harness/rules/` 라 **저장소 밖이라 루프가 못 고친다.**
   **처방 후보가 하나 늘었다** — 잡음이 방아쇠라면 막을 자리는 손이 아니라 **러너의 출력**이다:
   판정 줄을 마지막에 다시 찍는 얇은 래퍼면 «가릴 이유» 자체가 없어지고, 그것은
   **저장소 안이라 계획으로 열 수 있다.** 판정 명령을 단독 실행으로 강제하는 PreToolUse
   검사는 여전히 저장소 밖 몫이다.

**5번이 닫혔다 — `history_current.md` 는 418 → 204줄이다.** 세 반복 연속 «회전으로는 300줄
아래로 못 내린다» 였던 원인은 항목이 아니라 **머리에 쌓인 회전 명부 233줄**이었다.
그 명부는 `digest.md` 의 `## 완료` «아카이브 명부» 줄과 내용이 겹치고 **검사
(`ArchiveIndexTest`)가 강제하는 것도 그쪽 하나뿐**이라, 이번 반복이 저장소 안에서
일원화했다 — **지운 것은 머리말이지 반복 기록이 아니다**(항목은 그대로 append 전용·수정
삭제 금지고, 그 규약을 두 파일 머리에 적었다). **저장소 밖 룰 파일은 안 건드렸다** —
그 갈림은 위 3번으로 남아 있다.

**사양이 사람에게 남긴 것 셋** (`docs/specs/concept.md` 의 `## 사람이 정할 것`):
속도 제한 시점(가정 — IP 당 분당 60회) · JSON 검색과 근거 문단의 분할(가정 — 쪼갠다,
**계획 46 이 그 가정을 따랐고 닫았다**) · 사양 숫자들이 초안이라는 것(90% · 500ms ·
60회/분). 첫째와 셋째는 아직 열려 있다.
