---
signal: GREEN
phase: 리뷰
step: 1/1
attempt: 0
iteration: 369
updated: 2026-09-06
ctx: 38
night_iterations: 186
night_red: 2
night_retries: 4
plan: anchor-net-cover 계획 63 (리뷰 1/1 완료 · 다음은 e2e)
---

## 현재 상태

**백지 패스가 이슈 하나를 잡았고 그 자리에서 고쳤다.** 개발이 끼운 잡음 행의 값이
표 안의 다른 행과 겹쳐, **서로 다른 변이 둘이 같은 관측(`9/9`)을 내고 있었다.**
값을 갈라 놓고 실패 메시지를 값→범인 대응으로 다시 적었다 — 판정은 0줄 바뀌었다.
승인 필요 항목 **0건**이라 개발 phase 로 안 돌아간다.

## 리뷰 — 이슈 1건 (critical 0 · informational 1)

**[자동 수정 · 적용함] `tests/test_docs.py` — 잡음 행의 `9/9` 가 ① 행의 `9/9` 와 겹쳤다.**

`StepPatternTest.TABLE` 의 ① `plan_endtag-cut-cover` 행이 이미 `9/9` 였다. 그래서
`test_row_is_picked_by_exact_slug` 가 `9/9` 로 실패했을 때 범인이 둘이었다:

| 관측 | 범인 A | 범인 B |
|---|---|---|
| `9/9` | 슬러그를 안 보고 앞 행을 물었다 | `^` 가 죽어 줄 중간 잡음 행을 물었다 |

**한 값이 두 원인을 가리면 진단이 아니다.** 게다가 실패 메시지의 원인 열거가 새로
들어온 잡음 행을 안 담아 *"`plan_index-step-sync-2` 를 접두로 집었다"* 라는 **틀린
범인을 지목**하고 있었다(`rules/review.md` 3절의 열거형 완전성).

고침 — 6줄:

- 잡음 행 `9/9` → **`8/8`**. 표 안의 넷이 `9/9`·`3/7`·`8/8`·`1/1` 로 전부 다르다.
- `StepPatternTest` 메시지를 값→범인 대응으로: `9/9` 면 슬러그, `3/7` 이면 접두,
  `8/8` 이면 `^`.
- `IterationPatternTest` 메시지에 `999`(= `^` 사망)를 열거에 넣었다.
- 클래스 주석에 **①과 ④의 수를 다르게 두는 것이 조건**임을 남겼다.

## 검증 — 리뷰가 직접 다시 걸었다

남의 관찰을 안 받았다. 저장소 밖 하네스(`scratchpad/rev63.py`)로 여덟 항목:

| 항목 | 결과 |
|---|---|
| M1a `"x step: 1/1"` · M1b `"step: 1/1x"` · M2 `"x plan: a"` | 변이면 매치 **PASS** |
| M3 변이가 `999` 를 집는다 / 실물은 `232` | **PASS** |
| M4 변이가 `8/8` 을 집는다 / 실물은 `1/1` | **PASS** |
| 없는 슬러그는 여전히 `None` | **PASS** |

전수를 고치기 전·후로 두 번 돌려 `Ran 620 tests · OK · rc 0` 무변.
`README.md:104` 「단위 620건」이 그대로 맞다.

## 테스트 phase 가 남긴 세 물음

1. **다섯 줄이 실물 판정을 바꾸는가** — 안 바꾼다. `232`·`1/1`·`None` 실측.
2. **잡음 행의 «대상 행 앞» 순서 의존이 주석에만 있는가** — 그렇다, 그대로 뒀다.
   안에서 재려면 저장소 **안** 에 변이 하네스를 들여야 하고 그건 이 계획의 범위가
   아니다. 대신 값 넷을 전부 다르게 만들어 **순서가 깨지면 실패 메시지가 범인을
   집는다** — 순서 의존을 없애진 못해도 조용히 죽지는 않는다.
3. **계획서 4절의 정정이 단언을 낮춘 것인가** — 아니다. 없앤 것은 「건수가 는다」는
   예측뿐이고 전수 초록·README 일치는 그대로 요구한다. 대조가 살아 있는 것은 테스트
   phase 가 `test_readme.UNIT_COUNT` 변이로 이미 쟀다.

## 하지 않은 것

- **판정 로직 0줄** — 정규식 넷도 `src/` 도 안 건드렸다. 고친 것은 잡음 행의 값
  하나와 실패 메시지 둘, 주석 한 줄이다.
- **`PLAN_SLUG` 에 `$` 안 붙였다 · `step_gap`/`iter_gap` 안 합쳤다** — 계획서 5절이
  이름으로 거부한 둘이다.
- **`README.md` 무접촉** — 건수 620 무변이라 고칠 것이 없다.
- **`digest.md` 무접촉** — 200줄 정각이고 리뷰가 새로 등재할 갭이 없다.

## 다음

**e2e phase.** 21종 실물 명령이 rc 0 인지 보고 완료 기준 8개를 끝까지 대조한다.
특히 7번(범위 무접촉 · `data/crawl.db` sha256)과 8번(`status.step` ↔ `index.md`
63번 행)은 커밋마다 이미 맞춰 왔으니 e2e 는 실물로 한 번 더 찍기만 하면 된다.

## 한도

- 병합은 사람 몫이다 — 계획 57~63 의 커밋이 `loop/passage-cost-band` 에 쌓여 있고
  `origin/main`(`d1fe3e9`) 무접촉 · PR 0(만들지도 조회하지도 않았다).
- `--force`·`--amend`·`rebase` 없음. 스텝 하나 커밋 하나로 그대로 쌓았다.
- 러너에 리다이렉션·파이프를 안 붙인다 — 오늘도 위반 **0회**(누적 38 유지).
- `PYTHONDONTWRITEBYTECODE=1` 과 `PYTHONPYCACHEPREFIX=$(mktemp -d)` 를 함께 줬다.
- 변이는 저장소 밖 `mock.patch.object` 로만 — `git status --porcelain` 은 하네스를
  도는 내내 빈손이었다.
- `history_current.md` 는 116줄에서 이번 항목을 더해 **144줄**(상한 300 · 다음 회전
  번호 `history_065.md`).
