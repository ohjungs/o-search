---
signal: DONE
phase: e2e
step: 4/4
attempt: 0
iteration: 433
updated: 2026-09-07
ctx: 40
night_iterations: 0
night_red: 0
night_retries: 0
plan: passage-prefer-body
---

## 현재 상태

**계획 74 `passage-prefer-body` 마감.** digest 후보 `[8]`(무엇이 뽑히나)·`[6]`(이름표를
쓰나)을 실물로 판정했다. **착수 전제가 스텝 1 에서 깨졌고, 축이 바뀌었다.**
근거 문단 **73→96건** · 본문 문단 **41.1→81.2%** · 전수 **655 OK rc 0**.

## 무엇이 뒤집혔나

**「실물 88.4% 가 `<p>` 로 회수 가능」이 틀렸다.** 계획 73 에서 그 숫자를 낸 스크립트는
**캡 없이** 파싱하고 「질의어를 담기만」 하면 셌다 — 실물 선택기는 35,000자만 파싱하고
**점수로** 고른다. 같은 자로 재면 회수 가능은 43건 중 **3건**, 점수로 이기는 것은 **0건**.

**그래서 규칙 넷(`p` 우대·컨테이너 감점·이름표 가중·본문 밀도)을 다 재고도 안 고쳤다.**
현행과 달라지는 문서가 1·3·8건뿐이라 손잡이가 없었다. **볼 것이 없던 게 아니라 볼
자리가 잘려 있었다** — `MAX_PASSAGE_HTML` 이 35,000 **바이트**인데 실물 중앙값이
159,008 이라 **92% 가 잘렸고**, 남은 앞부분은 `<head>`·내비·목차다.

## 처방 — 자를 바이트에서 태그로 바꿨다

비용은 **태그**에 비례한다(실물 4.08 · 적대적 2.78 ms/1k태그, 1.5배). 바이트로 재면
18배로 흔들린다(0.052 대 0.93) — **바이트 캡은 적대적 페이지로 눈금을 잡아 실물을 깎고
있었다.** `MAX_PASSAGE_TAGS = 3_000` (실물 태그 중앙값 3,225 = 「중앙값 페이지를 통째로
읽는」 지점) · `_clip_by_tags()` 는 1.13MB 에 0.70ms 로 파싱의 1%.

| | 옛 바이트 35,000 | **태그 3,000** |
|---|---|---|
| 근거 문단 | 73건 | **96건** |
| 본문 `p` | 41.1% | **81.2%** (p78 li7 div6 td3 dt1 pre1) |
| `/passages` p95 | 43ms (예산 8.7%) | **138.10ms (27.6%)** |
| `/search` p95 | — | 15.69ms (예산 5.2%, 안 건드렸다) |
| 최악 모델 10건 | 350ms | **120ms** |

**최악 상한은 넓힌 게 아니라 좁혔다.** 예산을 8,000 까지 올려도 근거는 2건(96→98)만 더
오고 값은 예산의 +21%p 라 **품질 고원의 무릎**에서 멈췄다.

## 남긴 것

- **블라인드 배선이 값을 냈다** — 규칙 이름을 가리고 블록을 라벨했기에 「내 규칙이 이기는
  라벨」을 못 만들었고, **규칙 넷이 전부 무의미하다**는 결론에 닿았다.
- **계수가 세 번 낡았던 이유가 풀렸다**(digest `[7]`) — 흔들린 것은 파서가 아니라 **자**다.
- **틀린 전제를 계획서에 남겼다**(4-a절) — 계획 73 의 5-a절과 같은 처리다.
- 전문: `docs/e2e/passage-prefer-body/result.md`
