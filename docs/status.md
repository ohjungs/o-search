---
signal: GREEN
phase: 설계
step: 1
attempt: 0
iteration: 229
updated: 2026-09-02
ctx: 58
night_iterations: 89
night_red: 0
night_retries: 0
plan: docs-record-guard
---

# 현재 상태

**계획 45 `docs-record-guard` 설계 완료 — `docs/design_docs-record-guard.md`.
갈림길 둘을 닫았다. 코드 0줄.**
**다음 반복은 개발 스텝 1** — 계획서 3절의 `IterationSyncTest`.

## 방금 한 것 (2026-09-02 · 설계)

계획서 5절의 갈림길 둘을 실측으로 닫았다.

**① 범위 = `digest.md` 의 `## 완료` 절만.** `digest` 전체를 보면 구멍이 4개인데,
001 을 초록으로 만드는 것이 **구멍을 신고하는 그 문장 자신**이다(`## 반복 실패` 의
*"회전을 돌리면서 그 아카이브의 `digest` 압축 줄을 안 남긴다"* 항목). 거짓 초록 계열의
네 번째라 여기서 끊는다. 절만 보면 정직하게 **5개**(001·002·003·005·019).

**② 색인 = `## 완료` 절 머리의 "아카이브 명부" 한 줄.** 본체는 **두 룰의 정면 충돌**이다 —
`rules/docs.md` 는 *"200줄을 넘으면 완료 항목부터 지운다"* 고 하고 새 불변량은 *"모든
아카이브 이름이 완료 절에 있어야 한다"* 고 한다. **이름 색인(명부 1줄)** 과 **판단 재료
요약(완료 항목들)** 을 분리하면 완료 항목 삭제가 다시 안전해진다. 명부는 회전 1회당
**약 18자**만 늘고 줄 수는 영원히 1이다. 넣을 1줄은 가장 오래된 완료 항목 1줄을 지워
만든다 — 실측으로 그 두 항목(`cooldown-burn`(011)·`normalize-gaps`(019))은
**맨 아카이브 이름을 하나도 안 갖고 있어** 삭제가 공짜다.

**설계 탐침이 계획서의 실측을 두 군데 확대했다** (전부 메모리 · 저장소 무수정):

1. **대안 3(살아있는 문서 어디든)이 완전히 죽었다.** 오늘 구멍 0 인데, 002·005 를
   이름으로 붙드는 자리가 `index.md` 의 계획 45 행 · `history_current.md` 의 이번 기록 ·
   `status.md` **셋뿐**이고 셋 다 이 계획이 직접 쓴 문장이다. 자기가 쓴 글을 자기가
   읽는 검사다.
2. **접두 오인 자리가 8곳이다**(`digest.md` 3 · `history_current.md` 1 · `index.md` 4).
   가장 뚜렷한 것은 **`index.md` 의 `history_022.md`** — 접두 형태 1회 · 맨 이름 **0회**라
   순진 매칭이면 **오늘 이미 거짓으로 잡고 있다.** 경계 매칭을 재는 변이 V7 을 계약에 박았다.

## 다음 반복이 할 것 — 개발 스텝 1

`tests/test_docs.py` 에 `IterationSyncTest` 하나. 설계서 4절 표가 계약이다 —
`metrics.md` 의 `^\| 반복 \| ([0-9]+) \|` 와 `status.md` 의 `^iteration: ([0-9]+)$`,
**매치가 `None` 이면 비교 전에 실패**. 변이 V1~V4 를 눈으로 RED 로 본 뒤 되돌린다.
스텝 2(`ArchiveIndexTest` + `digest.md` 명부)는 의존이 없어 순서를 바꿔도 된다.

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-01 확인)

`origin/main` 은 `494313b`(PR #5 병합). 계획 43·44 의 커밋들(`bc40ea9`~`79952ee`)이
아직 안 갔다. **남은 것 (사람 결정)**: 그것들을 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 — 둘이 열려 있다 (하나가 닫혔다)

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
   offset 이 0 이 되는 날 검사기가 멈추고, 짝은 그때 열릴 계획의 일이다.
3. ~~기록 3파일 불일치~~ — **계획 45 가 열었다** (설계까지 왔다).

## 한도 (매 반복 확인)

- `main` 직접 커밋 금지 · `--no-verify` 금지 · 외부 네트워크 금지 ·
  `docs/specs/` 읽기만 · `data/crawl.db` 무변경(sha256 확인) · 의존성 추가 금지(stdlib 만).
- **러너 파이프 0 — 연속 카운트 1.** 이번 반복은 러너를 **맨몸으로 한 번만** 불렀다.
  직전 반복(⑬)이 `2>&1 | tail -3` 으로 **빨강을 가렸던** 바로 다음 반복이라, 설계서를
  다 쓴 뒤의 재확인도 파이프 없이 돌렸다 — 방아쇠였던 "이미 초록일 것 같은 실행" 그것이다.
  ※ 문서를 잘라 읽는 데는 `python3 - <<EOF` 를 썼다 — 러너가 아니라 판정 줄도 종료 코드도
  없다. 세지 않되 적어는 둔다.
- **변이는 전부 메모리 사본에서 했다** — 저장소 파일을 한 번도 안 고쳤으므로
  `.mutation-lock` 이 필요 없었고 제자리 변이 0건이다.
- **계획 42 의 검사가 설계서를 통과시켰다.** 설계서를 쓸 때부터 `digest.md`·`index.md`·
  `history_current.md` 를 **항목 이름으로만** 가리켰고 `DocCitationTest` 는 처음부터
  초록이었다 — 직전 반복은 같은 자리에서 두 곳을 잡혔다.
- 이번 반복 최종 실측(맨몸): `PYTHONPATH=src python3 -m unittest discover tests`
  **475건 OK 11.927초** · `src/` **0줄** · 의존성 0 ·
  `data/crawl.db` sha256 `85c96744…` 무변경.
- **집안일 없음** — `history_current.md` 는 이 기록을 붙여 **178줄**(상한 300) ·
  `digest.md` **200줄**(상한 200, 꽉 찼다 — 스텝 2 가 명부를 넣으며 1줄을 비운다) ·
  아카이브 **22개** · 색인 구멍 **5개**.
