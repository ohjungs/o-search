# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**반복 97~122 는 `history_006.md`, 123~128(계획 020 `deadline` 전체)은
`history_007.md`, 계획 21~24(짧은 경로 3건 + 보류 패치 소진, 2026-08-29 14:20~17:40)는
`history_008.md` 로 밀려났다** (2026-08-29 회전 3회).
**계획 25~29 는 `history_009.md`, 계획 30·31(짧은 경로) + 반복 152 탐색 +
계획 33 `clock-injection` 의 계획·설계·개발 1~4 는 `history_010.md` 로 밀려났다**
(2026-08-30 회전, 393줄 → 139줄. 상한 300줄을 넘겨 놓고 두 반복을 갔다).
**계획 33 의 마지막 네 반복(개발 5 · 테스트 1 · 리뷰 1 · e2e 1)은 `history_011.md`**
(2026-08-30 회전, 322줄 → 210줄. 상한을 넘긴 채 반복 두 번을 또 갔다 — 스텝 3 이
"미결(문서 위생)" 로 넘긴 것을 스텝 4 가 닫았다).
계획 33 의 전 phase 서술은 `plan_history_020.md` 의 `## 기록` 에 그대로 있다.
**계획 34 `graceful-interrupt` 의 앞 여덟 반복(계획 1 · 설계 1 · 개발 1 세 번 · 2 · 3 · 4)은
`history_012.md`** (2026-08-30 회전, 300줄 → 85줄. **상한에 정확히 닿아 넘기기 전에 돌렸다** —
앞 두 번은 넘긴 채 반복을 더 갔다).
**계획 34 의 마지막 세 반복(테스트 1 · 리뷰 1 · e2e 1)은 `history_013.md`**
(2026-08-31 회전, 307줄 → 244줄. 상한 300 을 넘긴 그 반복에서 바로 돌렸다 —
`digest ## 반복 실패` 가 지적한 "회전이 두 반복 늦었다" 를 안 되풀이한다).
**계획 35 `deadline-stop` 전체 여섯 반복(계획 · 개발 1 · 테스트 2 · 리뷰 · e2e)은
`history_014.md`** (2026-08-31 회전, 336줄 → 188줄. 상한 300 을 넘긴 채 테스트 반복
하나를 더 갔고 **리뷰가 그것을 잡았다** — 회전은 phase 룰이 아니라 문서 위생이라
`status.md` 의 "집안일 미결" 로만 있었다. 완료된 계획 하나를 통째로 미는 경계로 잘랐다).

**계획 36 `signal-budget-cover` 전체 두 반복과 계획 37 `indexer-interrupt` 전체 여섯 반복은
`history_015.md`** (2026-08-31 회전, 318줄 → 96줄. **계획 38 리뷰가 잡아서 돌렸다** —
테스트 스텝이 305줄인 것을 알고 `status.md` 에 적어 둔 채 그대로 갔다. 393·322·336·318,
네 번째 지각이다. 완료된 계획 둘을 통째로 미는 경계로 잘랐다).

**계획 38 `digest-rotate` 전체 다섯 반복과 계획 39 `indexer-lock` 전체 다섯 반복은
`history_016.md`** (2026-09-01 회전, 299줄 → 145줄. **계획 40 리뷰가 제때 돌렸다** —
테스트 반복이 "다음 반복이 회전한다" 고 적어 둔 그대로다. 393·322·336·318 은 지각이었고
이번은 아니다. 완료된 계획 둘을 통째로 미는 경계로 잘랐다 — 계획 40 을 여는 반복 198 부터 남는다).

---

## 반복 198 — 계획 39 DONE · 계획 40 `exit-code-contract` 개설 (2026-09-01)

- 한 일: 계획 39 를 DONE 으로 마감(`git mv` → `docs/plan_history_025.md` · `index.md` 완료)
  하고, discover 1~8순위를 돌려 6순위 `digest.md ## 다음 계획 후보` `[7]`(세 CLI 종료 코드
  계약이 갈렸다)로 계획 40 을 열었다. `docs/plan_exit-code-contract.md` · 브랜치
  `loop/exit-code-contract`(기점 `064e8a5`). `src/`·`tests/`·`e2e/` **0줄**.
- 결과: 1~5순위 전부 0건(단위 **462건 OK 11.883초** · 린터 없음 · 코드 `TODO` 0 ·
  `candidates.md`·`scripts/` 없음 · `## 보류` 비어 있음). 실측 rc — 없는 DB 2 ·
  비 DB 파일 2 · `serve --port 80` **1** · `file://` 시드 2 · 무인자 2.
  `crawl.db` sha256 무변경 · `digest.md` 50항목·191줄 그대로.
- 배운 것: **백지 패스가 남긴 후보의 세부도 백지 패스다** — `[7]` 이 "세 모듈짜리" 라고
  적었지만 `crawl.main` 에는 환경 오류 갈래가 **0개**이고, 환경으로 센 `NoUsableSeedsError`
  는 `crawl.py:25-32` 가 **사용자 입력 오류**라고 이유까지 적어 뒀다. 계획 39 가 "관례를
  따랐다는 주장은 전수여야 한다" 로 배운 것이 **그 리뷰가 낳은 후보에서 되풀이됐다.**
  둘째: 관례로 인용된 `serve` 의 rc 1 은 **단언이 0건**이다 — 39 리뷰의 M6 와 같은 모양이라
  계획 40 의 첫 스텝을 "값을 바꾸기 전에 오늘의 값을 못박는다"(`src/` 0줄)로 잡았다.
- 다음: 설계 1. 대안 셋(A 환경을 rc 1 로 · B `serve` 를 rc 2 로 · C 재시도 축을 별도 코드로)
  중 고르고 7절 e2e 를 되받아 적는다. `docs/design_exit-code-contract.md`.

## 반복 199 — 계획 40 설계 1 · 대안 A(환경 rc 1 / 명령줄 rc 2) (2026-09-01)

- 한 일: `docs/design_exit-code-contract.md` 를 쓰고 계획 6절의 물음 셋에 답했다.
  **A** 를 골랐다 — `indexer.main` 의 환경 오류 네 갈래를 rc 2 → **rc 1**(제품 diff 는
  `indexer.py` 4줄, `serve`·`crawl` 0줄). **C**(재시도 축을 별도 값으로)는 보류.
  `src/`·`tests/`·`e2e/` **0줄**.
- 결과: 단위 **462건 OK 11.783초**(기준선 유지) · e2e 생략(코드 0줄) · `crawl.db`
  sha256 무변경 · `digest.md` 50항목·191줄 그대로. 개발 스텝을 **2 → 1개로 합쳤다**.
- 배운 것: **호출을 grep 해 커버리지를 세면 헬퍼 뒤의 단언이 통째로 안 보인다** —
  계획이 "`serve` 의 rc 1 을 붙드는 단언 0건" 이라고 적은 근거는 `grep "serve.main"`
  이었는데, `tests/test_serve.py:890` 이 `assertEqual(rc, 1)` 로 값을 붙들고 docstring 에
  규칙까지 적어 뒀다(단언은 헬퍼 `call()`(`:792`)을 거쳐 부른다). 계획 39 가 "관례를
  따랐다는 주장은 전수여야 한다" 로 배운 것의 **측정 도구 판**이다 — 전수를 세려면
  호출 지점이 아니라 **값**(`rc, 1`)을 세야 했다. 그 정정이 대안 B 의 비용을 0곳에서
  1곳 + 기록된 근거 삭제로 올렸고, "오늘의 값을 못박는" 스텝 1 의 전제도 함께 무너뜨렸다.
  둘째: **가른 것은 크기가 아니라 틈이었다** — A 는 단언 7곳을 옮기고 B 는 제품 1줄인데,
  파이썬이 **잡히지 않은 예외를 이미 rc 1 로 끝내므로** B 를 고르면 처리한 환경 오류 2 ·
  처리 못 한 것 1 로 영구히 갈린다. 두 파일이 그 사실을 이미 적어 뒀다
  (`serve.py:329-333` "안 잡을 때도 1 이었다" · `test_indexer.py:230` "고치기 전엔
  트레이스백 + rc=1"). **저장소가 고른 값이 아니라 런타임이 내고 있던 값이 기준선이다.**
- 다음: 개발 1(마지막 스텝). TDD 로 단언 7곳(`test_indexer.py` 6 + `tokenizer_e2e.py:153`)을
  먼저 뒤집어 RED 를 보고 `indexer.py` 4줄 + `README` 계약 표로 GREEN. 변이 M1.

## 반복 200 — 계획 40 개발 1/1 · 환경 오류가 rc 1 로 내려왔다 (2026-09-01)

- 한 일: TDD 로 단언을 먼저 뒤집어 **462건 중 6건 RED**(`2 != 1`)를 본 뒤
  `indexer.py` 의 환경 오류 네 갈래(`:245`·`:251`·`:256`·`:265`)를 `return 2` →
  **`return 1`** 로 내리고 `README.md:28` 아래 계약 표(0·1·2·130)를 넣었다.
  `crawl.py`·`serve.py` **0줄**. 제품 diff 는 설계가 그은 한 파일 그대로다.
- 결과: 단위 **462건 OK 11.890초**(기준선 유지 — 값만 바뀌고 건수는 안 변했다) ·
  e2e 3종 전부 통과(`tokenizer`·`indexer`·`indexer_interrupt`) · `crawl.db` sha256
  무변경 · `digest.md` 50항목·191줄 그대로. 변이 **M1**(`:245` 만 2 로 되돌리기)이
  **새 단언 하나만**(`test_missing_db_is_error_not_traceback`) 죽였다.
- 배운 것: **값으로 세는 것으로는 전수가 안 된다 — 단언의 *문법형*도 세어야 한다.**
  설계서의 "옮기는 자리 전수" 는 7곳이라고 적었는데 여덟째가 있었다
  (`e2e/indexer_interrupt_e2e.py:281`, 중단 뒤 질의의 `assert q_rc == 2`).
  반복 199 가 "호출을 grep 하면 헬퍼 뒤의 단언이 안 보인다" 를 배우고 **값**(`rc, 1`)으로
  세는 것으로 고쳤는데, 그 교정도 `assertEqual` 형만 봤다 — e2e 는 맨 `assert` 문에
  변수명도 `q_rc` 라 같은 그물에 안 걸렸다. **계획 → 설계 → 개발로 내려오며 같은 종류의
  누락이 세 번 좁아졌을 뿐 사라지지 않았다**(호출 지점 → 값 → 문법형). 다행히 빠진 것은
  결론이 아니라 범위뿐이라 계약은 그대로 갔다.
  둘째: **변이 사본은 심기 전에 기준선을 찍어야 한다.** `src`+`tests` 만 복사해 심었더니
  438건·오류 7건이 나왔는데 이것은 변이가 아니라 README·docs 를 읽는 테스트가 파일이
  없어 깨진 것이었다. 통째로 다시 뜨고 **변이 전 462 OK 를 먼저 확인**하니 M1 이 정확히
  1건을 죽이는 것이 보였다. 기준선 없이 봤으면 "변이가 죽였다" 와 "사본이 부실했다" 가
  같은 빨간불로 보였다 — `digest [8]`(BSD sed 거짓 초록)의 **반대 방향 짝**이다.
- 다음: 테스트 phase. 새 코드가 아니라 **계약이 실제로 전수인지**를 본다 —
  `assert` 문 형태의 rc 단언이 e2e 15파일에 더 없는지 `returncode`·`_rc` 양쪽으로 센다.

## 반복 201 — 계획 40 테스트 1/1 · 계약은 전수였고, 계약을 적은 문서가 틀렸다 (2026-09-01)

- 한 일: 새 테스트를 안 쓰고 **빠진 것을 찾았다.** ① 종료 코드 자리 전수 — 프로세스 rc 를
  정하는 곳은 `sys.exit(main(...))` **3곳뿐**이라 `main` 의 반환값 14개가 계약의 전부다
  (`indexer` 8 · `crawl` 7 · `serve` 4). ② 변이 4종 추가 — `indexer.py:251`·`:256`·`:265`
  와 `serve.py:333` 을 `.git` 없는 사본에서 각각 `return 2` 로 되돌렸다. ③ 세 CLI 를
  **진짜 프로세스로** 15가지 상황에 돌렸다(전부 `cwd=<임시 디렉터리>`, 시드는 127.0.0.1).
  ④ `README.md` 종료 코드 표에 **"어디서" 칸을 되살렸다**(설계서엔 있는데 옮기며 빠졌다).
  제품 `src/` **0줄**.
- 결과: 단위 **462건 OK 11.815초** · e2e 3종 통과 · 변이 **5/5 사망**(M1 포함, 각각
  1·1·1·3·1건) — **rc 1 자리 다섯 중 아무 테스트도 안 붙들고 있는 자리는 0곳**이다 ·
  `crawl.db` sha256 무변경 · `digest.md` 51항목·200줄.
- 배운 것: **grep 은 이번에도 못 셌고, 이번엔 내가 걸렸다.** 갭 탐색을 `\b(rc|_rc)\b *==`
  로 시작했는데 이 그물도 `q_rc == 2` 를 **또** 놓친다(`q_rc` 안에서는 `\b` 가 안 걸린다).
  199 는 호출 지점으로, 200 은 값으로, 201 은 단어 경계로 놓쳤다 — **같은 누락이 네 번째로
  모양만 바꿔 나왔다.** 결론: 전수의 그물은 grep 이 아니라 **러너다.** 여덟째 자리는
  `assert` 문법형을 세지 않아도 e2e 를 한 번 돌리면 죽는다.
  둘째: **계약은 코드에서 전수였는데 계약을 적은 표가 거짓이었다.** `serve <없는 DB>` 는 rc 를
  안 낸다 — **뜬 채로 요청마다 500 이다**(서버는 종료 코드로 알릴 상대가 없다). README 표는
  그것을 "세 명령이 같은 값" 아래 rc 1 로 적어 뒀다. **값은 다 옮기고 설명하는 칸을 흘렸다.**
- 다음: 리뷰 phase. `crawl` 환경 오류 트레이스백은 `digest [6]` 으로 넘겼다(제품 diff 한도).
  **이 파일이 299줄이다 — 다음 반복이 회전한다**(`digest` 가 네 번 적은 "회전이 늦는다").

## 반복 202 — 계획 40 리뷰 1/1 · 표는 고쳤는데 같은 칸이 한 번 더 좁았다 (2026-09-01)

- 한 일: **백지 패스를 먼저 회수하고** 대조 패스를 열었다(`74bf266..HEAD`). 백지에서
  rc 표를 **17가지 상황으로 다시 실측**했고(세 CLI 를 진짜 프로세스로, `crawl` 은 사례마다
  새 디렉터리 — 200 이 밟은 `data/` 오염 자리다), 열거형 완전성을 `tests/`·`e2e/` 전수로
  다시 셌다. 대조에서 계획서 7절·8절을 봤다. 집안일로 `history_current.md` 를 회전했다
  (299 → 145줄, 계획 38·39 열 반복 → `docs/history_016.md`).
- 결과: **지적 4건 · 제품 동작 지적 0건 · 자동 수정 4** — [A1] `indexer.py:243` 주석이
  문장 중간에 끊겨 있었다 · **[A2] `README` rc 1 의 `serve` 칸이 여전히 좁았다**(특권 포트도
  rc 1 인데 *"포트 점유"* 라고만) · [B1] 계획서 7절이 세 반복째 *"미정 · 설계가 정한다"*
  였다 · [B2] 계획서 8절 한도의 `digest` 열린 항목이 50 인데 실제는 51. 검증: 단위
  **462건 OK 11.954초** · e2e 3종 통과 · `crawl.db` sha256 무변경 · `digest.md` 51항목·200줄.
  **한도 하나를 밟았다** — 재검증에서 러너를 `| tail -4` 로 감쌌다(맨몸으로 다시 돌려 확인).
  `digest ## 반복 실패` 의 파이프 항목을 **2 → 3회**로 올렸다(압축해 200줄 유지).
- 배운 것: **[A2] 가 [8] 과 같은 자리에서 같은 모양으로 한 번 더 나왔다.** 201 이 설계서의
  "어디서" 칸을 되살리면서 `serve`(bind) 를 *"포트 점유"* 로 **다시 좁혔다** — 고치는
  손이 같은 종류의 축약을 되풀이한 것이다. 표와 세 줄 아래 산문(*"포트를 못 열었을
  때뿐"*)이 서로 다른 말을 하고 있었는데 **둘 다 같은 반복이 썼다.** 일반화:
  **좁힌 칸은 원본이 아니라 실측과 대조해야 잡힌다** — 201 은 설계서와 대조해 [8] 을
  잡았고, 202 는 실측과 대조해 그 수정본에서 [A2] 를 잡았다.
  둘째: **계획서가 스스로 "여기에 되받아 적어라" 고 적어 둔 자리는 아무도 안 채운다**(B1).
  설계·개발·테스트가 각자 자기 문서에만 적었다. 되받아 적기는 리뷰의 대조 패스에서만
  기제가 있다 — 그래서 리뷰가 적었다.
- 다음: **DONE 마감 한 반복.** e2e 는 새로 붙일 것이 없다(7절 판정 — 프로세스 경계가
  필요한 두 자리를 `tokenizer_e2e:153`·`indexer_interrupt_e2e:281` 이 이미 잰다).
  마감이 할 것은 계획서 `git mv` · `index.md` 완료 · `digest [7]` 닫기(→ 50).
  `digest.md` 는 200줄 상한에 닿아 있고 **다음 회전은 삭제가 아니라 장문 항목 압축**이다
  (방아쇠: 항목을 하나라도 더할 때).
