---
signal: GREEN
mode: night
plan: cooldown-burn
phase: 테스트
step: 2/2
attempt: 0
iteration: 85
night_iterations: 9
night_red: 0
night_retries: 0
night_self_amendments: 0
updated: 2026-08-27 (반복 85)
ctx: 37% / 200k
rules: rules/test.md
---

# 현재 상태

**`cooldown-burn` 스텝 2/2 완료. 다음은 테스트 phase(갭 탐색).**
브랜치 `loop/cooldown-burn` (기점 `9bd3771`, `loop/tokenizer`).
계획 `docs/plan_cooldown-burn.md` · 설계 `docs/design_cooldown-burn.md`.

| 반복 | 커밋 | 무엇 |
|---|---|---|
| 83 | `5a66070` | 설계 — 대안 A(최소) 채택 |
| 84 | `a159e30` | 스텝 1 — 간격 시계를 팝에서 떼어 실제 발신에 건다 |
| 85 | `46b69cf` | 스텝 2 — `perf_crawl` 에 [차단]·[예외] 시나리오 |

## 지금 값

- **차단 사이트 처리량 4.47 → 10.24/s** (목표 9.0 · 컨셉 성능 2는 5.0)
- 페이지 최소 간격 1.004s · 차단 경로 요청 0건 · 워커 예외 섞어도 간격 유지
- **265건 통과** · `crawl_delay_e2e.py` 종료 0 · `perf_crawl.py` 3시나리오 전부 통과
- 되돌리기 경로(`perf_crawl.py 1`) 문서집합 sha1 `541d455a` — workers=8 과 같다

## 채택한 계약 (설계 A)

`Frontier.next()` 는 `_last_fetch` 를 **읽기만** 한다. 시계를 거는 자리는
`mark_sent()` 하나다. 워커 예외 경로는 **요청이 나갔는지 알 수 없으므로**
`mark_sent(domain, now())` 로 보수적으로 건다.
버린 것: B 리스 구조(호출처 1곳뿐) · C 취소 방식(틀린 전제를 남겨 재발) ·
제출 시점(실측 0 회수) · add 시점 robots 필터(동시화 계약 4 위반).

## 변이로 확인한 것 — 검사가 진짜 잡는다

| 변이 | 잡은 곳 | 그때 통과해 버리는 곳 |
|---|---|---|
| 팝 쓰기 복원(원래 버그) | **[차단] 4.48/s** · 단위 3건 | **[열림] 10.24/s 통과** |
| 예외 경로 마킹 제거(순진한 수정) | **[예외] 0.411s** · 단위 1건 | **[차단] 10.29/s 통과** |

오른쪽 열이 이 계획의 교훈이다. **검사 하나가 초록이라고 구멍이 없는 것이 아니다.**

## 테스트 phase 가 볼 것

- `rules/test.md` 갭 탐색. 새로 만진 public 표면은 `Frontier.next`(계약 변경) ·
  `_store_result`(시그니처에 `now` 추가) · `perf_crawl` 시나리오 2개
- 이미 있는 것: 스킵·차단이 안 태움 + **긍정 짝** · 예외 뒤 간격 유지 ·
  `test_next_does_not_start_the_clock`
- 생각해 볼 갭: `mark_sent` 를 **한 번도 안 부른** 도메인(첫 요청)이 예외로 끝나는 경로,
  `set_delay` 가 걸린 도메인에서 예외가 났을 때 요구 간격이 유지되는가

## 열지 않는 것 (승인 대기 — 야간 금지)

`recrawl`(`fetched_at` 스키마) · `X-Robots-Tag`(스키마) · `loop/*` 브랜치 병합.
`store.has` 상태 불문 스킵은 **recrawl 계획 소관**(같은 DB 재실행은 시드부터 스킵돼
0으로 끝난다 — 반복 82 탐침).

## digest 후보 (이번 밤)

6. **거래를 재려면 양쪽을 같은 자로 재라** (`porter` — 반복 81, digest 에 반영함)
7. **기록된 답을 실행 전에 재라.** digest 가 8일 전에 적어 둔 `[high]` 의 처방
   ("제출 시점 `mark_sent`")은 실측하니 **0을 회수했다.** 보류 항목의 *진단*은 옳아도
   *처방*은 그때의 추정이다. digest 의 "영향은 이어받기 크롤 한정" 도 같은 자리에서 틀렸다
8. **측정 하네스가 재지 않는 세계가 곧 버그가 숨는 곳이다.** 기준선에는 값만이 아니라
   **어떤 세계에서 잰 숫자인지**를 적는다. 10.25/s 를 안전한 숫자로 읽는 동안 실제로
   도는 경로는 4.5/s 였다. 변이 표의 오른쪽 열이 이것을 두 번 보여준다
9. **깨지지 않았다고 멀쩡한 테스트는 아니다.** 팝 쓰기를 지웠을 때 깨진 것은 9건이었지만,
   `test_no_directive_keeps_default_interval` 과 `test_other_domain_served_while_first_cooling`
   은 **깨지지도 않으면서 자기가 적어 둔 것을 안 재고 있었다**(전자는 시계를 안 걸어서,
   후자는 간격이 아니라 라운드로빈이 답을 내주고 있어서). 계약을 바꿀 때는
   깨진 테스트만이 아니라 **그 계약을 재는 테스트 전부**를 본다
10. **탐침의 배선을 먼저 의심한다.** 두 트리를 비교했는데 숫자가 **똑같이** 나오면
    측정 결과가 아니라 같은 코드를 두 번 잰 것이다 — `perf_crawl.py` 가 import 시점에
    저장소 `src` 를 `sys.path[0]` 으로 밀어서 실제로 그렇게 됐고, 하마터면 설계 A 를
    "아무것도 회수 못 함" 으로 버릴 뻔했다
