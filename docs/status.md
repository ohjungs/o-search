---
signal: GREEN
phase: 개발
step: 1
attempt: 0
iteration: 209
updated: 2026-09-01
ctx: 55
night_iterations: 72
night_red: 0
night_retries: 0
---

# 현재 상태

**계획 42 `docs-citation-guard` 열림 (2026-09-01).** 계획서 `docs/plan_docs-citation-guard.md`.
브랜치 `loop/docs-citation-guard`, 기점 `53332e0`(계획 41 HEAD — `tests/test_docs.py` 가
`origin/main` 에 없다. 계획 38 산출물이고 PR #3 이 아직 미병합이라 main 기점으로는
계획이 성립하지 않는다). **설계 생략** — 기존 테스트 파일에 검사 한 건 추가, 트리거 없음.
**스텝 1개** — TDD RED→GREEN 이 한 반복이고, 가르면 첫 커밋이 RED 로 남는다.

## 방금 것 (2026-09-01 · 계획 1/1 · 반복 209)

**탐색 1~5순위가 전부 0건이다 — 두 반복 연속이고, 6순위에서 여는 것도 두 번째다.**

| 순위 | 출처 | 건수 |
|---|---|---|
| 1 실패 테스트 | 러너 맨몸 실행 | **0** (466건 OK 11.934초) |
| 2 타입·린트 | 도구 없음(`project.md ## 명령`) · ruff·flake8·mypy·pyflakes 미설치 | **0** |
| 3 코드 `TODO`/`FIXME`/`HACK` | `src/`·`tests/`·`e2e/` 전수 | **0** |
| 4 `docs/candidates.md` | 파일 없음 | **0** |
| 5 `digest.md` 보류 | 승인 난 것 없음 | **0** |
| 6 `digest.md` 다음 계획 후보 | `[6]` 인용 형태 | **1** ← 여기서 열었다 |
| 7 `digest.md` 반복 실패 | 파이프 8회 — 판정만 | (열지 않음) |
| 8 이슈 | `gh issue list` | **0** |

**탐색이 막혔다는 신호다.** 1~4순위는 전부 "기계가 만드는 신호"인데 이 저장소에는
그 기계가 없다(린터 없음 · `candidates.md` 없음 · `TODO` 를 안 쓰는 관례 · 스위트는
항상 초록). 남은 우물은 `digest.md` 하나이고, 열린 항목 49 중 대부분은 게이트가 닫혀
있다(승인 대기 · 도달 불가 · "실물에서 보이면 그때" · "다른 계획을 열 때 함께").
**루프가 고칠 것이 아니라 사람이 볼 신호라 적어만 둔다.**

**7순위 우선 후보 판정 — 파이프 8회는 저장소 안에 고칠 몫이 없다.** 처방 후보
(`make test`·별칭으로 러너 호출을 굳힌다)를 사다리로 내려보니 1번에서 멈춘다:
① 저장소에 `Makefile`·`scripts/` 가 없고 명령은 `project.md` 가 이미 정확히 굳혀 뒀다
② 검증을 세는 기제는 저장소 밖이라 `make test` 를 러너로 인정하는지 알 수 없고,
인정 못 하면 **새는 자리를 하나 더 만든다** ③ `make test | tail -4` 도 똑같이 된다 —
파이프를 못 막는다 ④ 여덟 방아쇠가 매번 다르다(망각·재확인·"사본이라 가볍다"·훅에
급히 답하려는 조급함) → 명령 조립이 아니라 **호출자의 규율** 문제다. **새 파일 안 만든다.**

**착수 실측이 근거를 한 칸 넓혔다.** `digest [6]` 은 "`digest.md:<줄번호>` 인용은 회전
한 번에 **죽는다**" 이고 계획 38 리뷰가 "살아 있는 문서 3곳을 이름 인용으로 고쳤다"고
적었는데 — 오늘 세어 보니 살아 있는 문서에 **3곳이 그대로 남아 있고**, 그중 둘은
죽은 게 아니라 **남의 항목을 가리킨다**:

- `docs/index.md` **22행**·**357행**: `` `digest.md:156` `[6]` `` → 오늘 그 줄은
  `[7] 살아남은 변이가 형제 구멍` 이다. 가리키려던 `[6]`(가짜 시계 10곳)은
  `## 판단 필요` 로 내려가 있다
- `docs/digest.md` **80행**: `` `index.md:24` `` → 오늘은 우연히 맞다

**계획 38 리뷰의 조사가 전수가 아니었다.** 이 계획이 심는 검사의 존재 이유가 그것이다 —
사람이 손으로 세는 전수는 다음 회전까지만 참이다. 아카이브 5개(`history_010`·
`history_011`·`plan_history_020`·`plan_history_021`·`plan_history_024`)에도 같은 형태가
있지만 **수정·삭제 금지**라 검사에서 명시적으로 뺀다.

**검증**: 단위 **466건 OK 11.934초**(맨몸 실행, 파이프 0) · `data/crawl.db` sha256
**무변경**(`85c96744…5bda18`) · `digest.md` **49항목 200줄** · `history_current.md`
**201줄**(상한 300 미달 — 회전 안 한다).

## 다음 스텝 (개발 1/1)

`docs/plan_docs-citation-guard.md` **3절 스텝 1** 을 그대로 돈다.

1. `tests/test_docs.py` 에 검사 한 건 추가 — 금지 형태는 기존 상수
   `APPEND_TARGETS`(`digest.md`·`index.md`·`history_current.md`) 에서 **조립**한다.
   검사 대상은 `docs/*.md` 중 아카이브 이름꼴(`history_<숫자>.md` ·
   `plan_history_<숫자>.md` · `design_history_<숫자>.md`) 제외.
2. **먼저 RED 3건을 눈으로 본다** — `docs/index.md` 22·357, `docs/digest.md` 80.
3. 세 줄을 이름 인용으로 고친다. **`docs/digest.md` 는 200줄 유지**(줄 안에서 고친다).
4. 러너 맨몸 실행 → **467건 OK**. 변이 M1~M3(계획서 3절).

## 한도 (넘으면 RED)

- `data/crawl.db` 실물·스키마 무변경 (sha256 `85c96744…5bda18`).
- 단위 **466건**이 기준선(11.9초). 이 계획은 **+1 → 467**. 그 아래면 RED ·
  e2e **19종**이 줄면 RED.
- `docs/digest.md` 열린 항목 **49**(`grep -c '^- \['`) · **200줄 상한**.
  이 계획은 줄을 늘리지 않는다.
- **러너를 파이프로 감싸지 않는다**(`## 반복 실패` **8회** — 여덟째 방아쇠는 **Stop 훅 자신**이다.
  훅이 "검증을 안 돌렸다" 고 막으면 급히 답하려다 파이프를 붙인다. 변종: 루프·`env` 접두·`timeout`·
  리다이렉션·백그라운드 태스크. 훅이 검증으로 세는 것은 **러너로 시작하는 세그먼트**뿐이라
  `PYTHONPATH=src python3 -m unittest` 도 안 세진다 — `export` 로 갈라 쓴다).
- 의존성 추가 금지(stdlib 만) · `docs/specs/` 읽기만 · `--no-verify` 금지 ·
  `main` 직접 커밋 금지 · 외부 네트워크 금지 · 도메인당 요청 간격 1초 이상 · robots 준수.
- **`src/` 는 이 계획에서 0줄이다.** 건드리면 YELLOW.
