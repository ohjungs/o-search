# 지표

<!--
매 반복 끝에 숫자만 갱신한다. 서술은 history_current.md 가 한다.
읽는 것은 계획 탐색 때와, 룰을 고칠지 판단할 때뿐이다. (~25줄)

목적: 임계값들이 감으로 정해져 있다. 데이터가 쌓여야 고칠 근거가 생긴다.
-->

## 누적

| | 값 |
|---|---|
| 반복 | 48 |
| 계획 (완료/폐기/보류) | 5 / 0 / 0 |
| 재시도 | 0 |
| RED | 0 |

## phase 분포

반복이 어디에 쓰이는지. 한 phase 가 과도하면 그 룰이 무겁다는 신호다.

| phase | 반복 수 |
|---|---|
| 계획 | 6 |
| 설계 | 4 (생략 0) |
| 개발 | 14 |
| 테스트 | 4 |
| 리뷰 | 4 |
| e2e | 4 |

## 리뷰 정확도 — 80점 임계 검증용

| | 값 |
|---|---|
| 발견 후보 | 29 |
| 80점 미만으로 버림 | 7 (기보류 중복 2 + search-api 2 + crawl-delay 3) |
| 보고함 | 22 |
| **그중 실제로 고친 것** | 19 |

**보고한 것 대비 실제로 고친 비율이 낮으면 임계를 올린다.**
버린 것 중에 나중에 진짜 문제로 드러난 게 있으면 내린다.
지금 80 은 gstack 에서 가져온 값이고 근거가 없다.

## 정지 사유

| 사유 | 횟수 |
|---|---|
| 계획 소진 | 0 |
| 반복 상한 | 0 |
| 컨텍스트 상한 | 4 |
| 재시도 3회 | 0 |
| 연속 RED | 0 |
| 보류(YELLOW) | 0 |

**컨텍스트 상한이 반복 상한보다 자주 걸리면** 야간 60 은 지킬 수 없는 숫자다.

## 야간

| | 값 |
|---|---|
| 실행 횟수 | 6 |
| 평균 반복 | 7.2 (13, 6, 7, 15, 1, 1) — 마지막 두 번은 컨텍스트 82~83% 로 한 스텝만 닫고 종료 |
| 남긴 패치 | 0 (적용됨 0) |
| 보류 | 0 |
| **야간 산출물** | 55 (커밋 55·패치 0) |
| **아침에 버린 것** | 0 |

**야간 폐기율 = 버린 것 / 산출물.** 밤새 20커밋을 만들어도 아침에 절반을 버리면
일한 것이 아니라 **판단할 일을 늘린 것**이다. 0.2 를 넘으면 무엇이 헛돌았는지 본다.

**보고서(`docs/reports/`)는 커밋하지 않으므로 여기 숫자가 유일한 장기 기록이다.**
아침에 패치를 버렸거나 브랜치를 지웠으면 그날 이 두 줄을 갱신한다 — 안 하면
폐기율을 나중에 복원할 방법이 없다.
