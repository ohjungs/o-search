---
signal: GREEN
phase: 개발
step: 2
attempt: 0
iteration: 237
updated: 2026-09-02
ctx: 60
night_iterations: 94
night_red: 0
night_retries: 0
plan: json-contract
---

# 현재 상태

**계획 46 `json-contract` 개발 스텝 1/3 완료 — "색인이 없다"가 이제 503 이다.**
설계 갈림길 B·C 가 코드가 됐다. 제품 순증 **10줄**(추정 12), 단위 **482 → 486건 OK**.
**다음 반복은 개발 스텝 2** — 응답 스키마에 버전(`VERSION = 1` 을 `_send` 한 곳에서).

| 상태 | 이전 | 지금 |
|---|---|---|
| DB 파일 없음 | 500 | **503** (JSON · 화면 둘 다) |
| 색인 드리프트 (`StaleIndexError`) | 500 (재는 단언 **0건**) | **503** + 새 픽스처가 잰다 |
| 크롤만 함 / 빈 DB / 색인 0건 / 무매치 | 200 | **200 유지** (갈림길 D · 계획 47) |
| 프로그래밍 오류 | 500 (재는 단언 **0건**) | **500 유지** + 새 단언이 잰다 |

## 방금 한 것 (2026-09-02 · 계획 46 개발 스텝 1)

- **`src/websearch/serve.py` 두 경로에 503 절을 넣었다.** `except (FileNotFoundError,
  indexer.StaleIndexError)` 를 `except Exception` **앞**에(순서가 계약이다 — 뒤면 영영
  안 닿는다). `do_GET` 은 JSON, `_do_html` 은 화면(갈림길 C). 본문·문구는 고정
  (*"색인이 아직 준비되지 않았다"*) — `str(exc)` 는 곧 DB 경로라 실을 수 없고, 원인은
  `log_error("색인 없음: %r")` 로 stderr 에만 남는다.
- **TDD 순서를 밟았고 RED 7건을 눈으로 봤다** (`TestMissingDb` 2 ·
  `TestHtmlPathFailsSafely` 2 · `TestDriftedIndex` 2 · README 카운트 가드 1).
- **설계가 예고한 500 커버리지 구멍을 먼저 막았다.** `TestProgrammingErrorStays500` 이
  `serve._page_hits` 를 `AttributeError` 로 갈아끼워 **JSON·화면 둘 다 500 인 것과 그
  로그**를 못박는다(변이 M5). 이 클래스는 구현 전부터 초록이다 — 재는 것이 새 동작이
  아니라 **안 변한 동작**이라서다. 안 심었으면 `except Exception` 을 통째로 503 으로
  바꿔도 486건이 전부 초록이었다.
- **드리프트를 HTTP 로 재는 픽스처가 처음 생겼다** — `TestDriftedIndex` 가 `docs` 를
  옛 fts5 정의로 갈아끼운다(변이 M3). 그전까지 `serve` 는 `StaleIndexError` 라는 이름을
  **0번** 불렀다.
- **e2e 하나가 이 변경으로 거짓이 됐다 — 계획서 8절이 예고한 그 자리다.**
  `e2e/tokenizer_e2e.py:149` 의 ⑥ 시나리오가 옛 색인 화면을 `== 500` 으로 못박고 있었다.
  503 으로 고치고 꼬리말 문장도 맞춘 뒤 **실제로 돌려서 통과를 봤다** — 단위 스위트는
  e2e 를 안 부르므로 안 돌렸으면 다음 phase 까지 몰랐다.
- **`TestUnindexedDb` 독스트링에 갈림길 D 를 못박았다** — 오늘의 `200 + []` 가 네 상태를
  덮고, 가르는 술어(`docs` 테이블 존재)를 `indexer.search` 가 `indexer.py:183-185` 에서
  이미 판별하고서 합친다는 사실까지. 안 적으면 다음 사람이 다시 연다.

## 한도 (매 반복 확인)

- `main` 직접 커밋 금지 · `--no-verify` 금지 · 외부 네트워크 금지 ·
  `docs/specs/` 읽기만 · `data/crawl.db` 무변경 · 의존성 추가 금지(stdlib 만).
- **러너 파이프 — 이번 반복 5회. 연속 카운트가 또 0 으로 되돌아갔다.** RED·GREEN·e2e·
  최종 확인에 전부 `| tail`/`| grep` 을 달았다. 직전 두 반복이 같은 실수를 적어 뒀는데
  **세 번째**다. 이번엔 잘려서 못 본 것이 없었지만 그것은 운이다 — 실패 목록이 길었으면
  또 못 봤다. 아래 수치는 마지막 **맨몸** 실행의 것이다.
- 이번 반복 실측(맨몸): `PYTHONPATH=src python3 -m unittest discover -s tests`
  **486건 OK**(12.108초) · `PYTHONPATH=src python3 e2e/tokenizer_e2e.py` 통과.
  제품 `src/` **+10줄** · `data/crawl.db` sha256 `85c96744…` **무변경**(테스트는 전부
  임시 디렉터리) · 의존성 0 · 새 제품 파일 0.
- README 카운트 가드가 **즉시 울었다** — 단위 482 → 486 을 `README.md` 검증 절에 반영.
  같은 파일의 rc 표 아래 *"요청마다 500 을 낸다"* 도 503/500 을 가르는 문장으로 고쳤다
  (설계 1절이 찾아낸 계획서 정정분).
- 제자리 변이 0 — `.mutation-lock` 없음. 변이 심기는 **테스트 phase** 몫이다.
- **집안일 — 회전 미달.** `history_current.md` 274줄 · 3항목(상한 300줄 · 20항목).
  항목을 붙인 **뒤에** `wc -l` 로 셌다 — 직전 반복이 적어 둔 절차 그대로다.
  아카이브 24개 그대로 · `digest.md` 명부 갱신 없음.
- **설계서가 한 화면을 넘는 건(186줄)은 그대로 열려 있다.** 계획 phase 에서 갈림길 수를
  세는 것이 옳은 자리라고 직전 반복이 적어 뒀다.

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-01 확인)

`origin/main` 은 `494313b`(PR #5 병합). 계획 43·44·45 의 커밋들(`bc40ea9` 이후)이
아직 안 갔다. **남은 것 (사람 결정)**: 그것들을 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).
계획 46 의 기점은 `3d766be`(계획 45 DONE 커밋)이고 `main` 이 아닌 이유는 계획서 머리에
적어 뒀다 — `main` 기점은 `serve.py` 를 계획 44 이전으로 되돌리고 기록 4파일을 반복 221
로 되돌려 계획 45 가 세운 두 가드를 스스로 깬다.

## 사람이 정할 것 — 셋이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
   offset 이 0 이 되는 날 검사기가 멈추고, 짝은 그때 열릴 계획의 일이다.
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   이제 *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, 회전을 지시하는 `~/.claude/skills/loop-harness/rules/docs.md` 에는
   그 예외가 없다. **저장소 밖이라 루프가 못 고친다.** 실동작은 안전하다는 것을
   계획 45 e2e 가 실측했다(잊으면 RED · 완료 항목을 지워도 색인은 산다). 룰 문장을
   맞출지는 사람 몫.

**사양이 사람에게 남긴 것 셋은 계획 46 의 범위 밖이다** (`docs/specs/concept.md` 의
`## 사람이 정할 것`): 속도 제한 시점(가정 IP 당 분당 60회) · JSON 검색과 근거 문단의
분할(가정 쪼갠다 — 계획 46 이 그 가정을 따랐다) · 사양 숫자들이 초안이라는 것
(90% · 500ms · 60회/분). 첫째와 셋째는 아직 열려 있다.
