# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**반복 97~122 는 `history_006.md` 로 밀려났다** (2026-08-29 회전).
아래 첫 항목의 "바로 위 두 항목" 은 그 파일의 마지막 두 항목(`normalize-gaps` 019)을 가리킨다.

---

## 2026-08-27 | deadline | 계획 | 시도0
- **날짜 정정**: 바로 위 두 항목의 `2026-08-28` 은 잘못 적힌 날짜다 — `git log` 의
  `9b51677`·`aeb2eeb` 는 **2026-08-27 23:42/23:48** 이다. 이 항목부터 실제 시계를 쓴다.
- 한 일: 019 DONE 뒤 열려 있던 후보 중 `--deadline` 을 열었다. 앞 세션이 정지 조건에
  안 걸렸는데도(ctx 68%) 새 계획을 안 열고 끝낸 자리다. 중복 확인 5곳 전부 통과
  (`index.md` 0건 · `digest ## 완료` 없음 · 활성 계획 없음 · `## 보류` 비어 있음 ·
  `docs/patches/` 디렉터리 없음).
- **근거를 착수 전에 다시 쟀다** (digest `[7]` — 기록된 답의 *처방*은 그때의 추정이다).
  탐침 2건 전부 로컬 실서버:
  1. 한 도메인 `Crawl-delay: 2` · 5페이지 → **8.05초**, 간격 2.01·2.01·2.01·2.00.
     `crawl()` 시그니처와 CLI 플래그 어디에도 시간 인자 **0개**,
     `grep -rn 'deadline|budget' src/websearch/` **0건**. digest 의 산수
     (`--max 100` × `Crawl-delay: 30` = 2970초)가 그대로 성립한다.
  2. SIGINT 를 **0.51초**에 보냈는데 종료는 **6.07초** — 대기 **5.56초**.
     `Crawl-delay: 3` × `RETRIES=2` 가 `before_send` 에서 그대로 잠으로 나온다.
     digest 가 적어 둔 "최대 30초" 는 `MAX_DELAY` 기준으로 **60초**가 맞다.
- **설계로 넘긴다.** `design.md` 트리거 둘에 걸린다 — 공개 시그니처 변경, 그리고
  대안이 갈린다: A(메인 스레드만 예산) 는 탐침 2 를 못 닫고, B(`threading.Event` 를
  워커까지) 는 닫지만 `tests/test_crawl.py` 의 `mock.patch("websearch.crawl.time.sleep")`
  **9곳**이 가짜 시계를 흘려보내는 이음매를 없앤다. **깨진 테스트를 지우는 답은 없다** —
  옮길 축이 있으면 B, 없으면 A 로 가고 그때는 탐침 2 를 digest 에 안 닫고 남긴다.
- 결과: `docs/plan_deadline.md` · 브랜치 `loop/deadline`(기점 `aeb2eeb`).
- 다음: 설계 phase — `docs/design_deadline.md`

## 2026-08-28 | deadline | 설계 | 시도0
- 한 일: `docs/design_deadline.md`. 대안을 **출발점 셋으로 갈라** 냈다(`design.md` 3-1절).
- **① 최소 — `timeout(1)` 로 감싸기를 진짜로 재 봤다.** SIGINT 탐침을 SIGTERM 으로만
  바꾸니 **대기 0.00초**(SIGINT 는 5.56초). **상한 정확도는 이쪽이 이긴다** — 그런데도
  버린 이유가 셋: 함수에는 상한이 안 걸린다(`crawl()` 직접 호출이 e2e 14종 전부) ·
  stock macOS 에 없다(`command -v` → `/opt/homebrew/bin/timeout`) · rc 0 과
  `수집 N 페이지` 가 안 나와 "예산대로 끝났다" 와 "죽었다" 가 구별 안 된다.
  **더 나은 축이 있는 안을 버릴 때는 그 축에서 졌다고 적는다.**
- **② 정공법 — `threading.Event` 를 워커까지.** 예산 초과분 ≈0 에 SIGINT 5.56초까지
  한 기계로 닫는다. **미뤘다**: `tests/test_crawl.py` 가 9곳에서
  `mock.patch("websearch.crawl.time.sleep")` + `side_effect` 로 가짜 시계를
  흘려보내는데 `Event.wait` 가 그 이음매를 없앤다. 이 저장소가 간격을 초 단위로
  단언할 수 있는 근거가 전부 거기다.
- **두 갈래 도피를 명시적으로 거부했다.** "`stop` 이 None 이면 `time.sleep`, 아니면
  `Event.wait`" 는 9곳을 초록으로 만들지만 **테스트가 도는 경로와 제품이 도는 경로가
  갈린다** — digest `[6]`(`**kw` 가짜는 있을 수 없는 협력자를 흉내낸다)이 그 실패다.
  초록을 사는 값으로 검증을 파는 형태라 설계 문서에 거부 사유로 박아 뒀다.
- **고른 것 ③ 되돌리기 우선** — `crawl(..., deadline=None)`, 주입된 `now()` 로 재고
  메인 스레드가 새 요청을 안 던지는 것으로 끝낸다. 보는 자리 셋(`while` 상단 ·
  제출 루프 · `seconds_until_ready()` 잠을 남은 예산으로 자르기). CLI 는 있는
  `_number_flag` 를 그대로. **기본값 None 이 곧 꺼진 플래그**라 별도 플래그를 안 둔다.
- **안 넣기로 한 것도 적었다**: 제출 시 남은 예산이 `interval × RETRIES` 보다 작으면
  `retries=0` — 초과분 90초를 타임아웃 한 번으로 줄이지만 **근거가 산수뿐**이라
  추측성 확장으로 보고 뺐다(`design.md` 3절 "다음이 편한가" 감점).
- **안 닫는 것 3건을 5절에 그대로 남겼다** — SIGINT 최악 대기 · 예산 초과분 90초 ·
  `fetcher` 재시도 구조. digest 의 Ctrl-C 항목은 **지우지 않고** 실측값으로 갱신한다.
- 결과: 코드 변경 0. `git diff aeb2eeb --stat` 은 `docs/` 만 낸다.
- 다음: 스텝 1/4 개발 — 설계 7절의 실패하는 테스트 5개부터

## 2026-08-28 00:12 | deadline | 개발 1/4 | 시도0

- 한 일: `docs/design_deadline.md` 7절의 **실패하는 테스트 5개를 먼저 쓰고 실패를 봤다**
  (`TypeError: crawl() got an unexpected keyword argument 'deadline'` 4건 +
  `--deadline` 플래그가 rc `0 != 2` 1건). 그 다음 `src/websearch/crawl.py` 구현.
- **설계 4절이 든 세 자리 중 둘만 넣었다.** `while` 상단 소진 검사와
  `seconds_until_ready()` 잠 자르기. **셋째(제출 루프)는 안 넣었다** — 바로 위
  `while` 상단 검사와 마이크로초 차이라 어떤 테스트로도 못 죽인다.
  죽일 수 없는 줄은 안 쓴다(카파시 2번). 설계를 줄이는 쪽으로 벗어난 것이라 여기 적는다.
- **CLI 에서 설계와 갈렸다.** 설계는 `_number_flag(args, "--deadline", None)` 라고
  적었는데, 이 플래그는 **없는 것이 정상값(`None`)** 이라 헬퍼의 오류값과 겹친다 —
  `--workers` 처럼 default 로는 못 가른다. `"--deadline" in args` 로 **있었는지를
  따로** 본다(3줄). 헬퍼는 안 고쳤다.
- 결과: 단위 **393건 OK** (기준선 388 + 5). 제품 코드 +30줄(주석·독스트링 포함,
  실질 로직 ~10줄) · 파일 2개 — 계획의 "건드릴 파일" 안이다.
- 다음: 스텝 2/4 테스트 — 변이

## 2026-08-28 00:25 | deadline | 테스트 2/4 | 시도0

- 한 일: **변이 없는 기준선 393건 OK 를 먼저 확인하고** 변이 7건을 하나씩 넣었다
  (`PYTHONDONTWRITEBYTECODE=1` 필수 — 같은 길이 변이는 `.pyc` 가 안 바뀐다).
  변이는 전부 **"이 줄을 안 썼다면 무엇이 되는가"** 로 골랐다(digest `[7]`).
- 결과:

  | 변이 | 죽은 테스트 |
  |---|---|
  | M1 `left` 계산 삭제(예산을 아예 안 본다) | 1 · 3 · 5 |
  | M2 `while` 상단 소진 검사 삭제 | **무한 루프(120초 타임아웃)** |
  | M3 소진 `print` 만 삭제 | 5 |
  | M4 `min(wait, left)` → `wait`(잠을 안 자른다) | 1 · 3 |
  | M5 CLI 의 `--deadline` 검증 삭제 | 4 |
  | M6 CLI 가 값을 `crawl` 에 안 넘긴다 | **살아남았다** |
  | M7 `_apply_delay(..., requested)` → `None` | 13건 (그중 1 · 2 · 3 · 5) |

- **M6 이 살아남은 것이 이 스텝의 값이다.** CLI 배선은 단위 테스트가 안 덮는다 —
  `main()` 을 도는 테스트는 rc 2 경로뿐이라 **정상값이 `crawl()` 까지 가는지 아무도
  안 본다.** 스텝 4/4 e2e 가 반드시 덮어야 할 것이 이것으로 확정됐다.
- **M2 는 죽는 게 아니라 매달렸다.** 소진 검사를 지우면 잘린 잠이 라이브락이 된다
  (`left <= 0` 이 `min` 을 타고 들어가 가짜 시계가 뒤로 간다. 진짜 시계면
  `time.sleep(음수)` 가 `ValueError`). **두 자리가 서로를 붙들고 있다는 뜻이다** —
  제품 코드에서는 위 검사가 항상 먼저 끊어 `left > 0` 만 잠에 닿는다. 도달 불가라
  `max(0, ...)` 가드는 안 넣고 여기 적는다.
- **1번과 3번의 죽는 집합이 같다 {M1, M4, M7} — 규칙대로 적는다.** 이 설계에서
  **예산은 구조적으로 간격을 깎을 수 없어서** 삭제 변이로는 3번을 고립시킬 수 없다:
  `Frontier.set_delay` 가 `max` 라 내려가지 않고, 자른 잠 바로 뒤가 `break` 다.
  3번이 잡는 것은 **지운 줄이 아니라 나중에 덧붙일 줄**이다(②안이 들어오거나
  제출 루프가 준비 검사를 우회하는 날). M7 이 그 축이 살아 있음을 보여준다 —
  간격이 5.0→1.0 이 되면 3번이 실제로 죽는다. 합치지 않고 남긴다.
- **3번의 단언 하나가 항상 참이었다 — 이 스텝에서 고쳤다.** `assertLess(n, 10)` 인데
  가짜 사이트에 페이지가 3쪽뿐이라 예산이 없어도 참이었다. 한 도메인 4쪽으로 바꾸고
  `n == 2`(8초 예산 / 5초 간격) · 경과 8.0초로 못 박았다. **고치기 전에는 M1·M4 를
  못 죽였다** — 지금 죽인다.
- 다음: 스텝 3/4 리뷰 — 백지 세션

## 2026-08-29 10:45 | deadline | 리뷰 3/4 | 시도0

- **34시간 공백 뒤 재개.** 마지막 커밋 `44cbf6e`(08-28 00:23, 테스트 2/4) 이후 두
  세션이 이 자리에서 끊겼다. 작업 트리는 깨끗했고 잃은 것은 없다 — 세 번째에 통과했다.
- 한 일: `rules/review.md` 0절대로 **백지 패스를 먼저** 했다. 리뷰 세션에 `docs/` 와
  `git log` 를 막고 `crawl.py`·`test_crawl.py` 의 diff 만 줬다. 지난 status 는
  "M6·M2·1↔3 을 리뷰에게 그대로 준다" 고 했지만 **룰이 배경 제공을 금지해서 안 줬다.**
  그 셋은 패스 B 에서 대조했다. 배경을 줬다면 아래 1번은 못 나왔을 것이다 —
  "떠 있는 요청은 끝까지 간다" 는 설계 문장을 먼저 읽으면 그 자리를 의심하지 않는다.
- 결과 4건 (critical 1, informational 3). **자동 2 적용 · 승인 2 보류.**

  | # | 자리 | 판정 |
  |---|---|---|
  | 1 | 예산 만료 `break` 가 in-flight 결과를 버린다 | critical → **보류(패치)** |
  | 2 | `--deadline=5` 가 조용히 무시되고 rc 0 | **보류(패치)** |
  | 3 | 오류 테스트가 가드 회귀 시 실네트워크로 나간다 | **적용** |
  | 4 | usage `SECONDS` 인데 파서는 `int()` | **적용** |

- **1번이 이 스텝의 값이다.** `break` 지점에 `inflight` 가 남아 있으면 그 Future 들은
  `_store_result` 를 못 지나고 버려진다. 그런데 `with ThreadPoolExecutor` 는 나갈 때
  **그것들을 어차피 기다린다** — 시간은 그대로 치르고 결과만 버린다. 다음 실행에서
  같은 URL 을 또 때리니 크롤 윤리로도 손해다(workers=8 이면 최대 7건).
- **왜 단위 테스트가 못 봤나 — 구조적이다.** 가짜 시계는 `time.sleep` 에서만 흐르고,
  `sleep` 은 `if not inflight:` 안에서만 한다. 즉 **`inflight` 가 비어 있을 때만
  시간이 간다.** "떠 있는 요청을 둔 채 예산 만료" 는 가짜 시계로 도달 불가다.
  실시계(`now=time.monotonic`)에서는 fetch 도는 동안 시간이 가므로 바로 난다.
  **그래서 패치에 단위 테스트를 안 붙였다** — 붙일 자리가 없다. e2e 몫이다.
- 보류로 돌린 이유(`rules/severity.md` 3절): 1번은 critical 이고 설계 4절이 결과를
  줍는지 버리는지를 **안 정했다**(설계 결정 · 사용자 눈에 보이는 동작 변경).
  2번은 근본 원인이 `_number_flag` 이고 `--max`·`--workers` 가 같이 걸린다 —
  셋 다 고칠지가 설계 결정이라 패치는 `--deadline` 만 막는다.
  둘 다 `git apply --check` 통과. 적용 상태로 393건 OK 인 것도 확인하고 되돌렸다.
- **M6 을 직접 재현했다** — 사본에 CLI 배선 변이를 넣으니 단위는 안 죽었다
  (`test_quality_*` 3건 오류는 `data/` 의존이라 사본 탓, 변이와 무관).
- 검증: `PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 -m unittest discover tests`
  → **393건 OK** (자동 수정 2건 적용 후, 건수 변화 없음).
- 다음: 스텝 4/4 e2e — CLI 배선(M6) + 실시계에서만 나오는 in-flight 자리

## 2026-08-29 13:55 | deadline | e2e 4/4 | 시도0

- 새 파일 `e2e/deadline_e2e.py` (미추적으로 남아 있던 것을 이어받았다). 도메인 4개를
  로컬 실서버로 띄우고 **예산 2초**로 돈다. rc=0 · 9.7~10.2s.
- **이전 세션이 배선을 안 하고 끊겼다.** `scenario_0_control` 과 `--control` 이
  **정의만 돼 있고 `main()` 에서 안 불렸다.** `main()` 세 줄로 배선했다:
  `BROKEN = "--control" in sys.argv[1:]` · 시나리오 0 을 맨 앞에서 호출 · 뒤에 로그 비움.
  잣대가 없으면 "예산이 덜 모았다" 가 **아무것도 못 모으는 세계에서도 참**이 된다
  (017 이 심고 018·019 가 덕을 본 가드와 같은 관용구).
- 계획 4절 시나리오 4개 전부 실측:

  | # | 재는 것 | 실측 |
  |---|---|---|
  | 0 | 대조군 — 예산 없이 | **16페이지 전부**, 4.0s |
  | 1 | CLI 배선이 `crawl()` 까지 | 7~8p / max 20, 2.1s, stderr 에 소진 보고 |
  | 2 | 실시계 in-flight | 저장 7~8 / 서버 응답 8, 2.0s |
  | 3 | 예의 — 예산이 간격을 깎았나 | 최소 **1.00s** (하한 1초) |
  | 4 | 측정 불능 `--control` | **rc=2** |

- **계획의 숫자를 키웠다.** 계획은 예산 3초·1도메인·5페이지였는데 그러면 워커 8개가
  도메인 하나를 1초 간격으로 기다려 **동시에 뜨는 요청이 1건뿐**이라 시나리오 2
  ("떠 있는 요청을 둔 만료")가 잘 안 만들어진다. 도메인 4개·16페이지로 올렸다.
- 변이 5종을 사본에서 돌렸다(`PYTHONDONTWRITEBYTECODE=1`):

  | 변이 | e2e | 단위 393 | 판정 |
  |---|---|---|---|
  | **M6** CLI 가 `deadline=` 을 안 넘긴다 | **죽음** | **OK** | 이 파일의 존재 이유 |
  | **M-A** `crawl()` 이 `left` 를 항상 `None` | **죽음** | — | 시나리오 1 |
  | **M-E** 팝 게이트 하한 `1.0→0.05` | **죽음** | 같이 죽음 | **시나리오 0 이 잡았다** |
  | **M-C** `interval = max→min` | 통과 | **죽음(10건)** | 단위 몫 |
  | **M-D** 예산이 `_fetch_one` 간격을 깎는다 | 통과 | 통과 | **등가 변이** |

- **M6 을 이번에 다시 실측했다** — 사본에 심고 **같은 트리에서** 단위를 돌려
  `Ran 393 / OK` 를 확인했다(지난 세션은 `test_quality_*` 3건이 `data/` 의존으로
  오류 나 섞여 있었는데, 이번엔 저장소 `tests/` 를 그대로 써서 깨끗하게 393 OK).
  단위가 눈먼 이유는 `tests/test_crawl.py` 가 `crawl` 을 목으로 갈아끼우고 CLI 를
  보기 때문이다(리뷰 3/4 가 지적한 자리).
- **M-D 는 등가 변이다.** 요청 간격은 **프런티어 팝 게이트**가 이미 벌려 놓아
  `_fetch_one` 에 넘기는 간격 인자는 이 구성에서 관측되지 않는 2선 방어다.
  잡을 버그가 없는 것이지 눈이 먼 게 아니다 — M-E 로 그 게이트를 깎자 **같은
  단언이 즉시 빨개진 것**이 근거다(`간격 0.306초 — 예산이 1초 하한을 깎았다`).
- **버려진 in-flight 응답 수는 단언하지 않는다.** 0~1건을 오간다 — 그 라운드에
  요청이 떠 있었는지가 실시계 타이밍에 달렸다. 보류 패치
  `deadline-inflight-reap` 의 근거로 출력만 한다. 패치는 **적용 안 했다**(승인 대기).
- 전체 검증: 단위 **393건 OK** · e2e **15종 전부 rc=0**
  (`crawl` 14.7 · `indexer` 2.9 · `noindex` 3.7 · `search_api` 15.0 · `crawl_delay` 4.6 ·
  `non_ascii` 2.6 · `design_check` 0.1 · `domain_key` 7.1 · `url_normalize` 5.1 ·
  `crawl_politeness` 5.1 · `pagination_ui` 11.8 · `retry_interval` 15.6 ·
  `quality_eval` 0.1 · `tokenizer` 5.8 · **`deadline` 9.7**).
- `unittest discover tests` 는 `e2e/` 를 안 집는다 — e2e 가 매 스텝 딸려 도는 일 없음
  (`rules/e2e.md` 1절 확인).
- 증거: `docs/e2e/deadline/result.md` · 명령은 `docs/project.md` 에 등록.
- **계획 020 DONE.** 아카이브: `plan_history_018` · `design_history_018` ·
  `index.md` 20번 항목.

## 2026-08-29 14:20 | indexer-cli-guard | 짧은 경로 | 시도0

- **탐색 결과 이것을 골랐다.** 1~4순위(실패 테스트·TODO/FIXME·`candidates.md`)가 전부
  비었다 — 단위 393건 OK, e2e 15종 rc=0, 코드에 `TODO`/`FIXME`/`HACK` 0건.
  `digest.md ## 반복 실패` 의 "CLI 가 예상 못 한 입력에 트레이스백을 낸다"(2회)를 집었다.
  중복 방지(`discover.md` 5절) 통과: `index.md` 21개 항목·`digest.md` 처리분·
  `docs/patches/` 2건(둘 다 `deadline-*`) 어디에도 없다.
- **착수 전 셋을 다 탐침했다** — digest `[7]`("처방은 그때의 추정이다") 관용구:

  | 진입점 | 준 입력 | 결과 |
  |---|---|---|
  | `serve.main` | `--port abc` · `--port`(값 없음) | 이미 막는다 |
  | `crawl.main` | 020 이 `--deadline` 까지 덮었다 | 이미 막는다 |
  | `indexer.main` | `pages` 없는 DB | **트레이스백 + rc=1** |

- **그래서 처방을 줄였다.** digest 가 적은 근본 원인은 "CLI 진입점마다 방어를 따로
  쓴다" 였고 그대로 읽으면 공통 방어층을 만드는 계획이 된다. 실제로 남은 구멍은
  **한 곳**이라 한 곳만 고쳤다 — 한 개짜리 문제에 추상을 세우지 않는다.
- 고친 자리: `index_pages()` 가 읽기 직전 `sqlite_master` 를 한 번 보고
  `NoCrawlDataError`. `main()` 은 `StaleIndexError` 와 **같은 관용구**로 받아
  복구법(`먼저 크롤한다: …`)을 찍고 rc=2. 새 관용구를 만들지 않았다.
- **0 으로 합치지 않았다.** "크롤 데이터가 없다" 와 "크롤했는데 색인할 게 0건" 은
  다른 상태다 — 합치면 DB 경로 오타가 조용한 성공으로 보인다. 이 저장소가 반복해서
  세우는 구분(`결과 없음` 출력·측정 불능 종료 2)과 같은 값이다.
- TDD: 먼저 2건을 빨갛게 만들고(`Ran 396 / FAILED (errors=2)`) 구현했다.
- 변이 2종에서 죽는 것 확인(사본, `PYTHONDONTWRITEBYTECODE=1`):
  - 가드 제거(원래 버그로 되돌리기) → **2건 ERROR**
  - `'pages'` 를 `'docs'` 로 오타 → **5건 ERROR**. 여기에 대조군
    `test_empty_pages_table_is_zero_not_an_error` 가 들어 있다 — 대조군이 제 값을 한다
- 검증: 단위 **393 → 396건 OK** · e2e **15종 전부 rc=0**.
- 브랜치 `loop/indexer-cli-guard` (기점 `9a47341`). 계획서·e2e 문서 없음(짧은 경로).
  `index.md` 21번에 한 줄 남겼다 — 안 적으면 중복 방지가 이 작업을 못 본다.
