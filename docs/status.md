---
signal: GREEN
phase: e2e
step: 1
attempt: 0
iteration: 241
updated: 2026-09-02
ctx: 50
night_iterations: 96
night_red: 0
night_retries: 0
plan: json-contract
---

# 현재 상태

**계획 46 `json-contract` 리뷰 phase 완료 — 제품 코드 결함 0건.**
백지 패스(`3d766be..HEAD` 만 보고 계획·설계를 안 연 채)에서 **가설 셋을 세워 둘을 실측으로
죽이고 하나를 살렸다.** 남은 발견은 **둘 다 low** 이고 제품 `src/` 무수정이다.
**다음 반복은 e2e phase.**

| # | 발견 | 등급 | 처리 |
|---|---|---|---|
| **[A]** | 변이 이름 **M15·M16 이 설계서 §8 의 다른 둘과 충돌** | low (버전·경로 불일치 → 자동) | **M17·M18 로 고쳐 부른다** (아래) |
| **[B]** | FTS 토큰이 안 나오는 질의는 DB 고장 판정을 **건너뛴다** | low · **이 diff 가 만든 것 아님** | `digest.md` 후보 `[4]` 에 붙였다 |
| ~~[C]~~ | 503 에 `Retry-After` 가 없다 | **기각** | 설계서 5·8절이 이미 닫았다 — *"서버가 모르는 숫자다"* |

## 방금 한 것 (2026-09-02 · 계획 46 리뷰 1/1)

- **백지 패스를 먼저 끝내고 대조 패스를 열었다**(`rules/review.md` 0절). 백지에서 나온
  가설 셋 중 둘이 대조 패스에서 죽었다 — `Retry-After`(설계 5·8절이 *"안 붙인다"* 를
  사유와 함께 이미 적어 뒀다)와 **미색인 200**(갈림길 D 의 명시적 결정이고
  `TestUnindexedDb` 독스트링·README 가 둘 다 정직하게 열거한다). 셋째만 살아남았다.
- **[A] 변이 이름이 겹쳤다 — 실물 충돌이다.** `design_json-contract.md` 8절이 **범위 밖**
  변이 둘에 이미 **M15**(`ORDER BY` 흔들기)·**M16**(탐침 행 누출)을 쓰고 있는데,
  반복 240 이 **새로 세운 변이 둘**에 같은 두 이름을 다시 붙였다. 저장소 자신의 규약
  (`DocCitationTest` — append 전용 문서는 **이름·항목**으로 인용한다)이 이름에 기대는데
  그 이름이 둘을 가리킨다. **이번 반복부터 M17**(양쪽 503 튜플에 `sqlite3.DatabaseError`
  추가 → 새 단언 3건 사망) **· M18**(같은 넓힘을 화면 경로에만 → 화면 단언 1건 사망)
  **로 부른다.** `history_current.md` 는 수정·삭제 금지라 이번 항목에 정정을 **append**
  했다 — 그 파일의 M15·M16 은 반복 240 것이고, 설계서 8절의 M15·M16 은 범위 밖 둘이다.
- **[B] 무토큰 질의가 DB 고장 판정을 건너뛴다 — 임시 디렉터리 탐침으로 실측.**
  `indexer.search` 는 `_fts_query(query)` 가 빈 문자열이면 `sqlite3.connect` **앞에서**
  `return []` 한다. 그래서 **손상된 DB + `q=%01` 은 200**(`q=김치` 는 같은 DB 에서
  `DatabaseError` → 500). 제어문자는 `_parse` 의 `strip()` 을 통과하고 `_fts_query` 가
  지운다. **없는 DB 는 `os.path.exists` 가 먼저라 어느 질의든 503 그대로다.**
  계획 46 이 «어떤 고장에 어떤 코드» 를 계약으로 만든 그 계약에 안 적힌 예외지만,
  **이 diff 이전에도 같은 갈래였고**(그땐 500 vs 200) 고칠 자리는 `serve` 가 아니라
  `indexer.search` 다 — 그래서 `[4]` TOCTOU 와 **같은 뿌리**로 묶어 계획 47 에 넘겼다.
  도달하려면 제어문자만으로 된 질의를 보내야 해 값이 낮다.
- **Pass 1(critical) 은 전부 비었다.** 데이터 안전 — 쓰기 0 · `data/crawl.db` 무변경.
  레이스 — 새 공유 상태 0(`VERSION` 은 모듈 상수). 주입 — 503 본문은 양 경로 다 **고정
  리터럴**이고 화면 쪽 사용자 입력(`typed`)은 `html.escape` 를 지난다. **신뢰 경계** —
  503·500 이 DB 경로·트레이스백을 안 흘리는 것을 JSON·HTML 네 단언이 못박는다.
- **열거형 완전성(새 상태 코드 503)을 diff 밖까지 전수로 봤다** — `README.md` 갱신됨 ·
  `e2e/tokenizer_e2e.py` ⑥ 갱신됨 · `docs/specs/concept.md` 디자인 5 는 처음부터 503 ·
  `docs/project.md` 는 serve 상태 코드를 열거하지 않는다. **빠진 곳 0.**
  다만 `e2e/search_api_e2e.py` 의 상태 코드 표는 400·404·501 만 돌고 **503 도 `version`
  도 안 잰다** — 결함이 아니라 **다음 e2e phase 가 채울 자리**다(아래 «다음»).
- **`_send` 의 `{"version": VERSION, **payload}` 는 payload 가 이기는 병합**이지만
  다섯 호출부 어디도 `"version"` 을 안 넘기고, 넘기는 날 `TestSchemaVersion` 의 최상위
  키 집합 단언이 문다. 방어를 더하지 않는다(ponytail 1번 · 신뢰도 80 미만).
- **대조 패스(렌즈 1·4·5)도 깨끗하다.** 설계서가 스텝별로 약속한 것과 코드가 한 줄씩
  맞고(순서 계약 `ValueError`→503→`Exception`, `_send` 한 곳, `src` 순증 17줄 ≈ 추정 16),
  계획서 정정 한 줄(README 를 스텝 2 → 1)도 실제로 스텝 1 에서 처리됐다.
  주석의 지침을 어긴 자리 0 · 직교 편집 0 · 커밋 메시지 전부 한국어.

## 한도 (매 반복 확인)

- `main` 직접 커밋 금지 · `--no-verify` 금지 · 외부 네트워크 금지 ·
  `docs/specs/` 읽기만 · `data/crawl.db` 무변경 · 의존성 추가 금지(stdlib 만).
- **러너 파이프 0회 — 연속이 다시 섰다.** 러너를 부른 것은 **맨몸 2회**뿐이고
  파이프·`grep`·`head`·`tail`·리다이렉션·백그라운드 전부 0회다. 두 실행 다
  `Ran … OK` 줄을 눈으로 읽었다(잘린 줄 0).
- 이번 반복 실측(맨몸): `PYTHONPATH=src python3 -m unittest discover -s tests`
  **495건 OK** — 리뷰 전 12.213초 · 문서 수정 후 재실행도 495건 OK.
  제품 `src/` **+0줄** · 새 파일 0 · 의존성 0 ·
  `data/crawl.db` sha256 `85c96744…` **무변경**(리뷰 탐침은 임시 디렉터리에서만 돌았다).
- **README 카운트 가드가 조용하다** — 단위 495 그대로라 다섯 반복 만에 처음 안 울었다.
- 제자리 변이 0 — `.mutation-lock` **없음** · `git status` 에 `src/` 수정 0건.
  이번 반복은 변이를 안 심었다(리뷰 phase 는 diff 를 읽는 자리다).
- **집안일 없음 — 회전 안 돌렸다.** `history_current.md` 170줄(상한 300) · 아카이브 26개 ·
  `digest.md` 명부 구멍 0 · **200줄 그대로**(`[4]` 항목을 줄 추가 없이 늘렸다).
- **설계서가 한 화면을 넘는 건(186줄)은 그대로 열려 있다.** 계획 phase 에서 갈림길 수를
  세는 것이 옳은 자리라고 반복 237 이 적어 뒀다.

## 다음 — e2e phase 가 채울 자리 (리뷰가 지목했다)

**계획 46 이 만든 계약 둘을 진짜 프로세스로 재는 e2e 가 아직 0건이다.** 단위는 495건이
덮지만 단위는 작성자 머리 안에 있다 — `rules/review.md` 0절이 드는 실제 사례가 그 자리다.

1. **503 을 e2e 로 잰다.** `e2e/search_api_e2e.py` 의 상태 코드 표(오늘 400·404·501)에
   **DB 없음 → 503** 을 더한다. 드리프트 쪽은 `tokenizer_e2e.py` ⑥ 이 **이미 화면으로**
   재고 있으니(500 → 503 으로 이번 계획이 고쳤다) 겹쳐 만들지 않는다.
2. **`version` 을 e2e 로 잰다.** 같은 파일이 JSON 을 파싱하는 자리가 이미 넷이라
   `first["version"] == 1` 한 줄이면 붙는다 — **새 스크립트를 만들지 않는다.**
3. **p95 를 기준선과 비교해 적는다** — 설계 7절 판정 ②(기준선 8.86ms 의 2배를 넘으면
   설계가 틀린 것). 판정 ①(300ms)은 `perf_search.py` 가 자동으로 문다.

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-01 확인)

`origin/main` 은 `494313b`(PR #5 병합). 계획 43·44·45 의 커밋들(`bc40ea9` 이후)이
아직 안 갔다. **남은 것 (사람 결정)**: 그것들을 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).
계획 46 의 기점은 `3d766be`(계획 45 DONE 커밋)이고 `main` 이 아닌 이유는 계획서 머리에
적어 뒀다 — `main` 기점은 `serve.py` 를 계획 44 이전으로 되돌리고 기록 4파일을 반복 221
로 되돌려 계획 45 가 세운 두 가드를 스스로 깬다.
**직전 반복이 원격 자동 스냅샷(`c3bd8c6`)을 `-s ours` 로 병합해 푸시를 풀었다** —
이번 반복도 푸시 전에 `git log --oneline -3` 을 확인했고 강제 푸시는 안 썼다.

## 사람이 정할 것 — 셋이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
   offset 이 0 이 되는 날 검사기가 멈추고, 짝은 그때 열릴 계획의 일이다.
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   이제 *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, 회전을 지시하는 `~/.claude/skills/loop-harness/rules/docs.md` 에는
   그 예외가 없다. **저장소 밖이라 루프가 못 고친다.** 실동작은 안전하다는 것을
   계획 45 e2e 가 실측했다(잊으면 RED · 완료 항목을 지워도 색인은 산다). 룰 문장을
   맞출지는 사람 몫.

**사양이 사람에게 남긴 것 셋은 계획 46 의 범위 밖이다** (`docs/specs/concept.md` 의
`## 사람이 정할 것`): 속도 제한 시점(가정 IP 당 분당 60회) · JSON 검색과 근거 문단의
분할(가정 쪼갠다 — 계획 46 이 그 가정을 따랐다) · 사양 숫자들이 초안이라는 것
(90% · 500ms · 60회/분). 첫째와 셋째는 아직 열려 있다.
