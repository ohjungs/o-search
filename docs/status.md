---
signal: GREEN
phase: 리뷰
step: 1
attempt: 0
iteration: 240
updated: 2026-09-02
ctx: 50
night_iterations: 96
night_red: 0
night_retries: 0
plan: json-contract
---

# 현재 상태

**계획 46 `json-contract` 테스트 phase 완료 — 개발 3스텝 + 테스트까지 끝났다.**
설계 변이 표 **M1~M14 를 전수 실물 대조**했고 **살아남은 변이 0건**이다.
표가 못 본 갭 하나(중요도 8)를 그 자리에서 메웠다 — 단위 **492 → 495건 OK**.
**다음 반복은 리뷰 phase.**

| 변이 | 무엇 | 결과 |
|---|---|---|
| M1~M14 (설계 표) | 503 절 · 버전 · 화면↔JSON 순서 | **14/14 사망** · 생존 0 |
| **M15 (새로 세움)** | 503 튜플에 `sqlite3.DatabaseError` 추가 | 전엔 **아무도 안 죽었다** → 이제 3건 사망 |
| **M16 (새로 세움)** | 같은 넓힘을 화면 경로에만 | 화면 단언 1건 사망 (두 경로 한 벌) |

## 방금 한 것 (2026-09-02 · 계획 46 테스트 1/1)

- **갭 하나를 찾아 메웠다 — `TestBrokenDbStays500` 3건**(`tests/test_serve.py`).
  두 경로의 500 을 재는 기존 단언 둘은 `_page_hits` 를 **mock** 으로 갈아
  `AttributeError` 를 던진다. 그래서 못박힌 것은 *"코드가 틀렸을 때 500"* 뿐이고
  **실물 DB 상태로 500 을 재는 단언은 0건**이었다 — 손상된 DB · 못 읽는 DB 가 그것이다.
  제품 `src/` **0줄** · 새 파일 0 · 의존성 0.
- **왜 8점인가.** 503 은 *"색인을 다시 돌리면 낫는다"* 는 약속이고 500 은 *"고쳐야 한다"* 다.
  손상된 DB 를 503 으로 내면 인프라가 **영영 재시도**한다. 그 변이(M15)가 495건 중
  **한 건도 안 죽이던 자리**였다 — M5(`except Exception` 통째로 503)는 mock 이 잡지만
  튜플 쪽으로 넓히는 변이는 그 사각지대로 들어온다.
- **설계서 §1 의 예외 이름 하나를 실물이 정정했다.** 못 읽는 DB 는 `PermissionError` 가
  아니라 `sqlite3.OperationalError('unable to open database file')` 로 온다
  (`DatabaseError` 의 하위라 **잡히는 자리는 같다**). 손상 DB 는
  `DatabaseError('file is not a database')`. 이름으로 짜는 단언이었으면 빗나갔다.
- **M1~M14 는 전부 제자리에 심어 확인했다** — `.mutation-lock` 아래, 변이마다
  치환 개수를 먼저 단언하고(안 심긴 것을 «생존» 으로 읽지 않는다) 원문 복원 후 재실행.
  죽은 단언 이름까지 봤다(M14 → `test_screen_and_json_list_the_same_urls_in_the_same_order`).
- **8점 미만 갭 하나는 `digest.md` 후보 절에 남겼다** — `[4]` `indexer.search` 의
  `os.path.exists` → `connect` TOCTOU(그 창에서 DB 가 지워지면 503 이 아니라 200 이 된다).
  **`serve` 가 아니라 `indexer` 를 고칠 일**이라 계획 47 이 그 파일을 열 때 같이 본다.
- **e2e 넷을 값으로 확인했다** — `search_api_e2e`·`pagination_ui_e2e`·`tokenizer_e2e`·
  `perf_search` 전부 rc=0. p95 **8.79ms**(예산 300ms 의 2.9%) — 기준선 8.86ms 그대로다.

## 한도 (매 반복 확인)

- `main` 직접 커밋 금지 · `--no-verify` 금지 · 외부 네트워크 금지 ·
  `docs/specs/` 읽기만 · `data/crawl.db` 무변경 · 의존성 추가 금지(stdlib 만).
- **러너 파이프 2회 — 연속 0 이 또 끊겼다.** 첫 전체 실행 `| tail -20` · 문서 가드
  `| tail -0`. 잘려 못 본 것은 없었지만 그것은 운이고, **판정 수치는 전부 맨몸
  재실행의 것**이다(변이 16회는 스크립트가 러너를 직접 불러 파이프 자체가 없다).
- 이번 반복 실측(맨몸): `PYTHONPATH=src python3 -m unittest discover -s tests`
  **495건 OK**(12.237초) · e2e 넷 전부 rc=0. 제품 `src/` **+0줄** ·
  `data/crawl.db` sha256 `85c96744…` **무변경**(변이·탐침 전부 임시 디렉터리) ·
  의존성 0 · 새 제품 파일 0.
- README 카운트 가드가 **네 반복 연속 울었다** — 단위 492 → 495 반영.
- 제자리 변이 0 — `.mutation-lock` **없음**(심는 동안만 두고 지웠다). 되돌림은
  sha256 이 아니라 **`python3 -B` 재실행 초록**으로 확인했다(같은 바이트 제자리 변이는
  해시가 같다) · `git status` 에 `src/` 수정 0건.
- **집안일 — 회전 돌렸다.** 설계·개발 네 반복 → `history_026.md`(290줄 → 116줄),
  **자기 항목을 붙이기 전에**. 아카이브 26개 · `digest.md` 명부 구멍 0 · 200줄 그대로.
- **설계서가 한 화면을 넘는 건(186줄)은 그대로 열려 있다.** 계획 phase 에서 갈림길 수를
  세는 것이 옳은 자리라고 반복 237 이 적어 뒀다.

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-01 확인)

`origin/main` 은 `494313b`(PR #5 병합). 계획 43·44·45 의 커밋들(`bc40ea9` 이후)이
아직 안 갔다. **남은 것 (사람 결정)**: 그것들을 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).
계획 46 의 기점은 `3d766be`(계획 45 DONE 커밋)이고 `main` 이 아닌 이유는 계획서 머리에
적어 뒀다 — `main` 기점은 `serve.py` 를 계획 44 이전으로 되돌리고 기록 4파일을 반복 221
로 되돌려 계획 45 가 세운 두 가드를 스스로 깬다.
**직전 반복이 원격 자동 스냅샷(`c3bd8c6`)을 `-s ours` 로 병합해 푸시를 풀었다** —
이번 반복은 푸시 전에 `git log --oneline -3` 과 fetch 를 확인했고 강제 푸시는 안 썼다.

## 사람이 정할 것 — 셋이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
   offset 이 0 이 되는 날 검사기가 멈추고, 짝은 그때 열릴 계획의 일이다.
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   이제 *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, 회전을 지시하는 `~/.claude/skills/loop-harness/rules/docs.md` 에는
   그 예외가 없다. **저장소 밖이라 루프가 못 고친다.** 실동작은 안전하다는 것을
   계획 45 e2e 가 실측했다(잊으면 RED · 완료 항목을 지워도 색인은 산다). 룰 문장을
   맞출지는 사람 몫.

**사양이 사람에게 남긴 것 셋은 계획 46 의 범위 밖이다** (`docs/specs/concept.md` 의
`## 사람이 정할 것`): 속도 제한 시점(가정 IP 당 분당 60회) · JSON 검색과 근거 문단의
분할(가정 쪼갠다 — 계획 46 이 그 가정을 따랐다) · 사양 숫자들이 초안이라는 것
(90% · 500ms · 60회/분). 첫째와 셋째는 아직 열려 있다.
