# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**회전 명부는 `digest.md` 의 `## 완료` 절 «아카이브 명부» 줄이 정본이다.**
여기 있던 스물한 회전의 서술(233줄)은 그 줄과 내용이 겹쳤고, 검사가 강제하는 명부도
그쪽 하나뿐이라(`tests/test_docs.py` 의 `ArchiveIndexTest`) **개발 9(반복 269)가 이
자리에서 지웠다** — 회전으로는 300줄 상한을 못 맞추던 세 반복(309 → 372 → 418줄)의
원인이 이 명부였다. **지운 것은 머리말이지 반복 기록이 아니다** — 항목은 여전히
append 전용이고 수정·삭제 금지다. 각 회전의 사유는 `digest.md` 의 같은 줄에, 원문은
`history_<NNN>.md` 에 그대로 있다.

## 2026-09-06 00:00 | (endtag-cut-cover 마감 → index-step-sync) | 계획 0/1 | 시도0

- 한 일: **계획 59 마감.** `plan_endtag-cut-cover.md` → **`plan_history_045.md`**(`git mv` ·
  내용 무변경 · 설계서는 반복 345 가 트리거 0 으로 생략해 **없다**). `index.md` 의 계획 59
  줄을 **진행 0/1 → 완료 1/1**(완료 기준 6/6 · 새 e2e 0개 · 21종 rc 0 · 회귀 0 · 제품 0줄)로
  닫고 아카이브 파일명을 적었다. `digest.md` `## 완료` 에 한 줄 · 후보 `[5]` 에 취소선.
- 한 일: **계획 60 탐색.** 1~5순위 실측 **0건** — 전수 `Ran 605 tests in 13.680s` · `OK` ·
  rc 0 · 린터/타입체커 없음 · `TODO`/`FIXME`/`HACK` 1건은 파서 입력 문자열 안 ·
  `candidates.md` 없음 · `patches/` 없음 · `digest ## 보류` 0건 · 이슈 0건.
- 결과: **6순위가 처음으로 소진됐다.** 취소선 안 그어진 항목을 전수로 훑으니 **열린 것
  전부가 「여는 조건 미도래」**다(실물 코퍼스 · 재색인 · 사람 결정 · 「그 파일을 손대는 날」).
  계획 59 가 집어간 `[5]` 가 여는 조건이 따로 없던 **유일한** 항목이었고 그 자리가 비었다.
- 결과: **7순위 `## 반복 실패` 에서 5회째가 HEAD 에 살아 있었다.** 「스텝을 커밋하면서
  `index.md`·`metrics.md` 숫자를 안 올린다」(4회 등재)를 착수 탐침이 다시 쟀다 — 최근
  30커밋에서 `status.md` `step` 과 `index.md` 진행 행을 나란히 뽑으니 **어긋난 커밋 11건**
  (계획 56 ×4 · 57 ×3 · 58 ×3 · 59 ×1)이고 **11/11 전부** `index` 가 뒤처지는 방향이다.
  `IterationSyncTest` 가 닫은 것은 `반복`↔`iteration` 축뿐이다.
- 결과: **기록된 처방을 갈아 끼웠다**(`digest [7]` 열한 번째 적용). 항목이 적어 둔 「`git log`
  의 개발 커밋 수와 대조」는 커밋 메시지 문자열에 기대는데, 같은 값을 **문서 둘**로 재면
  `IterationSyncTest` 와 같은 모양이다. 그 판정과 11건 실측을 `digest ## 반복 실패` 에 적었다.
- 다음: **설계 phase.** `rules/design.md` 1절 **「대안이 갈림」에 걸린다** — 진행 행이 0개인
  커밋이 실재해(`9c6f763`·`e66968c`·`724d8c8`) A(정확히 1행) · B(있을 때만) · C(`git log`)가
  서로 다른 것을 잡는다. 눈금은 「30커밋에 먹여 진짜 11건을 잡고 오탐 몇 건인가」다.
  고친 파일은 `docs/` 뿐이다 — `src/`·`tests/`·`e2e/`·`README.md`·`docs/specs/`·
  `data/crawl.db` 무접촉. 러너 규율 위반 **0회**(누적 37).

## 2026-09-06 01:20 | index-step-sync | 설계 0/1 | 시도0

- 한 일: **문서 한도 두 건을 append 전에 닫았다.** `history_current.md` 299줄 → 계획 58
  `passage-cost-band` 의 여섯 반복(339~344 · 113줄)을 **`docs/history_061.md`** 로 회전하고
  `digest.md` 「아카이브 명부」에 등재(`ArchiveIndexTest` 초록 · 남은 186줄). `digest.md`
  202줄 → 가장 오래된 완료 항목 둘(계획 50 `runner-quiet`·51 `hidden-passage`)을 지워 **200줄**
  (원본은 `plan_history_036/037.md`·`index.md` 에 남는다). 커밋 `61e82e7` 로 먼저 끊었다.
- 한 일: **계획 60 설계 phase.** 산출물은 `docs/design_index-step-sync.md` 하나 ·
  제품 `src/`·`tests/` **0줄**. 계획서가 세운 A·B·C 를 눈금(최근 30커밋 · 진짜 어긋남 11건)에
  먹여 실측하고, **네 번째 안 D(‹`plan:` 슬러그로 `index.md` 행을 집는다›)를 세워 골랐다.**
- 결과: **A 11잡음/오탐 3 · B 11잡음/조용한 통과 3 · D 12잡음/오탐 0/조용한 통과 0.**
  C 는 계획서 2절 ②가 이미 약화 판정해 측정 대상이 아니다. D 가 하나 더 잡는 `9c6f763` 은
  **오탐이 아니라 진짜 결함**이다 — `status` 가 계획 57 을 설계 phase 로 열어 놓았는데
  `index.md` 에 그 계획의 행이 **아예 없었다**. 슬러그로 집으면 A·B 가 씨름하던
  **「진행 행 0개」라는 상태 자체가 사라진다**(행이 있거나 · 없으면 그것이 결함이거나).
- 결과: **가정을 깨봤다**(`design.md` 3-2절) — 「`plan:` 첫 토큰 == `index` 의 `plan_<슬러그>`
  행 이름」은 오늘 형식을 쓰는 **최근 344커밋에서 342건 중 341건 참**이고 유일한 예외가 위
  `9c6f763` 이다. 슬러그가 없는 2건(`a4a4da1`·`bfc4c17`)은 **둘 다 `plan: null`+`step: 0/0`**
  이라 계약의 `null` 갈래가 덮는다 — 하네스 템플릿의 초기 상태 그대로다. **천장**: 그보다 옛
  구간은 `index.md` 에 계획 행 자체가 없던 시절이라 D 를 먹이면 대량으로 빨개진다(검사는
  작업 트리 HEAD 에서만 도니 대상 아님). 탐침 셋은 전부 **스크래치패드**에서 돌렸고
  저장소 파일·`data/crawl.db` 는 안 건드렸다.
- 결과: **계획서도 함께 고쳤다** — 3절의 「상태 칸이 `진행` 인 행」을 「`plan:` 슬러그가
  가리키는 행」으로, 완료 기준 4 의 넓히는 변이 예시를 「슬러그를 안 보고 아무 행이나 잡는다 ·
  접두로 넓혀 `plan_index-step-sync-2` 를 통과시킨다」로. 설계가 계약을 바꿨으면 계획서에
  자국을 남긴다(`design.md` 6절).
- 결과: 문서 가드 `Ran 11 tests` · `OK` · rc 0(`tests.test_docs`) · 전수 `Ran 605 tests` ·
  `OK` · rc 0(맨몸 · 리다이렉션 0). `status.md`(iteration 351)와 `metrics.md`(반복 351 ·
  설계 23→24)를 맞췄다(`IterationSyncTest`).
- 다음: **개발 1/1.** `tests/test_docs.py` 에 `StepSyncTest`·`StepPatternTest` 를 세운다 —
  계약은 설계서 「계약」 절이 정본이다. 러너 규율 위반 **0회**(누적 37).

## 2026-09-06 02:05 | index-step-sync | 개발 1/1 | 시도0

- 한 일: **스텝 축 가드를 세웠다.** `tests/test_docs.py` 에 `StepSyncTest`(실물 문서)·
  `StepPatternTest`(합성 표)를 `IterationSyncTest`·`IterationPatternTest` **옆자리**로 더하고,
  상수 셋 `STEP_LINE`(`^step: (N/M)$`)·`PLAN_SLUG`(`^plan: ([A-Za-z0-9_-]+)`)·
  `STEP_ROW`(슬러그를 `re.escape` 해 끼우는 템플릿)를 `ITER_ROW`·`ITER_LINE` 옆에 뒀다.
  헬퍼는 `step_row(slug)` 한 줄이다. 제품 `src/` **0줄** · 새 파일 0개.
- 한 일: **TDD 0절 — 실패를 양방향으로 눈으로 봤다.** ① 검사를 먼저 쓰고 `index.md` 의
  스텝 칸만 `1/1` 로 비틀어 `'1/1' != '0/1'` RED ② 되돌리고 `status.md` 의 `step` 만 `1/1` 로
  비틀어 `'0/1' != '1/1'` RED — **한쪽만 재는 검사가 아니다**(완료 기준 2·3). 둘 다
  되돌리면 초록이었고, 비틀기는 즉시 원복했다.
- 결과: **변이 5종 전멸**(`mock.patch.object` · 메모리 · 저장소 파일 무변). M1 슬러그를 안
  본다 → **3죽음**(`StepSyncTest` + 패턴 둘) · M2 접두 일치로 넓힌다 → 1(`-2` 를 대신 집는다)
  · M3 `PLAN_SLUG` 이 줄 전체를 슬러그로 읽는다 → 2 · M4 `STEP_LINE` 이 `N/M` 을 안 요구한다
  → 1 · **M5 상태 칸을 `진행` 으로 좁힌다 → 1.** M5 가 죽는 것이 **안 D 를 A·B 와 가르는
  자리**다 — 합성 표의 대상 행은 상태가 `완료` 이고 D 는 상태를 안 본다. 변이 없음은 4/4 초록.
- 결과: **이 계획이 자기 첫 시험대였다.** 착수 시점 관례가 갈려 있었다 — 계획 57·58 의 개발
  커밋은 `status.step` 을 `1/1` 로 올리고 `index` 를 `0/1` 에 뒀고(그것이 어긋남 11건의 정체)
  계획 59 는 반대로 `status` 를 `0/1` 로 되돌렸다. **이 커밋은 둘을 함께 `1/1` 로 올린다** —
  새 검사가 요구하는 방향이다.
- 결과: 전수 `Ran 609 tests` · `OK` · **rc 0**(맨몸). 605 → **609**(+4) 라 `README.md` 의
  단위 건수 줄을 609 로 맞췄다(`ReadmeCommandsTest` 초록 — 이 실패가 건수 갱신을 먼저 물었다).
  `status.md`(iteration 352)·`metrics.md`(반복 352 · 개발 81→82)를 맞췄다(`IterationSyncTest`).
- 결과: **러너 규율 위반 1회 — 누적 37 → 38.** 전수를 `| tail -4` 로 잘라 `rc` 가 파이프의
  0 으로 가려졌다(README 건수 실패가 `FAILED` 인데 `rc=0` 으로 보였다). 맨몸으로 다시 돌려
  판정 줄과 `rc` 를 눈으로 봤다.
- 다음: **테스트 phase.** 새 가드 둘의 갭을 훑는다. 아는 천장 둘은 설계서 「범위 밖」에
  적혀 있다 — ① 상태 칸 자신의 정합성은 안 D 가 못 본다 ② 검사는 작업 트리 HEAD 만 본다.

## 2026-09-06 02:45 | index-step-sync | 테스트 1/1 시도0

- 한 일: **계획 60 테스트 phase.** 계획은 제품 `src/` 0줄이라 룰 3절(갭 탐색)만 돌았다.
  어제 선 `StepSyncTest`·`StepPatternTest` 에 변이를 하나씩 걸고 **전수**를 돌려
  「죽이는 단언이 몇 건인가」를 셌다 — 변이는 전부 **메모리에서** 걸었다(소스를 문자열로
  치환해 이미 로드된 `test_docs` 모듈에 다시 exec · 저장소 파일 무변경 · 하네스는
  스크래치패드 `gap60.py` · `PYTHONPYCACHEPREFIX=$(mktemp -d)`).
- 결과: **8점짜리 갭 하나 — `StepSyncTest` 의 판정 전체가 아무에게도 안 재어지고 있었다.**
  실물 두 문서가 오늘 서로 맞아 있어서(초록 데이터) 판정을 어떻게 비틀어도 전수 609건이
  초록이다 — **변이 6종 6/6 생존**: ① null 갈래 기대값 `0/0`→`9/9` · ② null 갈래 통째 삭제 ·
  ③ 대조를 자기비교로(`r.group(1)`→`s.group(1)`) · ④ 「index 에 행이 없다」 가드 삭제 ·
  ⑤⑥ 「status 에 `step:`/`plan:` 줄이 없다」 가드 삭제. `StepPatternTest` 가 재던 것은
  **정규식 셋**이지 그 위의 판정이 아니었고, 설계서가 적은 「조용히 지나가는 갈래는 0개다」를
  **재는 것이 0개**였다. `CitationPatternTest`·`ArchiveMatchTest` 독스트링의
  「린트형 데이터가 초록일 때 검사는 자신을 못 잰다」 세 번째 사례다.
- 결과: **닫았다 — 새 관용구를 만들지 않고 이 파일에 이미 있는 모양을 그대로 썼다.**
  `done_section`·`indexed` 가 `ArchiveMatchTest` 를 위해 나온 것과 같게, 판정을
  **`step_gap(status_text, index_text)` 순수 함수**(어긋남 한 줄 또는 `None`)로 빼고
  실물은 `StepSyncTest` 가, 갈래는 새 `StepGapTest` 5건이 합성 문자열로 부른다.
  단언은 한 줄도 안 낮췄다(`rules/test.md` 6절) — 같은 두 문서를 읽어 같은 것을 요구하고
  실패 메시지는 `assertIsNone(gap, gap)` 으로 그대로 나온다.
- 결과: **고친 뒤 같은 변이 6종 재측 6/6 사망**, 각각 의도한 단언이 죽였다
  (①② `test_null_plan_requires_the_zero_step` · ③ `test_step_mismatch_is_reported` ·
  ④ `test_missing_index_row_is_reported` · ⑤⑥ `test_missing_status_lines_are_reported`).
  **하네스에 이빨이 있는지 양성 대조로 봤다** — `STEP_ROW` 에서 슬러그를 빼면 7건이 죽는다
  (`StepPatternTest` 2 + `StepGapTest` 3 + `StepSyncTest` 1 + README 건수 1).
- 결과: 전수 609 → **614건 · `OK` · rc 0**(맨몸 · 파이프/리다이렉션 0 · 판정 줄과 `rc` 를
  눈으로 봤다). +5 라 `README.md` 단위 건수 줄을 614 로 맞췄다 — 이번에도
  `ReadmeCommandsTest` 가 먼저 물었다(변이 표의 대조군이 그 실패 1건으로 나왔다).
  `status.md`(iteration 353)·`metrics.md`(반복 353 · 테스트 45→46)를 맞췄다(`IterationSyncTest`).
- 결과: **남긴 갭 하나 — 6점.** `IterationSyncTest` 도 같은 구멍이다(축만 다르다):
  대조를 자기비교로 비트는 변이가 전수 614건에서 **생존**한다(실측). 갈래가 하나뿐이라
  썩을 표면이 스텝 축의 1/4 이고 `iteration` 은 매 반복 손으로 읽는 자리라 조용히 안 흐른다.
  오늘 손대는 것은 직교 편집이라 `digest ## 다음 계획 후보 (테스트 phase 갭)` 에 `[6]` 으로
  남겼다(여는 조건 「반복 축 검사를 손대는 날」 · 답은 `iter_gap` 세 줄).
- 결과: `digest.md` 가 200/200 이라 새 후보 한 줄을 넣으며 **가장 오래된 완료 항목**
  (계획 52 `focus-ring-combinator`)을 지웠다 — 내용은 `index.md` 52번 행과
  `plan_history_038.md`·`design_history_038.md` 에 그대로 있고 **아카이브 명부는 무접촉**이다
  (`ArchiveIndexTest` 초록). 다시 200줄.
- 만진 파일: `tests/test_docs.py`·`README.md`·`docs/` 뿐이다. 제품 `src/` **0줄** ·
  `e2e/`·`docs/specs/`·`data/crawl.db` 무접촉 · 재색인·스키마·새 의존성 0 · 새 파일 0개.
- 다음: **리뷰 phase 1/1.** 볼 것은 ① 함수로 빼면서 계약이 조용히 바뀐 곳(특히 `s`/`p`
  검사 순서 — 옛 코드는 둘을 본 뒤 실패했고 새 코드는 `step:` 을 먼저 문다) ② `StepGapTest`
  의 합성 `INDEX` 가 실물 표 모양과 어긋나지 않았나 ③ 지운 완료 항목이 정말 다른 곳에 남아 있나.
  러너 규율 위반 **0회**(누적 38).

## 2026-09-06 03:30 | index-step-sync | 리뷰 1/1 | 시도0

- 한 일: **회전을 먼저 했다**(append 전에). `history_current.md` 293/300 이라 계획 59
  `endtag-cut-cover` 구간(반복 345~349) **131줄**을 원본 그대로 **`history_062.md`** 로
  밀고, `digest.md` 의 **아카이브 명부**에 등재했다(`ArchiveIndexTest` 초록).
  `history_current.md` 162줄 · `digest.md` **200줄 그대로** — 계획 59 의 압축 줄은
  반복 350 이 마감하며 이미 `## 완료` 에 넣어 뒀고 명부는 한 줄 안에서 늘어난다.
- 한 일: **백지 패스를 별도 세션에 넘겼다**(`rules/review.md` 0절). 준 것은
  `tests/test_docs.py` diff 와 그 파일뿐이고 계획·설계·상태 문서는 안 줬다.
  받은 4건을 전부 코드에서 다시 확인해 **2건 채택 · 2건 폐기**했다.
- 결과: **[자동 수정 1건]** `step_gap` 의 「행이 없다」 메시지가 두 원인을 뭉갠다.
  `STEP_ROW` 가 스텝을 **넷째 칸**으로 못박으므로 `index.md` 의 열 모양이 바뀌면 행이
  멀쩡해도 매치가 없어지는데, 그때 나가던 말은 "계획 커밋과 등재 커밋이 갈렸다" 였다 —
  엉뚱한 수리로 보낸다. 「등재가 빠졌거나 표의 열 모양이 바뀌었다(스텝을 넷째 칸으로
  가정한다)」로 넓혔다. **갈래는 안 쪼갰다** — 원인을 가르는 둘째 정규식은 자기 단언을
  또 요구하고 오늘 얻는 것이 메시지 한 줄과 같다.
- 결과: **[설계 「범위 밖」에 등재]** 닫힌 계획의 행은 두 번 다시 안 본다. D 가 대조하는
  것은 `status.plan` 이 가리키는 **한 행**뿐이라(실물 37행 중 1행) 계획을 닫는 **마지막
  한 번의 쓰기**를 아무도 안 붙든다. 4회 재발한 결함 자체는 계획이 살아 있는 동안
  뒤처지는 것이라 다음 커밋에서 잡힌다. `digest` 가 200/200 이라 완료 항목을 또 지우는
  값이 아니고, 설계서에 그 용도의 절이 이미 있어 여는 조건과 함께 거기 적었다.
- 결과: **버린 것 둘.** ① 「계획 행이 `index.md` 밖으로 회전해 나가면 오진한다」 —
  **거짓이다.** 회전하는 것은 `plan_<slug>.md` 문서지 `index.md` 의 행이 아니고
  (`rules/docs.md` 4절은 「한 줄 추가」만 말한다), 계획 1 `plan_crawler-core` 의 행이
  오늘도 표 첫 줄에 있다. ② 「`"null"` 이 매직 리터럴이라 하네스가 표기를 바꾸면 상시
  적색」 — 템플릿이 오늘도 `step: 0/0` 을 쓰는 것을 확인했고 주석이 출처를 이미 적는다.
- 결과: **착수 전 세 물음 전부 답이 났다.** ① 검사 순서 **안 바뀌었다** — 옛 코드도
  `assertIsNotNone(s)` 를 `p` 보다 먼저 했다(`49bd4e2` 실측). ② 합성 `INDEX` 는 실물과
  **안 어긋난다** — 실물 계획 행 **37/37** 이 `STEP_ROW` 에 물리고 스텝 칸이 `N/M` 이
  아닌 행은 0개다(실물은 6열에 비고 안쪽 `|` 가 섞이지만 정규식이 넷째 칸에서 멈춘다 —
  그 가정이 위 자동 수정의 대상이다). ③ 지운 계획 52 는 `plan_history_038.md`·
  `design_history_038.md`·`docs/e2e/focus-ring-combinator/result.md`·`index.md` 52번 행에
  전부 살아 있고 `digest` 안에도 취소선 `[5]` 로 이름이 남는다.
- 만진 파일: `tests/test_docs.py`(실패 메시지 **1줄**) · `docs/` 뿐이다. 제품 `src/`
  **0줄** · `e2e/`·`README.md`(건수 614 이미 맞다)·`docs/specs/`·`data/crawl.db` 무접촉 ·
  재색인·스키마·새 의존성 0 · 새 파일은 회전 아카이브 하나다.
- 다음: **e2e phase 1/1.** 볼 것은 ① `rules/e2e.md` 3절 면제가 실제로 서는가 — 제품 0줄에
  고친 것이 실패 메시지 한 줄이라 프로세스 밖에서 달라지는 동작이 0이다(단위 614건과
  e2e 21종 전수 rc 0 으로 재고 새 e2e 파일 0개 예정) ② 계획 60 마감 준비 — 완료 기준 대조와
  `index.md` 계획 60 행을 `진행 1/1 → 완료 1/1` 로 닫는 것. 병합은 사람 몫이라
  `loop/passage-cost-band` 에 쌓아만 둔다(`origin/main` 무접촉 · PR 0).
  러너 규율 위반 **0회**(누적 38).
