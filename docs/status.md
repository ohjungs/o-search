---
signal: GREEN
phase: 계획
step: 0
attempt: 0
iteration: 162
updated: 2026-08-30
ctx: 62
night_iterations: 42
night_red: 0
night_retries: 0
---

# 현재 상태

**계획 33 `clock-injection` e2e phase 통과 — `DONE`. 열린 계획 0.**
e2e **17종 전부 rc=0**(약 137초) · 단위 **433건 OK**(2.92초, `discover tests` — 전체) ·
보류 패치 0 · RED 0. 아카이브: `plan_history_020.md` · `design_history_020.md` ·
`index.md` 33번 · `docs/e2e/clock-injection/result.md`.
`main` 은 `f888518` 그대로 — `loop/clock-injection` 병합은 사람 판단 항목이다.

## e2e 결과 — 물음은 하나였다

**"가짜 시계가 초록인데 진짜 대기가 사라지지 않았는가."**
단위 433건은 전부 가짜 시계 위에서 돈다. 이 계획이 바꾼 것이 **그 시계를 꽂는 방식**이라
**단위가 초록인 것은 계획이 안전하다는 증거가 못 된다.** 그래서 통과 여부만 적지 않고
`main`(`f888518`)을 `git archive` 로 스크래치패드에 풀어 **같은 세션·같은 기계에서
벽시계 A/B** 를 했다. `git diff f888518 HEAD -- e2e/` 가 **비어 있어** 양쪽이 문자
그대로 같은 시나리오를 돌렸다(공정성의 근거).

| 무엇을 재나 | `main` f888518 | HEAD | 기준 |
|---|---|---|---|
| `crawl_delay` — `Crawl-delay: 2` 최소 간격 | 2.01s | **2.01s** | 요구 2s |
| 〃 — 하한 도메인 | 1.00s | **1.00s** | 하한 1s |
| `crawl_politeness` — 워커 예외 뒤 / 재시도 | 2.00s / 1.01s | **2.01s / 1.00s** | 2s / 1s |
| `retry_interval` — https 재시도 / 대조군 | 5.01s / 1.00s | **5.01s / 1.00s** | 선언 5s / 1s |
| `perf_crawl` — [열림] / [차단] 처리량 | 10.25 / 10.31 /s | **10.33 / 10.35 /s** | 5.0 / 9.0 |
| `deadline` — 예산 2초 / 버려진 응답 | 8p·2.1s / 0건 | **8p·2.1s / 0건** | 대조군 16p |

차이는 전부 **±0.01초·±0.1문서/초** — 같은 스크립트를 두 번 돌린 폭이다.
대기가 사라졌다면 초 단위로 내려앉는다(리뷰가 실측한 `sleep=no-op` 판본은 워커 간격이
`[1.005, 1.005]` → `[0.0, 0.0]` 이었다). **안 내려앉았고, 늘어나지도 않았다.**

**계획서가 안 적은 둘을 벽시계 조에 더 넣었다** — `crawl_politeness_e2e` ·
`retry_interval_e2e` 는 `crawl()` 을 `sleep=` **없이** 부르는 e2e 라, 리뷰가 찾은
"기본값으로 새는" 갈래(변이 M4)를 실시계로 밟는 **유일한 두 자리**다.
계획의 표적 3종만 돌았으면 그 갈래는 A/B 에 한 번도 안 들어왔다.

품질 기준 6축도 기준선 그대로: recall@10 **100% / 95%** · 오탐 평균 **14.0**(최소 11) ·
p95 **9.33ms**(예산의 3.1%) · JS **0 B** · 최저 명암비 **4.87:1** · LCP 대리 2.4ms/0.8ms.
`quality_eval` 의 유일한 미검출도 그대로 `[en] loaf`(digest `[6]`, 코퍼스 동결) —
새로 깨진 것이 없다는 뜻이다.

관문 선통과: 전체 검증(단위 433 OK · 린트·타입체크는 이 프로젝트에 없다) ·
CI 없음(`.github` 부재) · 품질 기준 위 표.

## 계획 33 이 남긴 것

`crawl()`·`_fetch_one()` 이 `now` 와 **같은 모양으로** `sleep=` 을 받는다.
`grep -c 'crawl\.time\.sleep' tests/test_crawl.py` = **0**(10곳이었다).
`src` diff 는 시그니처 2줄·호출 2줄·독스트링 1줄이 전부고 **CLI 표면 변화 0**.
`digest.md:156` `[6]` 을 닫았다 — **중단 신호는 이제 주입 지점 한 곳만 바꾸면 된다.**

## 집안일 — `history_current.md` 회전

393줄(상한 300)이라 **139줄로 회전**했다. 계획 30·31 + 반복 152 탐색 + 계획 33 의
계획·설계·개발 1~4 를 `history_010.md` 로 밀고 `digest.md` 에 두 줄로 압축했다.
**상한을 넘겨 놓고 두 반복을 갔다** — 회전은 계획을 닫는 자리에서 같이 한다.

## 다음 — 계획(탐색) phase

열린 계획이 0 이라 `discover.md` 1절로 돌아간다. 반복 152 의 분류가 그대로 유효하다:

**(a) 사람 승인 대기 — 무인 모드가 손대지 않는다**
`[8]` indexer 증분이 갱신을 반영 안 함(**스키마 변경**) · `[high]` `store.has` 상태
불문 스킵 재크롤 정책(**사람 판단**) · URL userinfo 가 `pages.url` PK 로 렌더됨
(**보안 경계**) · `[5]` `X-Robots-Tag` http-equiv(**스키마**) · `loop/*` 병합 여부.

**(b) 도달 불가 — 고치면 오히려 정상값을 거절한다**
`[4]` `int_max_str_digits`(3.9.6) · `[4]` `quality_eval` `limit=100`(실물 최대 28건) ·
`[4]` `fixture_defects` 의 `ja`(코퍼스 동결).

**(c) 정식 계획 크기** — ~~`[6]` 가짜 시계 이음매 10곳~~ = **계획 33 이 닫았다** ·
`[4]` pop 시각 간격 · `[7]` OFFSET 드리프트.
계획 33 이 열어 준 것: **중단 신호(`Event.wait`)가 이제 주입 지점 하나짜리 일이다** —
`[4]` `Ctrl-C` 가 최대 30초 안 먹는 건과 짝이다.

계획 33 이 남긴 갭 둘(8 미만이라 `digest.md` 로 갔다): `[6]` `_fetch_one` 위치 인자
순서를 지키는 단언이 없다 · `[5]` `sleep=no-op` 일 때 메인 루프 91만 회 공회전
(CLI 도달 불가, 중단 신호 계획이 `Event.wait` 로 바꿀 때 같이 사라진다).

## 한도 (안 넘는다)

`data/crawl.db` 실물 · URL 은 `pages.url` PK · CI 90초 · `docs/specs/` 의 4축.
