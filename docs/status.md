---
signal: GREEN
phase: 테스트
step: 3
attempt: 0
iteration: 297
updated: 2026-09-03
ctx: 58
night_iterations: 126
night_red: 2
night_retries: 0
plan: hidden-passage
---

# 현재 상태

**테스트 3 — 갭 탐색이 8점 이상 셋을 찾아 같은 반복에서 닫았다.** 둘은 **제품 결함**이고
(깨진 marked section 이 색인을 통째로 날린다 · 암묵적 닫기가 이름표 스택을 안 걷는다)
하나는 **기록의 결함**이다([R51-4] 의 계수가 재현 불가능한 «모양 이름» 에 붙어 있었다).

**신호는 GREEN** — 단위 **592건 OK** rc 0(맨몸·단독 **13.479초**) · **e2e 20종 전수 rc 0** ·
새 단언 메서드 4 · 제품 `src/` 2파일 · **RED 0**(넷 다 같은 반복에서 초록으로 닫혔다) ·
재시도 0. 브랜치 `loop/hidden-passage`, 기점 `d5367fa`(**`main` 아님** — 아래 「원격」 절).

## [T3-1] 크롤 HTML 여덟 글자가 그 실행의 색인을 지운다 — 9점

`_markupbase` 는 아는 키워드(`cdata`·`include`·`if`…)가 아닌 `<![foo]>` 를 만나면
`error()` 로 보내고 `HTMLParser` 는 그것을 안 덮는다 — **`NotImplementedError` 가
`feed()` 밖으로 나간다**(stock 3.9.6. 3.13 은 `AssertionError`). 그 다음이 값이다:

- `index_pages()` 는 문서마다 `try` 가 **없고** `commit()` 이 루프 **끝**에 있다 →
  페이지 한 장이 터지면 **그 실행이 넣던 색인이 통째로 사라진다.**
- `/passages` 는 같은 파서를 지나므로 **500** 이다.
- 크롤 HTML 은 **남이 쓰는 입력**이다 — 신뢰 경계이고, 이 여덟 글자는 아무나 만든다.

**파서가 둘이라 수리도 둘이다.** 공통 부모 `_Parser` 하나를 두고 `parse_marked_section`
만 덮어 **명세대로 bogus comment**(다음 `>` 까지 버린다)로 떨어뜨렸다 — 브라우저와 같은
쪽이다. `_TextParser` 와 `_MetaRobotsParser` 가 **둘 다** 상속한다. **후자가 더 이른
자리다**: `index_pages()` 가 `is_noindex()` 를 **먼저** 부르므로 거기서 터지면 앞 수리는
닿지도 못한다. 실제로 변이 ⑥(`_MetaRobotsParser` 만 안 고친다)이 **처음엔 초록으로 살아
있었고**, `TestIsNoindex` 에 단언을 세워 죽였다.

기존 `test_broken_html_no_raise` 가 못 밟은 이유: 저기서 깨진 것은 **태그**고 여기서
깨진 것은 **선언**이라 파서의 다른 가지로 간다. 얼마나 흔한지는 이 저장소가 증거다 —
회귀 대조 표본 422개 중 **10개에서 HEAD 가 터졌고**, 그중 둘은 HTML 도 아닌
`tests/test_docs.py` 의 정규식 리터럴(`(?<![A-Za-z_])`)이다.

## [T3-2] 암묵적 닫기가 이름표 스택을 안 걷었다 — 8점

개발 2 가 넣은 `_implied_end` 는 `_els` 만 pop 했다. `<p>첫 문단<div>둘째 문단</div>꼬리`
의 꼬리가 **이미 끝난 `<p>` 의 이름표를 달고** 나온다(HEAD 실측
`[('p','첫 문단'),('div','둘째 문단'),('p','꼬리 텍스트')]`). 기존 테스트가 못 밟은 이유는
그것이 **닫는 태그**로 같은 계약을 재기 때문이다 — 종료 태그가 **아예 없는** 문서가 안
재지고 있었다. 값이 둘이다: 이름표는 갈림길 6 을 재는 축이고(소비자는 아직 0곳 —
digest `[6]`), **비용은 실물이다** — 안 걷으면 `_open` 이 «안 닫힌 `<p>` 수» 만큼 쌓이고
닫기 관용구가 그 길이에 붙어 깊이²다(캡 35,000자를 `<p>가<span>나</span>` 로 채운 실측
`_open` 최대 **1250 → 1** · **0.422 → 0.304 ms/1,000자**).

## 넷 다 «고치기 전에 빨간 것» 을 눈으로 봤다 — 그리고 첫 탐침이 거짓 RED 였다

HEAD `extract.py` 를 제자리에 놓고 각각 돌렸다(끝나고 원복 True): 셋은 **예외**
(`errors`), 이름표는 **값 불일치**(`failures=1`). 작업 트리에서는 넷 다 OK.

**첫 탐침은 가짜였다.** 저장소 루트에서 `python3 -m unittest test_extract.<...>` 로
불렀더니 `tests/` 가 `sys.path` 에 없어 **ImportError 가 `FAILED (errors=1)`** 로 나왔다 —
넷이 나란히 «빨갛다» 고 나와서 그럴듯했다. 판정 줄만 보고 적었으면 **RED 근거가 통째로
가짜**였다. `discover -s tests -k` 로 고쳐 다시 쟀다. **러너를 바꾸면 색이 바뀐다**
(digest `[9]`·`[5]` 와 같은 뿌리이고, 이번엔 그 고장이 **빨간 쪽**으로 났다).

## 변이 여섯 — 넷은 죽었고, 살아남은 둘은 사유가 서로 다르다

| 변이 | 결과 |
|---|---|
| ① 이름표 스택 걷기 삭제 | 죽음(실패 1) |
| ③ `parse_marked_section` 오버라이드 삭제 | 죽음(**실패 6**) |
| ⑤ `parse_bogus_comment` → `i + 2` | 죽음(실패 3) |
| ⑥ `_MetaRobotsParser` 는 안 고친다 | **초록 → 죽음**(실패 1) |
| ② pop 가드를 빼고 늘 pop | **살았다 — 등가 변이** |
| ④ `AssertionError` 가지 삭제 | **살았다 — 3.9.6 에서 도달 불가** |

**②는 등가 변이다** — 열기 15 × 15 × 닫기 8 × 방아쇠 7 = **12,600 조합 전수에서 갈라진
입력 0개**. 동어반복 단언을 세우는 대신 가드를 **남겼다**: 형제인 `handle_endtag` 가 쓰는
관용구와 같은 모양이고, `option` 이 `_NON_BLOCK_TAGS` 로 옮겨지는 것 같은 **그럴듯한
편집에서 곧바로 갈린다**. **④는 3.13 이 예외 종류를 바꾼 것을 받는 이식성 가지**라 이
파이썬에서는 죽일 수 없다 — 신뢰 경계라 «못 죽이니 지운다» 로 안 갔고 주석에 사유를 적었다.

## [R51-4] 재측정 — 계수는 참이고, 기록이 틀린 것을 붙들고 있었다

기록된 모양(낱말마다 `<b>`/`<i>`)을 그대로 재니 **0.436 ms/1,000자**(기록 0.44)라
**오늘도 참**이고, 이번 반복의 두 수리가 그 모양에 붙인 비용은 **0** 이다.

**그런데 계수가 세 번 낡은(0.118 → 0.352 → 0.44) 뿌리는 «위키·CMS 출력» 이라는 이름이
재현 불가능하다는 것이었다** — 재는 사람마다 다른 점을 잰다. 축은 **태그 밀도**다:

| 모양 | 태그/1,000자 | ms/1,000자 | 10건 |
|---|---|---|---|
| 기록된 위키·CMS | 169 | **0.436** | 154ms · 31% |
| 같은 말의 더 촘촘한 벌 | 200 | 0.502 | 176ms |
| `<li>가</li>` | — | 0.535 | 187ms |
| `<p>가</p>` | — | 0.665 | 233ms |
| **안 닫은 `<p>가`(캡 안 최악)** | — | **0.901** | **315ms · 예산의 63%** |

법칙은 `ms/1000자 ≈ 0.0025 × 태그/1000자` 이고, **캡 안 진짜 최악은 기록된 154ms 의
2.1배**다. **숫자를 또 올리지 않았다** — 초록을 유지하는 0.47 을 고르면 역산이고, 진짜
천장 0.901 을 단언에 넣으면 **RED** 인데 그 처방은 캡을 35,000 → 18,000 으로 내리는
**계획 몫**이다(«긴 문서의 뒷부분 근거» 를 반으로 자른다). 대신 **배정치 1/3 이 «여유» 가
아니라 그 모양 배수를 흡수하는 자리**임을 기록 자리 셋(`indexer.py:41~51` ·
`tests/test_indexer.py` 성능 단언 주석 · `docs/project.md`)에 **같은 문장으로** 적었다 —
1/3 을 지키면 최악 모양이 500ms 안에 든다(154 × 2.1 = 323ms). 단언 리터럴 `0.44`·`500/3`
은 **안 건드렸다**(둘 다 오늘 참이다). 남은 갭 «단언이 여전히 한 점을 붙든다» 는
`digest` 에 `[7]` 로 등재했다.

## 완료 기준 대조 (계획 51 5절)

1. **숨은 텍스트 0/7 · 본문 7/7** — 실제 임시 DB 로 `index_pages()` → `passages()`.
   다섯 모양 + `svg><title>` 대조군 + **이번 반복이 넣은 「깨진 선언 안의 숨은 블록」**.
2. 숨은 블록만 질의어를 담은 문서는 문단 0개 — 위와 같은 탐침에서 확인.
3. **회귀 0** — 코퍼스 64문서 + `tests/` HTML 리터럴 358개 = 표본 422개를 HEAD 와 대조:
   **블록 총계 416 = 416 · 텍스트 달라진 표본 0 · 이름표만 달라진 표본 2**(둘 다 [T3-2]
   가 고친 자리다) · `data/crawl.db` 무변 · `e2e/`·`docs/specs/` 무변.
4. 정상 HTML 블록 목록 무변 — 위 3 과 단위 592건이 함께 붙든다.
5. 단위 **592건**(588 + 새 단언 4) 전부 초록 · 변이 표는 위.
6. **e2e 20종 전수 rc 0** — `passage_eval` 블록 **253** · 정확도 **100.0%**(398/398) ·
   채택률 99.5% · p95 **1.52ms** · `quality_eval` ko **20/20** · en **19/20** · 1위 **39** ·
   매치 평균 14.0 · `perf_search` p95 **8.91ms** · `search_api` p95 **2.11ms** ·
   `perf_crawl` **10.22 / 10.24/s** · 문서집합 sha1 `541d455a…c078654b` **무변** ·
   `design_check` 4축 통과.
7·8·9. 변이는 **양방향**으로 심었고(좁아지기 ①③⑤⑥ · 넓어지기 ②) 매번 원복 ·
   `python3 -B` · 끝나고 `git diff -- src/` 로 **되돌림을 확인**했다 — 커밋된 변이 **0**.

## 러너 규율

판정에 쓴 실행은 **맨몸·단독**이다 — `PYTHONPATH=src python3 -m unittest discover -b -s
tests` → `Ran 592 tests in 13.479s / OK`, e2e 20종도 각각 `PYTHONPATH=src python3
e2e/<이름>.py` 그대로. `for`·`env`·`timeout`·`> /dev/null` **0회**.

**어긴 것도 적는다 — 이번 반복에 파이프 2회다.** 회전 직후 아카이브 명부가 아직 안
맞아 났던 실패(`ArchiveIndexTest`)의 **이름을 찾느라** `| tail`·`| sed` 로 두 번 걸렀다.
숫자를 만든 실행이 아니라 진단용이었고 그 뒤 맨몸으로 다시 돌려 판정을 냈지만, 규율은
«판정에만 맨몸» 이 아니라 «러너에 파이프를 안 붙인다» 이므로 **0회가 아니다.** 러너 출력
조작은 0회다. 변이·탐침 스크립트는 전부 **저장소 밖 스크래치패드**에 있고 커밋하지 않는다.

## 문서

`status.md`(이 파일 · 반복 **297**) · `metrics.md`(반복 **297** · 테스트 **36**) ·
`index.md`(51번 줄에 테스트 3) · `history_current.md`(**회전 먼저** — 274줄이라 테스트
1·리뷰 1 두 항목 77줄을 `history_049.md` 로 밀어 197줄로 만든 뒤 테스트 3 항목을 붙여
**272줄**. 스물아홉 회전 연속 지각 0) · `digest.md`(명부에 `history_049.md` 더하고 회전
사유를 기존 완료 줄에 이어 붙였다 · 새 `[7]` 갭 하나 · **200줄 정확히** · 새 색인 구멍 0) ·
`project.md`(성능 계수 절에 밀도 축) · `README.md`(단위 건수 **588 → 592**).

## 다음 — 리뷰 3

겨눌 자리 둘: ① `_Parser` 가 `html.parser` 의 **다른 예외 가지**도 덮어야 하는지
(`parse_html_declaration`·`parse_pi` 쪽에 같은 모양이 있는지), ② 성능 단언이 아직 **한
점**을 붙드는 것(digest `[7]`). 살아남은 변이 ②④ 는 «못 죽인 것» 이 아니라 **사유를 적고
남긴 것**이라 리뷰가 그 판단부터 본다.

## 원격 — `origin/main` 은 계획 47 까지다

- **커밋·푸시 뒤에 `git ls-remote origin` 을 다시 읽었다**(digest 의 「원격을 바꾸는 동작을
  «명령을 보냈다» 로 기록하지 않는다」 항목): `refs/heads/loop/hidden-passage` =
  **`8cea553`**(테스트 3 커밋, `769a5b8` → **fast-forward · force 아님**) ·
  `refs/heads/main` = **`687a159` 무변** · `refs/pull/7/head` = **`d5367fa` 무변** ·
  `refs/pull/7/merge` = `e3a70d0`(GitHub 이 만드는 것이고 내가 안 건드린다).
  이 절을 고치는 커밋이 하나 더 나가므로 그 커밋은 다음 반복이 확인한다.
- **PR #7** — `loop/merge-48-50` → `main`, 계획 48·49·50. **OPEN·미병합.** 병합은
  **사용자가 처리한다** — 이 반복은 PR 을 열지도 닫지도 않았고 그 브랜치를 건드리지도,
  `main` 으로 리베이스하지도 않았다.
- **계획 51 의 기점은 `main` 이 아니다** — `origin/main`(`687a159`)에는 계획 48·49·50 이
  없어 `README.md` 의 건수 단언이 거기서는 RED 다. `d5367fa` 에서 브랜치를 땄다.
- 앞선 병합: PR #6 = 계획 44~47(`687a159`). 충돌 0.

## 사람이 정할 것 — 다섯 (PR #7 이 붙었다)

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
2. **`--focus` 는 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 이 0 일
   때만 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **반응형 360px 미검증** — 브라우저가 없어 이 저장소의 누구도 그 화면을 못 그린다.
4. **회전 규약·러너 규율의 저장소 밖 절반**과 **사양이 남긴 둘**(`docs/specs/concept.md` 의
   `## 사람이 정할 것` — 속도 제한 시점, 사양 숫자 90%·500ms·60회/분이 초안이라는 것).
5. **PR #7 병합** — 위 「원격」 절.
