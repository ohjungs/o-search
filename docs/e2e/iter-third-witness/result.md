# e2e 결과 — 계획 85 `iter-third-witness` (반복 530 · 2026-09-10)

**판정: 통과.** 계획서 스텝 1 이 지정한 증거(`git show 9947049:docs/*.md`)를 **그날 아침
트리 통째로** 꺼내 새 자를 먹였다. 같은 트리에서 **자만 갈아** 끼웠더니 옛 자는 70건
전부 초록, 새 자는 **실패 정확히 1건**이고 그것이 셋째 증인이다.

```
반복 번호가 어긋났다 — history_current.md 최대 494 ≠ status.md `iteration` 490
```

## 0. 새 e2e 파일을 안 만든다 (`rules/e2e.md` 3절) — 계획 90 과 같은 자리

`git diff --stat main HEAD -- src/ e2e/` 가 빈손이다. 이 계획이 산 것은
`tests/test_docs.py` 의 픽스처 한 줄과 주석이고, `src/` 에 `tests` 를 import 하는 줄은
0건이라 HTTP 응답·CLI stdout·exit code·DB 스키마 중 무엇도 어제와 다르지 않다.
새 파일을 만들면 `unittest discover` 와 겹쳐 「1회만」이 깨진다(1절).
**`e2e/*.py` 는 어제와 같은 22개**다.

## 1. 사전 관문 (`rules/e2e.md` 1절)

| 항목 | 명령 | 결과 |
|---|---|---|
| 전수 | `PYTHONPATH=src python3 -m unittest discover -b tests` | **749 OK · rc 0** |
| 린트/타입체크 | `project.md` 에 **없음** | 검증되지 않음(축이 없다) |
| CI | 없음 | 해당 없음 |
| 품질 기준(경량·성능·디자인) | 이 계획이 건드린 파일은 `tests/test_docs.py` 뿐 | **해당 없음** — 제품 축 무접촉 |

## 2. 시나리오 — 트리는 그대로 두고 **자만 간다**

`.git` 없는 `/tmp` 사본에서만 돌렸고(저장소 워킹트리 무접촉), 매 판
`PYTHONPYCACHEPREFIX` 를 새로 줬다. **부분 복사를 안 했다** — 오늘 낮 검증에서
`src tests docs` 만 복사해 `SpecCitationTest` 3건이 거짓으로 빨갰던 장비 결함이 있었고,
계획 90 의 e2e 가 같은 자리에서 「대조군이 초록이 아니면 나머지를 못 읽는다」를 등재했다.
그래서 **`git archive` 로 추적 트리 통째**를 꺼냈다.

| # | 트리 | 자(`tests/test_docs.py`) | 결과 | 무엇을 보였나 |
|---|---|---|---|---|
| E0 | 2026-09-09 아침(`9947049`) | **그날 것** | `Ran 70` · **OK** | 490·490·494 를 앞에 두고 **아무도 안 물었다** — 계획 1절이 적은 실물이 재현된다 |
| E1 | 2026-09-09 아침(`9947049`) | **오늘 것** | `Ran 83` · **failures=1** | `IterationSyncTest` 가 「최대 494 ≠ `iteration` 490」. **이 자 하나만 물었다** |
| E2 | 오늘(`3c92c7a`) · 회전 흉내(최신 3항목만 남김) | 오늘 것 | `Ran 83` · **OK** | 계획 2절의 「회전은 이 자를 안 깬다」가 참이다 — 최댓값 529 가 살아남는다 |
| E3 | 오늘 · 항목을 **전부** 지움 | 오늘 것 | `Ran 83` · **failures=2** | `HistoryCapTest` 하한 + **한 줄 신고**(「`### 반복 <수>` 머리를 못 찾았다`」). `max()` 트레이스백이 아니다 |

**E0↔E1 이 이 계획의 전부다.** 트리·시드·명령이 전부 같고 갈린 것은 자 하나뿐이라,
「실물이 빨갛다」와 「자에 이빨이 있다」를 섞지 않고 뒤쪽만 보인다.

**E1 의 실패가 1건인 것도 값이다.** 오늘 자는 그날보다 13건 많은데(70 → 83, 계획 90 의
`live_gap` 포함) 그날 아침 문서에 대해 **나머지는 전부 조용했다** — 새 자가 그날의 드리프트를
싸잡아 물어서 1건이 섞여 보이는 것이 아니라, 겨냥한 자리 하나만 물었다.

## 3. 계획서 완료 기준 대조

| 스텝 | 완료 기준 | 실측 |
|---|---|---|
| 1 | 갈래 테스트 · 전수 초록 · **`git show 9947049:docs/*.md` 로 490 ≠ 494 신고** | E1 이 그대로 냈다 |
| 2 | `### 반복` 머리가 494 → 495~498 → 499 로 이어진다 | 오늘 `history_current.md` 에서 그렇다 |
| 3 | 변이 3종이 각각 잡힌다 | 반복 528 이 **4판** 실측(③이 두 갈래로 읽혀 둘 다 쟀다). ③b 가 1건뿐이라 픽스처를 고쳐 3건으로 만들었고, 반복 529 가 반사실까지 쟀다 |

## 4. 남는 것

**옛 겹침(482 · 485~486 · 487~490 의 계획 83 쪽)은 그대로다** — 계획 5절이 안 건드리기로
한 것이고, 이 자가 무는 것은 최댓값 하나라 겹침이 남아도 판정이 선다. E0~E3 이 그것을
확인한다(겹침이 있는 아침 트리에서도 새 자는 **의도한 한 건만** 물었다).
