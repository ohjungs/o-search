# 반복 기록 아카이브 — 계획 48 `passage-api` 의 개발 세 반복 (252~254)

<!-- history_current.md 에서 밀려난 원본. 수정·삭제 금지. -->
## 2026-09-02 12:20 | passage-api | 개발 1 | 시도0
- 한 일: **설계 48 의 스텝 1 을 그대로 냈다 — 문단을 돌려주는 함수 하나.**
  `src/websearch/extract.py` 에 `_BlockParser`(`_TextParser` 하위 클래스) + `extract_blocks()`,
  `src/websearch/indexer.py` 에 `passages(db_path, query, limit=10)`.
  TDD 로 갔다: `tests/test_extract.py` 에 `TestExtractBlocks` 9건,
  `tests/test_indexer.py` 에 `TestPassages` 15건을 **먼저 쓰고 RED 를 눈으로 봤다**
  (`ImportError: cannot import name 'extract_blocks'` + `AttributeError: no attribute 'passages'`).
- **`_TextParser` 를 한 줄도 안 고쳤다** — 부모가 블록 경계마다 이미 부르는 `_separate()`
  를 하위 클래스가 가로채 `_normalize` 한 뒤 버퍼를 비운다. 마지막 블록은 `close()` 가
  낸다. 색인 경로(`extract_text`)는 **호출조차 안 지나간다** — 회귀 위험이 구조적으로 0이다.
- **`passages()` 는 `search()` 를 부른다.** 자체 질의를 짜면 503(없는 DB·옛 색인)·500(손상)
  판정을 한 벌 더 갖게 되고 계획 47 이 한곳에 모은 것이 다시 흩어진다. 대신 `pages.html`
  을 읽으려 연결을 하나 더 연다. 고르는 규칙은 설계대로 **질의어 + 그 2-gram(`_bigrams`)을
  가장 많이 담은 블록**이고, 동점은 `>` 부등호가 먼저 나온 블록을 남긴다.
- 결과: **단위 506 → 530건 OK(12.509초)**. 새 24건 전부 초록. README 카운트 가드가
  예고대로 울어 `README.md` 67행을 `506 → 530` 으로 갱신했다.
- **변이 M1~M5 다섯 전부 RED 를 눈으로 봤다**(`.mutation-lock` 걸고 매번 되돌린 뒤 초록
  확인 · `git status` 로 `src/` 무변경 확인 · **커밋된 변이 0**):
  M1(버퍼 비우기 삭제) 8건 · M2(마지막 flush 삭제) **딱 1건**(깨진 HTML `<p>가<p>나<div>다`)
  · M3(블록 마커를 `_normalize` 에) 10건(**대조군 `TestExtractText` 7건 포함**)
  · M4(매치 없는 문서에 첫 블록) 2건 · M5(빈 블록도 센다) 4건.
  **M2 가 설계의 예고를 실물로 확인해 줬다** — 정상 HTML 만 썼으면 이 변이는 살아남는다.
  M6~M8 은 `serve` 것이라 스텝 2 로 미뤘다(한 스텝을 안 넘긴다).
- **설계 탐침 숫자가 실제 코드에서 전부 재현됐다**(스크래치패드 탐침 1회, 저장소 밖):
  불변식 `" ".join(blocks) == extract_text(h)[1]` **64/64** · 문서당 블록 min 3·avg 4.0·max 4
  · 파싱 20.4KB 1.62→**1.83ms(+13%)**, 설계는 +12% 였다 · `passages()` p50 **0.59ms** ·
  p95 **0.68ms**(예산 500ms 의 0.14%) · 채택률 **398/400 = 99.5%** · 정확도 100%(기본값) ·
  문단이 본문 통째인 경우 **0건**. 문장 `<p>` 포장과 통짜 포장의 색인 본문 **64/64 동일** —
  스텝 3 `passage_eval` 의 전제가 함께 섰다.
- **불변식이 깨지는 유일한 자리를 테스트로 고정했다** — 블록 전체가 제어문자면 통짜
  정규화가 겹공백(`'가  나'`)을 남기고 블록 쪽은 그 블록을 버린다.
  `test_control_only_block_is_dropped_and_that_breaks_the_join` 이 **양쪽 현 동작을 둘 다**
  단언한다. 천장은 `extract_blocks` docstring 의 `ponytail:` 주석에 있다.
- `data/crawl.db` **무변경**(`85c96744…`) · 스키마·`docs` 무변경 · 의존성 0(stdlib) ·
  새 파일 0개 · `docs/specs/` 무변경 · PR 안 열었다.
- 러너 호출 **8회**(전수 3 + 변이용 단일 모듈 5) · **출력 조작 0 · 명령 잇기 0** —
  세 반복 연속 깨끗하다.
- 다음: **개발 스텝 2** — `serve.do_GET` 에 `/passages` 갈래 하나(`_send`·`_parse` 재사용 ·
  `PASSAGE_LIMIT = 10` · `page != 1` 이면 400 · `MAX_PASSAGE = 2000`), 변이 M6~M8.

## 2026-09-02 12:36 | passage-api | 개발 2 | 시도0
- 한 일: **설계 48 의 스텝 2 — `GET /passages` 배선.** `src/websearch/serve.py` 에
  상수 둘(`PASSAGE_LIMIT = 10` · `MAX_PASSAGE = 2000`)과 `do_GET` 갈래 하나.
  `tests/test_serve.py` 에 6개 클래스 14건을 **먼저 쓰고 RED 18건을 눈으로 봤다**
  (전부 `404 없는 경로: /passages`). 제품 순증 **24줄**(주석 포함, 설계 추정 16).
- **404 줄을 `not in ("/search", "/passages")` 로 넓히고 try 사다리는 한 벌로 뒀다.**
  갈래마다 `try` 를 두면 400·500·503 판정이 **세 벌**이 되고 계획 47 이 한곳에 모은
  것이 다시 흩어진다 — `passages()` 가 `search()` 를 부른 것과 같은 이유다. 그래서
  `/search` 의 200 조립은 `else` 안에 **그대로 있고**(경로별 분기만 앞에 섰다) 화면 경로는
  손도 안 댔다. 계약 셋을 못박았다: 키는 `{"version","query","passages"}` 뿐(`has_next`·
  `page` **없다**) · 문단 수는 서버 상수라 `&limit=50` 이 안 먹는다 · `page != 1` 은 400.
- 결과: **단위 530 → 544건 OK(12.749초)** · README 카운트 가드가 예고대로 울어
  `README.md` 67행을 `530 → 544` 로 갱신 · `e2e/search_api_e2e.py` rc 0
  (p95 **2.09ms** — `/search` 계약 무변경을 프로세스 밖에서 확인).
- **변이 M6~M8 셋 전부 RED 를 눈으로 봤다**(`.mutation-lock` 걸고, 원복은 **초록이 아니라
  `git diff` 로** 확인 — 기준 패치를 떠 놓고 매번 diff-of-diffs 로 잔여물 0 확인 ·
  커밋된 변이 0): **M6**(`page != 1` 거절 삭제) 2건 — 실패 출력이 *"page=2 에 1페이지와
  똑같은 문단 목록"* 을 그대로 찍어 «조용히 무시하면 소비자가 같은 문단을 영원히 받는다»
  는 설계의 문장이 실물이 됐다 · **M8**(자르기 삭제) 1건(`5999 != 2000`) ·
  **M7**(`_send` 안 쓰고 직접 JSON) 2건.
- **M7 이 단언 하나를 더 낳았다 — 변이가 `version` 만이 아니라 `nosniff` 도 같이 잃었다.**
  기존 `test_responses_carry_nosniff_and_html_carries_a_csp` 가 경로 **셋만** 돌아서
  새 진입점의 보안 헤더를 아무도 안 재고 있었다. `/passages` 를 목록에 더하고 **변이가
  살아 있는 동안 RED 를 본 뒤** 원복했다(사다리 적용 제외 — 신뢰 경계).
- `data/crawl.db`·스키마·`docs.body`·`extract.py`·`indexer.py` **무변경** · 의존성 0(stdlib)
  · 새 파일 0개 · `docs/specs/` 무변경 · PR 안 열었다.
- 러너 호출 **6회**(전수 5 + e2e 1) · **출력 조작 0 · 명령 잇기 0** — 네 반복 연속 깨끗하다.
- 다음: **개발 스텝 3** — 새 파일 `e2e/passage_eval.py`(정확도 ≥90% + 채택률, 종료 코드
  0/1/2)와 README·`project.md` 갱신. e2e 19 → 20종이라 README 카운트가 또 움직인다.
  **집안일 예고**: 이 항목으로 `history_current.md` 가 286줄(상한 300) — 다음 반복은 회전이다.

## 2026-09-02 12:55 | passage-api | 개발 3 | 시도0

- 한 일: 설계 갈림길 4 의 **측정기**를 세웠다 — 새 파일 `e2e/passage_eval.py`(제품 `src/`
  **0줄**). 사양 기능 8(문단의 90% 가 질의어 또는 그 2-gram 을 포함)과 성능 5(p95 500ms)를
  **실행 가능한 판정**(종료 0/1/2)으로 만든다. `README.md` 는 e2e 19 → **20종**과
  품질 기준 두 줄, `docs/project.md` 는 측정 명령을 받았다.
- **`quality_eval.py` 와 코퍼스·질의 JSON 은 한 글자도 안 고쳤다** — 대신 `_load`·
  `fixture_defects`·`_QUALITY` 를 **import 해 쓴다**(저장소 첫 e2e 간 import 다). 새로
  하는 것은 **HTML 포장 하나**다: 본문 전체를 `<p>` 하나로 감싸던 것을 **문장 단위 `<p>`**
  로 바꿔 문서당 블록이 1 → 평균 4.0개(총 253개)가 됐다. p95 정의는 `perf_search.pct` 를
  불러 한 벌만 둔다 — 두 숫자가 `project.md` 에 나란히 실리는데 정의가 갈리면 못 읽는다.
- **결과 — 설계 탐침이 실제 코드·HTTP 에서 재현됐다**: 채택률 **99.5%**(문단 398 / 매치
  문서 400, 탐침 199/200 과 같은 값) · 정확도 **100.0%** · p95 **1.38ms**(예산 500ms 의 0.3%).
  `PAGE_SIZE == PASSAGE_LIMIT` 단언이 채택률 분모의 전제를 매 실행 붙든다.
- **정확도 100% 는 숨기지 않고 도구 첫 화면에 적었다** — 고르는 규칙(`indexer.passages`)과
  재는 규칙이 **같은 술어**라 나오는 기본값이다(`_bigrams` 를 양쪽이 같이 쓴다). 그래서
  숫자를 둘 찍고, **이 도구의 이빨은 정확도가 아니라 종료 2 가드 넷**이다.
- **변이로 RED 를 눈으로 봤다**(`.mutation-lock` 걸고, 원복은 `git diff -- src/` **0줄**로
  확인 · 커밋된 변이 0): **P1**(점수를 무시하고 첫 블록을 고른다) → 정확도 **10.2%** ·
  **rc 1** · 근거 아닌 문단 10줄을 stderr 로 · 단위도 6건 죽었다. 가드는 rc **2** 를 냈다:
  **G**(포장을 통짜로 되돌림) → G4(블록 <2) + G6(문단 == 본문 통째) · **W**(마침표를 버려
  색인 본문이 바뀜) → G7 · fixture 경로 오타 → `quality_eval.fixture_defects` 경로.
  예산을 1ms 로 낮춘 변이는 **rc 1**(p95 갈래).
- **설계 표의 M4 는 이 도구가 못 죽인다 — 실측으로 확인했고 적어 둔다.** 매치 없는 문서에
  첫 블록을 대신 내면 정확도가 100 → **99.5%** 로만 내려가 합격선 90% 를 못 건드린다
  (채택률은 99.5 → 100% 로 움직인다 — 설계가 이 숫자를 둔 이유가 여기서 실물이 됐다).
  M4 를 죽이는 것은 **단위 `TestPassages` 둘**이고(실측 RED), 설계 표도 그렇게 적어 뒀다.
- **W 변이가 계약 구멍 하나를 잡아 고쳤다** — 불변식을 `assert` 로 두니 파이썬 기본
  종료 코드 **1** 로 죽었다. 1 은 «미달» 에 예약된 값인데 이건 «잴 수 없다» 라 **rc 2**
  여야 한다(`quality_eval` 이 fixture 오류에 세운 규칙과 같다). `ValueError` → rc 2 로 바꿨다.
- **G6 이 처음엔 38KB 를 토했다** — 문단마다 한 줄이라 398줄이다. 사유가 하나라 앞 셋만
  찍고 건수를 함께 낸다(G4 와 같은 꼴). 판정 줄이 스크롤 밖으로 밀리면 그 자체가 위험이다.
- **같은 구멍을 하나 더 찾아 닫았다** — `--repeat 0` 은 표본 0으로 `UnboundLocalError`
  트레이스백을 내며 **rc 1** 로 죽었다(실측). W 변이가 드러낸 것과 같은 «미달 코드로 새는
  측정 불능» 이라 세 줄 가드로 **rc 2** 로 돌렸다. 저장소 CLI 들의 *"--max 는 1 이상의
  숫자 하나를 받는다"* 와 같은 꼴이다. **이 갈래를 붙드는 단언은 아직 0개다.**
- 단위 **544건 OK**(추가 0 — 산출물이 e2e 스크립트다) · `data/crawl.db`·스키마·
  `docs/specs/`·`quality_eval.py`·코퍼스 **무변경** · 의존성 0(stdlib) · PR 안 열었다.
- 러너 호출은 전부 맨몸이었는데 **한 번 어겼다** — 변이 확인 중 `2>/dev/null` 을 붙여
  판정 줄이 통째로 잘렸고, `project.md` 가 경고한 그 실패 모드를 그대로 재현했다.
  같은 명령을 맨몸으로 다시 돌려 6건 실패를 눈으로 확인했다. **다섯 반복 연속이 깨졌다.**
- **집안일**: `history_current.md` 286줄 → 이 항목을 붙이기 전에 회전(`history_032.md`,
  계획·설계 두 반복). 명부에 이름을 더했고 `digest.md` 는 기존 회전 줄에 이어 붙였다.
- 다음: **개발 phase 완료 → 테스트 phase.** 설계 변이 표 M1~M8 전수가 그 자리다.
  M4 는 단위가 잡는다는 것이 위에서 확인됐으므로 **표의 «passage_eval 정확도 하락» 칸은
  근거가 아니라 관측 기록으로 읽어야 한다** — 테스트 phase 가 그 칸을 다시 판정한다.
