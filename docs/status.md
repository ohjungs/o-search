---
signal: YELLOW
phase: 계획
step: 0/0
attempt: 0
iteration: 531
updated: 2026-09-10
ctx: 65
night_iterations: 5
night_red: 0
night_retries: 0
plan: null
---

## 정지 — 탐색 막힘 · 8개 출처를 전부 재고 근거 0건

계획 85 를 닫고 다음 계획을 찾았는데 **`discover.md` 1절의 여덟 출처가 전부 비었다.**
「할 일이 없어 보였다」가 아니라 **하나씩 돌려서 0건을 확인했다.**

| 순위 | 출처 | 실측 |
|---|---|---|
| 1 | 실패하는 테스트 | 단위 **749 OK** · e2e **22종 중 21종 `rc 0`** (아래 하나 예외) |
| 2 | 타입·린트 | `project.md` **없음**(stdlib 소규모) — 축이 없다 |
| 3 | 코드 `TODO`/`FIXME`/`HACK` | `src/`·`e2e/` **0건** |
| 4 | `docs/candidates.md` 큐 | 파일 없음 — **스캐너가 구조적으로 0건이다**(아래 등재) |
| 5 | `digest` 보류 | 하나 남았고 `userinfo-leak` 다 — **보안이라 밤이 못 댄다**(패치도 이미 있다) |
| 6 | `digest` 다음 계획 후보 | **84항목 전수 판독** — 35개가 「여는 조건」 미충족, 나머지는 교훈이거나 도달 불가·저가치 |
| 7 | `digest` 반복 실패 | `iteration` 39행은 **오늘 계획 85 가 닫았다** |
| 8 | 이슈 | `gh issue list` 빈손 |

**`perf_crawl.py` 만 `rc 1` 이다** — `[차단] 초당 8.92문서 · 기준선 9.0 미달`. 이것은
후보 `[8]` 이 **어제 이미 등재한 그 건**이고(「기준선 9.0 은 이 기계에서 여유가 0 · 코드가
아니라 환경이 내려앉았다 · A/B 로 갈랐다」) **아침 할 일**에 올라 있다. 밤이 기준선을
내리는 것은 「통과시키려고 시나리오를 낮추는 것」(`e2e.md` 7절)이라 손대지 않았다.
**오늘 값 8.92 는 어제 판정에 데이터 한 점을 더한 것**이라 여기 적어 둔다.

## 이번 반복이 산 것 — 4순위가 왜 늘 비어 있었는지

탐색이 빈손이라는 결론 옆에서 **자 하나가 조용히 죽어 있는 것**을 봤다. digest 후보 절에
등재했다(`[6]`). 원인이 두 겹이고 **둘 다 실측했다**:

1. `candidates-scan.js` 는 `## 재발` 절을 찾는데 **하네스 자신의 템플릿은 `## 반복 실패`** 다 —
   어느 프로젝트에서도 안 맞는다.
2. 앵커를 고쳐 다시 재도 **여전히 0건**이다. 블록마다 `severity` 낱말과 `기제화: 미완`
   표식을 요구하는데 이 저장소 항목은 산문 + `**N회**` 라 그 토큰이 없다.

**`check.test.js` 30번이 초록인 것도 픽스처 탓이다** — 픽스처가 `## 재발` 을 써서,
템플릿이 절대 내지 않는 표기 위에 자가 서 있다. **계획 85 가 오늘 배운 바로 그 자리다.**

## 사람 결정 대기 — 밤이 못 넘는 선

1. **digest 항목이 `severity`·`기제화` 토큰을 달 것인가** — 84개 항목의 표기 계약이고,
   고칠 도구는 `~/.claude` 의 **공유** 스크립트라 다른 프로젝트에도 걸린다.
2. `perf_crawl` 기준선 9.0 을 어떻게 할 것인가(오늘 8.92 · 어제 A/B 로 환경 쪽이라 갈랐다).
3. `userinfo-leak` 패치 적용 판단(보안 · `docs/patches/` 에 그대로 있다).
4. `deadline_e2e` 1초 하한 · `status.md` 본문 드리프트를 잴 것인가(후보 `[5]`).

## 이 밤의 결산

**계획 1개 완료**(85 `iter-third-witness` · 반복 527~530) · 반복 5 · RED 0 · 재시도 0 ·
`src/` **0줄** · 남긴 패치 0. `digest.md` 208줄(상한 200 — 무인은 회전하지 않는다).

**statusLine 은 이 밤 내내 꺼져 있었다**(마지막 갱신이 30분 이상 묵었다) — 컨텍스트
게이지를 못 믿어 **반복 상한에만 의존**했다. 마지막으로 읽힌 값 ctx 65 · 5h 9 · 7d 61.
