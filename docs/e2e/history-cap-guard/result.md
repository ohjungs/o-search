# e2e 결과 — 계획 76 `history-cap-guard` (반복 446 · 2026-09-07)

**판정: 통과 — 시나리오 4/4 재현. 그중 4 가 기록의 오류를 하나 잡아 고쳤다
(「상한 둘을 다 넘겼다」는 항목 축에 대해 거짓 — 20 은 정확히 상한).
전수 672 OK · 품질 4축 회귀 0 · 완료 기준 2/2 → DONE.**

## 0. 면제 — 새 e2e 파일을 안 만든다 (`rules/e2e.md` 3절)

계획 61·71·75 와 같은 자리다. 오늘도 「해당 없음」으로 넘기지 않고 **없다는 것을 쟀다.**

1. **프로세스 밖에서 달라진 것이 0.** `git diff --stat main...HEAD -- src/ e2e/ docs/specs/ data/`
   가 빈손이다. 움직인 것은 `tests/test_docs.py` 와 문서뿐이라 HTTP 응답·CLI stdout·
   exit code·DB 스키마 중 무엇도 어제와 다르지 않다.
2. **산출물이 제품 경로에서 도달 불가능하다.** `cap_gap` 은 `tests/test_docs.py` 의
   모듈 수준 함수이고 `src/` 에 `tests` 를 import 하는 줄이 0건이다.
3. **전체 명령이 이미 집어간다.** 새 파일을 만들면 `discover -b -s tests` 와 겹쳐
   「1회만」이 깨진다.

**이 계획의 사용자는 사람이 아니라 매 반복 `history_current.md` 를 읽는 루프 자신**이고,
재는 방법은 **실물 문서를 실제로 상한 밖으로 밀어 보고 전수가 무는지 보는 것**이다.
네 시나리오 모두 실물 파일을 진짜로 바꿔서 돌렸고(백업 → 변이 → 전수 → 복원),
복원 뒤 매번 초록을 확인했다.

## 1. 앞 관문 — 전체 검증과 품질 기준

| 축 | 명령 | 결과 |
|---|---|---|
| 전수 | `PYTHONPATH=src python3 -m unittest discover -b -s tests` | **672 OK** (18.2s) |
| 린트·타입체크 | 없음 | — |
| 경량·디자인 | `e2e/design_check.py` | **rc 0** · 4축 전부 통과 |
| 근거 문단 | `e2e/passage_eval.py` | **rc 0** · 정확도 100.0% · 채택률 99.5% · p50 1.51ms · **p95 1.60ms** |
| 검색 품질 | `e2e/quality_eval.py` | **rc 0** · ko 20/20 · en 19/20 · 매치 평균 14.0 · 합격선 80% 통과 |

`src/` 0줄이라 품질 4축은 **회귀 없음 확인용**이고, 세 값 모두 계획 75 의 기준선과
같은 자리다(p95 1.63 → 1.60ms · 같은 동결 코퍼스).

## 2. 시나리오

### 1. 실물 뒤에 더미를 붙여 301줄 — 줄 수 축 단독 · **통과**

만든 상태 **301줄**. 전수 `FAILED (failures=1)` ·
`history_current.md 가 상한을 넘었다 — 줄 수 301 > 300. 오래된 항목부터
history_<NNN>.md 로 밀어내고 digest.md 에 한 줄로 압축한다 (rules/docs.md 3절)`.
**되돌린 뒤 672 OK.** 경계 바로 위 한 줄에서 문다.

### 2. 줄 수는 상한 안, 항목만 21개 — 항목 축 단독 · **통과**

만든 상태 **23줄 · 21회**. 메시지는 `항목 수 21 > 20` 하나뿐이고 줄 수 축은 조용하다 —
**짧은 반복이 스물한 번 쌓이는 날**을 두 번째 축이 실제로 잡는다는 증거다.
되돌린 뒤 672 OK.

### 3. 머리 문구를 `## 반복` 로 드리프트 — 하한 못 · **통과**

만든 상태 **232줄 · `### 반복` 0회 · `## 반복` 12회**. 상한 판정은 조용해지지만
(넘긴 것이 없으니 초록) **하한 못이 물었다**: `0 not greater than or equal to 1 :
history_current.md 의 `### 반복` 항목이 0개다 — 파일이 비었거나 항목 머리 문구가
바뀌었고, 그러면 항목 축은 아무것도 안 잰다`. **조용한 초록이 아니다.**

### 4. 반복 440 회전 직전 상태 재현 — **통과 + 기록 오류 1건 발견**

`git show 99873ed:docs/history_current.md` 를 그대로 실물에 놓았다 —
만든 상태 **383줄 · 20회**. 전수가 물었고 메시지는 **`줄 수 383 > 300` 하나**다.

**여기서 이 계획이 스스로 적어 온 문장이 틀렸다는 것이 드러났다.** 계획서 근거·
`status.md`·`index.md`·`digest.md`·`HistoryCapTest` 의 docstring 이 모두 그날을
「**상한 둘을** 다 넘긴 채」라고 적었는데, **항목 20 은 정확히 상한이라 조용한 것이
맞다**(`test_the_boundary_is_not_over` 가 못박은 바로 그 규칙 — 룰 문구가 「넘으면」).
실제로 넘긴 것은 줄 수 축 하나(**83줄 초과**)다. 다섯 자리를 전부 고쳤다.

**계획의 근거는 흔들리지 않는다** — 넘긴 축이 하나든 둘이든 전수 664건이 여섯 반복
동안 조용했다는 사실은 그대로고, 이 못은 그 상태에서 실제로 빨개진다(위 실측).
움직인 것은 **얼마나 넘겼나의 서술**이다. 계획 75 의 시나리오 3 이 「낡은 쪽이 문서가
아니라 코드 주석」을 찾은 것과 같은 부류이고, 이번에는 **낡은 쪽이 이 계획 자신의
근거 문장**이었다.

## 3. 완료 기준 대조

| 스텝 | 기준 | 결과 |
|---|---|---|
| 1 | `cap_gap(text)` + `CapGapTest` 갈래 ①~⑥ | 6건 · 변이 6종 죽음(경계·축 무력화·머리 느슨·라벨 3종) |
| 2 | `HistoryCapTest` 2건 + 회전 직전 재현을 눈으로 | 실물 초록 · 하한 못 · 재현 출력 위 시나리오 4 |

## 4. 남긴 것

- 새 파일 **0개**(위 면제). `docs/e2e/history-cap-guard/result.md` 만 추가한다.
- 실물 `docs/history_current.md` 는 네 시나리오 전부 **복원 확인**했다
  (`git status --porcelain` 로 매번 확인 · 마지막도 초록 672).
