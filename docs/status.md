---
signal: GREEN
phase: 리뷰
step: 1
attempt: 0
iteration: 255
updated: 2026-09-02
ctx: 60
night_iterations: 101
night_red: 0
night_retries: 0
plan: passage-api
---

# 현재 상태

**테스트 phase 가 끝났다 — 다음은 리뷰.** 활성 계획 `passage-api` · 설계서
`docs/design_passage-api.md` · 브랜치 `loop/passage-api` · 기점 `6e99031` ·
개발 3/3 완료. **단위 553건 OK(13.304초)** · `e2e/*.py` **20종** · 제품 `src/` **0줄**.

## 이번 반복이 한 것 (테스트 1/1)

**설계 변이 표 M1~M8 을 전수 재확인했다 — 여덟 전부 RED.** `.mutation-lock` 아래
제자리에 다시 심고, 매번 되돌린 뒤 **`git diff -- src/` 0줄로** 확인했다(커밋된 변이 0).
- **M2 는 깨진 HTML 케이스 단 1건만 죽였다** — 설계가 예고한 그대로다.
- **M3·M7 은 대조군이 제 일을 했다** — M3 은 기존 `TestExtractText` 8건(색인 경로를
  지킨다), M7 은 `TestPassagesSchemaVersion`(계약 물려받기).
- **M4 의 «passage_eval 정확도 하락» 칸을 실측으로 갈랐다** — 심고 러너를 돌리니
  정확도 100.0% → **99.5%**, 채택률 99.5% → **100.0%**, **rc 는 그대로 0**. 러너는
  M4 를 **안 죽인다**. 죽이는 것은 `TestPassages` 둘이고, 표를 그렇게 고쳤다.
  **사양 숫자는 회귀를 못 잡는다 — 잡는 것은 단위와 종료 2 다.**

**갭 탐색이 결함 하나를 찾아 닫았다 (중요도 9).** `passage_eval.main()` 의
`assert serve.PAGE_SIZE == serve.PASSAGE_LIMIT` 는 `assert` 라서 **파이썬 기본 종료
코드 1** 로 죽는데, 1 은 «미달»(정확도<90% · p95>500ms)에 예약된 값이다 — 설정이 갈린
것이 **«사양 미달» 로 보인다.** W 변이가 G7 에서 드러내고 `--repeat` 가드가 되풀이한
*«측정 불능이 미달 코드로 샌다»* 의 **형제 자리가 안 고쳐진 채 남아 있었다.**
실측(`PASSAGE_LIMIT=7`): rc **1** + 트레이스백 → 세 줄 가드로 **rc 2**.

**스텝 3 산출물의 갈래를 붙드는 테스트를 세웠다** — `tests/test_passage_eval.py` **9건**
(`test_quality_eval.py` 와 같은 방식: 동결 fixture 를 최소한만 비튼다). `--repeat 0`·
음수 · 없는 `--corpus` · 깨진 `--queries` · PAGE_SIZE 드리프트 = 전부 **rc 2** · 가드
G4/G6 · 대조군 rc 0. **넷을 변이로 확인했다** — 가드를 되돌리거나 비우면 전부 RED이고,
`--repeat 0` 은 개발이 만난 `UnboundLocalError` rc 1 이 그대로 재현됐다.

**G7 은 닿을 수 없는 가드였다.** `" ".join(wrap(b)) == b` 는 `split(". ")`/`join` 의
**대수적 항등식**이라 어떤 본문에도 참이다(합성 12종 · 코퍼스 64/64 · 무작위 2만종
위반 **0건**). 러너와 설계가 적은 *"`wrap` 의 단언이 매 실행 확인한다"* 는 아무것도
확인하지 않는다 — 근거는 «색인 본문 64/64 바이트 동일» 실측 쪽이다. 값이 0이라 안
지우고 판단 근거만 테스트로 남겼다(`digest` 에 올렸다).

**README 카운트 가드가 제 일을 했다** — 544→553 이 안 맞아 빨개졌고 README 를 고쳤다.
**러너 호출 규율**: 맨몸·단독 실행, 파이프·리다이렉션·`2>/dev/null`·명령 잇기 **0회**.

## 다음 — 리뷰 phase (계획 48)

- **볼 자리**: 종료 코드 1/2 의 갈림이 러너 세 곳(`--repeat`·fixture·PAGE_SIZE)에
  흩어져 있다. 한 벌로 묶을지가 판단거리다 — 이번엔 기존 두 가드의 모양을 그대로
  따랐다(새 구조를 안 만들었다).
- **닿을 수 없는 가드(G7)를 남긴 판단**도 리뷰 몫이다. 지우는 쪽이 옳다면 설계서
  갈림길 4 의 문장도 같이 지워야 한다.
- 제품 `src/` 는 이 반복에서 **0줄**이다 — 리뷰가 볼 diff 는 `e2e/passage_eval.py`
  (+8/-4) · `tests/test_passage_eval.py`(새 파일) · 문서다.
- **설계가 재색인·스키마 변경을 안 골랐다** — 계획 5절의 보류 조건에 안 걸린다.
- 남은 확장 둘: **속도 제한**(사람이 시점을 정한다) · `recrawl`(원래 사양의 마지막).
- `digest.md ## 다음 계획 후보` 의 열린 `[4]` 들은 그대로다. 이번 반복이 `[5]` 둘을
  더했다(G7 도달 불가 · G5 재현 난이도).
- **집안일**: `history_current.md` 279줄 → 이 항목 붙이기 전에 회전(`history_033.md`,
  개발 세 반복). `digest.md` 는 **정확히 200줄**이라 다음 회전은 완료 항목을 지워야 한다.

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-02 확인)

`origin/main` 은 `494313b`(PR #5 병합). 계획 43·44·45·46·**47** 의 커밋들(`bc40ea9` 이후)이
아직 안 갔다. **남은 것 (사람 결정)**: 그것들을 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).
**리뷰 커밋 `fe8b6a4` 가 안 올라가 있던 것은 이번 반복이 함께 푸시했다.**

## 사람이 정할 것 — 넷이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, `~/.claude/skills/loop-harness/rules/docs.md` 에는 그 예외가 없다.
   **저장소 밖이라 루프가 못 고친다.**
4. **«러너 호출을 다른 것과 섞지 않는다» 를 문장이 아니라 훅으로 옮기는 일.**
   재발 **17회**. 저장소 쪽 자리(`docs/project.md ## 명령`)에 이미 적혀 있는데도 났고,
   남은 자리는 `~/.claude/skills/loop-harness/rules/` 라 **저장소 밖이라 루프가 못 고친다.**
   **이번 반복이 0 으로 끝냈다고 항목이 닫히지는 않는다** — 28회 중 0 은 한 표본이고,
   ⑭⑮⑯(출력 조작)을 끊자 ⑰(명령 잇기)이 났던 것이 «문장을 조여도 옆으로 샌다» 의
   근거다. 닫으려면 **판정 명령을 단독 실행으로 강제하는 PreToolUse 검사**가 필요하다.

**사양이 사람에게 남긴 것 셋** (`docs/specs/concept.md` 의 `## 사람이 정할 것`):
속도 제한 시점(가정 IP 당 분당 60회) · JSON 검색과 근거 문단의 분할(가정 쪼갠다 —
**계획 46 이 그 가정을 따랐고 닫았다**) · 사양 숫자들이 초안이라는 것(90% · 500ms ·
60회/분). 첫째와 셋째는 아직 열려 있다.
