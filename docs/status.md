---
signal: GREEN
phase: 테스트
step: 1/1
attempt: 0
iteration: 379
updated: 2026-09-06
ctx: 62
night_iterations: 187
night_red: 2
night_retries: 4
plan: readme-band-cover 계획 65 (테스트 1/1 완료 · 8점 이상 갭 0건 · 다음은 리뷰)
---

## 현재 상태

**테스트 phase 를 닫았다 — 새로 쓴 테스트는 0줄이고, 대신 서른셋을 다시 쟀다.**
`rules/test.md` 는 이 자리를 「빠뜨린 것을 찾고(3절) 전체를 돌리고(1절) 단언을 안 낮췄는지
보는(6절)」 곳으로 정의한다. 갭 셋을 찾았는데 **셋 다 8점 미만**이라(4절) 스위트는
안 건드리고 `digest` 후보 한 줄로 남겼다. 전수 `Ran 623` · `OK` · **rc 0**.

## 개발의 표를 다시 걸었다 — 열셋 전부 같은 수다

저장소 밖 사본(`src`·`tests`·`e2e`·`docs`·`README.md`만 tar 복사 · `.git`·`data/` 없음)에서
변이마다 전수를 다시 돌렸다. 워킹트리 무변경 · 러너 출력 리다이렉션 0.

| 변이 | 개발(378) | 테스트(379) |
|---|---|---|
| M0 무변이 대조군 | 0 | **0** (오탐 0 · `Ran 623` · `OK` · rc 0) |
| M3·M4·M5·M6·M7 (상수 축 생존 다섯) | 각 1 | **각 1** |
| M1·M2·M8 | 각 2 | **각 2** |
| M9 | 3 | **3** |
| R1·R2·R3 (문서 축) | 각 1 | **각 1** |

**어긋난 값 0.** 실패 메시지도 그대로 `band='검색 합격선 %'` 처럼 범인 행을 집는다.

## 이 계획 고유의 구멍 셋을 실측했다

- **① 정규식이 죽으면 조용한가 — 아홉 행 각각 쟀다(P1~P9).** 수치는 그대로 두고 문구만
  갈아 패턴을 죽였다(「10개 중에」→「10건 가운데」 · 「80%」→「80퍼센트」 · 행 머리
  `| 빠른가 |`→`| 검색이 빠른가 |` 등). **9/9 가 운다** — 조용한 행 0. 메시지는
  「정확히 하나 못 뽑았다: [] (정규식 …)」이라 죽은 패턴을 그대로 보여 준다.
  반대 방향도 쟀다 — README 에 「10개 중에」를 한 줄 더 넣으면 `['10','10']` 로 **2개라서**
  실패한다. 0개도 2개도 실패라는 개발의 주장이 양쪽에서 참이다.
- **② 환산 경로가 틀리면 우는가 — 아홉 배율 전부 갈았다(S1~S9).** 초→ms 둘(1000→1) ·
  KB→B(1024→1000) · 배율 1인 여섯을 10·60·100 으로. **9/9 가 운다.**
  **값이 우연히 맞아떨어지는 자리는 하나도 없다** — 배율 1인 여섯도 곱하면 값이 갈린다.
- **③ 값이 겹치는 수치에서 범인 행 하나를 집는가(V1~V6).** 대비 4.5↔3 을 서로 바꾸고,
  상위 10 을 80 으로, 「95번은 N초」 두 행을 0.3↔0.5 로 뒤집었다. **6/6 이 정확히 한 행만**
  죽고 메시지가 그 행을 이름으로 적는다 — 두 예산 행이 서로 섞이지 않는다(V5·V6 이 그 증거).
- **④ 이름이 틀리면 그 자리에서 터진다** — 계획 5절의 주장이 참이다. 상수 이름 오타는
  `AttributeError: module 'quality_eval' has no attribute 'TOP_NN'`, 모듈 이름 오타는
  `ModuleNotFoundError` 로 각각 ERROR 1건이다.
- **⑤ 격리** — `test_readme.py` 만 단독으로 돌려도 `Ran 6` · `OK` · rc 0 이다.
  앞 모듈(`test_design_check`·`test_quality_eval`·`test_passage_eval`)이 먼저 임포트해 준
  덕이 아니라는 뜻이다. 전수에서 알파벳 순 뒤에 있는데도 순서 의존 0.

## 찾은 갭 셋 — 전부 8점 미만이라 안 열었다

`rules/test.md` 3절 여섯 카테고리를 항목마다 대조했고 「해당 없음」으로 넘긴 칸은 없다.
4절대로 점수를 붙였고 **8 이상이 0건**이라 이번 스텝의 스위트 변경은 **0줄**이다.

1. **[7] 표에 행이 늘면 조용하다.** README 「잘하고 있나 재는 자」 표에 열 번째 행을
   끼운 변이가 `Ran 623` · `OK` · rc 0 · **죽은 단언 0**. `QUALITY_BAND` 는 리터럴 아홉 줄이라
   **새 합격선은 대조 없이 들어온다.** 계획 65 가 닫은 축의 여집합이고, ①은 표의 **구조**
   축이라 계획 5절이 「표의 세 번째 칸은 새로 안 잰다」로 잘라 둔 자리 옆이다(카파시 3번).
2. **[5] `Decimal` 대 `float` 는 이진 정확값 위에서만 참이다.** 상수와 README 를 **함께**
   4.5→4.6 으로 옮긴 **일치 상태**에서 `Decimal('4.6') != 4.6` 으로 **거짓 RED** 가 난다.
   오늘 아홉(10·80·90·300·500·5.0·51200·4.5·3.0)은 전부 이진 정확이라 잠복이다.
   처방은 `decimal.Decimal(str(actual))` 한 줄.
3. **[4] 세 번째 사본 `docs/specs/concept.md`.** 계획 5절이 남긴 것이고 **읽기만 했다.**
   오늘 셋이 일치한다 — 23행(상위 10 · 80%) · 44행(초당 5) · 51행(JS 50KB) · 53행(대비 4.5) ·
   115행(문단 90%) · 127~129행(300ms · 500ms). **비텍스트 3배만 사양에 없다**(그 상수의
   주석이 「WCAG 2.1 SC 1.4.11 — concept.md 는 텍스트만 적었다」로 이미 적어 뒀다).

셋을 `digest ## 다음 계획 후보 (테스트 phase 갭, 8점 미만)` 에 `[7]` 한 줄로 등재했다.
여는 조건도 함께 적었다 — 「표에 행을 더하거나 아홉 중 하나를 이진 비정확 값으로 옮기는 날」.

## 단언을 안 낮췄다 (6절)

`tests/`·`e2e/`·`src/`·`README.md` **0줄**. 통과시키려고 고친 것이 없으니 낮출 것도 없었다.

## 검증

- 전수 **맨몸**(리다이렉션·파이프 0) `Ran 623 tests` · `OK` · **rc 0**.
- 변이 서른셋은 **저장소 밖 사본에서만** 돌았다 — 도는 내내 `git status --porcelain` 이
  의도한 문서 넷 외에 비어 있었다(계획 33·34·35·39·41 이 다섯 번 재발시킨 「제자리 변이를
  자동 스냅샷이 커밋한다」의 처방 그대로다).
- `data/crawl.db` sha256 `85c96744…5bda18` 무변 · 재색인 0회 · `docs/specs/` 읽기만 ·
  PR #7 무접촉(`gh` 호출 0) · `main` 무접촉 · 러너 위반 0(누적 38).
- 고친 파일은 기록 다섯뿐이다 — `docs/status.md` · `docs/history_current.md` ·
  `docs/history_066.md`(신규) · `docs/digest.md` · `docs/index.md` · `docs/metrics.md`.

## 회전했다

`history_current.md` 가 286줄이라 이번 append 로 상한 300 을 넘었다. 계획 64
`archive-scope-cover` 의 다섯 항목(반복 371~375 · 158줄)을 **`docs/history_066.md`** 로
옮겨 128줄이 됐고, **`digest.md` 의 아카이브 명부에 `history_066.md` 를 등재했다**
(계획 64 e2e 가 명부 누락으로 대조군을 빨갛게 만든 사례가 있다 — `ArchiveIndexTest` 가 문다).
`digest.md` 는 200줄 정각을 지키려고 가장 오래된 완료 항목 하나(계획 59
`endtag-cut-cover`, 2026-09-05)를 뺐다 — 원본은 `index.md` 59번 행에 그대로 있다.

## 다음

**리뷰 phase.** 볼 것은 `tests/test_readme.py` 의 `QUALITY_BAND`·`QualityBandTest` 뿐이고
(제품 `src/` 0줄), 위 갭 셋은 이미 점수와 여는 조건까지 적혀 있으니 리뷰가 다시 안 판다.
e2e 는 그다음 반복 몫이다.

## 한도

- 병합은 사람 몫 — 계획 57~65 의 커밋이 `loop/passage-cost-band` 에 쌓여 있고
  `origin/main`(`d1fe3e9`) 무접촉 · PR #7 무접촉.
- `--force`·`--amend`·`rebase` 없음. 스텝 커밋 하나.
- 러너에 리다이렉션·파이프를 안 붙인다 — 이번 반복도 위반 **0회**(누적 38 유지).
- `PYTHONDONTWRITEBYTECODE=1` 과 `PYTHONPYCACHEPREFIX=$(mktemp -d)` 를 함께 준다.
- `night_iterations` 는 187 그대로다(대화형 반복이라 안 올린다).
- `docs/history_current.md` 는 회전 직후 **128줄**(상한 300)이라 여유가 있다.
