---
signal: GREEN
phase: 개발
step: 1/3
attempt: 0
iteration: 435
updated: 2026-09-07
ctx: 62
night_iterations: 2
night_red: 0
night_retries: 0
plan: project-const-drift
---

## 현재 상태

**계획 75 `project-const-drift` 스텝 1/3 완료.** `const_gap()` 순수 함수와 갈래 7건.
변이 4종(판정 무력화 · `= 숫자` 요구 제거 · 없는 상수 묵인 · 쉼표 무시)이 전부 죽는다.
단위 655 → **662건**. 다음은 스텝 2 — 살아 있는 `project.md` 에 물린다(오늘은 RED 가 정상).

## 무엇이 낡았나

`docs/project.md:188-189` 가 `indexer.MAX_PASSAGE_HTML` **= 35,000자**라고 적었는데
실제 값은 **2,000,000** 이고, 재파싱을 자르는 자는 계획 74 가 갈아 끼운
`MAX_PASSAGE_TAGS = 3_000` **태그**다. 값도 단위도 틀렸고, 거기서 유도한 숫자
(최악 350ms · 동결 `35,000 × 10` · 상한 1.4286 ms/1000자)가 전부 딸려 틀렸다.

**`project.md` 는 루프가 매 반복 읽는 네 파일 중 하나**이고 그 절이 판단의 눈금이라,
거짓 눈금은 「기록된 답을 실행 전에 다시 재라」(digest `[7]`)가 경고하는 바로 그 자리다.

## 왜 계획인가 — 세 번째다

계획 57(`e6f375c` 「기록 자리 둘을 오늘 값으로 맞춘다」)·계획 58 은 이 자리를 **손으로**
맞췄고 계획 74 는 잊었다. 아무도 `project.md` 를 **입력으로 읽지 않는다** —
`tests/test_docs.py` 에 gap 검사가 넷(`iter_gap`·`step_gap`·`strike_gap`·`verdict_gap`)
살아 있는데 상수 인용을 재는 것만 없다. digest `[6]` 이 그 자리를 이미 지목해 뒀다.
그래서 이 계획은 문단을 고치는 것이 절반이고, **다시 낡지 못하게 만드는 것**이 나머지다.

## 탐색 기록 (8순위 전수)

1 실패 0(**655 OK rc 0**) · 2 린트 설정 없음 · 3 코드 `TODO` 0(유일한 히트
`tests/test_indexer.py:789` 는 fixture 문자열) · 4 `docs/candidates.md` 없음 ·
5 보류 0(주석뿐) · 8 이슈 트래커 없음. 6·7 의 열린 후보는 여는 조건이 안 섰다 —
`[7]` robots 비ASCII(새 호출처 없음) · `[6]` `loaves`(사전 필요) · `[6]` porter
부분접두(실물 검색 로그 필요) · `[5]` 색인 2.4배(경량 축 미도달) · `[7]` 숨은 텍스트
남은 축(`_SKIP_TAGS` 가 색인 경로 공유 → 재색인, 야간 승인 게이트) · `[9]`·`[5]`·`[6]`
셋은 계획 73·74 가 닫았다. **근거는 후보 목록이 아니라 오늘 실측에서 나왔다.**
