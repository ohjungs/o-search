---
signal: GREEN
phase: 리뷰
step: 1/1
attempt: 0
iteration: 336
updated: 2026-09-05
ctx: 48
night_iterations: 157
night_red: 2
night_retries: 0
plan: passage-cost-axis 계획 57 (개발 phase 완료 · 완료 기준 1~7 전부 참)
---

# 현재 상태

**계획 57 개발 phase 를 닫았다 — 완료 기준 6 «세 기록 자리의 숫자가 서로 같다» 가
직전 반복까지 거짓이었고, 낡은 둘을 오늘 값으로 맞췄다.** 고친 것은 주석과 문서뿐이라
제품 **동작 0줄**이고 단위 건수 **605** 도 그대로다.

## 무엇을 맞췄나 — 두 자리, 같은 단언

| 자리 | 앞 | 뒤 (테스트와 동일) |
|---|---|---|
| `src/websearch/indexer.py` `MAX_PASSAGE_HTML` 주석 | 0.44 · 154ms · 예산의 31% · «배정치 1/3 이 모양 배수를 흡수한다» | 0.95 · 332.5ms · **67%** · «흡수할 배수가 없다» |
| `docs/project.md ## 품질 기준` 근거 문단 절 | 같은 낡은 값 + 「예산 25% 선에서 막는다」 | 같은 새 값 + 상수 동결(`캡 × PASSAGE_LIMIT ≤ 35,000 × 10`) · 계수 상한 **1.60 ms/1000자** |

두 자리 모두 **한도가 `500/3` → `500` 으로 펴진 사실**과 **그 대가로 세운 문 둘**을 적는다.
「몫을 다시 긋는 것은 사람 판단」(사양 성능 4 의 검색 몫 300ms 가 같은 500ms 안에 산다)도
같이 적어, 예산 67% 라는 숫자가 «합격» 으로 읽히지 않게 했다.

## 검증 — 전부 맨몸으로 돌려 rc 를 봤다

- 전수 `Ran 605 tests · OK` · rc 0 · **13.8초**(앞뒤 동일 — 텍스트만 고쳤다).
- 문서 가드 4종(`IterationSyncTest`·`ArchiveIndexTest`·`DocCitationTest`·`tests/test_readme.py`)
  `Ran 8 tests · OK` · rc 0.
- **낡은 숫자 잔존 grep 0건** — `src/`·`docs/project.md`·`README.md`·`e2e/` 에 남은 `0.44`·
  `1/3` 은 「계수가 세 번 낡았다」는 **이력 서술**뿐이다.
- `data/crawl.db` sha256 앞뒤 동일(`85c96744…5bda18`) · 재색인 0 · 스키마 0 · 새 의존성 0 ·
  `tests/`·`e2e/`·`README.md`·`docs/specs/` **무접촉** · PR 무접촉(`gh` 호출 0).
- 문서 회전 — `history_current.md` 가 300줄 상한에 닿아 계획 56 다섯 반복을
  **`history_059.md`** 로 밀고 `digest.md` 명부·회전 줄을 갱신했다(새 색인 구멍 0).

## 다음 스텝 — 리뷰 phase

계획 4절 완료 기준 **1~7 이 전부 참**이다(1 전수 OK · 2~3 변이 M1·M2·M3 은 직전 두 반복이
실측 · 4 벽시계 증가 0.2초 · 5 README 단위 건수 무변 · 6 이번 반복 · 7 DB·specs 무변).

**리뷰에 넘기는 구멍 하나** — 세 기록 자리를 묶는 **기계 검사가 없다.** 이번에도 사람이
grep 으로 맞췄고 `DocCitationTest` 는 append 전용 문서를 줄번호로 가리킨 인용만 본다.
계수가 **네 번째로** 낡으면 같은 자리가 다시 갈린다(계획 7절 위험 둘째가 예고한 것).

## 사람 결정 대기

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1).
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 0 일 때만.
3. **반응형 360px 미검증** — 브라우저가 없어 저장소의 아무도 안 그려 봤다.
4. **3시간 자동 스냅샷 잡을 루프 작업 중에도 세울 것인가**(반복 328 사고). RED 중간을
   덮치면 깨진 상태가 원격에 올라간다. 반복 334 에서 RED 아닌 자리에서 실제로 일어났다.
5. **재파싱에 예산 얼마를 줄 것인가** — 캡 최악이 예산의 **67%** 라는 사실이 이제 세 자리에
   모두 적혀 있다. 캡·`PASSAGE_LIMIT` 를 내리는 것은 «긴 문서의 뒷부분 근거» 쪽 판단이다.
