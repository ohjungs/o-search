# e2e 결과 — 계획 50 `runner-quiet` (2026-09-03 · 반복 286)

브랜치 `loop/runner-quiet` · 기점 `373cafd` · 직전 커밋 `0071cf0` · **새로 만든 e2e 파일 0개**.
러너 호출 **21회 전부 맨몸·단독**이다 — 파이프·리다이렉션·명령 잇기 **0회**.

## 1. 선행 관문 셋 (`rules/e2e.md` 1절)

| 관문 | 명령 | 결과 |
|---|---|---|
| 전체 검증 | `PYTHONPATH=src python3 -m unittest discover -b -s tests` | **579건 OK · 13.585초** · 실패 0 · **화면 5줄** |
| 린트·타입체크 | `docs/project.md` 에 **없음** | 해당 없음 — "통과" 라고 쓰지 않는다 |
| CI | `.github` 부재 | 해당 없음 (1절 2항) |
| 품질 기준 | 아래 4절 | 전 축 통과 · 기준선 무변 |

**전체 검증 명령 자신이 이번 계획의 산출물이다** — `-b` 가 들어간 그 줄을 이 phase 가
처음으로 «계획을 닫는 관문» 으로 썼고, 판정 줄 `Ran 579 tests / OK` 가 **화면의 마지막**이었다.

## 2. 이 phase 가 재는 것 — 완료 기준 C 하나만 남아 있었다

계획 50 의 완료 기준 A(사라져야 할 것 3줄)·B(그대로여야 할 것 4줄)는 개발 1 과 테스트 1 이
닫았고, 리뷰 1 이 8점 이상 0건으로 통과시켰다. **e2e phase 가 재는 것은 5절 C —
`e2e/*.py` 20종 전수 rc 0** 이다.

계획이 고친 것은 제품이 아니라 **저장소가 자기 테스트 명령을 적어 둔 두 줄**이라, 이 phase 의
물음은 «그 두 글자가 e2e 를 흔드나» 다. **답은 구조적으로 아니오다** — `e2e/` 아래에서
`unittest` 를 부르는 스크립트는 **0개**이고(유일한 문자열 히트는 `e2e/quality/corpus.json`·
`queries.json` 의 코퍼스 본문이다), 20종은 전부 `python3 e2e/<이름>.py` 로 직접 도는
독립 프로세스다. 그래도 **전수를 실제로 돌려서** 확인했다 — 구조적 논증은 실행이 아니다.

## 3. 회귀 — `e2e/*.py` **20종 전수 rc 0**

| e2e | 실측 |
|---|---|
| `crawl_e2e` | 수집 15 · 차단 0 · 최소 간격 **1.005s** |
| `indexer_e2e` | 3문서 · 재실행 0문서(증분) · 없는 상대 경로 rc 1 |
| `noindex_e2e` | 4페이지 → **2문서** · 뒤늦은 noindex 제거 |
| `search_api_e2e` | 15문서 · 1p 10건/2p 5건 · 겹침 0 · `/passages` 계약 · 손상 DB 500 · p95 **2.11ms** |
| `crawl_delay_e2e` | 6페이지 4.5s · `Crawl-delay:2` 최소 **2.00s** · 하한 도메인 **1.00s** |
| `non_ascii_e2e` | 3행(`%EA%B0%80.html`) · 서로게이트 시드 1개 건너뜀 |
| `design_check` | **4축 전부 통과 · 종료 0** (아래 4절) |
| `tokenizer_e2e` | 6문서 crawl→색인→CLI 화면(HTML) · 옛 색인 503/rc 1 → 재색인 복구 |
| `domain_key_e2e` | 7.0s · 같은 서버 4회 간격 **2.01·2.01·2.01** · robots 1회 · 대조군 1.01/1.00 |
| `deadline_e2e` | 대조군 16p 4.1s · CLI 8p **2.1s** · 버려진 응답 **0건** · 안 답하는 서버 1건 **10.1s** rc 0 |
| `interrupt_e2e` | 대조군 3p 2.1s · SIGINT 뒤 **10.0s rc 130** · 떠 있던 1행 줍기 · 2차 Ctrl-C **rc -2** |
| `indexer_interrupt_e2e` | 2000문서 · 재구축 중단 **rc 130 · 옛 색인 2000행 그대로** · 재실행 복구 rc 0 |
| `crawl_politeness_e2e` | 5.1s · 예외 뒤 최소 **2.01s** · 대조군 1.00s · 재시도 1.01s |
| `retry_interval_e2e` | 15.6s · https 재시도 최소 **5.01s** · 대조군 1.00s |
| `url_normalize_e2e` | 5.0s · 표기 10 → **문서 6** |
| `pagination_ui_e2e` | 11문서 · 다음을 따라가 doc00 에 닿음 · 마지막에 다음 없음 |
| `quality_eval` | ko **20/20 (100%)** · en **19/20 (95%)** (아래 4절) |
| `passage_eval` | 정확도 **100.0%** · p95 **1.48ms** (아래 4절) |
| `perf_search` | p95 **8.97ms** (아래 4절) |
| `perf_crawl` | 3시나리오 전부 통과 (아래 4절) |

**20종이다 — 계획 50 은 e2e 파일을 하나도 안 만들었다**(제품 `src/` 0줄이라 프로세스
경계에서 새로 밟을 것이 없다. 계획 41·42·44·49 와 같은 판정). `README.md` 의
`e2e 시나리오 20종` 은 무변이고 `단위 578건` 은 개발 1 이 **579** 로 고쳤다 —
두 줄 다 `tests/test_readme.py` 가 실제 스위트·실제 디렉터리와 대조한다.

## 4. 품질·성능 축 — 전 축 무변

- **디자인 4축**: 외부 서브리소스 **0건** · JS gzip **0 B**(예산 51200) · 문서 gzip
  1443/1755 B(예산 102400) · 서버 2.4/0.8 ms(예산 300) · 최저 텍스트 대비 **4.87:1**(기준 4.5) ·
  비텍스트(포커스 링) **3.56:1**(기준 3.0) · `overflow-wrap` 있음
- **검색 품질**: ko **20/20 (100%)** · en **19/20 (95%)** · 순위 1위 **39** · 2~10위 0 ·
  미검출 1(`loaf`) · 매치 평균 **14.0** 최소 **11** 최대 28 — 기준선 그대로
- **근거 문단**: 채택률 **99.5%**(398/400) · 정확도 **100.0%**(398/398) ·
  p50 1.32ms · p95 **1.48ms**(예산 500ms 의 0.3%)
- **검색 성능**: `perf_search` p95 **8.97ms**(예산 300ms 의 3.0%) · `search_api` p95 2.11ms
- **크롤 처리량**: [열림] **10.24/s**(기준 5.0) · [차단] **10.23/s**(기준선 9.0) ·
  [예외] 최소 간격 1.006s · 문서집합 sha1 `541d455a383482de8b1f5216a2398cc4c078654b` — 무변

## 5. 웹 UI 8단계 (`rules/e2e.md` 4절) — 브라우저 없이 닿는 자리만

Playwright 는 이 저장소에 없다(`project.md` 의 e2e 는 전부 파이썬 스크립트다).
브라우저가 필요한 항목은 **검증되지 않았다고 적는다** — "통과" 라고 쓰지 않는다.

| 단계 | 어떻게 | 결과 |
|---|---|---|
| 1 시각 스캔 | `design_check` 의 대비·`overflow-wrap`·크기(대리 지표) | 통과 |
| 2 상호작용 | `pagination_ui_e2e` 가 «다음» 링크를 실제로 따라간다 | 통과 |
| 3 폼 | `search_api_e2e` 400(빈 질의·잘못된 page)·501 | 통과 |
| 4 내비게이션 | 1p→2p→마지막, 1페이지에 이전 없음 | 통과 |
| 5 상태 | 무결과 안내(`indexer_e2e`) · 503(색인 없음) · 500(손상 DB) | 통과 |
| 6 콘솔 | 외부 서브리소스 **0건 · JS 0 B** — 실행될 JS 가 없어 예외가 날 자리가 없다 | 통과(구조적) |
| 7 반응형 | 고정 `width` 없음 · 뷰포트 실측(360px)은 브라우저 몫 | **미검증** |
| 8 인증 경계 | 인증이 없는 제품이다 | 해당 없음 |

**8단계는 이번 계획과 맞닿은 축이 없다** — 계획 50 은 화면을 한 픽셀도 안 건드렸다.
그대로 옮겨 적은 것이 아니라 위 20종을 오늘 다시 돌려 얻은 판정이고, 7번이 계획 49 때와
같은 이유로 여전히 **미검증**이다(360px 을 그려 보는 자가 이 저장소에 없다).

## 6. 범위 고정 — 계획 5절 C

- 제품 `src/` **0줄** · `e2e/` **0줄** · 새 e2e 파일 **0개**
  (`git diff --stat 373cafd HEAD -- docs/specs data/crawl.db src e2e` **빈 출력**)
- 새 의존성 **0** · 스키마·마이그레이션 **없음**
- `docs/specs/` **무변경**
- `data/crawl.db` sha256 `85c967448c2432251004d6abe1be4571934c02d47719bb86824fb7b4975bda18` — **무변경**
- 계획 전체가 고친 파일은 **`README.md`·`docs/project.md`·`tests/test_readme.py` 셋**과
  기록 문서뿐이다(계획서 4절이 적은 정확히 그 3개)
- 이 phase 가 만든 파일은 **이 문서 하나**(`rules/e2e.md` 3절의 세 예외 중 첫째)
- `.mutation-lock` 은 이번에도 필요 없었다 — 변이는 테스트 phase 가 먹였고
  e2e phase 는 저장소를 안 비틀었다

## 7. 남기는 것 — 다음 세션이 이어받을 것

1. **`-b` 는 통과한 테스트의 경고도 삼킨다** — 리뷰 1 이 독립 탐침으로 재고
   `digest ## 다음 계획 후보 (테스트 phase 갭, 8점 미만)` 의 `[4]` 로 등재했다.
   **오늘의 손실은 0** 이다(맨몸으로 `-b` 없이 돌린 579건 어디에도 경고가 0줄).
2. **결합자 뒤의 링**(`a:focus-visible+.hint`)은 계획 49 가 남긴 그대로 거짓 초록이다.
   제품 CSS 에 결합자 뒤 링은 **0곳**이라 오늘의 3.56:1 은 참이다.
3. **룰 문장을 조이는 절반은 여전히 사람 몫이다** — 계획 50 이 닫은 것은
   저장소 안쪽 절반(자기 명령을 적어 둔 두 줄)뿐이고, 저장소 밖
   `~/.claude/skills/loop-harness/rules/` 는 한 글자도 안 건드렸다.
4. **`main` 으로 보내는 PR 은 사람 몫이다** — `origin/main` 은 PR #6 까지만 받아
   **계획 48·49·50 세 계획이 미병합**이다. 무인 모드는 PR 을 열지도 병합하지도 않는다.
