---
signal: GREEN
phase: 개발
step: 2
attempt: 0
iteration: 277
updated: 2026-09-02
ctx: 60
night_iterations: 116
night_red: 1
night_retries: 0
plan: focus-rule-scope
---

# 현재 상태

**계획 49 `focus-rule-scope` 의 개발 1/2 — 조건 5(셀렉터 극성)를 넣어 ⓒⓓ 를 죽였다.**
`e2e/design_check.py` +12 −2 · `tests/test_design_check.py` +9 −0 · 제품 `src/` **0줄**.
새 코드는 주석 빼고 **3줄**(정규식 둘 · 조건 한 줄)이고 설계서가 잡은 6줄 안이다.
브랜치 `loop/focus-rule-scope`.

단위 **576건 OK**(맨몸 13.472초 실측) · `PYTHONPATH=src python3 e2e/design_check.py`
**종료 0** · e2e **20종** · `data/crawl.db` 무변경 · `docs/specs/` 무변경.

## 무엇이 바뀌었나 — 3줄

```python
NOT_RE = re.compile(r":not\([^()]*\)")
FOCUS_RE = re.compile(r":focus(?:-visible)?(?![\w-])")
if not FOCUS_RE.search(NOT_RE.sub("", selector)):
```

기존 `":focus" not in selector` 를 **대체**했다(설계서 3절 조건 5). 메시지는
`… 키보드 포커스용이 아니다: <셀렉터>` 로 한 낱말만 늘었고, V11(`:hover`)의 단언이 무는
`포커스용이 아니다` 는 부분 문자열로 그대로 살아 **설계가 예상한 그대로** 판정 무변이다.
`focus_rule` 의 반환·예외 모양은 안 바뀌었다 — 공개 인터페이스 변경 0.

## TDD — RED 를 눈으로 봤다

테스트를 먼저 쓰고 돌렸다. ⓒⓓ 둘 다 **`unmeasurable` 0건 · 종료 0** 으로 살아남으면서
무효가 된 `라이트 --focus on --bg-page 3.56:1 / 기준 3.0 OK` 를 화면에 찍었다 —
계획 44 가 죽이러 온 고장의 형태 그대로다(`FAILED (failures=2)`, 576건 중 2건).
구현 뒤 같은 명령이 `Ran 576 tests in 13.472s / OK`. 단위 건수는 576 그대로다 —
둘 다 기존 `test_ring_that_is_not_drawn_is_unmeasurable` 의 `subTest` 로 들어갔다.

## 조건의 각 부분이 짐을 진다 — 변이 셋

스크래치패드 탐침에서 `NOT_RE`·`FOCUS_RE` 만 갈아끼워 부분마다 하나씩 무력화했다
(`python3 -B` · 저장소 파일 무편집 · `.mutation-lock` 불필요 · **커밋된 변이 0**).

| 무력화한 부분 | ⓒ `:not(:focus-visible)` | ⓓ `:focus-within` |
|---|---|---|
| 없음(원본) | **종료 2** | **종료 2** |
| M1 `:not()` 벗기기를 뺀다 | **종료 0 생존** | 종료 2 |
| M2 낱말 경계 `(?![\w-])` 를 뺀다 | 종료 2 | **종료 0 생존** |
| M3 옛 부분 문자열 가드로 되돌린다 | **종료 0 생존** | **종료 0 생존** |

**오탐 0** — 정상 CSS 와 `:not(.sb-x):focus-visible`(정당한 `:not()` 과 포커스가 한
셀렉터에 있는 형태) 둘 다 네 줄 전부에서 종료 0 이다. 종료 코드는 반환값이 아니라
`dc.main()` 을 실제로 돌려 확인했다 — 제품 CSS 를 ⓒ 로 바꿔 서버에 태우니 **종료 2**.
`git status` 는 두 파일만 M 이고 변이 잔재(`sb-x`·`(?!x)x`) 검색 0건이다.

## 러너 규율 — 이번 반복 1회 (누적 30회)

**밟았다.** `design_check.py` 를 맨몸으로 한 번 제대로 돌려 4축 전부와 `4축 전부 통과.`
까지 본 **뒤에**, 종료 코드를 재확인하겠다고 `> /dev/null; echo "종료코드=$?"` 를 붙였다.
파이프·리다이렉션·`;` 잇기 셋을 한 줄에서 어겼다. 방아쇠는 「출력이 길다」가 아니라
**「종료 코드를 눈으로 못 봤다」** — 지금까지 29회와 다른 새 방아쇠다. 실제로는 맨몸
호출이 이미 증명하고 있었다(러너가 0 이 아니면 Bash 툴이 `Exit code N` 으로 먼저
띄운다 — 이 반복의 RED 도 그렇게 `Exit code 1` 로 왔다). **불필요했고, 안 했어도 아는
것이 그대로였다.** 단위 두 번은 맨몸·단독으로 돌렸고 판정 줄 아래 크롤 로그를 안 가렸다.

## 다음 — 개발 2

조건 6(at-rule 위치, 3줄)을 넣어 ⓐⓑ 를 종료 2 로 만든다. 링 규칙 앞의 `{`·`}` 개수가
같아야 한다는 것 하나다(설계서 3절). 건드릴 파일은 이번과 같은 둘이고 `src/` 는 0줄이다.
설계서가 **at-rule 화이트리스트를 안 만든다**고 못박았으니 넓히지 않는다.

## 기록·기점

`origin/main` 은 PR #6 까지만 받아 **계획 48 이 없다**. `README.md` 의 `단위 576건`·
`e2e 시나리오 20종` 과 `tests/test_readme.py` 의 건수 단언이 현재 기점에 맞춰져 있어
`main` 에서 브랜치를 따면 첫 커밋이 RED 다. 그래서 `loop/focus-rule-scope` 는
**`18d485f` 에서 땄다**. 이번 반복은 README 가 무변경이다 — 단위 576·e2e 20종 그대로.
푸시는 정상 절차다(강제 푸시만 금지).
**PR 은 안 열었다 · 웹 UI 의 *Update branch* 도 안 눌렀다** — 무인 모드는 열지도
병합하지도 않고, rebase-merge 로 해시가 새로 쓰인 뒤의 그 버튼은 머지가 아니라
복제다(PR #3 을 깨뜨린 원인).
`night_*` 세 필드는 이번 호출이 야간 세션이라는 근거가 없어 **안 건드렸다**.

## 사람이 정할 것 — 넷 그대로다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
2. **`--focus` 는 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 이 0 일
   때만 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** — 후보 B 가 닫는 것은 저장소 안쪽 절반
   뿐이고, 룰 문장을 조이는 나머지 절반은 여전히 사람 몫이다.
4. **사양 확장이 남긴 것 둘** (`docs/specs/concept.md` 의 `## 사람이 정할 것`):
   속도 제한 시점(가정 — IP 당 분당 60회)과 사양 숫자들이 초안이라는 것(90% · 500ms ·
   60회/분). 분할 가정은 계획 46·48 이 따랐고 둘 다 닫혔다.
