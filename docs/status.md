---
signal: GREEN
phase: 테스트
step: 5
attempt: 0
iteration: 271
updated: 2026-09-02
ctx: 68
night_iterations: 114
night_red: 1
night_retries: 0
plan: passage-api
---
# 현재 상태

**리뷰 6 의 반려 둘을 닫았다 — 갈림길 6 은 «닫힌 것»이 아니라 «열린 채로 적힌 것»이다.**
활성 계획 `passage-api` · 설계서 `docs/design_passage-api.md` · 브랜치 `loop/passage-api` ·
기점 `6e99031`. **단위 573건 전부 초록**(맨몸 단독 실행 확인, 13.5초) · 제품 `src/` 두 파일
(`extract.py`·`indexer.py`) · 새 파일 0 · 의존성 0 · 스키마·마이그레이션 없음 ·
`docs/specs/`·`data/crawl.db` 무변경 · `.mutation-lock` **0**.
`passage_eval` 정확도 **100.0%(398/398)** · 채택률 99.5% · p95 **1.46ms**(예산 500ms).

## 이번 반복이 한 일 (개발 10)

**리뷰 6 이 못박은 둘만 했다. 새 축은 안 열었다.** 범위 밖의 손잡이는 안 만들었다.

**[R6-1] 동점 키를 「길이」로 되돌렸다 — ㉠ 을 골랐다.** 리뷰의 19행 재측을 내 탐침으로
독립 재현했다: **점수만 11/19 · 이름표(`tag == "p"`) 12/19 · 길이 15/19.**
㉡(동점 키 없음)은 9/19 라 ㉠ 을 골랐다. 근거는 «더 낫다» 가 아니라 **«덜 틀린다»** 다.
뿌리는 리뷰가 짚은 그대로다 — 이름표는 **최내곽 태그**라 컨테이너를 못 보고
(`<footer><p>ⓒ…</p></footer>` 의 이름표가 `p`), 실물에서 가장 흔한 `<p>` 대 `<p>` 동점을
아예 못 가른다.

**못 맞추는 넷을 숨기지 않고 표에 박았다.** `_SHAPES` 를 8 → 15행으로 늘리고, **어떤 후보도
못 맞추는 4행**은 `_UNMATCHED_SHAPES` 에 «오늘 나오는 오답» 으로 고정했다
(본문 뒤 긴 푸터 · 본문 앞 긴 사이드바 · 긴 내비 + 문단 안 `<script>` ·
짧은 `<blockquote>` 뒤의 긴 저작권 `<p>`). 합쳐 **19행**이다.
검사는 `assertNotEqual(want, today)` 로 «아직 틀린다» 를 못박는다 — **맞게 되면 그 검사가
빨개져서** 다음 사람이 표를 고치게 된다. 초록불이 천장을 가리지 않는다.

**[R6-2] 이름표 누수를 뿌리에서 닫았다.** 증상은 둘이었지만 원인은 하나다 —
`_tag` **필드 하나**가 «지금 열린 블록» 을 흉내내고 있었다. `_open` **스택**으로 바꾸니
셋이 한자리에서 닫혔다: ① `<title>` 은 부모가 안 끊으므로 스택도 안 건드린다
② 닫는 태그가 주인을 바깥으로 **되돌린다**(`</p>` 뒤 꼬리는 `<article>` 것이다)
③ **몰랐던 셋째** — 같은 이름이 겹치면 **안쪽부터** 닫는다(`rindex`).
③ 은 변이가 찾아냈다(아래).

**변이 일곱 — 여섯은 즉사, 하나는 살아남아 검사를 늘렸다.**
A 닫는 태그 pop 삭제 사망 · B `_in_title` 가드 삭제 사망 · C `_open[-1]` → `[0]` 3건 사망 ·
**D `rindex` → `index` 생존** → 중첩 `<div>` 단언을 더해 사망 · E 동점 키 삭제 9건 사망 ·
F `>` → `>=` 1건 사망 · G 동점을 짧은 쪽으로 뒤집기 15건 사망.
**D 가 이번 반복의 값이다** — 생존을 «천장» 으로 적고 넘어가는 대신 검사를 늘렸고,
그 자리가 실물 HTML 의 기본형(중첩 `<div>`)이었다.

## 넘긴 것 — 갈림길 6 은 여전히 열려 있다

**닫는 조건은 갈림길 5 의 캡과 같다: 실물 크롤 코퍼스다.** 손잡이 셋(점수만·이름표·길이)
중 어느 것도 19행을 다 못 맞추고, **19행 표도 내가 만든 자라 정답표가 아니다.**
`data/crawl.db` 는 2문서 × 66자, e2e 코퍼스는 한 문장짜리 `<p>` 뿐이라
«보일러플레이트가 `<p>` 로 싸이는 빈도» 를 저장소가 못 잰다.
**설계서 갈림길 6 은 열린 채로 두고, 오늘의 선택과 그 이유를 거기 적었다.**

**범위는 그대로 못박혀 있다** — 인라인 연접(`김치찌개된장찌개`) · 갈림길 5 의 캡 ·
`_SKIP_TAGS` 를 색인 경로에서 빼는 것은 **계획 48 에서 다시 열지 않는다**(`digest` 후보).
다음은 **테스트 5** 다. 그 뒤 리뷰를 거쳐 **e2e 20종 전수로 보내 DONE 으로 닫는다.**

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-02 확인)

`origin/main` 은 PR #6 으로 계획 44~47 까지 받았다. 계획 48 의 커밋들은 브랜치에만 있다.
**이번 반복도 `origin/loop/passage-api` 로 푸시했다** — 푸시는 루프의 정상 절차다
(강제 푸시만 금지).
**남은 것 (사람 결정)**: 계획 48 이 끝난 뒤 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 — 넷이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, `~/.claude/skills/loop-harness/rules/docs.md` 에는 그 예외가 없다.
   **저장소 밖이라 루프가 못 고친다.**
4. **«러너 호출을 다른 것과 섞지 않는다» 를 문장이 아니라 훅으로 옮기는 일.**
   **재발 25 → 26회 — 이번 반복이 한 번 밟았다.** 변이 E 판정에서 `| tail -5` 와
   `2>/dev/null` 을 붙였고, 판정 줄이 stderr 라 **화면에 아무 판정도 안 나왔다.**
   즉시 맨몸·단독으로 다시 돌려 `FAILED (failures=9)` 를 읽었다.
   **방아쇠는 26번째도 「출력이 길다」다** — 크롤 로그가 화면을 덮는 것이 이 스위트의
   상수고, 그것을 가리려는 손이 매번 판정 줄까지 같이 가린다.
   처방은 이미 판정해 뒀다: `unittest` 의 **`-b`** 가 방아쇠를 뿌리에서 없앤다
   (실측 약 90줄 → 5줄, 거른 테스트 0, 새 코드 0). `digest.md ## 다음 계획 후보` 에
   **`[9]`** 로 등재돼 있다. 단서 — `-b` 는 테스트가 직접 낸 stdout/stderr 만 버퍼링하므로
   하위 프로세스가 fd 로 직접 쓰는 로그는 샌다. 판정 명령을 단독 실행으로 강제하는
   PreToolUse 검사는 여전히 **저장소 밖 몫**이다.

**사양이 사람에게 남긴 것 셋** (`docs/specs/concept.md` 의 `## 사람이 정할 것`):
속도 제한 시점(가정 — IP 당 분당 60회) · JSON 검색과 근거 문단의 분할(가정 — 쪼갠다,
**계획 46 이 그 가정을 따랐고 닫았다**) · 사양 숫자들이 초안이라는 것(90% · 500ms ·
60회/분). 첫째와 셋째는 아직 열려 있다.
