---
signal: YELLOW
phase: 계획
step: 0/0
attempt: 0
iteration: 448
updated: 2026-09-07
ctx: 51
night_iterations: 15
night_red: 0
night_retries: 0
plan: null
---

## 현재 상태

**야간 정지 — 근거 소진.** `discover.md` 1절 8순위를 전수로 훑었고 **오늘 열 수 있는
근거가 0건**이다. 없는 일을 지어내지 않는다(`discover.md` 2절). 계획 **2개**를 닫고
끝낸다(75 `project-const-drift` · 76 `history-cap-guard` · 최대 3).

## 탐색 결과 — 반복 448

| 순위 | 출처 | 실측 |
|---|---|---|
| 1 | 실패 테스트 | **0** — 전수 **672 OK** |
| 2 | 린트·타입 | 설정 **없음**(`project.md`) |
| 3 | 코드 `TODO`/`FIXME`/`HACK` | **0** — 유일 히트 `tests/test_indexer.py:789` 는 HTML 주석 **fixture 문자열** |
| 4 | `docs/candidates.md` | 파일 없음 |
| 5 | digest 보류 | **0**(절이 주석뿐) |
| 6 | digest 다음 계획 후보 | **열 수 있는 것 0** — 아래 |
| 7 | digest 반복 실패 | 최상위 후보(「회전 압축 줄 5개 누락」)의 **처방이 이미 서 있다** — `ArchiveIndexTest`, 오늘 실측 **73/73** 명부 등재 |
| 8 | 이슈 | 트래커 없음 |

**6순위를 하나씩 깬 결과** — 점수순으로:

- `[9]` `/passages` p95 → **닫혔다**(계획 73 실물 361문서가 결론을 뒤집었다 · 35.9%)
- `[7]` 숨은 텍스트가 근거 문단 → **실물 96건에서 0건**이고 남은 축은 `_SKIP_TAGS`·
  `_INLINE_TAGS` 가 색인 경로 공유라 **재색인 = 승인 게이트**(야간 금지)
- `[7]` `rules/docs.md` 문서 상한 → history 축은 **계획 76 이 닫았고**, digest 축은
  처방이 **삭제**라 야간이 스스로 못 푸는 RED 가 된다
- `[7]` robots 비ASCII → **도달 불가**, 여는 조건은 「`robots.py` 직접 호출처가 생기는 날」
- `[6]` `index.md` 다섯째 칸 · `[6]` 후보 절 포인터 모집단 → 둘 다 **여는 조건 미충족**
- `[5]` `html` 열 없는 DB 의 화면 사다리 → **죽일 변이가 안 선다**(형제 단언이 이미 죽인다)
- `[4]` 4300자리 숫자 인자 → stock 3.9.6 에서 **도달 불가**

## 아침에 사람이 정할 것

- **`digest.md` 는 201줄이다** — 반복 440 의 회전 기록 한 줄이 상한 200 을 1줄 넘겼다.
  무인 모드는 삭제를 안 하므로 **무엇을 버릴지는 사람이 정한다**(야간 보고서 1번).
  이것이 후보 `[7]` 의 digest 축을 못 여는 이유이기도 하다.
- **`.context-state.json` 이 낡았다**(반복 448 실측 **56분**) — statusLine 이 꺼진 것으로
  보고 `SKILL.md` 대로 **반복 상한에만 의존**했다. 마지막 값 51/30/45 는 전부 여유.
