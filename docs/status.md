---
signal: DONE
phase: e2e
step: 1
attempt: 0
iteration: 221
updated: 2026-09-01
ctx: 46
night_iterations: 81
night_red: 0
night_retries: 0
plan: null
---

# 현재 상태

**계획 43 `focus-contrast` DONE — e2e 1/1 완료, 새로 만든 e2e 파일 0개.**
브랜치 `loop/focus-contrast` · 결과 `docs/e2e/focus-contrast/result.md` ·
계획서 `docs/plan_history_029.md` · 설계서 `docs/design_history_028.md`.
**활성 계획이 없다 — 다음 반복은 계획 phase(탐색)다.**

## 방금 것 (2026-09-01 · e2e 1/1)

**계획 완료 기준 여섯 줄을 그대로 실행했고 전부 통과다.** 전부 맨몸(파이프 0회).

- 단위 **473건 OK 11.628초** · 린터·타입체커 없음 · **CI 없음**(`.github` 부재라
  `rules/e2e.md` 1절 2항 해당 없음)
- `design_check.py` **rc 0** — `[3]` 절 **16행**, 그중 비텍스트 2행:
  라이트 `--focus` on `--bg-page` **3.56:1 / 기준 3.0 OK** · 다크 **11.14:1**
- 회귀 **`e2e/*.py` 19종 전수 rc 0**
- 품질·성능 축 무변: ko **20/20 (100%)** · en **19/20 (95%)** · 매치 평균 14.0(최소 11) ·
  p95 **8.85ms**(예산 300ms 의 2.9%) · [열림] 10.24/s · [차단] 10.23/s
- `data/crawl.db` sha256 **무변경**(`85c96744…5bda18`) · 제품 diff 는 CSS 색값 1개 그대로

**새 e2e 0개의 근거를 진짜 응답 바이트로 확인했다.** e2e 가 사는 네 자리 중 이 계획이
걸리는 것은 "실제 응답 바이트" 하나인데 `design_check.py` 가 이미 로컬 서버(포트 0)를
세워 그 자리를 덮는다 — 만들면 19종이 20종이 될 뿐이다(계획 41·42 와 같은 판정).
계획 7절의 반증 신호 둘(맞는 색이 없다 / M3 이 M1 과 겹쳐도 초록)은 **둘 다 발동 안 함**.

## 사람이 정할 것 ① — 검사기가 링의 *존재*는 안 본다 (형제 구멍, 실측)

e2e 탐침이 새로 쟀다. 응답 CSS 에서 포커스 링 규칙(`:focus-visible{outline:3px solid
var(--focus)}`)을 **통째로 지운 뒤** `check_contrast` 를 돌리면 **기준 위반 0건 · 측정
불능 0건**이다. 토큰은 여전히 선언돼 있어 검사기는 3.56:1 을 찍고 rc 0 을 낸다 —
**아무에게도 안 보이는 링을 초록으로 재는 자리**다.

계획 13 이 닫은 것은 *어느 맵을 보나*, 계획 43 이 닫은 것은 *어느 이름을 보나*,
이것은 *그 토큰을 쓰는 규칙이 살아 있나* 다. **같은 계보의 다음 칸**이고 처방은
검사 한 줄(응답 CSS 에 `var(--focus)` 를 쓰는 규칙이 있나)로 끝나며 새 의존성이 없다.
**개발 스텝을 하나 여는 일이라 승인 필요.** `digest ## 다음 계획 후보` 에 적었다.

## 사람이 정할 것 ② — 앞 반복이 넘긴 셋 (그대로 열려 있다)

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1, 입력창의 유일한 경계). `NO_PAIR`
   사유가 사실대로 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1** — `outline-offset:2px` 의 흰 틈 1겹이
   유일한 구분이다. 나머지 세 변이 3.56:1 이라 링은 보이므로 급하지 않다.
3. **기록 3파일 불일치가 5회째 재발** — 처방은 `tests/test_docs.py` 에 검사 한 줄
   (`metrics.md ## 누적` 의 반복 수와 `status.md` 의 `iteration` 이 어긋나면 죽는다).
   정규식 3줄이면 되고 새 의존성이 없다. **개발 스텝을 여는 일이라 승인 필요.**

## 닫힌 판단 — 원격 갈라짐 (2026-09-01 · `83d3f91`)

`de12184`(웹 UI 의 *Update branch*, 계정 `ohjungs`)가 들여온 630줄이 **한 줄도 빠짐없이
중복**이라 `merge -s ours` 로 이력은 남기고 트리만 `f066e70` 으로 고정했다. 원격은
fast-forward 로 받았고 force-push 는 안 했다. **교훈**: `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 *Update branch* 는 머지가 아니라 복제다(PR #3 을 깨뜨린 것과 같은 원인).

**남은 것 (순서대로 · 사람 결정)**

1. **PR #4 병합** — https://github.com/ohjungs/o-search/pull/4 에서 **"Create merge commit"**.
   squash·rebase 를 고르면 해시가 또 새로 쓰여 이 사고가 세 번째로 난다.
2. 그 다음 **계획 42 체리픽 PR → 계획 43 체리픽 PR.** 둘 다 `origin/main` 기점이 아니라
   지금 그대로 PR 을 열면 PR #3 과 같은 사고가 난다. **이 반복도 PR 을 안 열었다.**
3. 병합 전까지 **웹 UI 의 *Update branch* 를 누르지 않는다.**

## 한도 (매 반복 확인)

- `main` 직접 커밋 금지 · `--no-verify` 금지 · 외부 네트워크 금지 ·
  `docs/specs/` 읽기만 · `data/crawl.db` 무변경(sha256 `85c96744…5bda18` 확인) ·
  의존성 추가 금지(stdlib 만 — 이 반복도 stdlib 만 썼다).
- **검증은 맨몸으로. 15회에서 멈춰 있고 이 반복도 러너 파이프 0회 — 두 반복 연속이다.**
  러너 **19종 + 단위**를 돌리는 동안 파이프도 리다이렉션도 안 붙였고, e2e 반복은 러너를
  가장 많이 부르는 자리라 표본이 가장 크다. **한 반복만 더 0회면 항목을 접는다.**
  ※ 문서를 잘라 읽는 데는 `grep`·`head`·`tail` 을 썼다 — 항목의 정의는 *"러너를 파이프로
  감싼다"* 이고 러너가 아닌 것에는 판정 줄도 종료 코드도 없다. 세지 않되 적어는 둔다.
- `docs/digest.md` 200줄 상한 · 열린 항목 **49**(위 ① 을 후보로 더했다) ·
  `history_current.md` 300줄 상한 — **회전을 자기 항목 넣기 전에 돌렸다**(293 → 70줄,
  계획 43 의 앞 여섯 반복이 `history_020.md`). 지금 **99줄**로 여유가 넉넉하다.
