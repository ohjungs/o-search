---
signal: GREEN
phase: 개발
step: 9
attempt: 0
iteration: 268
updated: 2026-09-02
ctx: 46
night_iterations: 112
night_red: 1
night_retries: 0
plan: passage-api
---
# 현재 상태

**리뷰가 계획 48 을 다섯 번째로 반려한다 — 갈림길 6 은 절반만 닫혔고, 절반은 새로 열렸다.**
활성 계획 `passage-api` · 설계서 `docs/design_passage-api.md` · 브랜치 `loop/passage-api` ·
기점 `6e99031`. **단위 569건 전부 초록**(맨몸 단독 실행 확인, 13.4초) · 제품 `src/` **0줄**
(리뷰는 설계서·기록만 고쳤다). 새 파일 0 · 의존성 0 · 스키마·마이그레이션 없음 ·
`docs/specs/`·`data/crawl.db` 무변경 · `.mutation-lock` **0**.

## 이번 반복이 한 일 (리뷰 5)

**백지 패스를 먼저 끝냈다.** 계획서·설계서를 열기 전에 diff 와 고친 파일만 봤다. 거기서
«같은 성질의 형제가 더 있나» 하나만 물었고 그것이 아래 둘을 냈다. 둘 다 탐침으로 재현했다 —
**이번 리뷰의 근거는 스위트가 아니라 실측이다. 569건은 둘 다에 눈이 멀어 있다.**

**[R5-1] high — 안 2 는 `<br>` 한 태그만 뺐다. 설계가 적은 «블록 태그로 좁히기» 가 아니다.**
`_separate()` 를 부르면서 문단 경계가 **아닌** 태그는 `<br>` 말고도 있다 — `_INLINE_TAGS` 에서
빠진 phrasing 태그 전부와 `_SKIP_TAGS` 셋이다. 일곱 모양 중 **여섯이 여전히 한 문단을 조각낸다**:
`<script>`·`<style>`(문단 안 광고·트래킹) · `<img>` · `<del>`/`<ins>`(넷으로 갈린다) ·
`<label>` · `<font>`. **개발 8 이 «고쳤다» 고 적은 실패가 `<br>` 을 `<script>` 로 바꾸면
그대로 재현된다** — 내비 링크가 조각난 본문을 이겨 근거로 나온다. 실물 HTML 에서 문단 안
`<img>`·인라인 `<script>` 는 `<br>` 보다 흔하다.

**이 축은 재색인 없이 닫힌다.** 설계서가 미룬 사유로 적은 *"`_SKIP_TAGS` 는 색인 경로와
공유라 손대면 재색인"* 은 **`_INLINE_TAGS` 로 옮길 때만 참이고 `block` 인자로는 거짓이다** —
`_TextParser._separate` 가 인자를 문법적으로 안 읽는 것이 개발 8 이 4,128건으로 실측한
바로 그 성질이라, 넘기는 집합을 넓혀도 `extract_text` 는 한 글자도 안 바뀐다. 일곱 모양
전부 불변식 `" ".join(extract_blocks(h)) == extract_text(h)[1]` 을 **7/7 유지**한다(실측).

**[R5-2] high 회귀 — 안 1 이 새 오답을 만들었다. 재확인 3 이 물은 그 자리다.**
본문 **뒤에** 오는 긴 푸터가 동점일 때 본문을 이긴다. 같은 입력이 직전 커밋 `2e39590` 에서는
**맞았고** `9ed460a` 에서 **틀려졌다**(기점 대조 실측). 사이드바가 **앞에** 오는 모양은
양쪽 다 틀리고, 점수가 갈리는 모양은 양쪽 다 맞는다 — 즉 안 1 은 이득이 아니라 **맞바꿈**이다.

**원인은 손잡이가 아니라 자다.** `_SHAPES` 여덟 행은 보일러플레이트를 **전부 본문 앞에** 둔다.
「길이」가 이득이 되는 방향만 표에 있으니 안 1 이 8/8 을 받았다. *"자가 안을 골랐다"* 는
*"자가 그 안 쪽으로 눈금이 있었다"* 이기도 하다. **개발 8 의 규율 자체는 옳다** — 자를 먼저
만든 것이 이 반복이 결함을 잡은 이유이기도 하다. 틀린 것은 규율이 아니라 **표의 대칭성**이다.

## 넘긴 것 — 개발 9 가 닫을 것은 이 둘뿐이다

1. **[R5-1]** `br` 리터럴 자리를 «문단 경계가 아닌 태그» 집합으로. 기제는 개발 8 이
   이미 만든 `block` 인자 그대로다. 색인 경로 불변을 같은 방식으로 다시 실측한다.
2. **[R5-2]** `_SHAPES` 에 **방향을 짝지은 행**을 심는다 — 보일러플레이트가 본문 **뒤에**
   오는 모양. RED 를 눈으로 본 뒤 동점 규칙을 고른다(안 1 유지·철회·다른 술어 셋 다 열려 있다).

**범위를 여기서 못박는다.** 인라인 연접(`김치찌개된장찌개`) · 갈림길 5 의 캡 ·
`_SKIP_TAGS` 를 색인 경로에서 빼는 것은 **계획 48 에서 다시 열지 않는다**(`digest` 후보).
**리뷰 6 은 위 둘의 재측만 한다** — 새 축을 열지 않는다.

## 계획 48 을 끝낼 수 있나 — **끝낼 수 있다**

저장소에서 가장 긴 계획이지만(개발 8 · 반려 5 · RED 1) **미궁이 아니다.** 세 스텝은 전부
붙어 있고(`indexer.passages` · `serve` 의 `/passages` 갈래 · `e2e/passage_eval.py`),
사양의 두 숫자는 이미 통과한다. 다섯 반려는 전부 **같은 한 곳**으로 좁혀졌다 — 「블록이
문단인가」. 이번 반려도 새 축이 아니라 **리뷰 4 가 연 그 축의 나머지 절반**이다.
개발 9 가 위 둘을 닫으면 **e2e 20종 전수로 보내 DONE 으로 닫는다.**

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-02 확인)

`origin/main` 은 PR #6 으로 계획 44~47 까지 받았다. 계획 48 의 커밋들은 브랜치에만 있다.
**이번 반복도 `origin/loop/passage-api` 로 푸시했다** — 푸시는 루프의 정상 절차다
(강제 푸시만 금지).
**남은 것 (사람 결정)**: 계획 48 이 끝난 뒤 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 — 다섯이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, `~/.claude/skills/loop-harness/rules/docs.md` 에는 그 예외가 없다.
   **저장소 밖이라 루프가 못 고친다.**
4. **«러너 호출을 다른 것과 섞지 않는다» 를 문장이 아니라 훅으로 옮기는 일.**
   재발 **22회 — 늘지 않았다. 이번 반복(리뷰 5)의 러너 판정 호출은 한 번이고 맨몸·단독이다**
   (`PYTHONPATH=src python3 -m unittest discover tests` → 569건 OK, 13.4초. 탐침 스크립트는
   판정 러너가 아니라 별도로 돌렸다). 직전 반복(개발 8)이 하나에서 셋을 밟았다
   (`design_check` 를 `> /dev/null` 로 ·
   단위 러너에 `-k` 를 붙여 스위트를 걸러 · 변이 아래에서 `2>/dev/null` — 셋 다 즉시 맨몸
   재실행해 판정 줄을 읽었다). **마지막 것은 `digest` 의 ⑮ 와 형태가 글자 그대로 같고,
   그 항목을 읽은 같은 반복 안에서 났다.** 직전 반복은 하나였고 그 전엔 열 번 연속 0 이었다:
   **연속 기록은 문장이 훅을 대신한다는 증거가 아니다.**
   방아쇠는 세 번 다 «잡음 줄이기» 였다 — 형태만 바뀐다(파이프 → 재지향 → 스위트 거르기). 저장소 쪽 자리(`docs/project.md ## 명령`)에 이미 적혀 있는데도 났고,
   남은 자리는 `~/.claude/skills/loop-harness/rules/` 라 **저장소 밖이라 루프가 못 고친다.**
   0 으로 끝낸 반복이 쌓이는 것이 항목을 닫지는 않는다 — 닫으려면
   **판정 명령을 단독 실행으로 강제하는 PreToolUse 검사**가 필요하다.

5. **`history_current.md` 를 회전으로는 더 이상 300줄 아래로 못 내린다.**
   개발 7 의 회전(→ `docs/history_041.md`)이 상한을 못 맞춘 첫 사례였고 **이번이 세 번째다**
   (309 → 372 → **418줄** — 두 반복이 append 한 만큼 그대로 늘었다). 343 → 309 로 내린 것이 그 회전이고,
   **항목을 하나도 안 남기고 다 밀어도** 머리의 회전 명부가 **233줄**이라 안 내려간다.
   스물한 번의 회전 서술이 그 자리에 쌓였고 내용은 `digest.md ## 완료` 의 회전 줄과 겹치는데,
   **검사(`ArchiveIndexTest`)가 강제하는 명부는 `digest.md` 의 `## 완료` 절 «아카이브 명부» 줄 하나뿐**이다.
   그 파일은 룰이 **append 전용**이라 루프가 못 줄인다 — 줄이려면 사람이 룰을 고치거나
   («명부는 `digest.md` 가 갖고 `history_current.md` 는 항목만 남긴다») 계획으로 열어야 한다.

**사양이 사람에게 남긴 것 셋** (`docs/specs/concept.md` 의 `## 사람이 정할 것`):
속도 제한 시점(가정 — IP 당 분당 60회) · JSON 검색과 근거 문단의 분할(가정 — 쪼갠다,
**계획 46 이 그 가정을 따랐고 닫았다**) · 사양 숫자들이 초안이라는 것(90% · 500ms ·
60회/분). 첫째와 셋째는 아직 열려 있다.
