# 반복 기록 아카이브 019 — 계획 42 `docs-citation-guard` 전체 (반복 209~214)

<!-- 수정·삭제 금지. history_current.md 에서 밀려난 것. 요약은 digest.md 에 있다. -->

## 반복 209 — 계획 42 `docs-citation-guard` 계획 1/1 (2026-09-01)

- 한 일: 계획 41 DONE 뒤 **새 계획을 탐색해서 열었다**(`discover.md` 1~8순위를 순서대로).
  브랜치 `loop/docs-citation-guard` 를 `53332e0` 기점으로 팠고 계획서
  `docs/plan_docs-citation-guard.md` 를 썼다. 코드 **0줄**.
- 결과: **1~5순위 전부 0건 · 6순위 1건 · 8순위 0건.** 단위 **466건 OK 11.934초**(맨몸,
  파이프 0) · 린터는 저장소에 없다(ruff·flake8·mypy·pyflakes 미설치) · `TODO`/`FIXME`/`HACK`
  `src`·`tests`·`e2e` 전수 **0** · `docs/candidates.md` 없음 · `gh issue list` 비어 있음 ·
  `crawl.db` sha256 무변경 · `digest` 49항목 200줄 · `history_current` 201줄(회전 미달).
- 배운 것 ①: **계획 41 과 같은 자리로 돌아왔다 — 탐색 막힘 신호다.** 1~4순위는 전부 "기계가
  만드는 신호"인데 이 저장소에는 그 기계가 없다(린터 없음 · 큐 없음 · `TODO` 안 쓰는 관례 ·
  스위트는 항상 초록). 남은 우물이 `digest.md` 하나이고 그 49항목 대부분은 게이트가 닫혀 있다
  (승인 대기 · 도달 불가 · "실물에서 보이면" · "다른 계획을 열 때 함께"). **루프가 고칠 것이
  아니라 사람이 볼 신호**라 계획서 2절과 `status.md` 에 적어만 뒀다.
- 배운 것 ②: **7순위 파이프 8회는 저장소 안에 고칠 몫이 없다.** digest 가 적어 둔 처방
  (`make test`·별칭으로 러너 호출을 굳힌다)이 사다리 1번에서 멈춘다 — 검증을 세는 기제가
  저장소 밖이라 감싼 명령을 러너로 인정하는지 알 수 없고(인정 못 하면 **새는 자리가 하나 는다**),
  `make test | tail -4` 도 똑같이 되며, 여덟 방아쇠가 매번 다른 것은 이것이 명령 조립이 아니라
  **호출자의 규율** 문제라는 뜻이다. **처방을 실행 전에 다시 잰다**(`digest [7]`)의 네 번째 적용.
- 배운 것 ③: **착수 실측이 근거를 한 칸 넓혔다.** `digest [6]` 은 줄번호 인용이 "죽는다"고
  적었는데 살아 있는 3곳 중 둘은 죽은 게 아니라 **남의 항목을 가리킨다**(가리킨 줄은
  오늘 `[7]`, 가리키려던 `[6]` 은 `## 판단 필요` 로 내려갔다). 그리고 계획 38 리뷰가 "살아 있는
  문서 3곳을 고쳤다"고 적은 **그 조사가 전수가 아니었다** — `index.md` 22·357 과 `digest.md` 80
  이 남아 있었다. 이 계획이 검사를 심는 이유가 정확히 그것이다.
- 다음: 개발 1/1 — `tests/test_docs.py` 에 검사 한 건(RED 3건 확인) → 세 줄을 이름 인용으로
  교정 → **467건 OK** → 변이 M1~M3.

## 반복 210 — 개발 1/1 `docs-citation-guard` (2026-09-01)

- **RED 는 3건이 아니라 13건이었다.** 계획서가 센 3곳(`index.md` 22·358 · `digest.md` 80) 외에
  **반복 209 의 계획 phase 자신이 10곳을 새로 만들었다**(계획서 6 · `status.md` 2 · `index.md` 1 ·
  `history_current.md` 1). 손으로 센 전수가 다음 편집까지만 참이라는 이 계획의 논지를 **계획서가 직접 증명했다.**
- 진짜 인용 3줄은 이름 인용으로(둘은 `[7]` 을 가리키고 있었다), 형태를 말하는 10곳은
  `digest.md:<156>` 자리표시자로. `digest.md` **200줄·49항목 무변**. `README.md` 건수 한 줄은
  기존 `test_readme` 가 강제해 함께 고쳤다(계획서 5절 밖 파일, YELLOW 아님).
- 변이 M1~M3 전부 죽음. M2 는 아카이브가 5개가 아니라 **8개 파일 27줄**임을 드러냈고,
  M3 은 자리표시자 표기 15줄을 잡아 숫자 요구가 형태를 좁힌다는 증거가 됐다. 단위 **467건 OK**.


## 2026-09-01 13:55 | docs-citation-guard | 테스트 1/1 | 시도0

- 한 일: ① 전체를 먼저 맨몸으로 돌려 기준선(467건 OK)을 잡았다. ② 갭 탐색 6카테고리.
  8점 이상 **둘**을 `tests/test_docs.py` 한 파일에서 메웠다(**+10줄**, 검사 메서드는 안 늘려
  건수 467 무변 — 사다리 2번, 기존 `CITATION` 을 넓혔다). ③ RED 11건을 눈으로 본 뒤 교정.
  ④ 변이 둘. 제품 `src/` **0줄**, `README.md` **0줄**.
- 결과: 단위 **467건 OK 12.002초**(맨몸, 파이프 0) · `data/crawl.db` sha256 무변경
  (`85c96744…5bda18`) · `digest.md` **200줄 · 49항목**(한 글자도 안 건드렸다) ·
  고친 문서 4개(`plan_docs-citation-guard` 6줄 · `status` 2 · `index` 1 · `history_current` 1).
  변이 **M6**(계획서 31행을 `N행` 꼴로 되돌림) → 새로 넓힌 가지만 사망 ·
  **M7**(`glob("*.md")`→`"*.mdx"`) → 새 가드만 사망, 같은 실행에서 `DocHeadTest ... ok`.
- 배운 것:
  ① **금지 형태를 좁게 적으면 그 정규식이 탈출구의 설계도가 된다.** 개발이 심은 검사는
  `` `digest.md:<줄번호>` `` 만 봤고, 더 자주 쓰는 `` `digest.md` <줄번호>행 `` 은 통과했다.
  그래서 커밋 `8546f3d` 이 반복 209 항목의 인용을 **콜론 꼴 → 한국어 꼴로 옮겨 적고 "고쳤다"**
  로 셌다 — 가리키는 줄은 여전히 `digest` 의 `[7]` 이라 오인용이 살아 있었다. **탈출까지
  한 커밋 걸렸고 탈출한 사람이 저자 자신이다.** 210 이 "계획 phase 가 10곳을 만들었다" 를
  본 것과 같은 자리인데, 이번엔 *검사를 심은 손*이 검사를 피해 갔다.
  ② **형제 테스트가 이미 막아 둔 함정이 옆 테스트에는 뚫려 있었다.** `DocHeadTest` 는
  "경로를 잘못 잡으면 빈 문자열 위에서 조용히 통과한다" 를 주석까지 달아 막았는데
  `DocCitationTest` 는 "훑은 것 0건" 과 "위반 0건" 을 구분하지 못했다. M7 이 그 차이를 실물로
  보였다 — 머리 검사는 여전히 `ok` 였다. **가드는 파일 단위로 복사되지 않는다.**
  ③ **공백 한 칸이 참·거짓을 갈랐다.** `` `index.md` 41 행 `` 은 줄 41 이 아니라 **계획 41 의
  행**이다(index.md 의 41번째 줄은 `plan_search-api` 서술이다). `행` 앞 공백을 안 받는 쪽으로
  좁혀 그 오탐을 뺐고, 덕분에 얼린 기록 한 줄을 안 고쳤다.
  ④ **마감 문서를 쓰는 손도 똑같이 걸렸다.** ①을 서술하려고 금지 표기를 예시로 적었더니
  최종 실행이 `status.md` 4줄과 이 항목 1줄을 잡았다(자리표시자로 바꿔 초록). 계획 phase 10곳 ·
  개발 phase · 테스트 phase — **세 phase 연속으로 그 phase 자신의 산문이 위반을 만들었다.**
  검사가 없었으면 세 번 다 아무도 못 봤다. 이것이 "손으로 센 전수는 다음 편집까지만 참" 의
  세 번째 실물이다.
- **넘긴 것 — 검사와 `append 전용. 수정·삭제 금지.` 가 충돌한다.** 초록으로 만들려면
  `history_current.md` 를 고쳐야 하고 개발과 이번 스텝이 각각 한 줄씩 고쳤다. 회전이 그 항목을
  아카이브로 밀면 제외 규칙에 걸려 못 고치므로 **지금은 창이 열려 있어서 안 보일 뿐**이다.
  `status.md` `## 판단 필요` 에 두 선택지와 함께 적었다. `digest` 항목은 안 열었다(49 유지).
- **파이프 없음.** 러너를 세 번 다 맨몸으로 불렀다(기준선 · 변이 둘 · 최종). `## 반복 실패` 는
  8회에서 안 늘었다.
- 다음: 리뷰 1/1 — 넓힌 경계의 방어 가능성, 위 룰 충돌, 계획서에서 줄번호를 지운 것이 기록
  손실인지.


## 2026-09-01 14:10 | docs-citation-guard | 리뷰 1/1 | 시도0

- 한 일: 백지 패스를 먼저 회수하고 패스 B 를 열었다. 백지에서 **축소 변이 4종**과
  **표기 10종**을 실측했고, 패스 B 에서 계획서 5절·7절과 앞 스텝이 넘긴 룰 충돌을 판정했다.
  후보 **7건** → 버림 2 · 보고 **5건** · 제품 동작 지적 **0건** · 자동 수정 **1**.
- 결과: 단위 **467건 OK 11.952초**(맨몸) · `data/crawl.db` sha256 무변경 ·
  `digest.md` **200줄 · 49항목** · `e2e/*.py` 19종 · `src/` **0줄**.
  고친 것은 `tests/test_docs.py` 독스트링 한 문단(파일이 두 검사를 갖게 됐는데 머리는
  아직 하나만 설명하고 있었다)과 `digest` `[6]` 인계 한 문장(항목 안에서 압축, 49 유지).
- 배운 것:
  ① **린트형 검사는 데이터가 초록일 때 자기 자신을 못 잰다.** 이 스텝의 산출물은
  "정규식을 넓힌 것" 인데, 정규식을 **콜론 꼴로 되돌리는 변이가 초록으로 산다** —
  문서를 다 고쳐 놓았으니 hits 는 어느 쪽이든 0 이다. 축소 변이 넷이 전부 생존했다.
  M6·M7 이 무언가를 죽인 것은 맞지만 **둘 다 데이터와 glob 을 건드렸지 정규식을
  안 건드렸다.** 넓어지는 변이는 잡히고 좁아지는 변이는 안 잡히는 **비대칭**이고,
  하필 이 스텝이 한 일이 "넓히기" 라 스텝 자신이 무방비다.
  ② **네 번째 탈출구는 표였다.** 정규식이 이름과 숫자의 인접을 요구해 마크다운 표
  칸 구분자 하나로 빠져나간다 — 계획서 `## 5. 건드릴 파일` 표의 두 행이 지금 그렇게
  통과 중이다. **검사가 지키려는 문서가 하필 통째로 표다.** 계획 phase(10곳) ·
  개발 phase · 테스트 phase 에 이어 **네 번째로 그 phase 자신의 산문이 위반을 만들었고**,
  이번엔 검사가 못 잡아서 리뷰까지 살아 왔다.
  ③ **그 인용은 이 계획이 도는 동안 이미 썩었다.** 계획서 표는 357 을 가리키는데
  `index` 가 한 줄 늘어 목표가 358 로 밀렸고 기록은 이미 358 로 적고 있다 —
  **한 문서 안에서 같은 인용이 두 값으로 갈렸다.** 계획서가 자기 논지를 세 번째로 증명했다.
  ④ **넓히지 않는 것도 재서 정한다.** `L12`·`#L12`·`(12)`·`line 12`·`라인 12` 는
  아카이브 62개를 포함한 **문서 77개 전수에서 0건**이다 — 211 반복 동안 안 쓴 표기라
  가지를 늘릴 근거가 없다(사다리 1번). 반대로 분리자 완화는 오탐 **0**으로 위반 2건을
  잡는다. **"넓힐까" 를 감으로 답하면 둘 다 틀린다.**
  ⑤ **앞 스텝이 넘긴 룰 충돌은 충돌이 아니었다.** 회전이 항목을 밀면 파일 이름이
  아카이브 꼴이 되어 제외 규칙에 걸리므로, 검사가 얼린 기록을 고치라고 요구하는
  상황은 생기지 않는다. 대가는 시간 창 하나 — 위반이 회전까지 살아남으면 아카이브에
  영구 보존되고 검사는 초록이 된다.
- **넘긴 것**: [A1] 자체 검사 한 건 · [A2] 분리자 완화 한 줄. 둘 다 `digest` `[6]` 에
  적었다. **DONE 에서 `[6]` 을 "닫혔다" 로 적으면 거짓이 된다.**
- **파이프를 저질렀다 — `## 반복 실패` 가 8 → 9회.** 본 검증 둘은 맨몸이었는데
  **마감 직전 "숫자만 다시 보자" 는 재확인 실행**에 `| tail -5` 를 붙였다. 맨몸 재실행으로
  값을 다시 받았다(467 OK 11.939초). ③과 같은 자리다 — **조항을 이 반복이 한도에
  적어 넣고서 그랬다.** ⑨로 **재확인이 두 번째로 잦은 방아쇠가 됐다(⑥⑨)**: 실패를
  의심할 때가 아니라 **이미 초록인 것을 다시 볼 때** 손이 짧은 출력을 원한다.
  조항은 그 순간을 못 막는다 — 처방은 호출을 한 줄로 굳혀 조립할 여지를 없애는 쪽이다.
- **이 반복은 커밋이 둘이다.** 마감 중 자동 스냅샷(`e5d9c0e`)이 편집 도중 상태를 잡아
  **원격까지 밀어** 산출물이 스냅샷과 리뷰 커밋에 갈렸다. `## 반복 실패` 의 스냅샷 항목은
  *제자리 변이*를 세는 자리라 이번 건(정상 작업 중간 상태)은 **안 세었다.** 되감지 않은
  이유는 이미 푸시됐고 다음 스텝이 읽는 것은 git 이력이 아니라 `docs/` 이기 때문이다 —
  **트리 최종 상태는 맞다.**
- 다음: e2e 1/1 — 7절 반증 신호 둘 실측 · 회귀 19종 rc 0 · `[6]` 을 닫지 않는 마감.

## 2026-09-01 14:30 | docs-citation-guard | 개발 2/2 | 시도0

- **phase 를 되돌렸다 (e2e → 개발).** 앞 반복이 `phase: e2e` 로 넘겼지만 **리뷰 판정이
  "조건부"** 였고 중요도 9·8 이 열린 채였다. [A2] 는 **저장소에 살아 있는 위반**이라,
  고치지 않고 e2e 로 가면 계획 42 가 "오인용을 막았다" 고 주장하면서 **자기 계획서에
  담은 오인용**을 통과시키게 된다. 조건부 판정은 문서로 받는 게 아니라 phase 로 받는다.
- 한 일: TDD 로 `tests/test_docs.py` 에 `CitationPatternTest` 를 먼저 넣고(금지 4꼴 ·
  허용 4꼴을 **코드 안 리터럴 표**로 단언) RED 를 본 뒤 정규식의 분리자를 완화했고
  (`` `?[^0-9]{0,3} ``), 드러난 위반 2건을 이름 인용으로 고쳤다. 계획서 3절에 스텝 2 를,
  5절 표에 `README.md` 와 계획서 자신을 적었다.
- RED: **FAIL 2건.** ① `test_pattern_catches_line_number_citations` 의 표 꼴 subTest —
  `Regex didn't match: '(?:digest\.md|index\.md|history_current\.md)(?::[0-9]|`? ?[0-9]+행)'`
  ② `test_readme` 의 `(467, 19) != (469, 19)`. **허용 표(오탐)는 처음부터 초록**이라
  완화가 넓히기만 하고 새 오탐을 안 만든 것도 같은 실행이 보여 줬다.
- 결과: 단위 **469건 OK 11.878초**(맨몸) · `data/crawl.db` sha256 **무변경**
  (`85c96744…5bda18`) · `digest.md` **200줄 · 49항목** · `src/` **0줄**.
  교정한 2건은 둘 다 계획서 `## 5. 건드릴 파일` 표 — `index.md` 행(`22행 · 357행` →
  `plan_clock-injection` 행 · 계획 33 서술)과 `digest.md` 행(`80행` → `## 반복 실패` ③).
  **357 은 이미 썩어 있었다**(목표는 358 로 밀렸고 기록은 358 로 적고 있었다).
- 변이 (`.git` 없는 스크래치패드 사본, 하나씩 심고 **되돌린 뒤** 다음을 심었다):
  **M8**(정규식을 스텝 1 의 콜론 꼴로) **사망 — subTest 3건** · **M9**(분리자 완화
  되돌리기) **사망 — 표 꼴 1건만**, 예측과 정확히 같다 · **M10**(`APPEND_TARGETS` 에서
  이름 하나 제거) **사망 — 1건**. 리뷰가 "전부 초록으로 산다" 고 잰 축소 변이 4종이
  이제 전부 죽는다. 사본은 삭제했다.
- 배운 것: **M9 를 심은 실행에서 `DocCitationTest` 는 초록이었다.** 검사를 좁히는 변이가
  들어와도 문서 쪽 검사는 아무 말이 없다 — 리뷰의 일반화("린트형 검사는 데이터가 초록일
  때 자기 자신을 못 잰다")를 변이가 **한 실행 안에서 재현했다.** 처방은 검사 대상을
  데이터에서 **코드 안 리터럴**로 옮기는 것이고, 그것이 `` `index.md` 41 행 `` 을 지키던
  주석 한 줄짜리 방어까지 함께 단언으로 바꿨다(허용 표).
- **파이프를 또 저질렀다 — `## 반복 실패` 가 9 → 10회.** RED 확인은 맨몸이었는데
  **GREEN 확인**에 `| tail -5` 를 붙였다(출력이 길어 꼬리만 보려고). 맨몸 재실행으로
  값을 다시 받았다(469 OK 11.878초). ⑨는 재확인, ⑩은 GREEN 확인 — **두 반복 연속으로
  "이미 초록일 것 같은 실행"** 이 방아쇠다. 앞 반복이 적은 처방(호출을 한 줄로 굳혀
  조립할 여지를 없앤다)이 아직 기제가 아니라 문장이라 또 샜다.
- 다음: 테스트 1/1 — 두 리터럴 표에 빠진 꼴 · 완화한 분리자의 전수 오탐 실측 ·
  `[6]` 은 DONE 에서만 닫는다.

## 2026-09-01 15:05 | docs-citation-guard | e2e 1/1 | 시도0

- 한 일: `rules/e2e.md` 1절 관문(전체 검증 · CI · 품질 기준)을 먼저 통과시키고,
  계획 7절의 **"새 e2e 0개(잠정)" 판정을 확정**했다. 그 다음 마감 — 회전 · `[6]` 닫기 ·
  계획서 아카이브 · `docs/e2e/docs-citation-guard/result.md`.
- **판정: 새 e2e 0개.** 근거는 실측 하나와 구조 하나다. ① 계획 기점 `53332e0` 대비
  `git diff --stat -- src/` 가 **빈 출력**이다 — 사용자가 보는 동작이 한 바이트도
  안 바뀌었다. ② 이 산출물에는 e2e 가 사는 네 자리가 하나도 없다(프로세스 경계 ·
  진짜 시계·신호 · 실제 응답 바이트 · 모듈 배선). 검사는 `unittest` 안에서만 살고
  `src/` 를 임포트조차 안 한다. **e2e 를 늘리는 것 자체는 목표가 아니다** — 만들면
  같은 계약을 두 번 재고 19종이 20종이 될 뿐이다(계획 41 과 같은 판정).
- **반증 신호 ① 발동 안 함**: 순회가 glob 이라 활성 계획서가 0개여도 안 죽고,
  존재를 요구하는 것은 append 대상 세 이름뿐이다. 예상 못 한 이름의 `.md` 는 훑는
  대상이 하나 느는 것으로 끝난다 — 운영 계약이 아니라 그냥 파일 읽기다.
- **반증 신호 ②를 가정이 아니라 실물로 쟀다 — 이 반복이 그 사건을 만들었다.**
  회전이 늦어 상한을 64줄 넘긴 상태(364줄)라 계획 41 다섯 반복(204~208)을 밀어
  `history_018.md` 를 신설했고(→ 221줄), 7절은 "실제 회전은 DONE 이후에나 일어난다"
  고 적어 뒀었다. 회전 **직후** 맨몸 재실행 **469건 OK 11.760초** — 새 이름이 제외
  정규식에 걸려 훑는 대상에서 빠졌다. **변이 M2 가 가상으로 재던 자리를 실제 회전이
  한 번 확인해 줬다.** 계획이 반증 신호를 적어 둔 값이 여기서 나왔다: 안 적었으면
  이 회전은 그냥 집안일이었지 판정의 증거가 아니었다.
- 결과: 단위 **469건 OK 11.917초**(맨몸) · `e2e/*.py` **19종 rc 0** 전수(하나씩,
  파이프 없이) · 품질 3축 무변(ko 20/20 100% · en 19/20 95% · p95 **8.83ms** ·
  [열림] 10.23/s · [차단] 10.25/s) · 디자인 4축 통과 · `data/crawl.db` sha256 무변경 ·
  `src/` **0줄**. 린터·타입체커 없음 · **CI 없음**(`.github` 부재)이라 1절 2항 해당 없음.
- **파이프 0회.** ⑩ 바로 다음 반복이라 러너 호출을 매번 한 줄로 그대로 쳤다 —
  검증 21회(단위 2 · e2e 19) 전부 맨몸이다. 기제가 아니라 규율로 막은 것이라
  다음 반복이 또 셀 자리다.
- 마감: `digest [6]` 을 닫았다(49 → **48항목**, 200줄 유지 — 완료 항목 사이 빈 줄
  하나를 접어 자리를 냈다). 닫는 근거는 "살아 있는 위반 0곳 + 재발은 스위트가 막는다"
  이고, 남은 아카이브 8곳은 수정·삭제 금지라 검사가 제외하는 것이 옳다는 것을 이번
  회전이 확인했다. 계획서는 `plan_history_028.md`(설계 없음).
- 다음: 계획 42 DONE. 브랜치를 푸시하고, **PR 은 안 연다** — 아래 `## 판단 필요`
  참조(PR #4 가 열려 있어 `origin/main` 기점이 아니다).
