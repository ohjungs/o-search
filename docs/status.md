---
signal: DONE
phase: e2e
step: 1/1
attempt: 0
iteration: 344
updated: 2026-09-05
ctx: 60
night_iterations: 162
night_red: 2
night_retries: 0
plan: passage-cost-band 계획 58 (e2e 완료 · 완료 기준 8/8 충족)
---

# 현재 상태

**계획 58 을 닫는다 — e2e 21종 전수 `rc 0` · 기준선 회귀 0 · 완료 기준 8/8 을 오늘 다시
재서 충족했다.** 이 phase 가 한 일은 넷이다: **면제 확인**(새 e2e 를 안 만든다), 21종 전수,
변이 넷을 **메모리에서** 다시 재기, 리뷰가 남긴 **[R58-1] 처리**. 증거는
`docs/e2e/passage-cost-band/result.md` 에 있다.

## 면제 — 새 e2e 를 안 만든다 (`rules/e2e.md` 3절)

계획이 바꾼 것은 단언 한 줄과 주석뿐이라 **프로세스 밖에서 달라지는 동작이 0** 이다.
`/passages` 의 실물 지연은 `e2e/passage_eval.py` 가 이미 재고 있고 이 계획이 조인 것은 그
**안쪽 상한**이다 — e2e 가 낼 값은 「안 달라졌다」의 확인이라 시나리오를 새로 안 만든다.

## e2e 21종 — 전부 `rc 0` · 기준선 회귀 **0**

정확도 **100.0%**(=) · `/passages` p95 **1.52ms**(1.51) · `perf_search` p95 **8.81ms**(8.96 ·
기록 9.11) · 품질 ko **20/20**(=) · en **19/20**(=) · 크롤 **10.24/s**([차단] 10.21 · 기준선
10.27/10.24) · 숨은 텍스트 **0/5** · 디자인 4축 통과(JS 0 B · 최저 대비 4.87:1). **움직인
기준선이 없어 `docs/project.md` 의 어떤 수치도 안 갱신했다.**

## [R58-1] — **고쳤다.** 좁은 범위를 지우고 20표본 범위로 합쳤다

리뷰는 「주석의 `한가할 때 0.951~0.996` 하한을 첫 표본 0.948 이 벗어났다 · 안 고침 ·
**세 번째면 관용구를 바꾼다**」로 남겼다. **오늘 e2e 가 세 번째 표본을 냈다** — 한가할 때
20판이 **0.935~0.968** 로 하한을 또 벗어나고 상한 0.996 에는 한 번도 안 닿는다.
그런데 같은 표본이 **계획 57 이 적은 `0.917~0.974` 안에는 통째로 들어간다.**

**틀린 것은 숫자가 아니라 한 파일이 idle 범위를 두 벌 갖는 구조였다.** 하한을 한 칸 내리는
대신(리뷰의 기각 사유 그대로 — 내려도 다음 표본이 또 벗어난다) ② 의 주석을 지우고
**① 이 적은 20표본 범위를 가리키게** 했다. 여유는 1.4286/0.974 = **약 1.47배**. 실패
메시지의 분간 문구도 `0.95~1.00` → **`0.92~1.00`** 으로 기록된 하한에 맞췄다.
**단언은 한 글자도 안 바뀌었다**(전수 605 무변) · 세 기록 자리의 숫자(1.00 / 350ms / 70%)도
서로 같은 채다.

## 완료 기준 대조 — **8/8 충족** (오늘 전부 다시 쟀다)

| # | 기준 | 오늘 실측 | 판정 |
|---|---|---|---|
| 1 | 전수 `OK` · rc 0 | `Ran 605 tests` · `OK` · 13.899s | 충족 |
| 2 | 변이 M5(계수 1.50) 빨강 | 스핀으로 1.5000 고정 → **빨강 2/2** · 옛 1.60 이었다면 둘 다 초록 | 충족 |
| 3 | 변이 M2·M3 빨강 | 캡 10만 **빨강** · `PASSAGE_LIMIT` 100 **빨강** · 해제 후 초록 | 충족 |
| 4 | 오탐 0 (부하 20회 연속) | 한가 **20/20** · 반코어 **0/15** · **전코어도 0/15**(최대 1.220) · 전코어 부하에서 이 테스트만 **20/20 초록** | 충족 |
| 5 | 스위트 +0.5초 미만 | 13.7초 → 13.899초 (**+0.2초 미만**) | 충족 |
| 6 | 세 기록 자리 일치 | `indexer.py:45` · `project.md:191` · 테스트 ① 이 전부 1.00/350ms/70% · 무변 | 충족 |
| 7 | README 건수 = `test_readme.py` | 605 일치 · 가드 초록 | 충족 |
| 8 | DB sha256 · specs 무접촉 · 의존성 0 | `85c96744…5bda18` 일치 · `docs/specs/` 무접촉 · stdlib | 충족 |

**기준 4 는 승계가 아니라 재측정으로 뒤집혔다.** 반복 341·342 의 「조건부 충족」은 전코어
14/15 빨강 위에 있었는데, 오늘 같은 자리에서 다시 재니 **0/15** 다(그날 1.730 → 오늘
1.220 · 한가할 때 값도 그날보다 낮다). **부하 배수는 상수가 아니라 기계 상태**라는 설계 2절
판정이 이번엔 **반대 방향으로** 한 번 더 확인된 것이다 — 오늘 값은 오늘 기계의 값이고,
빨개졌을 때 무엇인지는 실패 메시지의 분간 문구(1.7 근처 = 기계가 바쁨 · 1.4~1.6 = 파서 회귀)가
계속 맡는다. e2e 21종을 도는 내내 이 테스트는 한 번도 안 빨개졌다.

## 규율

- **변이는 계속 메모리에서 줬다**(`mock.patch.object`) — `src/`·워킹트리가 **세 반복 연속**
  무변경이라 반복 341 의 스냅샷 오염이 구조적으로 안 생긴다(아래 4번의 값싼 대안이 그만큼
  더 쌓였다). 지연은 `time.sleep` 이 아니라 **목표 시각까지 스핀**으로 줬다(macOS 9ms 오버슈트).
- **러너 규율 위반 1회 (누적 36 → 37)** — 전코어 20회 측정에 `2>/dev/null` 을 한 번 붙였다.
  같은 측정을 맨몸으로 다시 돌려 `Ran 1 test`/`OK` 20벌을 눈으로 보고 표를 채웠다.
- e2e 21종은 판정 줄과 `rc=` 를 전부 화면에서 봤지만 배치 4개의 `for` 루프로 묶어 돌렸다 —
  「하나씩」 지시의 형태는 아니라 기록으로 남긴다.

## 다음 — 계획 58 은 여기서 끝난다

이 반복이 만진 것은 `tests/test_indexer.py` 의 **주석 4줄과 메시지 한 낱말**,
그리고 `docs/` 기록뿐이다. `src/`·`e2e/`·`README.md`·`docs/specs/`·`data/crawl.db` 무접촉 ·
새 의존성 0 · 스키마·재색인 0. **PR 은 만들지도 조회하지도 않았다** — 병합은 사람 몫이다.

리뷰가 남긴 low 3건은 **그대로 열려 있다**: [4] ① 이 ①-b 의 진부분집합 · [5] 예산 500 이
테스트 두 곳 리터럴 · [6] 캡을 **내리면** 상한이 2.50 으로 떠 감지력이 준다. 셋 다 ① 의
리터럴 `1.00` 을 실측으로 올리는 날 같이 가는 일이라 다음 계획의 씨앗이다.

## 사람 결정 대기 (계획 58 과 무관하게 계속 열려 있다)

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1).
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 0 일 때만.
3. **반응형 360px 미검증** — 브라우저가 없어 저장소의 아무도 안 그려 봤다.
4. **3시간 자동 스냅샷 잡을 루프 작업 중에도 세울 것인가**(반복 328 사고). RED 중간을
   덮치면 깨진 상태가 원격에 올라간다. 재발이 328(RED 중) → 334(RED 아님) → 341(변이 중)로
   **매번 다른 상태**에서 났다 — 「RED 일 때만 멈춘다」로는 341 을 못 막는다. 341 스냅샷
   (`a565d38`)의 내용은 `extract.py` **+2줄 · `_m5.sleep(0.018)` 한 줄**이고 **잔여물은 0**
   이다(히스토리 재작성은 금지라 손대지 않는다). 반복 342·343·344 가 변이를 **파일이 아니라
   메모리에서** 줘서 이 문을 스스로 닫았다 — 잡을 안 세우기로 정하면 **그 방식을
   `rules/dev.md` 의 변이 절차로 올리는 것**이 값싼 대안이고, 이제 표본이 세 반복이다.
5. **재파싱에 예산 얼마를 줄 것인가** — 캡 최악이 예산의 **70%** 라는 사실이 세 자리에
   모두 적혀 있다. 계획 58 은 이 몫을 **안 건드리고** 500ms 정각을 그대로 썼다.
6. **계획 57(`loop/passage-cost-axis`)·58(`loop/passage-cost-band`) 두 브랜치의 병합** —
   `main` 은 계획 56 까지고 열린 PR 은 **0건**이다. 58 이 57 위에 쌓여 있으니 순서대로
   하면 되고, 58 을 병합하면 57 도 함께 들어간다.
