---
signal: GREEN
mode: night
plan: cooldown-burn
phase: 개발
step: 0/2
attempt: 0
iteration: 83
night_iterations: 7
night_red: 0
night_retries: 0
night_self_amendments: 0
updated: 2026-08-27 (반복 83)
ctx: 36% / 200k
rules: rules/dev.md
---

# 현재 상태

**`cooldown-burn` 설계 완료. 다음은 개발 phase 스텝 1.**
브랜치 `loop/cooldown-burn` (기점 `9bd3771`, `loop/tokenizer`).
계획 `docs/plan_cooldown-burn.md` · 설계 `docs/design_cooldown-burn.md`.
출처는 digest `## 판단 필요` `[high]`.

## 설계가 정한 것 (반복 83) — 개발은 이 계약을 지킨다

**대안 A(최소) 채택** — `Frontier.next()` 는 `_last_fetch` 를 **읽기만** 하고,
시계는 `mark_sent()` 만 건다. 예외 경로는 `mark_sent(domain, now())` 로 **보수적으로**
건다(요청이 나갔는지 모른다). `frontier.py`+`crawl.py` **한 커밋**.
버린 것: B 리스 구조(호출처 1곳뿐 — 추측성 확장) · C 취소 방식(틀린 전제를 남겨
스킵 경로가 늘면 재발) · 제출 시점(실측 0 회수) · add 시점 robots 필터(계약 4 위반).

**사용자 승인 불필요** — `design.md` 4절 넷 중 어디에도 안 걸린다(재확인).

## 설계 phase 에서 실측한 것 — 실제 코드 사본으로 (흉내 아님)

| 코드 | 차단 사이트 처리량 | 예외 주입 시 최소 발신 간격 |
|---|---|---|
| 현재 | 4.47/s | 1.010s (팝 쓰기가 덮어준다) |
| **설계 A** | **10.28/s** | **1.010s** |
| 순진(팝 쓰기만 삭제) | 10.31/s | **0.310s — 계약 위반** |

1. **008 리뷰의 경고가 옳았다.** 순진한 수정은 실제로 1초를 깬다.
2. **`exclude=busy` 가 팝→발신 창을 덮는다**(가정 1, 참). 한 도메인 6URL·워커 8 로
   굶겨도 1.010s. `crawl.py:71` 이 제출 **전에** `busy.add` 한다.
3. **차단 시나리오만으로는 순진한 수정과 올바른 수정이 구별되지 않는다** —
   둘 다 10.3/s·간격 통과. 구멍은 **예외를 주입해야** 보인다.
   → 스텝 1 완료 기준 ③과 스텝 2 e2e 시나리오 4를 빼면 이 설계가 검증되지 않는다.

**탐침에서 한 번 속았다** — `e2e/perf_crawl.py` 는 import 시점에 저장소 `src` 를
`sys.path[0]` 으로 민다. 사본을 재려면 **`websearch` 를 먼저 import** 해야 한다.
안 그러면 두 트리가 같은 숫자를 내고 그게 "회수 0" 으로 읽힌다(실제로 그렇게 읽었다).

**직전 계획 `tokenizer`(010) 는 DONE 이고 아카이브까지 끝났다** —
`plan_history_010.md` · `design_history_010.md` · `docs/e2e/tokenizer/result.md`.
`loop/tokenizer` 는 **병합하지 않았다**(야간 금지).

## 이 계획이 여는 것

`Frontier.next()` 가 팝 시점에 간격 시계를 건다(`frontier.py:70`). 팝은 요청이 아니다 —
`store.has` 로 건너뛴 URL 과 **robots 가 막은 URL** 이 **요청을 하나도 안 보내고**
그 도메인을 1초 재운다. 동시화 뒤로는 워커 풀이 덜 차는 **처리량 손실**이다.

## 계획 phase 에서 실측한 것 (반복 82)

`perf_crawl.py` 하네스를 빌리되 **robots 가 페이지 11개 중 6개를 막는 사이트**로 바꿔 쟀다.

| | 처리량 | 요청 없이 태운 쿨다운 | 페이지 최소 간격 |
|---|---|---|---|
| **현재** | **4.48/s** | 팝 120회 중 **72회** | 1.003s |
| 팝 쓰기 제거 + **제출 시점** `mark_sent` | 4.49/s | 0회 | 1.005s |
| 팝 쓰기 제거 + **실제 발신 시각만** | **10.25/s** | 0회 | 1.007s |

**둘을 알아냈다.**

1. **현재 값이 컨셉 성능 2(초당 5문서)를 밑돈다** — 막는 것이 있는 사이트에서 4.48/s.
   `perf_crawl.py` 는 아무것도 막지 않는 사이트만 재서 8일 동안 이걸 못 봤다.
   기준선 10.25/s 는 **막는 것이 없는 세계의 숫자**였다 → 스텝 2가 이 눈을 만든다.
2. **digest 가 적어 둔 답이 아무것도 회수하지 못한다** — "제출 시점 `mark_sent`" 는
   4.48 → 4.49/s. 제출 시점에는 robots 판정이 아직 안 났다(`_fetch_one` 이 워커
   **안에서** 부른다). 회수는 **실제로 나간 요청에만** 시계를 걸어야 나온다.

## 설계가 먼저 닫을 구멍 (008 리뷰의 경고 — 유효하다)

팝 쓰기를 그냥 지우면 워커가 **예외로 끝날 때** `_store_result` 의 `except` 가지가
`mark_sent` 없이 반환하는데 **요청은 이미 나갔을 수 있다** → 다음 팝이 즉시 나간다.
**순진한 수정은 RED.** 보수적 방향(실제 발신보다 늦게 잡기)은 안전하다 —
`mark_sent` 가 `max` 로 늦은 쪽으로만 움직인다.

## 넘지 않는 선 — 어기면 RED

**도메인당 페이지 요청 1.0초 하한과 robots 준수는 어떤 커밋에서도 낮추지 않는다.**
회수는 "안 보낸 요청의 쿨다운을 안 태운다" 로만 얻는다. 간격을 깎아서 얻지 않는다.
그래서 스텝 1은 `frontier.py` 와 `crawl.py` 를 **한 커밋**에 넣는다 — 쪼갠 중간 상태가
계약이 깨진 상태다.

## 설계 phase 가 받을 것

- 대안 최소 3: ① 발신 시각만 ② 제출 시점(실측으로 무효 — 근거 있음) ③ add 시점 robots 필터
  (메인 스레드에 네트워크 I/O 를 들여 동시화 계약 4를 깬다 — 이 이유로 버릴 것으로 본다)
- **사용자 승인 불필요.** `design.md` 4절 넷 중 어디에도 안 걸린다 — 프런티어는 **내부**
  계약이고 `Frontier` 호출처는 `crawl.py:66` 하나뿐(grep 실측). 데이터 형태·의존성·보안 무관
- 스텝 1 ④ 가 요구하는 것: 팝 쓰기를 지우면 `test_frontier.py` **9건**이 깨진다(실측).
  **지우지 말고 새 계약으로 다시 쓴다** — 재는 것은 그대로 두고 시계 거는 방법만 바꾼다

## 열지 않는 것 (승인 대기 — 야간 금지)

`recrawl`(`fetched_at` 스키마 변경) · `X-Robots-Tag`(스키마) · `loop/*` 브랜치 병합.
`store.has` 상태 불문 스킵도 **recrawl 계획 소관**이라 이 계획에서 안 연다
(반복 82 탐침으로 재확인: 같은 DB 재실행은 시드부터 스킵돼 **0으로 끝난다**).

## digest 후보 (이번 밤)

6. **거래를 재려면 양쪽을 같은 자로 재라** (`porter` — 반복 81, digest 에 반영함)
7. **기록된 답을 실행 전에 재라.** digest 가 8일 전에 적어 둔 `[high]` 의 수정 방안
   ("제출 시점 `mark_sent`")은 **실측하니 0을 회수했다.** 보류 항목의 *진단*은 옳아도
   *처방*은 그때의 추정이다 — 처방까지 검증된 것으로 읽으면 그대로 구현하고 아무것도
   못 고친 채 닫는다. digest 의 "영향은 이어받기 크롤 한정" 도 같은 자리에서 틀렸다
8. **측정 하네스가 재지 않는 세계가 곧 버그가 숨는 곳이다.** `perf_crawl.py` 가
   아무것도 막지 않는 사이트만 재서, robots 가 막는 순간 처리량이 컨셉 목표(5.0/s)
   밑으로 떨어지는 것을 8일 동안 못 봤다. 기준선은 **어떤 세계에서 잰 숫자인지**까지 적는다
