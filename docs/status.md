---
signal: DONE
phase: e2e
step: 4/4
attempt: 0
iteration: 428
updated: 2026-09-07
ctx: 66
night_iterations: 0
night_red: 0
night_retries: 0
plan: realweb-corpus
---

## 현재 상태

**계획 73 `realweb-corpus` 마감.** 사용자 지시(「실물 크롤 진행해」)로 열었고,
실물 코퍼스 **361문서**로 digest 후보 `[9]`·`[8]`·`[6]` 의 여는 조건을 판정했다.
**셋 다 열렸고 둘은 결론이 뒤집혔다.** 전수 **653 OK rc 0**.

## 결과 세 줄

| 후보 | 판정 |
|---|---|
| `[9]` 성능 | **뒤집힘** — `/search` 는 총 p95 의 35.9%(「대부분」 아님) · 예산의 8.7% 라 지금 문제 아님 |
| `[8]` 근거 문단 | **열림 · 진단이 좁았다** — 비-본문 58.9%, 주범은 숨은 텍스트가 아니라 **목차·내비·제목** |
| `[6]` 이름표 | **뒤집힘** — 실패의 88.4% 가 같은 문서 `<p>` 로 회수. 「지울 축」이 아니라 **「아직 안 쓴 축」** |

전문 `docs/e2e/realweb-corpus/result.md` · 계획서 7절.

## 이 계획이 남긴 것

- **코퍼스는 안 남는다** (`data/` 는 gitignore). 남는 것은 **프로필과 그것으로 잰 숫자**다.
- **`--same-site` 범위 손잡이** (`src/websearch/frontier.py`·`crawl.py`, 테스트 5건).
  기본값 무변 — 「열린 웹 검색엔진」에서 밖으로 나가는 것은 제품이지 결함이 아니다.
- **실물 크롤 예절이 실측으로 확인됐다**: 3.67 pages/s 는 4 도메인 × 1초 간격의
  이론 상한이다. **속도를 올리려면 간격이 아니라 도메인 수를 늘려야 한다.**

## 다음 계획이 받을 것

`[8]`(목차·내비가 근거 자리를 이긴다)과 `[6]`(이름표를 **쓴다**)은 **같은 뿌리**다 —
둘 다 「어떤 블록이 근거로 뽑히나」(설계서 갈림길 6)이고, 실물 숫자가 이제 있다.
`[9]` 는 예산의 8.7% 라 **닫지 않고 미룬다** — 코퍼스가 한 자릿수 배 커지는 날 다시 연다.
