---
signal: GREEN
phase: 설계
step: 1
attempt: 0
iteration: 235
updated: 2026-09-02
ctx: 55
night_iterations: 94
night_red: 0
night_retries: 0
plan: json-contract
---

# 현재 상태

**계획 46 `json-contract` 를 열었다 — 계획서까지. 코드 0줄.**
`docs/specs/concept.md` 가 2026-09-01 에 소비자를 하나 더 받았는데(사람의 브라우저
옆에 **다른 시스템**), 그 시스템이 붙을 자리인 `GET /search` 의 JSON 은 계획 9
`search-api` 이후 8일째 이미 돌고 있었다. **새 사양이 그 자리에 요구한 셋 중 둘이
오늘 거짓이다** — 응답에 버전이 없고(기능 9), "색인이 없다" 가 500 으로 나간다
(디자인 5). 셋째(기능 6 판정 — 화면과 JSON 이 같은 순서)는 구조상 참인데 **아무도
안 재고 있다**(단언 0건). 새 엔드포인트를 만들지 않고 **있는 엔드포인트를 계약으로**
만드는 계획이다. 계획서는 `docs/plan_json-contract.md`.
**다음 반복은 설계 phase** — 트리거 셋에 걸린다(5절).

## 방금 한 것 (2026-09-02 · 계획 46 수립)

탐색 1~8순위 전수. **1~4순위 착수 가능 0건**(482 OK · `compileall` 0줄 ·
`TODO`/`FIXME`/`HACK` 코드에 0건 · `docs/candidates.md` 없음). 5~7순위의 열린 항목은
넷 다 항목 자신이 조건을 적어 뒀다(락 e2e 를 여는 계획에서 · 값이 낮아 미뤘다 ·
고치지 않는 것이 답 · 지금은 도달 불가). **근거는 8순위 사양에서 나왔다.**

| # | 사양 항목 | 실측 명령 | 결과 |
|---|---|---|---|
| 1 | 디자인 5 "503 색인이 없다" | `grep -rn "503" src/` | **0건** — 코드에 503 이 없다 |
| 2 | 같은 항목 | `grep -rn "StaleIndexError" src/` | `indexer.py` 4곳 · `serve.py` **0곳** |
| 3 | 기능 9 "스키마는 버전을 갖는다" | `grep -n "version" serve.py test_serve.py` | **0건** |
| 4 | 기능 6 판정 "화면 순서 == JSON 순서" | `test_serve.py` 의 `def test_` **90건 전수** | 맞대 보는 단언 **0건** |

- **`indexer.search` 가 자기가 세운 규칙을 절반만 적용한다.** *"빈 목록을 내면 «결과
  0건» 과 구분되지 않는다 — 원인이 다르니 소리를 낸다"* 는 주석이 드리프트 갈래에만
  붙어 있고, 바로 위의 *"아직 색인 전"* 갈래는 `return []` 라 `serve` 가 그것을 200 으로
  내보낸다. 이 저장소가 네 번 닫은 *"실패가 성공으로 위장된다"* 계보(계획 21·26·29·37)와
  같은 모양이다. **다만 여기서는 200 이 옳을 수도 있어 설계 갈림길 D 로 넘겼다** —
  서버는 멀쩡하고 색인만 안 돌았다.
- **`TestMissingDb` 의 독스트링이 *"500 이 맞지만"* 이라고 적어 두고 그 값을 못박고
  있다.** 틀린 코드가 아니라 **사양이 뒤집은 판정**이다 — 고쳐야 할 자산이지 고장이 아니다.
- **넓은 `except` 는 다시 안 연다.** `digest.md ## 다음 계획 후보` 의 그 항목이 2026-08-29
  야간에 이미 쟀고 무죄였다(유출 0 · `KeyboardInterrupt` 안 삼킴 · `src` 0줄). 이번에 묻는
  것은 다른 축이다 — *"그 안에서 «색인이 없다» 만 따로 꺼내야 하나"* 이고 답은 사양이 정한다.
- **버전은 소비자가 생기기 전에만 공짜다.** 지금은 필드 하나이고, 남의 시스템이 붙은
  뒤에는 그 자체가 조용한 스키마 변경이다. 이것이 근거 문단 API(기능 7·8)보다 이 계획을
  **먼저** 여는 이유이고, 사양 `## 사람이 정할 것` 2번의 가정(*"쪼갠다 — JSON 검색이
  먼저"*)과도 같다.
- **막혀 있던 후보 하나의 길이 여기서 열린다(열지는 않는다).** `digest.md ## 판단 필요`
  의 *"`has_next` 가 두 뜻을 한 bool 로 합친다"* 항목이 **"`/search` 응답 스키마 변경이라
  사용자 확인 후보"** 로 멈춰 있다. 버전이 없으니 스키마를 못 건드리고, 스키마를 못
  건드리니 버전도 안 생기던 자리다. 계획 46 의 `## 하지 않을 것` 에 명시적으로 뒀다.
- **설계 트리거 걸림** — 공개 인터페이스 변경(500 → 503) · 필드 추가 · 대안 갈림 넷
  (버전을 어디에 두나 · 503 을 어디서 가르나 · 화면도 503 인가 · 미색인 DB 도 «없다» 인가).

## 한도 (매 반복 확인)

- `main` 직접 커밋 금지 · `--no-verify` 금지 · 외부 네트워크 금지 ·
  `docs/specs/` 읽기만 · `data/crawl.db` 무변경 · 의존성 추가 금지(stdlib 만).
- **러너 파이프 — 이번 반복 0회. 연속 카운트를 1 부터 다시 센다.** 앞 반복이
  *"이미 초록일 것 같은 실행"* 에 `| tail -60` 을 붙여 0 으로 되돌아갔던 자리다.
  이번 반복의 스위트 실행은 **맨몸 1회**이고 판정 줄을 그대로 봤다.
- 이번 반복 실측(맨몸): `PYTHONPATH=src python3 -m unittest discover -s tests`
  **482건 OK**(11.729초). 제품 `src/` **0줄** · `tests/` 0줄 · `data/crawl.db` 무변경
  (읽지도 않았다) · README 카운트 가드 무변(482 · 19종).
- 제자리 변이 0 — `.mutation-lock` 없음. 이번 반복은 계획서와 기록만 썼다.
- **집안일 — 회전 미달이다.** `history_current.md` 는 이 기록을 붙여도 상한 300 아래고
  기록 4회(상한 20)다. 아카이브 23개 그대로라 **명부 갱신 없음.**

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-01 확인)

`origin/main` 은 `494313b`(PR #5 병합). 계획 43·44·45 의 커밋들(`bc40ea9` 이후)이
아직 안 갔다. **남은 것 (사람 결정)**: 그것들을 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).
계획 46 의 기점은 `3d766be`(계획 45 DONE 커밋)이고 `main` 이 아닌 이유는 계획서 머리에
적어 뒀다 — `main` 기점은 `serve.py` 를 계획 44 이전으로 되돌리고 기록 4파일을 반복 221
로 되돌려 계획 45 가 세운 두 가드를 스스로 깬다.

## 사람이 정할 것 — 셋이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
   offset 이 0 이 되는 날 검사기가 멈추고, 짝은 그때 열릴 계획의 일이다.
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   이제 *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, 회전을 지시하는 `~/.claude/skills/loop-harness/rules/docs.md` 에는
   그 예외가 없다. **저장소 밖이라 루프가 못 고친다.** 실동작은 안전하다는 것을
   계획 45 e2e 가 실측했다(잊으면 RED · 완료 항목을 지워도 색인은 산다). 룰 문장을
   맞출지는 사람 몫.

**사양이 사람에게 남긴 것 셋은 계획 46 의 범위 밖이다** (`docs/specs/concept.md` 의
`## 사람이 정할 것`): 속도 제한 시점(가정 IP 당 분당 60회) · JSON 검색과 근거 문단의
분할(가정 쪼갠다 — 계획 46 이 그 가정을 따랐다) · 사양 숫자들이 초안이라는 것
(90% · 500ms · 60회/분). 첫째와 셋째는 아직 열려 있다.
