# 반복 기록 아카이브 022 — 계획 44 `focus-ring-presence` 전체 여섯 반복 (반복 222~227)

<!-- 수정·삭제 금지. history_current.md 에서 밀려난 것. 요약은 digest.md 에 있다. -->

## 2026-09-01 17:25 | focus-ring-presence | 계획 1/1 | 시도0

- 한 일: **계획 44 를 열었다.** 사용자가 승인 게이트를 열어 앞 반복이 *"개발 스텝을
  여는 일이라 승인 필요"* 로 닫아 둔 후보(포커스 링의 **존재**를 아무도 안 잰다)가
  풀렸다. `rules/discover.md` 1절 1~8순위를 전수로 돌고, 착수 탐침으로 후보의
  처방을 다시 쟀다(`digest` 의 *"기록된 답을 실행 전에 다시 재라"*).
  계획서 `docs/plan_focus-ring-presence.md` (`phase: 설계`).
- 결과(전부 맨몸 · 파이프 0회): 1순위 **473건 OK 11.754초**(실패 0) · 3순위 코드
  `TODO`/`FIXME`/`HACK` **0건** · 4순위 `docs/candidates.md` **파일 없음** ·
  5순위 `digest ## 보류` **0건** · 8순위 `gh issue list` **0건** ·
  6순위 열린 **21건 중 착수 가능 1건** · 7순위 `## 반복 실패` **3건 전부 판정**.
  **착수 탐침이 구멍을 다섯 배로 넓혔다** — 링을 죽이는 변이 **6종 전부**가
  기준 위반 0 · 측정 불능 0 으로 살아남는다(규칙 삭제 · `outline:none` ·
  `var(--focus)`→`var(--line)` · `:focus-visible`→`:focus` · `outline-offset`
  제거 · `outline-offset:0`). `digest` 항목은 **첫째 하나만** 쟀다. 심긴 것을
  먼저 단언했다. 제품 코드 **0줄** · `data/crawl.db` 무변경.
- **두 가지가 후보의 진술을 뒤집었다.** ① **처방이 한 칸 틀렸을 수 있다** — 항목은
  *"없으면 종료 2"* 라 적었는데 같은 파일의 형제 관용구(`check_mobile` 의
  `overflow-wrap:anywhere`)는 **종료 1** 이다(실측 `fail=1 unm=0`). 계획 41 에서
  물려받은 처방이 자리도 타입도 틀렸던 것과 같은 형태다. ② **`outline-offset` 을
  건드리는 변이는 `NONTEXT_PAIRS` 주석이 명시한 짝 선택의 근거를 무효로 만든다** —
  `status.md ## 사람이 정할 것` 의 2번(`--focus` on `--bg-button` 1.45:1)이
  가정에서 실측 대상으로 바뀐다. 그래서 **`rules/design.md` 1절 "대안이 2개 이상
  갈림" 에 걸렸고 짧은 경로가 아니다**(4-1 다섯 중 둘이 거짓).
- **기점 전제가 낡아 있었다** — 다시 재니 `origin/main` 은 `494313b`(PR #5 병합)이고
  계획 42·43 의 코드 산출물이 둘 다 `main` 에 있다. `origin/main` 의 트리는
  `bc40ea9` 의 부모와 **같은 해시**다. 그래서 기점을 `bc40ea9` 로 잡았다 —
  `main` 기점이면 반복 221 기록 넷이 되돌아가 아래 재발 항목을 여섯 번째로 만든다.
- 다음: **설계 1/1**. 갈림길 둘(종료 코드 · 무엇을 자국으로 삼나)을 세 출발점에서
  풀고, 가정 *"응답 바이트의 자국으로 '링이 보인다'를 대리할 수 있다"* 를 깬다.
  거짓이면 개발로 가지 않는다.

## 2026-09-01 17:45 | focus-ring-presence | 설계 1/1 | 시도0

- 한 일: 설계서 `docs/design_focus-ring-presence.md`. **후보 자국 3종 × 변이 11종**을
  탐침으로 갈랐다(계획의 6종에 V7 뒤 규칙 `outline:none` · V8 뒤 규칙
  `outline-width:0` · V9 같은 규칙 안 `outline-width:0` · V10 **다크 블록 안**
  `outline:none` 을 더했다). 제품 코드 **0줄**, 탐침은 저장소 밖 스크립트라
  되돌릴 것이 없다. `data/crawl.db` 무변경.
- 결과(맨몸 · 러너 파이프 0회): `PYTHONPATH=src python3 -m unittest discover tests`
  **473건 OK 11.698초** · `PYTHONPATH=src python3 e2e/design_check.py` **4축 전부 통과**.
  후보별 실측 — **A `"var(--focus)" in css`(digest 처방) 3/11** ·
  **B 규칙을 읽는다 9/11** · C 짝을 CSS 에서 파생은 판정만 맞고 메시지가 틀린다.
- **선택은 B.** A 는 문자열이 *어디에* 있는지 안 봐서 `body{color:var(--focus)}` 만
  남아도 통과하고, 못 잡는 V5·V6 이 하필 `NONTEXT_PAIRS` 주석의 전제를 깨는 변이다.
  **C 를 버린 근거는 실측이다** — V3 에서 rc 2 는 나오는데 메시지가
  `짝도 제외도 없는 색 토큰: --focus` 라 *"링이 `--line` 으로 그려진다"* 를 못 말한다.
  **판정만 맞고 진단이 틀린 검사기는 야간에 아무도 못 고친다.**
- **갈림길 A(종료 코드)는 2 다.** 형제 `check_mobile` 이 1 인 것(재확인 `fail=1 unm=0`)과
  갈리는 이유를 적었다 — 거기서는 [4]번 축의 다른 숫자가 그대로 참인데, 여기서는
  **[3]번 축이 찍은 3.56:1 자체가 무효**가 된다. **1 과 2 를 가르는 것은 고장의 크기가
  아니라 "찍은 숫자가 아직 참인가" 다.**
- **가정을 깼고 참이었다 — 그리고 조건으로 승격시켰다.** 자국이 캐스케이드를 대리하려면
  `outline` 을 정하는 규칙이 하나뿐이어야 하고(실측 1개), 그 가정을 깨는 V7·V8·V10 이
  **규칙 수 세기 하나에 셋 다** 걸린다. 주석으로 적을 것을 검사로 올렸다.
- **일부러 안 잡는 둘에 사유를 적었다**: V4(`:focus`)는 키보드에 링이 그대로 보이므로
  이 축이 재는 것이 안 깨진다 · V9 는 지금 CSS 에 0곳이고 잡으려면 한 규칙 안의 선언
  순서를 해석해야 한다. 둘 다 `ponytail:` 천장으로 남긴다.
- **곁에서 실물 하나** — `NO_PAIR["--line"]` 의 *"(다크도 1.08:1)"* 이 **어느 짝도
  아니다**. 여섯 짝 실측: 라이트 page/input 1.34 · button 3.86, 다크 page 1.37 ·
  input 1.27 · button 4.88. 사유가 계약인 파일이라 개발 스텝에서 **숫자 하나만** 고친다
  (판단 보류 문장은 안 건드린다). `status.md`·계획서의 *"라이트에서 1.08:1"* 도 같은 오기.
- 다음: **개발 1/1**. `focus_rule(css)` 약 15줄 + 호출 한 줄 + 출력 한 줄, 단언 7건.
  계약은 설계서 `## 계약` 9항. `src/websearch/serve.py` 0줄이고 `design_check.py` 는
  계속 rc 0 이어야 한다.

## 2026-09-01 17:55 | focus-ring-presence | 개발 1/1 | 시도0

- 한 일: **TDD 로 단언 7건을 먼저 쓰고 실패를 눈으로 봤다.** `tests/test_design_check.py`
  에 `RING_RULE` 상수 + `test_product_css_reports_the_ring_rule` +
  `test_ring_that_is_not_drawn_is_unmeasurable`(V1·V2·V3·V5·V6·V7 subTest).
  **RED 실측: 여섯 변이 전부 `unmeasurable` 0건**으로 통과했다 — 착수 탐침이 적은
  "여섯이 다 산다"를 이 반복에서 직접 재확인한 것이다. 그 뒤 `e2e/design_check.py` 에
  `focus_rule(css)` + `RULE_RE`·`OUTLINE_RE` 를 넣고 `check_contrast` 에서 불렀다.
- **계약 9항 전부 이행.** ① `focus_rule` 하나(`ValueError` 관용구) ② 성공하면 요약 줄 +
  `NONTEXT_PAIRS` 측정, 실패면 사유만 담고 **비텍스트 짝을 안 잰다**(텍스트 7짝 × 2맵은
  양쪽 다 잰다) ③ 상수 4개와 커버리지 강제 무변경 ④ 요약 줄
  `포커스 링 규칙 1개 · outline var(--focus) · offset 2px` ⑤ 메시지 갈래
  ⑥ `NONTEXT_PAIRS` 주석을 단언에서 포인터로 ⑦ `NO_PAIR["--line"]` 의 `1.08` →
  **`1.27`**(다크 `--line`/`--bg-input` 실측, 판단 보류 문장은 안 건드렸다)
  ⑧ 단언 7건 ⑨ `src/websearch/serve.py` **0줄**.
- **계약에 없던 것 둘을 적는다.** ⓐ `:focus` 셀렉터 조건은 설계 첫 문단의 네 조건 중
  하나인데 `## 계약` 5항의 메시지 네 갈래에 없어서 **다섯 번째 메시지**를 만들었다
  (`outline 을 정하는 규칙이 포커스용이 아니다`). ⓑ **README 의 단위 수 473→475** —
  `tests/test_readme.py` 가 강제해서 안 고치면 RED 다.
- 결과: `PYTHONPATH=src python3 -m unittest discover tests` **475건 OK 11.940초** ·
  `PYTHONPATH=src python3 e2e/design_check.py` **4축 전부 통과**(요약 줄이 찍힌다,
  `--focus` 라이트 3.56:1 · 다크 11.14:1) · `PYTHONPATH=src python3 e2e/pagination_ui_e2e.py`
  **통과**. `data/crawl.db` 무변경 · 의존성 0.
- **러너 파이프 1회 — 네 반복 연속 0회를 내가 깼다.** README 를 고친 뒤 확인이 급해
  `... discover tests 2>&1 | tail -5` 를 돌렸고, 판정 줄과 종료 코드가 잘려 **출력이
  통과처럼 보였다**(`project.md ## 명령` 이 경고한 바로 그 모양이다). 방아쇠도 그 파일이
  적어 둔 대로 *"이미 초록일 것 같은 실행"* 이었다. 즉시 맨몸으로 다시 돌려 475건 OK 를
  확인했다. **그래서 `digest` 의 그 항목을 안 접는다 — 연속 카운트는 0 으로 되돌아간다.**
- 다음: **테스트 phase**. 이 반복은 개발 1/1 로 끝이다(설계가 개발 스텝을 1개로 정했다).

## 2026-09-01 18:05 | focus-ring-presence | 테스트 1/1 | 시도0

- 한 일: **새 단언 3건**(`tests/test_design_check.py` 의 `test_ring_that_is_not_drawn_is_unmeasurable`
  subTest 6 → 9). 제품 코드·검사기 **0줄**. 갭 탐색 6개 카테고리를 훑어 나온 셋이
  전부 8점 이상이었다:
  - **V8 뒤 규칙이 폭만 0 으로 덮는다**(8) · **V10 다크 블록 안에서 덮는다**(9) —
    설계가 *"규칙 수 세기 하나에 V7·V8·V10 셋이 걸린다"* 고 적고 그 가정을 조건으로
    승격시켰는데, 개발이 붙든 것은 **V7 하나**였다(계약 8항이 여섯 변이만 열거했고
    개발은 계약을 정확히 지켰다 — 빠진 것은 계약 쪽이다).
  - **V11 셀렉터가 포커스용이 아니다**(8) — 개발이 계약 밖에서 만든 **다섯 번째 메시지**
    (앞 반복의 ⓐ)이고 단언이 **0건**이었다. 계약에 없이 태어난 가드는 계약을 훑어
    만든 단언 목록에도 안 들어온다.
- **셋 다 반사실로 붙드는 것을 확인했다**(`digest [8]` ①② 두 단계 — 변이가 심겼나 ·
  그러고 빨개지나). 사본/원숭이 패치로 각 가드를 하나씩 빼니 그 변이가 되살아난다:
  `OUTLINE_RE` 에서 `-width` 를 빼면 **V8 이 측정 불능 0건** · 규칙을 **다크 블록을
  잘라낸 CSS**(이 파일 `token_maps` 가 실제로 쓰는 관용구다)에서 세면 **V10 이
  `('--focus', '2px')` 로 통과** · `:focus` 가드를 빼면 **V11 이 요약 줄까지 찍고 통과**.
  검사기 원본은 안 건드렸다.
- **8점 미만 셋은 안 붙이고 `digest` 로 넘겼다**(`test.md` 4절): V4·V9 천장 pin(6 —
  설계서·`ponytail:` 주석·기록 세 곳에 이미 있고 네 번째 사본은 값이 없다) ·
  음수 `outline-offset`(6 — 실측 `-2px` 는 정상 거절이나 `re.match(r"[\d.]*")` 가
  빈 문자열을 내는 우연에 기댄다) · `outline:` 축약형 없이 `outline-width` 만 있는
  규칙(5).
- **단위 건수는 475 그대로다** — `subTest` 는 `testsRun` 을 안 올린다. 그래서
  `tests/test_readme.py` 가 강제하는 README 의 단위 수도 **안 건드렸다**(앞 반복이
  473→475 로 고친 값이 여전히 참이다).
- 결과(전부 맨몸 · **러너 파이프 0회**): `PYTHONPATH=src python3 -m unittest discover tests`
  **475건 OK 11.942초** · `PYTHONPATH=src python3 e2e/design_check.py` **4축 전부 통과**
  (요약 줄 `포커스 링 규칙 1개 · outline var(--focus) · offset 2px` · `--focus` 라이트
  3.56:1 · 다크 11.14:1) · `PYTHONPATH=src python3 e2e/pagination_ui_e2e.py` **통과**.
  `data/crawl.db` sha256 무변경 · 의존성 0 · `src/` 0줄.
- 집안일: `digest` 가 200줄 상한이라 새 갭 한 줄을 넣으며 **가장 오래된 완료 항목**
  (반복 70~83 → `history_003.md`, 자신이 *"건질 교훈은 아래 항목들로 이미 들어와 있다"*
  고 적어 둔 항목)을 지웠다 — 파일 머리말이 정한 그 순서다. 200 → 198줄.
- 다음: **리뷰 1/1.**

## 2026-09-01 23:20 | focus-ring-presence | 리뷰 1/1 | 시도0

**통과 (수정 1건).** 후보 6 → 버림 3(80점 미만) → 보고 3 → 고침 1 · `digest` 인계 2.
백지 패스를 먼저 회수하고 계획·설계 대조 패스를 열었다(`rules/review.md` 순서).

- **백지 패스 수율.** diff(`e2e/design_check.py` +73 · `tests/test_design_check.py` +64)
  만 보고 낸 지적 3건 중 **확증 2 · diff 밖 1**. 확증 둘은 아래 ⓐⓑ 이고 diff 밖 하나는
  docstring 출처 오기(ⓒ, 고쳤다). **제품 `src/` 0줄**은 diff 로 확인했다.
- **목표 1 — 축소 변이 여섯, 전부 죽는다.** `.mutation-lock` 을 걸고 검사기를 좁히는
  쪽으로 여섯 번 비틀었다(각각 즉시 되돌림): `OUTLINE_RE` → `r"outline\s*:"` 축약형만
  (**V8** 사망) · `RULE_RE` 앞에서 다크 블록을 벗겨 at-rule 안쪽을 못 보게(**V10** 사망) ·
  `len(rules) != 1` → `> 1`(**V7·V8·V10** 셋 사망) · `:focus` 조건 무력화(**V11**) ·
  그려지는 토큰 일치 조건 무력화(**V3**) · `offset > 0` 조건 무력화(**V5·V6**).
  **다섯 조건과 두 정규식 어느 하나도 단언 없이 사라지지 못한다** — 계획 42 리뷰가
  실측으로 남긴 *"넓어지는 변이는 잡히고 좁아지는 변이는 안 잡히는 비대칭"* 이
  이 검사기에서는 닫혔다. 이 저장소가 그 비대칭을 실제로 닫은 첫 사례다.
- **목표 2 — V4·V9 는 살아 있고 사유도 참이다. 그런데 가드가 사유보다 넓다.** (ⓐ)
  `:focus` 조건은 `":focus" in 셀렉터` 문자열이라 **`:not(:focus-visible)`** 과
  **`:focus-within`** 도 통과시킨다 — 둘 다 키보드 포커스에 링을 **안** 그리는데
  검사기는 3.56:1 을 찍는다. 설계가 V4 를 천장으로 둔 사유(*"`:focus` 도 키보드
  포커스에 링을 그린다"*)는 `:focus` 에만 참이지 부정형·조상형에는 거짓이다.
  (ⓑ) `focus_rule` 은 규칙을 세지만 **그 규칙이 어느 at-rule 안인지는 안 본다** —
  링 규칙을 다크 `@media` 블록 안으로 옮기거나 `@media print` 안에 넣으면 규칙 수는
  1 이라 rc 0 이다(V10 이 잡히는 것은 규칙이 *둘*이 되기 때문이지 위치를 보기
  때문이 아니다). **새 변이 넷이 rc 0 · 라이트 3.56:1 을 그대로 찍는다.**
  둘 다 설계서 `## 범위 밖` 이 명시로 뺀 자리라 **안 고치고 `digest` 로 인계했다.**
- **목표 3 — `history_003.md` 는 고아가 아니다.** 기점 `bc40ea9` 에서 지워진 `digest`
  회전 줄이 `docs/` 안의 유일한 003 포인터였던 것은 맞다. 그런데 **같은 커밋
  (`2c85b96`)이 `history_current.md` 에 대체 포인터를 넣었다**(`git show` 로 확인).
  전수로 재니 아카이브 20개 중 **001·002·003·005·019** 다섯에 `digest` 압축 줄이
  없고, 대체 포인터 자신도 300줄로 회전하는 문서 안이다 — **`digest ## 반복 실패`
  에 5개로 올렸다.** 처방(`tests/test_docs.py` 검사 한 줄)이 "기록 3파일 불일치" 와
  같은 파일·같은 꼴이라 **한 계획으로 묶인다**고 적어 뒀다.
- **고친 것 (ⓒ, 낮음).** `test_ring_that_is_not_drawn_is_unmeasurable` docstring 이
  *"계획 44 착수 탐침에서 앞 여섯이 전부 종료 0 으로 살아남았다"* 고 적었는데
  **탐침의 여섯은 V1·V2·V3·V4·V5·V6**(계획서 1절 표)이고 표의 앞 여섯은 V4 대신
  **V7** 이다 — V7 은 탐침이 아니라 설계가 따로 심어 본 셋(V7·V8·V10) 중 하나다.
  출처를 갈라 적었다. **코드·단언 무변경**이라 단위 475 그대로.
- **버린 후보 3건(80점 미만).** 임계 주석 지적 1(억제 목록) · `RING_RULE` 상수가
  제품 CSS 를 복제한다는 지적 1(`twist` 가 자리 소멸을 단언하므로 무해) ·
  `focus_rule` 의 순진한 중괄호 쪼개기 1(코드가 `ponytail:` 주석으로 천장을 이미
  적었고 깨지면 측정 불능으로 멈춘다 — 조용한 통과가 아니다).
- **대조 패스 — 계약 9항 전부 이행.** 7항의 `1.27:1` 은 다크 `--line:#3a2a22` /
  `--bg-input:#1e1917` 의 WCAG 상대휘도를 손으로 계산해 독립 검증했다(**1.2707**).
  8항은 단언 6건이라 적었는데 실물은 9건 — 설계 본문 대안 표가 *"9종(V4·V9 제외)"*
  이라 **계약이 덜 센 것이고 테스트가 옳다**(범위 초과가 아니다). `CLAUDE.md` ·
  `project.md` 위반 0 · 새 의존성 0 · 색 리터럴은 기대값이 아니라 비틀 자리다.
- 결과: `PYTHONPATH=src python3 -m unittest discover tests` **475건 OK 11.932초** ·
  `PYTHONPATH=src python3 e2e/design_check.py` **rc 0 · 4축 전부 통과** ·
  `PYTHONPATH=src python3 e2e/pagination_ui_e2e.py` **rc 0**.
- **러너 파이프 1회 — 정직하게 센다.** 변이 검증 열 몇 번은 전부 맨몸이었는데 기록을
  다 쓴 뒤 스위트를 재확인하며 `... discover tests 2>/dev/null` 을 돌렸고 판정 줄이
  삼켜져 `rc=0` 만 봤다. 즉시 맨몸으로 다시 돌렸다. **방아쇠가 앞 반복(⑨)과 똑같은
  "이미 초록일 것 같은 재확인"** 이다 — 규율로 두 반복 연속 실패했으니 `digest` 의
  처방(러너 호출을 별칭·`make test` 로 굳히기)은 이제 계획 후보다. 연속 0회는 **0** 으로
  리셋된다.
  `data/crawl.db` sha256 무변경 · `src/` 0줄 · `.mutation-lock` 생성·삭제 확인.
- 집안일: `digest` 가 **197 → 199줄**(항목 [7] 닫고 새 항목 둘). **한 줄 남았으므로
  다음 반복이 회전을 돌린다.**
- 다음: **e2e 1/1.** 아직 아무도 안 밟은 것은 **진짜 프로세스의 rc 2** 다 —
  단위는 `check_contrast` 를 함수로 부르고 리스트만 본다.

## 2026-09-01 23:45 | focus-ring-presence | e2e 1/1 | 시도0

- 한 일: **설계 `## 갈림길 A` 의 rc 2 를 프로세스 경계에서 처음 밟고 계획 44 를 DONE 으로
  닫았다.** 새 e2e 파일 0개 · 결과 `docs/e2e/focus-ring-presence/result.md`.
- 결과(전부 맨몸 · **러너 파이프 0회**): 단위 **475건 OK 11.892초** · `e2e/*.py` **19종 rc 0** ·
  링을 죽인 CSS 둘(`outline:none` · `outline` 선언 삭제)에서 **rc 2** 이고 `[3]` 절이 14행으로
  줄어 **`3.56:1` 이 화면에 없다** · ko 20/20 · en 19/20 · p95 **8.90ms** · [열림]/[차단] 10.22/s ·
  `crawl.db` sha256 무변경 · `.mutation-lock` 생성·삭제·되돌림 확인(작업 트리 깨끗).
- 집안일 둘 다 닫았다: `history_current` 293 → 269줄 회전(`history_021.md`) · `digest` 200줄 유지.
- 다음: **활성 계획 없음 — 계획 phase(탐색).** PR 은 안 연다(사람 결정).
