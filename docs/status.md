---
signal: GREEN
phase: 개발
step: 5
attempt: 0
iteration: 259
updated: 2026-09-02
ctx: 55
night_iterations: 104
night_red: 0
night_retries: 0
plan: passage-api
---

# 현재 상태

**직전 반복이 세운 값 가드가 계수를 잘못 골라 예산을 2.3배 넘긴 채 초록이다 — 리뷰 2/2
완료, 개발 5 로 반려한다.**
활성 계획 `passage-api` · 설계서 `docs/design_passage-api.md` · 브랜치 `loop/passage-api` ·
기점 `6e99031`. **단위 557건 OK(13.534초)** · `e2e/*.py` **20종** ·
이번 반복의 제품 `src/` **0줄**(재는 일만 했다).

## 이번 반복이 한 것 (리뷰 2/2 — 재확인)

리뷰 1 이 반려한 «본문 크기 축에 상한이 없다» 를 개발 4 가 `MAX_PASSAGE_HTML`(10만자)로
닫았다. **이번 리뷰는 그 값이 실제로 예산을 지키는지만 다시 쟀고, 안 지킨다.**
백지 패스 A 는 `git diff 6e99031..HEAD -- src e2e tests` 만 보고 열었고,
그 뒤 계획·설계·CLAUDE.md 대조 패스 B 를 열었다. **후보 6 → 버림 5 · 보고 1**
(승인 필요 1 · 자동 수정 0). Pass 1(critical)은 **0건**이다.

- **[승인 필요 · high] 가드가 고른 계수 0.118 이 평범한 문서에서 세 배 틀리다.**
  개발 4 의 값 가드는 `MAX_PASSAGE_HTML / 1000 × 0.118 × PASSAGE_LIMIT ≤ 125ms` 라는
  **순수 산수**다 — 즉 초록불의 근거는 계수 하나뿐이다. 코퍼스와 무관하게 10만자를
  세 모양으로 만들어 `extract_blocks` 만 재니 **1,000자당** 태그 성긴 영문 **0.026ms** ·
  한글·태그 촘촘 **0.130ms** · 낱말마다 `<b>`/`<i>`/`<em>` 낀 인라인 촘촘 **0.352ms**.
  개발 4 가 잰 것은 가운데 모양이고, 위키·CMS·에디터 출력의 평범한 모양인 셋째가
  **3배 비싸다**. 가드는 `118ms ≤ 125ms`(여유 **6%**)를 증명했다고 적지만 실제는
  **288ms** 라 예산의 **58%** 를 문단 재파싱이 혼자 먹는다.
- **프로세스 밖에서 HTTP 로 확인했다** — 상한 크기(10만자) 문서 10건을 색인하고
  서버를 띄워 두드리니 `/passages` p95 **790ms**, 예산 500ms 의 **158%**.
  같은 코퍼스 `/search` p95 를 빼면 **문단 경로 몫 354ms(71%)**. 짧은 질의다.
- **초록불은 여기서도 아무것도 안 보증한다.** `e2e/passage_eval.py` 의 p95 는 1.38ms
  (예산의 0.3%)인데 동결 코퍼스 본문이 최대 305자다. **리뷰 1 이 반려한 결함과 같은
  모양이 한 층 아래에서 반복됐다** — 이번엔 e2e 가 아니라 가드가 축을 못 밟는다.
- **처방(개발 5)**: 계수 `0.118 → 0.352`(가드가 **288ms > 125ms** 로 RED 가 된다) ·
  `MAX_PASSAGE_HTML` `100,000 → 35,000`(35 × 0.352 × 10 = **123ms ≤ 125ms**).
  **예산을 넓히지 않는다** — 사양의 성능 5 가 «못 지키면 문단 수를 줄이지, 예산을
  늘리지 않는다» 고 적고 있다. 상한값은 품질 손잡이라 `rules/severity.md` 3·4절이
  자동 적용을 막는다 → 개발 스텝 5 로 넘긴다.
- **보안 패스는 0건이다.** `/passages` 의 200 · 400(q없음 · 201자 · `page=2` ·
  `page=0`) · 404 가 전부 `application/json; charset=utf-8` + `nosniff` + `version:1`
  이고, 본문에 DB 경로·트레이스백·내부 식별자가 없다. `/search` 응답 계약도
  프로세스 밖에서 계획 46 그대로다(`has_next`·`page`·`query`·`results`·`version`).
- **버린 다섯(80점 미만)** 중 하나는 내가 만든 것이다. 백지 패스에서 «200자 질의가
  2020ms» 를 봤는데, 그 코퍼스가 같은 문서 10벌이라 FTS 쪽 인공물이었다. 동결
  코퍼스로 다시 재니 어절 66(197자) 질의에 `search` **5.2ms** · 문단 몫 **0.7ms**.
  긴 질의 축은 어차피 `/search` 몫이고 이 diff 것이 아니다. 나머지 넷은 기보류
  중복·도달 불가·«기존에 이미 있던 문제»(`severity.md` 로 0점).

검증은 전부 맨몸·단독 호출이다(파이프·리다이렉션·명령 잇기 **0회**):
`PYTHONPATH=src python3 -m unittest discover -s tests` **557건 OK(13.534초)**,
세 번 돌려 13.534/13.405/13.353초 · `e2e/passage_eval.py` rc **0** ·
`e2e/search_api_e2e.py` rc **0**. `.mutation-lock` 아래 심은 변이 **0개**(이번 반복은
값을 재기만 했다). 제품 `src/` 0줄 · 의존성 0(stdlib) · 스키마·마이그레이션 없음 ·
`data/crawl.db` 무변경 · `docs/specs/` 무변경.

## 다음 — 개발 5 (리뷰 반려분)

`rules/review.md` 6절대로 승인 필요 1건이 남아 개발으로 돌아간다. 볼 곳 셋:

1. **가드를 산수로 둘지 실측으로 바꿀지.** 지금 모양은 흔들리지 않는 대신 계수가
   틀리면 조용히 거짓말한다. 타이밍 단언으로 바꾸면 CI 에서 흔들린다. 계수를 고치되
   **어느 모양에서 잰 값인지 주석에 적는 쪽**이 기본값이다(`severity.md` 4절).
2. **fixture 가 상수에서 만들어지는 모양(W2)** 이 새 값에서도 눈멀지 않는지.
   값 가드는 리터럴로 적혀 있어 살아 있지만, 계수를 상수로 빼면 같은 병에 걸린다.
3. **35,000 이 기능을 얼마나 깎는지.** 잘린 뒤의 문단은 못 찾는다는 천장이
   `ponytail:` 주석에 이미 적혀 있고, 그 천장이 3분의 1로 내려온다 — 설계서의
   «기능 7» 과 충돌하지 않는지 한 줄 확인이 필요하다.

- 남은 확장 둘: **속도 제한**(사람이 시점을 정한다) · `recrawl`(원래 사양의 마지막).
- **집안일**: 이번 반복이 회전했다 — 리뷰 1·개발 4 를 `docs/history_035.md` 로 밀고
  이 항목을 붙여 현재 **273줄**이다(300 상한 아래). `digest.md` 는 여전히
  **정확히 200줄**이라 다음에 넣으려면 닫힌 항목을 지워 자리를 만들어야 한다.
  명부 줄·규약 줄은 지우지 않는다.

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-02 확인)

`origin/main` 은 PR #6 으로 계획 44~47 까지 받았다. 계획 48 의 커밋들은 브랜치에만 있다.
**남은 것 (사람 결정)**: 계획 48 이 끝난 뒤 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 — 넷이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, `~/.claude/skills/loop-harness/rules/docs.md` 에는 그 예외가 없다.
   **저장소 밖이라 루프가 못 고친다.**
4. **«러너 호출을 다른 것과 섞지 않는다» 를 문장이 아니라 훅으로 옮기는 일.**
   재발 **18회**. 저장소 쪽 자리(`docs/project.md ## 명령`)에 이미 적혀 있는데도 났고,
   남은 자리는 `~/.claude/skills/loop-harness/rules/` 라 **저장소 밖이라 루프가 못 고친다.**
   이번 반복도 14회를 0 으로 끝냈지만 그것이 항목을 닫지는 않는다 — 닫으려면
   **판정 명령을 단독 실행으로 강제하는 PreToolUse 검사**가 필요하다.

**사양이 사람에게 남긴 것 셋** (`docs/specs/concept.md` 의 `## 사람이 정할 것`):
속도 제한 시점(가정 — IP 당 분당 60회) · JSON 검색과 근거 문단의 분할(가정 — 쪼갠다,
**계획 46 이 그 가정을 따랐고 닫았다**) · 사양 숫자들이 초안이라는 것(90% · 500ms ·
60회/분). 첫째와 셋째는 아직 열려 있다.
