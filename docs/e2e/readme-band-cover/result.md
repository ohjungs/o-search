# e2e 결과 — 계획 65 `readme-band-cover` (반복 381 · 2026-09-06)

**판정: 통과 — 21종 전수 `rc 0` · 기준선 회귀 0 · 완료 기준 10/10 → DONE.**
**곁가지로 계획서의 「안 잰다」 주장 하나가 실측에서 뒤집혔다**(5절 · `digest [7]④` 등재).

## 0. 면제 — 새 e2e 를 안 만든다 (`rules/e2e.md` 3절)

계획 61·62·63·64 와 같은 자리다. 그때처럼 「해당 없음」으로 넘기지 않고 **없다는 것을 오늘 쟀다.**

`rules/e2e.md` 3절은 수단을 넷으로 적는다 — 웹 UI · HTTP API · CLI · 라이브러리.
이 계획의 산출물이 그 넷 중 **어디에도 안 닿는다는 근거 셋**:

1. **프로세스 밖에서 달라진 것이 0.** `git diff --stat 44b4eb8 HEAD -- src/ e2e/ docs/specs/ data/`
   가 **빈손**(rc 0, 무출력)이다. 계획 65 가 움직인 파일은 `tests/test_readme.py`(+63/-…)와
   `README.md` 건수 한 줄, 그리고 기록 문서들뿐이라 HTTP 응답·CLI stdout·exit code·DB
   스키마 중 무엇도 어제와 다르지 않다.
2. **산출물이 제품 경로에서 도달 불가능하다.** 오늘 고친 것은 `tests/test_readme.py` 의
   모듈 상수 `QUALITY_BAND` 와 그것을 도는 클래스 하나(`QualityBandTest`)이고, `src/` 에는
   `tests` 를 import 하는 줄이 **0건**이다(`grep -rn "test_readme\|tests\." src/` → 무출력, rc 1).
   **이 계획은 반대 방향으로 한 칸 더 걸린다** — 검사가 `e2e/*.py` 다섯을 *임포트*하지만
   그 방향은 «테스트 → e2e» 라 제품이 테스트를 부르는 자리는 여전히 0 이다.
3. **전체 명령이 이미 집어간다.** 새 파일을 만들면 `PYTHONPATH=src python3 -m unittest
   discover -b -s tests` 와 겹쳐 「1회만」이 깨진다(`rules/e2e.md` 1절). 겹치는 것을
   또 만드는 것은 ponytail 사다리 1번이다.

**새 e2e 파일 0개** — `ls e2e/*.py` 는 어제와 같은 **21개**고 `README.md:105` 의
「e2e 시나리오 21종」이 그대로 맞다.

**대신 사용자 관점 검증을 하나 실행했다**(3절). 이 가드의 「사용자」는 **README 의 합격선을
손으로 고치거나 `e2e/*.py` 상수를 튜닝하는 사람**이다.

## 1. e2e 21종 — 전부 `rc 0`

| # | 시나리오 | rc | 오늘 실측 |
|---|---|---|---|
| 1 | `crawl_e2e.py` | 0 | 수집 15 · 차단 요청 0 · 최소 간격 1.002s |
| 2 | `indexer_e2e.py` | 0 | 3문서 색인 · 증분 0문서 · 없는 상대경로 rc 1 |
| 3 | `noindex_e2e.py` | 0 | 4페이지 중 2문서 · 뒤늦은 noindex 제거 |
| 4 | `search_api_e2e.py` | 0 | 15문서 · 10+5건 겹침 0 · 400/404/501/503/500 · p95 **2.10ms** |
| 5 | `crawl_delay_e2e.py` | 0 | `Crawl-delay:2` 최소 2.01s · 하한 1.00s |
| 6 | `non_ascii_e2e.py` | 0 | 한글 경로 3행 · 서로게이트 시드 1건 건너뜀 |
| 7 | `hidden_passage_e2e.py` | 0 | 숨은 텍스트 **0/5** · 본문 문단 **5/5** · 오탐 대조군 4종 |
| 8 | `design_check.py` | 0 | 4축 통과 · JS **0 B** · 최저 대비 **4.87:1**(포커스 3.56:1) |
| 9 | `tokenizer_e2e.py` | 0 | 화면(HTML)에서 복합어·어순·굴절·AND · 503→재색인 복구 |
| 10 | `domain_key_e2e.py` | 0 | 같은 서버 간격 2.01×3 · robots 1회 · 대조군 1.00/1.01 |
| 11 | `deadline_e2e.py` | 0 | 대조군 16 / CLI 8페이지 2.2s · **버려진 응답 0건** |
| 12 | `interrupt_e2e.py` | 0 | SIGINT 10.0s rc 130 · DB 0행 · 두 번째 Ctrl-C rc -2 |
| 13 | `indexer_interrupt_e2e.py` | 0 | 재구축 중단이 옛 2,000행 보존 · 재실행 복구 |
| 14 | `crawl_politeness_e2e.py` | 0 | 예외 뒤 2.01s · 대조군 1.00s · 재시도 1.01s |
| 15 | `pagination_ui_e2e.py` | 0 | 다음을 따라가 doc00 · 마지막에 다음 없음 |
| 16 | `retry_interval_e2e.py` | 0 | https 재시도 5.00s · 대조군 1.01s |
| 17 | `url_normalize_e2e.py` | 0 | 표기 10개가 문서 6개로 |
| 18 | `perf_crawl.py` | 0 | [열림] **10.23/s** · [차단] **10.24/s** · [예외] 간격 1.002s |
| 19 | `perf_search.py` | 0 | p50 1.35ms · **p95 8.81ms** (예산 300ms 의 2.9%) |
| 20 | `quality_eval.py` | 0 | ko **20/20** · en **19/20** · 매치 평균 14.0 · 최소 11 · 최대 28 |
| 21 | `passage_eval.py` | 0 | 정확도 **100.0%** · 채택률 99.5% · p95 **1.48ms** |

전부 맨몸으로 돌렸다 — 파이프·리다이렉션 **0회**(`docs/project.md` 「명령」 절 규율).

## 2. 기준선 대조 — 회귀 **0**

| 축 | 오늘 | 기준 | 직전(계획 64) |
|---|---|---|---|
| 근거 문단 정확도 | **100.0%** | ≥90% | 100.0% |
| `/passages` p95 | **1.48ms** | ≤500ms | 1.51ms |
| `/search` p95 | **8.81ms** | ≤300ms | 8.95ms |
| 품질 ko / en | **20/20 · 19/20** | ≥80% | 20/20 · 19/20 |
| 매치 수 평균/최소/최대 | **14.0 / 11 / 28** | 기록 | 14.0 / 11 / 28 |
| 크롤 [열림] / [차단] | **10.23 / 10.24** | 5.0 / 9.0 | 10.25 / 10.26 |
| 디자인 JS / 최저 대비 | **0 B / 4.87:1** | 50KB / 4.5:1 | 0 B / 4.87:1 |
| 숨은 텍스트 문단 | **0/5** | 0/5 | 0/5 |

**여덟 축 전부 같은 띠 안이고 움직인 기준선이 없어 `docs/project.md` 의 수치는 한 줄도
안 갱신했다.** `/search` p95 8.95→8.81ms, `/passages` p95 1.51→1.48ms, 크롤 10.25→10.23/s
는 각각 예산의 2.9%·0.3% 와 기준의 2배 위라 전부 잡음이다(계획 60~64 가 9.01·8.69·8.78·
9.01·8.95 였다). 계획이 제품 `src/` 를 0줄 고쳤으니 당연한 결과고, 이 표는 그 「당연함」이
실제로 그런지를 잰 것이다.

**이번 계획은 기준선 대조가 두 겹이다** — 위 표의 여덟 축 중 여섯이 곧 이 계획이 README 와
묶어 놓은 합격선이라, 표가 초록이면 `QualityBandTest` 가 무는 상수도 오늘 움직이지 않았다는
뜻이다(그리고 실제로 전수가 초록이다).

## 3. 사용자 관점 검증 — 실물 사본에 사람이 낼 편집 아홉

저장소(`src`·`tests`·`e2e`·`docs`·`README.md`)를 `mktemp -d` 아래로 복사하고 그 **복사본만**
고친 뒤, 복사본 안에서 `python3 -m unittest -b tests.test_readme` 를 진짜 서브프로세스로
돌렸다. 저장소 워킹트리는 매 실행 뒤 `git status --porcelain` 이 **빈손**이었다.
이번 축은 **「어느 한쪽만 고치면 빨개진다」가 실물 파일 편집에서도 참인가**다 —
앞 phase 의 변이 서른셋은 전부 메모리(`mock.patch.object`)였다.

| # | 편집(사람이 낼 법한 것) | 결과 | 가드가 낸 말 |
|---|---|---|---|
| U0 | 손 안 댐 (대조군) | `Ran 6 · OK` · rc 0 | — |
| U1 | README 「맞는 게 80% 이상」→「85% 이상」 (문서만) | `failures=1` rc 1 | `band='검색 합격선 %'` · `Decimal('85') != Decimal('80')` |
| U2 | `e2e/perf_search.py` `BUDGET_MS 300→250` (상수만 튜닝) | `failures=1` rc 1 | `band='검색 예산 초'` · `Decimal('300.0') != Decimal('250')` |
| U3 | README 「JS 50KB 이하」→「100KB 이하」 (문서만) | `failures=1` rc 1 | `band='JS 예산 KB'` · `Decimal('102400') != Decimal('51200')` |
| U4 | 크롤 속도 5→6 을 README·`TARGET_RATE` **양쪽** 다 | `Ran 6 · OK` · rc 0 | — (정당한 변경은 안 막는다) |
| U5 | README 문구만 다듬기 — 「배경과 4.5배 이상 차이 나게」→「배경보다 4.5배 밝게」 | `failures=1` rc 1 | `band='텍스트 대비 배수'` · `1 != 0` 「수치를 정확히 하나 못 뽑았다」 |
| U6 | `MIN_CONTRAST_NONTEXT 3.0→3` (표기만, 값 동일) | `Ran 6 · OK` · rc 0 | — (`Decimal('3') == Decimal('3.0')`) |
| U7 | README 표에서 「화면이 가벼운가」 행을 통째로 삭제 | `failures=1` rc 1 | `band='JS 예산 KB'` · `1 != 0` |
| U8 | README 표 셋째 칸의 측정기 파일 이름을 오타로 (`quality_eval.py`→`quality_evals.py`) | `Ran 6 · OK` · rc 0 | **없음 — 조용하다** |

**U1·U2·U3 이 세 방향을 각각 걸었다** — 문서만 고침 · 상수만 고침 · 단위 환산이 낀 축.
셋 다 `subTest` 라벨이 정확히 그 행 하나고, 실패 메시지가 **양쪽 값과 배율을 다 찍어**
어느 쪽을 고쳐야 하는지가 화면에 있다. **U4·U6 이 반대 방향이다** — 양쪽을 함께 옮긴
정당한 튜닝과 표기만 바꾼 편집은 **안 막는다**. 한쪽만 재는 검사가 아니다.

**U7 은 U1~U3 과 다른 단언에서 죽는다.** 값 비교가 아니라 「정확히 하나 못 뽑았다」쪽이다 —
표에서 행이 사라지면 대조할 값 자체가 없어지는데, 그 자리를 「찾을 게 없으니 통과」로
넘기지 않는다는 것이 이 검사의 핵심이다(리뷰 380 이 고친 `Decimal` 축과는 별개 축).

### 3-1. 알려진 천장 — U5 는 설계된 소음이다

수치 `4.5` 는 한 자도 안 틀렸는데 문구를 다듬었다는 이유로 빨개진다. **이것은 결함이 아니라
`tests/test_readme.py:47-49` 가 명시적으로 고른 쪽이다** — 정규식이 문구를 통째로 담으면
문구를 다듬는 날 이 표도 같이 고쳐져 **거울**이 되고, 그러면 대조가 사라진다. 소음의 값은
「빈손 위에서 조용히 통과」를 막는 것이고, 고치는 법도 실패 메시지에 정규식으로 찍혀 있다.
**여는 조건**: README 문구를 자주 다듬게 되는 날 — 오늘은 안 왔다.

### 3-2. 계획서의 「안 잰다」 주장 하나가 실측에서 뒤집혔다 — U8

계획 65 5절은 이렇게 적어 두고 잘랐다:

> **표의 세 번째 칸(측정기 파일 이름)이 실재하는지는 새로 안 잰다.** `ls e2e/*.py` ↔
> 「21종」 대조가 이미 있고, **새 표가 모듈을 임포트하므로 이름이 틀리면 그 자리에서 터진다.**

**뒤 절반이 틀렸다.** `QUALITY_BAND` 가 임포트하는 것은 **튜플 자신의 셋째 원소**
(`"quality_eval"` 같은 리터럴)이지 README 셋째 칸이 아니고, `E2E_COUNT` 대조는 파일 **개수**만
센다. 그래서 README 가 없는 파일을 가리켜도 `Ran 6 · OK · rc 0` 이다 — 이 저장소가 이 파일을
만든 이유(『README 가 없는 모듈 `websearch.cli` 를 안내한 채로 푸시됐다』, `test_readme.py:3-6`)와
**정확히 같은 모양의 구멍**이 표의 셋째 칸에 남아 있다. `MODULE` 정규식이 `-m websearch.<모듈>`
형태만 잡아서 표의 백틱 경로는 안 걸린다.

**오늘 고치지 않는다** — 계획 5절이 범위 밖으로 자른 자리라 e2e phase 가 스스로 넓히면
카파시 3번(직교 편집)이다. `digest [7]④` 로 등재하고 처방까지 적어 둔다.
**완료 기준에는 영향이 없다** — 이 축은 어느 기준에도 안 들어 있고, 오늘 실물 아홉 칸은
전부 실재한다(`ls e2e/*.py` 로 대조).

## 4. 완료 기준 10/10 — 오늘 실행으로 다시 쟀다

| # | 실측 | 기준 | 판정 |
|---|---|---|---|
| 1 | U2 와 같은 축을 실물 사본에서 — `passage_eval.ACCURACY` 축은 앞 phase 재측 1건, 오늘은 형제 축(`perf_search.BUDGET_MS`)을 파일 편집으로 밟아 `band='검색 예산 초'` **1건** | M3 이 1건 이상 죽는다 | 충족 |
| 2 | U2 → `failures=1` · `band='검색 예산 초'` (배율 1000 이 실제로 곱해진 것을 메시지가 찍는다) | M4 가 1건 이상 | 충족 |
| 3 | 같은 자리 — 문단 예산 행은 행 머리(`\| 문단도 빠른가 \|`)로 갈라져 U2 에서 **안 섞였다**(죽은 것은 검색 예산 하나) | M5 가 1건 이상 | 충족 |
| 4 | U4 가 `TARGET_RATE` 축을 양방향으로 밟았다 — 한쪽만(README 5→6, 상수 5.0) 이면 RED, 양쪽이면 OK | M6 이 1건 이상 | 충족 |
| 5 | U3 → `failures=1` · `band='JS 예산 KB'` · `Decimal('102400') != Decimal('51200')` (×1024 환산 실측) | M7 이 1건 이상 | 충족 |
| 6 | U1(합격선 %) · U3(JS KB) · U7(행 삭제) — 문서 축 **3/3 사망** | R1~R3 3/3 | 충족 |
| 7 | 표의 아홉 칸이 `ls e2e/*.py` 의 실물 다섯 모듈과 대조돼 전수 초록. 값 일치는 `Ran 623 · OK` 가 매 실행마다 재확인 | README 아홉 ↔ 상수 아홉 일치 | 충족 |
| 8 | 이미 물던 넷(TOP_N·THRESHOLD·MIN_CONTRAST·NONTEXT)의 축이 오늘도 살아 있다 — U1 은 `THRESHOLD` 축에서 `QualityBandTest` 가, U6 은 `NONTEXT` 표기 변경에서 **아무도** 안 운다(오탐 0) | 증강 유지 | 충족 |
| 9 | `README.md:104` 「단위 623건」 ↔ 실제 623 · `:105` 「e2e 시나리오 21종」 ↔ `ls e2e/*.py` 21개 | README 건수 갱신 | 충족 |
| 10 | 맨몸 전수 `Ran 623 tests in 15.787s` · `OK` · **rc 0** · e2e 21종 개별 **21/21 rc 0** | 전수 초록 | 충족 |

**범위도 다시 쟀다**: `git diff --stat 44b4eb8 HEAD -- src/ e2e/ docs/specs/ data/` **빈손** ·
`data/crawl.db` sha256 `85c96744…5bda18` 무변 · 재색인 0 · 스키마 0 · 새 의존성 0(stdlib).

## 5. e2e 가 잡은 것 — 하나

diff 의 주장 대부분은 실물과 다시 대조해 어긋난 곳이 없었다(값 아홉 일치 · 세 축 환산 ·
`subTest` 라벨 · 오탐 0). **어긋난 것은 계획서 5절의 배제 근거 한 줄이고, 그것은 diff 가
아니라 계획서에 있다**(3-2절). 코드에 결함이 없는데 «안 재도 되는 이유» 가 틀렸던 것이라
반려 사유가 아니고, 처방은 `digest` 로 넘긴다.

## 6. 한도 — 지킨 것

- **러너 규율 위반 0회(누적 38 유지).** 전수·e2e 21종·사용자 편집 9판을 전부 맨몸으로
  돌렸다. 판정 줄(`Ran … / OK / rc`)을 매번 눈으로 확인했다.
- **편집은 저장소 밖에서만** — `mktemp -d` 사본. 매 판 뒤 `git status --porcelain` 빈손.
  실물 `README.md`·`e2e/*.py` 는 이 phase 에서 **0줄** 움직였다.
- `PYTHONDONTWRITEBYTECODE=1` 과 `PYTHONPYCACHEPREFIX=$(mktemp -d)` 를 함께 줬다.
- `data/crawl.db` 무변경 · 재색인 0 · 스키마 0 · 새 의존성 0(stdlib) · `docs/specs/` 읽기만.
- 원격 `loop/passage-cost-band` 위에 오늘 커밋을 쌓는다. `origin/main`(`d1fe3e9`) 무접촉 ·
  PR #7 무접촉(`gh` 호출 0) · `--force`·`--amend`·`rebase` 0회. 병합은 사람 몫이다.
- 도구 산출물 없음 — 브라우저 도구를 쓰지 않는 프로젝트라 `test-results/` 에 해당하는
  경로가 없다. 위 표의 숫자가 실행 출력 전부다.
