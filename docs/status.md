---
signal: DONE
phase: e2e
step: 1
attempt: 0
iteration: 234
updated: 2026-09-02
ctx: 62
night_iterations: 93
night_red: 0
night_retries: 0
plan: null
---

# 현재 상태

**계획 45 `docs-record-guard` DONE — e2e 1/1 통과.** 제품이 아니라 저장소 자신의 기록
규율을 고친 계획이라 e2e 질문을 **"진짜 절차가 이 가드를 통과하는가"** 로 잡고 절차
셋(회전 · 반복 기록 · 계획 완료)을 실제로 밟았다. **새 `e2e/*.py` 0개.**
단위 **482건 OK**(맨몸) · `e2e/*.py` **19종 rc 0** · 품질 4축 전부 기준선 ·
`src/` **0줄** · `data/crawl.db` 무변경. 증거는 `docs/e2e/docs-record-guard/result.md`.
**다음 반복은 계획 phase (탐색).**

## 방금 한 것 (2026-09-02 · e2e 1/1)

회전이 이번 반복에 안 걸려(`history_current.md` 194줄 · 기록 2회) 회전 절차 셋은
**`.git` 없는 스크래치패드 전체 사본**에서 밟았다(`digest` 의 "제자리 변이를 자동
스냅샷이 커밋한다" 처방). 사본이 실물과 같은 판정을 내는 것부터 확인했다.

| # | 절차 | 어디서 | 실측 |
|---|---|---|---|
| P0 | 무변경 | 사본 | 482건 OK — 사본 = 실물 |
| P1 | **회전하고 명부를 안 고친다** | 사본 | **RED** — `ArchiveIndexTest` 하나만, `[] != ['history_024.md']` |
| P2 | 명부에 이름을 더한다 | 사본 | GREEN · `digest.md` **200줄 유지** |
| P3 | **상한 → 완료 항목 한 줄 삭제** | 사본 | **GREEN** — 명부가 020~023 을 붙들었다 |
| L1 | **`status.md` 만 234 로 올린다** | 실물 | **RED** — `'233' != '234'` |
| L2 | `metrics.md` 도 올린다 | 실물 | GREEN |
| L3 | 계획서·설계서를 아카이브로 | 실물 | **고아 0** (아래) |

- **P3 이 설계 3절의 정면 충돌을 실제로 풀었다.** 아카이브 넷(020~023)의 산문 포인터를
  통째로 가진 완료 항목 한 줄을 지웠는데 **GREEN**이다. `rules/docs.md` 의 *"완료
  항목부터 지운다"* 와 새 불변량이 더는 안 부딪히고, 계획 43 의 회전이 색인 구멍을
  만들던 사고가 **구조적으로 안 난다.** 갈림길 B 대안 1 이 고른 값이 여기서 나온다.
- **L1 — 가드가 이번 반복 자기 기록을 잡았다.** 재발 항목 ④와 같은 모양이고 **리뷰가
  없는 phase 라 예전이면 조용히 어긋난 채 갔을 자리**다. 이틀 연속 실사용 발화.
- **L3 — 계획서 2-4 절의 임시 상태를 실제로 걷어냈다.** `plan_history_031.md`·
  `design_history_031.md` 로 옮기니 살아있는 문서가 **6개로 복귀 · 아카이브 23 · 고아 0.**
  `## 완료` 절에서 **명부가 유일한 포인터는 다섯**(001·002·003·005·019, 리뷰 [A1] 그대로)
  이고 그중 **살아있는 문서 어디에도 다른 포인터가 없는 것은 `history_002.md`·
  `history_005.md` 둘**이다 — 착수 시점의 진짜 고아 둘을 지금 붙드는 것이 명부 한 줄뿐이다.
- **새 `e2e/*.py` 를 왜 0개로 두나**: 검사는 `read_text` + `re.search` 뿐이라 프로세스로
  잴 것이 없다(계획 41·42 와 같은 판정). 잴 것이 하나 더 있었지만 **절차는 사람이 밟는
  순서지 프로그램이 아니라** 스크립트로 굳히면 회전 절차의 사본이 하나 더 생긴다.

## 한도 (매 반복 확인)

- `main` 직접 커밋 금지 · `--no-verify` 금지 · 외부 네트워크 금지 ·
  `docs/specs/` 읽기만 · `data/crawl.db` 무변경(sha256 확인) · 의존성 추가 금지(stdlib 만).
- **러너 파이프 — 이번 반복 1회. 연속 0 이 끊겼다.** 사본 기준선 P0 **첫 실행에
  `| tail -60`** 을 붙였다. 방아쇠는 항목이 적어 둔 그대로 *"이미 초록일 것 같은 실행"*
  (사본을 막 만들고 무변경을 확인하는 자리)이다. 판정 줄은 살았지만 **테스트 표준출력이
  뒤에 붙어 창 안에 들어온 운**이라 실패로 센다. 즉시 맨몸 재실행했고 이후 전 실행
  (사본 4 · 실물 3 · e2e 19종)이 맨몸이다. **접기 관찰 카운트는 0 으로 되돌아간다.**
- **L1 의 의도된 RED 창에 `.mutation-lock` 을 걸었고** L2 GREEN 뒤 지웠다. 회전 절차
  변이는 전부 `.git` 없는 사본이라 제자리 변이 0. 작업 트리에 `.mutation-lock` 없음.
- 이번 반복 최종 실측(맨몸): `PYTHONPATH=src python3 -m unittest discover -s tests`
  **482건 OK** · `e2e/*.py` **19종 전부 rc 0** · ko 20/20 · en 19/20 · p95 **8.86ms** ·
  처리량 10.23·10.25/s · 디자인 4축 통과(최저 4.87:1) · `data/crawl.db` sha256
  `85c96744…` **무변경**(시작·끝 두 번) · README 카운트 가드 무변(482 · 19종).
- **집안일 — 회전 미달이다.** 이 기록을 붙여도 상한 300 아래다. 아카이브 23개 그대로라
  **명부 갱신 없음.** 다음 회전이 명부에 이름을 더할 의무는 그대로고, P1 이 그것을
  실물로 확인해 뒀다.

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-01 확인)

`origin/main` 은 `494313b`(PR #5 병합). 계획 43·44·45 의 커밋들(`bc40ea9` 이후)이
아직 안 갔다. **남은 것 (사람 결정)**: 그것들을 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 — 셋이 열려 있다 (하나가 닫혔다)

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
   offset 이 0 이 되는 날 검사기가 멈추고, 짝은 그때 열릴 계획의 일이다.
3. ~~기록 3파일 불일치~~ — **계획 45 가 닫았다** (DONE).
4. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   이제 *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, 회전을 지시하는 `~/.claude/skills/loop-harness/rules/docs.md` 에는
   그 예외가 없다. **저장소 밖이라 루프가 못 고친다.** P1·P3 이 실동작은 안전하다는
   것을 실측했다(잊으면 RED · 완료 항목을 지워도 색인은 산다). 룰 문장을 맞출지는 사람 몫.
