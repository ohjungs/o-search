# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**반복 97~122 는 `history_006.md`, 123~128(계획 020 `deadline` 전체)은
`history_007.md`, 계획 21~24(짧은 경로 3건 + 보류 패치 소진, 2026-08-29 14:20~17:40)는
`history_008.md` 로 밀려났다** (2026-08-29 회전 3회).
**계획 25~29 는 `history_009.md`, 계획 30·31(짧은 경로) + 반복 152 탐색 +
계획 33 `clock-injection` 의 계획·설계·개발 1~4 는 `history_010.md` 로 밀려났다**
(2026-08-30 회전, 393줄 → 139줄. 상한 300줄을 넘겨 놓고 두 반복을 갔다).
**계획 33 의 마지막 네 반복(개발 5 · 테스트 1 · 리뷰 1 · e2e 1)은 `history_011.md`**
(2026-08-30 회전, 322줄 → 210줄. 상한을 넘긴 채 반복 두 번을 또 갔다 — 스텝 3 이
"미결(문서 위생)" 로 넘긴 것을 스텝 4 가 닫았다).
계획 33 의 전 phase 서술은 `plan_history_020.md` 의 `## 기록` 에 그대로 있다.
**계획 34 `graceful-interrupt` 의 앞 여덟 반복(계획 1 · 설계 1 · 개발 1 세 번 · 2 · 3 · 4)은
`history_012.md`** (2026-08-30 회전, 300줄 → 85줄. **상한에 정확히 닿아 넘기기 전에 돌렸다** —
앞 두 번은 넘긴 채 반복을 더 갔다).
**계획 34 의 마지막 세 반복(테스트 1 · 리뷰 1 · e2e 1)은 `history_013.md`**
(2026-08-31 회전, 307줄 → 244줄. 상한 300 을 넘긴 그 반복에서 바로 돌렸다 —
`digest ## 반복 실패` 가 지적한 "회전이 두 반복 늦었다" 를 안 되풀이한다).
**계획 35 `deadline-stop` 전체 여섯 반복(계획 · 개발 1 · 테스트 2 · 리뷰 · e2e)은
`history_014.md`** (2026-08-31 회전, 336줄 → 188줄. 상한 300 을 넘긴 채 테스트 반복
하나를 더 갔고 **리뷰가 그것을 잡았다** — 회전은 phase 룰이 아니라 문서 위생이라
`status.md` 의 "집안일 미결" 로만 있었다. 완료된 계획 하나를 통째로 미는 경계로 잘랐다).

**계획 36 `signal-budget-cover` 전체 두 반복과 계획 37 `indexer-interrupt` 전체 여섯 반복은
`history_015.md`** (2026-08-31 회전, 318줄 → 96줄. **계획 38 리뷰가 잡아서 돌렸다** —
테스트 스텝이 305줄인 것을 알고 `status.md` 에 적어 둔 채 그대로 갔다. 393·322·336·318,
네 번째 지각이다. 완료된 계획 둘을 통째로 미는 경계로 잘랐다).

**계획 38 `digest-rotate` 전체 다섯 반복과 계획 39 `indexer-lock` 전체 다섯 반복은
`history_016.md`** (2026-09-01 회전, 299줄 → 145줄. **계획 40 리뷰가 제때 돌렸다** —
테스트 반복이 "다음 반복이 회전한다" 고 적어 둔 그대로다. 393·322·336·318 은 지각이었고
이번은 아니다. 완료된 계획 둘을 통째로 미는 경계로 잘랐다 — 계획 40 을 여는 반복 198 부터 남는다).

**계획 40 `exit-code-contract` 전체 여섯 반복(198~203)은 `history_017.md`** (2026-09-01
회전, 291줄 → 새 항목을 넣기 전에 돌렸다. **테스트 phase 가 자기 손으로 돌린 첫 회전이다** —
205 가 "다음 반복이 회전 자리다" 라고 적어 둔 그대로다. 완료된 계획 하나를 통째로 미는 경계).

**계획 41 `crawl-db-guard` 전체 다섯 반복(204~208)은 `history_018.md`** (2026-09-01
회전, 364줄 → 221줄. **e2e phase 가 마감 작업으로 돌렸다** — 300줄 상한을 64줄 넘긴
채로 개발 2/2 가 지나갔다. 완료된 계획 하나를 통째로 미는 경계. 이 회전이
`DocCitationTest` 의 아카이브 제외 규칙을 **실물로 시험한 첫 사례**다 — 계획 42
7절 반증 신호 ②가 예고한 그 자리이고, 새 이름이 규칙과 맞아 스위트는 초록이다).

**계획 42 `docs-citation-guard` 전체 여섯 반복(209~214)은 `history_019.md`** (2026-09-01
회전, 292줄 → **설계 반복이 자기 항목을 넣기 전에 돌렸다** — 넣었으면 300줄을 넘겼다.
계획 41 회전이 64줄 지각한 바로 다음 회전이고, 이번엔 앞에서 잘랐다. 완료된 계획 하나를
통째로 미는 경계).

**계획 43 `focus-contrast` 의 앞 여섯 반복(215~220)은 `history_020.md`** (2026-09-01
회전, 293줄 → 70줄. **e2e 반복이 자기 항목을 넣기 전에 돌렸다** — 넣었으면 311줄이다.
계획 42 회전과 같은 자리에서 같은 순서로 돌았고, 두 번 연속 지각이 없다. 완료된 계획
하나를 통째로 미는 경계).

**계획 43 의 마지막 반복(e2e 1/1 · 반복 221)은 `history_021.md`** (2026-09-01 회전,
293줄 → 269줄. **계획 44 e2e 반복이 자기 항목을 넣기 전에 돌렸다** — 넣었으면 300줄에
닿는다. 완료된 계획 하나를 통째로 미는 경계이고, 계획 43 은 이 회전으로 다 밀렸다).

---

**계획 44 `focus-ring-presence` 전체 여섯 반복(계획 · 설계 · 개발 · 테스트 · 리뷰 · e2e)은
`history_022.md`** (2026-09-01 회전, 306줄 → 107줄. **상한 300 을 넘긴 그 반복에서
바로 돌렸다** — 계획 phase 가 자기 손으로 돌린 첫 회전이다. `digest.md` 압축 줄은 200줄
상한 때문에 새 줄을 더하지 않고 **기존 회전 줄에 이어 붙였다**(`## 완료` 의 2026-09-01
`history_021.md` 줄) — 완료 항목은 하나도 안 지웠으므로 **새 색인 구멍 0** 이다).

**계획 45 `docs-record-guard` 의 앞 네 반복(계획 · 설계 · 개발 1/2 · 개발 2/2)은
`history_023.md`** (2026-09-02 회전, 318줄 → 140줄. **테스트 반복이 자기 항목을 넣고
상한을 넘긴 것을 보고 그 자리에서 돌렸다.** 진행 중인 계획을 가르는 경계라 완료 계획을
통째로 미는 평소 경계와 다르다 — 계획 33·34 회전과 같은 꼴이고, 나머지 서술은
`plan_docs-record-guard.md` 와 이 계획이 DONE 될 때 남길 아카이브가 갖는다.
**이번 회전은 새 의무를 하나 지고 돈 첫 회전이다** — 계획 45 자신이 만든
`ArchiveIndexTest` 때문에 `digest.md` 의 아카이브 명부에 `history_023.md` 를 더하지
않으면 스위트가 RED 다. 규약이 문서에만 있던 어제와 달리 오늘은 검사가 강제한다).

**계획 45 `docs-record-guard` 의 마지막 세 반복(테스트 · 리뷰 · e2e)은
`history_024.md`** (2026-09-02 회전, 303줄 → 155줄. **직전 계획 반복이 "회전 미달"
이라 적었는데 실측은 303줄로 이미 상한을 넘겨 있었다** — 그 반복이 자기 항목을 붙인 뒤
다시 안 셌다. 다음부터 회전 판정은 **항목을 붙인 뒤** `wc -l` 로 센다. 이 회전으로
계획 45 는 통째로 밀렸고(앞 넷은 `history_023.md`), 완료된 계획 하나를 통째로 미는
평소 경계로 돌아왔다. 명부에 `history_024.md` 를 더했다 — `ArchiveIndexTest` 가
강제하는 두 번째 회전이다. `digest.md` 는 200줄 상한이라 새 줄을 안 만들고
`## 완료` 의 2026-09-01 회전 줄에 이어 붙였다 — **완료 항목을 안 지웠으므로 새 색인 구멍 0**).
**계획 46 `json-contract` 의 계획 phase 반복은 `history_025.md`** (2026-09-02 회전,
317줄 → 257줄. **직전 회전이 적어 둔 "항목을 붙인 뒤 센다" 를 처음 그대로 따랐다** —
개발 2/3 항목을 append 한 뒤 `wc -l` 로 재서 317줄을 보고 그 자리에서 돌렸다.
진행 중인 계획을 가르는 경계라 설계·개발 항목은 남는다. 명부에 이름을 더했고
`digest.md` 는 같은 회전 줄에 이어 붙였다 — **새 색인 구멍 0**).

**계획 46 `json-contract` 의 설계·개발 네 반복(236~239)은 `history_026.md`**
(2026-09-02 회전, 290줄 → 116줄. **테스트 반복이 자기 항목을 붙이기 전에 돌렸다** —
직전 반복이 *"상한에 닿았으니 다음 반복이 항목을 붙인 뒤 세어 회전한다"* 고 예고했고,
붙이면 330줄이라 계산이 아니라 예고 그대로 앞에서 잘랐다(일곱 회전 연속 지각 0).
**진행 중인 계획을 가르는 경계**라 계획 46 의 남은 phase 는 이 파일에 이어 쌓인다.
명부에 `history_026.md` 를 더했고 `digest.md` 는 200줄 상한이라 새 줄을 안 만들고
`## 완료` 의 같은 회전 줄에 이어 붙였다 — **새 색인 구멍 0**).

**계획 46 의 테스트·리뷰 두 반복(240~241)은 `history_027.md`** (2026-09-02 회전,
301줄 → 174줄. **e2e 반복이 자기 항목을 붙인 뒤 세어 301줄을 보고 그 자리에서 돌렸다**
— 직전 회전이 세운 *"항목을 붙인 뒤 센다"* 그대로다(여덟 회전 연속 지각 0). 계획 46 은
DONE 이지만 **마지막 e2e 항목은 남긴다** — 다음 반복이 매번 읽는 파일에 직전 결과가
있어야 한다. 명부에 `history_027.md` 를 더했고 `digest.md` 는 200줄 상한이라 새 줄을
안 만들고 `## 완료` 의 같은 회전 줄에 이어 붙였다 — **새 색인 구멍 0**).

**계획 47 `db-open-atomic` 의 계획·설계 두 반복(242~244)은 `history_029.md`**
(2026-09-02 회전, 325줄 → 235줄. **테스트 반복이 자기 항목을 붙인 뒤 세어** 325줄을
보고 그 자리에서 돌렸다(아홉 회전 연속 지각 0). **진행 중인 계획을 가르는 경계**라
개발 1/2·2/2 와 테스트 항목은 남는다 — 다음 리뷰 반복이 읽을 것이 그 셋이다.
35줄만 밀면 290줄이라 다음 반복이 곧바로 또 회전한다 — 계획·설계 둘을 함께 밀어
여유를 남겼다. 명부에 `history_029.md` 를 더했고 `digest.md` 는 200줄 상한이라
새 줄을 안 만들고 `## 완료` 의 같은 회전 줄에 이어 붙였다 — **새 색인 구멍 0**).


**계획 47 의 개발 두 반복(245~246)은 `history_030.md`** (2026-09-02 회전,
301줄 → 237줄. **리뷰 반복이 자기 항목을 붙인 뒤 세어** 301줄을 보고 그 자리에서
돌렸다(열 회전 연속 지각 0). 직전 회전이 *"여유를 남겼다"* 고 적었는데 리뷰 항목 하나가
그 여유를 다 썼다 — 예고와 실측이 어긋난 첫 사례다. **진행 중인 계획을 가르는 경계**라
테스트·리뷰 항목은 남는다: e2e 반복이 읽을 것이 그 둘이다(개발 서술은 `status.md` 의
«그 전 반복» 절과 `plan_db-open-atomic.md` 에도 있다). 명부에 `history_030.md` 를
더했고 `digest.md` 는 200줄 상한이라 새 줄을 안 만들고 `## 완료` 의 같은 회전 줄에
이어 붙였다 — **새 색인 구멍 0**).

**계획 47 의 마지막 세 반복(테스트 1 · 리뷰 1 · e2e 1)은 `history_031.md`**
(2026-09-02 회전, 308줄 → 169줄. 계획 phase 반복이 **자기 항목을 붙인 뒤 세어** 308줄을
보고 그 자리에서 돌렸다 — 열한 회전 연속 지각 0이고, 직전 회전이 남긴 예고
*"다음 반복이 자기 항목을 붙이면 넘을 자리"* 가 그대로 맞은 첫 사례다. **계획 47 이
DONE 이라 경계가 깨끗하다** — 남은 것은 계획 48 의 항목 하나뿐이고, 47 의 서술은
`plan_history_033.md` 와 `docs/e2e/db-open-atomic/result.md` 에 그대로 있다.
명부에 `history_031.md` 를 더했고 `digest.md` 는 200줄 상한이라 새 줄을 안 만들고
`## 완료` 의 같은 회전 줄에 이어 붙였다 — **새 색인 구멍 0**).



**계획 48 `passage-api` 의 계획·설계 두 반복(251~252)은 `history_032.md`**
(2026-09-02 회전, 286줄 → **개발 3/3 항목을 붙이면 300 을 넘길 자리라 붙이기 전에
돌렸다** — 직전 반복이 *"다음 반복은 회전이다"* 라고 예고한 그대로다(열두 회전 연속
지각 0). **진행 중인 계획을 가르는 경계**라 개발 1·2·3 세 항목은 남는다 — 다음 테스트
반복이 읽을 것이 그 셋이고, 계획·설계의 서술은 `plan_passage-api.md` 와
`design_passage-api.md` 에 원본이 그대로 있다. 명부에 `history_032.md` 를 더했고
`digest.md` 는 200줄 상한이라 새 줄을 안 만들고 `## 완료` 의 같은 회전 줄에 이어
붙였다 — **새 색인 구멍 0**).

**계획 48 의 개발 세 반복(252~254)은 `history_033.md`** (2026-09-02 회전, 279줄 →
이 항목을 붙이면 300 을 넘길 자리라 **붙이기 전에 돌렸다** — 직전 회전의 예고 그대로이고
열세 회전 연속 지각 0. 직전 회전이 개발 세 항목을 남긴 이유가 *"다음 테스트 반복이 읽을
것이 그 셋"* 이었고 **이 반복이 그 테스트 반복이라 다 읽고 밀었다** — 남은 것은 이 항목
하나다. 명부에 더했고 `digest.md` 는 200줄 상한이라 같은 회전 줄에 이어 붙였다 —
**새 색인 구멍 0**).

**계획 48 의 테스트 1(반복 255)은 `history_034.md`** (2026-09-02 회전, 285줄 →
이 항목을 붙이면 303줄이라 **붙이기 전에 돌렸다**(열네 회전 연속 지각 0).
**한 항목만 밀었다** — 리뷰 1·개발 4 는 이 반복이 닫은 반려분의 근거라 다음 리뷰
반복이 그대로 읽어야 한다. 밀린 테스트 1 의 M1~M8 재확인은 `design_passage-api.md`
변이표가 원본을 갖고 있다. 명부에 `history_034.md` 를 더했고 `digest.md` 는 200줄
상한이라 새 줄을 안 만들고 `## 완료` 의 같은 회전 줄에 이어 붙였다 —
**새 색인 구멍 0**).

**계획 48 의 리뷰 1·개발 4 두 반복(256~257)은 `history_035.md`** (2026-09-02 회전,
291줄 → **자기 항목을 붙인 뒤 세어** 308줄을 보고 그 자리에서 돌렸다(열다섯 회전 연속
지각 0). **처음엔 리뷰 1 하나만 밀고 «개발 4 는 개발 5 가 읽어야 한다» 고 적었는데,
붙여 놓고 세니 308 이라 예고가 틀렸다** — 계획 47 회전이 «예고와 실측이 어긋난 첫
사례» 라 적은 것과 같은 자리다. 룰(«항목을 붙인 뒤 `wc -l` 로 센다»)이 예고를 이겼다.
**둘은 같은 반려 한 라운드**라 함께 미는 것이 경계로도 깨끗하고, 개발 5 가 필요한 값
(계수 0.118→0.352 · 캡 100,000→35,000)은 아래 리뷰 2 항목이 전부 다시 적는다.
명부에 `history_035.md` 를 더했고 `digest.md` 는 200줄 상한이라 새 줄을 안 만들고
`## 완료` 의 같은 회전 줄에 이어 붙였다 — **새 색인 구멍 0**).


## 2026-09-02 | passage-api | 테스트 2 | 시도0
- 한 일: 직전 반복이 넘긴 «형제 상한 셋이 값 변경에 눈멀어 있다» 를 **문서로 믿지 않고
  변이로 다시 쟀다.** 결과 **그 진단은 절반이 틀렸다** — 셋 중 둘은 이미 물려 있었고,
  대신 아무도 안 본 넷째(`REQUEST_TIMEOUT`)가 완전히 열려 있었다. 실측 변이 7건 전부
  `.mutation-lock` 아래, 전부 원복, `git diff -- src/` **0줄**(커밋된 변이 0).
- **변이 7건 실측**: V1 `MAX_QUERY` 200→20000 **RED(3)** · V2 `MAX_PAGE` 100→10000
  **RED(1)** · V3 `MAX_PASSAGE` 2000→5000 **GREEN(557 OK)** · V4 2000→20 **RED(2)** ·
  V5 2000→500 **GREEN(557 OK)** · V6 `PASSAGE_LIMIT` 10→11 **RED(3)** ·
  V7 `REQUEST_TIMEOUT` 10→600 **GREEN(557 OK)**.
- **왜 둘은 이미 살아 있었나** — `test_serve.py` 가 상수가 아니라 **리터럴**로 두드리기
  때문이다(`&page=101`, `"가" * 201`). 직전 반복이 지목한 «상수를 참조하는 fixture» 는
  `MAX_PASSAGE` 한 곳에만 해당했다. 규칙(«받은 진단을 그대로 믿지 않는다»)이 또 값을
  했고, **믿었으면 이미 물린 둘에 가드를 덧대고 진짜 구멍 둘은 그대로 뒀을 것이다.**
- **닫은 구멍 둘(≥8점)**: ① **`MAX_PASSAGE`** 는 대략 [40, 5999] 구간에서 **양방향**
  무저항이었다. V4 가 RED 인 것도 실력이 아니라 우연이다 — fixture 본문이 30자쯤이라
  20 으로 좁혀야 겨우 걸린다. 좁히면 RAG 소비자가 받는 근거 문단이 말 중간에서 잘리고
  (계획의 기능 7 그 자체), 넓히면 응답이 조용히 부푼다. 8,000자 본문을 넣고 **정확히
  2000** 을 받는지 본다. ② **`REQUEST_TIMEOUT`** 은 슬로로리스 가드가 `is not None`
  만 봤다 — **`is not None` 은 값을 안 붙든다.** 600 으로 넓혀도 557건이 전부 초록으로
  지나갔다(실측). 이 값은 공격자가 스레드 하나를 공짜로 붙잡는 초 수라 신뢰 경계이고,
  ponytail 의 «신뢰 경계 검증은 줄이지 않는다» 가 걸린다. `assertEqual(handler.timeout, 10)`
  으로 바꿨다.
- **가드는 공식이 아니라 리터럴로 적었다.** 상수를 다시 참조하면 W2·V3 와 같은 이유로
  상수와 함께 움직여 아무것도 안 잰다. 이미 살아 있는 둘이 리터럴이라 사는 것과 같은
  이유다.
- 재확인: 세 변이를 **다시 심어** 새 가드가 무는지 봤다 — `MAX_PASSAGE`=500 →
  `500 != 2000` · =5000 → `5000 != 2000` · `REQUEST_TIMEOUT`=600 → `600 != 10`.
  셋 다 원복 후 `git diff -- src/` **0줄**, `.mutation-lock` 제거.
- 결과: **단위 557건 OK(13.411초)** · `e2e/passage_eval.py` rc **0**(문서 64건·블록 253,
  질의 40 × 5회, 채택률 99.5% · 정확도 100.0% · p50 1.25ms · **p95 1.38ms**, 예산 500ms
  의 0.3%). 러너 호출 **14회 전부 맨몸·단독** — 파이프·`grep`·리다이렉션·명령 잇기 **0회**.
- 실측: 제품 `src/` **0줄**(테스트만 고쳤다: `tests/test_serve.py` +1 · ±1 형태 변경) ·
  새 파일 0 · 의존성 0 · 스키마·마이그레이션 없음 · `data/crawl.db` 무변경 ·
  `docs/specs/` 읽기 전용.
- 다음: **리뷰 2.** 볼 곳 — ① 상한 다섯 중 8점 미만이라 안 닫은 셋
  (`MAX_SNIPPET`·`PAGE_SIZE`·`VERSION`)이 리뷰 눈에도 같은 점수인지 ② 이 반복이 만든
  «값은 리터럴로 두드린다» 규칙이 저장소 전체에서 지켜지는지 ③ 계획 48 의 남은
  수용 기준.


## 2026-09-02 | passage-api | 리뷰 2 | 시도0
- 한 일: **리뷰 1 이 연 반려의 재확인.** 반려 사유(«`/passages` 재파싱 입력에 상한
  없음»)는 **닫혔다** — 입력은 이제 `MAX_PASSAGE_HTML` 로 묶인다. 그런데 **그 값을
  떠받치는 계수를 다시 재니 최악값이 아니었다.** 반려 사유는 닫혔고 **한 칸 아래에서
  같은 모양이 다시 났다** → **반려(medium 1건, 신뢰도 95).**
- **[반려 · medium · 95점] `0.118 ms/1,000자` 는 최악이 아니라 중간이다.**
  개발 4 는 계수를 *"실측 최악(태그가 촘촘한 한글)"* 이라 부르고 가드
  (`test_cap_and_passage_limit_together_stay_inside_the_budget`)에 리터럴로 박았다.
  **모양을 갈라 다시 쟀다**(DB 없이 `extract_blocks` 만, 문서 1건 100,000자, 중앙값 5회):
  ① 통짜 한글(`"봄"*N` — **테스트 fixture 가 실제로 만드는 모양**) **0.026** ·
  ② 문단 HTML(`<p>` 60자마다 — 개발 4 가 잰 모양) **0.130** ·
  ③ **인라인 촘촘**(`<b>`·`<i>`·`<em>` 이 낱말마다 — 위키·CMS·에디터 출력) **0.352**.
  «태그가 촘촘하다» 의 정의가 **문단 태그에서 멈췄다** — 인라인 마크업은 그 **2.7배**다.
- **그래서 가드가 보증하는 숫자가 틀렸다.** 가드는 `100,000/1,000 × 0.118 × 10 = 118ms
  ≤ 125ms`(예산 25%)로 **6% 여유**를 남기고 통과한다. 계수 ③ 을 넣으면 같은 식이
  **288ms = 예산의 58%**, 천장을 **2.3배** 넘는다. HTTP 로도 봤다(상한 크기 문서 10건,
  **짧은** 질의): p95 **790ms**, 그중 **문단 경로 몫 354ms = 예산의 71%**.
- **이것이 리뷰 1 이 반려한 바로 그 결함이다, 한 칸 아래에서.** 리뷰 1 의 문장은
  «e2e 코퍼스가 이 축을 못 밟아 초록불이 예산을 보증하지 않는다» 였다. 지금
  `passage_eval` 의 p95 **1.38ms(예산의 0.3%)** 는 여전히 그 축을 안 밟고(코퍼스 최대
  305자), 설계 갈림길 4 도 *"문서 크기 축은 여기 안 들어 있다"* 고 정직하게 적어 뒀다.
  **그러면 크기 축을 재는 것은 그 값 가드 하나뿐인데, 그 하나가 산수다** — 상수가
  바뀌면 죽지만(제 몫은 한다) **계수가 틀린 것은 영원히 못 본다.**
- **처방(개발 5)**: 계수를 실측 최악 **0.352** 로 고치면 가드가 즉시 RED(288ms > 125ms)
  가 된다. 그것을 초록으로 되돌리는 손잡이는 **`MAX_PASSAGE_HTML` 을 약 35,000 으로**
  내리는 것이다(35 × 0.352 × 10 = 123ms ≤ 125ms). 사양 성능 5 가 *"못 지키면 문단 수를
  줄이지 예산을 늘리지 않는다"* 고 못박았으므로 **예산 쪽은 손대지 않는다.**
- **재 보고 버린 것 하나** — 백지 패스가 «`MAX_QUERY` 200자면 needle 132개라 채점이
  O(needle × 본문)» 를 냈고 합성 코퍼스에서 `search()` **2,020ms** 가 나왔다. 그런데
  **동결 코퍼스 64문서로 다시 재니 어절 66(197자)이 `search` 5.2ms · 문단 몫 0.7ms**
  였다 — 앞의 숫자는 «같은 문서 10벌» 이 만든 FTS 아티팩트였다. 반복 256 의
  «재 보니 없음» 과 같은 칸이다. **내 자신의 1차 가설도 같은 자에 걸어 버렸다.**
- **재확인한 것(전부 프로세스 밖)**: `/passages` 200·400(q 없음·201자·`page=2`·
  `page=0`)·404 **전부** `application/json; charset=utf-8` + `nosniff` + `version:1` ·
  오류 본문에 **경로·트레이스백·내부 식별자 0** · 404 의 경로 반사는 JSON 인코딩 +
  json CT 라 실행 경로 없음(계획 46 것, 이 diff 밖) · **`/search` 계약 무변경**
  (키 `has_next·page·query·results·version`, `results[0]` = `snippet·title·url`).
- 결과: **단위 557건 OK — 맨몸·단독 3연속(13.534초 · 13.405초 · 13.353초)** ·
  `e2e/passage_eval.py` rc **0**(채택률 99.5% · 정확도 100.0% · p50 1.26ms · p95 1.38ms).
  러너 호출 4회 전부 맨몸·단독 — 파이프·`grep`·리다이렉션·명령 잇기 **0회**.
- 실측: 제품 `src/` **0줄**(`git diff -- src/` 로 확인 · 커밋된 변이 0) · 새 파일 0 ·
  의존성 0 · 스키마·마이그레이션 없음 · `data/crawl.db` **읽기만**(mtime·크기 그대로) ·
  `docs/specs/` 무변경. 탐침은 전부 스크래치패드 사본이다.
- 다음: **개발 5(리뷰 반려분).** 계수 0.118 → 0.352, `MAX_PASSAGE_HTML` 100,000 →
  35,000. 볼 곳 — ① 가드를 산수로 둘지 실측으로 바꿀지(타이밍 단언은 흔들린다) ②
  fixture 가 상수에서 만들어지는 모양(W2)이 새 값에서도 눈멀지 않는지.

## 2026-09-02 15:20 | passage-api | 개발 5 | 시도0

- 한 일: 반려 처방을 그대로 적용했다. **RED 를 먼저 눈으로 봤다** — 가드
  (`tests/test_indexer.py`)의 계수만 0.118 → 0.352 로 고치니 실패했다
  (`AssertionError: 352ms not less than or equal to 125.0`). **기대 288ms · 실제 352ms**:
  288 은 «한글·태그 촘촘» 을 **잰** 값, 352 는 계수를 식에 넣은 **산수**이고 가드가
  판정하는 것은 산수다. 그다음 `indexer.MAX_PASSAGE_HTML` 100,000 → **35,000** 으로
  초록(123ms ≤ 125ms). 제품 `src/` 동작 **1줄** + 주석.
- **계수를 직접 다시 쟀다**(모양 3종 × 두 길이 · 중앙 5회): 태그 성긴 영문 **0.070** ·
  한글·태그 촘촘 **0.285** · 인라인 촘촘(위키·CMS) **0.327** ms/1,000자. 리뷰의 0.352
  보다 낮게 나왔고 **낮은 쪽으로 안 내렸다**(가드 계수는 최악이라야 거짓말을 안 한다).
  값 옆에 **모양을 적었다** — `digest [8]` 의 «어떤 세계에서 잰 숫자인지를 적는다».
- **0.352 도 최악이 아니다 — 새로 연 축.** `<p>가</p>` 만 반복하는 합성 문서가
  **0.489**(35,000자 ×10건 = 171ms > 125ms) — 블록 수가 비용을 끈다. 골라 만든 모양이라
  실물 근거가 없어 **계수로 안 썼고** 주석·설계서에 적었다. 보이면 고칠 곳은 파서다.
- **예산을 프로세스 밖에서 확인했다**(10만자 인라인 촘촘 10건 · HTTP): `/passages` p95
  **199.9ms(40%)** · **문단 몫 118.3ms** 로 가드의 123ms 와 맞는다(리뷰 2 는 **790ms·158%**).
- **사양 기능 8 은 안 움직인다(실측).** 동결 코퍼스의 포장 HTML 은 최대 **409자**(캡의
  1/85.6)라 잘리는 문서 **0건** · `passage_eval` 도 정확도 100.0% · 채택률 99.5% · p95
  1.39ms 로 직전과 같다. **천장이 3분의 1로 내려온 실물 대가는 못 쟀다** — 실물
  `data/crawl.db` 가 2문서 × 66자다(읽기만 했다).
- 결과: 단위 **557건 OK(13.338초)** · `passage_eval` rc **0** · `search_api_e2e` rc
  **0**(p95 2.12ms) · 러너 4회 전부 **맨몸·단독**(다섯 반복 연속) · 새 파일 0 · 의존성
  0 · 스키마·`docs/specs/` 무변경 · 건수 불변이라 README 그대로. 변이는 안 심었다 —
  **RED 로 본 352ms 가 곧 W2(캡 넓히기)를 죽인다는 증거**다.
- 다음: **테스트 3.** ① 0.489 축을 테스트로 세울지 ② **회전**(300줄 · 상한 300).
