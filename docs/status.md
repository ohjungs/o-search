---
signal: GREEN
phase: 테스트
step: 3
attempt: 0
iteration: 260
updated: 2026-09-02
ctx: 58
night_iterations: 105
night_red: 0
night_retries: 0
plan: passage-api
---

# 현재 상태

**리뷰 2 가 반려한 계수를 고치고 캡을 내렸다 — 예산이 프로세스 밖에서도 닫혔다
(790ms → 199.9ms). 개발 5 완료, 테스트 3 으로 넘긴다.**
활성 계획 `passage-api` · 설계서 `docs/design_passage-api.md` · 브랜치 `loop/passage-api` ·
기점 `6e99031`. **단위 557건 OK(13.338초)** · `e2e/*.py` **20종** ·
이번 반복의 제품 `src/` **동작 1줄**(+주석).

## 이번 반복이 한 것 (개발 5 — 리뷰 반려분)

**RED 를 먼저 눈으로 봤다**(`rules/dev.md` 0절). 가드의 계수만 `0.118 → 0.352` 로 고친
상태에서 `AssertionError: 352ms not less than or equal to 125.0`. **기대와 실제가 한 곳
갈렸다** — 반려 문서는 288ms 를 예고했는데 실제는 352ms 다. 288 은 «한글·태그 촘촘» 을
**잰** 값이고 352 는 계수를 식에 넣은 **산수**인데, 가드가 판정하는 것은 산수다.
그다음 `indexer.MAX_PASSAGE_HTML` 을 `100,000 → 35,000` 으로 내려 초록(123ms ≤ 125ms).

- **계수를 직접 다시 쟀다**(모양 3종 × 두 길이 · 중앙 5회): 태그 성긴 영문 **0.070** ·
  한글·태그 촘촘 **0.285** · 인라인 촘촘(위키·CMS) **0.327** ms/1,000자. 리뷰의 0.352
  보다 낮았지만 **낮은 쪽으로 안 내렸다** — 가드 계수는 최악이라야 거짓말을 안 한다.
  값 옆에 **어느 모양에서 잰 것인지**를 주석·설계서·`project.md` 에 함께 적었다.
- **예산이 실제로 닫혔다(프로세스 밖 HTTP).** 10만자 인라인 촘촘 문서 10건에
  `/passages` p95 **199.9ms(예산의 40%)** · `/search` 81.7ms · **문단 경로 몫 118.3ms**
  로, 가드가 약속한 123ms 와 맞는다. 반려 시점은 790ms(158%) · 문단 몫 354ms 였다.
- **사양 기능 8 은 안 움직인다(실측).** 동결 코퍼스의 포장된 HTML 은 최대 **409자**로
  캡의 **1/85.6** 이라 잘리는 문서 **0건** — `passage_eval` 도 정확도 100.0% · 채택률
  99.5% · p95 1.39ms 로 직전과 같다. 캡은 CI 숫자를 건드리지 않는다.

검증은 전부 맨몸·단독 호출이다(파이프·리다이렉션·명령 잇기 **0회** · 다섯 반복 연속):
`PYTHONPATH=src python3 -m unittest discover -s tests` **557건 OK(13.338초)** ·
`e2e/passage_eval.py` rc **0** · `e2e/search_api_e2e.py` rc **0**(p95 2.12ms).
변이는 따로 안 심었다 — **RED 로 본 352ms 가 곧 W2(캡 넓히기)를 죽인다는 증거**다.
새 파일 0 · 의존성 0(stdlib) · 스키마·마이그레이션 없음 · `data/crawl.db` **읽기만** ·
`docs/specs/` 무변경 · 단위 건수 불변(557)이라 README 카운트 그대로.

## 넘긴 것 — 테스트 3 이 볼 곳

1. **[열린 축] 0.352 도 최악이 아니다.** `<p>가</p>` 만 반복하는 합성 문서가
   **0.489 ms/1,000자**(35,000 × 10 = **171ms > 125ms**)다 — 비용을 끄는 것은 문자 수가
   아니라 **블록 수**다. 계수로 안 쓴 이유는 그 모양이 실물이 아니라 **골라 만든 것**
   이어서다(쓰면 «누가 최악을 정했나» 가 또 코드 밖에 남는다). 설계 갈림길 5 에
   대안 셋과 판정을 적어 뒀고, **닫는 조건은 실물 크기 분포**다 —
   `data/crawl.db` 는 2문서 × 66자라 저장소에 그 분포가 **없다**.
2. **천장이 3분의 1로 내려온 실물 대가는 아직 아무도 안 쟀다.** «잘린 뒤의 문단은
   근거로 못 나온다» 는 `ponytail:` 주석에 있고 동결 코퍼스는 캡의 1/85.6 이라
   이 축을 못 밟는다. 사양 기능 7 과 충돌하지는 않는다(위치 없는 문단 0건은 그대로).
3. **집안일 — 회전이 이제 방아쇠다.** `history_current.md` 가 이 항목으로 **정확히
   300줄**(상한 300)이다. `digest.md` 도 **정확히 200줄**이라 넣으려면 닫힌 항목을
   지워 자리를 만들어야 한다. **명부 줄·규약 줄은 지우지 않는다.**
- 남은 확장 둘: **속도 제한**(사람이 시점을 정한다) · `recrawl`(원래 사양의 마지막).

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-02 확인)

`origin/main` 은 PR #6 으로 계획 44~47 까지 받았다. 계획 48 의 커밋들은 브랜치에만 있다.
**이번 반복이 `origin/loop/passage-api` 로 푸시했다** — 직전 반복이 «저장소 밖 외부
네트워크» 한도를 오독해 `c6d6430` 을 안 올린 채 뒀던 것을 함께 올렸다. 푸시는 루프의
정상 절차다(강제 푸시만 금지).
**남은 것 (사람 결정)**: 계획 48 이 끝난 뒤 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 — 넷이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, `~/.claude/skills/loop-harness/rules/docs.md` 에는 그 예외가 없다.
   **저장소 밖이라 루프가 못 고친다.**
4. **«러너 호출을 다른 것과 섞지 않는다» 를 문장이 아니라 훅으로 옮기는 일.**
   재발 **18회**. 저장소 쪽 자리(`docs/project.md ## 명령`)에 이미 적혀 있는데도 났고,
   남은 자리는 `~/.claude/skills/loop-harness/rules/` 라 **저장소 밖이라 루프가 못 고친다.**
   이번 반복도 4회를 0 으로 끝냈지만 그것이 항목을 닫지는 않는다 — 닫으려면
   **판정 명령을 단독 실행으로 강제하는 PreToolUse 검사**가 필요하다.

**사양이 사람에게 남긴 것 셋** (`docs/specs/concept.md` 의 `## 사람이 정할 것`):
속도 제한 시점(가정 — IP 당 분당 60회) · JSON 검색과 근거 문단의 분할(가정 — 쪼갠다,
**계획 46 이 그 가정을 따랐고 닫았다**) · 사양 숫자들이 초안이라는 것(90% · 500ms ·
60회/분). 첫째와 셋째는 아직 열려 있다.
