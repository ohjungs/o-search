# 반복 기록 아카이브 027

`history_current.md` 에서 밀려난 항목. **수정·삭제 금지.**
계획 46 `json-contract` 의 테스트 1/1 · 리뷰 1/1 두 반복(반복 240~241, 2026-09-02 회전).

## 2026-09-02 03:20 | json-contract | 테스트 1/1 | 시도0

- 한 일: **설계 변이 표 M1~M14 를 전수 실물 대조하고, 표가 못 본 갭 하나를 메웠다.**
  `tests/test_serve.py` 에 `TestBrokenDbStays500` 3건. 제품 `src/` **0줄** ·
  새 파일 0 · 의존성 0. 새로 쓴 테스트가 아니라 **빠뜨린 것**을 찾는 phase 대로 갔다.
- **M1~M14 열넷을 제자리에 심어 전부 죽는 것을 봤다**(`.mutation-lock` 아래, 각 변이마다
  원문 복원·치환 개수 사전 단언·러너 재실행). 열넷 중 **살아남은 변이 0건**이다 —
  설계가 *"새로"* 라 적은 열하나는 개발 세 스텝이 실제로 심었고, *"오늘 0건"* 이라 적은
  셋(M3·M5·M11)도 이제 각각 2·4·1건을 죽인다. 어느 단언이 죽는지도 이름으로 확인했다
  (예: M14 → `test_screen_and_json_list_the_same_urls_in_the_same_order` 하나만).
- **표가 못 본 갭 하나 — 중요도 8, 그 자리에서 메웠다.** 두 경로의 500 을 재는 단언
  둘은 `_page_hits` 를 **mock 으로** 갈아 `AttributeError` 를 던진다. 그래서 못박힌 것은
  *"코드가 틀렸을 때 500"* 뿐이고 **실물 DB 상태로 500 을 재는 단언은 0건**이었다.
  M5(`except Exception` 을 통째로 503)는 그 mock 이 잡지만, **503 튜플에
  `sqlite3.DatabaseError` 를 더하는 변이**(*"DB 오류도 인프라니 재시도"*)는 스위트를
  **하나도 못 죽인다** — 넓히는 변이가 튜플 쪽으로 들어오면 M5 의 사각지대다.
- 그래서 변이 둘을 새로 세워 실물로 확인했다 — **M15**(양쪽 튜플에 `DatabaseError` 추가)
  → 새 단언 3건 사망 · **M16**(같은 넓힘을 화면 쪽에만) → 화면 단언 1건 사망.
  M16 이 화면 쪽 테스트가 군더더기가 아님을 보인다(갈림길 C 의 «두 경로 한 벌»).
  손상은 기다린다고 낫지 않는다 — 503 은 *"색인을 다시 돌리면 낫는다"* 는 약속이라
  거기 넣으면 인프라가 영영 재시도한다.
- **설계서 §1 의 예외 이름 하나가 실물과 다르다(탐침으로 정정).** 못 읽는 DB 는
  `PermissionError` 가 아니라 `sqlite3.OperationalError('unable to open database file')`
  로 온다 — `DatabaseError` 의 하위라 **잡히는 자리는 같지만** 예외 이름으로 짜는 단언은
  빗나간다. 새 클래스 독스트링에 이 측정을 적어 뒀고, 손상 DB 는
  `DatabaseError('file is not a database')` 다. 권한 테스트는 `os.geteuid() == 0` 이면
  건너뛴다 — root 는 파일 권한을 무시해 «못 읽는 DB» 자체가 안 만들어진다.
- **8점 미만 갭 하나는 `digest.md ## 다음 계획 후보 (테스트 phase 갭, 8점 미만)` 에
  남겼다** — `[4] indexer.search` 의 `os.path.exists` → `connect` 사이 TOCTOU(그 창에서
  DB 가 지워지면 503 이 아니라 200 이 된다). **`serve` 가 아니라 `indexer` 를 고칠 일**이라
  계획 47 이 `indexer` 를 열 때 같이 본다. `digest.md` 는 200줄 상한이라 새 줄을 안
  만들고 절 안의 빈 줄 하나를 항목으로 바꿨다 — **줄 수 200 그대로**.
- **파이프 2회 — 연속 0 이 또 끊겼다(정직하게 적는다).** 첫 전체 실행에 `| tail -20`,
  문서 가드 확인에 `| tail -0` 을 붙였다. 잘려서 못 본 것은 없었지만(`Ran/OK` 와 실패
  한 줄이 둘 다 tail 안에 들어왔다) **그것은 운이다** — 직전 반복들이 적어 둔 그대로다.
  **아래 수치는 전부 맨몸 재실행의 것**이고, 변이 16회 판정은 스크립트가 러너를
  `subprocess` 로 직접 부른 것이라 파이프가 없다.
- 결과: 맨몸 실측 `PYTHONPATH=src python3 -m unittest discover -s tests` → **495건 OK**
  (12.237초). e2e 넷(`search_api`·`pagination_ui`·`tokenizer`·`perf_search`) 전부 rc=0 ·
  p95 **8.79ms**(예산 300ms 의 2.9% · 기준선 8.86ms). `data/crawl.db` sha256
  `85c96744…` **무변경**(변이·탐침은 전부 임시 디렉터리) · `.mutation-lock` 없음
  (`ls` 로 확인) · 되돌림은 해시가 아니라 **`python3 -B` 재실행 초록**으로 확인 ·
  `git status` 에 `src/` 수정 **0건**.
- 기록 갱신: `README.md` 카운트 492 → **495**(가드가 네 반복 연속 울었다) ·
  **회전 돌렸다**(설계·개발 네 반복 → `history_026.md`, 290줄 → 116줄) ·
  `digest.md` 명부에 `history_026.md` 추가 + 같은 회전 줄에 이어 붙임(새 줄 0) ·
  `status.md`(반복 240 · phase 리뷰) · `metrics.md`(반복 240 · 테스트 phase 24→25) ·
  `index.md`(계획 46 행에 테스트 반영).
- 다음: **리뷰 phase** — 제품 `src/` 는 이번 반복에 0줄이니 볼 것은 계획 46 전체의
  diff(503 가르기 · `VERSION` · 두 계약 단언)와 문서 넷의 정합이다. 리뷰가 볼 자리
  하나를 미리 적어 둔다: **`TestSchemaVersion.test_503_and_500_carry_it_too` 와
  `TestProgrammingErrorStays500` 이 같은 mock 을 두 벌 쓴다** — 합칠지 남길지는
  «재는 것이 다르다»(버전 / 상태 코드)로 갈리는 판단이라 리뷰의 몫이다.

## 2026-09-02 03:23 | json-contract | 리뷰 1/1 | 시도0

- 한 일: 계획 46 전체 diff(`3d766be..HEAD` — 제품 `serve.py` 순증 **17줄**, 나머지는
  테스트·문서)를 `rules/review.md` 대로 **백지 패스 먼저** 읽고 그다음 계획·설계 대조
  패스를 열었다. 제품 `src/` **0줄 수정** — 이번 반복이 고친 것은 기록 문서뿐이다.
- **백지 패스에서 가설 셋을 세웠고 대조 패스가 둘을 죽였다.** ① `503` 에
  `Retry-After` 가 없다 → **기각**: 설계서 5절이 *"`Retry-After` 는 안 붙인다 — 색인이
  언제 도는지 서버가 모른다"*, 8절이 같은 사유를 `## 범위 밖` 에 한 번 더 적어 뒀다.
  ② 미색인 DB 가 200 이라 사양 디자인 5 의 *"503 = 색인이 없다"* 와 어긋난다 →
  **기각**: 갈림길 D 의 명시적 결정이고 `TestUnindexedDb` 독스트링·`README.md` 가 둘 다
  «503 은 DB 파일 없음·옛 색인» 이라고 **정직하게 열거**한다(넓게 적어 놓고 안 지키는
  꼴이 아니다). ③ 만 살아남았다 — 아래 [B].
- **[A] 변이 이름 M15·M16 이 두 개를 가리킨다 (low · 자동 적용).** `design_json-contract.md`
  8절 `## 범위 밖` 이 **M15**(`ORDER BY` 흔들기)·**M16**(탐침 행 누출)을 이미 쓰고 있는데,
  직전 반복이 **새로 세운 변이 둘**에 같은 두 이름을 다시 붙였다(바로 위 항목). 저장소
  자신의 규약이 *"append 전용 문서는 **이름·항목**으로 인용한다"*(`DocCitationTest`)라
  이름 하나가 둘을 가리키면 그 규약이 무너진다. **이번 반복부터 M17**(양쪽 503 튜플에
  `sqlite3.DatabaseError` 추가 → 3건 사망)**·M18**(같은 넓힘을 화면 경로에만 → 1건 사망)
  **로 부른다.** 위 항목은 **수정·삭제 금지**라 손대지 않았다 — 정정을 여기 append 하고
  `status.md` 를 새 이름으로 적었다. 설계서 8절의 M15·M16 은 그대로 범위 밖 둘이다.
- **[B] FTS 토큰이 안 나오는 질의는 DB 고장 판정을 통째로 건너뛴다 (low · 이 diff 가
  만든 것이 아니다).** 임시 디렉터리 탐침 실측: `indexer.search` 는 `_fts_query(query)` 가
  빈 문자열이면 `sqlite3.connect` **앞에서** `return []` 하므로 **손상된 DB + `q=%01` 은
  200**(같은 DB 에 `q=김치` 는 `DatabaseError` → 500). 제어문자는 `_parse` 의 `strip()` 을
  통과하고(파이썬 `strip()` 은 공백만 지운다) `_fts_query` 가 지운다. **없는 DB 는
  `os.path.exists` 가 먼저라 어느 질의든 503 그대로**여서 새 503 계약 자체는 안 샌다.
  계획 46 이 세운 «어떤 고장에 어떤 코드» 계약에 안 적힌 예외지만 **이 diff 이전에도
  같은 갈래**였고(그땐 500 vs 200), 고칠 자리는 `serve` 가 아니라 `indexer.search` 다.
  그래서 `digest.md ## 다음 계획 후보 (테스트 phase 갭, 8점 미만)` 의 `[4]` TOCTOU 항목에
  **같은 뿌리로 이어 붙였다**(새 줄 0 — `digest.md` 200줄 그대로). 계획 47 이 본다.
- **직전 반복이 리뷰에 넘긴 판단에 답했다 — 두 테스트를 안 합친다.**
  `TestSchemaVersion.test_503_and_500_carry_it_too` 와 `TestProgrammingErrorStays500` 이
  같은 mock 을 두 벌 쓰는 것은 맞지만 **재는 계약이 다르다**(«오류 응답도 버전을 갖는가»
  / «프로그래밍 오류가 503 으로 새지 않는가»). 합치면 한쪽 계약이 죽었을 때 다른 쪽까지
  같이 빨개져 원인이 흐려진다. `severity.md ## 지적하지 말 것` 의 *"일관성만을 위한 변경"*
  이 정확히 이 자리다 — 중복은 mock 두 줄이고 값은 계약 둘의 분리다. **남긴다.**
- **Pass 1(critical) 은 전부 비었다.** 데이터 안전(쓰기 0 · `data/crawl.db` 무변경) ·
  레이스(새 공유 상태 0, `VERSION` 은 모듈 상수) · 주입(503 본문은 양 경로 다 고정
  리터럴, 화면 쪽 `typed` 는 `html.escape` 를 지난다) · 신뢰 경계(DB 경로·트레이스백
  누출 0 — JSON·HTML 네 단언이 못박는다).
- **열거형 완전성(새 상태 코드 503)을 diff 밖까지 전수로 봤다** — `README.md` ✓ ·
  `e2e/tokenizer_e2e.py` ⑥ ✓ · `docs/specs/concept.md` 디자인 5 는 처음부터 503 ✓ ·
  `docs/project.md` 는 serve 상태 코드를 열거하지 않는다 ✓. **빠뜨린 곳 0.**
  `e2e/search_api_e2e.py` 만 상태 코드 표가 400·404·501 이고 503 도 `version` 도 안
  재는데, **결함이 아니라 다음 phase 의 일**이라 `status.md` 에 세 줄로 적어 넘겼다.
- 결과: 맨몸 실측 `PYTHONPATH=src python3 -m unittest discover -s tests` → **495건 OK**
  (12.213초). 문서 수정 뒤 한 번 더 **495건 OK**(12.090초), 마감에 또 한 번
  **495건 OK**(12.250초), 정정 문서를 넣고 마지막으로 **495건 OK**(12.193초).
  **러너 호출 5회 = 맨몸 4 + 파이프 1.** 마지막 맨몸이 **정정된 세 문서를 읽은 뒤**라
  자기 가드 넷이 정정본 위에서 초록이고, 그 뒤에 바꾼 것은 이 횟수 숫자뿐이다.
- **파이프 1회 — 또 끊겼다. 13번째이고 이번 자리가 가장 나쁘다(정직하게 적는다).**
  맨몸 둘을 끝내고 `status.md` 를 고친 뒤 «한 번만 더 확인» 하는 **마감 실행**에
  `2>&1 | tail -4` 를 붙였다. `Ran/OK` 줄이 **실제로 잘려 안 보였고**(이전 사례들은
  운으로 tail 안에 들어왔었다) 그것을 알아채고 **즉시 맨몸으로 재실행해 495건 OK 를
  눈으로 읽었다.** **하필 같은 반복의 `status.md` 에 방금 «파이프 0회 — 연속이 다시
  섰다» 라고 적어 둔 뒤였다** — 기록이 자기를 거짓으로 만든 첫 사례다. 두 문서를 전부
  정정했고 `digest.md ## 반복 실패` 의 파이프 항목에 **⑬** 로 남겼다(새 줄 0).
  **방아쇠가 또 «이미 초록일 것 같은 실행»** 이고, 열세 번 중 넷이 그 종류다 —
  조항이 «판정 실행» 을 겨냥해 **마감·재확인 실행이 예외처럼 읽히는 것**이 뿌리로 보인다.
  룰 파일은 저장소 밖이라 루프가 못 고친다(사람 몫). 백그라운드 0 · 변이 0.
  제품 `src/` **+0줄** · 새 파일 0 · 의존성 0 · `.mutation-lock` 없음 · 변이 0회 ·
  `data/crawl.db` sha256 `85c96744…` **무변경**(리뷰 탐침은 임시 디렉터리에서만 돌았다) ·
  `README.md` 카운트 가드 **조용함**(495 그대로 — 다섯 반복 만에 처음).
- 기록 갱신: `digest.md`(`[4]` 항목에 형제 구멍 추가 · **200줄 그대로**) ·
  `status.md`(반복 241 · phase e2e · M17·M18 로 정정 · 다음 phase 가 볼 세 자리) ·
  `metrics.md`(반복 241 · 리뷰 phase 23→24) · `index.md`(계획 46 행에 리뷰 반영) ·
  **회전 안 돌렸다**(`history_current.md` 상한 300줄 · 아카이브 26개 · 명부 구멍 0).
- 다음: **e2e phase** — `status.md ## 다음 — e2e phase 가 채울 자리` 의 셋이다.
  ① `search_api_e2e.py` 상태 코드 표에 «DB 없음 → 503» 한 줄(드리프트는 `tokenizer_e2e`
  ⑥ 이 이미 화면으로 잰다 — 겹쳐 만들지 않는다) ② 같은 파일에 `first["version"] == 1`
  한 줄 ③ p95 를 기준선 8.86ms 와 비교해 e2e 문서에 적는다(설계 7절 판정 ②).
  **새 스크립트 0 · 새 의존성 0** 이 셋 다의 전제다.
