# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

---
## 반복 38 — 2026-08-25 · 계획 (crawl-delay)
- `docs/plan_crawl-delay.md` 작성 — robots `Crawl-delay` 를 도메인 간격에 반영, 스텝 4개
- 계획 단계에서 stdlib 을 **실측**했다: `RobotFileParser.crawl_delay` 가 소수를 버리고(`3.5`→`None`), 상한이 없다(`86400`→`86400`). 둘 다 설계로 넘겼다
- 배선 제약 확인: robots 는 그 도메인 첫 URL 을 팝한 **뒤** 적재된다 (`crawl.py` 루프 순서) — 간격을 프런티어가 미리 알 수 없다
- 설계 필요(파일 3개·`Frontier` 인터페이스 추가·대안 갈림) → 다음 반복 phase: 설계

## 반복 39 — 2026-08-25 · 설계 (crawl-delay)
- `docs/design_crawl-delay.md` — 대안 3개(밀어넣기/당기기/루프 sleep) 비교, **A 밀어넣기** 채택. 간격 판정을 프런티어 한 곳에 유지하는 유일한 안이고 가장 적게 쓴다
- 갈림길 2개를 컨셉 우선순위(크롤 윤리 1순위)로 갈랐다: 소수 Crawl-delay 는 **본문에서 직접 긁는 폴백**(느린 쪽으로만 틀린다), 30초 초과는 **도메인 폐기**(깎으면 위반이다)
- 가정 탐침(커밋 없음): 첫 팝 뒤 `set_delay` 를 불러도 두 번째 팝부터 먹는다 → 참. 덤으로 `next()`·`seconds_until_ready()` 가 모듈 상수를 직접 읽어 **메서드 추가만으로는 안 먹는다**는 것을 확인
- 다음: 개발 1/4 (`RobotsCache.delay`)

## 반복 40 — 2026-08-25 · 개발 1/4 (crawl-delay)
- `RobotsCache.delay(url) -> float | None`. `allowed()` 와 같은 캐시 1회를 쓴다. 테스트 6건 추가, 130/130
- 소수 폴백 구현: stdlib 이 `Crawl-delay: 3.5` 를 버리므로 본문에서 정규식으로 긁어 **가장 느린 값**을 쓴다
- 변이 검사 2건: 폴백 제거 → 잡힘. `max`→`min` → **안 잡혀서** 여러 줄 케이스 테스트를 추가하고 다시 확인(잡힘)
- **함정 기록**: 같은 길이 변이(`max`↔`min`)는 되돌려도 `__pycache__` 가 옛 .pyc 를 재사용해 실패가 남는다. `docs/project.md` 에 `PYTHONDONTWRITEBYTECODE=1` 로 적었다

## 반복 41 — 2026-08-25 · 개발 2/4 (crawl-delay)
- `Frontier.set_delay(domain, seconds)` + `_interval(domain)`. `next()`·`seconds_until_ready()` 가 도메인별 간격을 본다. 테스트 5건, 135/135
- 하한 `DOMAIN_INTERVAL`(사이트가 1초를 풀어줄 수 없다), `MAX_DELAY = 30.0` 초과는 큐를 비우고 이후 `add()` 도 거부
- 변이 4건(하한 제거·폐기 대신 깎기·대기시간·재유입) 전부 잡힘. 처음엔 하한 변이가 안 잡혀 시계를 0.5초 밀도록 테스트를 고쳤다
- **사고 1건**: 변이를 `git checkout` 으로 되돌려 **미커밋 구현을 통째로 날렸다.** 재작성 후 커밋 먼저 하고 사본으로 되돌리는 방식으로 바꿨다. RED 는 아니다 — 같은 반복 안에서 복구

## 반복 42 — 2026-08-25 · 개발 3/4 (crawl-delay)
- `crawl.crawl()` 이 `robots.allowed()` 직후 `frontier.set_delay(netloc, robots.delay(url))`. 3줄 + 주석. 138/138
- 테스트 3건: 선언 간격이 실제 요청 간격이 된다 / 지시 없으면 1초 그대로 / 지킬 수 없는 간격(3600)은 **첫 접촉 뒤 그 도메인을 더 안 간다**
- 계약 하나를 명시했다: **첫 요청은 어떤 간격도 어기지 않는다** — 어길 수 있는 것은 두 번째부터라, 폐기 도메인도 첫 페이지는 받는다
- 변이(배선 제거) 잡힘. 다음: 개발 4/4 e2e

## 반복 43 — 2026-08-25 · 개발 4/4 (crawl-delay)
- `e2e/crawl_delay_e2e.py` — 4.6s 통과. `127.0.0.1`/`localhost` 를 **두 도메인**으로 쓰고 Host 헤더로 robots 를 가른다(2/0초)
- 서버 수신 시각으로 잰다: `Crawl-delay: 2` 는 2초 이상, `Crawl-delay: 0` 은 1초 이상이면서 2초 미만(**남의 간격이 새지 않는다**)
- 변이(간격 무시)로 e2e 가 실패하는 것까지 확인 — 돌아가는 것과 잡는 것은 다르다
- `docs/project.md` 에 명령·품질 기준 등록. 스텝 4/4 완료 → 다음은 테스트 phase

## 반복 44 — 2026-08-25 · 테스트 phase (crawl-delay)
- 전체 140/140. 갭 2건(8점 이상) 보강: `MAX_DELAY` 경계(30.0 유지/30.1 폐기), 망가진 `Crawl-delay` 값 5종
- **내 예상이 틀린 것을 테스트가 잡았다**: `5s` 를 "지시 없음"으로 보면 1초로 **빨라진다**. 규칙을 주석에 못박음 — 느린 쪽으로만 틀린다
- 8점 미만 3건은 digest 로: 폐기 되돌리기 없음[6], `delay()` 팝마다 호출[5], robots.txt 는 간격 밖[4]
- 다음: 리뷰 phase

## 반복 45 — 2026-08-25 · 리뷰 phase (crawl-delay)
- 백지 패스(서브에이전트, 계획·설계 안 줌) 지적 10건. **전부 실재했다.** 8점 이상 2건 + 값싼 4건 수정, 3건은 digest 로
- **[7] http/https 가 서로의 간격을 덮어썼다** — robots 캐시 키는 `scheme://netloc`, 프런티어 키는 `netloc`. `set_delay` 를 늘어나는 방향으로만 움직이게 고쳤다(docstring 이 이미 그렇게 약속하고 있었다)
- **[6] 폴백이 남의 UA 그룹 값을 집었다** — 다른 봇의 86400 을 우리 값으로 읽어 지킬 수 있는 사이트를 버렸다. `_applicable_delay()` 가 우리 그룹 → 없으면 `*` 만 본다
- 그 외: 폐기를 stderr 로 알림, `1e3`→1000초, docstring/잔여 상태/e2e 잡티. 146/146 + e2e 통과

## 반복 46 — 2026-08-25 · e2e phase (crawl-delay) → **DONE**
- 앞 관문: 146/146 + 기존 e2e 4개 회귀 없음. 새 e2e 포함 5개 전부 통과
- 시나리오 5건 전부 통과: 2초 요구 → 2.01s / 6페이지 누락 0 / 지시 없으면 1초 / `Crawl-delay: 0` 도 1초 / 남의 간격 안 샘
- 변이(간격 무시)로 e2e 가 실패하는 것까지 확인. `docs/e2e/crawl-delay/result.md` 기록
- 아카이브: `plan_history_005.md` · `design_history_005.md`, `index.md`·`digest.md` 갱신. 계획 5개 완료

## 반복 47 — 2026-08-25 · 계획 (quality-eval)
- 한 일: `docs/plan_quality-eval.md` 작성(스텝 4, 설계 필요 판정) + 브랜치 `loop/quality-eval` 분기 + `docs/index.md` 한 줄. 함께 `history_current.md` 회전(반복 1~37 → `docs/history_001.md`, 37회/273줄이 상한 초과)과 `digest.md` 완료 절에 빠져 있던 crawler-core(001) 보충
- 결과: 착수 전 기준선 146/146 통과. 코드 변경 0줄. 계획 근거는 `docs/index.md` 사양 분할 7번(concept 기능 2, 정답 포함률 80%)
- 다음: 설계 phase — `docs/design_quality-eval.md`. 답할 것 셋: 방해 문서 설계 / 정답이 1건인가 집합인가 / fixture 위치·형식
## 반복 48 — 2026-08-25 · 설계 (quality-eval)

- `docs/design_quality-eval.md` — 갈림길 셋을 다 닫았다: 토픽 클러스터 코퍼스(64문서) / **정답 1건** / `e2e/quality/*.json` 2파일
- **탐침이 설계를 바꿨다**: `_fts_query` 다어절은 **AND** 라 `김치찌개 맛집` 이 0건. 2어절 질의는 매치가 10 밑으로 떨어져 측정 자체가 불능이 된다 → **1어절 원칙**으로 고침
- 같은 탐침에서 확인: 조사 `김치찌개를` **잡힘**(접두 `*` 가 하는 일) / 복합어 `돼지김치찌개` 못 잡음 / 띄어쓰기 `김치 찌개` 못 잡음
- 거짓말 방지를 **러너 가드 G2**(질의당 매치 ≤ 10 이면 종료 코드 2, 측정 불능)로 못박았다 — `limit=10` 이라 10건 이하면 정답은 구조적으로 항상 상위 10 안이다
- 계획서 오기 수정(`SCHEMA` 열 `text`→`body`), 스텝 3 에 가드 완료 기준 추가. 코드 0줄. 다음: 개발 1/4 — **컨텍스트 82% 로 야간 정지**

## 반복 49 — 2026-08-25 · 개발 1/4 (quality-eval)
- 한 일: `e2e/quality/corpus.json` 64문서(언어당 2토픽 × 16문서) + 먼저 쓴 실패 테스트 `tests/test_quality_corpus.py` 5건(개수·필드·URL 유일·토픽 4×16·색인 64건 및 토픽 어휘 공유)
- **한 번 다시 썼다**: 첫 판은 변별어를 1~5문서에만 넣어 계획서 완료 기준(색인 64건)은 통과했지만, 탐침에서 **40질의 전부가 러너 가드 G2(매치 ≤ 10 = 측정 불능)** 에 걸렸다. 설계 `## 구조` 가 요구한 "16문서가 토픽 어휘를 전부 공유" 를 문자 그대로 지키도록 질의어를 토픽당 12~16문서에 뿌리고 정답 문서만 제목에 걸었다
- 결과: 151/151 통과(146 → +5). 변이 검사로 단언 확인(토픽 어휘 제거 → 공유 단언 실패, URL 중복 → 색인 63건). 코드(`src/`) 0줄
- 탐침 실측(커밋 없음): 40질의 매치 11~28건으로 G2 여유. 정답 순위는 35건이 1위, **의도적으로 심은 토크나이저 실패 5건**(복합어 2 · 띄어쓰기 1 · 영어 복수/활용 2)만 미포함 → ko 18/20 · en 18/20 예상
- 다음: 개발 2/4 — `e2e/quality/queries.json` 40개. 후보와 정답 URL 은 `status.md` 표에 그대로 있다

## 반복 50 — 2026-08-25 · 개발 2/4 (quality-eval)
- 한 일: `e2e/quality/queries.json` 40질의(ko 20 / en 20). 토픽마다 질의어 10개가 그 토픽 01~10 문서를 하나씩 가리키고 11~16 은 정답 없는 순수 방해 문서로 남는다. 검사는 새 파일 대신 `tests/test_quality_corpus.py` 에 `TestQualityQueries` 5건으로 붙였다 — 같은 fixture 계약이라 둘 곳이 하나다
- 결과: 156/156 통과(151 → +5). 코드(`src/`) 0줄. 커밋 `d014408`
- 탐침 실측(커밋 없음): 매치 11~28건으로 G2 여유, 35건이 순위 1위. 미포함 5건은 설계가 예고한 토크나이저 실패 → ko 18/20 · en 18/20
- **기록이 한 스텝 밀렸다**: 이 반복을 커밋한 세션이 API 502 로 죽어 `status.md`·여기·`metrics.md` 가 반복 49 에 멈춰 있었다. 다음 세션이 커밋 로그·파일 실측으로 대조해 소급 기록했다 — 커밋은 있는데 기록이 없으면 다음 반복이 같은 스텝을 다시 한다
- 다음: 개발 3/4 — 러너 `e2e/quality_eval.py`

## 반복 51 — 2026-08-26 · 개발 3/4 (quality-eval)
- 한 일: 러너 `e2e/quality_eval.py` 146줄 + 먼저 쓴 실패 테스트 `tests/test_quality_eval.py` 8건(진짜 fixture 를 최소한만 비틀어 종료 코드 0·1·2 갈림길을 때린다). 164/164 통과. `src/` 0줄
- **처음 잰 숫자: 한국어 17/20 (85%) · 영어 18/20 (90%) → 종료 코드 0.** `concept.md:22-23` 기능 2 합격. 미포함 5건은 전부 설계가 예고한 토크나이저 실패였다
- **변이 검사가 테스트를 두 번 고치게 했다**: 처음 7건은 G2 경계(`≤10` → `<10`)와 상위 10 자름선(`rank ≤ 10` → `≤100`)을 **못 잡았다**. 0건 매치로만 G2 를 때렸고, 미포함 5건이 전부 `순위 밖`(= 아예 매치 안 됨)이라 11위와 10위를 가르는 데이터가 없었기 때문이다. `레시피` 를 16문서 중 6문서에서 지워 **매치 정확히 10건**을 만들고, 정답을 **16위 문서**로 돌리는 두 경우를 더해 7종 변이를 전부 잡았다 — 이 설계의 핵심 가드가 경계에서 무방비였다
- 앞 세션이 status 에 남긴 `ko 18/20` 은 낡은 탐침값이었다(커밋 `d014408` 메시지는 17/20 로 맞다). 실측으로 정정
- 40줄 한도 초과 적용: `rules/dev.md` 6절 세 물음 통과(계획의 예상 파일 · 같은 스텝의 테스트 8건 · 1인 저장소, 보안 무관)
- 다음: 개발 4/4 — `docs/project.md` 품질 기준 등록 + e2e 시나리오 3(방해 문서 제거 시 포함률이 오르는가)

## 반복 52 — 2026-08-26 · 개발 4/4 (quality-eval)
- 한 일: `docs/project.md` 에 검색 품질 명령·기준선 등록, `design_quality-eval.md` `## 가정` 을 실측으로 닫음, `digest.md` 후보 3건. `src/`·`e2e/` 코드 0줄 (문서만)
- **설계가 남겨둔 가정이 거짓으로 판명됐다.** "16문서 토픽에서 정답이 상위 10 밖으로 밀릴 수 있다" → 밀린 건 **0건**이다. 순위 분포가 `1위 35건 / 2~10위 0건 / 매치 없음 5건` 이라 **recall@1 == recall@10** — 상위 10 이라는 창이 아무 일도 하지 않는다
- 절제로 갈랐다(방해 문서를 빼고 정답 40문서만 색인): 포함률이 **오르지 않고 35/40 그대로**, G2 만 38/40 발화. 즉 **85/90% 는 측정이 후해서 나온 숫자도, 랭킹이 좋아서 나온 숫자도 아니다** — 매치되면 1위·아니면 미검출인 이진 상태다. bm25 정렬은 이 fixture 에서 한 번도 시험되지 않았다
- 그래서 **G2 가드의 한계도 드러났다**: "방해 문서가 존재한다" 는 보증하지만 "순위를 흔든다" 는 보증하지 못한다. 설계가 핵심이라고 못박은 가드가 필요조건이지 충분조건이 아니었다 → digest [6]
- 미포함 5건의 원인을 문서 본문까지 열어 확정: 복합어 뒷부분(`김치찌개보관법`←`보관법`, `성산일출봉`←`일출봉`) · 띄어쓰기(`올레 길`←`올레길`) · 영어 굴절(`loaves`←`loaf`, `tuple`←`tuples`). **랭킹 문제가 아니라 매치 문제** → digest [8] 로 후속 후보(trigram 병행 색인 / 질의측 s-접미)를 남김
- 러너의 네 갈림길을 이 반복에서 직접 변이로 확인(2·2·2·1·0). 앞 세션 관찰을 물려받지 않았다
- 다음: 테스트 phase

## 반복 53 — 2026-08-26 · 테스트 phase (quality-eval)
- 전체 166/166 통과(164 → +2), 기존 e2e 5개 회귀 없음, `quality_eval.py` 종료 코드 0 유지
- **갭 [8] 하나를 찾아 고쳤다 — 읽을 수 없는 fixture 가 "품질 미달"로 보고됐다.** `--corpus`/`--queries` 경로를 잘못 치거나 JSON 이 깨지면 트레이스백과 함께 파이썬 기본 종료 코드 `1` 이 나간다. 그런데 이 러너의 `1` 은 **"검색 품질이 80% 미만"** 으로 이미 예약된 값이라, CI 가 돌리면 **오타가 품질 회귀로 둔갑한다**. 계약(`## 계약`)은 사용법 오류도 `2` 라고 적혀 있었다 — 코드가 계약을 안 지킨 것
- TDD 순서대로: 재현 테스트 2건 → 실패 확인(exit 1 + 트레이스백) → `main` 에서 `(OSError, ValueError)` 를 잡아 2 반환 → 통과
- **새 테스트가 두 번째 결함을 바로 잡아냈다**: `json.JSONDecodeError` 메시지에는 **파일 이름이 없다**(`Expecting value: line 1 column 10`). 두 fixture 중 어느 쪽이 깨졌는지 알 수 없어 `_load` 가 경로를 붙여 다시 던지게 했다
- 변이 2종으로 새 테스트가 진짜 잡는지 확인: `return 2`→`1` 이면 2건 실패, 경로 붙이기 제거하면 1건 실패
- 8점 미만 1건은 digest 로: G3 가 ko·en 개수만 세서 `lang: ja` 질의가 조용히 사라짐[4]
- 다음: 리뷰 phase

## 반복 54 — 2026-08-26 · 리뷰 phase (quality-eval)
- 백지 패스(서브에이전트에 계획·설계를 주지 않고 diff 만) 8건 + 패스 B(계획·설계 대조) 1건. 6건 반영, 3건 digest
- **[85] 이름이 재는 것과 실제로 재는 것이 다르다** — 백지 패스가 개발 4/4 의 실측과 **독립적으로 같은 결론**에 닿았다(35건 전부 1위 → `recall@10` == `recall@1`). 지적의 절반만 받았다: 히스토그램을 출력한다(**판정은 안 바꾼다**). 나머지 절반인 `G2 를 3*TOP_N 으로` 는 **거절** — 현 fixture 최대 매치가 28건이라 40질의 전부가 측정 불능이 되고, 방금 만든 기준선이 무효가 된다. 자를 만들면서 재는 대상을 같이 바꾸지 않는다 → digest [6] 로 미룸
- **[80] 미포함 5건은 fixture 버그다** → **반만 맞다.** 정답 문서에 질의어 문자열이 없는 건 사실이지만(`김치찌개보관법` 안에 `보관법`), 그건 **사용자가 치는 말과 문서에 적힌 말이 다른** 실제 검색 상황이다. fixture 를 고치면 측정이 쉬워질 뿐 검색이 좋아지지 않는다. 대신 출력에서 **미검출과 순위 밖을 갈랐다** — 원인이 다르니 같은 말로 적으면 안 된다
- **[70] 테스트가 깨진 동작을 계약으로 굳힌다** → 받았다. `assertEqual(ko, 17)` 은 토크나이저가 좋아져 18이 되면 **빨간불**이 된다. 기준선은 하한이지 정답이 아니므로 `assertGreaterEqual` 로 바꾸고 en 도 함께 건다
- **[30] fixture 를 이스케이프 없이 HTML 에 끼워 넣는다** → 받았다. 점수는 낮지만 **조용한 어긋남**이라 값이 싸다. 탐침으로 형태를 찾았다: `100 < 200` 은 무해하고 `a<b`(글자 바로 뒤 `<`)가 태그를 열어 뒤 본문을 통째로 삼킨다. 그 형태로 테스트를 다시 쓰고 실패를 본 뒤 `html.escape`
- **[20] 모듈 간 가정이 안 박혔다** → 받았다. 러너가 `limit=100` 한 번으로 재고 앞 10건을 `limit=10` 이라 믿는데 그 등가는 `indexer` 쪽 `ORDER BY bm25, rowid` 에 달렸다. 계약 테스트로 박았다
- **패스 B 가 문서 오인용을 잡았다**: 계획·설계가 `concept.md:76` 을 "형태소 분석기 금지" 로 인용했는데 원문이 금지한 것은 "자체 **개발**" 이고 같은 줄이 "기존 것을 쓰거나 trigram 으로 버틴다" 를 허용한다. digest [8] 의 후속 방향이 걸려 있어 문구를 고쳤다
- 변이 2종으로 새 테스트 검증(1위 집계를 2위로 / 히스토그램 출력 제거 — 각각 실패). 170/170
- **임계 80 에 데이터가 하나 붙었다**: 이번에 고친 6건 중 **3건이 80점 미만**(70·30·20)이었다. 셋 다 진짜였고 셋 다 몇 줄이었다 → `metrics.md`
- 다음: e2e phase

## 반복 55 — 2026-08-26 · e2e phase (quality-eval) → **DONE**
- 시나리오 3개를 리뷰 후 코드로 다시 돌렸다: 1 통과(exit 0) · 2 통과(a: G1 exit 2 / b: 15/20 75% exit 1) · **3 은 반증**(방해 문서를 다 빼도 35/40 그대로, G2 38건 발화). `docs/e2e/quality-eval/result.md`
- `html.escape` 가 숫자를 바꾸지 않음을 확인했다 — 현 코퍼스 64문서에 `<`·`&` 가 0건이다. 이스케이프는 앞으로를 위한 것이고 새 테스트가 지킨다
- 기존 e2e 5개 회귀 없음. perf p95 **7.06ms**(예산 300ms). 170/170
- **DONE**: `plan_quality-eval.md`·`design_quality-eval.md` → `*_history_006.md` 로 아카이브, `index.md` 6번째 완료 줄, `digest.md` 완료 절. 살아 있는 참조(코드·테스트·문서)의 옛 파일명도 함께 고쳤다
- **이 계획이 남긴 것**: 컨셉 기능 2 의 자가 처음 생겼고 ko 85% · en 90% 로 합격했다. 동시에 **그 자의 한계가 실측으로 문서화됐다** — 랭킹은 못 잰다. 다음에 토크나이저를 건드리는 계획은 이 기준선을 기준으로 비교한다
- 다음: 계획 탐색

## 반복 56 — 2026-08-26 · 계획 (non-ascii-url)
- 한 일: `docs/plan_non-ascii-url.md` 작성(스텝 4, 설계 필요 판정) + 브랜치 `loop/non-ascii-url` 분기 + `index.md` 한 줄. 코드 0줄
- 근거는 **사용자 지시 + 로컬 재현**이다. `rules/discover.md` 1~3순위를 먼저 값싸게 확인했다: 실패 테스트 0(170/170) · 린트/타입체커 없음(`project.md`) · 코드의 `TODO|FIXME|HACK` **0건**. 중복 방지 5곳 전부 확인 — `index.md`·digest 완료·활성 계획·보류 3건·`docs/patches/`(폴더 없음) 어디에도 없다
- **재현이 지시보다 넓었다**: `fetch()` 가 예외를 밖으로 흘리는 것 외에, ① `links.extract` 가 한글 URL 을 날것으로 프런티어·`pages` 에 넣어 **같은 페이지가 두 행**이 될 수 있고 ② IDN 호스트는 원인이 다르다(`latin-1` / IDNA) ③ `RobotsCache.allowed` 는 **안 죽는다**(ASCII 호스트면 robots.txt URL 도 ASCII). 셋 다 계획서 `## 실측` 표에 넣었다
- 설계 필요 판정: 대안이 갈린다(정규화를 fetch 안에 넣나 링크 경계로 올리나 — `pages.url` 키가 달라진다) + 새 모듈 가능성
- 다음: 설계 phase

## 반복 57 — 2026-08-26 · 설계 (non-ascii-url)
- 한 일: `docs/design_non-ascii-url.md` 작성. 코드 0줄. 대안 셋을 서로 다른 출발점에서 냈다 — ① `fetch` 안 정규화(최소) ② URL 이 태어나는 경계(정공법) ③ `Frontier.add` 한 곳(되돌리기 우선)
- **③을 버린 근거가 이번 설계의 소득이다.** 한 줄로 끝나 제일 게을러 보였는데, `crawl.py:36` 의 `page_url = result.url or url`(리다이렉트 최종 URL)이 프런티어를 **안 거치고** `store.upsert` 로 직행한다 — 정규화 안 된 키가 그대로 `pages` 에 남는다. ①은 계획 목표("두 행이 되지 않는다")를 아예 못 채운다(`fetch` 는 저장 키를 정하지 않는다)
- **계획이 걱정한 `quote(safe=...)` 문제가 규칙 하나로 사라졌다**: "비ASCII 문자만 하나씩 `quote`" 로 하면 ASCII 구분자(`? & = / # %`)를 손댈 일이 없다. 덤으로 "ASCII URL 은 한 글자도 안 바꾼다" 가 **멱등**과 `%` 이중 인코딩 차단을 동시에 준다
- `robots.py` 는 계약상 건드리지 않는다 — 프런티어를 통과한 ASCII URL 만 받으므로 `_base()` 도 ASCII 다 (계획의 위험 1 종료)
- 되돌리기: 새 파일 1 + 한 줄 편집 3 = 커밋 하나로 revert. 플래그 불필요
- 다음: 개발 step 1/4 (`urls.to_ascii` + `tests/test_urls.py`, TDD)

## 반복 58 — 2026-08-26 · 개발 step 1/4 (non-ascii-url)
- 한 일: `src/websearch/urls.py` 의 `to_ascii` + `tests/test_urls.py` 15건. TDD — `ImportError` 실패를 먼저 봤다. **185/185**(기존 170 + 15). 커밋 62cec7b
- 변이 4종 전부 잡혔다: `isascii()` 반전(20건) · 조기 반환 삭제(2건) · `_quoted` 를 통짜 `quote` 로(2건) · `except ()`(7건 ERROR). `PYTHONDONTWRITEBYTECODE=1` 로 돌려 `.pyc` 함정 회피
- **설계의 예시 하나가 틀렸다**: `http://a..b/` 는 ASCII 라 계약 1의 조기 반환에 걸려 `None` 이 안 된다. 테스트는 비ASCII 빈 라벨 `http://가..나/` 로 바꿨다. **계약은 안 바꿨다** — 예시만 틀린 것이라 `design.md` 6절의 "계약 변경" 이 아니다
- **조기 반환의 진짜 값이 예상과 달랐다.** 문자 단위 `quote` 는 그 자체로 멱등이라 `%25` 이중 인코딩은 조기 반환 없이도 안전하다. 실제로 깨지는 것은 `urlsplit`/`urlunsplit` 왕복이 삼키는 **빈 `?` 와 `#`** 다. 그 두 케이스를 계약 1의 앵커 테스트로 박았다
- 부수 소득: 닫히지 않은 IPv6(`http://[::1/가`)이 `ValueError` 를 낸다 — `except (UnicodeError, ValueError)` 가 이미 잡고 있었다
- 다음: 개발 step 2/4 (호출처 3곳 연결)

## 반복 59 — 2026-08-26 · 개발 step 2/4 (non-ascii-url)
- 한 일: 호출처 3곳 연결. **소스 4줄** — `links.extract`(스킴 검사 뒤·`seen` 앞 3줄) · `crawl` 시드 1줄 · `crawl` 리다이렉트 최종 URL 1줄. 테스트 7건. **192/192**. `crawl_e2e.py` exit 0(수집 15, 최소 간격 1.004s — 회귀 없음). 커밋 1f37fb2
- TDD: 6건을 먼저 쓰고 `FAILED (failures=6)` 확인 후 구현
- **변이 검사가 무검증 분기를 하나 잡았다.** 세 줄은 지우면 각각 3·2·1건이 깨졌는데 `page_url = ... or url` 의 **폴백만 0건**이었다 — 리다이렉트 최종 URL 이 `None` 이 되는 경로를 아무 테스트도 안 밟았다. 폴백이 없으면 `store.upsert(None, ...)` 로 NULL 키가 들어가고 `links.extract(None, html)` 로 이어진다. 테스트를 하나 더 넣어(총 7) 막았다
- **리다이렉트 정규화는 링크로는 검증할 수 없다** — `links.extract` 도 정규화하니 base 가 뭐든 결과가 같다. 그래서 "ASCII 표기로 다시 온 같은 페이지를 두 번 받지 않는다" 는 관찰 가능한 결과로 단언했다
- 리뷰로 넘긴 것: 정규화 못 하는 시드를 **조용히** 버린다(간격 초과 도메인은 stderr 로 알리는 선례가 있다). 계약 밖이라 이 스텝에 안 넣었다
- 다음: 개발 step 3/4 (`fetcher` 최후 방어선)

## 반복 60 — 2026-08-26 · 개발 step 3/4 (non-ascii-url) → 개발 완료
- 한 일: `fetcher.py` 에 `except UnicodeError: return FetchResult(0, None, None)` **2줄**(`HTTPError` 뒤·`URLError` 앞, 재시도 안 함) + 테스트 2건. **194/194**. 커밋 359c5f4
- **원인 지점을 정확히 찍었다**: 경로 비ASCII 는 `http/client.py:_encode_request` 의 요청 줄에서 `'ascii'` 코덱, IDN 호스트는 `putheader('Host', ...)` 에서 `'latin-1'` 코덱. **원인이 둘인데 예외 타입은 하나**(`UnicodeEncodeError`)라 2줄로 둘 다 덮인다
- **연결 전에 터진다** — `putrequest`/`putheader` 는 버퍼에만 쓰고 `connect()` 는 `endheaders` 뒤다. 소켓도 DNS 도 안 간다. 그래서 테스트가 로컬 서버 없이 안 열린 포트 9 와 없는 `.test` TLD 로 **결정론적**이다
- **IDN 은 stdlib 이 살려주지 않는다**: `putrequest` 에 `encode("idna")` 폴백이 있지만 urllib 이 `do_request_` 에서 `Host` 를 미리 넣어 `skip_host=1` 이 되고 그 폴백을 건너뛴다. 계약대로 status 0
- 재시도 없음 확인은 `mock.patch(..., wraps=urlopen)` 의 `call_count == 1` — 기존 `test_timeout_retries_then_gives_up`(3회)과 대조된다
- 변이: `except` 2줄 삭제 → 신규 2건 정확히 ERROR
- 다음: 테스트 phase (갭 탐색 + 전체)

## 반복 61 — 2026-08-26 · 테스트 phase (non-ascii-url)
- 한 일: 갭 탐색 + 테스트 63줄(`tests/` 2파일). **소스 0줄.** 196/196(`expected failures=1`). e2e·성능 7개 전부 종료 0 — `quality_eval` ko 85%·en 90%(기준선 동일) · `perf_search` **p95 6.80ms**(기준선 7.06ms, 예산 300ms 의 2.3%)
- **가장 큰 구멍이 비ASCII 가 아니라 ASCII 쪽에 있었다.** `fetch` 가 `http.client.InvalidURL` 을 못 잡는다 — `HTTPException` 이라 `OSError` 그물에도 `UnicodeError` 에도 안 걸린다. 도달 경로 3개가 **전부 평범한 HTML 에서 `links.extract` 가 만들어낸다**(`href="/a b"` 공백 · 제어문자 · 숫자 아닌 포트). **크롤 루프가 통째로 죽는다 — 이 계획이 닫은 버그와 같은 부류다.** 계획 이전부터 있었지만 step 3 이 `fetch` 를 "최후 방어선" 으로 규정한 순간 그 계약의 미완성이 됐다 → 리뷰로
- **설계가 `robots.py` 를 범위 밖에 둔 근거가 순서 하나뿐이었다** — `robots.allowed()` 도 비ASCII 호스트에서 예외를 흘린다(탐침 확인). `crawl` 에서는 URL 이 태어나는 자리에서 이미 ASCII 라 도달 불가. 그 순서를 `test_robots_and_store_never_see_non_ascii_url` 로 박았다
- `to_ascii` 가 비ASCII URL 의 끝 `?`·`#` 를 삼킨다(ASCII 는 조기 반환이라 보존) — "두 표기가 1행" 이 이 조합에서만 샌다 → 리뷰로
- 변이 5종 전부 검출. 새 계약 테스트는 시드·`links` 두 변이에서 **단독 발화**해 실제로 하중을 받는다
- 재현 못 한 `HTTPException`(`IncompleteRead` 등)은 **고치지 않았다**(`rules/test.md` 2-1절)
- 다음: 리뷰 phase

## 반복 62 — 2026-08-26 · 리뷰 phase (non-ascii-url)
- 한 일: 백지 패스 A 후보 7건 → 80점 넘은 **3건 적용**(소스 누적 21줄, 자동 적용 40줄 한도 안). **199/199**(`expectedFailure` 0 — 테스트 phase 가 남긴 재현이 계약으로 승격). e2e·성능 7개 종료 0, p95 6.79ms. 커밋 0bdc98c
- **패스 A 가 계획서 없이 [9]·[6]·시드 침묵을 독립적으로 다시 찾았다** — 넘겨받은 목록과 일치
- **[9] 는 한 줄이 아니었다.** 테스트 phase 진단("`except (UnicodeError, InvalidURL)` 한 줄이면 196/196")은 맞지만 **테스트가 그 구멍을 안 덮고 있었기 때문**이었다. 뿌리는 `http.client` 예외가 전부 `HTTPException` 이라 `OSError` 그물에 안 걸린다는 것 — `BadStatusLine`·`IncompleteRead` 도 똑같이 `fetch` 를 뚫는다(변이로 확인). 그물을 **URL 오류(즉시 0) / 연결·응답 오류(재시도)** 로 다시 그었다
- **[6] 을 고치니 코드가 늘지 않고 줄었다.** 삼켜진 `?`·`#` 를 되돌려 붙이는 대신 `urlsplit`/`urlunsplit` **재조립 자체를 버렸다** — 원본 문자열 위에서 갈아끼우니 삼킬 것이 없다
- **예상 못 한 전제 하나**: `urlsplit("http://한\t국.com/가").netloc` 이 `한국.com` 이라 **원본의 부분문자열이 아니다**(WHATWG 탭 제거). 새 구현의 전제가 조용히 깨질 뻔해 `translate` 로 같은 자리에서 먼저 떼고 테스트로 박았다
- 설계를 먼저 고치고 코드를 밀었다(`design.md` 6절) — 계약 2(재조립 금지)·`fetcher` 표 신설·`## 설계를 고친 곳` 절
- 보류로 넘김: **[7] `robots.allowed()` 도 같은 뿌리의 구멍**(도달 불가 — 순서 계약 테스트가 막는다) · [5] 호스트 대소문자(`한국.COM` → `xn--3e0b707e.COM` 실측). 둘 다 `digest.md`
- 다음: e2e phase (마지막)

## 반복 63 — 2026-08-26 · e2e phase (non-ascii-url) → **DONE**
- 한 일: `e2e/non_ascii_e2e.py` 신규(로컬 서버 + 자식 프로세스에서 진짜 `crawl.crawl()`, **2.6s, 종료 0**). 시나리오 3개 전부 통과 — ① 한글 경로 링크 크롤 `수집 3 페이지`·DB 에 비ASCII URL **0건** ② 두 표기 → **1행**(서버 수신도 1건) ③ 서로게이트 시드는 stderr 로 알리고 건너뛰며 나머지 3페이지 전부 수집. `docs/e2e/non-ascii-url/result.md` + `project.md` e2e 줄
- **변이 4종 중 2종만 잡힌다.** 시드 정규화 제거(exit 1)·링크 정규화 제거(`수집 2 페이지`)는 잡고, **저장 키 정규화**와 **`fetcher` 의 `InvalidURL`** 은 이 시나리오가 **닿지 않는 경로**다 — 전자는 비ASCII `Location:` 리다이렉트에서만, 후자는 `links.py` 가 앞에서 다 ASCII 로 바꿔서. 다중 방어라 단위 테스트가 담당한다. **e2e 를 늘려 덮지 않고 `result.md` 에 적었다**
- **예상과 달랐던 것**: 변이 A 에서 크롤이 `fetch` 가 아니라 **그 앞 `store.has()`** 에서 죽는다 — 서로게이트를 SQLite 파라미터로 넘기다 `UnicodeEncodeError`. `fetcher` 방어만으로는 이 시드를 못 막는다는 뜻이고, **시드 정규화가 실제로 짐을 진다**는 증거다
- 앞 관문 전부 종료 0: 199/199(1.59s) · crawl(간격 1.000s) · crawl-delay(2.00s/1.00s) · indexer · noindex · search-api · quality-eval(ko 85%/en 90%) · perf **p95 6.76ms**(기준선 6.71ms)
- **DONE**: `plan_non-ascii-url.md`·`design_non-ascii-url.md` → `*_history_007.md` 아카이브, `index.md` 9번 완료, `digest.md` 완료 절. 살아 있는 참조(`e2e/non_ascii_e2e.py`·`result.md`)의 옛 파일명도 함께 고쳤다
- **이 계획이 남긴 것**: 사용자가 실제로 밟은 크래시를 닫았고, **그 뿌리를 따라가다 계획 이전부터 있던 크래시를 하나 더 닫았다**(`InvalidURL`). "URL 이 태어나는 자리에서 정규화한다" 는 경계가 생겼고, `robots.py` 가 그 경계 밖이라는 사실이 순서 계약 테스트로 박혔다
- 지시받은 계획이 끝났다 → **정지**(새 계획 탐색 안 함)

## 반복 64 — 2026-08-27 · 계획 phase (crawl-throughput) — 야간 시작

- 한 일: `docs/plan_crawl-throughput.md` 신규(스텝 3개). **소스 0줄.** 브랜치 `loop/crawl-throughput`
- **근거는 사용자 실측이다** — 실제 웹 크롤에서 **초당 0.5문서**. `concept.md:44` 성능 축 2번(초당 5문서)의 10배 미달. 원인은 `crawl.py:26-51` 이 `fetcher.fetch()` 앞에서 통째로 멈춘다는 것 — **처리량 = 1 / 평균 응답시간** 이라 CPU·디스크가 논다
- **방법이 컨셉에 이미 적혀 있었다**(`concept.md:44-45`): "도메인 다양성으로 해결하는 문제이지 간격을 줄여 해결하는 문제가 아니다". 그래서 `DOMAIN_INTERVAL` 1.0 은 `## 하지 않을 것` 첫 줄로 못박았다
- **`Frontier` 가 이미 라운드로빈 + 도메인별 간격을 큐 수준에서 보장한다**(`frontier.py:50-63`) — 간격 로직을 새로 만들 일이 없다. 동시화는 "어느 URL 을 언제 꺼내는가" 가 아니라 "꺼낸 것을 몇 개나 동시에 띄우는가" 의 문제로 좁혀졌다
- **1,700문서 크래시(`sqlite3.OperationalError: database is locked`)를 같은 계획에 넣었다** — 죽은 크롤의 처리량은 0.5가 아니라 0이다. 다만 스텝 3으로 **독립·4줄 규모**로 가둬 범위가 새지 않게 했다(`store.py` 만, `indexer` 는 안 건드림)
- **측정 수단이 스텝 1이다.** 도메인 다양성을 로컬에서 만드는 법: **포트가 곧 netloc** — `127.0.0.1:PORT` 12개면 `Frontier` 에게 12개 도메인이다(`crawl_delay_e2e.py` 가 호스트 2개로 하던 것의 확장). 인위 지연 0.4초가 없으면 순차와 동시가 같은 숫자를 낸다
- 설계 트리거 **3개** 걸림(공개 인터페이스·3파일 이상·대안 갈림) → 설계 phase 로
- 깨뜨릴 가정 하나를 미리 적었다: `Frontier.next()` 가 팝 시점에 `_last_fetch` 를 찍으므로 **응답이 1초보다 길면 같은 도메인이 in-flight 인 채로 다시 팝된다** — 참이면 동시성이 politeness 를 먹는다
- 다음: 설계 phase

## 반복 65 — 2026-08-27 · 설계 phase (crawl-throughput)

- 한 일: `docs/design_crawl-throughput.md` 신규(계약 8개). **소스 0줄**
- 출발점 셋에서 대안을 냈다: **A 최소**(네트워크만 던진다) · **B 정공법**(워커가 팝→저장까지 전부, 락 + 스레드별 커넥션) · **C 되돌리기 우선**(코드 무변경, 도메인 샤딩 프로세스 N개)
- **A 채택.** 동시성이 필요한 이유가 **네트워크 대기 하나뿐**이라, 대기가 있는 곳만 던지면 락도 스레드별 SQLite 커넥션도 필요 없다. `Store`·`Frontier` 는 계속 메인 스레드 전용
- **C 를 버린 이유가 컨셉에 이미 적혀 있었다** — 샤드가 발견한 남의 도메인 링크를 넘길 방법이 없는데 "링크를 따라 프런티어를 넓힌다" 가 이 크롤러의 정의다(`concept.md:24`). 게다가 실측에서 크롤을 죽인 `database is locked` 를 정면으로 늘리고, `concept.md:74`(분산 크롤러 2단계까지 금지)에도 걸린다
- **깨보려던 가정이 참이었다 — 설계가 커졌다.** `frontier.py:53-54` 의 자격 조건은 **팝 이후 경과 시간뿐**이라 응답이 간격보다 길면 **같은 도메인이 in-flight 인 채 다시 팝된다**. 순차 루프에서는 다음 팝 자체가 응답 뒤였으니 불가능했던 구멍을 **동시화가 처음 연다**. 거짓이었으면 `crawl.py` 만 고치면 됐다 → 참이라 `Frontier.next(exclude=())` 와 계약 3(**도메인당 in-flight 1개**)이 설계에 들어갔다. 코드를 읽어 보였으므로 탐침은 안 했다(`plan.md` 3-1절)
- 되돌리기 수단을 **`--workers 1`** 로 잡았다 — 배포도 revert 도 아닌 설정 한 글자이고, in-flight 최대 1개면 팝→fetch→저장이 오늘과 같은 순서로 직렬화된다. 스텝 2가 이걸 테스트로 확인한다
- 기본값 8은 **보정 손잡이**로 CLI 에 노출한다 — 로컬 서버의 응답 분포는 실제 웹과 다르다
- 실패 격리(계약 6): 워커 예외는 `Future` 가 잡고 메인이 **fetch 실패(status 0)와 같게** 취급 → 워커 하나가 죽어도 크롤은 안 죽는다
- 다음: 개발 스텝 1/3 (`e2e/perf_crawl.py` — RED 를 먼저 본다)

## 반복 66 — 2026-08-27 · 개발 스텝 1/3 (crawl-throughput) — 측정 수단

- 한 일: `e2e/perf_crawl.py` 신규 137줄. **`src/` 0줄.** RED 기준선 **초당 1.95문서** 확보(`concept.md:44` 기준 5.0)
- 로컬에서 도메인 다양성을 만드는 법: **포트가 곧 netloc 이다**(`frontier.py:44`). `ThreadingHTTPServer` 12개면 프런티어에게 12도메인 — `crawl_delay_e2e.py` 가 호스트 2개(`127.0.0.1`/`localhost`)로 하던 것의 확장. 인위 지연 0.4초가 없으면 순차와 동시가 같은 숫자를 낸다
- 판정을 셋으로 나눈 이유: **처리량만 재면 간격을 깎아서 통과할 수 있다.** ① 처리량 ② 도메인 간격 ③ 중복 — 윤리를 먼저 단언한다(`concept.md:59`)
- **변이 검사에서 예상 못 한 것이 나왔다: 변이를 하나도 안 넣은 원본 코드가 도메인 간격을 깬다.** 도메인 2개·응답 0.4초에서 **0.819초** 간격. 12도메인 본 실행에서는 간격이 5초씩 벌어져 숨는다 — **기존 e2e 가 5개나 있는데 아무도 못 본 이유가 이것**이다
- 뿌리는 `digest.md [4]` 로 이미 적혀 있었다 — 간격 시계가 **발신이 아니라 팝 시각**에서 시작한다. 팝 직후 `robots.allowed()` 가 robots.txt 를 받느라 0.4초를 쓰고, 다음 번엔 캐시라 곧바로 나가므로 실제 간격 = `interval - robots왕복`. **적혀 있던 것과 실제로 밟은 것은 다르다**
- **동시화는 이걸 반드시 악화시킨다** — 간격을 1초까지 좁히는 것이 동시화의 목적이라 모든 도메인의 첫 간격이 0.6초가 된다. `concept.md:25` 에서 이건 리뷰 RED
- → **설계 자가수정 1건**: 계약 9(`Frontier.mark_sent(domain, at)` — 워커가 `fetch` 직전 시각을 재서 결과에 실어 보내고 메인이 프런티어에 반영. 워커는 여전히 공유 상태를 안 만진다). 계획서 `## 하지 않을 것` 의 해당 줄도 취소선으로 고쳤다(`rules/design.md` 6절 — 코드를 밀지 말고 설계를 먼저 고친다)
- 변이 3종 전부 확인: ②는 `DOMAIN_INTERVAL=0`, ③은 중복 방어 두 겹(`_seen`·`store.has`)을 동시에 꺼야 발화한다(다중 방어라는 뜻), ①은 실행 자체가 발화
- **범위는 좁게 유지했다** — 계약 9는 **페이지 요청 사이 간격**만 고친다. robots.txt 요청 자체를 간격 시계에 싣는 것은 그대로 범위 밖
- 다음: 개발 스텝 2/3 (크롤 루프 동시 fetch)

## 반복 67 — 2026-08-27 · 개발 스텝 2/3 (crawl-throughput) — 동시 fetch

- 한 일: `crawl.py`·`frontier.py` 동시화 + 테스트 6종. **초당 1.95 → 10.30문서**(`concept.md:44` 기준 5.0의 2배). 209/209. e2e 4종 전부 종료 0
- **TDD 로 갔다** — 테스트 9개를 먼저 써서 9 errors 를 보고(RED) 구현했다. 프런티어 4건이 먼저 초록이 되고 크롤 5건이 남는 것으로 두 파일의 경계가 실제로 갈렸다
- 설계 계약대로 **네트워크만** 던졌다. 워커(`_fetch_one`)는 `robots.allowed()` → `robots.delay()` → 발신 시각 → `fetch()` 만 하고 튜플을 돌려준다. `Store`·`Frontier` 는 메인 전용이라 **락 0 · 스레드별 SQLite 커넥션 0**
- **동시성을 시간으로 판정하지 않았다** — `threading.Barrier(4)`(넷이 만나야 통과)와 도메인별 동시 실행 최고치로 본다. 부하 걸린 기계에서 흔들리지 않는다
- **예상 못 한 계약 충돌 1건.** `test_redirect_final_url_normalized_before_store` 가 깨졌다 — 리다이렉트로 수렴하는 두 URL(`a.com/moved2` → `b.com/가`, 시드에 `b.com/%EA%B0%80`)이 **동시에 떠 있으면** 뒤엣것을 못 막는다. 리다이렉트가 어디로 갈지는 응답이 와야 알고, 그때 다른 워커는 이미 나갔다. **테스트를 지우지 않고 `workers=1` 로 좁혔다** — 되돌리기 경로가 옛 보장을 그대로 갖고 있다는 증거로 남겼다. 큐에 남은 URL 은 여전히 막히고(제출 전 `store.has`), 저장은 1행이고, 간격도 안 깨진다. **잃은 것은 요청 하나뿐**
- **계약 9(`mark_sent`)가 실제로 값을 했다** — 스텝 1이 잡은 0.819초 위반이 닫혔고, 변이로 되돌리니 0.596초로 되살아난다. 동시화가 간격을 1초까지 좁히자 예상대로 더 크게 벌어졌다
- `seconds_until_ready(exclude=)` 는 구현 중에 추가로 필요해졌다 — 떠 있는 도메인이 0초로 읽혀 그 0에 가리면 정작 쿨다운이 풀리는 도메인을 놓치고 완료만 기다린다(계약 8의 구멍)
- 변이 2종 확인: `exclude=busy` 제거 → `TestConcurrency` 실패 · `mark_sent` 제거 → 간격 위반 재발. 되돌리기 경로(`workers=1`)도 실제로 돌렸다 — 1.82/s, 간격 위반 0, 중복 0
- CLI `--workers N` 은 **보정 손잡이**다(기본 8). `_number_flag()` 로 `--max` 와 방어를 합쳤다 — "CLI 진입점마다 방어를 따로 쓴다"가 digest 반복실패 항목이라 세 번째를 만들지 않았다
- 다음: 개발 스텝 3/3 (`store` 잠금 내성)

## 반복 68 — 2026-08-27 · 개발 스텝 3/3 (crawl-throughput) — 잠긴 DB 내성

- 한 일: `store.py` 연결 두 줄(WAL + `timeout=30`) + `TestConcurrentAccess` 2개. **211/211**. e2e 4종 전부 종료 0, `perf_crawl` 10.22/s
- **RED 가 사용자 크래시와 같은 예외였다** — 고치기 전 테스트가 `sqlite3.OperationalError: database is locked` 로 죽었고, 스택도 `store.py` 의 `self._db.commit()` 으로 사용자가 1,700문서에서 밟은 그 줄이다. 재현 없이 고치지 않았다
- **두 줄이 서로 다른 것을 막는다.** WAL 은 **읽는** 연결이 쓰는 연결을 막는 것(`indexer` 가 검색·집계로 읽는 경우), `timeout` 은 **쓰는** 연결끼리 부딪히는 것. WAL 만으로는 후자가 안 닫힌다
- **`timeout=30` 을 근거 없이 넣지 않았다.** 기본값이 이미 5초라 "0 → 30" 이 아니라 "5 → 30" 이다. 탐침으로 5초가 부족한지 실제로 쟀다: 쓰기 트랜잭션 6초 붙들기 → 기본 5.0은 **5.2초 만에 죽고** 30은 **6.5초 기다렸다 성공**. 5초가 부족한 이유도 코드에 있다 — `indexer.index_pages` 가 색인 **전체를 트랜잭션 하나로** 돌리고 끝에서 commit 한다(`indexer.py:39`→`:52`). 문서가 많으면 그 커밋이 5초를 넘는다
- **탐침을 커밋하지 않았다** — 6초짜리라 2초 스위트에 넣으면 스위트가 4배 느려진다. 대신 근거를 커밋 메시지와 여기에 남겼다. 테스트 phase 가 "느린 테스트를 넣을지" 판정하도록 `status.md` 에 올렸다
- 변이 1종 확인: `PRAGMA journal_mode=WAL` 제거 → `TestConcurrentAccess` 실패(복원 확인). `timeout` 쪽은 커밋한 테스트로는 안 잡힌다 — 위 항목이 그 대가다
- **스위트가 빨라졌다** — `test_store` 5.25초 → 0.013초. RED 상태에서 5초는 SQLite 가 기본 timeout 만큼 기다린 시간이었다
- 탐침을 두 번 썼다 — 첫 판은 `threading.Timer` 가 **다른 스레드에서 만든 커넥션**을 만져 `ProgrammingError` 로 홀더가 커밋을 못 했고, 그래서 둘 다 "죽음" 으로 나왔다. 탐침이 거짓말한 것이다. 커넥션을 만든 스레드가 끝까지 소유하게 고치니 갈렸다. **결과가 "둘 다 실패" 로 나오면 탐침 자신을 먼저 의심한다**
- 스키마 변경이 아니라고 판단했다(야간 금지 항목) — 테이블·컬럼이 그대로고 `PRAGMA journal_mode=DELETE` 로 되돌아간다. 사이드카 `-wal`·`-shm` 이 `data/` 에 생기는 것은 `status.md` 보류에 적었다
- 다음: 테스트 phase (`rules/test.md`) — 계획 3스텝 전체를 한 번에 본다

## 반복 69 — 2026-08-27 · 테스트 phase (crawl-throughput 3스텝 전체)

- 한 일: `rules/test.md` 3절 7카테고리로 계획 전체를 훑었다. **8점 이상 갭 1건 — 그 자리에서 메웠다.** 212/212, **e2e 7종 전부 종료 0**
- **찾은 갭(8점): `timeout` 이 하는 일에 커밋한 테스트가 0이었다.** 스텝 3은 두 줄인데 변이로 잡히는 건 WAL 한 줄뿐이었다 — "쓰기끼리 부딪히면 죽지 않고 기다린다" 는 아무도 안 밟는다. 크롤 프로세스가 통째로 죽는 경로(사용자가 실제로 밟은 그 크래시)라 8점을 줬다
- **느린 탐침을 빠른 테스트로 바꿔 넣었다.** 탐침은 6초(기본 timeout 5초를 넘겨야 갈린다)라 스위트에 못 넣는다. 대신 **락을 0.3초만 붙들고 "죽지 않는다" 만 단언**한다 — 고정하는 계약이 "얼마나 오래" 가 아니라 "기다린다" 라는 걸 docstring 에 적었다. 변이 `timeout=0` → 실패 확인. 0.42초
- **갭이 아닌 것 4건을 확인만 하고 넘겼다** (`rules/test.md` 5절 — 이미 덮인 것에 테스트를 더하지 않는다): 워커 예외 격리 `test_crawl.py:279` · `--workers` 잘못된 입력 3종 `:305-309` · `seconds_until_ready(exclude=)` `test_frontier.py:163` · `workers=1` 되돌리기 `:289`
- **DB 를 읽는 e2e 3종을 처음 돌렸다** — `indexer`·`noindex`·`search_api`. WAL 은 `store.py` 만 고쳤지만 그 파일을 **읽는 쪽**이 따로 있다(`indexer.py` 는 `sqlite3.connect` 를 직접 한다). 스텝 검증에서는 크롤 계열 e2e 만 돌렸으니 여기가 원거리 회귀 자리였다. 전부 종료 0
- **④ 불안정 패턴에 안 걸렸다** — 동시성 판정은 `threading.Barrier` 와 주입한 `itertools.count` 시계다. 실시간(`sleep`·경과시간) 단언은 `e2e/perf_crawl.py` 안에만 있고 거긴 e2e 라 제자리다
- 다음: 리뷰 phase (`rules/review.md`)

## 반복 70 — 2026-08-27 · 리뷰 phase (crawl-throughput)

- 한 일: 패스 A(백지)를 **별도 세션에 넘겼다**. 지적 5건 중 3건 반영·1건 보류·1건 부분기각. 213/213, `perf_crawl` 10.27/s
- **패스 A 를 남에게 넘긴 값을 했다.** `rules/review.md` 0절이 경고한 그대로 — 내가 쓴 코드의 내가 먼 자리를 리뷰어가 짚었다. **반복 69에 "8점 갭을 메웠다" 고 쓴 것은 틀렸다**
- **틀린 이유가 교훈이다: 변이를 잘못 골랐다.** `timeout=30` 을 검증한다며 변이를 `timeout=0` 으로 줬는데, **진짜 되돌리기는 인자 삭제**이고 sqlite3 기본 `busy_timeout` 은 0이 아니라 **5000ms** 다. 0.3초만 붙드는 테스트는 인자를 지워도 통과한다. **변이는 "이 값을 지웠을 때"로 잡아야지 "극단값을 넣었을 때"로 잡으면 거짓 초록이 나온다**
- 고침: `PRAGMA busy_timeout > 5000` 한 줄. 6초짜리 느린 테스트 대신 **값 계약만 고정**했다 — 행동 테스트(0.3초)는 "빨리 실패하지 않는다" 만 맡는다고 독스트링에 명시. 변이 `sqlite3.connect(path)` → 실패 확인
- **리뷰어 제안을 그대로 받지 않은 것 1건 (`rules/review.md` "findings 를 그대로 믿지 않는다").** `store.has` 스킵이 쿨다운을 태우는 건 사실이지만, 제안된 수정(팝의 `_last_fetch` 쓰기 제거)은 **간격 위반 구멍**을 연다 — 워커가 예외로 끝나면 `mark_sent` 없이 반환하는데 요청은 이미 나갔을 수 있다. `concept.md:59` 가 크롤 윤리를 성능 위에 두니 야간에 손대지 않는다. 영향 범위도 쟀다: `_seen` 이 한 실행 안 중복을 막아 `store.has` 는 **이어받기 크롤**에서만 참이라 이 계획의 목표 숫자와 무관하다
- **부분기각 1건.** `perf_crawl` 기준선이 헐겁다는 건 맞지만 "문서와 어긋난다" 는 틀렸다 — 문서 주장은 "지연 0이면 순차와 동시가 같아진다" 뿐이고 참이다. `TARGET_RATE` 는 `concept.md:44` 의 제품 목표라 **야간에 목표를 옮기지 않았다**. 못 잡는 범위를 상수 옆에 적는 것으로 갈음
- **코드로 안 고치고 문서로 고친 것 1건.** Ctrl-C 지연에 `cancel_futures` 는 약이 안 된다 — 취소되는 건 대기 중 작업뿐인데 여기선 제출한 것이 곧 실행 중인 것이다. 안 듣는 처방을 넣는 대신 독스트링에 적었다
- 패스 B(대조)는 앞서 끝냈다 — 설계 문서 2곳이 코드에 뒤처져 고쳤고, 렌즈 3·4·5 무소득
- 다음: e2e phase (`rules/e2e.md`)

## 반복 71 — 2026-08-27 · e2e phase · crawl-throughput DONE

- 한 일: e2e 시나리오 4개 전부 통과. `docs/e2e/crawl-throughput/result.md` 작성. **계획 008 DONE**
- **시나리오 4를 진짜로 돌렸다.** "워커 1로 돌려도 결과 문서 집합이 같다" 는 그동안 유닛(`test_crawl.py:289`)에만 있었다. `perf_crawl.py` 가 인자로 워커 수를 받게 6줄 고쳐서 e2e 수준에서 확인했다 — **48개 경로의 sha1 이 동시 실행과 동일**(`541d455a…`), 24.59초 대 4.68초. 되돌리기 경로가 느릴 뿐 같다는 증거가 이제 명령 한 줄로 재현된다
- 포트가 실행마다 달라 URL 전체가 아니라 **경로만** 비교했다 — 안 그러면 두 실행이 영원히 다르다
- 처리량 판정은 `workers < 8` 이면 건너뛴다. 되돌리기 경로가 느린 건 정상인데 같은 잣대를 대면 e2e 가 거짓말을 한다. 대신 **간격·중복·문서집합은 그대로 판정한다** — 느린 경로라고 윤리 검사를 빼지 않는다
- 최종 숫자: 사용자 실측 **0.5/s** → e2e **10.25/s**. 순차 1.95/s 대비 5.3배, `concept.md:44` 기준 5.0의 2배. 간격 위반 0, 중복 0, 213/213, e2e 7종 종료 0
- **남긴 것을 status.md 에 크게 적었다** — 쿨다운 태우기(이어받기 크롤에서만 드러난다), Ctrl-C 30초 지연, perf 기준선이 못 잡는 범위. 셋 다 아침에 사람이 판단할 자리다
- 다음: discover — 다음 계획을 고른다. 1순위 후보는 쿨다운 태우기

## 반복 75 — 2026-08-27 · 리뷰 반영 (search-ui)

- 한 일: 백지 리뷰의 지적 9건 중 **high 1·medium 3·low 5** 를 분류해 8건 반영. 235/235, e2e 9종 전부 종료 0
- **리뷰가 확인해 준 것과 뒤집은 것이 갈린다.** 이스케이프 자체는 안 뚫렸다 — 적대적 URL 8종 실사격에서 `href="javascript:` 0건, `<script` 0건. 뚫린 건 **검증 장치**였고 넷 다 같은 유형이다: **없어진 것을 못 보는 검사**
- **[high] 다크 블록이 없으면 라이트 맵이 다크 행세를 했다.** `dark = dict(light, **{})` 라 "다크 7쌍 전부 OK" 를 찍고 **종료 0**. 다크 CSS 를 통째로 지워도 통과했다 — 파일 첫머리 독스트링이 금지한 바로 그것이다. 변이 확인: 다크 블록 제거 → 종료 2
- **다크 미디어쿼리가 2개면 `"".join()` 결과가 원문에 없어 `replace` 가 무동작**이 되고 **라이트 맵이 다크 값으로 오염**됐다(리뷰 실측 `--bg-page` = `#000000`). 인덱스 슬라이싱 + 중괄호 세기로 고쳤다. non-greedy `.*?\}\s*\}` 는 중첩 깊이를 모른다. `and (min-width:…)` 가 붙은 형태도 이제 찾는다
- **[medium] 고정폭 검사가 `px` 만 봤는데 제품 CSS 는 전부 `rem`** 이다(`max-width:36rem`, `44rem`). **검사기의 눈먼 자리가 정확히 제품이 사는 곳이었다.** rem/em/vw/% 환산으로 고치고, 모르는 단위는 종료 2. 변이 `width:40rem` → `(= 640) > 360` 으로 종료 1
- **[medium] `PAIRS` 커버리지 강제가 라이트 맵만 봐서 다크 전용 `--fg-` 토큰이 빠져나갔다** → 두 맵 합집합. 변이 `--fg-visited` 를 다크에만 추가 → 종료 2
- **테스트에도 같은 것이 있었다.** 이 파일 XSS 단언이 전부 `assertNotIn` 이라 **`_safe_href` 가 무조건 `None` 을 돌려줘도 전부 통과**했다 — 링크가 통째로 사라지는 변이를 아무도 못 잡는다. 긍정 짝(`assertIn('href="http://ok.test/')`)을 붙이고 fixture 에 멀쩡한 URL 을 하나 넣었다. 변이 확인: 항상 `None` → 4건 실패
- **[low] `_safe_href("http")` 가 통과했다** — `"http".split(":")[0]` 은 `"http"` 다. `//evil.test/x` 같은 스킴 없는 상대 경로도 뚫렸다. 신뢰 경계라 짧아도 닫았다(`partition` 으로 콜론 유무 확인)
- **[low] 오류 페이지가 `SEARCHBOX % ("", " autofocus")`** 였다 — 친 질의어를 버려 다시 치게 만들고, autofocus 가 스크린리더 사용자를 **오류 문구를 지나쳐** 입력창으로 끌고 간다. `serve.py:104` 주석이 결과 페이지에 대해 직접 금지한 것과 같은 상황이다
- **[low] `h1` 이 어디에도 없었다** — 제목 계층이 `h2` 부터 시작했다. 홈 브랜드와 결과 안내문을 `h1` 으로 올리고 크기는 CSS 가 그대로 유지한다(**계층은 의미용, 크기는 시안용**). `assert_page_basics` 에 단언 추가
- **[low] CSP·nosniff 추가.** 이스케이프가 단일 방어선이던 자리에 두 번째 선을 뒀다. JS 0KB 라 `script-src 'none'` 이 잃을 것이 없다
- **반영 안 한 것 2건** (계획 범위 밖, `status.md` 에 남김): 페이지네이션 UI 없음 · 모르는 경로가 JSON 404
- **변이 검사 전에 커밋했다** — 두 번 잃고 배운 것이다
- 다음: e2e phase (`rules/e2e.md`)

## 반복 76 — 2026-08-27 · e2e phase · search-ui DONE

- 한 일: 사용자 경로 5개를 라이브 서버에서 걸었다. 5/5 통과. `docs/e2e/search-ui/result.md` 작성. **계획 009 DONE**
- **적대적 문서 2개를 색인에 섞어 둔 채로 걸었다** — URL 이 `http://evil.test/"><script>…` 인 문서와 URL 자체가 `javascript:alert(1)` 인 문서. `<script` 0건, `href="javascript:` 0건. `javascript:` URL 은 **링크가 안 되고 글자로만** 나온다(결과를 숨기지는 않는다)
- **오탐 하나를 기록해 뒀다**: `onerror=` 라는 글자가 화면에 한 번 나오는데 `&lt;img …&gt;` 안이라 텍스트다. **문자열만 세면 틀리는 자리**라 result.md 에 적었다
- 실측: 홈 200·2.4ms·gzip 1,340 B / 결과 200·0.7ms·gzip 1,629 B / `page=0` → **400·질의어 보존·autofocus 없음·트레이스백 없음** / `/search` JSON **무손상**
- 디자인 4축: JS **0 B** · 외부 서브리소스 **0건** · 최저 대비 **4.87:1**(`--fg-url`) · 초과 고정폭 **0건** · `overflow-wrap` 3곳
- 회귀: 235/235 · e2e 9종 전부 종료 0 · **크롤 처리량 10.23/s**(기준선 10.25, 회귀 없음) · 검색 p95 6.93ms
- `concept.md:5-6` 이 제품으로 정의한 **"브라우저로 접속해 검색"이 이제 있다.** 그전까지 있던 건 JSON 뿐이었다
- **이번 계획에서 가장 값진 산출물은 코드가 아니라 리뷰다.** digest 1순위 후보를 `status.md` 에 적었다 — **부정 단언에는 긍정 짝을 붙인다**(이번 밤 이 실패 유형을 3회 겪었다)
- 다음: discover — 후보는 `tokenizer`(컨셉 기능 2 에 직접 걸림) 또는 `pagination-ui`(방금 만든 화면의 명백한 구멍)

## 반복 77~79 — tokenizer(010) 계획·설계·개발 3/3 (2026-08-27 야간)

- 계획: 탐침으로 `trigram` 을 착수 전에 버렸다 — 한국어 질의 20개 중 **10개가 2자**라
  토큰이 0개가 되고 recall 이 35/40 → 27/40 으로 후퇴한다. 실측으로 거른 대안이다
- 설계: 한글 2-gram 을 **제목·본문 열로 나눠** 넣는다. 한 열로 합치면 정답이 꼴찌(13/13)로
  밀리고, 나누면 가중치 상수 없이 1위가 된다. 가정 하나를 깼더니 거짓이었다 —
  `snippet(-1)` 이 2-gram 열을 골라 화면에 `김치 치찌 찌개` 를 내보낸다. 설계를 고쳤다
- 개발: 스키마 드리프트 재구축(1/3) → `porter` + 2-gram 열(2/3) → 오탐·성능 측정(3/3)
- 결과: ko 17/20 → **20/20** · en 18/20 → **19/20** · 255/255 · 변이 14종 전부 잡힘
- 남은 미검출 1건은 `loaf`←`loaves` (영어 불규칙 복수). 사전 없이 못 고쳐 범위 밖으로 적었다

## 반복 80 — 2026-08-27 야간 · tokenizer 리뷰 phase

- 백지 서브에이전트가 7건을 냈다. **전부 직접 재현한 뒤에** 렌즈로 넘겼다(`rules/review.md` 37행)
- **[높음] 어절을 이어 붙여 2-gram 분기를 걸은 것이 진짜 결함이었다.** 그러면 그 분기가
  어절이 문서에 **그 순서로 붙어 있을 때만** 산다 — `냉장 보관법`·`올레길 안내`·`7코스 올레길`
  이 0건. 이 스텝이 내건 약속이 **한 어절 질의에서만** 성립하고 있었고, 기존 다어절
  테스트 2개는 둘 다 plain 분기가 이미 이기는 경우라 구멍을 못 봤다
- 고치는 중에 하나 더 배웠다: 리뷰가 제안한 `{title_ng body_ng}` 병합을 그대로 넣었더니
  한국어가 **20/20 → 17/20**(정답 13위). 설계가 열을 나눈 이유가 질의 쪽에도 걸린다.
  **받은 제안을 그대로 믿지 않는다** 가 여기서 값을 했다 — 열은 나눈 채로 두고 어절 분리만 취했다
- **그 회귀는 어떤 e2e 게이트도 못 잡았을 것이다.** 17/20 = 85% 라 `quality_eval` 합격선
  80% 를 통과한다. 잡은 것은 `tests/test_quality_eval.py` 의 기준선 상수 4개다 —
  게이트는 "합격인가", 기준선은 "어제보다 나빠졌는가". 다른 질문이라 둘 다 있어야 한다
- **[높음·보류] `porter` 접두 손실은 사람 몫으로 남겼다.** 양쪽을 전수로 쟀다:
  porter 있음 en 19/20 · 어휘 448개 중 49개(10.9%)가 접두 매치를 잃음 / porter 없음
  en 18/20 · 손실 0. 사는 것은 `tuples` 한 건이고 영어는 porter 없이도 90% 로 합격선을
  넘는다. **이미 통과하던 축을 올리려고 접두 검색을 깨는 거래**라 `severity.md` 3절대로 보류했다
- 리뷰 전에 내가 따로 찾은 것 하나: 드리프트 재구축이 `before + indexed - after` 산술에서
  **noindex 제외로 둔갑**해 "3 문서 색인 제외" 를 거짓으로 찍었다. `_doc_count` 가 옛 정의를
  0 으로 보게 했다 — `search()` 가 옛 색인을 못 쓰는 것으로 보는 기준과 같다
- 낮음 2건(본문 없는 문서의 빈 스니펫, CLI stale 트레이스백)은 자동 적용. 중간 2건은
  천장 주석으로만 남겼다(2-gram 의 공백 경계 넘기, `prefix='2 3'` 의 `*_ng` 낭비)
- 회귀: **260/260** · e2e 5종 종료 0 · ko 20/20 · en 19/20 · 매치 평균 14.0 · p95 9.28ms
- porter 대조는 **작업 트리를 건드리지 않고** 임포트 후 `indexer.SCHEMA` 를 갈아 끼워 쟀다
  (반복 78 사고의 교훈을 그대로 적용)
- 다음: e2e phase — 어순 질의를 시나리오에 반드시 넣는다

## 반복 81 — e2e(tokenizer 3/3) · 계획 010 DONE · porter 보류를 닫음

- **앞 세션이 e2e phase 도중 API 502 로 죽었다.** 커밋 안 된 `e2e/tokenizer_e2e.py` 가
  작업 트리에 남아 있었다. 끝까지 쓰여 있었고 rc=0 이었지만 **통과했다는 사실은 근거가
  아니다** — 변이 6종(2-gram 분기 제거·어절 이어붙이기·스니펫 `-1`·porter 제거·드리프트
  감지 무력화 ×2)으로 다시 세웠고 **6/6 잡혔다.** 한 줄도 안 고치고 그대로 커밋
- 변이 검사에서 약점 하나를 찾아 `result.md` 에 적었다: ⑥ 의 `status == 500` 은 드리프트
  감지를 꺼도 통과한다(질의가 어차피 터져서 500 이 난다). 실제로 가른 것은 CLI rc=2 다
- 전체 검증 실제 실행: **260/260** · e2e **8종 전부 rc=0** · ko 20/20 · en 19/20 ·
  미검출 5→**1** · **매치 수 평균 14.0**(기준선 13.8, +1 이내) · p95 9.05ms · 크롤 10.25/s ·
  JS 0 B · 최저 대비 4.87:1. **짝으로 갈렸다** — 미검출이 줄면서 오탐이 안 늘었다
- **반복 80 이 사람 몫으로 올린 `porter` 를 닫았다 — 유지.** 앞 세션은 **손실만 448개
  어휘로 재고 이득은 fixture 질의 1건으로 쟀다.** 앞 세션이 손실 쪽에서 정확히 짚은
  눈멂(영어 질의 20개가 전부 완전한 낱말)이 **이득 쪽에도 똑같이 걸린다** — 그 20개는
  거의 전부 기본형이다. 같은 어휘로 다시 재니 굴절형 질의 생존율이 **96.3% vs 1.0%**,
  접두 손실은 11.2% 였다. 856건을 사고 50개를 파는 거래라 유지가 맞다
- `design.md` 4절 넷 중 어디에도 안 걸려 **루프가 정해도 되는 결정**이었다(데이터 형태
  아님·`search()`/`GET /search` 무손상·의존성 없음·보안 아님, 되돌리기는 한 낱말).
  판 것은 천장으로 명시했다: `y→i` 때문에 `any*` → `anything` 도 0건이다. 우회는 완타
- 아카이브 `plan_history_010`·`design_history_010` · `index.md` 한 줄 · digest 후보 6번 추가
- 다음: 쿨다운 태우기(digest `[high]`) — 계획 phase 부터. `mark_sent` 누락 구멍을 설계에서 먼저 닫는다

## 반복 82 — 계획(cooldown-burn): digest [high] 를 열었다. 처방이 틀렸다

- 보류 사유부터 재검토했다. "프런티어 계약 변경 = 설계 승인" 이었는데 프런티어는
  **내부** 계약이고 호출처가 `crawl.py:66` 하나뿐이라(grep 실측) `design.md` 4절
  넷 중 어디에도 안 걸린다 — 설계가 스스로 정하면 되는 항목이었다
- **digest 의 처방이 아무것도 회수하지 못한다는 것을 실측으로 찾았다.** 8일 전에
  적힌 답은 "제출 시점 `mark_sent` + 팝 쓰기 제거" 였는데 **4.48 → 4.49/s**.
  제출 시점에는 robots 판정이 아직 안 났기 때문이다(`_fetch_one` 이 워커 안에서 부른다).
  **실제 발신 시각에만** 시계를 걸어야 **10.25/s** — 2.29배가 나온다
- 시나리오도 digest 가 적은 것("이어받기 크롤 한정")과 달랐다. 이어받기는 `store.has`
  가 시드부터 막아 **크롤이 0으로 끝난다**(별개 [high], recrawl 소관) — 잴 수가 없다.
  값이 나오는 곳은 **robots 가 막는 신규 크롤**이고 그건 지금 당장 도는 경로다
- **덤으로 하나 더 나왔다: 현재 값이 컨셉 성능 2(5.0/s)를 밑돈다.** robots 가 절반을
  막는 사이트에서 4.48/s. `perf_crawl.py` 가 **아무것도 막지 않는 사이트만** 재서
  8일 동안 안 보였다 — 기준선 10.25/s 는 막는 것이 없는 세계의 숫자였다. 스텝 2가 그 눈이다
- 008 리뷰가 남긴 경고는 유효했다: 팝 쓰기를 그냥 지우면 워커 예외 경로에서 `mark_sent`
  가 빠져 간격 위반이 열린다. **스텝 1에서 `frontier.py`·`crawl.py` 를 한 커밋으로 묶는다**
  — 쪼갠 중간 커밋이 1초 계약이 깨진 상태이기 때문이다. 계약은 계획 끝이 아니라 매 커밋에서 지킨다
- 팝 쓰기를 지우면 `test_frontier.py` **9건**이 깨진다(복사본에서 실측). 지우지 말고
  새 계약으로 다시 쓴다 — 재는 것은 그대로 두고 시계 거는 방법만 바꾼다
- 탐침은 전부 scratchpad. `perf_crawl.py` 하네스를 빌려 썼고 네트워크를 치지 않았다
- 다음: 설계 phase (대안 3 · 예외 경로 구멍을 설계에서 먼저 닫는다)
