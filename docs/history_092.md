# 기록 092 — 반복 616~624 (계획 105 `crawl-gap-ab` 개발~마감 · 짧은 경로 `interval-call-site` · 탐색 빈손)

### 반복 616 — 계획 phase · 못이 사양보다 느슨하다 · 계획 105 `crawl-gap-ab`

`deadline_e2e` 를 오늘 2판 돌려 **1판 실패**(대조군 간격 **0.948초**) — 9일 전 후보 `[6]` 이
그대로 살아 있다. 후보가 건 여는 조건(「1초인가 ±50ms 인가」)은 **`concept.md:25` 가 이미
정했다** — 「1초 이상은 전제 조건」. 못 `0.95` 가 사양보다 느슨하다. 발신/수신을 가른다.

### 반복 617 — 개발 phase · 스텝 1 · 탐침이 **자를 지목했다**(②수신)

10판. **도착 시각 26칸 전부 ≥1.0**(최소 1.007) · 1.0 을 깬 9칸은 **전부 응답시작 쪽**(최소 0.907).
rc=1 두 판도 죽은 자리가 응답시작이고 **같은 판 같은 도메인의 도착은 1.054·1.030 으로 멀쩡했다.**
원인은 핸들러가 `time.sleep(delay)` **뒤**에 찍는 것 — `delay` 는 상수라 상쇄되나 **잠의 초과분**이
매번 달라 4도메인 동시일 때 100ms 넘게 스윙한다. **후보 `[6]` 은 크롤러 버그가 아니라 자의 버그다.**

### 반복 618 — 개발 phase · 스텝 2 · 자를 옮기고 못을 사양에 붙였다

단언을 도착 쪽으로 옮기고 못 `0.95` → **`1.0`**(두 파일), 「예산이 깎았다」를 원인 중립으로.
**5판 rc=0**(도착 1.002~1.146) · 같은 판 응답시작은 0.908 까지 내려갔다 — **옛 못이면 오늘도 죽었다.**
변이 넷: 제품 간격 0.5 **죽음** · 못만 0.5 **삼** · 단언 삭제 **삼** · 도착 로그 삭제 **죽음**(새 하한 못).
**사양 인용 가드가 물었다** — 낫표로 인용하면 대조가 안 된다. 큰따옴표라야 원문과 붙는다(전수 2건 빨감).
제품 `src/` **0줄** · 전수 **809 OK**.

### 반복 619 — 테스트 phase · **이틀치 발견이 전수에 자국을 안 남겼다**

갭을 변이로 쟀다: 단언을 옛 자리로 되돌려도, 못을 `0.5` 로 내려도 **둘 다 809 OK**.
9일을 조용히 산 두 줄이 다시 조용해질 수 있다 — 8점으로 보고 `tests/test_e2e_interval.py`
를 세웠다(`test_e2e_fd.py` 관용구: e2e **소스의 낱말**을 센다 · 하한 못은 「정규식이 0건」).
**변이 넷 전부 죽는다** · 전수 **811 OK** · README 건수 가드가 또 강제했다(계획 76·98 과 같은 자리).
**게이트 ⑨ 네 번째**: autocommit 이 스텝 2 를 스냅샷으로 가져가 푸시했다(내용 온전 · 이력만 갈림).

### 반복 620 — 리뷰 phase · **diff 가 제 주제를 어긴 자리를 물었다**

패스 A(넘길 세션 없음 · 규율로만) 2건 자동 수정. **[R105-1]** 로그 셋 중 시나리오 2 뒤에만
`del REQUEST_LOG[:]` 가 짝 없이 있었다 — 오늘은 뒤에서 도착을 읽는 자가 없어 무해하나
**「두 시계를 짝으로 다룬다」가 이 계획의 주제다.** `clear_logs()` 한 자리로 모아 갈래를 없앴다.
**[R105-2]** `make_handler` docstring 의 「도착」이 새 `ARRIVAL_LOG` 와 낱말이 겹친 것.
패스 B: 계획서 파일 목록에 `tests/`·README 가 없으나 각각 갭 처리·건수 가드가 강제 — 정당.

### 반복 621 — e2e phase · **자를 고치니 못이 2ms 위에 섰다**

`deadline_e2e` **5판 연속 rc=0** — 도착 15칸 최소 **1.006**, 같은 판 응답시작 최소 **0.950**
(판 3 대조군은 한 쌍을 두 시계가 **81ms 차**로 읽는다). `crawl_delay_e2e` 3판 rc=0.
음성 대조가 **새 구멍**을 냈다: 못을 내린 변이는 가드가 물지만 **간격 검사 «호출»을 지운
변이는 아무도 안 문다** — 가드가 함수의 몸만 읽는다. 제품 하한은 `test_frontier.py:319`
가 따로 물어 RED 가 아니라 **후보**다. 품질 축은 크롤 간격·의존성 0 만 통과.

### 반복 622 — 마감 · **이 밤은 셋을 닫고 끝난다 (3계획 상한)**

계획 105 아카이브(`plan_history_080.md`) · `index.md` **완료 · e2e 통과** · `digest` 후보
`[6]` 에 취소선과 닫힌 사유(9일 묵은 것) · 새 후보 1건(「소스를 읽는 가드는 호출 자리도
세야 한다」 · 여는 조건 없음). 스텝 실측 41줄은 버리지 않고 e2e 기록 부록으로 옮겨
`status.md` 를 218→177줄로 줄였다. **정지 사유는 계획 상한**이다 — 5h 30%·7d 55% 로
한도는 여유가 있다. **게이트 ⑨ 네 번째 피해**를 `status.md` 에 박았다(아침 1순위).

### 반복 623 — 짧은 경로 `interval-call-site` · **재는 자는 불려야 잰다**

어젯밤이 등재한 후보 `[6]` 를 **하루 만에** 닫았다. 먼저 **재현**: `check_intervals` 호출
세 자리를 지우니 `deadline_e2e` 는 `rc=0`, 전수는 **811 OK** — 가드가 함수의 **몸**만 읽어
호출 0을 못 본다. `CHECK_CALLS` 로 `def` 줄을 뺀 호출 자리를 세 셋을 못박았다. 변이 2판
**둘 다 죽는다**(전부 지움 3≠0 · 하나만 지움 3≠2). 건수 가드가 **다섯 반복째** 물어
README 811→**812**. 제품 `src/`·`e2e/` **바이트 단위 무변경**. 후보가 함께 주장한 「같은
모양이 `test_e2e_fd`·`test_readme` 에도 있다」는 **실측으로 반증** — 그 둘은 애초에 호출과
사용처를 센다. 커밋 `38a0a8e`.

### 반복 624 — 탐색 phase · **빈손이고 그것이 결론이다** (정지: 탐색 막힘)

여덟 출처 전수 재측. **1~3 은 0건**(전수 **812 OK** · 코드의 `TODO`/`FIXME`/`HACK` **0건**) ·
4순위 큐는 **파일이 비어 있다**(후보 `[6]` 의 「구조적으로 늘 0건」 그대로). 5·6순위의 열린
항목을 하나씩 실측해 갈랐더니 **전부 ⓐ교훈 ⓑ이미 닫힌 것 ⓒ사람 판단 대기** 셋 중
하나였다 — 상세는 `status.md` 「반복 624」 절. `concept.md` 축도 댔으나 **기능 6** 은
`test_serve.py:547`, **성능 1·4** 는 `design_check` LCP 대리와 `perf_search` 가 이미 덮고,
남은 **성능 3** 은 오늘 파이프라인이 손으로 거는 배치라 **「지연」의 정의부터 사람 몫**이다.
**없는 일을 지어내지 않았다**(`discover.md` 2절) — 계획 상한(3)도 한도도 아닌 **탐색 막힘**으로 멈춘다.
