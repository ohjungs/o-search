---
signal: GREEN
phase: 계획
step: 0/0
attempt: 0
iteration: 424
updated: 2026-09-07
ctx: 42
night_iterations: 0
night_red: 0
night_retries: 0
plan: null
---

## 현재 상태

**대화형 짧은 경로 한 건. 후보 `[5]` 를 열려다 실측하고 닫았다.** 전수 **648 OK rc 0**.

## 짧은 경로

- **근거**: `digest.md` 후보 `[5]` 「단위 스위트 벽시계의 **68%** 가 테스트 하나다」의
  숫자가 낡았다. 오늘 실측 — 대상 1건 단독 **8.060초** · 전체 648건 **15.923초** ·
  그 1건만 뺀 647건 **7.725초** → 비중은 **50.6%** 다. 절대값 8초는 8일째 그대로고
  분모가 462→648 로 커진 만큼 비율만 내려갔다.
- **할 일**: 그 후보에 오늘 실측을 부기하고 **열지 않고 닫는다**(취소선 + 닫는 이유).
  `docs/digest.md` 한 줄만.
- **완료 기준**: 전수 648 OK · `digest.md` 줄 수 **198 유지**(상한 200).
- **이미 한 것**: 전부. 편집·검증·기록 완료.

## 왜 열지 않고 닫았나 (거래를 양쪽 다 쟀다)

항목이 적어 둔 처방은 「느린 행동 검증을 e2e 로 옮긴다」였다. **양쪽을 같은 자로 재니 손해다.**

| | 버는 것 | 치르는 것 |
|---|---|---|
| e2e 로 이동 | 스텝당 **8초** | 「락을 실제로 기다린다」는 **행동** 검증이 매 스텝 → **계획당 한 번** |

한 스텝의 실비용은 LLM 왕복이라 8초는 잡음이다. 항목이 대체재로 지목한
`test_every_connection_waits_the_same_thirty_seconds` 는 30초라는 **값**만 고정하지
기다림을 안 잰다 — 대체가 아니다. **일반화: 비율로 놀란 항목은 절대값으로 다시 잰다.**

## 사람 몫 — 셋이 그대로다

1. **`digest.md` 회전** — **198줄**, 상한 200 에 여유 두 줄. 무인 모드는 삭제를 못 하므로
   **다음 회전은 사람이 연다.**
2. **후보 `[7]`** — 문서 상한(200·300)을 재는 자 0개. 1번이 먼저 정해져야 못을 세운다.
3. **후보 `[6]`** — 실물 후보 절의 포인터 모집단을 재는 자 0개. 규약 변경이라 별건.

**높은 점수 후보 셋(`[9]`·`[8]` 뒷부분·`[6]`)은 전부 같은 문을 여는 조건으로 지목한다 —
「실물 크롤 코퍼스」.** 오늘 저장소에 실물은 없다(`e2e/quality/corpus.json` 64건은 `q.test`
합성 · `data/crawl.db` 12KB). **문을 여는 것은 외부 사이트 실크롤이라 사람이 정할 일이다.**

## 검증 (맨몸 · 리다이렉션 0)

```
PYTHONDONTWRITEBYTECODE=1 PYTHONPYCACHEPREFIX=$(mktemp -d) PYTHONPATH=src python3 -m unittest discover -b -s tests
Ran 648 tests in 15.9s · OK · rc 0
```

`src/`·`e2e/` **0줄** · `docs/specs/` 무접촉 · `data/crawl.db` 무개봉 · 바깥 네트워크 0.
