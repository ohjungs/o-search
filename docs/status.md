---
signal: YELLOW
phase: 계획
step: 0/0
attempt: 0
plan: null
iteration: 563
updated: 2026-09-13
mode: night
night_iterations: 1
night_red: 0
night_retries: 0
night_self_amendments: 0
ctx: 측정 불가 — 공유 파일의 `session_id` 가 **내 슬러그에 없다**(게이트 ⑦ · 563 이 판정을
  한 줄로 줄였다). statusLine 이 꺼진 것과 동일 취급 — 반복 상한에만 의존한다
note: 열두 번째 빈손. 핀이 네 밤 연속 맞았다(첫 줄 빔 · 이슈 0 → 전수만 1회 **763 OK** 20.7s).
  한 것은 둘 — **⑦ 소유 판정을 추정 없이 한 줄로**(state 의 `session_id` 로 `<내 슬러그>/<id>.jsonl`
  존재를 본다) · **읽기 상한 599/600 을 닫힌 서술 압축으로 풀었다**(status 126→92)
---

# 현재 상태

계획 **96 `docs-delete-rowid` 완료** 이후 새 계획이 없다. **탐색 0건 — 열한 세션 연속**이다
(552~555 는 여덟 출처를 네 번 따로 전수, 556~563 은 아래 핀).

## 빈손 대조 핀 — 여덟 출처를 다시 안 재도 되는 조건

기준 트리는 **`c725ed7`** (560~563 이 전수로 확인한 것: 763 OK · 코드 `TODO` 0 · 4순위 스캐너
0건 · 이슈 0 · 패치는 `userinfo-leak` 하나 · 활성 `plan_*.md` 은 보류분 하나). 1~7순위는
**트리의 함수**라 트리가 같으면 결과가 같다. **8순위(이슈)만 트리 밖이라 둘째 줄을 «항상» 돌린다.**

```bash
git diff --stat c725ed7..HEAD -- src tests e2e scripts docs/digest.md docs/candidates.md docs/specs
gh issue list --state open --limit 20     # 트리 밖 — 항상 돌린다
```

**560~563 네 밤 연속 맞았다** — 첫 줄이 비어서 5·6·7순위를 안 열고 전수 한 번으로 끝냈다.
**4순위 칸은 스캐너지 파일이 아니다**(562 정정) — `candidates.md` 는 존재한 적이 없고 없는 파일은
스캐너가 죽어도 늘 0 이다. **핀의 수명은 회전까지다** — 핀을 깨는 유일한 것이 `digest.md` 이고
그걸 건드리는 유일한 것이 `history_current.md` 300줄 초과 회전이라, 그 밤은 오경보를 낸다.
**핀은 «코드»의 함수지 «환경»의 함수가 아니다** — 파이썬·OS 가 움직이면 같은 트리가 다른 결과를
낸다. 그래서 **전수는 밤마다 돌리고**(763 OK · 20.7s · Python 3.9.6) 빔을 확인하면 기준을 HEAD 로 옮긴다.

## 사람 결정 대기 — 하나를 열어야 이어진다

근거·재현·처방은 갖춰졌고 막는 것은 판단뿐이다. 밤에 새로 막힌 것은 **여기 먼저 적는다**
(야간 보고서가 이 절을 그대로 퍼 간다). **서술은 닫혔다 — 상세는 괄호 안 반복 번호에 있다.**

- **① 동시 색인이 만드는 중복 `docs` 행** — 반복 550 리뷰 보류. 고치는 자리는 `index_pages`
  의 트랜잭션 경계(`BEGIN IMMEDIATE`)이고 재구축 갈래의 `BEGIN`(`indexer.py:217`)과 중첩을
  함께 풀어야 한다. **자기 계획 크기**이고 레이스 컨디션이라 밤이 안 연다. 승인하면 바로 계획.
- **② `perf_crawl.py` 기준선 여유 0.5%**(digest 후보 8점) — ①재기준선 ②[열림] 대비 비율
  ③참고용 강등 **셋 중 택1**. 밤이 못 고르는 이유는 셋 다 「통과시키려고 시나리오를 낮추는
  것」과 겉모양이 같아서다.
- **③ `docs/patches/userinfo-leak-refuse-credentials.patch`** — URL 자격증명이 크롤·저장·렌더로
  새는 건. 적용 상태에서 초록 확인 후 뽑아 뒀다. **보안 경계**라 밤이 영구히 못 연다.
- **④ `~/.claude/settings.json` 미커밋** — 내 변경이 아니고(`model: opus → opus[1m]` + `allow`
  자리 정렬 · 권한 상향 아님을 diff 로 확인) 코어층 가드가 막았다. 승인하면 `--no-verify`.
- **⑤ `~/.claude/hooks/ojeong-guard.cjs`** — `RUNNER` 가 `^python3` 로 앞머리를 고정해 **환경변수로
  시작하는 이 저장소의 정식 테스트 명령을 검증으로 한 번도 안 센다.** 고칠 내용은
  `~/.claude/hooks/guard.test.js` 가 명세한다. 분류기가 그 파일 쓰기를 막아 내가 못 고친다 —
  나를 제약하는 가드를 내가 푸는 것은 권한 상향이라 우회하지 않는다.
- **⑥ `docs/digest.md` 211줄**(상한 200) — **561 이 산술로 확인: 승인으로도 안 풀린다.** 유일한
  레버(「오래된 완료 항목부터 지운다」)가 닿는 줄이 **넷**뿐이라 다 지워도 **207** 이고, 남는
  175줄은 규칙이 남기라 한 절과 **탐색이 입력으로 읽는** 후보 절·사람 대기열이다.
  `tests/test_docs.py:420-424` 가 **일부러 안 재는** 축이다 — 처방이 삭제라 못을 박으면 야간이
  스스로 못 푸는 RED 가 된다. **아침 처방**: 「지우게 해 달라」가 아니라 **「상한 200 이 이 파일
  모양에 안 맞으니 레버나 숫자를 고쳐라」**.
- **⑦ 정지 조건이 `session_id` 를 안 본다** (557 최초 · 563 이 일곱 번째) — `.context-state.json`
  은 **모든 세션이 공유하는 파일 하나**고 신선도 검사는 누가 썼는지를 안 본다. 562 가 잰 천장:
  신선도는 **한 세션 안에서 탈락→통과로 뒤집힌다**(남의 세션이 살아서 같은 값을 계속 덮는다).
  **563 이 판정을 한 줄로 줄였다 — 「최신 전사본이 내 것」 추정이 필요 없다:**
  `~/.claude/projects/<내 cwd 슬러그>/<state 의 session_id>.jsonl` 이 **없으면 남의 것**이다
  (563 실측: `402da0af…` 는 홈 슬러그에만 · 내 슬러그 32개 중 없음 · 내 것은 `b5a63f54…`).
  부정 판정에 내 id 가 필요 없어 **동시 두 세션 천장도 이 방향으로는 안 걸린다.**
  처방은 그대로(남의 것이면 **statusLine 꺼진 것과 동일 취급**)고, 고칠 자리가 SKILL.md 의
  «나를 멈추는 룰»이라 밤이 안 연다 — ⑤ 와 같은 이유다. **⑧ 의 `ctx` 칸은 이미 오염됐다.**
- **⑧ `metrics.jsonl` 이 밤마다 다른 것을 센다** (560 최초 · 561·562 확인). 셋이 동시에 깨졌다.
  **(ㄱ) `iterations`** — SKILL.md:226 의 「야간 시작 시 리셋」이 세션인지 그 밤인지를 안 정해
  (`metrics-record.js:48` 이 그대로 적는다) **합 626 > 누적 반복 559** 다. 손목록 37줄 대 jsonl
  55줄이라 「평균 반복 4.7」도 근거가 없다. **(ㄴ) 해상도 0** — 빈손 밤엔 변하는 칸이 `ctx` 뿐인데
  그게 ⑦ 로 굳어 최근 8줄이 **값 둘뿐**이다(562 `uniq -c` 실측). 세션을 구별하는 유일한 성질이
  **줄의 중복도**라 `sort -u` 한 번에 세션 수가 소리 없이 줄고 복원 못 한다. **(ㄷ) 「정지 사유」**
  — 53줄이 **스키마 두 가지**(32줄엔 `stop` 칸이 없다)라 표가 아는 건 40% 고, 아는 구간에서도
  `계획 소진` 3/7 · `컨텍스트 상한` 6/2 로 **뒤바뀌어 있다** — 아침에 반대 처방을 낸다.
  처방: 리셋 단위를 세션으로 못박고 줄에 세션 식별자를 넣는다. 자리가 공유 스크립트라 밤이 안 연다.

병합은 손으로 조립하지 않는다 — `scripts/merge-to-main.sh` (전수가 초록일 때만 민다).

## 규모 축 (2026-09-13)

`pages` **52,172** · `docs` **40,347** · 2.48GB · 전수 **763 OK**.
필수 읽기 **589줄**/600 (`project.md` 249 + `status.md` 103 + `history_current.md` 237) —
회전분은 `history_083.md`(반복 532~551). `project.md` 는 제 예산 60 의 4배지만 「거의 안 바뀌는
사실」이라 안 건드렸다 — **남은 최대 레버는 여기다.**

**563 이 읽기 상한을 풀었다** — 562 가 599/600 으로 남기며 고른 둘 중 **② 닫힌 서술 줄이기**다
(① 조기 회전은 digest 를 건드려 ⑥ 을 악화시키고 핀을 깬다). 지운 것은 ⑦·⑧ 의 재현 서사와 ⑥ 의
산술 과정뿐 — **근거·처방·왜 밤이 못 여는가는 전부 남았고** 서사는 `history_current.md` 561·562 에 있다.
**다음 못은 읽기 상한이다**(회전은 237/300 으로 두세 밤 뒤). **압축은 벌써 한 번 제자리였다** —
563 이 26줄 벌고 첫 기록 27줄로 도로 600 이었다. **진짜 레버 둘**: ㉠ `project.md` **249줄**
(562·563 둘 다 「거의 안 바뀌는 사실」이라며 **안 쟀다**) · ㉡ **기록이 규격의 4~6배**
(SKILL.md append 5줄 · 최근 20~30줄 · 563 이 제 것을 10줄로 줄였다). ㉡ 는 밤이 스스로 지킨다.
