# 기록 (현재)

### 반복 532~534 — 계획 91 `frontier-persist` · 크롤이 실행을 넘어 이어진다

- **계획 87 이 「정답」으로 적고 88 이 절반만 닫은 것을 마저 닫았다.** 88 은 `pages` 에
  **이미 있는** 미완 URL 을 되찾았지만 **큐에만 있다가 종료된 URL** 은 여전히 잃었다.
  컨셉 1단계(10만)는 한 번에 도는 크기가 아닌데 `--max` 로 끊긴 크롤이 다음 실행에
  그 지점을 못 이어받았다.
- **실물: 1회차 수집 3 · discovered 20 → 2회차 수집 5**(시드가 신선한데도 이어받았다).
  **고치기 전 2회차는 0장**이었다.
- **`pages` 에 안 박고 `discovered` 표를 따로 뒀다** — `pages` 는 「받아 본 것」이고
  발견은 수집이 아니다. 거기 넣으면 계획 84·86 이 429 에서 닫은 거짓(**「안 받은 것을
  받았다고 적는다」**)을 다시 여는 것이다. **지우는 자리는 안 만들었다**: 받고 나면
  `pages` 행이 생겨 `unfinished()` 가 알아서 뺀다(지우는 곳이 둘이면 한쪽만 고쳐진다).
- **`Frontier.add` 가 받아들인 것을 돌려주게 했다.** 처음엔 `u in frontier` 로 물으려
  했는데 그 연산자가 없었고 **만들지 않았다** — 부르는 쪽이 범위·중복을 다시 판정하면
  거르는 자리가 둘이 되고 새 호출부가 생기는 날 샌다. 덕분에 계획 88 의 「되찾기가
  `--same-site` 범위를 안 넓힌다」가 여기서도 **공짜로** 지켜진다.
- 전수 **753 OK rc 0**.

### 반복 535 — 짧은 경로 `verdict-head` · 자리 약속을 이름으로 바꿨다

- **어젯밤 「여덟 출처 전부 0건」은 절반이 틀렸다.** 6순위를 「84항목 전수 판독」으로
  닫았는데 `digest.md` 의 후보 절은 **둘**이고 「테스트 phase 갭, 8점 미만」 절을
  안 봤다 — 거기 착수 가능한 `[6]` 이 둘 있었다. 오늘 그중 하나로 돌았다.
- `verdict_gap` 은 「index.md 다섯째 칸이 e2e」라는 **자리 약속** 위에 서 있었고,
  행 수 대조가 잡는 것은 열이 **줄어든** 쪽뿐이었다. e2e **앞에** 열을 끼우면 개수가
  맞아 통과하고 엉뚱한 칸을 판정으로 읽어 **틀린 RED** 를 낸다. 자리 대신 **이름**을
  본다(`VERDICT_HEAD` — 머리의 다섯째가 `e2e` 가 아니거나 머리가 없으면 신고).
- **단언을 어디에 거느냐가 이 스텝의 전부였다.** 새 케이스의 행은 옛 갈래(날짜 칸)로도
  울어서 `assertIsNotNone` 만으로는 **다른 이유로 초록**이다 — `표 머리` 라는 낱말에
  걸어야 이 자를 잰다(`digest` 「부정 단언 하나가 다른 이유로 통과할 수 있다」의 적용).
  TDD RED 출력이 오귀속을 그대로 찍었다: ``e2e 칸에 판정이 아니라 날짜가 있다 … `2026-09-07` ``.
- 곁가지: `test_verification_counts_match_reality` 가 README 단위 수를 물어 753 → **755**.
  전수 **755 OK** · 제품 `src/` **0줄** · 계획서·설계서·e2e 문서 없음(짧은 경로).

### 반복 536~538 — 계획 92 `relink-backfill` · 닭-달걀을 네트워크 없이 푼다

- **코퍼스 14,105장인데 크롤이 0장을 받았다.** 429 행 0(야간이 전부 회수) · 30일 넘은
  행 0 · `discovered` 0 → 시드 12개가 전부 신선 → 팝에서 스킵 → 링크 추출 0 → 큐가
  빔. **계획 91 의 `discovered` 가 막게 돼 있는데 그 표가 비어 있었다** — 영속화가
  들어오기 **전에** 마지막 크롤이 끝났기 때문이다. 채우려면 크롤이 돌아야 하고 크롤은
  전부 신선해서 못 돈다 — **30일을 기다리는 것 말고 길이 없었다.**
- **링크는 이미 우리 손에 있다.** `pages.html` 원문에 `links.extract` 를 다시 돌리면
  된다 — **요청 0건**. 실측 200장 → 38,794개 · 20.8ms/장 · 전체 환산 293초.
- **`frontier.empty()` 로 물었더니 영영 안 돌았다** — 시드가 아직 큐에 있어 거짓이고,
  스킵은 그 뒤 팝에서 일어난다. 「되찾을 것이 없고 시드가 전부 신선하다」가 **한 건도
  안 나가는 상태**의 정확한 표현이다. **평소 비용 0**, 막혔을 때만 한 번.
- **전수 241초를 한 번 오해했다** — 계획 90 과 같은 부류(실물 코퍼스 접촉)를 의심했는데,
  모듈별 합계 39초 · 재실행 62초(CPU 11%)로 **첫 실행의 일회성**이었다. 숫자 하나로
  결론 내지 않고 갈라 잰 것이 값을 냈다. 전수 **758 OK rc 0**.

### 반복 539~540 — 계획 93 `default-db-binding` · 계획 90 의 수정이 실은 안 먹고 있었다

- **파이썬 기본 인자는 정의 시점에 묶인다.** 계획 90 이 `setUpModule` 로
  `crawl.DEFAULT_DB` 를 패치해 「테스트가 실물 코퍼스를 건드리는 것」을 막았다고 믿었는데,
  `def crawl(..., db_path=DEFAULT_DB)` 가 그 값을 이미 고정해 **모듈 속성을 바꿔도
  소용이 없었다.**
- **그때는 안 드러났다** — 실물이 전부 신선해 크롤이 아무것도 안 받았기 때문이다.
  **계획 92 의 링크 되살리기가 그것을 깨웠다**: 테스트 한 번에 실물 DB 로 **1,001행**이
  들어갔고, 그 전 실행에서는 **3,580개 도메인**으로 실제 요청이 나갔다.
  **느린 것보다 나쁘다 — 남의 서버로 요청이 나간 것이다.**
- 처방은 `db_path=None` 으로 두고 **함수 안에서** 해석하는 것. 확인: 전수
  **240초 → 20초**, 전수 전후 `pages` **21,168 → 21,168 무변**.
- **계획 90 은 증상(느려짐)을 고쳤지 원인을 못 고쳤다.** 그때 「전수 전후 행 수가
  같은가」를 한 번만 쟀으면 그날 잡혔다 — 이번에 그 단언을 테스트로 남겼다.
- 전수 **759 OK rc 0**.

### 반복 541~542 — 계획 94 `merge-script` · 손으로 조립하던 절차를 파일로 옮긴다

- **근거는 어제 내가 낸 사고다.** 병합을 매번 손으로 조립하다 `… && 전수 ; git push` 로
  연쇄가 한 번 끊겨 **전수가 실패했는데 푸시가 됐다.** 원인이 문서 상한이라 무해했지만
  구조는 「빨간 main 을 민다」였다. **손으로 조립하는 절차는 언젠가 반드시 한 번 틀린다.**
- `scripts/merge-to-main.sh` 로 옮겼다. **`set -e` 는 안 쓴다** — worktree 정리는 실패해도
  반드시 돌아야 하는데 `set -e` 가 그 분기 전에 죽는다. 갈래마다 `rc` 를 명시 확인한다.
  전수는 **병합 결과**에서 돈다(브랜치에서만 돌면 병합이 만든 충돌을 못 본다).
- **계약 셋을 진짜 git 으로 못박았다**(로컬 bare 원격 · 네트워크 0): 빨가면 main 이
  **안 움직인다** · 초록이면 **움직인다**(음성 대조가 없으면 「아무것도 안 미는 스크립트」도
  통과한다) · `loop/` 아닌 브랜치는 rc 2.
- **변이로 이빨을 쟀다** — 전수 확인 줄 하나를 지우면 빨간불, 되돌리면 초록. 전수 **762 OK**.

### 반복 543~544 — 계획 95 `unfinished-scope` · 자르고 거르면 거른 것이 안 남는다

- **계획 92 가 큐를 253,026개로 채웠는데 크롤이 또 0장을 받았다.** 원인은
  `unfinished()` 가 **범위 필터보다 먼저 상한(1,000)을 걸어서**다 — 돌려준 1,000개가
  **전부 범위 밖**(`tampabay.com`)이라 `Frontier.add` 가 다 버렸고, 범위 안 일감
  **148,000개**는 한 건도 안 나왔다.
- 처방은 판정식을 `accept` 로 받아 **자르기 전에** 거르는 것. **SQL 로 안 거른다** —
  범위 판정은 `Frontier.add` 한 곳에 있어야 하고(계획 88·91 계약) `LIKE` 로 다시 쓰면
  두 벌이 된다. 넉넉히 읽는 배수는 실측 기반(64% 가 범위 밖 · 앞 1,000개는 **100%**).
- **같은 증상이 세 번째다**(계획 88·92·95) — 「큐에 일감이 있는데 크롤이 0장」.
  세 번 다 **원인이 달랐다**: 큐에 못 들어옴 → 큐가 빔 → 큐를 잘못 잘라 읽음.
  전수 **762 OK rc 0**.

### 반복 545 — 이어받기 문서 갱신 · ctx 90% 로 정상 종료

- **컨텍스트 90% 는 하네스의 정상 종료 지점**이다(`SKILL.md` 정지 조건). 실패가 아니라
  반복 상한보다 먼저 차는 것이 정상이고, **스텝을 마무리하고 기록을 남긴 뒤** 끝낸다.
- `status.md` 를 지금 사실로 갈아 끼웠다 — **크롤이 백그라운드에서 돌고 있다**는 것과
  그 진행을 보는 명령, 규모 축 다섯의 현재값(색인 10,461 · pages 23,268 · 발견 큐
  321,913 · 1.05GB), **병합은 `scripts/merge-to-main.sh` 를 쓴다**는 것.
- **오늘의 교훈을 status 에 직접 적었다** — 「큐에 일감이 있는데 0장」이 세 번 났고
  원인이 셋 다 달랐다(못 들어옴 · 빔 · 잘못 잘라 읽음). **증상이 같다고 원인이 같지
  않다.** 그리고 「고쳤다고 믿은 것」이 안 막고 있던 자리가 둘 더 있었다(계획 93·94).
  **처방은 규율이 아니라 절차다.**

### 반복 546 — 계획 96 `docs-delete-rowid` · 후보가 적어 둔 여는 조건이 오늘 열렸다

- 탐색 1~3순위 **0건**(전수 762 OK · 린터 없음 · 코드 `TODO` 0건 — 잡힌 하나는
  `tests/test_indexer.py:984` 의 **픽스처 문자열**이다). 4순위 큐 파일 없음.
- 5순위 digest 후보 `[7]`: 갱신 경로의 `DELETE FROM docs WHERE url = ?` 가 FTS5 전수
  스캔이라 코퍼스 크기에 선형(1천 0.09 → 4만 **3.35ms**/건). **그 후보가 8점이 아닌
  이유로 적어 둔 여는 조건이 「코퍼스가 만 단위로 올라가는 날」이었고 오늘 열렸다** —
  `docs` **10,461행**·`pages` 37,704행 실측(400문서 시절 값은 0.036ms/건이었다).
- **중복 방지 5곳 확인**: `index.md` 무 · digest 완료 무 · 활성 계획 0 · 보류는
  `userinfo-leak` 하나 · `docs/patches/` 도 그 하나. digest `[8]`·`[high]`(indexer 증분
  갱신 미반영)는 **계획 80 `recrawl` 이 이미 닫았다** — 후보 절이 낡은 것이다.
- 설계 생략(트리거 무). **짧은 경로도 안 쓴다** — 바꾸는 것이 삭제의 열쇠라 틀리면
  엉뚱한 행이 지워지는 방향이고 실물 1.37GB DB 가 옆에서 돌고 있다.
- **자동 스냅샷 훅이 계획 phase 커밋을 가로챘다**(`0b34c95` · 원격까지). `digest ## 반복
  실패` 의 「제자리 변이를 자동 스냅샷이 커밋한다」 **6번째**다 — 이번엔 변이가 아니라
  정상 산출물 5파일이고 내용은 의도한 그대로다. **되감지 않는다**(선례 ⑤ · 이미 원격에
  있다) — 메시지만 「자동 스냅샷」으로 남는다.

### 반복 547 — 계획 96 개발 1/2 · 삭제 열쇠를 `rowid` 로

- **빨간불을 먼저 봤다**: 같은 `url` 행이 둘인 DB 에서 갱신을 돌리니 **1행으로 접혔다** —
  첫 쌍의 `DELETE FROM docs WHERE url = ?` 가 **조인이 아직 건네지 않은 행까지** 지웠다.
  워터마크 질의에 `d.rowid` 를 얹고 세 `DELETE` 를 `WHERE rowid = ?` 로 바꾸니 둘 다 갱신.
- **변이 3판으로 이빨을 쟀다**(`.git` 없는 사본 · `PYTHONPYCACHEPREFIX` 새 디렉터리):
  M3(갱신 갈래 → `url`) **죽는다** · M1(404·410)·M2(noindex) **산다**. 뒤 둘은 지우고 끝이라
  중복이 둘이든 하나든 0행으로 같다 — **갈리는 것은 값뿐**이고 그 값은 스텝 2 가 잰다.
  천장을 테스트 docstring 에 적었다.
- **첫 탐침이 배선 때문에 거짓 빨강을 냈다** — `src tests e2e README.md` 만 복사해 `docs/`
  를 안 옮겼더니 `test_docs` 15건이 에러였다(digest 「탐침의 배선을 먼저 의심한다」).
  `rsync --exclude .git --exclude data` 로 통째 복사해 기준선 763 OK 부터 다시 세웠다.
- 전수 **763 OK**. README 의 단위 건수 762→763(기존 가드가 강제한 셋째 파일 · 계획서에 등재).

### 반복 548 — 계획 96 개발 2/2 · 규모에서 값을 쟀다

- **양성·음성 둘 다 쟀다.** 1천·1만·4만 문서 임시 DB(`/tmp`, 실물 무관)에서 갱신 1건당
  `DELETE` 값을 옛 열쇠(`url`)와 새 열쇠(`rowid`)로 각각, 2판. 새 열쇠 **0.012 → 0.014ms**
  (4만/1천 **1.17배** — 코퍼스 크기와 무관) · 옛 열쇠 **0.164 → 11.950ms**(**72.9배** —
  후보 `[7]` 의 선형이 그대로 재현). 4만에서 **881배**. 판정 ①② 둘 다 참.
- **음성 대조가 판정의 절반이다** — 새 열쇠만 쟀으면 「원래 빠른 기계」와 구별되지 않는다.
- **표가 못 재는 것을 표 안에 적었다**: 404·noindex 갈래는 두 열쇠의 **결과가 같아**
  단위 변이 M1·M2 가 살아남는 자리고, 여기서도 갈리는 것은 값뿐이다. 실물 총량은
  「문서당 값 × 재크롤 건수」이고 재크롤 크기는 크롤 일정이 정한다.
- 측정 스크립트는 커밋하지 않았다(새 파일은 설계 트리거 · 상시로 둘 근거가 아직 없다).
  명령 전문을 결과 문서에 적어 다음 사람이 재현한다. **컨텍스트 85% 로 정상 종료.**

### 반복 549 — 계획 96 `docs-delete-rowid` · 테스트 phase · 갭 0건

- 전수 `PYTHONPATH=src python3 -m unittest discover -b tests` **763 OK** (21.3s). 변이 3판
  재현: 세 `DELETE` 열쇠를 각각 `url` 로 되돌림 → 404(288) **생존** · noindex(295) **생존** ·
  갱신(303) **사망**. 스텝 1 이 docstring 에 적어 둔 천장과 일치한다.
- 갭 탐색 6종 중 8점 이상 **0건** — 새 테스트 없음. ⑥ 404·noindex 열쇠(4점): 한 url 의
  `docs` 행들은 `pages` 행이 하나라 조인에 **전부 들어오거나 전부 빠진다** → 두 열쇠의
  상태 차이가 원리적으로 없다. 갈리는 것은 값뿐이고 값은 e2e `result.md` 가 잰다.
- ⑦ 다문서 배치의 **rowid 재사용**(3점): FTS5 가 최고 rowid 를 실제로 재사용하는 것은
  실측 확인. 그러나 `_insert_doc` 시점에 미처리 행이 전부 현존하므로 새 rowid = max+1 >
  모든 미처리 rowid → 충돌 불가. `docs.rowid` 를 참조하는 다른 테이블도 없다(전수 grep).
- **기록 자신이 회귀를 냈고 스위트가 잡았다** — 이 항목 제목을 `## 반복 549` 로 써서
  `IterationSyncTest` 가 빨갛게 울었다(`HIST_ITER` 는 `### 반복 <수>` 를 읽는다).
  **한 번 더 돌린 것이 잡았다** — 기록만 고친 뒤라 「이미 초록일 것 같은 실행」이었다.
  `project.md` 의 맨몸 규율이 겨냥하는 바로 그 자리다.

### 반복 550 — 계획 96 리뷰 · 백지 패스가 「접는 것」의 값을 되찾았다

- 패스 A 를 별도 세션에 넘겼고(diff + 수정 파일만), **지적 2건을 전부 실물로 재현**했다.
- **잃은 성질 하나를 개발도 테스트도 못 봤다** — 옛 `url` 삭제가 중복 `docs` 행을 1행으로
  접는 것이 중복을 지우는 **유일한 경로**였다. 중복은 `index_pages` 동시 실행으로 실제로
  생기고(배리어로 겹치니 10 pages → **20행**) `search()` 가 고유 3건을 **6건**으로 낸다.
  첫 탐침은 스레드가 우연히 직렬화돼 **0건**이었다 — 「안 재현됨」이 배선이었다.
- **오진이 판단을 낳았다.** 주석이 「조인이 아직 건네지 않은 행」이라고 적었는데 조인은
  중복 행을 **전부 건넨다**. 「안 본 행이니 접히는 건 우연」이라는 결론이 거기서 나왔고,
  그래서 그 우연이 **복구 경로이기도 하다**는 것을 못 봤다. 주석·docstring 자동 수정.
- 보류: 고칠 자리는 열쇠가 아니라 `rows` 앞의 트랜잭션 경계다 — 레이스 컨디션이라
  야간 금지(`severity.md` 3절). `digest ## 판단 필요` 등재. 전수 **763 OK**.
