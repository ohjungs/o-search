---
signal: DONE
phase: e2e
step: 1
attempt: 0
iteration: 208
updated: 2026-09-01
ctx: 50
night_iterations: 71
night_red: 0
night_retries: 0
---

# 현재 상태

**계획 41 `crawl-db-guard` DONE (2026-09-01).** e2e 1/1 통과 — 새 e2e 0개 판정을
진짜 프로세스로 확인하고 회귀를 전수로 돌렸다. 제품 diff 는 리뷰 이후 **0줄**.
계획서 `docs/plan_history_027.md` · 설계서 `docs/design_history_027.md` (아카이브) ·
결과 `docs/e2e/crawl-db-guard/result.md`. `digest [6]` 을 닫아 열린 항목 **49**.

## 방금 것 (2026-09-01 · e2e 1/1 · 반복 208)

### ① 판정을 믿지 않고 반증 신호를 직접 쟀다

계획 7절이 *"단위에서 세운 상황이 2-2 의 실측과 다른 예외를 내면 e2e 로 올린다"* 를
반증 신호로 적어 뒀다. 단위는 이 계약을 **목으로** 재고 사람이 보는 것은 셸의 rc 와
stderr 다 — 그래서 진짜 CLI 를 **프로세스로** 띄워 네 상황을 임시 디렉터리에서 다시 쟀다:
비 DB 파일 · 부모가 일반 파일 · 경로가 디렉터리 · 부모 쓰기 불가 **전부 rc 1 ·
stderr 정확히 한 줄 · `Traceback` 0회**(착수 실측은 같은 넷이 14~16줄) ·
대조군(정상 디렉터리)은 **rc 0 · `수집 0 페이지`**. 다른 예외 0건 → **판정 유지, 새 e2e 0개.**
탐침은 스크래치패드에서만 살고 저장소에 안 들어온다 — 남길 값이 없다(단위 셋이
같은 넷을 붙들고, 프로세스 배선은 계획 40 이 전수로 확인했다).

### ② 회귀 전수 — 19종 전부 rc 0

러너를 파이프로 감싸지 않고 한 번에 하나씩 맨몸으로 돌렸다(`## 반복 실패` 7회).
**19종 중 12종이 `crawl()` 을 직접 부르는데 새 예외가 정상 경로를 안 가로챘다** —
그물을 `Store(db_path)` **한 줄**에 건 설계 결정이 실물로 확인된 자리다(대안 A 였으면
크롤 도중 예외가 이 그물에 걸렸다). 품질·성능 한 축도 안 움직였다:
ko 100% · en 95% · p95 **8.74ms**(예산 300ms) · 열림 **10.25/s** · JS 0 B · 대비 4.87:1.

### ③ 곁가지 — `index.md` 41 행에 리뷰 줄이 통째로 없었다

반복 207 이 `status.md` 에만 적고 `index.md` 를 안 봤다. `## 판단 필요` 의
*"어긋나는 것은 늘 `index.md`"* 와 같은 자리의 **네 번째**다(이번엔 스텝 칸이 아니라
phase 요약 줄). e2e 가 DONE 을 적으며 리뷰·테스트 줄까지 함께 채웠다.

**검증**: 단위 **466건 OK 11.910초**(맨몸 실행, 파이프 0) · `e2e/*.py` **19종 rc 0** ·
`data/crawl.db` sha256 **무변경**(`85c96744…5bda18`) · `digest.md` **49항목 200줄**.

## 다음 스텝 (계획 42 — 탐색부터)

계획 41 이 닫혔다. 다음 반복은 **계획 탐색**(`discover.md` 1~5순위)이다.
착수 전에 `git push` 와 PR 상태를 먼저 본다 — 이 브랜치는 PR 을 열어 뒀다.

**남긴 것**: 크롤 **도중**의 `store.upsert` 실패는 오늘도 트레이스백이다(설계 물음 1 이
"N페이지를 줍고 나서 죽었다" 는 다른 문구가 필요하다며 일부러 안 덮었다) ·
락 갈래를 따로 안내하는 것은 `Store` 의 `timeout=30` 을 손대는 계획이 열 때 함께 잰다.

## 한도 (넘으면 RED)

- `data/crawl.db` 실물·스키마 무변경 (sha256 `85c96744…5bda18`).
- 단위 **466건**이 기준선(11.9초). 466 미만이면 RED · e2e **19종**이 줄면 RED.
- `docs/digest.md` 열린 항목 **49**(`grep -c '^- \['`) · **200줄 상한**.
- **러너를 파이프로 감싸지 않는다**(`## 반복 실패` 7회. 변종: 루프·`env` 접두·`timeout`·
  리다이렉션·백그라운드 태스크. 훅이 검증으로 세는 것은 **러너로 시작하는 세그먼트**뿐이라
  `PYTHONPATH=src python3 -m unittest` 도 안 세진다 — `export` 로 갈라 쓴다).
- 의존성 추가 금지(stdlib 만) · `docs/specs/` 읽기만 · `--no-verify` 금지 ·
  `main` 직접 커밋 금지 · 외부 네트워크 금지 · 도메인당 요청 간격 1초 이상 · robots 준수.
