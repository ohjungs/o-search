---
signal: GREEN
phase: plan
step: 0
attempt: 0
iteration: 287
updated: 2026-09-03
ctx: 50
night_iterations: 117
night_red: 1
night_retries: 0
plan: null
---

# 현재 상태

**계획 48·49·50 이 `main` 으로 들어갔다 — PR #7 병합 완료.** 활성 계획 없음 — 다음은 계획 51 탐색.
직전 산출은 계획 50 `runner-quiet` e2e 1/1 통과(DONE)이고, 그 기록은 아래 절들이 그대로 남긴다.
아카이브: `docs/plan_history_036.md`(설계서 없음) · 결과 `docs/e2e/runner-quiet/result.md`.
브랜치 `loop/runner-quiet`, 기점 `373cafd` · 병합 브랜치 `loop/merge-48-50`, 기점 `38e9c50`.

## 계획 50 이 남긴 것 — 한 줄

**«판정 줄을 가린다» 의 방아쇠를 사람 손이 아니라 러너의 출력에서 없앴다.**
저장소가 자기 명령을 적어 둔 두 줄에 `unittest` 의 `-b` 를 넣어 통과한 테스트의
stdout/stderr 를 삼키고 **실패한 것만** 내게 했다 — 화면 **약 90줄 → 5줄**,
`Ran 579 tests / OK` 가 **화면의 마지막 줄**, 거른 테스트 **0건**, 시간 차 없음.
제품 `src/` **0줄** · 새 파일 0 · 새 코드 19줄 · 새 의존성 0 · 한 스텝 · 반려 0 · RED 0.

## e2e 1/1 실측

| 관문 | 결과 |
|---|---|
| 전체 검증 (`discover -b -s tests`) | `Ran 579 tests in 13.585s` · `OK` · rc 0 · **화면 5줄** |
| 린트·타입체크 | 이 저장소에 **없다** — 해당 없음(«통과» 라고 쓰지 않는다) |
| CI | `.github` 부재 — 해당 없음 |
| `e2e/*.py` 전수 | **20종 rc 0** · 새 e2e 파일 **0개** |

**전체 검증을 계획이 만든 그 명령으로 돌았다** — `-b` 가 들어간 줄이 계획을 닫는
관문으로 쓰인 첫 자리이고, 판정 줄이 화면의 마지막이었다.

**두 글자가 e2e 를 흔들 자리는 구조적으로 없다** — `e2e/` 아래에서 `unittest` 를 부르는
스크립트는 **0개**다(유일한 문자열 히트는 `e2e/quality/corpus.json`·`queries.json` 의
코퍼스 본문). 그래도 논증으로 안 끝내고 전수를 실제로 돌렸다.

## 품질·성능 축 — 전부 기준선 그대로

ko **20/20** · en **19/20** · 순위 1위 39 · 매치 평균 14.0(최소 11) ·
문단 정확도 **100.0%** · 채택률 99.5% · p95 **1.48ms** · `perf_search` p95 **8.97ms** ·
`search_api` p95 2.11ms · 처리량 [열림] **10.24/s** · [차단] **10.23/s** ·
문서집합 sha1 `541d455a…c078654b` · 디자인 4축(외부 0 · JS 0 B · 텍스트 4.87:1 · 링 3.56:1).

## 저장소 불변

`git diff --stat 373cafd HEAD -- docs/specs data/crawl.db src e2e` **빈 출력** ·
`data/crawl.db` sha256 `85c96744…75bda18` **무변경** · 제품 `src/` **0줄** ·
계획 전체가 고친 코드·문서는 `README.md`·`docs/project.md`·`tests/test_readme.py` **셋**뿐이다.

## 러너 규율 — 이번 반복 0회 (누적 30회)

**러너 호출 2회(단위 2 — 문서 편집 전후)를 전부 맨몸·단독으로 돌렸다** —
`PYTHONPATH=src python3 -m unittest discover -b -s tests` 가 두 번 다
`Ran 579 tests / OK` rc 0, 화면 5줄. 파이프·리다이렉션·`;`·`&&` 잇기 0회,
`> /dev/null` 0회. **열한 반복 연속 0회다.**

**적어 두는 것 하나.** 러너가 아닌 곳(`git`·`gh`·`grep`·`ls`)에서 `&&` **4회** · `;` **7회** ·
파이프 **3회** · heredoc **3회**(PR 본문 · `history_current` append · 커밋 메시지).
계수에는 안 넣지만 «0회» 를 «한 번도 안 이었다» 로 읽으면 거짓이라 남긴다.
앞 반복(`&&` 12 · `;` 1 · 파이프 6)과 견줘 `&&` 와 파이프가 준 것은 규율이 아니라
**일의 모양**이다 — 이번 반복은 읽기와 문서 편집이 대부분이었다.

## 문서 마감

**계획 50 의 마감**(앞 반복)은 그대로다 — 계획서를 `plan_history_036.md` 로 아카이브했고
(설계서는 없다 — 계획 6절이 생략 사유를 적었고 유일한 갈림길은 `digest [9]` 가 숫자째로
닫았다) `index.md` 50번 행을 **완료**로 올렸고 `digest.md ## 완료` 명부에 계획 50 을
등재했다(193 → **194줄**).

**이 반복(287)이 더한 것**은 병합 기록뿐이다 — 이 문서의 「원격」 절 · `index.md` 표 아래
병합 한 줄 · `history_current.md` append(**6회** · 상한 300줄/20회 안이라 회전 없다) ·
`metrics.md`(`반복` **287** · 계획 완료 **35** · 진행 **0** · `## phase 분포` 의 e2e **27**).
`반복` 은 `status.md` 의 `iteration` 과 함께 움직여야 한다(`IterationSyncTest`).
`digest.md` 무변 · `README.md` 무변경(단위 **579건** · e2e **20종** 이 그대로 참이다) ·
제품 `src/` **0줄** · `night_*` 세 필드는 야간 세션이라는 근거가 없어 안 건드렸다.
`signal` 은 **GREEN**, `phase` 는 **plan** 으로 둔다 — 다음 반복이 계획 51 탐색을 잇는다.

## 다음 — 계획 51 탐색

`digest ## 다음 계획 후보` 의 `[4]`(`-b` 가 통과한 테스트의 **경고**도 삼킨다 —
오늘의 손실은 0 이고 여는 조건은 «경고가 처음 하나 생길 때») · `[5]` · `[6]` 과
`## 다음 계획 후보 (테스트 phase 갭, 8점 미만)` 의 4~5점들이 열려 있다.

## 사람이 정할 것 — 넷 그대로다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
2. **`--focus` 는 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 이 0 일
   때만 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **반응형 360px 미검증** — 브라우저가 없어 이 저장소의 누구도 그 화면을 못 그린다.
4. **회전 규약·러너 규율의 저장소 밖 절반**과 **사양이 남긴 둘**(`docs/specs/concept.md` 의
   `## 사람이 정할 것` — 속도 제한 시점, 사양 숫자 90%·500ms·60회/분이 초안이라는 것).

## 원격 — `origin/main` 은 계획 50 까지 받았다

**미병합 0.** 사용자가 PR 권한을 명시적으로 위임해 이 반복이 열고 병합했다.

- **PR #7** — `loop/merge-48-50` → `main`. **계획 48·49·50 을 한 PR 로** 보냈다.
- **셋을 하나로 보낸 이유는 브랜치가 사슬이기 때문이다** — 49 는 48 의 HEAD 에서,
  50 은 49 의 DONE 커밋 `373cafd` 에서 땄다. `loop/runner-quiet` 하나가 이미 세 계획을
  전부 담고 있어(`git merge-base --is-ancestor` 로 48·49 둘 다 rc 0 확인) PR 셋은 같은
  커밋을 세 번 보내는 것이 된다.
- **충돌 0** — PR #6 의 병합 커밋 `687a159` 와 계획 48 기점 `6e99031` 의 **트리가 동일**해서
  (`git diff --stat` 빈 출력) 병합은 계약상 fast-forward 와 같은 내용이었다.
- 앞선 병합: PR #6 = 계획 44~47(`687a159`).

**다음 반복이 볼 것** — 병합 후 `origin/main` 과 `loop/merge-48-50` 은 같은 내용이고,
계획 51 은 `main` 을 기점으로 따면 된다(계획 42 가 겪은 「기점이 main 에 없다」 가 없다).
