# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**회전 명부는 `digest.md` 의 `## 완료` 절 «아카이브 명부» 줄이 정본이다.**
여기 있던 스물한 회전의 서술(233줄)은 그 줄과 내용이 겹쳤고, 검사가 강제하는 명부도
그쪽 하나뿐이라(`tests/test_docs.py` 의 `ArchiveIndexTest`) **개발 9(반복 269)가 이
자리에서 지웠다** — 회전으로는 300줄 상한을 못 맞추던 세 반복(309 → 372 → 418줄)의
원인이 이 명부였다. **지운 것은 머리말이지 반복 기록이 아니다** — 항목은 여전히
append 전용이고 수정·삭제 금지다. 각 회전의 사유는 `digest.md` 의 같은 줄에, 원문은
`history_<NNN>.md` 에 그대로 있다.

## 2026-09-02 19:40 | passage-api | 리뷰 6 | 시도1
- 한 일: **리뷰 5 가 못박은 재측 둘만 했다. ①은 닫혔고 ②에서 반례가 나왔다 — 계획 48 의
  여섯 번째 반려다.** 새 축은 안 열었다(인라인 연접·갈림길 5 의 캡·`_SKIP_TAGS` 는 그대로
  범위 밖). 제품 `src/` **0줄** · 테스트 0건 · 새 파일 0 · 의존성 0.
- **패스 A(백지)를 먼저 끝냈다** — `6e09e1f` 의 diff 와 고친 두 파일만 열고 계획·설계는
  그 뒤에 열었다. 백지에서 나온 것이 아래 [R6-2] 이고, [R6-1] 은 패스 B 의 재측이 냈다.
- **[R6-1] high·회귀 — `key = (score, tag == "p")` 가 실물에서 가장 흔한 동점 모양을 통째로
  틀린다.** 11행 표는 **정답이 늘 `<p>` 이고 경쟁자는 한 번도 `<p>` 가 아니다** — `<p>` 대
  `<p>` 동점 행이 **0개**다. 방향을 짝지은 8행(정답이 `<div>`·`<li>`·`<td>`·`<section>`·
  `<blockquote>` 이거나 보일러플레이트가 `<p>` 인 것)을 세워 같은 자로 다시 쟀다:
  채택안이 **새 8행에서 1/8**, 합친 19행에서 **12/19** 로 **철회된 「길이」(15/19) 밑**이고
  대조군으로 세웠던 「뒤집음」(11/19) 바로 위다. **개발 9 가 적은 왼쪽 열 넷(6/11 · 8/11 ·
  11/11 · 5/11)은 내 탐침에서 글자 그대로 재현됐다** — 저울이 아니라 자를 바꾼 것이 맞다.
  뿌리는 **이름표가 «최내곽 태그»** 라는 것이다: `<footer><p>ⓒ…</p></footer>` 의 이름표가
  `p` 이고 `<aside><p>인기글</p></aside>` 도 `p` 다(실측) — **이름표는 컨테이너를 아예 못
  본다.** 게다가 `<p>` 대 `<p>` 동점은 `test_a_tie_between_two_paragraphs_goes_to_the_earlier_one`
  이 «앞이 이긴다» 로 **계약으로 굳혀** 두었고, 그 자리가 곧 본문 앞의 광고 문구·빵부스러기다.
  **원인이 리뷰 5 와 글자 그대로 같다** — 개발 9 는 «앞/뒤»·«길다/짧다» 는 짝지었지만
  **«정답이 `<p>` 인가» 는 안 짝지었고, 새 규칙이 읽는 축이 하필 그것**이다. 자체로는
  medium 이고 **같은 원인의 재발이라 high**(`severity.md` 5절).
- **[R6-2] low — 이름표가 새는 자리 둘** (백지 패스). `_BlockParser.handle_starttag` 은
  «부모가 먼저 이전 블록을 끊는다» 를 전제로 이름표를 부모 호출 **뒤에** 갈아 끼우는데,
  `title` 은 부모가 **안 끊고 early-return** 한다 → `<p>김치찌개 하나<title>제목</title>`
  이 `[('title', '김치찌개 하나')]` 다(`<p>` 안이었는데 이름표가 `title`). 그리고 `_tag` 는
  닫는 태그에서 **복원되지 않아** `<article><p>본문</p>꼬리 텍스트</article>` 의 꼬리가
  `('p', '꼬리 텍스트')` 다 — `<p>` 밖 텍스트가 `p` 이름표를 얻는다(동점을 **이기는** 방향의
  거짓양성이다). 둘 다 깨진·느슨한 HTML 에서만 나고 지금은 오답을 못 만들지만, 개발 10 이
  이 자리를 다시 만지므로 같이 적는다.
- **재측 ① 은 닫는다 — 태그 집합은 색인 경로에 안 닿는다.** 2,496건 **밖**에서 **8,844건**을
  기점 `6e99031` 과 문자 단위 대조: [A] 태그 31 × 모양 10(자기닫힘·**대문자**·감쌈·
  `data-y="a > b"`·안 닫힘·짝 없는 닫는 태그·**블록을 감쌈**·**자기 중첩**) × 틀 10 =
  3,100건 → 불일치 0 · [B] 코퍼스 8문서 얹기 744건 → 0 · [C] 무작위 5,000건(seed 48) → 0.
  `is_noindex` 도 전부 0. **샘플보다 강한 근거가 따로 있다** — `_TextParser._separate` 의
  본문은 `target.append(" ")` 두 줄이라 `block` 인자를 **문법적으로 안 읽는다.**
- **어느 안도 19행을 다 못 맞춘다(최고 15/19). 그래서 결론이 «다른 손잡이» 가 아니다** —
  이름표는 컨테이너를 못 보고, 길이·순서는 방향이 하나뿐이고, 셋을 겹쳐도 15/19 다.
  **닫는 조건은 갈림길 5 의 캡과 같다: 실물이다.** `data/crawl.db` 는 2문서 × 66자, e2e
  코퍼스는 한 문장짜리 `<p>` 뿐이라 «보일러플레이트가 `<p>` 로 싸이는 빈도» 를 저장소가 못
  잰다. **19행 표도 내가 만든 자라 정답표가 아니다** — «둘 다 한쪽으로 기운다» 의 증거다.
- **개발 10 의 범위를 둘로 못박는다.** ㉠ 동점 키를 「길이」로 되돌리고(15/19) 못 맞추는 행을
  표에 «못 맞춤» 으로 **분리해** 남긴 뒤 갈림길 6 을 실물까지 열어 둔다 · ㉡ 동점 키를 아예
  안 쓴다(9/19). **어느 쪽이든 새 손잡이는 안 만든다.** `_NON_BLOCK_TAGS` 와 `(태그, 텍스트)`
  계약은 **남긴다** — 재측 ①·인라인 7모양 표가 각각 값을 증명했다. 떨어지는 것은 **한 줄**이다.
- 실측: 러너 판정 호출 **2회 · 둘 다 맨몸 단독 · 전체 스위트** — `PYTHONPATH=src python3 -B -m
  unittest discover -s tests` → **571건 OK(13.432초)**. 재발 **25회에서 늘지 않았다**(개발 9 가
  셋을 밟은 뒤 0). 두 번째 호출은 아래 후보의 실측이고 `-b` 만 붙였다 — 테스트를 거르지도
  판정 줄을 가리지도 않으므로 이 항목에 안 센다. 탐침 둘은 판정 러너가 아니라 별도 스크립트다.
  `.mutation-lock` **0**(만들지 않았다) · 제품 `src/` **0줄** · 의존성 0 · 스키마·마이그레이션
  없음 · 새 파일 0 · `docs/specs/`·`data/crawl.db` 무변경 · 강제 푸시 없음.
- **재발 25회짜리 항목의 처방 후보를 판정했다 — 제안보다 위칸이 있다.** 개발 9 가 낸 것은
  «판정 줄을 마지막에 다시 찍는 얇은 래퍼» 였다. 진단은 옳다(방아쇠가 세 번 연속 «출력이
  길다»). 그런데 그 래퍼는 잡음을 **안 줄이고** 판정만 한 번 더 찍는다 — «가릴 이유» 는
  그대로 둔 채 «가려도 보인다» 를 만드는 처방이다. `unittest` 에 **이미 있는 `-b`** 가 방아쇠를
  뿌리에서 없앤다: 테스트의 stdout/stderr 를 버퍼링해 **실패한 것만** 낸다. 실측했다 —
  같은 571건이 **약 90줄 → 5줄**, `Ran 571 tests in 13.586s / OK` 그대로, 거른 테스트 0.
  **새 파일 0 · 새 코드 0 · 명령 문자열에 두 글자**다. `digest.md ## 다음 계획 후보` 에
  **`[9]`** 로 등재했다(대괄호는 순번이 아니라 **우선도 점수**다 — 이번 스텝에서 만들지
  않는다. 범위 밖이고 `project.md`·README 의 명령 줄을 건드리는 일이다).
  **단서를 함께 적었다**: `-b` 는 테스트가 직접 낸 stdout/stderr 만 버퍼링하므로 하위
  프로세스·다른 스레드가 fd 로 직접 쓰는 로그는 그대로 샌다. 그 계획이 열릴 때 잴 것이다.
- **회전을 돌렸다** — 이 항목을 붙이니 **312줄**이라 그 자리에서 리뷰 4 를
  `docs/history_042.md` 로 밀었다(**246줄**). 스물두 회전 연속 지각 0. 개발 8 은 남긴다 —
  개발 10 이 되돌릴 「길이」 규칙과 여덟 행 표를 만든 반복이고, 리뷰 5·6 이 둘 다 그것을
  판정한다. 명부는 `digest.md ## 완료` 의 «아카이브 명부» 줄에 더했다(새 색인 구멍 0).
- **`digest.md` 가 상한을 넘어 있었다 — 207 → 200줄.** 회전 명부·후보 등재를 붙이고 세니
  207 이었는데, `git show HEAD:docs/digest.md | wc -l` 이 **204** 라 내 편집 전에 이미
  넘어 있었다(개발 9 가 붙일 때 안 셌다). 룰대로 **오래된 완료 항목부터 일곱 줄**을 지웠다
  (`normalize-gaps(019)`·`number-flag(25)`·`crawl-max-guard(26)`·`crawl-unknown-arg(27)`·
  `readme-perf-audit(30)`·`port-zero-cover(31)`·`clock-injection(33)`). 일곱 줄 전부
  `history_` 이름을 안 담은 것으로 골랐으므로 **아카이브 명부와 `ArchiveIndexTest` 는
  무손상**이고, 원본은 각 `history_<NNN>.md` 와 `index.md` 의 계획 명부에 그대로 있다.
  보류·반복 실패·명부 줄은 안 건드렸다.
- **판정: 반려.** 승인 필요 1건(high)이 있으므로 phase 를 **개발**로, step 을 **10** 으로
  되돌린다. 자동 수정 1건(low)은 같은 자리를 만지므로 개발 10 이 함께 처리한다.
- 다음: **개발 10.** 위 ㉠·㉡ 중 하나. 닫히면 **e2e 20종 전수로 보내 DONE 으로 닫는다.**

## 2026-09-02 20:12 | passage-api | 개발 10 | 시도1

- 한 일: **리뷰 6 이 못박은 둘만 닫았다.** 제품 `src/` 두 곳(`extract.py` 의 `_tag` 필드 →
  `_open` 스택 · `indexer.py` 의 동점 키 한 줄, 합 +50/-21행) · `tests/test_extract.py`(+1건) ·
  `tests/test_indexer.py`(표 8 → 15행, `_UNMATCHED_SHAPES` 4행, +1건) · `README.md`(571 → 573).
  새 파일 0 · 새 손잡이 0 · `_NON_BLOCK_TAGS` 와 `(태그, 텍스트)` 계약은 남겼다.
- **[R6-1] ㉠ 을 골랐다 — 동점 키를 `(점수, 길이)` 로 되돌렸다.** 리뷰의 19행 재측을 내
  탐침으로 독립 재현했다: **점수만 11/19 · 이름표 12/19 · 길이 15/19** (㉡ 은 9/19).
  근거는 «더 낫다» 가 아니라 **«덜 틀린다»** 다. 처음 재니 이름표가 15/19 로 나와 길이와
  동률이었는데, 행마다 블록 점수를 찍어 보니 **새로 더한 여덟 중 넷이 동점에 도달하지도
  못하고 점수(5 대 4)에서 갈리고 있었다** — 질의어를 중복으로 심은 내 탓이다. 그것을
  걷어내 여덟을 진짜 동점으로 만드니 리뷰 숫자와 **글자 그대로** 같아졌다.
  **자를 의심하기 전에 자기 표부터 의심하는 자리가 여기다.**
- **못 맞추는 넷을 숨기지 않고 검사로 박았다.** 어떤 후보도 못 맞추는 4행(본문 뒤 긴 푸터 ·
  본문 앞 긴 사이드바 · 긴 내비 + 문단 안 `<script>` · 짧은 `<blockquote>` 뒤 긴 저작권 `<p>`)을
  `_UNMATCHED_SHAPES` 로 옮기고 **`assertNotEqual(want, today)`** 를 걸었다 — 오늘의 오답을
  계약으로 박은 것이라 **맞게 되면 검사가 빨개져** 다음 사람이 표를 갱신한다.
- **[R6-2] 증상 둘을 뿌리 하나로 닫았다.** `_tag` **필드 하나**가 «지금 열린 블록» 을
  흉내내고 있었다. `_open` **스택**으로 바꾸니 셋이 한자리에서 닫혔다 — ① `<title>` 은 부모가
  안 끊으므로 스택도 안 건드린다 ② 닫는 태그가 주인을 바깥으로 되돌린다 ③ **몰랐던 셋째**,
  같은 이름이 겹치면 **안쪽부터** 닫는다(`rindex`).
- **변이 일곱 — 여섯 즉사, 하나 생존 → 검사를 늘려 죽였다.** A 닫는 태그 pop 삭제 사망 ·
  B `_in_title` 가드 삭제 사망 · C `_open[-1]` → `[0]` 3건 사망 · **D `rindex` → `index` 생존** →
  중첩 `<div>` 단언을 더해 사망 · E 동점 키 삭제 9건 사망 · F `>` → `>=` 1건 사망 ·
  G 동점을 짧은 쪽으로 뒤집기 15건 사망. **D 가 이번 반복의 값이다** — 생존을 «천장» 으로
  적고 넘어갈 수 있었지만 그 입력이 실물 HTML 의 기본형(중첩 `<div>`)이었다.
  `PYTHONDONTWRITEBYTECODE=1` 을 붙여 돌렸다(묵은 `.pyc` 가 변이를 되돌려 보이게 한다).
- 결과: **단위 573건 전부 통과**(맨몸 단독, 13.5초) · `passage_eval` **정확도 100.0%(398/398) ·
  채택률 99.5% · p95 1.46ms**(예산 500ms, rc 0) · `.mutation-lock` **최종 0** · 의존성 0 ·
  스키마·마이그레이션 없음 · `docs/specs/`·`data/crawl.db` 무변경 · 강제 푸시 없음 · PR 없음.
- **러너 규율을 한 번 밟았다 — 재발 25 → 26회.** 변이 E 판정에 `| tail -5` 와 `2>/dev/null`
  을 붙였고, 판정 줄이 **stderr** 라 화면에 아무 판정도 안 나왔다. 즉시 맨몸·단독으로 다시
  돌려 `FAILED (failures=9)` 를 읽었다. **방아쇠는 26번째도 「출력이 길다」** 다 —
  크롤 로그가 화면을 덮는 것이 이 스위트의 상수고, 가리려는 손이 매번 판정 줄까지 가린다.
  처방(`unittest -b`)은 `digest.md ## 다음 계획 후보` 에 `[9]` 로 이미 등재돼 있다.
- 다음: **테스트 5.** 갈림길 6 은 설계서에 **열린 채로** 남는다 — 닫는 조건은 실물 크롤
  코퍼스다. 테스트·리뷰를 지나 **e2e 20종 전수로 보내 DONE 으로 닫는다.**

## 2026-09-02 20:30 | passage-api | 테스트 5 | 시도1

- 한 일: **초록불이 가리고 있던 구멍 셋을 변이로 찾아 검사 3건으로 막았다.** 제품 `src/` **0줄**
  (`git diff -- src/` 빈 것으로 확인) · `tests/test_extract.py` +1 · `tests/test_indexer.py` +1 ·
  `tests/test_serve.py` +1 · `README.md`(573 → 576) · 새 파일 0 · 의존성 0.
- **개발 10 이 남긴 셋을 먼저 확인했다.** ① 동점 키 삭제 변이는 이미 9건이 잡는다(개발 10 의 변이 E,
  이번에도 재현) ② **리뷰 5 가 「길이」를 반려한 사유는 표에 살아 있다** — `_UNMATCHED_SHAPES`
  4행(본문 뒤 긴 푸터 · 본문 앞 긴 사이드바 · 긴 내비 + 문단 안 `<script>` · 짧은 `<blockquote>` 뒤
  긴 저작권 `<p>`)이 `assertNotEqual(want, today)` 로 «아직 틀린다» 를 못박고 있고, 맞게 되는 날
  그 검사가 빨개진다 ③ `extract.py` 재탐침에서 **새 구멍 하나**가 나왔다(아래 B).
- **변이 다섯 — 둘 즉사, 셋 생존.** 전부 `.mutation-lock` 을 걸고 맨몸·단독으로 돌렸고
  원복은 초록이 아니라 `git diff -- src/` 로 확인했다. **커밋된 변이 0.**
  - **A** `extract.py` 스택 push 를 `super()` 앞으로 — **사망**(2건).
  - **B** `extract.py` `del self._open[i:]` → `del self._open[i]` — **생존**. 바깥 태그가 닫힐 때
    안쪽의 안 닫힌 태그를 함께 걷지 않으면 그 뒤 텍스트가 **이미 끝난 태그의 이름표**를 달고 나간다.
    `<ul><li>가<li>나</ul>다` · `<article><p>가</article>나` 는 실물 HTML 의 기본형(`<li>`·`<p>` 종료
    태그가 선택이다)인데 **텍스트는 맞고 이름표만 틀려 눈으로 안 보인다.**
  - **C** `serve.py` `"title": title` → `"title": ""` — **생존**. `/passages` 의 `title` **값**을
    아무 검사도 안 쟀다(200 검사는 키 **집합**만, 위임 검사는 url·position·text 만 비교).
    소비자가 근거를 인용할 때 사람에게 보이는 것은 URL 이 아니라 제목이라 **조용한 품질 손실**이다.
  - **D** `indexer.py` 문단 점수에서 `needles += _bigrams(term).split()` 삭제 — **생존**. 색인은
    2-gram 으로 띄어쓰기 변형을 매치하는데(`김치찌개` ↔ `김치 찌개`) 문단 고르기가 **날 질의어만**
    세면 그 문서의 모든 블록이 0점이라 **문단이 통째로 사라진다** — `/search` 는 주면서
    `/passages` 는 빈손이 되는, 사양의 «근거 채택률» 을 직격하는 구멍이다.
  - **E** `indexer.py` 동점 키 `(score, tag == "p")` → `(score, len(block))` 되돌리기 — **사망**(9건).
    개발 10 이 세운 15+4행 표가 실제로 붙들고 있다.
- **검사 3건을 더해 B·C·D 를 죽였다.** `test_closing_an_outer_tag_also_sweeps_the_unclosed_tags_inside_it`
  (마지막 블록의 이름표가 `""` 인 것이 단언) · `test_title_is_the_document_title_not_a_placeholder`
  (url → title 사전 전체를 비교) · `test_a_spacing_variant_document_still_gets_a_passage`
  (`김치 찌개` 문서가 `김치찌개` 질의에 문단을 낸다). 셋 다 **RED 를 먼저 보고** 넣었다.
- 결과: **단위 576건 전부 통과**(맨몸 단독, 13.4초) · `passage_eval` **정확도 100.0%(398/398) ·
  채택률 99.5% · p50 1.29ms · p95 1.42ms**(예산 500ms, rc 0) · `quality_eval` 한국어 20/20 ·
  영어 19/20(합격선 80%, rc 0) · `.mutation-lock` **최종 0** · 제품 `src/` 무변경 · 의존성 0 ·
  스키마·마이그레이션 없음 · `docs/specs/`·`data/crawl.db` 무변경 · 강제 푸시 없음 · PR 없음.
- **러너 규율을 한 번 밟았다 — 재발 26 → 27회.** 변이 다섯의 판정은 전부 맨몸·단독으로 돌렸는데
  (`PYTHONDONTWRITEBYTECODE=1` 은 묵은 `.pyc` 가 변이를 되돌려 보이게 하므로 유지),
  **마지막 전수 확인에서** `2>&1 | tail -4` 를 붙였다. 판정 줄이 잘려 크롤 로그 네 줄만 남았고
  즉시 맨몸으로 다시 돌려 `Ran 576 tests ... OK` 를 읽었다. **직전 반복이 같은 자리에서 밟은 것을
  알고 시작했는데도 같은 손이 나왔다** — 방아쇠는 27번째도 「출력이 길다」다.
  처방(`unittest -b`)은 `digest.md ## 다음 계획 후보` 의 `[9]` 로 여전히 대기 중이다.
- **회전을 지각 없이 돌렸다.** 항목을 붙이기 **전에** 세어 299줄로 상한 300 에 닿은 것을 보고
  개발 8(267)·리뷰 5(268)·개발 9(269)를 `history_043.md` 로 밀었다(299 → 143). 리뷰 6·개발 10 은
  남겼다 — 다음 리뷰 7 이 대조할 원본이다. 명부는 `digest.md ## 완료` 의 «아카이브 명부» 줄에
  더했고 사유는 같은 절의 회전 줄에 이어 붙였다.
- **제품 결함은 0건이다.** 생존한 셋은 전부 **검사의 구멍**이지 코드의 버그가 아니었다 — 코드는
  세 자리 모두 맞게 동작하고 있었고, 다만 틀려도 아무도 안 잡는 상태였다. 넘길 결함이 없다.
- 다음: **리뷰 7.** 이번 반복이 만든 것은 검사뿐이고 제품은 한 줄도 안 건드렸다 — 리뷰가 볼 것은
  «세 검사가 실제로 그 구멍을 막는가»(변이를 다시 심어 확인) 와 «갭 탐색이 여기서 멈춰도 되는가» 다.

## 2026-09-02 20:50 | passage-api | 리뷰 7 시도1

- 한 일: **리뷰 6 이후의 diff(개발 10 + 테스트 5, `19317eb..81c3a5b`)를 패스 A(백지) 먼저 보고
  계획·설계를 열었다.** 앞 반복이 남긴 세 물음을 전부 **남의 자로 다시 쟀다**.
  기록 갱신은 `status.md`·`history_current.md`·`metrics.md`·`design_passage-api.md`·`digest.md`.
- **① 19행 재측이 독립 탐침에서 그대로 재현됐다.** `passages()` 를 안 부르고 고르는 루프만
  스크래치패드에 다시 써서 쟀다 — **점수만 11/19 · 「길이」 15/19 · 「이름표」 12/19**, 개발 10·
  리뷰 6 과 글자 그대로 같다. 현행이 틀리는 4행이 `_UNMATCHED_SHAPES` 4행과 같고 거기 적힌
  «오늘의 오답» 이 탐침 출력과 **4/4 일치**한다 — **리뷰 5 가 「길이」를 반려한 반례 셋은 표에서
  안 사라졌다.** **새 사실: 겹쳐도 안 맞는다** — `(점수, p인가, 길이)` **13/19** ·
  `(점수, 길이, p인가)` **15/19**. 뒤엣것이 「길이」와 **같다**는 것은 「길이」가 가른 뒤 남는 동점이
  이 표에 한 행도 없다는 뜻이고, 그래서 **이름표는 3순위로도 한 번도 안 걸린다.**
  «조합을 안 써 봐서 그렇다» 는 다음 반복의 유혹을 여기서 닫았다.
- **② `_UNMATCHED_SHAPES` 가드는 양방향이다** — 「나빠져도 초록」인 구멍이 아니다.
  `assertEqual(got, today)` 가 오늘의 오답을 문자열로 못박으므로 좋아져도 **다른 오답으로
  바뀌어도** 빨개진다. 실측: 동점 키를 「이름표」로 바꾸니 `_SHAPES` 6건 + **`_UNMATCHED_SHAPES`
  3건**이 함께 죽어 총 9건 — 개발 10 이 적은 «사망(9)» 과 같은 수다.
- **③ 테스트 5 의 검사 3건이 각각 자기 변이를 죽인다.** 다섯을 따로 심어 전수로 확인했다:
  `del self._open[i:]`→`[i]` **사망(1)** · `"title": title`→`""` **사망(1)** · 2-gram 바늘 삭제
  **사망(1)** · 동점 키→「이름표」 **사망(9)** · `_open[-1]`→`_open[0]` **사망(4)**.
  전부 `.mutation-lock` 을 걸고 제자리에 심었고 원복은 초록이 아니라 **`git diff -- src/` 가 빈
  것**으로 확인했다. **커밋된 변이 0 · 최종 `.mutation-lock` 0.**
- **찾은 것은 하나 — [R7-1] 이름표를 읽는 소비자가 0곳이다** (medium · 신뢰도 95).
  `extract_blocks` 의 `(태그, 텍스트)` 중 첫 원소를 읽는 제품 코드가 없다: 소비자는
  `indexer.passages`(`_tag` 로 버린다)와 `e2e/passage_eval.py`(`len()` 만)뿐이고 `/passages`
  응답 스키마는 `{url,title,position,text}` 다. ①의 «3순위로도 안 걸린다» 가 여기 붙어
  **지금 값이 0** 이다. 유지비는 `_open` 스택 · `handle_endtag` · `_flush` 의 이름표 줄 · 검사 3건.
  **그래도 안 지웠다** — 지우면 갈림길 6 을 재는 자에서 축 하나가 사라지고, 그 갈림길은 실물
  코퍼스로 닫히는 중이다. **되돌릴 일이 아니라 같은 문으로 닫힐 일**이라 `digest [6]` 으로 등재.
- **고친 것은 주석 2곳뿐이다(동작 0줄).** 이 diff 가 들여온 거짓말 한 문장 —
  `extract_blocks` docstring 의 *"소비자가 «이 텍스트가 무엇이었나» 를 볼 수 있게 같이 낸다"* —
  는 **그 소비자가 없어서** 실측으로 바꿨고, `indexer.py` 동점 키 주석에 조합 측정(13/19 ·
  15/19)을 한 줄 더했다. 리뷰가 제품 코드를 고치는 것은 이 한 자리로 그쳤다.
- 결과: **단위 576건 전부 통과**(맨몸 단독, 기록을 다 쓴 뒤 두 번 재서 13.447·13.517초) ·
  `passage_eval` **정확도 100.0%(398/398) · 채택률 99.5% · p50 1.27ms · p95 1.42ms**(예산 500ms
  의 0.3%, rc 0) · 새 파일 0 · 의존성 0 · 스키마·마이그레이션 없음 · `docs/specs/`·`data/crawl.db`
  무변경 · PR 안 열었다.
- **러너 규율: 이번 반복은 안 밟았다.** 러너 호출 **8회**(전수 7 + `passage_eval` 1)가 전부
  맨몸·단독이고 파이프·`&&`·백그라운드 0, 변이 판정은 `python3 -B` 였다. 직전 세 반복이 각각
  한 번씩 밟았으니 **넷 만에 처음**이다 — 한 번으로는 추세가 아니라 `status.md ## 사람이 정할 것`
  4번과 `digest [9]`(`unittest -b`)를 그대로 둔다.
- **회전은 안 돌렸다** — 이 항목을 붙이고 세어 **237줄**로 상한 300 아래다(항목 4개).
- 다음: **e2e 20종 전수.** 통과하면 계획 48 을 DONE 으로 닫는다. 갈림길 6 은 열린 채로
  넘어간다 — 닫는 조건은 실물 크롤 코퍼스이고 그것은 **다음 계획**이다.

## 2026-09-02 21:20 | passage-api | e2e 1 | 시도1

- 한 일: **e2e 20종 전수 + `/passages` 를 프로세스 밖으로 처음 꺼냈다.** 새 e2e 파일은
  안 만들고 `e2e/search_api_e2e.py`(+38 −2)에 갈래 둘을 얹었다 — ⑧ `/passages` 계약,
  ⑨ 손상 DB. 제품 `src/` **0줄**. 결과는 `docs/e2e/passage-api/result.md`.
- **왜 새 파일이 아니라 그 파일인가.** 계획 48 이 만든 엔드포인트를 재는 자가 셋인데
  셋 다 같은 자리를 안 본다: `passage_eval.py` 는 **200 한 갈래뿐**(정확도·p95 전용),
  단위 576건은 `make_server()` 를 **스레드**로 부르고 `tempfile` **절대 경로**를 쓴다.
  계획 47 이 열어 둔 서버가 이미 **README 형태의 상대 경로**로 떠 있어서, 경로 하나를
  더 때리는 것으로 계약 전체가 재진다(ponytail 2번 — 프로세스를 더 띄우면 재는 것은 같다).
- **손상 DB 500 을 저장소 사상 처음 밟았다.** 계획 47 의 result.md 5절이 *"500 은 한 번도
  안 났다"* 로 닫혀 있었다 — 재시도해도 안 낫는 상태(500)와 색인을 다시 돌리면 낫는
  상태(503)를 프로세스 밖에서 가른 자리가 0개였다. 이제 진짜 파일에
  `not a sqlite database` 를 써서 `/search`·`/passages` 둘 다 **500 · `version` 1 ·
  경로·트레이스백 유출 0** 을 확인한다. 서버 로그가 **어느 문이 열렸는지**까지 적는다.
- **대조군 둘, 전부 RED.** M1 `_send(500,…)`→`_send(503,…)` · M2 `"position": pos`→`str(pos)`.
  **둘 다 단위가 먼저 죽인다(8건 · 2건)** — 이 e2e 는 눈먼 자리를 메운 것이 아니라 형태를
  바꿔 잰다. 그렇게 적는 것이 정직하다. **다만 `passage_eval` 은 M2 에 완전히 눈이 멀었다**
  (rc 0 · 정확도 100.0% · p95 1.43ms). **품질을 재는 자와 계약을 재는 자는 다른 자다** —
  반복 267 의 눈금(«숫자가 안 움직이는 것은 그 축을 안 쟀다는 뜻일 수 있다»)이 또 맞았다.
  원복은 초록이 아니라 **`git diff -- src/` 0줄**로 확인했다. 커밋된 변이 0 · `.mutation-lock` 0.
- 결과: 단위 **576건 OK**(맨몸 2회 13.433·13.418초) · e2e **20종 전수 rc 0** ·
  정확도 **100.0%(398/398)** · p95 **1.40ms** · recall ko 20/20·en 19/20 · 처리량 10.22/s ·
  디자인 4축 통과 · `perf_search` p95 8.90ms · `data/crawl.db` sha256 무변경 ·
  README 카운트 무변경(e2e 파일을 안 늘렸다 — 20종 그대로).
- **러너 규율: 밟았다(28회째).** 두 번째 전체 스위트에 `2>&1 | tail -3` 을 붙여 판정 줄이
  크롤 로그에 밀렸다. 즉시 맨몸 재실행해 `Ran 576 tests OK` 를 읽었고 **판정은 그 호출 위에서
  안 내렸다.** 방아쇠·형태·자리가 전부 기존 칸이라 **새로 보이는 것이 없다는 것이 이 표본의
  값**이다 — 문장은 소진됐고 남은 것은 `digest [9]`(`unittest -b`)뿐이다.
- **회전은 안 돌렸다** — 이 항목을 붙이고 세어 상한 300 아래다.
- 다음: **계획 48 DONE.** 계획·설계서를 `plan_history_034.md`·`design_history_034.md` 로
  아카이브했고 `index.md` 확장 분할 10번과 `digest ## 완료` 에 등재했다. **PR 은 사람 몫이라
  안 열었다.** 다음 반복은 계획 탐색이다 — 갈림길 6 은 열린 채로 남고(닫는 조건이 실물
  코퍼스) `digest` 의 `[6]`·`[9]` 가 후보로 서 있다.
