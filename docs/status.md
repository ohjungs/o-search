---
signal: GREEN
phase: 개발
step: 1
attempt: 0
iteration: 276
updated: 2026-09-02
ctx: 55
night_iterations: 116
night_red: 1
night_retries: 0
plan: focus-rule-scope
---

# 현재 상태

**계획 49 `focus-rule-scope` 의 설계를 닫았다.** 산출물은 설계서
`docs/design_focus-rule-scope.md` 하나다. 제품 코드 `src/` **0줄** · 검사기 `e2e/` 0줄 ·
새 테스트 0건 · 새 e2e 0개. 브랜치 `loop/focus-rule-scope`.

단위 **576건 OK**(맨몸 13.479초 실측) · `PYTHONPATH=src python3 e2e/design_check.py`
**종료 0** · e2e **20종** · `data/crawl.db` 무변경 · `docs/specs/` 무변경.

## 무엇을 골랐나 — C(최상위 요구), 9줄

**말로 비교하지 않고 후보 넷을 실제로 만들어 변이 12종에 먹였다.** 표와 사유는 설계서
1·2절에 있다. 계획서가 설계에 넘긴 물음(「C 는 다크 전용 CSS 를 영구히 거절하는데 그것이
옳은 거절인가」)은 **갈림의 자리가 아니었다.**

**갈림은 다른 데 있었다 — A 계열이 거짓 초록을 다시 만든다.** at-rule prelude 를 해석하는
안은 블랙리스트(`prefers-color-scheme|print`)로 서는데, 블랙리스트는 **모르는 조건을
안전으로 분류한다**. `@media (forced-colors:active)` 와 `@layer` 안의 조건부 링을 둘 다
종료 0 으로 통과시켰다 — 계획 44 가 죽인 고장과 **같은 형태**다. 화이트리스트로 뒤집으면
통과하는 at-rule 이 0개가 되고, 그것이 곧 C 다.

**at-rule 은 전부 조건이다.** 조건부 링의 대비 한 숫자는 참이 아니다. 그래서
`@supports`(구형 브라우저에 링 없음) · `@media (min-width:20em)`(360px 에 링 없음 —
디자인 4축이 재는 바로 그 화면)에 대한 C 의 거절은 **오탐이 아니라 정탐**이고, 판정이
1 이 아니라 **2(측정 불능)** 라 검사기는 거짓말 대신 멈춘다.

**B 는 계획서 예상대로 ⓑ(`@media print`)에서 종료 0** — 10줄로 짧아도 닫으라는 것을
안 닫는다. C 는 ⓐⓑⓒⓓ 넷을 전부 종료 2 로 만들면서 V1·V10·V11 을 한 칸도 안 움직였고,
정상 CSS 진단 줄이 무변이다(`라이트 --focus on --bg-page 3.56:1 / 기준 3.0 OK`).

## 계약 (설계서 3절)

`focus_rule` 의 **반환·예외 모양은 안 바뀐다** — 공개 인터페이스 변경 0. 조건을 둘 더한다.

- **조건 5 (스텝 1, 6줄)** — 셀렉터에서 `:not(…)` 안을 지운 뒤 `:focus`/`:focus-visible`
  이 **낱말로** 남아야 한다. 기존 `":focus" not in selector` 를 **대체**한다(넓어서 틀린
  가드였다). V11(`:hover`)은 메시지 문구만 바뀌고 판정은 그대로다.
- **조건 6 (스텝 2, 3줄)** — 링 규칙 앞의 `{`·`}` 개수가 같아야 한다(at-rule 밖).

스텝은 계획서의 둘 그대로다 — 쪼개지도 합치지도 않았다. **두 축이라 검사도 둘이다.**

## 러너 규율 — 이번 반복 0회

출력 조작 **0회** · 명령 잇기 **0회**. 단위와 `design_check.py` 를 각각 맨몸·단독으로
돌렸고, 판정 줄(`Ran 576 tests in 13.479s` / `OK`) 아래로 크롤 로그가 더 흐르는 것을
가리지 않았다. 직전 반복은 각각 3회였다. `digest ## 반복 실패` 의 최다 재발 항목이고
누적 29회다 — **이번 반복은 안 밟았다.** 처방 후보 `[9]`(`unittest -b`)는 계획서 5절이
별도 경로로 미뤄 둔 것이라 이번 스텝에서 안 만들었다.

## 저장소 변이 0

후보 A·A′·B·C 의 구현이 **전부 스크래치패드 탐침 안에 있다** — `design_check.focus_rule`
을 그때만 갈아끼우고 `finally` 로 원복했다. 저장소 파일을 한 줄도 안 고쳤으므로
`.mutation-lock` 이 필요 없었고 커밋된 변이는 0이다. 변이마다 `mutated != css` 로
**실제로 심어진 것을 먼저 단언**했다.

## 문서 집안일

`docs/history_current.md` **300줄 → 92줄** — 상한에 정확히 닿은 자리라 설계 항목을 붙이기
전에 회전했다(계획 48 의 리뷰 6·개발 10·테스트 5·리뷰 7 → `history_044.md`). 계획 48 이
DONE 이라 경계가 깨끗해 48 의 항목을 전부 밀었다. `digest.md` 는 명부와 회전 줄에 **이어
붙여** 198줄 유지(새 색인 구멍 0).

## 다음 — 개발 1

조건 5(셀렉터 극성)를 `focus_rule` 에 넣어 ⓒⓓ 를 종료 2 로 만든다. TDD RED 를 눈으로
보고 간다. 건드리는 파일은 `e2e/design_check.py` 와 `tests/test_design_check.py` 둘뿐이고
제품 `src/` 는 0줄이다.

## 기록·기점

`origin/main` 은 PR #6 까지만 받아 **계획 48 이 없다**. `README.md` 의 `단위 576건`·
`e2e 시나리오 20종` 과 `tests/test_readme.py` 의 건수 단언이 현재 기점에 맞춰져 있어
`main` 에서 브랜치를 따면 첫 커밋이 RED 다(계획 43 `focus-contrast` 가 정확히 그
이유로 체리픽을 남겼다). 그래서 `loop/focus-rule-scope` 는 **`18d485f` 에서 땄다**.
푸시는 정상 절차다(강제 푸시만 금지).
**PR 은 안 열었다 · 웹 UI 의 *Update branch* 도 안 눌렀다** — 무인 모드는 열지도
병합하지도 않고, rebase-merge 로 해시가 새로 쓰인 뒤의 그 버튼은 머지가 아니라
복제다(PR #3 을 깨뜨린 원인).
`night_*` 세 필드는 이번 호출이 야간 세션이라는 근거가 없어 **안 건드렸다**.

## 사람이 정할 것 — 넷 그대로다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
2. **`--focus` 는 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 이 0 일
   때만 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** — 후보 B 가 닫는 것은 저장소 안쪽 절반
   뿐이고, 룰 문장을 조이는 나머지 절반은 여전히 사람 몫이다.
4. **사양 확장이 남긴 것 둘** (`docs/specs/concept.md` 의 `## 사람이 정할 것`):
   속도 제한 시점(가정 — IP 당 분당 60회)과 사양 숫자들이 초안이라는 것(90% · 500ms ·
   60회/분). 분할 가정은 계획 46·48 이 따랐고 둘 다 닫혔다.
