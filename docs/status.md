---
signal: GREEN
phase: 개발
step: 2
attempt: 0
iteration: 213
updated: 2026-09-01
ctx: 46
night_iterations: 75
night_red: 0
night_retries: 0
---

# 현재 상태

**계획 42 `docs-citation-guard` 개발 2/2 완료 — 리뷰 조건부 판정의 [A1]·[A2] 를 닫았다.**
검사가 이제 **자기 자신을 붙든다**(`CitationPatternTest`: 금지 4꼴 · 허용 4꼴을 코드 안
리터럴 표로 단언) 그 표가 드러낸 **살아 있는 위반 2건**을 교정했다. 제품 `src/` **0줄**.

## phase 를 되돌렸다 (e2e → 개발)

앞 반복이 `phase: e2e` 로 넘겨 놓았지만 **리뷰 판정이 "조건부"** 였고 중요도 9·8 지적이
열린 채였다. 그중 [A2] 는 **지금 저장소에 살아 있는 위반**이라, 고치지 않고 e2e 로 가면
계획 42 가 "오인용을 막았다" 고 주장하면서 그 오인용을 **자기 계획서에 담은 채** 통과하게
된다. 판정을 문서가 아니라 phase 로 받는 자리라 되돌렸다 — 계획서 3절에 스텝 2 를,
5절 표에 이 스텝이 건드린 파일 둘을 적었다.

## 방금 것 (2026-09-01 · 개발 2/2 · 반복 213)

### [A1] 검사 자신을 붙드는 단언 — 축소 변이 4종이 이제 죽는다

`CitationPatternTest` 는 **문서가 아니라 코드**를 잰다. 문서를 다 고쳐 놓으면 hits 는
정규식이 넓든 좁든 0 이라 좁아지는 변이가 전부 산다 — 그래서 검사 대상을 코드 안
리터럴로 고정했다. `` `index.md` 41 행 `` 을 지키던 **주석 한 줄짜리 방어도 이제 단언**이다
(허용 표 4꼴). 변이 실측(`.git` 없는 스크래치패드 사본, 하나씩 심고 되돌렸다):

- **M8 정규식을 개발 phase 의 콜론 꼴로 되돌리기 → 사망** (subTest 3건 FAIL).
  **이 스텝의 핵심 증거다** — 리뷰가 "초록으로 산다" 고 잡은 바로 그 변이다.
- **M9 표 분리자 완화 되돌리기 → 사망** (표 꼴 단언 **1건만** FAIL, 예측대로).
  같은 실행에서 `DocCitationTest` 는 **초록으로 남았다** — 린트형 검사가 데이터가
  깨끗할 때 자기를 못 잰다는 리뷰의 일반화를 변이가 그대로 재현했다.
- **M10 `APPEND_TARGETS` 에서 이름 하나 빼기 → 사망** (축소 변이 4종의 나머지 대표).

### [A2] 네 번째 탈출구(표)를 막고 위반 2건을 교정했다

정규식이 이름과 숫자의 **인접**을 요구해 마크다운 표 칸 구분자로 빠져나갔다.
닫는 백틱 뒤 분리자를 **3자까지** 받게 완화했다(`` `?[^0-9]{0,3} ``). 교정한 2건은 둘 다
계획서 `## 5. 건드릴 파일` 표다:

- `index` 행: 줄번호 둘 → `plan_clock-injection` 행 · 계획 33 서술
  (**앞의 것은 이미 썩어 있었다** — `index` 가 한 줄 늘어 목표가 밀렸고 기록은 밀린 값으로
  적고 있었다. 한 문서 안에서 같은 인용이 두 값으로 갈린 것을 이름 인용이 없앤다.)
- `digest` 행: 줄번호 하나 → `## 반복 실패` ③

**이 스텝의 산문도 검사에 걸렸다 — 다섯 번째다.** 위 두 줄을 처음엔 위반 원문 그대로
인용해 적었고 러너가 `status.md` 두 줄을 이름과 함께 뱉었다. 계획·개발·테스트·리뷰에
이어 다섯 번째로 그 phase 자신이 위반을 만들었는데 **처음으로 검사가 잡았다** —
[A2] 를 닫은 완화가 없었으면 이번에도 그냥 통과했을 자리다.

**RED 를 먼저 봤다**: 단언만 넣고 돌린 러너에서 **FAIL 2건** — ① 표 꼴 리터럴
(`Regex didn't match` — 계획서 5절 표의 `digest.md` 행) ②
`test_readme` 의 `(467, 19) != (469, 19)`. 허용 표(오탐)는 처음부터 초록이었다.

**검증**: 단위 **469건 OK 11.878초**(맨몸) · `data/crawl.db` sha256 **무변경**
(`85c96744…5bda18`) · `docs/digest.md` **200줄 · 49항목** · `src/` **0줄**.

## 다음

- **테스트 1/1.** 개발이 두 번 돌았으니 테스트도 새 산출물을 다시 받는다. 볼 것:
  ① `CitationPatternTest` 의 두 표에 **빠진 꼴**이 있는지(허용 쪽이 특히 얇다 — 4꼴뿐이다)
  ② 완화한 분리자가 **아카이브 62개를 포함한 문서 77개 전수**에서 오탐 0 인지 실측
  ③ `[6]` 은 **DONE 에서만** 닫는다 — 아카이브 8곳이 남아 있어 지금 닫으면 거짓이다.
- **집안일 미결 — `history_current.md` 회전이 늦었다.** 이 반복 **전에 이미 327줄**로
  상한 300 을 넘겨 있었고 이번 항목으로 **364줄**이다. 안 돌린 이유는 계획 5절 밖의 새
  파일(`history_018.md`)이 생기고 `digest.md` 가 200줄·49항목 상한에 닿아 압축이 먼저
  필요해서다 — **이 스텝의 몫이 아니라 다음 반복의 첫 일**이다. `digest ## 반복 실패` 의
  *회전 지각* 항목으로 다섯 번째다.

## 한도 (매 반복 확인)

- 브랜치 `loop/docs-citation-guard` · `main` 직접 커밋 금지 · `--no-verify` 금지 ·
  외부 네트워크 금지 · `docs/specs/` 읽기만 · `data/crawl.db` 무변경.
- **`src/` 는 계획에서 0줄이다.** 건드리면 YELLOW.
- **검증은 맨몸으로.** `python3 -m unittest discover -s tests` 를 포그라운드에서 그대로 돌린다.
  파이프·`grep`·`head`·`tail`·`for`·`env`·`timeout`·리다이렉션으로 감싸지 않고,
  백그라운드 태스크로도 돌리지 않는다. (`digest ## 반복 실패` 의 해당 항목 **10회** —
  **⑩을 이 반복이 저질렀다**: GREEN 확인에 `| tail -5`. 맨몸 재실행으로 값을 다시 받았다.
  ⑨는 재확인, ⑩은 GREEN 확인 — **방아쇠는 둘 다 "이미 초록일 것 같은 실행"** 이다.)
- `docs/digest.md` 200줄 상한 · 열린 항목 49.
