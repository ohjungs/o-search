---
signal: GREEN
phase: 테스트
step: 1
attempt: 0
iteration: 283
updated: 2026-09-03
ctx: 45
night_iterations: 117
night_red: 1
night_retries: 0
plan: runner-quiet
---

# 현재 상태

**계획 50 `runner-quiet` — 개발 1/1 완료. 다음은 테스트 1/1.**
계획서는 `docs/plan_runner-quiet.md`. 브랜치 `loop/runner-quiet`, 기점 `373cafd`.

**저장소가 자기 명령을 적어 둔 두 줄에 `-b` 를 넣었고, 검사 하나가 그것을 붙든다.**
화면이 **약 90줄 → 5줄**이 되고 `Ran 579 tests / OK` 가 **마지막 줄**이다.
제품 `src/` **0줄** · 새 파일 **0개** · 새 의존성 0 · `docs/specs/` 무변경 ·
`data/crawl.db` 무변경. 건드린 파일 **3개**, `+24 −3`.

## 무엇을 했나 — TDD 순서 그대로

① `tests/test_readme.py` 에 `test_guided_runner_commands_buffer_output` 을 더하고
**RED 를 눈으로 봤다**(`FAILED (failures=2)` — 새 단언 하나와, 아직 578 이던
`test_verification_counts_match_reality` 하나).
② `README.md:69` 를 `discover -b -s tests` 로, `docs/project.md:14` 를
`discover -b tests` 로 고쳤다. **두 줄은 형태가 서로 다르고 그대로 뒀다** —
검사는 `-b` 만 본다. `project.md` 의 실행 확인 날짜도 **2026-09-03** 으로 갱신했다.
③ 맨몸·단독으로 두 형태를 **각각** 돌려 둘 다 `Ran 579 tests` · `OK` 를 읽었다.
④ `README.md` 의 `단위 578건` 을 **579** 로 고쳤다(③④ 같은 커밋 — `test_readme.py`
자신이 그 숫자를 실제 스위트로 대조한다).

**`docs/project.md` 의 맨몸 규율 문장은 안 지웠다.** 대신 `-b` 가 **통과한** 테스트의
출력만 삼킨다는 두 줄을 명령 바로 아래 붙였다 — `-b` 는 파이프의 대체재가 아니라
파이프를 붙일 이유를 없애는 것이다.

## 실측 — 맨몸·단독 3회

| 명령 | 결과 |
|---|---|
| `-k ReadmeCommandsTest` (RED 확인) | `Ran 5` · `FAILED (failures=2)` |
| `discover -b -s tests` (README 형태) | `Ran 579 tests in 13.461s` · `OK` · **5줄** |
| `discover -b tests` (project.md 형태) | `Ran 579 tests in 13.440s` · `OK` · **5줄** |

**`OK` 가 화면의 마지막 줄이다.** 계획 phase 가 잰 「늦은 stdout 플러시」(`OK` 뒤로
`수집 0 페이지`·`1 문서 색인`)가 **0줄**이 됐다. 578 → 579 의 +1 은 이 계획이 의도해서
더한 단언 하나이고, 거른 테스트는 **0건**이다. 시간은 13.416 → 13.461초(±0.5초 안).

## 변이 검증 — 2건, 저장소 파일 무편집

계획 49 가 쓴 **메모리 exec 탐침**으로 갔다(스크래치패드의 `probe_b.py`, `python3 -B`).
두 문서의 사본을 임시 디렉터리에 만들어 `-b` 를 **각각 한쪽씩만** 지우고 새 단언만 돌렸다.

| 변이 | 판정 |
|---|---|
| 없음(대조군) | GREEN |
| `README.md` 쪽 `-b` 만 삭제 | **RED** |
| `docs/project.md` 쪽 `-b` 만 삭제 | **RED** |

**가드가 두 자리를 각각 붙든다** — 한쪽만 고치고 다른 쪽을 놔두는 것이 이 검사가
막으려던 실패다. `git diff` 로 확인한 작업 트리 변경은 의도한 3개 파일뿐이고,
`.mutation-lock` 은 파일을 안 건드려 필요 없었다. **커밋된 변이 0.**

## 완료 기준 — 어디까지 왔나

- **사라진다**: 화면 90줄 → **5줄** ✔ · `OK` 뒤 늦은 플러시 **0줄** ✔ ·
  통과 테스트의 로그 **0건** ✔
- **그대로다**: `Ran 579` · `OK` · rc 0 ✔ · 거른 테스트 **0건** ✔ · 시간 ±0.5초 ✔ ·
  **`-b` 를 지우면 새 단언이 죽는다** ✔(위 변이 2건) ·
  **실패했을 때 stdout·stderr 가 둘 다 보인다** — 계획 phase 가 임시 스위트로 봤고
  **테스트 phase 가 변이 1건으로 다시 실측한다**(남은 하나).
- **저장소 불변**: `docs/specs/` 무변경 · `data/crawl.db` 무변경 · 제품 `src/` 0줄 ·
  새 e2e 파일 0개 · e2e 한 종도 안 건드렸다.

## 품질·성능 축 — 전부 기준선 그대로

ko **20/20** · en **19/20** · 순위 1위 39 · 매치 평균 14.0(최소 11) ·
문단 정확도 **100.0%** 채택률 99.5% p95 **1.46ms** · `perf_search` p95 **8.89ms** ·
`search_api` p95 2.10ms · 처리량 [열림]·[차단] **10.28/s** ·
문서집합 sha1 `541d455a…c078654b` · 디자인 4축(외부 0 · JS 0 B · 텍스트 4.87:1 · 링 3.56:1).

## 러너 규율 — 이번 반복 0회 (누적 30회)

러너 3회와 탐침 1회를 전부 맨몸·단독으로 돌렸다. **러너에 붙인 파이프·리다이렉션·
`;`·`&&` 잇기 0회, `> /dev/null` 0회. 일곱 반복 연속 0회다.** 이번 반복부터는
**화면이 5줄이라 `| tail` 이 손에 잡히지도 않았다** — 계획이 겨냥한 것이 그것이다.

**적어 두는 것 둘.** ① RED 확인용 한 번에 `-k ReadmeCommandsTest` 를 붙였다. 출력을
자르는 손이 아니라 러너 자신의 필터이고 판정 줄은 그대로 나오지만, **「전체를 돌려라」
와는 다르다** — 그래서 그 뒤 전체를 두 번 맨몸으로 돌렸다. ② 러너가 아닌 곳에서
`&&` **3회**(`git status && git log` 둘, `git add && git commit` 하나). 계수에는 안
넣지만 **«0회» 를 «한 번도 안 이었다» 로 읽으면 거짓**이라 남긴다. 직전 반복이
`git push` 에 붙였던 `| tail -4` 는 **이번엔 안 붙였다** — 맨몸으로 밀었다.

## 문서 마감

`index.md` 50번 행을 진행 **1/1** 로, `metrics.md ## 누적` 의 `반복` 을 **283**,
`## phase 분포` 의 개발을 **68** 로 올렸다(`IterationSyncTest`).
`history_current.md` 는 이 항목을 붙여 **109줄**·2회로 상한(300줄/20회) 안이라 회전 없다.
`digest.md` 는 191줄 그대로 — 이번 반복이 낸 새 재발도, 새 후보도 없다.
`night_*` 세 필드는 **이번 호출이 야간 세션이라는 근거가 없어 안 건드렸다**
(계획 49 e2e 반복이 같은 이유로 같은 판단을 했다).

## 다음 — 테스트 1/1

남은 완료 기준은 **「실패했을 때 그 테스트의 stdout·stderr 가 둘 다 보인다」** 하나다.
제품 코드 한 줄을 깨서 빨갛게 만들고, 그 테스트가 찍은 로그가 실패 보고서 안에
`Stdout:`/`Stderr:` 블록으로 나오는 것을 눈으로 읽는다. **`-b` 의 천장도 같은 자리에서
확인한다** — 빨간 실행에서는 `FAILED` 뒤에 늦은 플러시가 그대로 붙는다(그건 봐야 할
것이라 무해하다는 판단을 계획 phase 가 적어 뒀다).

## 사람이 정할 것 — 넷 그대로다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
2. **`--focus` 는 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 이 0 일
   때만 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** — 룰 문장을 조이는 절반은 사람 몫이다.
   이번 계획이 닫은 것은 **저장소 안쪽 절반**뿐이다.
4. **사양 확장이 남긴 것 둘**(`docs/specs/concept.md` 의 `## 사람이 정할 것`):
   속도 제한 시점(가정 — IP 당 분당 60회)과 사양 숫자들이 초안이라는 것(90% · 500ms · 60회/분).

## PR 은 사람 몫이다

`origin/main` 은 `687a159` 로 PR #6 까지만 받아 **계획 48·49 가 둘 다 미병합**이고
계획 50 도 그 위에 쌓인다. 무인 모드는 PR 을 열지도 병합하지도 않는다.
