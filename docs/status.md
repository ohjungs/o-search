---
signal: GREEN
phase: 계획
step: 0/0
attempt: 0
iteration: 450
updated: 2026-09-08
ctx: 40
night_iterations: 1
night_red: 0
night_retries: 0
plan: null
---

## 현재 상태

**야간 450 — 코드가 자기 실측을 인용하는 축을 닫았다.** 후보 `[5]` ③ 이 등재한
두 자리를 고쳤다. 전수 **672 OK rc 0** · 제품 동작 0줄(주석만).

## 짧은 경로 — comment-cite-drift

- 근거: `docs/digest.md` 후보 `[5]` ③ (계획 75 e2e 실측) — ① `src/websearch/indexer.py:79`
  무릎 표의 3,000 행 p95 가 **137ms** 인데 `docs/e2e/passage-prefer-body/result.md:56` 실측은
  **138.10ms** ② `tests/test_indexer.py:845` 주석이 오늘 캡(3,000)의 품질을 **98건·80.6%** 로
  적는데 그것은 같은 표의 **8,000 행**이다(3,000 행은 96건·81.2% — result.md:43).
- 할 일: 두 자리의 숫자 셋을 실측으로 맞춘다. 그 두 파일만.
- 완료 기준: 인용 = 실측 · 전수 672 OK · 후보 `[5]` ③ 에 닫는 근거.
- 이미 한 것: **전부.** 숫자 셋 수정 + 인용 전수 대조 + 후보 닫기 · 672 OK.

## 여는 조건을 안 기다린 이유 — 아침에 뒤집기 싼 판단이다

후보 `[5]` ③ 의 여는 조건은 「`indexer.py` 캡 문단이나 `test_indexer` 값 가드를 **다시
손대는 계획**」이었고 오늘 그런 계획은 없다. 그런데도 열었다:

1. **미룬 것은 「고칠까」가 아니라 「자를 세울까」였다.** 항목 자신이 *"둘뿐이면 자보다
   고치는 게 싸다(ponytail 1번)"* 로 적어 뒀고, **오늘 그 「둘뿐」을 실제로 셌다** —
   `indexer.py` 캡 문단이 인용한 나머지 전부(2,000·4,000·8,000 행 · 옛 바이트 73건/41.1%/43ms ·
   1.13MB 0.70ms/66.8ms · 계수 3.952·3.746·2.885·2.773 · 120ms · 92%(370/400) · 중앙값 3,225)를
   `result.md` 와 대조하니 **어긋난 것은 등재된 둘뿐**이다. 그래서 자는 안 세운다.
2. **거짓 인용이 값 가드 바로 옆에 산다.** `test_indexer.py:845` 는 「3,000 과 8,000 의
   품질이 같다」로 읽히는데(둘 다 98건·80.6%), 그 두 줄 아래가 상수를 8,000×10 으로
   동결하는 단언이다. 상수를 올릴지 판단하는 사람이 **읽는 순서상 먼저 만나는 문장**이다.
3. 되돌리는 값이 `git revert` 한 번이다 — 주석 셋이고 제품 동작·단언 전부 무변이다.

**아침에 「여는 조건을 기다렸어야 한다」고 보면 되돌리면 된다.** 그 경우에도 위 1번의
대조 결과는 남는다 — 자를 세울지 묻는 질문은 어느 쪽으로 결론이 나든 닫혔다.

## 남은 「사람이 정할 것」

- 없음. 후보 `[5]` 의 ①(README 성능 표)은 오늘 실측으로 **여는 조건 미충족**을 확인했다 —
  README 의 숫자 축은 `test_readme.py` 가 이미 잰다(단위 672건 · e2e 21종 · 합격선 아홉 행이
  전부 e2e 상수와 대조된다). 낡은 자리 0건이라 열 근거가 없다.
