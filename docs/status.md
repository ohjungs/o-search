---
signal: GREEN
phase: 리뷰
step: —
attempt: 0
iteration: 489
updated: 2026-09-09
ctx: 50
night_iterations: 5
night_red: 0
night_retries: 0
plan: userinfo-leak
---

## 다음 반복이 읽을 것 — 테스트 phase 완료. 다음은 **리뷰 phase**다

**야간 처분은 그대로 패치만 남기기다.** 마지막에 `docs/patches/` 로 뽑고 작업 트리를
되돌린다. **적용된 채로 남기지 않는다.**

- **변이는 하나가 아니라 둘이었다.** 계획 완료 기준 6 은 「가드를 지우면 2·4·5 가
  빨개진다」고 적었는데, 실물에는 **가드가 둘**이다. 각각이 자기 몫만 죽인다:
  - **변이 A** (`normalize` 의 `has_credentials` 거절 삭제) → 기준 2·4 빨강,
    **기준 5 는 초록으로 살아남았다** — 시드 가지는 `has_credentials` 를 직접 부르므로.
  - **변이 B** (시드 자격증명 가지 삭제, `normalize` 가드는 유지) → 기준 5 빨강
    (「자격증명」 대신 「읽을 수 없는」이 나온다 = 거짓 사유).
  둘 다 필요하다는 것이 이번 phase 의 소득이다.
- **갭 둘을 메웠다** (`test.md` 3절 ①·6절 ⑥):
  - **기준 4 에 테스트가 아예 없었다.** 변이 A 가 조용히 통과해서 드러났다 —
    링크 경로는 시드와 달리 **남이 심는 자리**라 실제 유입구다. `test_links.py` 에
    자격증명 href 둘은 버리고 같은 페이지의 정상 링크는 남기는 단언 + 경로 `@` 대조군.
  - **새 public 술어 `has_credentials` 에 직접 테스트 0.** `TestHasCredentials`
    추가 — 특히 **「못 읽는 URL 에 `False`」 가지**는 아무도 안 밟고 있었다(거기서
    던지면 시드 하나가 크롤 전체를 끝낸다).
- 전수 **717 OK** (712 → +5) · README 단위 수 712 → 717.
- **남은 것**: 리뷰 → e2e(`e2e/domain_key_e2e.py` 시나리오 1 갈아 끼우기, 아직 안 고쳤다)
  → 패치 추출 · 작업 트리 되돌리기 → 야간 보고서.
