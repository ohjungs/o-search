---
signal: GREEN
phase: 계획
step: 0/2
attempt: 0
iteration: 387
updated: 2026-09-06
ctx: 30
night_iterations: 190
night_red: 2
night_retries: 4
plan: spec-citation-address — 계획 67 (계획 등재 완료 · 다음 phase 개발 · 설계 생략)
---

## 현재 상태

**계획 67 `spec-citation-address` 를 등재했다 — `docs/plan_spec-citation-address.md`.**
근거는 `digest [7]③`(`QualityBandTest` 항목 ③ — 「세 번째 사본 `docs/specs/concept.md` 는
여전히 안 묶여 있다」)이고, 반복 386 이 「첫 자리」로 지목한 둘 중 하나다.

**예산을 먼저 못박고 들어왔다.** 앞선 두 시도가 변이 실험에 들어가 산출물 없이 48분·39분
정지한 전례(`digest ## 반복 실패` 2회)라 이 반복은 **전수 1회 · 착수 탐침 정적 판독만 ·
도구 호출 30회 이내**로 상한을 정했다. **변이는 0판 심었다** — 재려던 것이 「인용 주소가
맞나」라 파일을 읽으면 끝난다(`digest`: 정적으로 읽으면 끝나는 것에 변이를 걸지 마라).

## 착수 탐침 — 판정 줄

| 판 | 무엇을 쟀나 | 결과 |
|---|---|---|
| 기준선 | 전수 | `Ran 625 tests in 15.969s` · `OK` · rc 0 |
| P1 | `src`·`tests`·`e2e` 의 `concept.md:<N>` 인용 전수 | **18건** · 그중 **5건이 빈 줄(49행)** |
| P2 | 인용문이 대상 줄에 있나 | **4건 불일치** (`:50` JS 50KB→51행 · `:22` 「상위 10건」·「80% 이상」→23행) |
| P3 | 인용을 읽는 단언 | **0건** (전부 주석·docstring) |

**digest 는 ③ 을 「오늘 셋 일치를 다시 확인했다」로 적고 넘겼는데 그 확인은 값만 봤다.
주소를 재니 오늘 이미 거짓이다** — 잠복이 아니라 열려 있는 여는 조건이다.
맞는 인용 아홉(`:44` ×3 · `:53` ×2 · `:54` · `:59` ×2 · `:22-23`)도 함께 갈랐다 —
가드가 이 아홉을 물면 배선 오류다(오탐 0 의 기준선).

## 탐색 경로 — 8개 출처

1순위 실패 테스트 **0건**(전수 625 `OK`) · 2순위 타입/린트 **해당 없음**(stdlib 전용) ·
3순위 코드 `TODO`/`FIXME`/`HACK` **0건**(`tests/test_indexer.py:759` 한 건은 HTML 주석
fixture 문자열이라 실행 코드가 아니다) · 4순위 `docs/candidates.md` **없음** ·
5순위 보류 — `## 판단 필요` 는 전부 **승인 대기**라 안 건드린다 · 6순위 후보 — 남은
것들이 대부분 「여는 조건 미도래」로 잠겨 있고(`[7]` 재파싱 예산 · `[9]` `/search` 몫 ·
`[5]` 예산 500 · `[5]` `test_readme.py` ①② · `[4]` `〃` 이어받기), **여는 조건이 실제로
온 것은 `[7]③` 하나**다 · 7순위 반복 실패 — 처방이 이미 적용됨(바이트코드 캐시) ·
8순위 이슈 — `gh` 0회.

## 설계 트리거 판정 — 생략

새 모듈 0 · 공개 인터페이스 0 · 데이터 구조 0 · 되돌리기 쉬움(테스트 한 벌 + 인용 아홉 줄) ·
대안 갈림 없음(사양 수정은 규정이 막고, 텍스트 인용 전환은 YAGNI 로 잘랐다).
**「3개 이상 파일」만 걸린다** — `tests/test_docs.py` 1개 + 인용 정정 4개 = 5개인데
넷이 주석/문자열 한 줄씩이라 설계가 고를 것이 없다. → **다음 phase 는 개발**이다.

## 범위·집안일

`src/` 0줄 · `tests/` 0줄 · `e2e/` 0줄 · `README.md` 0줄 · `docs/specs/` 무변 ·
`data/crawl.db` 무변 · 재색인 0 · 스키마 0 · 새 의존성 0(stdlib) · PR #7 무접촉(`gh` 0회) ·
`--no-verify`·`--force`·`--amend`·`rebase` 0회 · 러너 리다이렉션 0회 · 변이 0판.
`docs/index.md` 에 계획 67 행을 **진행**으로 열었고 `history_current.md` 는 상한 아래라
회전 없음. `digest.md` 는 이번 반복이 안 건드렸다(항목을 닫는 것은 e2e 몫이다).
`metrics.md` 는 `반복` 을 387 로 맞춘 한 줄뿐이다(`IterationSyncTest` 가 무는 자리) —
탐색 막힘 집계는 안 늘렸다. 근거를 찾았으므로 막힘이 아니다.

## 다음

**계획 67 개발 스텝 1/2 — 주소 축.** `tests/test_docs.py` 에 인용 주소 가드를 **먼저**
심어 RED 5건을 판정 줄로 받고, 그 뒤 인용 다섯 자리(`49`→`50` · `49-54`→`50-54`)를
고친다. 사양은 안 고친다. 완료 기준과 죽어야 하는 편집 C1~C7 은 계획서 4절에 있다.
