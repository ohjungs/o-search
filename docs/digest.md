# 아카이브 요약

<!--
history_current.md 가 상한을 넘어 밀려날 때, 밀려나는 내용을 1~2줄로 압축해 여기 남긴다.
원본은 history_<NNN>.md 에 그대로 있다.

이 파일은 평소 읽지 않는다. 계획 탐색(discover.md) 때만 읽는다.
그래서 "다음에 뭘 해야 하는지 판단하는 데 필요한 것"만 남긴다:
  - 무엇을 완료했나 (같은 걸 또 하지 않기 위해)
  - 무엇이 보류됐나 (승인 받으면 재개할 것)
  - 반복해서 터진 문제 (근본 원인이 남아있다는 신호)

상한 200줄. 넘으면 오래된 완료 항목부터 지운다. 보류·반복실패와 `## 완료` 첫 줄의 아카이브 명부는 남긴다 — 회전은 새 history_<NNN>.md 이름을 명부에 더한다. **회전 명부는 이 파일의 `## 완료` 절이 정본이다** — `history_current.md` 머리에 겹쳐 쌓이던 회전 서술 233줄은 계획 48 개발 9(반복 269)가 지웠다(검사가 강제하는 명부는 이쪽 하나뿐이라 새 색인 구멍 0). 저장소 밖 룰 파일은 안 건드렸다.
-->

## 완료

<!-- - 2026-08-04~08 | plan_auth | 토큰 검증·만료 처리. 재시도 잦았음 — JWT 라이브러리 버전 이슈 -->

- **아카이브 명부** (회전이 지우지 않는 줄 · `tests/test_docs.py` 의 `ArchiveIndexTest` 가 붙든다. 원본은 각 파일에 그대로 있고, 아래 완료 항목이 이름을 안 남기고 지워져도 여기 이름은 남는다) | `history_001.md` `history_002.md` `history_003.md` `history_004.md` `history_005.md` `history_006.md` `history_007.md` `history_008.md` `history_009.md` `history_010.md` `history_011.md` `history_012.md` `history_013.md` `history_014.md` `history_015.md` `history_016.md` `history_017.md` `history_018.md` `history_019.md` `history_020.md` `history_021.md` `history_022.md` `history_023.md` `history_024.md` `history_025.md` `history_026.md` `history_027.md` `history_028.md` `history_029.md` `history_030.md` `history_031.md` `history_032.md` `history_033.md` `history_034.md` `history_035.md` `history_036.md` `history_037.md` `history_038.md` `history_039.md` `history_040.md` `history_041.md` `history_042.md` `history_043.md` `history_044.md` `history_045.md` `history_046.md` `history_047.md` `history_048.md` `history_049.md` `history_050.md` `history_051.md` `history_052.md` `history_053.md` `history_054.md` `history_055.md` `history_056.md` `history_057.md` `history_058.md` `history_059.md` `history_060.md` `history_061.md` `history_062.md` `history_063.md` `history_064.md` `history_065.md` `history_066.md` `history_067.md` `history_068.md` `history_069.md` `history_070.md` `history_071.md` `history_072.md` `history_073.md` `history_074.md` `history_075.md` `history_076.md` `history_077.md`
- 2026-09-09 | 회전 | 반복 452~456(계획 77 `page-store-compress`)·457~460(계획 78 `index-watermark`)을 `history_076.md` 로 밀었다 — `history_current.md` 303줄이 상한 300 을 밟았고 두 항목이 나가 **267줄**이 됐다. 원본은 그 파일에 그대로다. **명부에 `history_075.md` 도 함께 박았다** — 어제 회전은 등재를 회전 줄에만 남겨, 그 줄이 지워지는 날 이름이 사라지는 자리였다(반복 실패 「회전하며 압축 줄을 안 남긴다」의 이웃 모양). **이 줄로 `digest.md` 는 206줄이 됐다(상한 200)** — 무인은 이 파일을 회전하지 않는다는 계획 76 의 선이 그대로라 회전 여부는 아침에 사람이 정한다(야간 448 과 같은 자리).
- 2026-09-09 | 회전 | 반복 461~474(계획 79 `rate-limit` 전부 · 계획 80 `recrawl` 계획~리뷰)를 `history_077.md` 로 밀었다 — `history_current.md` 321줄이 상한 300 을 넘겨 `HistoryCapTest` 가 울었다(계획 76 의 가드가 두 번째로 발화). 남긴 것은 반복 476 부터다
- 2026-09-07 | 회전 | **`digest.md` 200줄 상한을 처음으로 실제로 맞췄다(216 → 193).** 앞 두 회전이 못 맞춘 이유(지울 완료 항목이 여덟 줄뿐 · 무인 모드는 삭제를 안 한다)를 사용자가 이 건에 한해 열어 줬다. 지운 것은 **닫힌 기록뿐**이다 — 후보 두 절의 취소선 **18줄**과 `## 완료` 의 계획별 요약 **7줄**(계획 63~69). 원본은 `index.md` 계획 행과 `plan_history_*.md` 에 그대로 있고 아카이브 명부·회전 기록·보류·반복 실패·열린 후보는 한 줄도 안 건드렸다. **위임은 이 건에 한해서였다** — 「닫힌 후보를 취소선으로 남기지 않고 지운다」는 항구적 정책이 아니라 오늘 한 번의 허가다. `rules/docs.md` 3절 문장은 안 바뀌었고 다음 회전은 다시 사람에게 묻는다. 다만 **그 한 번이 밟는 하한 못은 같은 계획이 함께 옮겼다**(계획 72) — 회전과 가드를 갈라 돌리면 사이 반복이 빨간 저장소를 물려받기 때문이다.
- 2026-09-07 | 회전 | `history_current.md` 줄 수 상한을 83줄 넘겨(383줄 · 항목은 정확히 20 = 상한) 반복 414~433 을 `history_073.md` 로 밀어냈다 — 야간 탐색 두 반복(423·424 짧은 경로)과 계획 72 `digest-rotate-guard` · 73 `realweb-corpus` · 74 `passage-prefer-body` 의 반복 서술이 그 파일에 그대로 있다. 남긴 것은 반복 434(계획 75 착수)부터다. **이 파일은 이 줄을 더해 200 → 201줄이 됐다** — 무인 모드는 삭제를 안 한다(`SKILL.md` 야간 금지 목록 · 2026-09-07 의 위임은 그 한 건뿐이었다). 무엇을 버릴지는 아침에 사람이 정할 일이라 야간 보고서에 올린다
- 2026-09-07 | plan_digest-rotate-guard | 계획 72. `digest.md` 216 → 193줄 회전과 그 회전이 밟는 하한 못을 한 계획에서 닫았다 — 못을 포인터 수(회전이 지우는 모집단)에서 **후보 절 머리 수**로 옮겼다(`CANDIDATE_HEAD_FLOOR = 2`). 등재 처방 B 는 실측으로 반증(포인터 10/10 이 취소선). 삭제는 **사용자가 이 건에 한해 위임**한 것이고 `rules/docs.md` 3절 문장은 안 바뀌었다 — 다음 회전은 다시 사람에게 묻는다. e2e 는 마감 편집을 취소선 유무로 갈라 초록/빨강 한 쌍을 냈다
- 2026-09-08 | 회전 | `history_current.md` 가 야간 450 의 **마감 기록으로** 줄 수 상한을 넘어(306줄 · 항목 18) 반복 434~443 을 `history_074.md` 로 밀어냈다 — 계획 75 `project-const-drift`(문서가 인용한 상수를 코드와 대조하는 자) 와 계획 76 `history-cap-guard` 의 개발까지다. **`HistoryCapTest` 가 세워진 뒤 처음으로 회전을 부른 반복이고, 부른 것이 그 반복 자신의 마감 기록이다** — 상한을 넘긴 채 여섯 반복을 간 반복 440 과 달리 같은 반복 안에서 밟혔다.
- 2026-09-08 | 회전 | `history_current.md` 가 계획 80 `recrawl` 개발 3/3 기록으로 상한을 넘어(305줄 · 항목 19) 반복 444~451 을 `history_075.md` 로 밀어냈다 — 계획 76 `history-cap-guard` 의 테스트·리뷰·e2e·아카이브와 야간 짧은 경로 둘(`digest` 회전 위임 판단 · 코드 자기 실측 인용 축), 그리고 근거 0 으로 끝난 탐색 마감이다. 남긴 것은 반복 452(계획 77 착수)부터다. **줄 수 축이 항목 수 축보다 먼저 물었다** — 오늘 밤 세 반복이 전부 16줄 안팎이라 항목은 19로 상한 안이었는데 줄이 먼저 찼다

## 보류 (승인 대기)

<!-- - 2026-08-09 | plan_db 스텝2 | 마이그레이션 필요 → 무인 모드가 보류 -->

- 2026-09-09 | plan_userinfo-leak | **계획 83 — 보안 건이라 밤이 적용하지 않는다**(`SKILL.md` 「보안 관련 — 줄 수 무관 항상 보류」). `docs/patches/userinfo-leak-refuse-credentials.patch` (382줄 · 구현 + 단위 + e2e · `git apply --check` 통과). **바뀌는 계약**: `urls.normalize` 가 netloc 에 `@` 를 든 URL 에 `None` 을 준다 — 018 의 「userinfo 보존」을 뒤집는다. 되돌리기는 조건 한 줄 삭제. 작업 트리는 되돌려 뒀다(전수 709 OK).

## 반복 실패

<!-- 같은 원인으로 2회 이상 막힌 것. 근본 원인이 안 고쳐졌다는 뜻이다. -->
- [리뷰 예산] **정적으로 읽히는 판정에 변이 판을 걸면 산출물 없이 정지한다** — 계획 66 리뷰에서 2회(48분·39분, 2026-09-06 반복 385). 둘 다 직전 반복이 성한 트리에서 이미 산 초록(`Ran 625 · OK`)을 변이로 되사려다 못 나왔다. **성한 트리 + 깨끗한 워킹트리 + 코드 0줄 변경이면 리뷰는 정적 판독으로 닫는다** — 정규식·상수는 실물에 태워 읽고, 변이는 «새 단언이 무엇을 죽이나» 를 처음 증명하는 개발·테스트 phase 에서만 산다.
- ~~**CLI 가 예상 못 한 입력에 트레이스백을 낸다**~~ — **닫혔다 — `indexer-cli-guard`(짧은 경로), 2026-08-29.** 2회였다: crawler-core 리뷰(bc98bc8)에서 crawl.main 을 고쳤는데 indexer.main 에서 같은 부류가 다시 나왔다(pages 없는 DB). `index_pages()` 가 읽기 직전 `sqlite_master` 를 보고 `NoCrawlDataError` → `main()` 이 복구법을 찍고 rc=2. **적어 둔 근본 원인("CLI 진입점마다 방어를 따로 쓴다")은 과했다** — 착수 때 셋을 다 탐침하니 `serve.main`(`--port` 비숫자·값 없음)과 `crawl.main` 은 이미 막고 있었고 실제로 남은 구멍은 **한 곳**이었다. 공통 방어층을 만들었다면 한 개짜리 문제에 추상을 세운 것이 된다. digest `[7]`(**보류 항목의 진단은 옳아도 처방은 그때의 추정이다 — 착수 때 다시 잰다**)의 또 다른 사례다. 다음에 CLI 를 또 만들 때 세 번째가 나오면 그때가 공통화할 때다

- **선례를 옮기면 코드는 따라오지만 그 선례가 성립하던 전제는 안 따라온다** — **3회, 전부 `extract.py` 에서 세 반복 연속.** ③ 2026-09-03 계획 51 리뷰 2 `[R51-3]`: 개발 2 가 `_implied_end` 의 `p` 가지에 부모의 술어(`tag not in _INLINE_TAGS and tag not in _NON_BLOCK_TAGS`)를 **읽기만 한다**며 재사용했는데, 그 술어는 원래 소비자(문단 경계)에서는 **여집합이어도 무해**(문단이 잘게 쪼개질 뿐)하지만 새 소비자(숨김 범위)에서는 **모르는 태그가 전부 `<p>` 를 닫아 숨은 텍스트가 근거로 나간다** — 표준 태그 127개 중 과잉 19 · 커스텀 요소는 전부 과잉(무한 집합) · 실제 DB 로 3/4 유출. ② 2026-09-03 테스트 2 `[T2-1]`: **같은 재사용 술어의 뒤 절이 한 번도 안 재지고 있었다**(기존 케이스가 `<b>` 하나뿐이라 변이가 588건 초록). ① 2026-09-03 개발 1 → 리뷰 1 `[R51-2]`: `_hidden` 닫기를 「`_open` 과 **같은 관용구**」로 정당화했는데, `_open` 은 빈틈없는 스택이라 이름으로 찾는 것이 맞고 성긴 스택에서는 틀렸으며 **관용구와 함께 비용(깊이²)도 옮겼다**. **근본 원인은 「같은 관용구/술어를 쓴다」가 리뷰에서 정당화로 통과된다는 것**이다 — 세 번 다 코드는 옳게 옮겨졌고 틀린 것은 **전제**(스택이 빈틈없나 · 집합이 여집합인가 · 담는 것이 넓어졌나)였다. 처방 후보: 코드를 재사용하며 «같은 X 로 한다» 라고 적을 때 **그 X 가 성립하던 전제 한 줄을 같이 적는다** — 안 적히면 그 자리가 다음 결함이다. 셋 다 다음 반복이 잡았으니 스위트는 이 부류에 눈이 멀어 있다(`[8]`·`[97]` 과 옆 칸).

- ~~**스텝을 커밋하면서 `docs/index.md`·`docs/metrics.md` 의 숫자를 안 올린다**~~ — **닫혔다 — 계획 60 `index-step-sync`, 2026-09-06**(위 완료 항목 · `plan_history_046.md` · `design_history_046.md` · `docs/e2e/index-step-sync/result.md`). **다섯 번 만에 규율이 아니라 기계가 됐다** — `tests/test_docs.py` 의 `StepSyncTest` 가 `status.md` 의 `step` 과 `index.md` 의 스텝 칸을 매 실행 대조하고, 판정은 `step_gap` 순수 함수로 나와 `StepGapTest` 5건이 갈래를 전부 밟는다(`metrics.md` 축은 ④ 이후 `IterationSyncTest` 가 이미 닫았다). **다만 반복 축 검사의 «판정» 은 아직 아무도 안 잰다** — 아래 테스트 phase 갭 `[6]` 이 그 자리이고 계획 61 이 받는다. 원문: **5회 · 2026-09-05 계획 60 `index-step-sync` 로 열었다**(`docs/plan_index-step-sync.md` · 탐색 **7순위** — 6순위가 처음으로 「여는 조건 미도래」로 전부 막혔다). ⑤ 반복 350 착수 탐침이 최근 30커밋에서 `status.md` 의 `step` 과 `index.md` 진행 행의 스텝 칸을 나란히 뽑으니 **어긋난 커밋 11건**(계획 56 ×4 · 57 ×3 · 58 ×3 · 59 ×1)이고 전부 `status` 가 앞서고 `index` 가 `0/1` 에 멈춘 방향이다 — ④ 이후 `IterationSyncTest` 가 닫은 것은 `반복`↔`iteration` 축뿐이고 **`index.md` 의 스텝 칸은 여전히 아무도 안 붙든다.** 아래 ③ 이 적어 둔 처방(`git log` 의 개발 커밋 수와 대조)은 **착수 탐침이 갈아 끼웠다**(`[7]` 「기록된 답을 실행 전에 다시 재라」 열한 번째 적용) — 커밋 메시지 문자열에 기대는 대신 같은 값을 **문서 둘**(`status.md` `step` ↔ `index.md` 진행 행)로 재면 `IterationSyncTest` 와 같은 모양이다. 다만 진행 행이 **0개**인 커밋이 실재해(계획 커밋과 index 등재 커밋이 갈린 자리) 「0행에서 조용히 통과」를 어떻게 막을지가 갈리고, 그것이 계획 60 의 설계 트리거다.
  ④ 2026-09-01 계획 40 개발(`426eb90`): 어긋난 것이 `index.md` 가 아니라 **`metrics.md`** 다 —
  `iteration` 200 인데 `반복` 199(커밋이 안 건드렸다). ③이 지목한 검사가 잡는 바로 그것이다.
  ③ 2026-08-31 계획 37 리뷰(이 항목): `index.md` 의 `plan_indexer-interrupt` 행이 `0/2` 인데 스텝 1·2 가
  `20d953e`·`f6d246f` 로 이미 커밋돼 있었다 — **개발 두 반복과 테스트 한 반복이 그 줄을
  안 봤다.** `metrics.md` 의 `반복`(185)과 `status.md` 의 `iteration`(185)은 이번엔
  맞았다 — 어긋나는 것은 늘 `index.md` 의 **스텝 칸**이다. **세 번째라 처방이 틀렸다는
  뜻이다**(`severity.md` 5절). ②가 낸 후보 둘 중 "기록 목록에 셋을 나란히 적는다" 는
  이미 있는데도 또 났다 — 남은 후보는 검사 한 줄이고, 그 검사는 `iteration` 대조가
  아니라 **`index.md` 의 스텝 칸 대 `git log` 의 개발 커밋 수**를 재야 한다.
  세 번 다 리뷰가 잡았고, 리뷰 없는 phase 에서는 조용히 어긋난 채로 간다.
  ① 2026-08-30 계획 34 리뷰(`1abc058`): `index.md` 가 `(미개설) 0/4` 인데 개발 4/4·
  테스트 1/1 이 이미 커밋돼 있었다. ② 2026-08-30 계획 35 리뷰(이 항목): 같은 줄이
  `0/2` 로 멈춰 있었고, `metrics.md` 의 `반복` 은 **175 인데 `status.md` 는 177** 이었다
  (반복 3회 동안 phase 칸이 1회만 올랐다 — 개발 1·테스트 2 중 하나만 셌다).
  **근본 원인은 "기록 파일이 셋인데 스텝 커밋은 `status.md` 만 본다"** 다.
  `index.md` 는 계획을 열 때, `metrics.md` 는 계획을 닫을 때만 만져지는 습관이 있다.
  다음 처방 후보: 스텝 커밋의 기록 목록에 셋을 나란히 적거나, `반복`(metrics)과
  `iteration`(status)이 어긋나면 죽는 검사 한 줄. **둘 다 리뷰가 잡았다 — 리뷰가
  없는 phase 에서는 조용히 어긋난 채로 간다는 뜻이다.**

- **제자리 변이를 자동 스냅샷이 커밋한다** — **5회, 뒤의 넷은 원격까지 갔다.**
  ⑤ 2026-09-01 `b919e68`(계획 41 계획): 마감 작업의 **절반**(`git mv` 둘 + `index.md` +
  `digest.md`)이 잡혔다. 갓 판 로컬 브랜치라 접을 수 있어 보였지만 **훅이 브랜치까지
  원격에 만들어** 이미 `origin/loop/crawl-db-guard` 가 있었다 — 되감지 않고 앞으로 커밋했다.
  **한 스텝이 두 커밋으로 갈렸다**("스텝 하나 = 커밋 하나" 가 깨진 첫 사례).
  ④ 2026-09-01 `9f034eb`(계획 39 리뷰): 리뷰가 더한 새 테스트가 그 커밋에 실려 원격까지.
  ③ 2026-08-30 `a9202b6`(계획 35 테스트): **변이가 아니라 정상 작업**이 잡혔다 — 처방은
  지켜졌는데 훅이 **미완성 트리를 커밋·푸시**했다(그 커밋은 `test_readme` 기준 RED).
  ① 2026-08-29 `0aac7b0`(계획 33 스텝 4): `crawl.py:179` 변이가 커밋됐고 다음 스텝이
  되돌렸다 — 로컬에서 끝났다. ② 2026-08-30 `06bc289`(계획 34 스텝 1): `# M1` 이
  **푸시까지** 됐고 `reset --soft` 로 접었다가 브랜치가 갈라져 `merge -s ours`(`d31560a`)
  로 풀었다. **근본 원인은 안 고쳐졌다** — 훅은 작업 트리가 왜 더러운지 안 본다.
  처방: 변이는 `.git` 없는 스크래치패드 사본에서만. 제자리면 `touch .mutation-lock` 후 삭제.
  접기 전에 `git log origin/<브랜치>` 를 본다. 후보: 스텝 시작에 `touch .loop-busy`.

- **러너의 판정 줄을 가린다(파이프가 대표적이다)** — **15회, 뒤의 열넷이 연속이다.** 훅이 보는 것은 파이프의 종료 상태라 검증으로 안 세고, `tail`·`grep` 이 삼키는 것이 하필 실패 원문이다. ⑩(반복 213)은 **GREEN 확인**에 `| tail -5` 를 붙였다 — ⑨(재확인)와 같은 자리이고, 방아쇠는 셋 다 **이미 초록일 것 같은 실행**이다. 조항으로 두 반복 연속 못 막았다. **⑬ 반복 241(리뷰) — 방아쇠가 또 그것이고, 이번엔 자리가 더 나쁘다.** 맨몸 2회를 마치고 «`status.md` 를 고친 뒤 한 번만 더 확인» 하는 **마감 실행**에 `2>&1 | tail -4` 를 붙였고 `Ran/OK` 줄이 **실제로 잘려 안 보였다**(운으로도 안 넘어갔다). 즉시 맨몸 재실행해 495건 OK 를 눈으로 읽었다. **같은 반복의 `status.md` 가 그 직전에 «파이프 0회 — 연속이 다시 섰다» 라고 적혀 있었다** — 기록이 자기를 거짓으로 만든 첫 사례이고, 마감·재확인 실행이 조항을 세 번째로 뚫었다. **처방 후보**: 조항이 «판정 실행» 만 겨냥해 마감 실행이 예외처럼 읽힌다 — «러너를 부르는 **모든** 호출» 로 문장을 조이는 일이고 **저장소 밖 룰 파일**이라 사람이 연다. **⑭ 반복 242(e2e) — 두 반복 연속이고 자리가 ⑬ 과 같다.** 맨몸 24회를 마치고 문서 줄 수를 세는 `wc -l` 뒤에 러너를 `&&` 로 붙이면서 `2>&1 | tail -4` 를 딸려 보냈다 — **`Ran/OK` 가 또 잘렸다.** 즉시 맨몸 재실행해 **495건 OK 12.209초**를 눈으로 읽었다. **새로 보이는 것: ⑬ 은 «마감 실행», ⑭ 는 «러너가 아닌 명령에 러너를 이어 붙인 것»이라 방아쇠가 파이프가 아니라 **다른 목적의 명령에 얹기**다** — `wc -l` 의 출력을 줄이려던 `tail` 이 러너까지 덮었다. 처방 후보에 한 줄 더: **러너는 다른 명령과 한 호출에 섞지 않는다.** **⑮ 같은 반복 242(e2e·회전 뒤 마감) — 파이프가 아니라 `2>/dev/null` 이다.** ⑭ 를 적고 «이제 파이프는 안 쓴다» 로 옮겨 간 자리에서 **잡음을 줄이려고 stderr 를 버렸는데 `unittest` 의 `Ran/OK` 가 바로 그 stderr 다** — 화면에 stdout 의 크롤 로그만 남고 판정이 통째로 사라졌다. 즉시 완전 맨몸으로 재실행해 **495건 OK 12.268초**를 읽었다. **이 항목의 이름이 «파이프» 였던 것이 조항의 구멍이었다** — 가리는 것은 `|` 만이 아니라 `2>/dev/null`·`2>&1`·`-q` 도 같다. 그래서 제목을 «판정 줄을 가린다» 로 넓혔다. 처방 후보 세 줄째: **러너 호출에는 출력 조작을 아무것도 붙이지 않는다 — 재지향도 포함이다.** **⑯ 반복 267(개발 8) — 한 반복에서 셋이다.** `design_check` 를 `> /dev/null` 로, 단위 러너에 `-k` 를 붙여 스위트를 걸러, **변이 아래에서 `2>/dev/null`** — 셋 다 즉시 맨몸 재실행했다. 마지막 것은 **⑮ 와 형태가 글자 그대로 같고**, 이 항목을 읽은 **같은 반복 안에서** 났다. 방아쇠는 이번에도 «잡음 줄이기» 다. **계수기 둘이 갈렸다 — 이 항목은 15, `status.md` 4번은 19(→22)를 적고 있었다.** 같은 실패를 세는 자리가 둘이고 어느 쪽도 자동이 아니라서다. 그것 자체가 이 항목의 논지다: **손으로 세는 규율은 손으로 지키는 규율만큼 샌다.** 계수는 `status.md` 쪽(22회)을 정본으로 삼는다. **⑰ 반복 269(개발 9) — 또 셋이다. 두 반복 연속으로 한 반복에 셋씩이다.** 변이 국면 진입 전 `2>/dev/null | tail -5` · 변이 아래 `2>&1 | tail -40` · 후보 측정에서 `python3 -m unittest tests.test_indexer` 로 **모듈 하나만** 돌렸다(스위트 거르기 — ⑯ 의 `-k` 와 같은 형태다). 셋 다 맨몸·단독으로 다시 돌려 판정 줄을 읽었고, 둘째는 **변이를 다시 심어** 같은 6건 실패를 재확인했다. **방아쇠가 셋 다 «출력이 길다» 로 같다** — 이 스위트의 크롤 로그가 화면을 덮는 것이 상수라 손이 매번 같은 자리로 간다. 계수는 `status.md` 쪽(22 → **25회**)이 정본이다. **처방 후보에 한 줄 더: 잡음이 방아쇠라면 막을 자리는 사람의 손이 아니라 러너의 출력이다** — 판정 줄을 마지막에 다시 찍는 얇은 래퍼면 «가릴 이유» 자체가 없어진다(저장소 안이라 계획으로 열 수 있다). **⑱ 반복 271(개발 10) — 하나다(셋에서 하나로 줄었다).** 변이 E 판정에 `| tail -5` 와 `2>/dev/null` 을 같이 붙였고 **판정이 통째로 안 나왔다**(⑮·⑯ 과 글자 그대로 같은 형태다) — 즉시 맨몸·단독으로 다시 돌려 `FAILED (failures=9)` 를 읽었다. 계수 **25 → 26회**. **방아쇠는 열여덟 번째도 «출력이 길다»** 이고, 이 항목이 「처방 후보」를 이미 등재해 둔 **뒤에도** 났다 — 등재는 방아쇠를 안 없앤다. 새로 보이는 것: 이번 자리는 «판정» 도 «마감» 도 아닌 **변이 배터리의 중간 실행**이다. 일곱 번을 연달아 돌리는 국면이라 «한 번만 짧게» 의 압력이 회당 붙고, 조항이 겨냥한 「판정 실행」이라는 말이 그 자리를 안 덮는 것처럼 읽힌다. 후보 [9](`unittest -b`)가 그대로 답이다 — 배터리에서 보고 싶은 것은 «몇 건 죽었나» 한 줄뿐이라 `-b` 면 손이 갈 이유가 사라진다. **⑲ 반복 274(e2e) — 하나이고, 자리는 ⑬ 과 같은 「이미 초록일 것 같은 실행」이다.** 선행 관문의 전체 스위트를 맨몸으로 한 번 돌려 **576건 OK** 를 읽은 뒤, *두 번째 측정만* 이라며 `2>&1 | tail -3` 을 붙였다 — 크롤 로그 세 줄이 `tail` 을 채워 `Ran/OK` 가 또 밀려났다. 즉시 맨몸으로 다시 돌려 **13.418초**를 읽었고 그 값이 `docs/e2e/passage-api/result.md` 1절의 숫자다. 계수 **27 → 28회**. **새로 보이는 것은 없다 — 그것이 이 표본의 값이다.** 방아쇠(출력이 길다)·형태(`2>&1 | tail -N`)·자리(두 번째·마감 실행)가 전부 기존 칸에 들어간다. 열아홉 번의 서술이 한 번도 못 막았고, 이 항목을 **읽은 반복 안에서** 또 났다(⑯ 과 같은 구조). **문장은 소진됐다 — 남은 것은 [9](`unittest -b`)뿐이고 그것은 계획으로 열 수 있다.** **⑳ 반복 304~305(계획 52) — 셋이다.** 계획 phase 확인 실행에 `| tail -4`, 개발 phase 의 변이 ② 판정에 `2>/tmp/nul`, 뒤이어 마감 확인에 `> /dev/null`. 셋 다 즉시 맨몸으로 재실행해 판정을 원문에서 읽었다(`Ran 593 tests · OK` · `FAILED (failures=1)` · `design_check` 0). 계수 **28 → 31회**. **새로 보이는 것: [9](`unittest -b`)는 이 자리를 못 막는다.** `-b` 가 삼키는 것은 *테스트 안의 stdout* 이라 변이 배터리의 긴 실패 리포트도, `unittest` 가 아닌 e2e 스크립트의 출력도 그대로다. 방아쇠(「출력이 길다」)의 나머지 절반은 **빨간 쪽이 길다** 이고, 그건 줄이면 안 되는 쪽이라 조항으로 닫을 수 없다. **㉑ 반복 314(계획 53 e2e) — 둘이고, 둘 다 «재확인 실행»이다.** ① 변이 탐침을 맨몸으로 한 번 돌린 뒤 «다시 한 번만» 하는 재확인에 `2>&1 | tail -40` 을 붙여 변이 ① 구간이 잘렸다. ② 그 위반을 문서에 적은 **바로 그 반복 안에서**, 문서를 고친 뒤 `test_docs`·`test_readme` 재확인에 `2>&1 | tail -30` 을 붙였다 — 16건이라 `OK` 는 살아남았지만 `rc` 는 파이프 뒤라 안 찍혔고, 살아남은 것은 조항이 아니라 **운**이다. 둘 다 즉시 맨몸으로 다시 돌려 판정 줄과 `rc` 를 읽었다(`Ran 16 … OK · rc=0` · `Ran 599 tests in 13.585s … OK · rc=0`). 계수 **31 → 33회**. **새로 보이는 것: 한 반복 안에서 두 번 났고, 두 번째는 첫 번째를 «위반 1회» 라고 적어 넣은 뒤에 났다.** ⑬(기록이 자기를 거짓으로 만든 첫 사례)의 다음 칸이다 — 그때는 «파이프 0회» 라는 기록이 거짓이 됐고, 이번엔 **«위반 1회» 라는 숫자가 잉크가 마르기 전에 틀렸다**. 방아쇠는 열아홉 번째도 같다(«이미 통과한 줄 알고 다시 돌리는 실행»). [9](`unittest -b`)는 여기도 못 막는다 — 삼킨 것은 테스트 출력이 아니라 **`rc`** 이고, `-b` 는 종료 상태를 안 지킨다. 이 칸에서 처음 또렷해진 것: 조항이 금지해야 할 것은 «긴 출력을 줄이는 것» 이 아니라 **«러너를 파이프의 왼쪽에 두는 것»** 이다 — `rc` 가 사라지는 것은 출력 길이와 무관하게 파이프 그 자체의 성질이다. **㉒ 반복 327(계획 56 계획 phase) — 하나다. 먼저 계수를 맞춘다**: ㉑ 뒤 계획 54 설계 반복이 적은 1회(`2>&1 | tail`, `history_057.md` 에 있다)가 이 항목에 안 들어와 있었다 — **33 → 34**, 그리고 오늘 것을 더해 **35회**. 오늘 것은 `-k` 의 폭발 반경을 재려고 `for` 루프 안에서 러너를 `2>&1 | tail -6` 으로 일곱 번 돌린 것이다 — `rc` 가 통째로 사라졌다. 즉시 필요한 둘을 맨몸·단독으로 다시 돌려 읽었다(`-k Serve` → `Ran 15 tests · OK · rc=0` · `-k readme` → `FAILED (failures=1) · rc=1`). **새로 보이는 것: 방아쇠가 «재확인»도 «출력이 길다»도 아니라 «한 번에 여러 번 돌린다»** 다 — 루프로 N회 돌리는 순간 화면 예산을 회당으로 나누게 되고, 그 나눗셈이 파이프를 부른다. 조항의 문장(«러너를 파이프 왼쪽에 두지 않는다»)은 이 자리도 덮는데, 덮는 것과 막는 것은 다르다는 표본이 스물두 번째다.
  ① 2026-09-01 그 앞 반복(지시서에만 경고) · ② 계획 40 테스트 `| tail -5` · ③ 계획 40
  리뷰 — 조항을 **자기 손으로 한도에 적어 놓고** `| tail -4` · ④ 계획 41 계획 — 한 반복에
  `| tail -3` 과 `| grep` **두 번** · ⑤ 계획 41 설계 — 한도로 **읽은 그 반복에** `| tail -4` ·
  ⑥ 계획 41 개발 — README 를 고친 **재확인 실행**에 `| tail -5` · ⑦ 계획 41 테스트 — **변이 사본의 기준선**에 `| grep -E` · ⑧ 계획 41 e2e — **Stop 훅이 "검증을 안 돌렸다" 고 막자** 급히 답하려고 `| tail -4`(맨몸 재실행 466 OK).
  ⑨ 계획 42 리뷰 — **마감 직전 "숫자만 다시 보자"** 는 재확인 실행에 `| tail -5`(맨몸 재실행 467 OK 11.939초). 같은 반복이 한도에 조항을 **적어 넣고서** 그랬다(③과 같은 자리).
  ⑪ 계획 44 테스트(반복 225) — README 의 단위 수를 고친 **재확인 실행**에 `2>&1 | tail -5`, 판정 줄과 종료 코드가 잘려 통과처럼 보였다(맨몸 재실행 475 OK). ⑫ 계획 44 리뷰(반복 226) — **기록을 다 쓴 뒤 스위트 재확인**에 `2>/dev/null`, 판정 줄이 삼켜져 `rc=0` 만 봤다(맨몸 재실행 475 OK 11.932초). **⑥⑨⑪⑫ 로 재확인이 넷 — 단독 최다 방아쇠다.**
  **방아쇠가 매번 다르다**: 망각(②③) → 재확인(⑥⑨) → "사본이라 가벼운 실행"(⑦) → **훅을 만족시키려는 조급함**(⑧ — 검증을 요구하는 기제가 검증을 새게 만든 새 축이다). 조항 하나로 못 막는다는 증거가 열하나다. **⑨ 로 재확인이 두 번째가 됐다 — 최다 방아쇠다: 이미 초록인 것을 다시 볼 때 손이 짧은 출력을 원한다.** 처방 후보: 러너 호출을 한 줄로 굳혀 두고(별칭·`make test`) 손으로 조립하지 않는 것 — **두 반복 연속으로 났으므로 규율이 아니라 계획으로 올린다.**
- **회전을 돌리면서 그 아카이브의 `digest` 압축 줄을 안 남긴다** — **5개.** 아카이브 20개(`history_001.md`~`history_020.md`) 중 **001·002·003·005·019** 다섯이 `digest.md ## 완료` 의 회전 줄에 이름이 없다(2026-09-01 계획 44 리뷰 전수 실측). `digest` 자신은 *"원본은 `history_<NNN>.md` 에 그대로 있다"* 로 **아카이브 색인을 자처**하는데 색인에 구멍이 다섯이면 그 반복들의 판단 재료는 이름으로 못 찾는다. 가장 최근 사례가 **003**: 계획 44 테스트 반복이 200줄 상한을 맞추려 회전 안내 3줄을 지웠고 그것이 `docs/` 안의 유일한 003 포인터였다 — 다만 **같은 커밋(`2c85b96`)이 `history_current.md` 에 대체 포인터를 넣어 고아는 면했다**(리뷰가 `git show` 로 확인). 그 대체 포인터도 **300줄 상한으로 회전하는 문서 안**이라 다음 회전에 같은 자리로 돌아온다. 처방은 `tests/test_docs.py` 에 검사 한 줄(`docs/history_[0-9]*.md` 의 모든 아카이브 이름이 `digest.md` 어딘가에 나오는가)이고, **위 "기록 3파일 불일치" 의 처방과 같은 파일·같은 꼴이라 한 계획으로 묶인다.**
- **규칙을 고른 자에 그 규칙이 읽는 축의 눈금이 없다** — **2회, 둘 다 계획 48 갈림길 6 이고 둘째가 첫째의 수리 안에서 났다.** ① 2026-09-02 리뷰 5: 개발 8 의 `_SHAPES` 여덟 행이 보일러플레이트를 **전부 본문 앞에·본문보다 짧게** 둬서 「동점이면 긴 쪽」이 6/8 로 뽑혔는데, 방향을 짝지으니 8/11 이었다. ② 2026-09-02 리뷰 6: 개발 9 가 «앞/뒤»·«길다/짧다» 방향은 짝지었지만 **«정답이 `<p>` 인가» 는 안 짝지었고**, 새로 고른 규칙이 읽는 축이 하필 그것이다 — 11행 표는 **정답이 늘 `<p>` 이고 경쟁자는 한 번도 `<p>` 가 아니며 `<p>` 대 `<p>` 동점 행이 0개**다. 그 표에서 11/11 을 받은 규칙이 방향을 짝지은 8행에서 **1/8**, 합친 19행에서 **12/19** 로 **철회했던 「길이」(15/19) 밑**이다. **근본 원인은 «자를 대칭으로 만들어라» 를 «지난번에 진 축을 대칭으로 만들어라» 로 읽은 것**이다 — 자의 대칭성은 **새 규칙이 읽는 축**에서 재야 하는데, 그 축은 규칙을 고르고 나서야 정해진다. 처방: **규칙을 고른 뒤 «이 규칙이 읽는 값이 표에서 양쪽 다 정답인 행이 있나» 를 한 번 더 묻는다** — 없으면 그 표는 이 규칙을 못 잰다. 리뷰 5 가 굳힌 «자가 안을 골랐다면 그 자에 반대 방향 행이 있나» 의 **다음 칸**이고, 순서가 «자 → 안» 이 아니라 **«자 → 안 → 자» 로 한 바퀴 더**여야 한다는 뜻이다. 세 번째가 나면 규율이 아니라 계획이다 — 실물 코퍼스가 저장소에 들어오는 일이다(`data/crawl.db` 는 2문서 × 66자, e2e 코퍼스는 한 문장짜리 `<p>` 뿐이라 어느 자도 정본이 아니다). **③ 2026-09-04 계획 51 e2e(302) — 세 번째다. 이번엔 규칙이 아니라 «자» 쪽이고 축은 오탐이다.** 새 e2e 의 오탐 대조군 3종을 **한 문서에** 몰아 뒀더니 과탐 변이 M3(`class` 에 `"hidden"` 이 있으면 숨김)이 `class="hidden-md"` 문단을 죽여도 **남은 두 문단이 대신 뽑혀** 자가 초록이었다 — 계획 8절이 「오탐이 제일 위험」이라 지목한 바로 그 방향인데 그 축의 눈금이 **한 칸도 없었다**(문단 하나가 사라져도 문서당 문단 1건이라는 출력이 안 움직인다). **문서 하나에 문단 하나**로 갈라 다시 돌리니 M3 이 죽었다. ①②는 «규칙이 읽는 축», ③은 «변이가 죽이는 축» 이지만 형태는 같다 — **자의 출력이 변이의 효과와 1:1 로 붙어 있나를 안 물었다.** 처방을 한 줄로 좁힌다: **변이 하나가 fixture 항목 하나에만 닿게 자른다** — 한 문서·한 응답에 항목을 몰면 나머지가 결과를 메운다. **세 번째가 났으므로 규율이 아니라 계획이다**(위 ①② 가 예고한 그 조건이고, 예고한 처방「실물 코퍼스」와는 다른 축이라 계획 후보로 따로 선다).

- **성능 초록불이 예산을 안 밟는 축 위에서 난다** — **2회, 둘째는 첫째의 수리 안에서 났다.** ① 2026-09-02 계획 48 리뷰 1: `/passages` 가 `pages.html` 을 통째로 재파싱하는데 e2e 코퍼스 본문이 208자라 크기 축을 못 밟아 `p95 1.76ms(예산의 0.4%)` 가 2.5MB 문서의 600.7ms 를 가렸다 → `MAX_PASSAGE_HTML` 로 닫았다. ② 2026-09-02 계획 48 리뷰 2: 그 캡을 지키는 **값 가드가 순수 산수**(`캡/1000 × 0.118ms × PASSAGE_LIMIT ≤ 125ms`)라 초록불의 근거가 **계수 하나뿐**인데, 그 0.118 을 «한글·태그 촘촘» 한 모양에서만 재서 낱말마다 `<b>`/`<i>` 가 낀 평범한 문서(**0.352**)에서 3배 틀렸다 — 가드가 증명한 118ms 의 실제는 **288ms**, 프로세스 밖 HTTP p95 는 **790ms(예산의 158%)** 다. **근본 원인은 둘 다 «최악 입력을 누가 정했나» 를 안 적은 것**이다 — ①은 코퍼스가, ②는 계수가 최악을 정했는데 어느 쪽도 그 사실이 문서에 없었다. 처방: **성능 숫자를 박을 때 그 숫자를 낸 입력의 모양을 같은 줄에 적는다** (위 `[8]` «기준선에는 어떤 세계에서 잰 숫자인지를 적는다» 의 세 번째 사례다). 세 번째가 나면 규율이 아니라 계획이다 — 최악 입력 fixture 를 저장소가 갖는 일이다.
- **원격을 바꾸는 동작을 「명령을 보냈다」로 기록한다 — 원격을 다시 안 읽는다** — **1회(첫 사례).** 2026-09-02 반복 287 이 `status.md` 와 `index.md` 에 「PR #7 병합 완료 · `origin/main` 은 계획 50 까지」로 적었는데, 반복 288 이 원격을 읽으니 `gh pr view 7` 은 `OPEN`·`mergedAt: null` 이고 `git ls-remote origin main` 은 `687a1598…`(계획 47)이었다 — **명령을 보낸 것이 결과로 기록됐다.** 러너 규율의 「판정 줄을 직접 본다」와 같은 문이고, 그 문이 `git`·`gh` 쪽에서 안 지켜진 첫 사례다. 처방: **원격을 바꾸는 동작은 「명령을 보냈다」가 아니라 「원격을 다시 읽어서」 기록한다** — 병합·푸시·PR 상태는 `git ls-remote`·`gh pr view` 의 출력을 본 뒤에 문서에 적는다. 반복 288 이 두 문서의 세 줄을 정정했다.

## 다음 계획 후보

- ~~[7] **저장 표현을 바꾸면 «값을 훑는 SQL» 이 조용히 죽는다**~~ (2026-09-08 계획 77 스텝 1 실측). `indexer` 의 「뒤늦게 noindex 를 선언한 문서를 뺀다」 경로가 `WHERE p.html LIKE '%robots%'` 로 후보를 골랐는데, `pages.html` 을 압축하자 **BLOB 이라 매치가 영영 0** 이 됐다 — 예외도 빨간불도 없이 **크롤 윤리 축이 초록불 아래서 죽는** 모양이다. **잡은 것은 픽스처가 제품 경로를 지났기 때문**이다(`Store.upsert` 로 넣는다) — raw SQL 로 넣었으면 그 테스트는 옛 표현만 검증하며 초록이었다(`test.md` 갭 ⑥). **일반화: 열의 «표현»에 기대는 SQL(`LIKE`·`substr`·`length`)은 그 열의 저장 방식과 묶인 계약인데 스키마에는 안 적힌다.** **닫혔다 — 2026-09-08 야간 탐색이 여는 조건을 실제로 셌다: 실제 SQL 0건.** `src/` 전수를 `LIKE`·`substr(`·`length(`·`instr(`·`glob` 으로 훑어 유일한 히트가 `indexer.py:225` 의 **주석**(계획 77 이 이 필터를 걷어냈다고 적어 둔 줄)이고, `SELECT`/`WHERE` 를 든 줄 중 이 넷을 쓰는 것은 0줄이다. **일반화는 남기고 자리는 닫는다** — 이 부류가 다시 생기는 것은 새 SQL 이 값의 표현에 기댈 때이고, 그때 잡는 것은 자가 아니라 **픽스처가 제품 경로를 지나는가**다(계획 77 이 잡은 방식).
<!--
리뷰·e2e 가 발견했지만 그 계획의 "하지 않을 것" 범위라 미룬 것.
근거가 이미 있으므로 계획 탐색 5순위로 바로 쓸 수 있다. (discover.md)
보류(승인 대기)와 다르다 — 이건 막힌 게 아니라 미룬 것이다.
-->
- ~~[9] **`MAX_PASSAGE_HTML` 35,000자가 실물 웹의 근거 문단을 망가뜨린다**~~ (2026-09-07 계획 74 스텝 1 실측 · 실물 361문서). 실물 HTML **중앙값 159,008자**에 **92%(370/400)가 캡에 잘린다.** 위키백과·MDN 은 앞 35KB 가 `<head>`·내비·목차라 선택기가 본문에 **닿지도 못한다.** 캡별 실측(질의 10 · 같은 자): **35K → 근거 73건·본문 `p` 41.1%·문서당 4.8ms** / **100K → 95건·67.4%·8.0ms** / **300K → 98건·80.6%·14.6ms** / 무제한 → 98건·80.6%·16.1ms. **300K 와 무제한이 같은 결과**라 실질 천장은 300K다. 값은 두 갈래로 온다 — ① 목차·`<h1>` 대신 본문 문단(`[8]` 의 실제 처방) ② **근거를 못 내던 문서 25건이 산다**(73→98). 비용은 파싱이고 `[9]` 가 실물에서 잰 `/passages` p95 43.37ms 는 concept 예산의 **8.7%** 라 자리가 있다. **여는 조건: 지금.** 판정에 필요한 실물 코퍼스가 계획 73 으로 생겼고 후보 `[8]`·`[6]` 이 여기로 수렴했다. **캡을 없애지 않고 올리는 이유**는 최대 페이지가 1.13MB 라 무제한은 꼬리에서 예산을 못 지키기 때문이다 — **닫혔다 (2026-09-07 계획 74 `passage-prefer-body`).** 다만 **처방이 바뀌었다**: 캡을 올린 게 아니라 **자를 바이트에서 태그로 바꿨다.** 비용은 태그에 비례하고(ms/1k태그 실물 4.08 · 적대적 2.78, 1.5배) 바이트에는 18배로 흔들린다(0.052 대 0.93) — **바이트 캡은 적대적 페이지로 눈금을 잡아 실물을 깎고 있었다.** `MAX_PASSAGE_TAGS = 3,000`(실물 태그 중앙값 3,225 = 「중앙값 페이지를 통째로 읽는」 지점) · `_clip_by_tags()` 는 1.13MB 에 0.70ms 로 파싱의 1%. 결과: 근거 **73→96건** · 본문 문단 **41.1→81.2%** · `/passages` p95 43→**138ms**(예산의 8.7→27.6%). **최악 상한은 넓힌 게 아니라 좁혔다** — 350ms → **120ms**. 예산을 8,000 까지 올려도 근거는 2건(96→98)만 더 오고 값은 예산의 +21%p 라 **품질 고원의 무릎**에서 멈췄다. **그리고 계수가 세 번 낡았던 이유가 여기서 풀렸다**(`[7]` 옆 칸) — 흔들린 것은 파서가 아니라 **자**였다. 실측: `docs/e2e/passage-prefer-body/result.md`
- ~~[5] **단위 스위트 벽시계의 68% 가 테스트 하나다** (2026-09-01 계획 39 리뷰 실측)~~ — **열지 않고 닫는다 (2026-09-07 반복 424 실측)**. 오늘 다시 재니 `test_index_waits_out_a_write_lock_instead_of_dying` 단독 **8.060초** · 전체 648건 **15.923초** · 그 1건만 뺀 647건 **7.725초** 로, 비중은 68% 가 아니라 **50.6%** 다 — 절대값 8초는 8일째 그대로고 분모가 462→648 로 커진 만큼 비율만 내려갔다. 락을 8초 쥐는 이유는 여전히 정당하다(sqlite3 기본 5초라 그보다 짧은 락은 `timeout` 을 지워도 통과한다 — 아래 `[7]` 이 그 함정이다). **닫는 이유는 거래가 손해라서다**: e2e 로 옮겨 버는 것은 스텝당 8초인데 한 스텝의 실비용은 LLM 왕복이라 8초는 잡음이고, 치르는 값은 「락을 실제로 기다린다」는 **행동** 검증이 매 스텝에서 계획당 한 번으로 줄어드는 것이다. `test_every_connection_waits_the_same_thirty_seconds` 는 30초라는 **값**만 고정하지 기다림을 안 잰다. **일반화 — 비율로 놀란 항목은 절대값으로 다시 잰다.** 68% 는 그때 분모가 작아서 난 숫자였고, 분모는 저장소가 자라기만 하는 축이다
- [4] **`deadline=None` 일 때 `futures.wait(timeout=None)` 이 유지되는지 안 잰다** (2026-08-30 deadline-stop 테스트). `if left is not None` 가드라 무예산 경로는 바이트 단위로 무변경이고, 가드를 지우는 변이는 `TypeError` 로 즉사한다 — 값이 낮아 미뤘다
- [4] **중단 e2e 의 시간 여유가 얇다** (2026-08-30 graceful-interrupt 리뷰, 백지 패스와 독립적으로 겹친 지적). `interrupt_e2e.py` 시나리오 1 은 소켓 10초에 상한 12.0초(여유 2초), 시나리오 2 는 응답 5초에 8.0초(여유 3초). 부하가 걸린 기계에서 흔들릴 수 있는 유일한 자리다. **지금 값은 계획의 12초 목표 그 자체**라 올리면 목표가 흐려지고 내리면 못 잰다 — 깨지는 날 `SLOW_SECONDS` 를 줄이는 쪽이 답이다(재는 것은 "응답을 줍는가"라 값과 무관하다고 파일이 이미 적어 뒀다). **셋째 자리가 생겼고 거기가 제일 얇다** (2026-08-30 deadline-stop 리뷰): `deadline_e2e.py` 시나리오 3 의 `HANG_LIMIT = 12.0` 은 예산 2 + 소켓 10 **정확히 그 합**인데, 위 둘과 달리 **서브프로세스 기동부터** 재므로 실측 10.1초의 여유가 1.9초다. 올리지 않는 이유도 같다 — 12초가 곧 계약의 값이다. 그리고 여기는 흔들려도 **오탐 RED 다**: 파일이 이미 "건수가 계약이고 시간은 파생값" 이라 적었고 진짜 단언(`len(hits) == 1`)은 시간과 무관하다. 깨지는 날 답은 값을 올리는 것이 아니라 **시간 단언을 경고로 낮추는 것**이다
- [7] **`robots.allowed()`·`delay()` 도 비ASCII 호스트에서 예외를 흘린다** (2026-08-26 non-ascii-url 리뷰). `robots.py` 는 `URLError`·`OSError` 만 잡고 `crawl` 에도 잡는 곳이 없어 그대로 크롤 루프를 죽인다. **지금은 도달 불가** — URL 이 태어나는 자리에서 이미 ASCII 가 되고, 순서가 밀리면 되살아나는 것은 `test_robots_and_store_never_see_non_ascii_url` 가 막는다. 설계가 `robots.py` 를 범위 밖에 뒀고 `robots.py` 를 직접 부르는 새 호출처가 생길 때가 고칠 때다. **뿌리는 `fetcher` 와 같다: "`OSError` 계열만 잡는다"** — `fetcher` 쪽은 이 계획에서 닫았다(URL 오류는 즉시 0, 연결·응답 오류는 재시도). ~~`robots.py:_fetch_robots` 의 `resp.read()` 무상한 건과 같은 파일이라 함께 열면 싸다~~ — 그 곁가지는 계획 `robots-read-cap`(`eb6697e`)이 `MAX_ROBOTS_BYTES = 512_000` 으로 닫았다(2026-09-07 탐색 실측). **함께 열 싼 건은 이제 없다** — 이 항목은 위 여는 조건(새 호출처)만으로 열린다

- [6] **`loaf` ← `loaves` 는 아직 미검출이다** (2026-08-27 tokenizer). 영어 **불규칙** 복수(f→ves)라 `porter` 가 `loaf`/`loav` 로 가른다. 고치려면 예외 사전을 저장소에 들여야 해서 계획이 처음부터 뺐다. 남은 유일한 미검출이고 en 19/20 의 그 1 이다
- [6] **`porter` 가 어휘 11.2%(50/448)의 부분 접두 검색을 깬다 — 천장으로 받아들인 값** (2026-08-27 tokenizer 반복 81). `y→i` 규칙 탓에 **짧은 접두도** 끊긴다(`any*` → `anything` 0건, 3/8자). 우회는 낱말을 끝까지 치는 것. **`quality_eval` 은 이 손실을 앞으로도 못 잰다** — fixture 영어 질의 20개가 전부 완전한 낱말이고 코퍼스는 동결이다. 그런데도 유지가 맞았다: 같은 어휘로 이득을 재니 굴절형 질의 생존율이 **96.3% vs 1.0%**(porter 없으면 사실상 전멸). **실물 검색 로그가 생기면 다시 열 값** — 사용자가 실제로 부분 낱말을 얼마나 치는지가 이 거래의 유일한 미지수다
- [5] **색인이 2.4배가 됐다** (1.3→3.1MB / 3000문서, 2026-08-27 tokenizer). bigram 열 둘의 값이다. `prefix='2 3'` 이 `*_ng` 열에도 걸리는데 2-gram 토큰은 길이가 전부 2 라 순수 낭비인데, **FTS5 는 prefix 인덱스를 열별로 끌 수 없다.** 실물에서 디스크가 문제되면 bigram 을 별도 테이블로 빼는 것이 다음 칸이다. 컨셉 경량 축(단일 머신 100만 문서)에 걸리기 전까지는 안 연다
  `serve.do_GET` 의 넓은 `try` 를 **고치기 전에 쟀다**: 프로그래밍 오류(`AttributeError`·
  `TypeError`)까지 500 이지만 유출 0 이고, `Exception` 이라 `KeyboardInterrupt` 는 안
  삼키고, `try` 는 `_parse`+`_page_hits` 만 덮는다(200 렌더링은 `else`). `src` 0줄.
  **넓은 `try` 의 유일한 실위험은 원인이 로그에만 있다는 것인데 그 로그가 무단언이었다** —
  화면 경로는 0건, JSON 쪽은 접두어만 봐서 `%r` 을 고정 문구로 바꿔도 초록이었다.
  일반화: **가려진 `except` 를 의심하면 `except` 가 아니라 그것이 남기는 자국을 재라.**
- [6] **거래를 재려면 양쪽을 같은 자로 재라** (2026-08-27 tokenizer 반복 80→81). 반복 80 은 `porter` 의 **손실을 448개 어휘 전수로** 재고 **이득은 fixture 질의 1건**으로 재고서 "사는 것은 한 건" 이라며 사람 몫으로 올렸다. 손실 쪽에서 정확히 진단한 fixture 의 구조적 눈멂이 **이득 쪽에도 똑같이 걸린다**(그 20개는 거의 전부 기본형)는 것을 보지 못했다. 반복 81 이 같은 어휘로 다시 재자 **결론이 뒤집혔다.** 한쪽만 정밀하게 재면 **잰 쪽이 이긴다** — 표본이 다르면 비교가 아니다. **보류를 올리기 전에 "이득 쪽도 손실 쪽과 같은 표본으로 쟀나" 를 먼저 묻는다**
- [5] **부정 단언 하나가 다른 이유로 통과할 수 있다** (2026-08-27 tokenizer e2e 변이 M5). "옛 색인이면 화면이 500" 단언은 **드리프트 감지를 통째로 꺼도 통과했다** — 감지가 없으면 질의 자체가 `OperationalError` 로 터져 어차피 500 이 난다. 실제로 가른 것은 CLI **rc=2** 단언이었다. 상태코드는 원인이 여럿이라 한 줄만으로는 무엇도 고정하지 못한다
- [8] **측정 하네스가 재지 않는 세계가 곧 버그가 숨는 곳이다** (2026-08-27 cooldown-burn). `perf_crawl` 이 **아무것도 안 막는 사이트**만 재는 동안 기준선 10.25/s 는 안전해 보였고, 실제로 도는 경로는 **4.5/s** 로 컨셉 목표 밑이었다 — 8일 동안. **기준선에는 값만이 아니라 어떤 세계에서 잰 숫자인지를 적는다.** 그리고 세계를 하나 더 재는 것이 대개 검사를 조이는 것보다 값이 크다
- [8] **깨우기와 접기는 하나다 — 한 줄씩은 0초를 회수한다** — **2회** (2026-08-30 계획 34 신호 축 · 2026-08-30 계획 35 예산 축). 종료를 앞당기려면 **자는 놈을 깨우는 변경**과 **깨어난 뒤 안 보내게 접는 변경**이 **둘 다** 있어야 하고, 하나만 넣으면 회수가 **0** 이다. 34(신호 축): 잠만 깨우면 `Crawl-delay: 30` 서버에 10초 간격 3발이 나가 **예절이 깨진다** — 그래서 "잠 깨우기"와 "재시도 취소"는 두 변경이 아니라 하나다. 35(예산 축): `.git` 없는 사본에서 갈라 재니 `stop.set()` 만 **70.08초** · `futures.wait` 를 남은 예산으로 자르기만 **70.07초** · **둘 다일 때만 10.05초 · 요청 1건**이다(무변경이 70.08초). 메인 루프가 `futures.wait(timeout=None)` 에 갇혀 예산 검사 자리에 못 오고, 깨어나도 워커의 재시도 잠은 `stop` 없이 안 접힌다. **일반화 — "여기 한 줄만 넣으면 된다" 는 보류 항목의 처방이 절반인지 의심할 자리가 바로 이 짝이다**(`[7]` 과 짝. 35 는 그 처방을 그대로 믿었으면 0초를 회수하고 초록으로 닫을 뻔했다). 다음에 또 나오면 그것은 우연이 아니라 이 코드베이스의 **동시성 구조**다 — 잠드는 자리마다 "누가 깨우나 / 깨면 접히나" 를 짝으로 센다
- [7] **기록된 답을 실행 전에 다시 재라** (2026-08-27 cooldown-burn). digest 가 8일 전에 적어 둔 `[high]` 의 **처방**("제출 시점 `mark_sent`")은 실측하니 **0을 회수했다**(4.48→4.49/s). 같은 항목의 "영향은 이어받기 크롤 한정" 도 틀렸다. **보류 항목의 *진단*은 옳아도 *처방*은 그때의 추정이다** — 처방을 근거로 계획을 세우지 말고, 착수 때 다시 잰다. 틀린 처방은 지우지 말고 **왜 틀렸는지와 함께** 남긴다
- [7] **깨지지 않았다고 멀쩡한 테스트는 아니다** (2026-08-27 cooldown-burn). 계약을 바꿨을 때 깨진 것은 9건이었지만, 안 깨진 것 둘이 **자기가 적어 둔 것을 안 재고 있었다** — 하나는 시계를 아예 안 걸어서, 하나는 간격이 아니라 라운드로빈이 답을 내주고 있어서. **계약을 바꿀 때는 깨진 테스트가 아니라 그 계약을 재는 테스트 전부를 본다**
- [6] **탐침의 배선을 먼저 의심한다** (2026-08-27 cooldown-burn 반복 84). 두 트리를 비교했는데 숫자가 **똑같이** 나오면 측정 결과가 아니라 같은 코드를 두 번 잰 것이다 — `perf_crawl.py` 가 import 시점에 **자기 위치 기준으로** 저장소 `src` 를 `sys.path[0]` 에 밀어 실제로 그랬고, 하마터면 옳은 설계를 "아무것도 회수 못 함" 으로 버릴 뻔했다. 사본에서 변이를 재려면 **트리를 통째로** 복사한다
- [6] **주장은 그 주장이 참인 범위까지만 적는다** (2026-08-27 cooldown-burn 리뷰). "간격 시계를 거는 유일한 자리다" 는 참이었지만 읽는 사람은 "요청이 나가는 유일한 자리" 로 받는다. 그 틈에 **`fetcher` 재시도 3회**가 숨어 있었다 — 주석이 틀려서가 아니라 **넓어서** 아무도 안 봤다. `유일한`·`항상`·`모든` 을 쓸 때는 **무엇에 대해 유일한지**를 같은 줄에 적는다
- [6] **백지 패스는 배경을 안 주는 만큼 값을 낸다** (2026-08-27 cooldown-burn 리뷰). diff 만 준 별도 세션이 낸 6~7건 중 4건이 통과선을 넘었고, **그중 둘은 배경을 아는 작성자에게 안 보이던 것**이다(과한 주장·단언의 netloc 누락). 같은 사람이 쓰고 리뷰하는 구조의 해독제다. 다만 **지적을 그대로 믿지 않는다** — 이번에도 1번 지적은 `grep` 해 보니 문자 그대로는 맞았고 틀린 것은 범위였다

- [8] **변이가 실제로 심어졌는지를 먼저 단언한다** (2026-08-29 readme-commands). `sed -i '' '0,/re/s//.../'` 로 변이를 심고 검사가 `OK` 를 냈는데, 잡은 게 아니라 **BSD sed 가 `0,/re/` 를 조용히 무시해 변이가 안 심어진 것**이었다 — 거짓 초록을 "이 변이는 안 잡힌다" 로 읽을 뻔했다. 변이 검증은 **두 단계**다: ① 원문이 실제로 바뀌었나(`assert new != old`) ② 그러고 나서 검사가 빨개지나. ①을 건너뛰면 검사가 아니라 sed 를 시험한 것이다. `SKILL.md` 가 경고하는 "검사 명령이 돌아가면서 틀린 답을 낸다" 의 세 번째 재발이다. **2회 — 2026-09-01 계획 41 테스트에서 반대편이 나왔다: ③ 직전 변이가 되돌려졌나.** `sub M4 && run M4 && revert` 로 이었더니 **변이가 성공한 순간**(러너가 1 을 낸다) `&&` 가 revert 를 건너뛰었고, `crawl.py` 가 M4 인 채 M5 가 `indexer.py` 를 같은 문구로 바꿔 **둘이 다시 같아져 466 OK — 거짓 생존**이 나왔다. 문구 동일성처럼 **두 파일의 관계**를 재는 검사에서 특히 위험하다(한쪽만 봤으면 못 찾는다). 변이 루프는 `&&` 로 잇지 않고 revert 를 무조건 돌린다(`trap`/별도 줄), 그리고 심기 전에 두 파일을 다 찍어 본다. **3회 — 2026-09-06 계획 66 테스트에서 ① 자신의 구멍이 나왔다: `assert new != old` 는 «바뀌었나» 만 재고 «의도한 것만 바뀌었나» 는 안 잰다.** U1(표에 행 하나 추가)을 심는 파이썬이 `text[:end] + ROW` 로 **뒤를 통째로 잘라** README 를 반 토막 냈는데 `new != old` 는 참이라 그대로 돌았고, 3건이 죽어 **「새 단언이 세다」로 읽힐 뻔했다** — 죽은 셋 중 둘(`test_guided_runner_commands_buffer_output`·`test_verification_counts_match_reality`)은 새 단언과 무관하게 **잘려 나간 `## 검증` 절** 때문이었다. 고친 뒤 다시 재니 죽는 것은 목표 하나뿐이다(`failures=1`). 심는 도구가 sed 든 파이썬이든 같다 — 러너 앞에 `diff -u 원본 사본` 한 줄을 붙이면 그 자리에서 보인다. **변이는 심은 뒤가 아니라 심은 것을 눈으로 본 뒤에 판정한다.**
- [7] **살아남은 변이가 등가 변이가 아니라 형제 구멍일 때가 있다** (2026-08-29 seed-scheme-guard). `if not ascii_seeds` → `if seeds and not ascii_seeds` 변이가 전 스위트를 통과했다. "빈 시드는 어차피 안 온다" 로 넘길 자리였는데 실측하니 `crawl --max 1` 이 **플래그가 `len(argv) < 2` 를 채워** usage 검사를 통과하고 시드 0건으로 `수집 0 페이지` rc 0 이었다. **살아남은 변이를 등가라고 판정하기 전에 그 경로가 정말 도달 불가인지 실측한다** — 도달 가능하면 그것은 커버 구멍이고, 대개 지금 고치는 것과 **같은 계약**이라 같은 자리에서 막힌다
- [6] **문서를 입력으로 읽는 검사가 한 벌은 있어야 한다** (2026-08-29 readme-commands). README 가 없는 모듈을 안내한 채로 419건 초록 위에서 8일을 갔다. 소스만 보는 테스트는 **코드와 문서 사이**가 깨진 것을 구조적으로 못 본다 — 안내문이 가리키는 대상이 실재하는지는 `find_spec` 한 줄이면 된다(임포트도 서브프로세스도 네트워크도 필요 없다). 같은 부류로 아직 안 잰 것: README 의 성능 표가 가리키는 기준선 숫자가 지금 `perf_*` 출력과 맞는지

- ~~[9] **`/passages` 총 p95 의 대부분은 `/search` 몫이다**~~ (2026-09-02 계획 48 리뷰 4 가 개발 7 의 판정을 확인하고 등재). 개발 7 은 «합성 코퍼스에서 총 p95 가 예산을 넘는 것(101%·569%)은 이 수리가 만든 것이 아니라 `/search` 가 그 대부분(386ms·2632ms)이고, 실물 64문서에서는 1.39ms(0.3%)» 라며 기록만 남겼다. **리뷰 4 가 그 판정을 확인했다 — 옳다**: `search`·`_connect`·`_bigrams`·`_page_hits`·`_parse`·`_results`·`_send_html` 일곱이 기점 `6e99031` 과 **바이트 단위로 같다**(함수 단위 대조). 계획 48 은 `/search` 를 한 글자도 안 건드렸고 그 비용은 물려받은 것이다. **여는 조건**: 질의어가 문서마다 수천 번 반복되는 합성 코퍼스에서만 나는 값이라 실물 분포가 먼저다 — 갈림길 5 의 «닫는 조건은 실물이다» 와 같은 문을 쓴다. 축은 term frequency 가 높은 문서에서 FTS5 가 쓰는 비용이고, 그 코퍼스에서는 `/search` 가 자기 예산 300ms 도 385ms 로 이미 넘긴다. **계획 48 안에서는 안 고친다**(캡·계수·`/search` 는 전부 그 스텝 밖이다) **여는 조건이 열렸다 — 그리고 결론이 뒤집혔다 (2026-09-07 계획 73 실물 코퍼스 361문서, 질의 10 × 5회).** 실물에서 `/passages` 총 p95 **43.37ms** 중 `/search` 몫은 **15.56ms = 35.9%** 다 — 「대부분」이 아니고, `passages` 고유 비용(27.81ms)이 더 크다. 위 판정은 **질의어가 문서마다 수천 번 반복되는 합성 코퍼스가 만든 것**이었다(「거래를 재려면 양쪽을 같은 자로 재라」의 성능 축 적용). **그리고 지금은 문제가 아니다** — concept 예산 대비 `search` 5.2% · `passages` 8.7%. 다시 여는 조건은 코퍼스가 한 자릿수 배 커지는 날이다. 실측: `docs/e2e/realweb-corpus/result.md` 3절 **취소선을 오늘 긋는다 (2026-09-09 야간 탐색).** 본문은 2026-09-07 에 이미 「지금은 문제가 아니다」로 닫혔는데 **머리의 9점이 그대로**라 이 파일에서 탐색이 가장 먼저 읽는 줄로 남아 있었다 — 오늘 밤이 실제로 그것부터 읽었다. 다시 여는 조건(코퍼스가 한 자릿수 배)은 위 본문에 그대로 있다.
- [7] **화면에 안 보이는 텍스트가 «근거 문단» 으로 나온다 — `<template>`·`aria-hidden`·`display:none`·`hidden`·0px 폰트** (2026-09-02 계획 48 리뷰 4 보안 훑기 실측). `_SKIP_TAGS` 는 `script`·`style`·`noscript` 셋뿐이라 나머지는 전부 샌다(다섯 모양 전부 블록으로 나왔다 · `svg><title>` 은 안 샌다). **색인에는 원래 있던 것**이라(`extract_text` 가 같은 텍스트를 `docs.body` 에 넣는다) 이 diff 가 만든 결함은 아니다. 그런데 **고르는 규칙이 «질의어를 가장 많이 담은 블록» 이라, 키워드를 채운 숨은 블록이 근거 자리를 확정적으로 이긴다** — 스니펫은 FTS5 가 매치 주변을 뜨는 것이라 노출이 우연이었는데 문단 선택은 밀도를 **적극적으로 선호한다**. XSS 는 아니다(`/passages` 는 JSON 전용 · `json.dumps` · `nosniff` · 화면 경로는 이 diff 를 안 지난다). **여는 조건**: `_SKIP_TAGS` 에 `template` 을 더하는 것은 `_TextParser` 가 공유하는 집합이라 **색인 본문이 바뀌고 재색인이 된다** — 계획 5절의 보류 조건이라 계획 48 밖이다. `aria-hidden`·`display:none` 은 속성·인라인 스타일이라 태그 집합으로는 못 닫고 별도 축이다 **같은 뿌리의 축이 하나 더 붙었다 (2026-09-02 개발 8).** 갈림길 6 을 닫고도 `<nav><a>김치찌개</a><a>된장찌개</a></nav>` 는 여전히 내비가 이긴다 — `<a>` 가 인라인이라 링크 텍스트가 `'김치찌개된장찌개'` 로 붙고 그 밀도가 본문 문단을 **점수로** 이긴다(동점이 아니라 동점 깨기로는 안 닫힌다). «보일러플레이트와 인라인 연접» 이라 «블록 경계» 와 다른 축이고, 여는 조건도 위와 같다 — `_INLINE_TAGS`·`_SKIP_TAGS` 가 색인 경로와 공유라 손대면 재색인이다. **계획 51 `hidden-passage` 가 앞 절반을 연다 (2026-09-03).** 계획 phase 가 오늘 다시 쟀다 — 다섯 모양 전부 블록으로 나오고 실제 DB 로 `indexer.passages()` 를 부르니 **5/5 가 숨은 텍스트를 골랐다**(본문 문단이 같은 문서에 있는데도). **처방은 갈아 끼웠다** — 여기 적어 둔 `_SKIP_TAGS` 확장은 색인 경로 공유라 재색인이고 승인 게이트 밖이라, 같은 파일의 `_NON_BLOCK_TAGS`(계획 48 리뷰 5) 선례를 따라 **블록 파서만 읽는 판정**으로 간다. 「기록된 답을 실행 전에 다시 재라」의 네 번째 적용이다. 뒷부분(`<nav>` 인라인 연접)은 `_INLINE_TAGS` 가 색인 경로와 공유라 여전히 열려 있다. **여는 조건이 열렸다 — 그리고 진단이 좁았다 (2026-09-07 계획 73 실물 코퍼스 361문서, 질의 10 · 근거 문단 73건).** 뽑힌 블록의 태그는 `p` 30 · `h1` 15 · `div` 14 · `li` 12 · `td` 2 로 **본문 문단은 41.1% 뿐이고 58.9% 가 비-본문**이다. 그런데 **실물의 주범은 「숨은 텍스트」가 아니라 「보이지만 본문이 아닌 것」**이었다 — `<li>` 「검색 엔진 범주 하위 문단 토글하기」(UI 컨트롤 라벨) · `<div>` 「3.3 어린이 안전 검색 엔진」(목차) · `<h1>` 다른 문서 제목. 여기 적힌 `_SKIP_TAGS`·`aria-hidden` 계열 처방으로는 **목차·내비가 안 잡힌다.** 재료도 합성과 다르다 — 실물 태그 분포는 `li` 119708 : `p` 25087 로 **`li` 가 `p` 의 4.8배**이고 합성 64문서에는 이 축이 아예 없었다. **원인이 밝혀졌다 — 태그 고르기가 아니다 (2026-09-07 계획 74 스텝 1).** `MAX_PASSAGE_HTML` 이 35,000자인데 **실물 HTML 중앙값이 159,008자라 92%(370/400)가 잘린다.** 위키백과·MDN 은 앞 35KB 가 `<head>`·내비·목차라 **선택기가 본문을 아예 못 본다** — 목차·`<h1>` 이 이긴 것이 아니라 **그것밖에 없었다.** 캡만 올려도 본문 `p` 비율이 **41.1% → 67.4%(10만) → 80.6%(30만)** 이고, 그보다 큰 것은 **근거를 아예 못 내던 문서 25건이 살아난다(73 → 98건)**. 처방은 `_SKIP_TAGS` 도 태그 가중치도 아니고 **캡**이다 — 아래 후보 「`MAX_PASSAGE_HTML` 35,000자가…」. 실측: `docs/e2e/realweb-corpus/result.md` 1·2절(정정 포함) **캡을 태그로 갈아 대부분이 닫혔다 (2026-09-07 계획 74).** 본문 문단 **41.1→81.2%**(p78 li7 div6 td3 dt1 pre1) — 실물에서 목차·`<h1>` 이 이기던 것은 **그것밖에 안 보였기** 때문이고, 캡을 태그 3,000 으로 바꾸니 본문이 보이자마자 스스로 이겼다. **남은 것은 축이 다르다**: `<template>`·`aria-hidden`·`display:none` 은 여전히 새고(`_SKIP_TAGS`·`_INLINE_TAGS` 가 색인 경로와 공유라 손대면 재색인 — 승인 게이트) `<nav>` 인라인 연접도 그대로다. **다만 실물 96건에서 이 다섯 모양이 뽑힌 것은 0건**이라 8→7 로 내린다.
- ~~[6] **`extract_blocks` 의 이름표를 읽는 소비자가 0곳이다 — 갈림길 6 이 닫히기 전에는 못 지운다**~~ (2026-09-02 계획 48 리뷰 7 `[R7-1]`, 신뢰도 95). 소비자는 둘뿐이고 **둘 다 안 읽는다**: `indexer.passages` 는 `for _tag, text in ...` 로 버리고 `e2e/passage_eval.py` 는 `len()` 만 본다. `/passages` 응답 스키마는 `{url,title,position,text}` 라 태그 필드가 **아예 없다**. 유지비는 `_BlockParser._open` 스택 · `handle_endtag` 의 되감기 · `_flush` 의 이름표 줄 · 그것을 붙드는 검사 3건이다. **지금 값이 0 이라는 것은 리뷰 7 이 숫자로 쟀다** — 동점 키를 「이름표」로 하면 12/19(길이 15/19·점수만 11/19)이고, 겹쳐도 `(점수,길이,p인가)` 15/19 가 「길이」 단독과 **같아서** 이름표는 **3순위로도 한 번도 안 걸린다**. **그런데도 안 지웠다**: 이름표는 설계서 갈림길 6(«근거 문단을 무엇으로 고르나»)을 재는 세 축 중 하나이고, 그 갈림길의 닫는 조건은 실물 크롤 코퍼스다(위 `[9]`·`[8]` 과 같은 문). 지우면 갈림길을 재던 자에서 축이 하나 사라지므로 **되돌리기가 아니라 그 갈림길과 같은 계획에서 함께 판단할 일**이다. **여는 조건**: 실물 코퍼스로 갈림길 6 을 닫는 계획. 그때 이름표가 이기면 남기고, 지면 스택·검사 3건과 함께 한 번에 지운다. **계획 48 안에서는 안 연다** — 5절의 «하지 않을 것» 이고 리뷰가 지울 크기도 아니다 **여는 조건이 열렸다 — 그리고 결론이 뒤집혔다. 지우지 않는다 (2026-09-07 계획 73 실물 코퍼스 361문서).** 비-`p` 로 뽑힌 43건 중 **같은 문서 안에 질의어를 담은 `<p>` 가 실제로 있던 것이 38건(88.4%)** 이다 — 이름표는 «지울 축» 이 아니라 **«아직 안 쓴 축»** 이고, 실물에서는 좋은 블록과 나쁜 블록을 가르는 바로 그 축이다. 값이 0 으로 측정됐던 것은 **합성 코퍼스가 `p` 중심이라 3순위로도 걸릴 일이 없었기 때문**이다(「측정 하네스가 재지 않는 세계가 곧 버그가 숨는 곳이다」 `[8]` 의 재확인). 유지비 항목(스택·되감기·검사 3건)은 그대로 두고, 다음 계획은 이 이름표를 **쓰는** 쪽으로 간다. **⚠ 이 88.4% 는 틀린 자로 잰 값이다 (2026-09-07 계획 74 스텝 1 이 정정).** 재던 스크립트가 **캡 없이 전체 HTML** 을 파싱하고 `<p>` 가 질의어를 «담기만» 하면 셌는데, 실제 선택기는 `MAX_PASSAGE_HTML` **35,000자 앞부분만** 파싱하고 **점수로** 고른다. 같은 자로 다시 재면 — 비-`p` 43건 중 **캡 안에 점수>0 인 `<p>` 가 있는 것은 3건**이고 **그 3건 중 현행 픽을 점수로 이기는 것은 0건**이다. 35건은 **캡 밖에만** 있었고 5건은 아예 없었다. **이름표는 «아직 안 쓴 축» 이 아니라 여전히 «할 일이 거의 없는 축» 이다** — 선택기가 보는 범위 안에서는 규칙을 바꿔도 문서 3~8건만 달라진다(`R1_p2순위` 1건 · `R2_p게이트` 3건 · `R3_헤딩배제` 8건). **진짜 축은 캡이다** — 아래 후보 「`MAX_PASSAGE_HTML` 35,000자가 실물 웹의 근거 문단을 망가뜨린다」. 「거래를 재려면 양쪽을 같은 자로 재라」를 인용해 놓고 **이득만 느슨한 자로 쟀다** — 자를 맞추는 것은 인용이 아니라 배선이다. 실측: `docs/e2e/realweb-corpus/result.md` 4절(정정 포함) **닫혔다 — 이름표는 이기지도 지지도 않았다 (2026-09-07 계획 74).** 규칙 넷을 블라인드로 재고 **하나도 안 골랐다.** 캡을 고치자 본문 `p` 가 81.2% 로 스스로 올라와 **이름표가 할 일이 남지 않았다.** 그래도 **지우지 않는다** — 유지비(스택·되감기·검사 3건)는 그대로지만 지금은 「값이 0 이라 지운다」가 아니라 「축이 바뀌어 안 쓴다」이고, 두 번 뒤집힌 자리라 세 번째 자를 들이대기 전까지 축을 없애지 않는다.
- [7] **이 저장소의 변이 실측이 낡은 바이트코드 위에서 돈다 — `PYTHONDONTWRITEBYTECODE=1` 만으로는 안 막힌다** (2026-09-05 계획 57 리뷰 medium · e2e phase 가 처방을 실제로 먹여 확인). macOS 시스템 파이썬은 `sys.pycache_prefix = ~/Library/Caches/com.apple.python` 를 써서 캐시를 **저장소 밖**에 쌓는다 — 저장소 안 `__pycache__` 를 지워도 남고, `PYTHONDONTWRITEBYTECODE=1` 은 **쓰기만** 막지 이미 쌓인 것을 **읽는 것은 안 막는다**. 크기가 같은 편집(`0.95`→`1.50`)을 같은 초에 원복하면 mtime(초)+size 검사를 통과해 **원복한 뒤에도 변이 결과가 다시 나온다** — 반복 337 리뷰가 실제로 한 번 속았다(M3 이 원복된 트리에서 525.0 을 냈다). 방향이 나쁜 쪽이다: **없는 결함을 있다고 읽거나, 죽은 변이를 살아 있다고 읽는다.** **처방은 이미 들어갔다** — `docs/project.md ## 명령` 의 「변이 검사」 줄에 **변이 실행마다 `PYTHONPYCACHEPREFIX=$(mktemp -d)` 를 함께 준다**를 못박았고, 계획 57 e2e phase 가 변이 넷(M1~M4)을 그 방식으로 다시 재서 **넷 다 오늘도 죽는 것**을 확인했다(`docs/e2e/passage-cost-axis/result.md` 4절). **8점이 아닌 이유**: 오늘 남은 것은 «지나간 기록의 신뢰도» 뿐이고, 그 기록이 가리키는 단언은 대개 다음 phase 가 다시 쟀다. **여는 조건은 옛 변이 결론 하나에 실제로 기대는 계획이 열릴 때**이고, 그날 답은 전수 재측정이 아니라 **그 한 건만 새 캐시 접두어로 다시 재는 것**이다

- [6] **`domain_key` 의 userinfo 접기가 크롤 경로에서 도달 불가가 됐다** (2026-09-09 계획 83). `normalize` 가 자격증명 URL 을 입구에서 거절하므로 프런티어·저장·색인에 오는 URL 에는 `@` 가 없다 — `domain_key` 의 `rpartition("@")` 는 크롤 경로에서 **영영 안 밟힌다**. 다만 `domain_key` 는 크롤 밖에서도 불리고(시드 스킵 메시지가 **호스트만 적는** 자리가 바로 그것이다), 그 자리는 **`normalize` 를 지나기 전**이라 접기가 살아 있어야 한다. **죽은 코드가 아니라 「호출자 둘 중 하나에서만 산다」** 는 뜻이라 지우는 판단은 호출자를 세는 것이 먼저다. **여는 조건**: `domain_key` 호출부 전수를 세어 `normalize` 앞을 지나는 것이 0 이 되는 날.

## 다음 계획 후보 (테스트 phase 갭, 8점 미만)

- [5] **`const_gap` 이 무는 문서는 `project.md` 하나뿐이고, 같은 사고가 `README.md` 에서 이미 한 번 났다** (2026-09-07 계획 75 테스트 phase). 계획 75 는 「`project.md` 가 인용한 상수 = 코드 값」을 못 박고 **인용 0건이면 조용한 초록**이던 구멍까지 `CONST_CITATION_FLOOR` 로 닫았다(변이 M5 가 물고, 못을 0 으로 내린 M6 는 같은 문서 상태에서 초록이라 등가 변이가 아니다). **못 잰 것이 둘이다** — ① `README.md` 의 성능 표(digest `[6]` 「문서를 입력으로 읽는 검사」가 지목해 둔 바로 그 자리)와 `digest.md` 의 숫자 인용은 여전히 아무도 안 읽는다. README 는 **단위 건수**만 재고 있고 그 가드는 이 계획에서만 세 번 울었다(655→662→663→664) — 값을 내는 자리인데 덮는 축이 하나뿐이다. ② **파생 숫자**(최악 120ms · 계수 4.0 · p95)는 코드에 없어 기계로 못 잰다. 이것은 설계된 천장이고 `project.md` 에 적어 뒀다 — 상수가 닻이고 닻이 움직이면 사람이 문단을 다시 읽는다. **여는 조건**: ①은 `README.md` 의 숫자가 실제로 낡은 것을 한 번 더 목격하는 날이다(오늘 표는 안 쟀다 — 「기록된 답을 실행 전에 다시 재라」). ②는 재려면 측정을 테스트에 넣는 것이라 흔들리는 검사가 되고, 그 거래는 지금 손해다 ③ **코드 주석의 실측 숫자는 여전히 아무도 안 읽는다 — e2e phase 가 실물에서 둘을 쟀다**: `src/websearch/indexer.py:80` 의 무릎 표가 3,000 행 p95 를 **137ms** 로 적었는데 계획 74 실측은 **138.10ms** 이고(`docs/e2e/passage-prefer-body/result.md:56` · `project.md` 는 138 로 맞다), `tests/test_indexer.py:845` 주석은 오늘 캡(3,000)의 품질을 **근거 73→98건 · 본문 41.1→80.6%** 로 적는데 그것은 무릎 표의 **8,000 행**이다(3,000 행은 96건·81.2%). **계획 75 의 「건드릴 파일」 밖이라 안 고쳤다**(`rules/e2e.md` 3절). 오늘 세운 자의 천장을 실물이 그대로 보여 준 자리다 — 문서가 코드를 인용하는 축은 닫혔고 **코드가 자기 실측을 인용하는 축은 열려 있다**. ~~**여는 조건**: `indexer.py` 의 캡 문단이나 `test_indexer` 값 가드를 다시 손대는 계획~~ — **③ 은 닫혔다 (2026-09-08 야간 450 `comment-cite-drift`, 짧은 경로).** 여는 조건을 안 기다렸다: 미룬 것은 「고칠까」가 아니라 「자를 세울까」였고, 이 항목이 적어 둔 «둘뿐이면 자보다 고치는 게 싸다» 의 **「둘뿐」을 오늘 실제로 셌다** — 캡 문단의 나머지 인용 전부(2,000·4,000·8,000 행 · 옛 바이트 73건/41.1%/43ms · 1.13MB 0.70ms/66.8ms · 계수 3.952·3.746·2.885·2.773 · 120ms · 92%(370/400) · 중앙값 3,225)를 `result.md` 와 대조하니 **어긋난 것은 등재된 둘뿐**이라 자는 안 세우고 숫자 셋만 고쳤다(137→138ms · 98건→96건 · 80.6→81.2%). **고친 값보다 센 이유가 둘째 자리에 있었다**: `test_indexer.py:845` 는 「3,000 과 8,000 의 품질이 같다」로 읽히는데 그 두 줄 아래가 상수를 8,000×10 으로 동결하는 단언이다 — 상수를 올릴지 판단하는 사람이 읽는 순서상 먼저 만나는 문장이 거짓이었다. **①·② 는 그대로 열려 있다** (①은 오늘 실측으로 여는 조건 미충족 재확인 — `test_readme.py` 가 단위 건수·e2e 종수·합격선 아홉 행을 이미 재고 낡은 자리 0건)
- [6] **`index.md` 표의 «다섯째 칸이 e2e» 라는 가정을 헤더로 확인하는 자가 0개다** (2026-09-07 계획 71 리뷰 실측 · 백지 패스가 주웠다). `verdict_gap` 은 행 머리 수와 파싱된 행 수를 대조해 **열이 줄어든** 쪽만 신고한다 — e2e **앞에** 열을 끼우면 개수가 맞아 통과하고 새 칸을 판정으로 읽어 **틀린 RED** 를 낸다(실측: `| 계획 | 상태 | 브랜치 | 스텝 | 날짜 | e2e | 비고 |` 로 늘리자 날짜 칸을 물었다). 조용한 초록이 아니라 시끄러운 오귀속이라 8점 미만이다. **처방**은 헤더 행의 다섯째 이름이 `e2e` 인지 보는 단언 한 줄이고, **여는 조건**은 표에 열을 실제로 더하는 날이다 — 그 전에 넣으면 오늘 없는 사고를 위해 자를 하나 더 세우는 것이다(ponytail 1번).
- [5] **범위 인용 넷은 뒤로 밀려도 산다 — 계획 68 이 세운 앵커 자가 «범위가 자기 밀림을 삼키는 것» 은 안 문다** (2026-09-06 계획 68 테스트 phase 정적 실측 · 변이 0판 · 전수 628건). `_anchors`·`_has_number` 와 세 단언의 판정식을 그대로 돌려 인용 **18자리 × 밀림 넷(`±1`·`±2`) = 72조합**을 계산했더니 **68이 죽고 4가 살았다**: `tests/test_serve.py:914 -2` · `e2e/design_check.py:2 -2` · `e2e/design_check.py:444 -2` · `e2e/quality_eval.py:3 -1`. **넷이 곧 이 저장소의 범위 인용 전부다**(`concept.md:50-54` 셋 · `22-23` 하나) — **단일행 인용 열넷은 0/56 으로 하나도 안 산다.** 원인은 조각 길이가 아니라 범위다: `50-54` 를 `-2` 밀면 `48-52` 라 문구가 있는 50행이 아직 범위 안이고, `22-23` 을 `-1` 밀면 `21-22` 라 22행이 남는다 — `test_spec_quotes_match_cited_lines` 의 `cited` 가 범위 전체를 이어 붙이기 때문이다. **계획 68 의 7절이 걱정한 「`"50KB"` 같은 짧은 조각이 이웃 줄에도 있다」는 72조합 어디서도 안 일어났다** — 걱정한 축은 무죄고 안 본 축이 열려 있었다. **처방 후보**: 문구 축을 범위 전체가 아니라 **시작행**에서 찾게 한다 — 오늘 넷의 앵커는 모두 범위의 첫 줄에 있다(다만 여러 줄에 걸친 문구를 옮겨 적는 날 그 자가 거짓 RED 를 낸다는 것을 그때 함께 재야 한다). **8점이 아닌 이유**: 밀림 72 중 4 이고 그 넷도 **뒤로** 미는 밀림에서만 산다 — 사양에 줄이 끼는 흔한 방향(`+1`)은 18자리 전부가 문다. **여는 조건**: 사양 인용이나 `SpecCitationTest` 를 다시 손대는 계획.
- [4] **`_open` 이름표 스택을 붙드는 단언이 축마다 한 칸뿐인데, 그 이름표를 읽는 제품이 0곳이다** (2026-09-05 계획 59 테스트 phase 실측 · 메모리 변이 6종 × 전수 605건). `handle_endtag` 의 「`_open` 을 **뒤에서부터** 찾아 자른다」를 앞에서부터로 뒤집으면 죽는 것이 `test_the_label_survives_a_title_and_a_closing_tag` **1건**, `handle_starttag` 의 암묵적 닫기에서 `_open` 걷기를 빼면 죽는 것이 `test_an_implied_end_also_sweeps_the_label_stack` **1건**이다 — 계획 59 가 오늘 닫은 `_els` 쪽(`del self._els[i:]`)과 정확히 같은 「한 칸」 모양이다. **8점이 아닌 이유는 값이 밖으로 안 새기 때문이다** — `extract_blocks()` 가 내는 `(태그, 본문)` 의 **태그를 읽는 곳이 저장소에 0곳**이고(제품 `src/websearch/indexer.py:334` 과 `e2e/passage_eval.py:93` 둘 다 `_tag` 로 버린다 · 읽는 것은 `tests/test_extract.py` 뿐), `src/websearch/extract.py:264` 주석도 「이름표는 제품이 아직 안 읽지만 비용은 실물이다」로 그렇게 적어 뒀다. 이름표가 틀려도 색인·검색·문단 어디에도 안 보이므로 이 두 변이는 사용자에게 보이는 오류를 못 낸다. **여는 조건**은 「`extract_blocks()` 의 태그를 처음 읽는 소비자가 생길 때」이고, 그때 먼저 볼 것은 단언을 더하는 쪽이 아니라 **`_open` 을 지울 수 있는지**(사다리 1번)다 — 지우면 남는 것은 깊이² 비용을 막는 걷기 한 줄뿐이다. 함께 잰 나머지 넷은 여유가 충분하다: `if tag in self._open` 가드 삭제 **45건**(실패 6 + 에러 39 — 열린 적 없는 닫는 태그가 `ValueError` 로 터진다) · `_open` 을 꼭대기만 버리기 **2건** · `_open` 자르기 off-by-one **3건** · 암묵적 닫기 `while` → `if` **8건**
- [5] **단위 스위트가 `sys.path` 에 여섯 칸을 남기고 끝난다 — 지금은 가릴 이름이 0개라 잠복이다** (2026-09-05 계획 56 테스트 phase 실측 · 칸 수는 e2e phase 재측, 반복 331). 전수를 한 프로세스에서 돌리고 프로세스 전역 **24축**을 앞뒤로 대조하니 움직인 것은 셋뿐이었고, 저장소가 낸 진짜 누출은 **`sys.path` +4** 하나다: `e2e` ×3(`tests/test_design_check.py:25`·`tests/test_quality_eval.py:17` 은 임포트 시점, `tests/test_passage_eval.py:184` 는 테스트 실행 중) · `tests` ×1(**`TestLoader.discover` 자신** — stdlib `loader.py:285` 가 top_level_dir 을 넣고 안 뺀다). 나머지 둘은 무해로 판정했다(`logging.Logger.manager.loggerDict` 0→3 은 `import concurrent.futures` 부산물이고 **root 로거의 level·handlers 는 무변** · `tempfile.tempdir` 은 `gettempdir()` 의 stdlib 메모이제이션). **누출이 실재하는 것은 변이로 증명했다** — 저장소 밖 사본에 `e2e/tempfile.py` 를 심어 표준 `tempfile` 을 가리니 전수가 `Ran 605 · FAILED(failures=22) · rc 1` 이고, **그러나 리뷰가 변이 E 를 다시 재니 그것은 누출을 잰 적이 없다(2026-09-05 반복 330)** — 실패는 자식 9건(`test_passage_eval`, `<string>` 프레임)과 **같은 프로세스 13건**(`test_quality_eval`)으로 갈리는데 둘 다 «남은 칸» 이 원인이 아니다. 자식엔 `tests/test_passage_eval.py:47` 이 `-c` 소스에 `sys.path.insert(0, E2E)` 를 **직접 써 넣어** 들어간다(`sys.path` 는 자식에게 상속되지 않는다 — 마커 실측 `CHILD_HAS_MARKER=False`. 「누출이 프로세스 경계를 넘는다」는 틀린 문장이었다). 나머지는 `test_quality_eval.py:17` 의 **살아 있는** insert 다. **그래서 위험한 것은 «끝나고 남는 네 칸» 이 아니라, 두 insert 가 임포트 시점(=`discover()` 중, 첫 테스트 전)에 돌아 `e2e/` 가 전수 내내 `sys.path[0]` 에 앉아 있는 것이다.** **8점이 아닌 이유 셋**: ① `tests/`+`e2e/` 모듈 이름 **38개**를 표준 라이브러리와 대조해 충돌 **0건**(38개가 서로도 안 겹친다) ② **터질 때 시끄럽게 터진다** — 거짓 초록이 아니라 즉시 22 FAILED 이고, 이 저장소가 8점을 매겨 온 것은 언제나 「조용히 초록인 것」이었다 ③ `TestLoader.discover` 자신이 「`sys.path` 에 남기지 마라」를 위반하고(stdlib `loader.py:285`) 그 호출자가 계획 56 이 고친 바로 그 줄이다 — **다만 리뷰는 이것을 「검사가 불가능하다」로 읽지 말라고 적는다**(앞뒤 대조가 저장소 몫 `e2e` ×3 과 stdlib 몫 `tests` ×1 을 경로로 가른다 — 테스트 phase 탐침이 이미 그 다섯 줄이었다). **더 싼 처방이 따로 있다**: 세 insert 자리에 `if E2E not in sys.path` 한 줄씩. 지금 `e2e` 칸이 **셋으로 중복**되는 것이 그 가드가 없다는 증거다. **여는 조건은 `tests/` 나 `e2e/` 에 표준 라이브러리와 같은 이름의 파일이 처음 생기는 날**이고, 그날 답은 검사를 세우는 것이 아니라 **이름 충돌 목록 하나를 세는 것**이다(38개 × stdlib 대조는 다섯 줄이다 — 오늘 탐침이 그 다섯 줄이었다) **e2e phase 가 그 「더 싼 처방」을 실제로 만들어 먹여 보고 «안 한다» 로 닫았다 (2026-09-05 반복 331 · 5점 유지).** 저장소 밖 사본 둘을 뜨고 한쪽에만 세 줄(`tests/test_design_check.py`·`test_quality_eval.py`·`test_passage_eval.py` 의 insert 를 `if E2E not in sys.path` 로 감싼다)을 심은 뒤 양쪽에 같은 변이(`e2e/tempfile.py`)를 심어 전수를 돌렸다 — **가드 없음 `FAILED(failures=23)` rc 1 · 가드 있음 `FAILED(failures=23)` rc 1 로 실패가 파일·줄까지 한 건도 안 달랐다.** 가드는 **중복 칸만** 막지 첫 insert 를 안 막아서, 이 항목이 위험이라고 지목한 「`e2e/` 가 전수 내내 `sys.path[0]` 에 앉아 있다」를 **한 칸도 안 건드린다** — 처방이 아니라 정리다. 계획 56 의 5절이 「다른 테스트의 전역 상태 감사는 넓히면 별건」으로 이 자리를 미리 잘라 뒀고, 값이 0 으로 측정됐으므로 그 판단을 유지한다. **같은 자리에서 칸 수도 틀린 것이 나왔다** — 공식 진입점(`unittest.main(module=None, argv=[…, 'discover', '-b', '-s', 'tests'])`)으로 앞뒤를 대조하면 **+4 가 아니라 +6** 이고 빠진 둘은 `src` 다(1칸 → 3칸: `e2e/quality_eval.py:28`·`e2e/passage_eval.py:48` 이 in-process 임포트될 때 각각 넣는다). 저장소 몫은 `e2e`×3 + `src`×2 = **다섯 칸**, stdlib 몫이 `tests`×1 이다. **여는 조건과 그날의 답은 그대로다**(이름 충돌 목록 하나). 가드 세 줄은 그날 함께 넣으면 되고, 그전에는 넣어도 재는 값이 안 움직인다
- [5] **「`html` 열 없음」 DB 의 화면(HTML) 사다리를 붙드는 단언이 없다 — 형제 클래스에는 있다** (2026-09-04 계획 54 테스트 phase 실측). `TestPassagesWithoutPagesTable` 은 `test_html_screen_still_answers_200` 으로 「화면 사다리를 안 넓힌 것이 계약」을 붙드는데, 열 축의 `TestPassagesWithoutHtmlColumn` 에는 그 줄이 없다. 오늘 실측은 **200 · 결과 보임**(제품 서버를 띄워 `/?q=김치` 를 때렸다)이라 계약은 참이다. **8점이 아닌 이유는 죽일 변이가 안 선다** — 화면은 `_page_hits` → `search()` 만 타고 `passages()` 를 아예 안 부르므로, 새 탐침이 화면을 깨려면 `search()`/`_connect()` 로 올라가야 하는데 그 변이는 같은 클래스의 `test_search_still_answers_200` 이 이미 죽인다(계획 53 이 테이블 축에서 그 줄을 세운 이유는 화면 사다리를 **넓히는** 반대 방향 변이였다). **여는 조건은 화면이 근거 문단을 그리기 시작하는 날**이고, 그날은 사다리가 실제로 넓어지므로 단언 하나가 아니라 클래스 한 벌이 필요하다
- [5] **`_IMPLIED_END` 를 무너뜨리는 변이 146건 중 101건이 아직 살아 있다 — 표는 «명세를 한 번 옮겨 적은 상수» 라 편집 위험만 남는다** (2026-09-04 계획 51 테스트 4 실측 · 메모리 변이 탐침, `test_extract`+`test_indexer`+`test_serve`). 8점 셋(`키 삭제 tfoot`·`뺄셈 제거 tfoot`·`키 삭제 dd`)은 이 반복이 닫았고 남은 것은 낱개 값 삭제다: `p` 값 **49개 중 44개** · `th`/`tr`/`td`/`colgroup`/`tfoot` 의 표 값 다수 · `rp` 키와 `rt`/`rp` 의 루비 값(4점 — 인라인이라 새어도 괄호 한두 글자). **안 여는 이유는 룰 5절이다** — 값 하나마다 단언을 붙이는 것은 학술적 완전성이고, 표를 테스트에 그대로 베끼는 거울 단언은 행동을 안 잰다(변이가 테스트 쪽도 같이 고치면 죽지 않는다). **여는 조건은 «`_IMPLIED_END` 를 손으로 다시 고치는 계획이 열릴 때»** 이고, 그때 답은 브라우저 오라클(HTML5 파서) 하나를 붙여 표 전체를 한 번에 대조하는 것이지 단언 125개가 아니다
- [4] **`_is_hidden` 이 브라우저와 갈리는 자리 셋 — 전부 `html.parser` 의 성질이다** (2026-09-03 계획 51 테스트 1 실측). ① **중복 `style` 속성**: `dict(attrs)` 가 **마지막**을 남기는데 브라우저는 **첫 번째**를 쓴다 — `style="display:none" style="display:block"` 은 브라우저에서 숨김인데 우리는 보임(미탐), 순서를 뒤집으면 오탐이다. ② **자기닫음 표기**: `<div style="display:none"/>` 는 `handle_startendtag` 가 즉시 닫아 뒤가 안 숨는데, HTML 에서 `/` 는 무시되므로 브라우저는 계속 열어 둔다(미탐). `<template/>` 도 같다. ③ **`font-size:00`**: 값이 0 인데 `_ZERO_FONT` 의 `(?![.0-9])` 가 뒤의 `0` 을 보고 안 문다(미탐). 셋 다 **깨졌거나 손으로 안 쓰는 HTML** 이고 방향이 대체로 «덜 숨긴다» 라 오늘 값이 낮다. ①만 오탐 방향이 있는데 그것도 중복 속성이 필요하다. 여는 조건은 **실물 크롤 코퍼스에서 이 셋이 실제로 보일 때**이고, 그날 답은 CSS 파서가 아니라 `HTMLParser.handle_starttag` 가 준 `attrs` **리스트를 그대로** 앞에서부터 읽는 것(①)과 void 가 아닌 태그의 자기닫음을 무시하는 것(②)이다 — `_is_hidden` 의 `ponytail:` 주석이 적은 천장(외부 CSS·클래스·`visibility`/`opacity`)과는 다른 축이다
- [5] **`passage_eval` 의 G7 가드는 닿을 수 없다** — `" ".join(wrap(b)) == b` 는 `split(". ")`/`join` 의 **대수적 항등식**이라 어떤 본문에도 참이다(2026-09-02 계획 48 테스트 phase 실측: 합성 12종 · 코퍼스 64/64 · 무작위 2만종 위반 **0건**). 러너 docstring 의 *"`wrap` 의 단언이 매 실행 확인한다"* 와 설계 갈림길 4 의 같은 문장은 **아무것도 확인하지 않는다** — 두 도구가 다른 코퍼스를 재는 것을 막는 것은 이 가드가 아니라 «색인 본문 64/64 바이트 동일» 실측 쪽이다. 값이 0이라 안 지웠고(가드는 공짜다) 판단 근거는 `tests/test_passage_eval.py` 의 항등식 테스트가 들고 있다. `wrap` 이 문장 부호를 더 다루게 되면 그 테스트가 먼저 빨개진다 — 그때 G7 이 살아난다. **`--corpus` 로 다른 코퍼스를 받는 손잡이가 있으므로 «사용자가 준 fixture» 에서는 여전히 못 미더운데, 거기서도 항등식이라 결론은 같다**
- [5] **G5(문단 0건)는 단독으로 재현하기 어렵다** — 매치는 되는데 문단이 하나도 안 나오는 fixture 를 만들려면 모든 문서가 «제목만 매치 / 2-gram 이 블록 경계를 넘어 매치» 여야 한다. G4(블록 1개)를 먼저 밟지 않고 그 상태만 만드는 코퍼스는 손으로 지어야 하고, 그러면 동결 fixture 를 비트는 원칙(`test_quality_eval.py` 머리말)을 깬다. 단위 `TestPassages` 가 같은 술어를 문서 단위로 이미 붙들고 있어 값이 낮다 (2026-09-02 계획 48 테스트 phase)
- [4] **4300자리가 넘는 숫자 인자는 Python 3.11+ 에서 `int()` 가 `ValueError`** (`int_max_str_digits`). 이제 파서가 한 자리라 `--max`·`--workers`·`--deadline`·`--port` 넷 다 해당된다. 이 저장소의 명령은 stock macOS **3.9.6** 이라 도달 불가고, 길이 상한을 넣으면 `--port 0008000` 같은 정상값을 대신 거절하게 되어 안 넣었다 (2026-08-29 number-flag, 24 의 판단 유지)
- [5] `<meta http-equiv="X-Robots-Tag" content="noindex">` 변형은 무시한다 (2026-08-25 noindex-respect 테스트 phase 탐침으로 확인). 표준은 HTTP 헤더이고 http-equiv 변형은 주요 검색엔진도 지원하지 않는다. X-Robots-Tag 헤더 계획(스키마 expand)을 열 때 함께 판단
- [4] 본문에 들어 있는 진짜 `<meta name="robots" content="noindex">` 도 페이지 전체를 색인에서 뺀다 — 사용자 콘텐츠(포럼 글 등)가 호스트 페이지를 통째로 빼는 오탐이 가능하다. head 로 제한하면 막히지만 깨진 HTML 에서 head 경계가 불확실해 지금은 안전한 쪽(색인 안 함)으로 둔다
- [7] **게이트가 열렸다 · 크기를 쟀다 — 2026-08-31 계획 37 탐색.** 이 항목이 미룬 이유 ("지금 화면이 없다")는 **013 `search-ui`·015 `pagination-ui` 로 사라졌는데 아무도 안 돌아왔다.** 실측(25문서·페이지당 10 · 요청 사이에 문서 하나 증분 색인): 사용자가 1페이지를 보고 2페이지로 넘어가면 **중복 1건(`/09`) · 누락 1건(`/19`)**. 실재한다. 다만 고치려면 `?page=N` 이라는 공개 API 를 커서로 바꿔야 하고, 그것은 015 가 "총 건수를 모르니 번호 목록도 안 그린다" 로 고른 이전/다음 화면과 정면으로 부딪힌다 — **루프가 열 계획이 아니라 사람이 정할 갈림길이다.** 컨셉 우선순위에서 이것은 정답 포함률(2순위)이 아니라 결과 일관성 이고 기능 추가는 5순위다. 원문: [7] bm25 동점 순서는 `ORDER BY bm25(docs), rowid` 로 고정했지만, **요청 사이에 색인이 바뀌면** OFFSET 페이지네이션은 여전히 항목을 밀거나 겹친다(indexer 는 증분 재실행 가능). 진짜 해법은 키셋 페이지네이션(bm25 점수 커서)인데 지금 화면이 없다 — `search-ui` 나 `recrawl` 에서 함께 판단. 참고로 타이브레이커를 `url` 로 두면 2만 문서에서 p50 이 13→27ms 로 두 배가 된다(실측). rowid 는 공짜다
- [6] `Frontier` 의 도메인 폐기(`MAX_DELAY` 초과)는 **되돌릴 수 없다** — 그 프로세스에서는 robots 가 바뀌어도 계속 제외다. robots 캐시 TTL 이 생기는 계획에서 함께 (2026-08-25 crawl-delay 테스트 phase)
- [5] `crawl.crawl()` 이 URL 팝마다 `robots.delay()` 를 부른다 — 캐시 조회라 싸지만 같은 값을 매번 다시 대입한다. 도메인 첫 팝에만 부르려면 "이 도메인 처음인가" 상태가 필요해 지금은 그대로 둔다
- [4] robots.txt 요청 자체는 도메인 간격 **밖**이다 — 어떤 도메인의 첫 URL 에서는 robots + 페이지가 연달아 나간다. 관례상 예외로 보지만 엄밀히는 연속 2요청이다 (`e2e/crawl_e2e.py`·`e2e/crawl_delay_e2e.py` 둘 다 robots 를 간격 계산에서 뺀다)
- [4] `MAX_DELAY = 30` 은 **요청당** 대기만 막는다. 30초 × 100페이지 = 50분이라 "무인 크롤이 밤을 통째로 쓴다"는 원래 걱정은 그대로다. 총 크롤 시간 예산(`--deadline`)이 진짜 답이다
- [8] **토크나이저가 못 잡는 세 가지가 검색 품질의 유일한 손실원이다** (2026-08-26 quality-eval 개발 4/4 실측). 미포함 5/40 이 전부 이것이고 랭킹 손실은 0건이다: 복합어 뒷부분(`김치찌개보관법` 을 `보관법` 으로 못 찾음) 2건 · 띄어쓰기 변형(`올레 길` ↔ `올레길`) 1건 · 영어 굴절(`loaves`←`loaf`, `tuple`←`tuples`) 2건. **컨셉이 이미 답을 적어놨다** — `concept.md:76` 이 금지한 것은 "자체 형태소 분석기 **개발**" 이고 같은 줄이 "기존 것을 쓰거나 **trigram 으로 버틴다**" 로 길을 열어뒀다(계획·설계가 이 줄을 "형태소 분석기 도입 금지" 로 넓게 인용한 것은 부정확하다 — 2026-08-26 리뷰 패스 B). 그래서 후보는 **FTS5 `trigram` 토크나이저 병행 색인**(접미·부분 매치가 공짜로 붙는다, 색인 크기 증가가 대가)과 **영어 굴절만 다루는 질의측 s-접미 처리**(`tuples` → `tuples* OR tuple*`, 몇 줄). 둘 다 랭킹을 바꾸므로 이 계획이 만든 기준선(ko 85 / en 90)을 **먼저 고정한 뒤에** 손대야 비교가 된다
- [6] **G2 가드는 "방해 문서가 존재한다" 만 보증한다 — "순위를 흔든다" 는 보증하지 못한다** (2026-08-26 절제 실측: 방해 문서를 다 빼도 포함률이 35/40 그대로고 G2 만 38건 발화). 지금 fixture 는 매치되면 1위·아니면 미검출인 **이진 상태**라 bm25 정렬 자체는 한 번도 시험되지 않았다. 랭킹을 재려면 정답과 방해 문서가 같은 질의어를 같은 강도로 갖는 fixture 가 따로 필요하다. 컨셉이 요구한 지표는 포함률 하나뿐이라 지금은 후보로만 남긴다
- [4] `quality_eval.report()` 의 `hits*100//total` 은 **내림**이다. 19/20 = 95% 처럼 딱 떨어지는 지금 fixture 에서는 차이가 없지만, 질의 수가 20 이 아니게 되는 날 경계에서 한 건을 손해 본다(예: 79.9% 도 79%). PER_LANG 가드가 20 을 강제하는 동안은 무해
- [4] `quality_eval.measure` 가 `limit=100` 으로 재서 출력의 `매치 N건` 은 **100 에서 잘린다** — 실제 매치가 300건이어도 100 으로 보인다. 지금 fixture 최대가 28건이라 안 걸리지만, 코퍼스를 키우면 G2 판정(≤10)은 멀쩡한데 진단 숫자만 조용히 거짓말한다 (2026-08-26 리뷰 [45])
- [4] `quality_eval.fixture_defects` 의 G3 는 **ko·en 개수만** 센다 — `lang` 이 `ja` 인 질의가 섞이면 가드를 통과하고, `report()` 의 출력 루프도 `LANG_NAMES` 만 돌아 그 질의는 **조용히 사라진다**(측정도 보고도 안 됨). 질의 셋이 손으로 쓰인 동안은 안 일어나지만, 언어를 늘리는 계획에서 제일 먼저 밟는다 (2026-08-26 quality-eval 테스트 phase)
- [5] **남은 상한 셋(`MAX_SNIPPET` 200 · `PAGE_SIZE` 10 · `VERSION` 1)은 값 변이를 아직 안 쟀다** (2026-09-02 계획 48 테스트 2). 이 반복이 상한 다섯 중 둘(`MAX_PASSAGE`·`REQUEST_TIMEOUT`)만 닫은 이유는 점수다: 그 둘은 각각 RAG 근거 문단이 잘리는 축과 신뢰 경계(스레드 점유 초)라 8점 이상인데, 남은 셋은 표시 손잡이(스니펫 길이·페이지 크기)이거나 계약 필드라 조용히 바뀌어도 손실이 «보기 나쁨» 에 그친다. 다만 **안 잰 것과 물려 있는 것은 다르다** — 이 반복이 배운 것이 정확히 그것이다(직전 반복은 형제 셋이 전부 눈멀었다고 적었지만 둘은 리터럴로 이미 물려 있었다). 상한을 하나라도 손대는 계획이 열리면 그때 V1~V7 과 같은 방식으로 잰다

- [4] **`focus_rule` 의 셀렉터 가드는 괄호 중첩을 한 겹까지만 읽는다** — `:not(:is(:where(:focus-visible)))` 처럼 두 겹부터는 `INDIRECT_RE` 가 못 지워 **거짓 초록**이다 (2026-09-02 계획 49 테스트 1). 한 겹(`:not(:is(…))`)은 이 반복이 닫았고 거기서 멈춘 이유는 점수다 — 두 겹 셀렉터는 이 저장소에 0곳이고 제품 CSS 에는 괄호가 아예 없다. 여는 조건은 «제품 CSS 에 괄호 있는 셀렉터가 처음 생길 때»이고, 그때 답은 정규식을 한 겹 더 늘리는 것이 아니라 **괄호 균형을 세는 것**(조건 6 이 중괄호에 쓰는 방식)이다. `e2e/design_check.py` 의 `ponytail:` 주석이 이 천장을 적고 있다

- [5] **안 닫힌 여는 괄호는 셀렉터를 한 덩어리로 만든다 — 반대쪽 반은 아직 열려 있다** — `_top_level` 은 안 열린 `)` 는 `max(0, depth-1)` 로 막지만(테스트 phase 가 그 가드에 행을 붙였다), 안 **닫힌** `(` 는 깊이가 1 에 남아 그 뒤의 결합자·쉼표를 전부 «괄호 안» 으로 본다. 실측: `a:focus-visible:not(.x .y` 가 **통과**한다(`.y` 에 그려도 초록). 8점이 아닌 이유는 그것이 **깨진 CSS** 라는 것이다 — 여는 괄호가 안 닫히면 브라우저도 규칙을 못 읽으므로 제품에 실재할 수 없고, 막으려면 「셀렉터 끝에서 depth != 0 이면 거절」 한 줄이 필요한데 그것은 **문법을 읽기 시작하는 쪽**이라 설계서 6절이 그은 선(«CSS 파서를 안 만든다») 위에 있다. 여는 조건은 «제품 CSS 에 괄호 있는 셀렉터가 처음 생길 때» 로 아래 [4] 두 겹 중첩과 같다 (2026-09-04 계획 52 테스트 1 실측)
- [5] **투명 의사클래스 **안**의 결합자는 아직 거짓 초록이다 — 계획 52 가 닫은 것은 괄호 **밖**이다** — `_top_level` 은 괄호를 세기만 하고 안 읽으므로 한 겹 안에서는 같은 고장이 그대로 산다. 실측 셋 다 **통과**: `:is(a:focus-visible > .hint)` · `:is(a:focus-visible .hint)` · `:where(a:focus-visible + .hint)` (2026-09-04 계획 52 리뷰 1 — 링 선언은 제품과 동일, 셋 다 `('--focus', '2px')`). 링이 `.hint` 에 그려지는데 초록이라 계획 44·49·52 가 겨눈 바로 그 자리이고, 다른 것은 **괄호 한 겹 뒤에 있다는 것뿐**이다. 8점이 아닌 이유는 값이다 — 제품 CSS 에 괄호가 0곳이라 오늘의 3.56:1 은 여전히 참이고, 닫으려면 `:is()`/`:where()` 의 인자를 다시 쉼표로 갈라 **재귀**해야 하는데 그것은 설계서 6절이 그은 「CSS 파서를 안 만든다」 밖이다. 여는 조건은 위 [4]·[5] 와 같은 «제품 CSS 에 괄호 있는 셀렉터가 처음 생길 때» 이고 그날 셋을 함께 연다. `e2e/design_check.py` 의 `_top_level` 에 `ponytail:` 천장으로 적었다
- [4] **문자열 안의 홀괄호가 안 무는 것은 순서에 기댄 성질이다** — `[data-x="("]` 처럼 따옴표 안에만 있는 괄호는 깊이를 어긋내지만, `focus_rule` 이 `COMMENT_OR_STRING_RE` 로 **문자열을 먼저 지운 뒤** 조건 5 를 보므로 `_top_level` 까지 오지 않는다(실측: `a:focus-visible[data-x="(a)"]` **통과** — 짝이 맞는 괄호라 어차피 안 문다). 지우기를 조건 5 **뒤로** 옮기는 리팩터링이 오면 조용히 거짓 초록이 되는데, 그것을 붙드는 단언은 지금 0개다. 값이 낮은 이유는 그 리팩터링이 가상이고 오늘 제품 CSS 에 따옴표 안 홀괄호가 0곳이라는 것이다 (2026-09-04 계획 52 테스트 1 실측)
- [7] **재파싱 몫의 예산 배정을 아무도 안 그었다 — 한도 500ms 는 «한 요청 전부» 의 예산이다** (2026-09-05 계획 57 테스트 phase). 사양 성능 5 는 `GET /passages` **p95 500ms 이하**이고 성능 4 는 그 안에 사는 JSON 검색에 **300ms** 를 준다(`docs/specs/concept.md:127-129`). 그런데 계획 57 설계가 단언 ① 의 한도를 `500/3` → `500` 으로 펴면서 **재파싱 한 단계가 요청 예산 100% 를 먹어도 초록**이 됐다 — 오늘 캡 최악은 이미 332.5ms(**예산의 67%**)라 검색 몫 300ms 와 더하면 632ms 로 사양 밖인데, 이 계산을 하는 자리가 저장소에 **0곳**이다. 옛 배정치 1/3(166.7ms)은 500−300=200ms 안에 있었으니 계수가 3배 틀렸어도 **선은 사양 쪽에 더 가까웠다**. 테스트 phase 는 «올리는 문만 닫는» ①-b 상수 동결로 급한 쪽을 막았고(변이 M4 캡 50,000 이 그전까지 전수 605 를 통과했다), **남은 것은 «재파싱에 얼마를 준다» 를 실측으로 긋는 일**이다 — 실물 코퍼스 p95 는 1.54ms 라 최악 모양 상한과 4자리가 벌어져 있고, `digest [9]`(「`/passages` 총 p95 의 대부분은 `/search` 몫이다」)가 그 배분을 재는 하네스를 이미 지목해 뒀다. **8점이 아닌 이유**: 실물에서 깨진 적이 없고(측정된 p95 가 예산의 0.3%) 오늘 값은 사양 안이며, 여는 조건은 «캡·`PASSAGE_LIMIT` 을 올리려는 판단이 실제로 생기는 날» 이다 — 그날 ①-b 가 사람을 이 자리로 데려온다.

- [6] **② 의 상한이 상수를 따라 뜨므로 캡을 **내리면** 회귀 감지력이 준다 — 사양은 안 깨진다** (2026-09-05 계획 58 테스트 phase 실측). 계획 58 이 ② 를 `500 / (캡kB × 건수)` 로 유도하게 만든 것은 **올리는** 방향(M2·M3)을 닫기 위해서였는데, 같은 유도가 **내리는** 방향으로는 상한을 올린다: 캡 20,000 이면 상한이 1.4286 → **2.5** 다. 파서를 2배 느리게 하는 변이(스핀 · 파일 무편집)를 심어 재니 **캡 35,000 은 빨강**(1.956 > 1.4286) **캡 20,000 은 초록**이고, ①(200ms ≤ 500)·①-b(200,000 ≤ 350,000)도 둘 다 초록이라 **아무도 안 잡는다.** **8점이 아닌 이유는 사양이 안 깨진다는 것**이다 — 그때도 최악은 20 × 1.956 × 10 = **391ms ≤ 500** 이라 계획의 목표(「② 초록 = 최악이 예산 안」)는 **어느 상수 조합에서도 참**이고, 잃는 것은 사양 적합이 아니라 **회귀 감지력**이다. 뿌리는 ① 이 「오늘 값」 기록을 자처하면서 **리터럴 `1.00` 이라 아무것도 안 재는 것**이다 — 오늘은 상한 1.4286 이 실측 0.95~1.00 에 가까워 ② 가 회귀 감지를 겸하고 있을 뿐이고, 그 겸업은 캡이 움직이면 끝난다. **여는 조건은 캡이나 `PASSAGE_LIMIT` 을 내리는 판단이 실제로 서는 날**(`status.md` 사람 결정 대기 5 의 «재파싱 몫» 과 같은 자리)이고, 그날 답은 ② 를 다시 리터럴로 되돌리는 것이 아니라 **① 의 리터럴을 실측으로 승격하거나 ② 의 상한에 «오늘 값 × 여유» 를 하한으로 함께 거는 것**(`min(예산유도, 1.2 × 오늘값)`)이다. 그전에는 재는 값이 안 움직인다
- [4] **① 은 ①-b 의 부분집합이라 단독으로는 빨개질 수 없다** (2026-09-05 계획 58 테스트 phase 실측). ①(`캡kB × 1.00 × 건수 ≤ 500`)이 무는 조건은 `캡 × 건수 > 500,000` 이고 ①-b(상수 동결)는 `> 350,000` 이라 **①-b 가 언제나 먼저 문다** — ① 이 혼자 잡는 케이스는 0건이다. 실측으로 갈랐다: 캡 50,000 변이는 **①-b 만 빨강**(① 은 정확히 `500 ≤ 500` 으로 통과 — 경계값이다) · M2(캡 10만)·M3(`PASSAGE_LIMIT` 100)은 ① 이 줄 순서상 먼저 빨강. **안 지우는 이유 둘**: ① 의 실패 메시지(`1000ms`)가 ①-b 의 `500000 not less than 350000` 보다 사람에게 읽히고, ① 은 「오늘 계수는 1.00 · 최악 350ms · 예산의 70%」라는 **세 기록 자리 중 하나**라 `docs/project.md`·`indexer.py` 와 짝을 이룬다(지우면 그 짝이 깨진다). **여는 조건은 ① 의 리터럴이 실측으로 승격되는 날**이고 — 위 [6] 이 여는 날과 같다 — 그날 ① 은 부분집합이기를 그만두고 회귀 감지 몫을 되찾는다
- [5] **예산 `500` 이 테스트 안에서 두 번 리터럴이고 `docs/specs/concept.md` 와 안 묶여 있다** (2026-09-05 계획 58 테스트 phase). `tests/test_indexer.py` 의 ①(`assertLessEqual(worst_ms, 500)`)과 ②(`budget_per_1k = 500 / …`)가 사양 성능 5(`docs/specs/concept.md:129` — 「근거 문단 p95 500ms 이하」)를 각각 **손으로 옮겨 적은 것**이고, 둘을 잇는 것도 사양에 잇는 것도 없다. 사양이 400 으로 조여지는 날 둘 중 하나만 고쳐도 스위트는 **조용히 초록**이고, 그것은 계획 58 이 닫으려던 「사양 밖인데 초록」과 정확히 같은 부류의 구멍이다. **8점이 아닌 이유**: 사양 숫자를 바꾸는 것은 드물고 **의도적인 사람의 행위**라 그날 사양 옆의 인용을 함께 본다는 기대가 합리적이고, 오늘 두 자리는 서로 일치한다(실측). 값이 낮은 자리에 검사를 세우면 `digest [5]`(`sys.path`)와 같은 «닿을 수 없는 가드» 가 하나 더 는다. **여는 조건은 사양 성능 4·5 의 숫자가 처음 움직이는 날**이고, 그날 답은 지역 상수 하나(`BUDGET_MS = 500`)로 테스트 안을 먼저 묶고 — 두 줄이다 — 사양-테스트 인용 가드는 `DocCitationTest` 가 **문서만** 훑는 오늘 구조를 넓히는 일이라 별건으로 판단한다
- [4] **`〃` 이어받기 가지는 변이로 안 눌린다 — 오늘 그 행의 측정기를 윗 행이 이미 이름으로 적어서다** (2026-09-06 계획 66 테스트 phase 갭 탐색. **정적 판독이다 — 변이로 안 쟀다**: 이번 반복의 변이 예산 8판을 완료 기준 다섯과 회귀 하나에 다 썼다). `test_every_band_row_names_an_existing_meter` 는 `METER_CELL` 이 안 걸리는 `〃` 행에서 `path` 를 **윗 행 값으로 이어받는다**(건너뛰면 그 행이 영영 안 재지는 눈먼 자리가 되니까). 그런데 README 의 유일한 `〃` 행(「글씨가 잘 보이나」)이 이어받는 `e2e/design_check.py` 는 **바로 윗 행(「화면이 가벼운가」)이 이미 백틱으로 적어** 같은 루프가 실재를 확인한다 — 이어받기를 `continue` 로 바꾸는 변이는 그래서 살아남는다. 오늘 잃는 것은 0이다. **8점이 아닌 이유**: 뚫려도 손실이 0이고, 이어받기가 **틀린 값을 이어받는** 경우(첫 행이 `〃`)는 `assertTrue(path)` 가 이미 막는다. **여는 조건**: `〃` 행이 둘 이상 이어지거나 `〃` 위의 행과 다른 측정기를 가리키게 표가 바뀌는 날, 그리고 그날 **먼저 이 변이 한 판을 실제로 돌려 본다**(지금 이 항목은 실측이 아니다)

- [5] **`indexer` 제거 질의의 `&#` 갈래에는 음성 대조가 없다** (2026-09-06 계획 69 테스트 갭 탐색). `extract` 쪽은 «문자참조만 있고 지시는 없는 문서» 를 `test_char_reference_without_a_directive_is_allowed` 로 붙들지만, `indexer` 쪽에는 «`&#` 이 든 기색인 문서가 후보로 뽑히고도 지워지지 **않는다**» 를 재는 자리가 없다. **오늘은 무해하다** — 넓힌 것은 후보뿐이고 최종 판정은 그 뒤의 `is_noindex()` 가 한다. 8점이 아닌 이유가 바로 그것이다. **여는 조건**은 후보 질의가 최종 판정 자리가 되는 날 (예: 성능 때문에 `LIKE` 결과를 그대로 지우는 최적화) 이고, 그날은 이 대조가 유일한 안전망이다

- [4] **`is_noindex()` 가 문서를 통째로 판다 — `</head>` 까지만 먹이면 x893 이다** (2026-09-07 계획 69 리뷰 phase. **처방까지 실측했다**: 100KB 문서 12.19 → 0.014ms). 계획 69 가 사전 필터에 `&#` 갈래를 더하면서 **`robots` 없이 문자참조만 든 문서**가 파서까지 가게 됐고(`&#38;`·`&#8212;` 는 흔하다), 그 대가를 리뷰가 쟀다 — 단건 **x44.5**(≈ +90ms/MB) · 제거 루프 3000문서×30KB 에서 **x1.22**(`robots` 55%) ~ **x3.64**(10%). `<meta>` 는 `<head>` 에 사는 것이라 파서 입력을 `</head>` 에서 자르면 이 대가가 통째로 사라지고 **`&#` 갈래와 무관하게 기존 `robots` 후보 55% 의 파싱까지 같이 싸진다**. **8점이 아닌 이유**: 오늘의 x3.64 는 `indexer.py:172` 가 이미 적어 둔 「매 실행 전수 조인」천장의 **상수 배**이고 검색 경로는 0이며, 사양 우선순위가 크롤 윤리를 성능 위에 둔다(`docs/specs/concept.md:59`). **그리고 처방에 천장이 있다** — `<body>` 안의 `<meta name="robots">` 를 놓친다(실측 True → False). 그것은 **미탐**이라 우선순위 최상단 축을 깎는 거래고, 오늘 버는 시간으로 사기에는 비싸다. **여는 조건**은 제거 루프가 recrawl 계획에서 증분이 된 **뒤에도** 전수 파싱이 색인 반영 예산(사양 성능 3 — 10분)을 밟는 날이고, 그날 답은 자르기가 아니라 「`<body>` 를 만나면 파서를 멈춘다」(`_MetaRobotsParser.handle_starttag` 한 줄)일 수 있다 — 그러면 천장 없이 같은 값이다

- ~~[5] **하한 못 `STRIKE_POINTER_FLOOR` 는 값 축에 안 붙어 있다 — 포인터가 문구 변경으로 하나 빠지는 것은 여전히 조용하다**~~ — **닫혔다 — 계획 72 `digest-rotate-guard`, 2026-09-07**(하한을 포인터 수에서 **후보 절 머리 수**로 옮겼다: `CANDIDATE_HEAD_FLOOR = 2`. **취소선의 뜻은 「이 표제의 상수가 사라졌다」이지 「문구 드리프트가 닫혔다」가 아니다** — 살아남은 축은 아래 후보 `[6]` 이 오늘의 말로 들고 있다) (2026-09-07 계획 70 리뷰 phase. **처방 둘을 실측했다**). 계획 70 이 세운 자의 입구는 「계획 NN `slug` … 로 열었다」 정규식이고, 다음 반복이 「… 로 **연다**」·「… 에서 **열림**」처럼 어미만 달리 적으면 그 줄은 영원히 안 잡힌다. 하한은 추출기가 **0** 으로 죽는 것만 막는다 — 오늘 실물 포인터는 **10**인데 하한은 **9** 라 **하나가 빠져도 초록**이다. **처방 A(싼 것) — 백틱 슬러그를 통째로 세어 엄격 포인터와 대조**: 오늘 실물에서 엄격 10 대 느슨 **31**, 차이 **21** 이 전부 오탐이다(「닫혔다 — 계획 55 `db-state-invariant`」 꼬리와 서술 안의 계획 인용이 다 물린다). **그대로는 못 쓴다.** **처방 B — 하한을 실측치에 붙인다(9→10)**: 문구가 하나 달라지는 날 바로 RED 다. 대신 후보 절을 **정리해서 줄이는** 날 거짓 RED 이고, 오늘은 append 규약이라 안 줄지만 회전 정책이 후보 절에 닿으면 갈린다. **8점이 아닌 이유**: 오늘 값이 틀린 것이 아니라 앞으로 문구를 달리 적는 날 조용해지는 잠복이고, 계획 70 이 7절에 천장으로 적고 나간 자리다(같은 반복의 갭 탐색이 잡은 **범위를 넓히는** 쪽 둘은 그 자리에서 닫혔다 — 여기 남은 것은 **좁아지는** 쪽이다). **여는 조건**: 후보 포인터 문구를 새로 쓰는 계획, 또는 `digest` 후보 절에 회전이 닿는 날. 그날 답은 B 이고 A 는 아니다 — **→ 2026-09-07 계획 72 `digest-rotate-guard` 로 열었다**. **B 는 그날 반증됐다** — 포인터 10개가 10개 다 취소선이라 회전이 모집단을 0 으로 만들고, 「실측치」가 0 이면 하한 0 이라 못의 목적 자체가 사라진다. 답은 **모집단을 옮기는 것**이었다(`CANDIDATE_HEAD_FLOOR = 2`)
- [4] **하한 못의 «값» 변이는 아무도 안 문다 — `CANDIDATE_HEAD_FLOOR` 를 `0` 으로 내리면 못이 이빨을 잃고 전수가 조용하다** (2026-09-07 계획 72 개발 1/2 실측, 다섯 판 중 ⑤). 절 머리를 드리프트시킨 실물 위에서 하한만 `2`→`0` 으로 바꾸니 **648건 전부 통과**했다. 형제 `VERDICT_ROW_FLOOR`(45)도 같은 성질을 갖는다 — 이 저장소의 하한 못 관용구 전체가 값 축에 무방비다. **처방이 안 보이는 것이 8점이 아닌 이유다**: 「하한이 0 이 아니다」를 재는 것은 못을 지키는 못이라 순환하고, 하한을 값에 붙이는 것은 계획 72 가 반증한 바로 그 처방(B)이다. **여는 조건**: 하한 못이 셋 이상으로 늘어 관용구 자체를 손보는 계획이 열리는 날, 또는 값 변이가 실제로 사고를 낸 날.
- ~~[7] **`rules/docs.md` 3절의 문서 상한을 재는 자가 0개다**~~ **둘 다 닫혔다 (2026-09-07 · history 축은 계획 76, digest 축은 여기서).** 계획 76 이 「사람이 정할 규약」으로 남긴 질문 — **무인이 `digest.md` 를 회전해도 되는가** — 에 답한다: **아니다.** 그래서 **digest 축에는 못을 세우지 않는다.** ① 처방이 삭제라 못을 박으면 야간이 스스로 못 푸는 RED 가 되고(계획 76 「하지 않을 것」), ② 삭제 권한을 야간에 주면 야간이 자기 판단으로 **판단 재료**를 지운다 — 원본이 git 에 남아도 다음 탐색은 그것을 안 읽으므로 실질은 되돌릴 수 없다. ③ 반대로 못이 없어서 치른 값은 초과 **16·15·13·1줄** 넷뿐이고 **네 번 다 사람이 알아챘다**(history 축은 83줄을 여섯 반복 동안 아무도 못 봤다 — 그래서 그쪽만 못을 세운 것이 옳았다). **대신 자라는 원인을 줄였다**: `## 완료` 의 회전 기록은 회전 한 번에 한 줄씩 늘어 상한을 스스로 밀어내는 트레드밀인데, **아카이브 명부가 파일 이름을 영구 보관**하고 어느 반복이 어디로 갔는지는 `index.md` 와 아카이브 파일 자체에 있다. 판단이 없는 회전 기록 셋(`history_070`·`071`·`072`)을 지워 **201 → 198줄**. 남긴 둘은 판단을 담은 것이다 (사용자가 삭제를 위임한 건 · 계획 72). (2026-09-07 계획 72 테스트 phase 실측). **오늘 사고가 그 부재의 실물 증거다** — digest 가 216줄까지 자라는 동안 전수 647건이 조용했고, 넘긴 것을 발견한 것은 사람의 눈이었다. 같은 반복에 잰 `history_current.md` 도 352줄로 **이미 넘어 있었다**(반복 419 가 회전했다). **8점이 아닌 이유**: 처방이 무인 루프를 교착시킨다 — 상한 초과를 RED 로 만들면 야간은 그 RED 를 스스로 못 푼다. digest 회전은 **삭제**라 오늘도 사용자 위임이 필요했고(위임은 이 건 한 번), history 회전은 아카이브로 **밀어내는** 것이라 야간이 해도 되는 일이다. 둘의 성질이 달라 못 하나로 덮이지 않는다. **여는 조건**: 「무인이 digest 를 회전해도 되는가」가 규약으로 정해지는 날, 또는 상한 초과가 두 번째로 사고를 내는 날. 그날 답은 두 상한을 한 못으로 묶는 것이 아니라 **history 쪽만 먼저 못 박는 것**일 수 있다 — 그쪽은 처방이 야간에 실행 가능하다
- [4] **`candidate_heads` 는 접두 일치라 「후보」로 시작하는 절이 하나 더 생기면 드리프트를 가린다** (2026-09-07 계획 72 테스트 phase 정적 실측). `startswith(CANDIDATE_HEAD)` 를 접두로 짠 것은 둘째 절 이름이 `## 다음 계획 후보 (테스트 phase 갭 …)` 이라 어쩔 수 없었고, 그 대가로 `## 다음 계획 후보를 고르는 법` 같은 절이 생기면 머리 수가 2 를 유지해 **진짜 머리 하나가 갈려도 조용하다**. **8점이 아닌 이유**: 오늘 실물에 그런 절이 없고, 정확 일치 둘로 바꾸면 후보 절 이름을 손보는 날마다 상수를 따라 고쳐야 한다 — 계획 72 가 피한 「값 축에 붙이기」의 사촌이다. **여는 조건**: 후보 절이 셋 이상으로 늘거나, 절 이름에 접미가 붙는 편집이 실제로 들어오는 날
- ~~[6] **실물 후보 절의 «포인터 모집단»을 재는 자가 0개가 됐다 — 문구가 드리프트해 포인터가 0 이 되어도 전수가 조용하다**~~ (2026-09-07 계획 72 리뷰 phase · 백지 패스가 주웠다. 별도 세션이 `candidate_pointers` 를 `return []` 로 죽여 `StrikeSyncTest` 2건이 그대로 통과하는 것을 실측했다). 계획 72 가 하한을 포인터 수에서 절 머리 수로 옮기면서 **실물 문서와 `STRIKE_POINTER` 를 잇는 자가 사라졌다** — 코드 변이는 합성 `StrikeGapTest` 셋이 물지만(같은 반복 실측: 정규식을 `개봉했다` 로 바꾸면 3건 RED) **문서 쪽 드리프트는 아무도 안 문다**. 옛 항목 [5] 가 말하던 축과 같은 것이고, 그 항목의 표제는 이제 없는 상수(`STRIKE_POINTER_FLOOR`)를 가리키므로 **여기서 오늘의 말로 다시 연다** — 계획 72 가 닫은 것은 「하한이 회전에 밟힌다」이지 이 축이 아니다. **8점이 아닌 이유**: 회전 뒤 실물 포인터가 **1개**뿐이라 어떤 하한도 다음 회전에 다시 밟히고(그것이 처방 B 가 반증된 이유다), 남은 처방은 「후보 줄에 계획 번호를 기계가 읽는 자리로 못 박는」 규약 변경이라 digest 전체를 손대는 별건이다. **여는 조건**: 후보 줄 규약을 새로 쓰는 계획이 열리는 날, 또는 닫힌 후보가 취소선 없이 다음 탐색을 오염시키는 사고가 **두 번째로** 나는 날 **닫혔다 — 2026-09-09 야간 탐색이 처방을 실제로 먹여 보고 반증했다.** 세 번 쟀다: ① **코드 드리프트는 이미 물린다** — `candidate_pointers` 를 `return []` 로 죽이니 `tests.test_docs` 가 **3건 RED** 다(합성 `StrikeGapTest` 가 문다). 위 실측이 「`StrikeSyncTest` 2건이 그대로 통과」였던 것은 참이지만 **전수는 안 조용했다** — 「무는 자가 0개」가 아니라 「무는 자가 옆 클래스에 있다」였다. ② **문구 드리프트는 오늘 이미 나 있다** — 같은 절의 `[7] 화면에 안 보이는 텍스트` 줄이 「**계획 51 `hidden-passage` 가 앞 절반을 연다**」로 적혀 있어 `STRIKE_POINTER` (`로`·`으로`·`를` + `열었다`)에 안 물린다. ③ **그런데 그 자리를 물게 하면 거짓 RED 다** — 정규식을 `(?:가|이) … 연다` 까지 넓혀 실물에 먹이니 `strike_gap` 이 곧바로 「`plan_hidden-passage` 는 `완료` 인데 후보 줄에 취소선이 없다」로 울었는데, 그 항목은 **뒷부분(`<nav>` 인라인 연접·캡)이 아직 열린 다축 항목**이라 취소선이 없는 것이 맞다. 어휘 못(`닫혔다` ⇒ 취소선)도 같은 이유로 후보 65줄 중 히트 7건에서 **2건이 거짓 RED** 다. **근본 원인은 문구가 아니라 «취소선은 줄 단위인데 항목은 다축»이다** — 한 계획이 항목의 절반만 닫는 것이 이 파일의 정상 상태라, 포인터를 아무리 정확히 읽어도 줄 단위 판정으로는 못 간다. 기계로 가려면 항목을 축 단위로 쪼개는 **형식 변경**이 먼저고, 그것은 계획 76 이 「무인은 digest 를 회전하지 않는다」로 그은 선과 같은 사람 몫이다. **자리는 닫고 일반화만 남긴다: 판정의 낟알이 기록의 낟알보다 굵으면 정확한 추출기도 못 쓴다.**

## 판단 필요 (리뷰 보류 — 승인 필요 판정)
- [6] **`**kw` 로 인자를 삼키는 가짜는 있을 수 없는 협력자를 흉내낸다** (2026-08-27 반복 95). 시그니처가 바뀌었을 때 가짜 10곳에 `**kw` 를 붙여 통과시켰는데, 그 가짜들은 `before_send` 를 **받고 안 불렀다** — 진짜 fetcher 는 요청이 나가면 반드시 부른다. 그 위에서 잰 간격은 전부 크롤러가 "요청이 안 나갔다" 로 읽는 상태에서 나온 값이라 거짓이었다(훅을 안 부르게 되돌리면 지금은 8건이 깨진다). **시그니처 변경에 `**kw` 로 답하지 마라 — 새 인자의 계약까지 흉내내는 얇은 래퍼 하나가 답이다**(`tests/test_crawl.py` 의 `sending()`). 같은 부류: 모듈을 통째로 Mock 하면 `fetcher.RETRIES` 도 Mock 이 돼 `range(1 + retries)` 가 깨진다
- [8] indexer 증분(`url NOT IN docs`)이 **갱신을 반영하지 않는다.** 탐침 실측(2026-08-25 테스트 phase): 같은 url 을 재크롤해 pages.html 을 바꾼 뒤 index_pages → 0건, 옛 본문이 계속 검색되고 새 본문은 안 나옴. 컨셉 기능 5(재크롤 30일 갱신 반영)에 걸린다. 고치려면 docs 에 fetched_at 을 두고 비교·재삽입 = **스키마 변경이라 무인 모드가 보류**. 위 store.has 항목과 같은 recrawl 계획 소관 **2026-09-08 야간 재측정 — 「스키마 변경」 사유가 사라졌다.** 오늘 탐침으로 결함 자체는 그대로 재현된다(같은 url 의 `pages.html` 을 갈고 `index_pages()` 재실행 → 새 본문 **영영 0건**, 옛 본문 1건). 그런데 처방이 더는 열을 요구하지 않는다 — 계획 78 이 만든 `index_meta.recheck_watermark` 루프가 이미 **`p.fetched_at >= mark` 로 「다시 받아 온 문서」 집합을 읽고 있고**(`indexer.py:243-260`) 그 자리에서 `p.html` 까지 손에 든다. 갱신 반영은 그 루프가 noindex 면 지우는 대신 **재추출·재삽입**하면 되는 모양이라 `docs`·`pages` 스키마 무변경 · 마이그레이션 0줄이다. **남은 승인 사유는 정책이지 스키마가 아니다** — 아래 `[high]` 참조.
- [high] store.has 가 상태 불문 스킵: 같은 DB 로 재실행하면 시드부터 스킵돼 0으로 끝남. 재크롤 정책(성공만 스킵/TTL/링크 재추출)은 recrawl 계획 소관 — 앞당길지 판단 필요 **2026-09-08 야간 재측정.** 실물 `data/crawl.db` 400행이 `fetched_at` 을 **400/400** 갖고 (`min 2026-09-07 09:09:12` · `max 09:11:01` · 30일 초과 0건) `pages` 정의에 원래부터 있는 열이라, 30일 판정(`fetched_at < datetime('now','-30 days')`)에 **스키마 변경도 마이그레이션도 없다.** 그래서 `index.md` 의 「`recrawl` 은 루프가 열 수 없다 — 스키마 변경 + 마이그레이션」은 오늘 기준 사실이 아니다(그 줄도 함께 고쳤다). **남은 것은 사람이 정할 정책 셋이다**: ① 실패 상태(4xx·5xx·`status 0`)도 다시 받나 ② 404·410 의 「삭제 반영」이 `docs` 행만인가 `pages` 행까지인가(**후자는 데이터 삭제라 야간 금지**) ③ 재크롤이 링크를 다시 추출해 프런티어를 넓히나(크롤 예산·간격 축에 걸린다). 셋 다 사양이 숫자를 안 준 자리라 야간이 넘겨짚지 않는다. **2026-09-08 사용자가 자율 판단을 위임했고, 셋을 정했다 — 여는 조건이 열렸다.** ① **실패 상태는 다시 받는다.** 4xx·5xx·`status 0` 은 「없는 문서」가 아니라 「그때 못 받은 문서」이고, 안 받으면 일시 장애가 **영구 결손**이 된다. 다만 성공과 같은 주기로 두드리면 죽은 URL 을 30일마다 영원히 친다 — **재시도 간격은 성공의 절반(15일)로 시작**하고, 연속 실패 횟수로 늘리는 것(백오프)은 실측 근거가 설 때 연다. ② **삭제 반영은 `docs` 행만이다. `pages` 는 안 지운다.** 지우면 「404 를 받았다」는 사실 자체가 사라져 다음 크롤이 그 URL 을 새 것으로 다시 줍는다 — 삭제가 **망각**이 되어 루프가 돈다. `pages` 행은 `status` 와 `fetched_at` 을 든 **묘비**로 남기고 검색에서만 뺀다. 데이터 삭제가 아니므로 **야간 금지에도 안 걸린다** — 정책을 이렇게 고른 이유의 절반이 그것이다. ③ **재크롤은 링크를 다시 추출한다.** 안 하면 색인이 이미 아는 URL 안에서만 늙어 **프런티어가 자라지 않고**, 그러면 컨셉 기능 4(10만 문서)에 영영 못 간다. 새 링크는 기존 프런티어와 **같은 예산·같은 간격**을 쓴다 — 크롤 윤리가 갈림길 1순위라 재크롤이라고 예외를 주지 않는다.

- [7] **변이 검사는 "값을 지웠을 때"로 잡아야 한다 — "극단값을 넣었을 때"로 잡으면 거짓 초록이 난다** (2026-08-27 crawl-throughput 리뷰 패스 A). `sqlite3.connect(timeout=30)` 을 검증한다며 변이를 `timeout=0` 으로 줬는데, **진짜 되돌리기는 인자 삭제**이고 sqlite3 기본 `busy_timeout` 은 0이 아니라 **5000ms** 다. 0.3초만 락을 붙드는 테스트는 인자를 지워도 통과한다 — 변이는 실패했으니 "테스트가 잡는다" 고 기록했지만 실제로는 아무것도 고정하지 못했다. **변이를 고를 때 기준은 "이 줄을 안 썼다면 무엇이 되는가" 다.** 기본값이 있는 인자는 특히 위험하다(0 이 아니다). **두 번째 사례 — 이번엔 mock 이 눈이 멀었다** (2026-08-30 clock-injection 리뷰 패스 A). `sleep=time.sleep` 기본값을 `mock.patch("time.sleep")` 로 감시했는데, 패치는 `time` **모듈의 속성**을 바꾸고 기본값은 **def 시점에 진짜 함수 객체를 붙든다** — 그래서 주입이 **기본값으로 새는** 변이(`pool.submit` 에서 인자만 빼기)는 `call_count` 가 0 그대로라 **초록으로 통과하면서 진짜로 잤다**(그 클래스가 0.003초 대신 **4.02초**). 개발 phase 의 변이 3종이 전부 **줄을 고치는** 것이라 **인자를 빼는** 갈래가 통째로 비어 있었다. 교훈 둘: ① **기본값 인자를 감시할 때 mock 은 못 본다 — 벽시계나 부작용으로 재라**(`assertLess(elapsed, 0.5)` 한 줄이 잡았다) ② **변이 목록에 "인자를 뺀다" 를 항상 넣어라**, "줄을 고친다" 만으로는 기본값이 뒤에서 받아준다
- [4] **Ctrl-C 가 최대 30초 안 먹는다** (2026-08-27 동시화가 만든 회귀). `with ThreadPoolExecutor` 는 나갈 때 떠 있는 요청을 기다린다(`fetcher` 타임아웃 10초 × 재시도). `cancel_futures` 는 약이 안 된다 — 취소되는 건 대기 중 작업뿐인데 제출한 것이 곧 실행 중인 것이다. 유실은 없다(upsert 마다 커밋). 진짜 답은 워커가 주기적으로 보는 중단 플래그이거나, 애초에 `--deadline` 이다(위 [4] 총 크롤 시간 예산 항목과 같은 수술). **2026-08-28 계획 020 이 실측했다 — "최대 30초" 는 낡은 숫자다.** `Crawl-delay: 3` 사이트에 SIGINT 를 0.51초에 보내니 종료가 **6.07초**(대기 **5.56초**)였다. 재시도 앞 잠이 그대로 나오므로 상한(`MAX_DELAY=30`)에서는 **60초**가 맞고, 여기에 타임아웃이 더 얹혀 최악 90초다. ~~최악 90초~~ **정정 2026-08-30 계획 34 착수 탐침 — 최악은 70초이고, `Event.wait` 는 그중 40초만 지운다.** `Crawl-delay: 30` + 응답 없는 서버에 SIGINT 를 0.51초에 보내니 종료 **69.57초**, 서버 수신 시각은 `t=0.05 / 30.07 / 60.07`(발신 간격 **30.01초**)이다. ① 90 은 `10×3 + 30×2` 로 더한 값인데 `before_send` 의 `remaining = interval - (now() - sends[-1])` 이 **이미 흘러간 타임아웃 10초를 뺀다** — 발신 간격은 `interval` 자체다. ② 70초의 분해는 **소켓 읽기 30초(`fetcher.py:39` `urlopen(timeout=10)` × 3) + 재시도 앞 잠 40초(`crawl.py:74` × 2)** 이고, `Event.wait` 로는 소켓 30초에서 **1초도 못 준다**. 손이 가장 많이 가는 상황(느린 서버·재시도 없음)은 **7.56초가 전부 소켓 읽기**라 `Event.wait` 가 **0초** 줄인다. ③ **잠만 깨우면 예절이 깨진다** — `Crawl-delay: 30` 서버에 10초 간격 3발이 나간다. 그래서 "잠 깨우기"와 "재시도 취소"는 두 변경이 아니라 **하나**다. ④ **아무도 안 세던 네 번째 자리** — 플래그만 세우는 핸들러는 `crawl.py:184` `concurrent.futures.wait(timeout=20)` 을 **못 깨운다**(실측 20.01초 다 잔다. `time.sleep(5)` 도 5.01초. `Event.wait(20)` 만 0.51초). PEP 475 로 락 획득이 남은 시간으로 재시도되기 때문이고, 오늘 Ctrl-C 가 그나마 먹는 이유는 기본 핸들러가 **예외를 던져서**다 — 플래그로 갈아타는 순간 그 성질을 잃는다. 대조군: **SIGTERM 은 대기 0.00초**(기본 동작이 즉시 종료라 `timeout 3600 python3 -m websearch.crawl …` 가 오늘 당장 되는 하드 상한이다 — 다만 stock macOS 에 `timeout` 이 없고 rc 0 이 안 나온다). **020 은 이것을 안 닫는다** — `docs/design_deadline.md` 2·5절이 왜 미뤘는지를 실측과 함께 적었다
- [6] **중단 신호를 워커까지 넣으려면 `tests/test_crawl.py` 의 가짜 시계 10곳을 먼저 옮겨야 한다** (2026-08-28 설계 020 에서 잰 값. **수 정정 2026-08-29 계획 33: 9곳이 아니라 10곳이다** — 65·622·672·742·787·843·948·1023·1096·1325, 전부 `tests/test_crawl.py` 이고 `e2e/` 는 0곳). **2026-08-30 계획 33 `clock-injection` 개발 phase 가 이 선결 조건을 걷어냈다** — `crawl()`·`_fetch_one()` 이 `now` 와 같은 모양으로 `sleep=` 을 받고, `grep -c 'crawl\.time\.sleep' tests/test_crawl.py` 가 **0** 이다. 변이 3종(대기 블록 삭제 / `:179` 전역 되돌리기 / 하한 `max`→`min`)이 전부 죽는 것으로 이음매가 진짜 잡는다는 것까지 확인했다. 이제 중단 신호는 **주입 지점 한 곳만** 바꾸면 된다 — 아래 두 갈래 걱정도 같이 사라진다(테스트 경로와 제품 경로가 안 갈린다). **다만 "한 곳만" 은 이음매 이야기지 지연 이야기가 아니었다** — 계획 34 착수 탐침이 잠드는 자리를 **네 곳**(소켓 읽기 · `:74` 재시도 앞 잠 · `:179` 메인 루프 잠 · `:184` `futures.wait`)으로 셌고 `sleep=` 주입이 덮는 것은 그중 둘이다. 위 `[4]` 의 정정 참조. **닫혔다 — 계획 33 이 2026-08-30 e2e 까지 통과해 `DONE`**(`plan_history_020.md` ·
  e2e `docs/e2e/clock-injection/`). 겉보기 무변경을 `main`(`f888518`)과의 **벽시계 A/B**
  로 증명했다 — `Crawl-delay:2` 2.01s↔2.01s · 1초 하한 1.00s↔1.00s · 처리량 10.25↔10.33/s.
  **선결 조건이 없어졌으니 중단 신호는 이제 주입 지점 한 곳만 바꾸면 된다.** `before_send` 의 `time.sleep(remaining)` 을 `threading.Event.wait` 로 바꾸면 위 Ctrl-C 항목이 닫히는데, 그 자리는 **9곳이 `mock.patch("websearch.crawl.time.sleep")` + `side_effect` 로 가짜 시계를 흘려보내는 이음매**다 — 이 저장소가 간격을 초 단위로 단언할 수 있는 근거가 전부 거기다(`TestCooldownBurn`·`TestRetriesKeepTheInterval`·`TestDelaySurvivesWorkerException`…). **"`stop` 이 None 이면 `time.sleep`, 아니면 `Event.wait`" 두 갈래는 답이 아니다** — 9곳을 초록으로 만들지만 테스트가 도는 경로와 제품이 도는 경로가 갈린다(`[6]` `**kw` 가짜와 같은 실패). **자기 계획 크기다**: 시계 주입을 다시 설계하는 것이 본체이고 중단 신호는 그 위에 얹힌다
- [4] **`e2e/perf_crawl.py` 의 `TARGET_RATE=5.0` 은 순차 대 동시까지만 가른다** — 상한이 `workers/LATENCY` 라 workers 가 8→4 로 줄어도 10/s 로 통과한다. 실제로 가르는 건 `workers=1` 뿐이다. 값을 올려 조이는 대신 그대로 뒀다(그 숫자는 `concept.md:44` 의 제품 목표지 회귀 탐지기가 아니다). 부분 회귀는 시간이 아니라 `TestConcurrency`(배리어)가 잡는다
- [7] **`if __name__ == "__main__"` 가 파일 끝이 아니면 그 아래 테스트는 존재하지 않는다** (2026-08-27 반복 100 리뷰 패스 A). `tests/test_serve.py` 에 새 클래스 3개(15건)를 가드 **뒤에** 붙였다. `python3 tests/test_serve.py` 로 직접 돌리면 `unittest.main()` 이 그 자리에서 `SystemExit` 을 내고 아래 클래스는 정의조차 안 된다 — **55건 OK**, `discover` 로는 70건 OK. 양쪽 다 초록이라 사라졌다는 신호가 어디에도 안 뜬다. 실행 방법 하나에 검증 전부가 달려 있었다. 가드를 파일 끝으로 옮겨 닫았다. 다른 테스트 파일 3개는 원래 마지막 줄이다 — **파일에 뭘 덧붙일 때 가드가 중간에 있는지 먼저 본다**
- [4] **`has_next` 가 "결과가 더 있다" 와 "서버 상한에 닿았다" 를 한 bool 로 합친다** (2026-08-27 반복 100 리뷰 패스 B). `len(hits) > PAGE_SIZE and page < MAX_PAGE` — `page == MAX_PAGE` 인데 결과가 남았을 때 JSON 클라이언트는 `has_next: false` 를 "결과 끝" 으로 읽고 **잘렸다는 사실을 구분할 수 없다**. 다음을 내주면 따라간 사용자가 400 을 맞으므로 지금 동작이 틀린 것은 아니고, 이 판정은 계획 015 이전(009 `search-api`)부터 있던 **공개 계약**이다 — 야간에 안 건드린다. 고치려면 `truncated` 같은 두 번째 필드가 필요하고 그건 `/search` 응답 스키마 변경이다. 사용자 확인 후보
- [6] **다른 스킴의 큰 선언이 워커를 더 오래 붙든다** (2026-08-27 반복 104 테스트 phase). `retry-interval`(016) 이후 `_fetch_one` 의 바닥값이 netloc 단위라, `http://x` 가 `Crawl-delay: 30` 을 선언하면 `https://x` 의 재시도도 30초씩 잔다 — 요청 하나가 워커를 최대 `retries × 30` 붙든다(전에는 그 경로가 1초였다). **의도한 값이다**(컨셉 1순위가 크롤 윤리다) 그러나 `[4] Ctrl-C 가 최대 30초 안 먹는다` 의 최악값을 키우는 쪽이라 같이 봐야 한다. 진짜 답은 둘 다 `--deadline` 이거나 워커가 주기적으로 보는 중단 플래그다 — 재시도를 크롤 루프로 승격하면 한 번에 풀린다(digest `[high]` 닫힘 항목이 "옳지만 크다" 로 남긴 그 후보)
- [7] **열쇠를 안전하게 만들어도 협력자가 URL 을 다시 판다** (2026-08-27 반복 110 백지 리뷰). `domain_key` 에 ValueError 가드를 넣고 "이제 안 죽는다" 고 적었는데, `robots._base` 는 여전히 날 `urlsplit(url).scheme` 을 불렀고 `RobotFileParser.can_fetch` 는 **자기가 URL 을 다시 파싱**했으며 `links.extract` 의 `urljoin` 도 같은 값에 던졌다. 더 나쁜 것은 예외가 나는 자리가 아니라 **예외를 잡은 자리**였다 — `crawl._store_result` 의 `except` 가 복구하려고 `known_delay(url)` 을 다시 부르면 같은 URL 이 **두 번째로** 던지고 그건 아무도 안 잡아 크롤 전체가 끝난다(`http://[::1/x` 링크 하나면 된다). **가드는 파싱하는 함수마다가 아니라 파싱을 한 곳으로 모은 뒤 거기 하나만** — `urls._split` 이 그 자리다. 그리고 **복구 경로도 같은 입력을 받는다**: except 블록 안에서 실패한 그 값을 다시 쓰면 두 번째 예외는 잡히지 않는다
- [high] **정규화는 새 DB 에서만 목적을 달성한다 — 기존 `data/crawl.db` 에는 옛 열쇠 행이 남는다** (2026-08-27 반복 115 백지 리뷰, 재현 실측). `pages.url` 은 PRIMARY KEY 이고 018 이전에 수집한 행은 정규화 안 된 문자열을 열쇠로 갖는다. 재현: `s.upsert('http://A.com:80/p', ...)` 뒤 `s.has('http://a.com/p')` → **False**. 다시 받고 다시 저장해 `pages` 2행 · `docs` 2행이 되고 `search('body')` 가 둘 다 낸다. `indexer.index_pages` 의 증분 조건이 `url NOT IN (SELECT url FROM docs)` 라(`indexer.py:80`) 옛 행은 영구히 남는다. 옛 열쇠를 접는 일회성 통합은 **마이그레이션이자 데이터 형태 변경이라 야간 금지** — 사람 승인이 필요하다. digest `[8]`·`[high]`(recrawl) 와 같은 수술로 묶는 것이 자연스럽다. **계획 018 은 이 사실을 문서로만 닫았다**(`plan_url-normalize.md` 4절)
- [high] **URL 에 실린 자격증명이 `pages.url` PK 로 저장되고 검색 결과에 렌더된다** (2026-08-27 반복 115 백지 리뷰). `normalize` 는 `userinfo@` 를 일부러 되붙인다 — 떼면 요청 내용이 바뀌기 때문이고 그 판단 자체는 옳다. 그러나 그 URL 이 그대로 DB 열쇠가 되고 `serve.py:205,207` 이 검색 결과 링크로 내보낸다. `<a href="http://u:pw@evil.test/">` 하나면 남의 자격증명이 우리 DB 와 검색 화면에 들어온다. 보안 경계라 **줄 수와 무관하게 야간 자동 적용 금지**. 답은 저장 열쇠에서만 userinfo 를 떼거나(요청 URL 과 저장 URL 을 가른다) 렌더 시점에 가리는 것 — 어느 쪽이든 공개 동작 변경이라 승인이 필요하다 **계획 83 이 이것을 패치로 닫아 뒀다 (2026-09-09 야간)** — `docs/patches/userinfo-leak-refuse-credentials.patch`. 처방은 후보가 적어 둔 둘(렌더 가리기 / 저장 열쇠에서만 떼기)이 아니라 **셋째 안: 입구에서 거절**이다. 앞의 둘은 **요청이 나가는 것을 그대로 두는데**, 남의 자격증명으로 남의 서버에 인증 요청을 보내는 것은 `concept.md` 갈림길 1순위(크롤 윤리)라 저장·렌더보다 앞선다. **보안 건이라 적용은 사람이 한다** — 이 줄은 패치가 병합되는 날 지운다. 남은 자리: **옛 DB 행 정리**(실물 400행에 자격증명 0건이라 오늘은 대상이 없다) · `serve.py` 렌더 가리기(여는 조건: 자격증명 행이 1건이라도 세어지는 날).
- [4] **`links.extract` 의 `urljoin` 은 점 세그먼트를 RFC 보다 넓게 접는다 — 같은 문서가 상대/절대 href 에 따라 다른 열쇠를 갖는다** (2026-08-28 반복 121, 019 백지 리뷰 + 전수 대조 실측). CPython `urljoin` 은 `..` 가 루트에 닿을 때 루트 표식까지 pop 한다: `urljoin('http://h/base/', '/..//a')` → `http://h/a` 인데 `urls.normalize('http://h/..//a')` → `http://h//a` 다(RFC 5.2.4 는 뒤가 맞다). 세그먼트 `{a, b, ., .., ""}` 깊이 5 전수 공간 3905개 중 **231개가 갈린다** — 전부 "`..` 가 루트에 닿은 직후 빈 세그먼트" 한 종류다. 019 가 점 경로 전부 갈리던 것을 이 한 부류로 **줄였지만 안 닫았다**. 닫으려면 `links.extract` 가 `urljoin` 앞에 끼어들어야 하는데 `urljoin` 은 상대 참조 해석 자체를 하는 자라 대체가 크다. 실물에서 `/..//a` 형태의 href 를 본 적이 없어 비용 대비가 안 맞는다 — **알고 남긴다.** 019 의 `_fold_dots` 는 같은 전수 공간에서 RFC 레퍼런스와 **불일치 0** 이다
- [5] **`test_readme.py` 의 두 문서 가드가 남긴 오탐 방향 둘과, 필터 아래 항상 빨간 개수 단언** (2026-09-03 계획 50 테스트 갭 탐색, 8점 이상 0건이라 넘긴다). ① `RUNNER_LINE` 은 `python3 -m unittest discover` 가 든 **줄**을 전부 물어서, 문서가 «이렇게 하지 마라» 반례 줄을 적는 날 `-b` 가 없다고 RED 를 낸다. ② `BUFFERED` 는 `-b` 만 알아 동치인 장형 `--buffer` 도 RED 다. 둘 다 **거짓 RED 방향**(놓치는 쪽이 아니라 과하게 무는 쪽)이라 지금은 무해하고, 문서가 반례를 적고 싶어질 때가 고칠 때다. ③ `test_verification_counts_match_reality` 는 `-k` 아래에서 **항상 RED** 다 — `-k` 가 `defaultTestLoader.testNamePatterns` 를 세팅해 그 테스트 **안**의 `discover()` 까지 걸러 (5, 20) 이 나온다. 개수 단언은 전체 실행에서만 참이라는 뜻이고, 닫으려면 그 안에서 패턴을 잠시 비웠다 되돌리는 두 줄이면 된다 **③은 계획 56 `loader-isolation` 이 가져갔다(2026-09-05 · `docs/plan_loader-isolation.md`). 착수 때 다시 재니 처방이 여기 적힌 두 줄보다 작았다 — `unittest.TestLoader()` 새 인스턴스 하나면 전역을 아예 안 만진다(필터 걸린 상태에서 605 vs 5). 오늘 실측값은 (605, 21) ≠ (5, 21) 이고, 실패 메시지가 「실제는 (5, 21)」이라 README 를 틀리게 고치라고 지시하는 것이 새로 보였다 — 오탐이 아니라 함정이다. ①②는 그대로 열려 있다.**
- [4] **`-b` 는 통과한 테스트가 낸 경고도 삼킨다 — 초록 실행에서 `DeprecationWarning`·`ResourceWarning` 이 영영 안 보인다** (2026-09-03 계획 50 리뷰 1 실측). `warnings` 의 기본 `showwarning` 은 호출 시점에 `sys.stderr` 를 찾는데 `-b` 가 그 자리를 테스트별 버퍼로 바꾸고, 통과한 테스트의 버퍼는 **버려진다**. 독립 탐침으로 확정했다 — 경고를 내면서 통과하는 테스트 하나를 `TextTestRunner(warnings="always")` 에 `buffer=True`/`False` 로 각각 먹이니 **`-b` 아래 안 보임 · `-b` 없이 보임**이고, `-b` 를 먼저 돌려 `__warningregistry__` 중복 억제를 배제했다. **오늘의 손실은 0이다** — 맨몸·단독으로 `-b` 없이 전수를 돌려 579건 어디에도 경고가 **0줄**인 것을 확인했다(나온 약 90줄은 전부 테스트가 낸 CLI 메시지다). 그래서 계획 50 의 완료 기준 밖이고 반려 사유가 아니다. **여는 조건은 «스위트에 경고가 처음 하나 생길 때»**이고, 그때 답은 `-b` 를 빼는 것이 아니라 경고를 실패로 올리는 것이다(`-W error::ResourceWarning` 계열) — 그러면 «안 보인다» 가 «빨개진다» 로 바뀌어 `-b` 와 공존한다. 지금 이것을 붙들어 두는 검사는 0곳이다
- [8] **상위 계층이 하위 계층의 e2e 를 흡수하면 축을 옮겨야 한다 — 지우거나 낮추면 안 된다** (2026-08-27 반복 117, 018 이 017 의 e2e 를 깼다). `domain_key_e2e.py` 는 대소문자·기본 포트로 표기를 갈라 "같은 서버는 한 칸" 을 쟀는데, 018 의 `normalize` 가 **URL 이 태어나는 자리에서** 그 둘을 접어 표기가 서버에 하나만 도착했다 — 재려던 상황 자체가 안 만들어진다. **017 이 심어 둔 "표본이 사라지면 종료 코드 2" 가 제 일을 했다**: 조용한 통과가 아니라 `세 표기가 다 도착하지 않았다: ['a.test']` 로 크게 실패했다. 답은 파일을 지우거나 단언을 낮추는 것이 아니라 **상위가 안 접는 축을 찾는 것** — 여기서는 userinfo 였다(`normalize` 는 보존하고 `domain_key` 는 뗀다). 옮긴 뒤 **되돌리기 변이로 여전히 하위 회귀를 잡는지 확인한다**: 프런티어를 날 `netloc` 으로 되돌리자 `표기가 다르다고 2초 선언이 새어 나갔다 — 간격 ['0.001', '0.000']` 로 죽었다. 확인 없이 옮기면 초록불만 남는다
- [5] **신규 색인 루프는 `pages.status` 를 안 본다 — 오늘 도달 불가인 것이 값을 낮춘다** (2026-09-08 계획 80 테스트 갭 탐색). 워터마크 루프는 404·410 을 `docs` 에서 빼지만, 그 옆 신규 색인 질의는 `WHERE html IS NOT NULL AND url NOT IN (SELECT url FROM docs)` 뿐이라 **상태를 한 번도 안 묻는다**. 본문 있는 404(흔한 「없는 페이지」 안내 화면)가 들어오면 **오류 화면이 문서로 색인된다**. 오늘 안 터지는 이유는 하나뿐이다 — `fetcher` 가 `HTTPError` 를 `FetchResult(e.code, None, url)` 로 돌려 **non-2xx 는 본문이 언제나 `None`** 이고(`fetcher.py:52`), 그래서 저 질의의 `html IS NOT NULL` 이 우연히 상태 필터 노릇을 한다. **8점이 아닌 이유**: 도달 경로가 오늘 0개고, 여는 조건은 「`fetcher` 가 오류 응답의 본문을 저장하게 되는 날」이라 그 변경이 이 자리를 데려온다. 그날 신규 루프에도 `status` 갈래를 넣는다 — 워터마크 루프와 갈래가 갈린 채로 두면 **처음 크롤한 404 는 색인되고 재크롤한 404 는 지워지는** 비대칭이 된다.
- [7] **갱신 경로의 `DELETE FROM docs WHERE url = ?` 는 코퍼스 크기에 선형이다 — 계획 78 이 지운 고정비와 같은 모양이 작은 계수로 돌아왔다** (2026-09-08 반복 474 리뷰 F3, 실측). `docs` 는 FTS5 이고 `url` 이 `UNINDEXED` 라 URL 로 지우는 것이 **전수 스캔**이다. 직접 재서: 1천 문서 **0.09ms**/건 · 5천 0.44 · 2만 1.71 · 4만 **3.35ms** (완전 선형 · 10만 환산 ~8.4ms/건). 갱신은 문서마다 이 `DELETE` 를 한 번 치므로 값이 **재크롤 집합 × 코퍼스 크기**다 — 설계 계약 4 가 `mark` 게이트를 정당화할 때 센 것은 재추출 23.81ms 뿐이라 이 항이 계산에 없다. **고치는 법은 안다**: 워터마크 조인에 `d.rowid` 를 얹고 세 `DELETE` 를 `WHERE rowid = ?` 로 바꾸면 된다(별도 세션 실측 4만 문서에서 **0.028ms**, 1000배). **8점이 아닌 이유**: 실물 코퍼스가 400문서라 오늘 값이 0.036ms/건이고, 야간에 세 삭제 갈래의 열쇠를 한꺼번에 `url`→`rowid` 로 바꾸는 것은 **엉뚱한 문서를 지우는 방향의 실수**를 4순위 축(색인 규모)을 위해 감수하는 것이다. 여는 조건은 코퍼스가 만 단위로 올라가는 날이고, 그때 `indexer.py` 의 `ponytail:` 주석이 이 자리로 데려온다.
- [4] **재크롤된 문서는 동점 랭킹에서 뒤로 밀린다 — `DELETE`+`INSERT` 가 rowid 를 새로 잡기 때문이다** (2026-09-08 반복 474 리뷰 F4). `search` 의 정렬은 `ORDER BY bm25(docs), rowid` 이고 그 주석은 「같은 틀로 찍힌 페이지들은 bm25 가 정확히 같다」고 적어 뒀다 — 그 묶음 안에서는 **내용이 안 바뀌어도** 재크롤을 한 번 받을 때마다 순위가 뒤로 간다. `OFFSET` 페이지네이션의 안정성도 같은 이유로 흔들린다. **8점이 아닌 이유**: `docs` 가 FTS5 라 rowid 를 보존하며 갈아 끼우는 길이 없고(그게 이 저장소가 지우고 넣는 관용구를 쓰는 이유다), 실물에서 순위 변화를 재 본 적이 없다. 여는 조건은 동점 묶음의 순위 안정성을 실제로 재는 자가 생기는 날이다.
- [3] **404·410 을 「영구 삭제」로 보면서 15일마다 다시 두드리는 것이 맞나 — 사람이 정할 몫으로 남긴다** (2026-09-08 반복 474 리뷰 F6). `indexer._GONE` 은 404·410 을 `docs` 에서 영구히 빼는데 `store.RETRY_DAYS = 15` 는 같은 행을 살아 있는 URL(30일)의 **두 배 빈도**로 재요청한다. 크롤 윤리가 갈림길 1순위인데 이 방향은 그 축에 역행한다. 다만 「실패도 다시 받는다」는 **2026-09-08 사용자가 위임해 정한 정책**(`plan_recrawl.md` 2절 1)이라 야간이 뒤집지 않는다. 실물 코퍼스에 실패 행이 **0건**이라 오늘 두드릴 죽은 URL 자체가 없는 것도 값을 낮춘다. 여는 조건은 실패 행이 실제로 쌓이는 날이고, 그때 `CASE` 에 `_GONE` 갈래를 하나 더 두는 것이 최소 수정이다.
