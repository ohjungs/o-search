---
signal: GREEN
phase: e2e
step: 1
attempt: 0
iteration: 212
updated: 2026-09-01
ctx: 52
night_iterations: 75
night_red: 0
night_retries: 0
---

# 현재 상태

**계획 42 `docs-citation-guard` 리뷰 1/1 완료 — 판정 조건부.**
스텝의 산출물은 옳고 한도는 전부 지켜졌지만, **검사가 겨냥한 바로 그 표기의
살아 있는 위반 2건이 지금 초록으로 통과 중이다.** 제품 `src/` 0줄 · 자동 수정 1
(`tests/test_docs.py` 독스트링) · 남은 몫은 `digest` `[6]` 에 인계했다(항목 안에서 압축, 49 유지).

## 방금 것 (2026-09-01 · 리뷰 1/1 · 반복 212)

지적 5건 보고 · 버림 2건 · 제품 동작 지적 0건. 백지 패스를 먼저 회수했다.

### [A1] 검사 자신을 붙드는 단언이 0개다 — 축소 변이 4종이 전부 생존 (중요도 9)

이 스텝의 산출물은 "정규식을 두 표기로 넓힌 것"인데 **그 넓힘을 지키는 단언이 없다.**
백지 패스가 축소 변이를 넷 심어 봤다 — ① 정규식을 개발 phase 의 콜론 꼴로 되돌리기
② 한국어 꼴만 남기기 ③ 대상 이름 하나 빼기 ④ 백틱 허용 지우기. **넷 다 hits 0 · 초록.**

이유는 구조적이다: 이 검사의 유일한 오라클이 **살아 있는 문서 자신**이라, 문서를
깨끗하게 만든 순간부터 정규식이 좁아진 것을 아무도 못 본다. 변이 M6·M7 이 각각
한 가지를 죽인 것은 맞지만 **둘 다 데이터(문서 한 줄)와 glob 을 건드렸지 정규식을
안 건드렸다** — 넓어지는 변이는 잡히고 좁아지는 변이는 안 잡히는 비대칭이다.

> 일반화: **린트형 검사는 데이터가 초록일 때 자기 자신을 못 잰다.**
> 금지·허용 표기를 표로 단언하는 자체 검사가 있어야 ④의 경계도 주석이 아니라 계약이 된다.

### [A2] 세 번째 탈출구가 이미 열려 있고, 그 안에 이 계획서가 있다 (중요도 8)

정규식이 이름과 숫자의 **인접**을 요구한다(`` `? ?``). 그래서 마크다운 **표 칸
구분자 하나**로 빠져나간다 — 그리고 그렇게 통과 중인 줄이 지금 둘이다:
`plan_docs-citation-guard.md` 의 `## 5. 건드릴 파일` 표 두 행. 검사가 지키려는
문서가 하필 **표로 되어 있다**(`index.md` 는 통째로 표다).

**그중 하나는 이 계획이 도는 동안 이미 썩었다** — 계획서 표는 아직 357 을 가리키는데
`index.md` 는 한 줄 늘어 목표가 358 로 밀렸고, 기록은 이미 358 로 적고 있다.
계획서 한 문서 안에서 같은 인용이 두 값으로 갈렸다.

실측: 분리자를 3자까지 허용하면 그 둘을 잡고 **오탐 0** — `` `index.md` 41 행 `` ·
`200줄` · `15~17번` · `49항목` · 자리표시자가 전부 그대로 통과한다.

### [A3] 버린 것 2건 — 넓히지 않는 쪽이 실측으로 옳다

- **`L12`·`#L12`·`(12)`·`line 12`·`라인 12`** — 아카이브 62개를 포함한 **문서 77개
  전수에서 0건.** 211 반복 동안 한 번도 안 쓴 표기다. 가지를 늘릴 근거가 없다(사다리 1번).
- **단위 없는 맨 숫자**(`` `index.md` `` 뒤 숫자 하나) — ④가 증명한 그 모호함 때문에
  **판정 불가**다. 같은 자리의 숫자가 줄일 수도 계획 번호일 수도 있고, 넓히면
  살아 있는 문서에서 오탐이 14건 뜬다. 천장으로 받아들이고 적어 둔다.

### [B1] 계획서 5절의 YELLOW 조항이 울려야 했는데 안 울렸다 (중요도 5)

5절이 예고한 파일은 셋(`tests/test_docs.py` · `index.md` · `digest.md`)이고 마감 문서
셋만 면제인데, 실제로는 **`README.md`**(건수 466→467)와 **계획서 자신**이 더 바뀌었다.
둘 다 기존 가드(`test_readme`)와 자기 기록이 강제한 것이라 **무해하고 정정할 것도 없다** —
다만 조항 문구가 좁아서 YELLOW 가 안 울린 것이지 조항을 지킨 것은 아니다.

### [B2] 계획서에서 줄번호를 지운 것은 기록 손실이 아니다 (판정)

지워진 자리가 전부 괄호 안에 이름을 이미 달고 있었고(`plan_clock-injection` 행 등),
숫자는 `816e363` 에 그대로 남아 있다. 게다가 **그 숫자는 이미 틀렸다**([A2]) —
버린 것은 기록이 아니라 오답이다.

### 룰 충돌 판정 — 검사는 `history_current.md` 를 계속 훑는다 (유지)

앞 스텝이 "영구 RED 가 된다"고 넘겼는데 **그렇게 안 된다.** 회전이 그 항목을 밀면
파일 이름이 `history_<숫자>.md` 가 되어 아카이브 제외에 걸리므로, 검사가 얼린 기록을
고치라고 요구하는 상황은 **생기지 않는다.** 창이 열려 있는 동안만 고치면 되고 그
편집은 회전 자체가 이미 하는 일이다. 대가는 하나 — 위반이 회전까지 살아남으면
아카이브에 영구 보존되고 검사는 초록이 된다. 그것이 이 가드의 시간 창이고 받아들인다.

### [B3] `index.md` 계획 42 행 — 이번엔 안 어긋났다 (확인)

세 커밋이 전부 `index.md` 와 `metrics.md` 를 함께 올렸다(계획·개발·테스트).
계획 41 e2e 가 네 번째로 적은 *"어긋나는 것은 늘 `index.md`"* 가 이번엔 안 났다.

**검증**: 단위 **467건 OK 11.952초**(맨몸, `export` 를 갈라 러너를 첫 낱말로 — 파이프 0) ·
`data/crawl.db` sha256 **무변경**(`85c96744…5bda18`) · `docs/digest.md` **200줄 · 49항목** ·
`e2e/*.py` **19종** · `src/` **0줄**.

## 다음

- **e2e 1/1.** 계획 7절은 **새 e2e 0개(잠정)** 이고 제품 diff 0줄이라 판정은 그대로일
  가능성이 높다. 볼 것: ① 7절의 반증 신호 둘(예상 못 한 이름의 `.md` · 회전이 만드는
  이름)을 실제로 재고 판정을 확정할 것 ② 회귀 `e2e/*.py` 19종 rc 0 전수
  ③ **DONE 에서 `digest` `[6]` 을 "닫혔다" 로 적으면 거짓이 된다** — 위 [A1]·[A2] 가
  남아 있다. 닫지 말고 남은 몫을 그대로 두거나, 후속 계획으로 여는 쪽을 고른다.

## 한도 (매 반복 확인)

- 브랜치 `loop/docs-citation-guard` · `main` 직접 커밋 금지 · `--no-verify` 금지 ·
  외부 네트워크 금지 · `docs/specs/` 읽기만 · `data/crawl.db` 무변경.
- **`src/` 는 계획에서 0줄이다.** 건드리면 YELLOW.
- **검증은 맨몸으로.** `python3 -m unittest discover -s tests` 를 포그라운드에서 그대로 돌린다.
  파이프·`grep`·`head`·`tail`·`for`·`env`·`timeout`·리다이렉션으로 감싸지 않고,
  백그라운드 태스크로도 돌리지 않는다. (`digest ## 반복 실패` 의 해당 항목 **8회**)
- `docs/digest.md` 200줄 상한 · 열린 항목 49.
