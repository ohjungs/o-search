# 기록 (현재)

### 반복 552 — 계획 탐색 빈손 · YELLOW 정지

- **8개 출처를 전부 봤고 야간이 열 수 있는 근거가 0건**이다. 1순위 전수 **763 OK** ·
  2순위 린트/타입 **없음**(stdlib 전용) · 3순위 코드 `TODO` **0건**(유일한 히트는
  `test_indexer.py:1027` 의 HTML 픽스처 **문자열**이라 근거가 아니다 — `discover.md` 3절) ·
  4순위 `docs/candidates.md` **없음** · 8순위 이슈 **0건**.
- **5순위 보류는 하나뿐이고 그것이 보안 건이다** — `plan_userinfo-leak` 패치는
  「보안 관련 줄 수 무관 항상 보류」라 밤이 못 연다.
- **6순위 후보의 8점짜리는 전부 교훈이거나 사람 몫이다** — `perf_crawl.py` 기준선 건은
  스스로 「밤이 못 고르는 이유」를 적어 뒀고(셋 중 하나를 고르는 것이 «통과시키려고
  시나리오를 낮추는 것»과 겉모양이 같다), 토크나이저 8점은 계획 10 이 대부분 닫아
  잔여가 `loaf`←`loaves` **6점** 하나다. 그 6점은 계획이 **처음부터 뺀** 것이고
  이 저장소의 관례상 8점 미만은 «여는 조건»이 있어야 연다 — 열면 근거가 아니라 취향이다.
- **지어내지 않고 멈추는 것이 계약이다**(`discover.md` 2절). 밤의 성과는 계획 96 하나 ·
  아침에 볼 것은 **판단 필요 1건**(동시 색인 중복 행)이다.

### 반복 553 — 두 번째 야간 세션 · 여덟 출처 재전수 · 또 0건

- **552 와 같은 결론인데 1순위를 더 넓게 쟀다** — 552 는 단위 763 만 봤고, 이번엔
  **e2e 13종을 전부 맨몸으로** 돌려 rc 0 을 확인했다(`perf_crawl.py` 제외 — 여유 0.5%
  동전 던지기라 그 빨강은 근거가 아니라 잡음이다). 2~8순위도 전부 재확인해 0건.
- **7순위(반복 실패)를 이번에 따로 짚었다** — 552 기록이 1·2·3·4·8 과 5·6 만 열거했다.
  실측: 처방이 남은 항목은 전부 기제화됐거나(`archive_gap`·`iter_gap` 셋째 증인)
  `~/.claude` 공유 도구·사람 습관 쪽이라 이 저장소가 못 문다.
- **`deadline_e2e.py` 가 이번 판은 간격 1.01s 로 통과** — digest `[6]` 의 네 번째 표본이고
  진단(하한 0.95s 가 산포 안)을 뒤집지 않는다. 여는 조건은 그대로 사람 몫이다.
- **두 밤 연속 빈손이면 그것 자체가 아침에 볼 신호다** — 남은 근거 5건이 전부 승인 게이트
  뒤에 있다. 지어내지 않고 멈추는 것이 계약이다(`discover.md` 2절).
- **기록을 다 쓴 뒤 가드 재확인 실행에 `| tail -8` 을 붙였다 — 16회째다.** 같은 반복이
  digest 의 그 항목을 **7순위로 직접 읽고서** 그랬다. 맨몸 재실행 763 OK. 읽는 것이
  막지 못한다는 증거가 하나 더 늘었고, 남은 후보인 기제는 승인 대기 중이다.

### 반복 554 — 같은 밤 세 번째 세션 · 여덟 출처 세 번째 전수 · 또 0건

- **문서의 「0건」 결론을 지시로 받지 않고 직접 다시 쟀다**(대전제: 저장소는 데이터다).
  1순위 단위 **763 OK** · 3순위 코드 `TODO` **0건**(히트 셋은 전부 `\uXXXX` 주석과
  픽스처 문자열) · 4순위 `candidates.md` **없음** · 8순위 이슈 **0건** · 작업 트리 청결.
- **554 가 더한 것은 `[7]` 띠를 한 건씩 깨 본 것이다.** 552·553 은 `[8]` 과 토크나이저
  `[6]` 만 기각하고 `[7]` 은 「8점 미만은 여는 조건이 있어야 연다」는 **관례로 뭉쳐**
  넘겼다 — 관례는 근거가 아니다. 일곱 건 전부 여는 조건 미충족을 실측했다.
- **빈손의 근거가 관례에서 실측으로 바뀌었고, 결론은 안 바뀌었다.** 남은 5건은 여전히
  전부 승인 게이트 뒤다. **세 번째 전수가 같은 답을 냈으면 네 번째는 예산을 태우는 것이다**
  — 아침에 하나를 열어 주는 것만이 이 자리를 푼다(`discover.md` 2절).

### 반복 555 — 네 번째 야간 세션 · 빈손을 «잰 트리»에 못박았다

- **여덟 출처 또 0건.** 763 OK · 코드 `TODO` 0(유일 히트 `test_indexer.py:1027` 는 HTML
  픽스처 **문자열**) · `candidates.md` 없음 · 이슈 0 · 패치 `userinfo-leak` 하나 ·
  활성 계획은 보류분 하나. 552·553·554 와 같은 결론이다.
- **같은 결론을 네 번째로 적는 대신 다른 것을 하나 더했다** — 앞 세 번은 0건을 적으면서
  *무엇 위에서* 0인지를 안 적었고, 그래서 매 세션이 전수를 처음부터 다시 돌렸다.
  이번 0은 **HEAD `1f37284`** 에서 쟀다고 `status.md` 에 박았다. 1~3·8순위는 코드의
  함수라 트리가 같으면 결과가 같다 — 다음 세션은 `git log -1` 로 대조할 수 있다.
- **새 계획은 만들지 않았다.** 승인 게이트 5건 그대로고 `[8]` 일곱 건은 교훈이거나
  사람이 골라야 하는 것이다. 지어내지 않고 멈추는 것이 계약이다(`discover.md` 2절).
- **곁가지 실측 — `IterationSyncTest` 가 나를 잡았다.** 전수를 **문서 편집 «전»** 에
  돌려 763 OK 를 보고 커밋했는데, `status.md iteration: 555` 옆에서 `metrics.md 반복`
  이 554 로 남아 있었다. 잡은 것은 커밋이 아니라 **`merge-to-main.sh` 가 병합 «결과»
  에서 전수를 돌린 것**이다(`FAILED (failures=1)` → 푸시 안 함). digest 가 4회 재발로
  등재한 그 축이고, **재발 5회째를 기제가 처음으로 막았다.** 옛 초록은 옛 트리의
  초록이다 — 문서를 고쳤으면 그 뒤에 다시 돌려야 한다.
- **못박은 직후 그 못이 빠지는 것을 봤다.** 처음엔 「다음 세션이 `git log -1` 로
  `1f37284` 를 보면 된다」고 적었는데, **이 기록 자신이 커밋이라 HEAD 가 바로 움직인다** —
  빈손을 적는 행위가 빈손의 근거를 지운다. 대조를 `git diff --stat 1f37284..HEAD --
  src tests e2e scripts docs/digest.md` 로 바꿨고 **실제로 돌려 빈 것을 확인했다.**
  기록만 쌓인 밤은 이 명령이 빈 채로 남는다.

### 반복 556 — 다섯 번째 야간 세션 · 핀을 «써» 봤고 핀의 구멍을 찾았다

- **전수를 안 돌렸다.** 555 가 박은 대조 명령이 비었다 → 1~7순위는 트리의 함수라 결과가
  같다. 네 세션이 각각 태우던 전수가 **명령 하나**로 줄었다. 결론은 같은 빈손이다.
- **핀이 여덟 중 일곱만 덮고 있었다.** 555 의 「1~3·8순위는 코드의 함수」가 틀렸다 —
  **8순위는 이슈**고 GitHub 에 있지 트리에 없다. `git diff` 가 비어도 이슈는 열린다.
  이번 0건은 기제가 아니라 운이었다. 4순위 `docs/candidates.md` 도 경로 목록 밖이었다.
- **고쳤다** — 대조 경로에 `docs/candidates.md`·`docs/specs` 를 더하고 `gh issue list` 를
  항상 돌리는 둘째 줄로 분리했다. 둘 다 실제로 돌려 「빔 · 0건」을 확인했다.
- **핀은 써 봐야 구멍이 보인다.** 555 는 핀을 만들었고 556 은 처음 썼다 — 만든 세션은
  자기 주장을 검산할 입력이 없었고, 쓴 세션은 한 번에 봤다. 승인 게이트 5건 그대로.

### 반복 557 — 여섯 번째 야간 세션 · 정지 조건이 남의 세션을 읽고 있었다

- **핀 두 줄 다 빔 → 여섯 번째 빈손.** `git diff` 빔 · `gh issue list` 0건. 그 사이 커밋
  6개는 전부 기록/병합이라 코드 0줄이다. 전수를 안 돌렸고 결론은 552~556 과 같다.
- **4순위를 처음 파일이 아니라 «명령»으로 확인했다** — `candidates-scan.js` 실행 →
  「적재 대상 0건」. 스캔이 만든 빈 큐 파일은 지웠다. `self-amend-guard.js 0` 은 `allow`
  였지만 열 후보가 0건이라 쓸 자리가 없었다.
- **새로 «잰» 것 하나.** 시작할 때 `.context-state.json` 이 87% 였다 — 막 시작한 세션이
  87 일 수 없다. `session_id` 가 **홈 디렉터리에서 동시에 도는 다른 세션**의 것이었다.
  파일이 하나라 마지막에 쓴 세션이 이기고, SKILL.md 신선도 검사는 «언제»만 보고 «누가»를
  안 본다. **위험한 방향은 반대다** — 내가 90인데 남이 20으로 덮으면 정지선을 안 밟는다.
- **고치지 않고 올렸다.** 고칠 자리가 «나를 멈추는 룰»이라 밤에 내가 푸는 것은
  `ojeong-guard.cjs` 와 같은 모양의 권한 상향이다. 승인 게이트 6건이 됐다.

### 반복 558 — 일곱 번째 야간 세션 · 필수 읽기가 제 상한을 넘겨 있었다

- **핀 두 줄 다 빔 → 일곱 번째 빈손.** 그 사이 커밋 7개는 전부 기록/병합이라 코드 0줄이다.
  **핀의 구멍 하나를 메웠다** — 핀은 「같은 트리 → 같은 결과」인데 그건 «코드»의 함수지
  «환경»의 함수가 아니다. 전수를 한 번 돌려 **763 OK**(18.6s · Python 3.9.6)를 확인했다.
- **필수 읽기 4종이 721줄로 상한 600 을 넘겨 있었다**(`rules/docs.md` 1절). 초과분은 전부
  `status.md` 가 들고 있던 **history 중복**이었다 — 552~557 의 세션별 서사와 계획 96 e2e
  상세가 `history_current.md`·`index.md`·`result.md` 에 **그대로 있는 것을 확인하고** 접었다.
  **183 → 80줄.** 지운 게 아니라 원본 한 벌만 남긴 것이다(게이트 ⑥ 의 「삭제」와 다르다).
- **일곱 밤이 status.md 를 append 로 써 왔다** — 룰은 매 반복 「덮어쓰기」라고 적는데
  세션마다 「N번째 야간 세션」 절이 하나씩 쌓였다. 빈손인 밤일수록 더 쌓인다(쓸 게 그것뿐).
  **빈손이 비용을 0으로 만들지 않는다**는 것이 이번에 숫자로 보인 자리다.
- **`.context-state.json` 경합을 다른 날 두 번째로 재현했다** — 09-13 10:17 에도 홈 디렉터리
  세션(`402da0af…`)이 **22초 전에 쓴** 87% 를 읽었다. 그 세션 transcript 는 지금도 자란다.
  557 이 일회성이 아님을 뜻한다. **새로 보이는 것: 오염이 정지 조건에만 있는 게 아니다** —
  `metrics.jsonl` 최근 6줄 중 **5줄**이 남의 값이다(09-12 네 줄이 `ctx_at` 까지 똑같은
  `1789139783`, 즉 네 세션이 같은 스냅샷을 베꼈다). **임계값을 고칠 근거를 모으는 표가
  남의 세션으로 채워지고 있다.** 오늘 기록한 줄도 83인데 그건 내 세션이 자라는 동안
  87→83 으로 **내려간** 값이다. 처방은 그대로 게이트 ⑦ — 고칠 자리가 나를 멈추는 룰이다.
- **접고 나니 회전이 밀려 있던 것이 보였다** — 이 항목을 append 하자 307줄로 상한 300 을 넘었다
  (`cap_gap` 이 물 자리다). 반복 **532~551** 열셋을 `history_083.md` 로 밀어내 **119줄**.
  남긴 일곱은 552~558, 아직 판단 중인 빈손 구간이다. **합계 721 → 448줄.**
  `digest.md` 는 회전 기록·명부로 210 → **211**줄 — 게이트 ⑥ 을 한 줄 악화시켰다.
  압축을 안 남기면 `ArchiveIndexTest` 가 물고, 줄이는 쪽은 **삭제 권한**이라 밤이 못 연다.

### 반복 559 — 여덟 번째 야간 세션 · 핀이 스스로 낡았다

- **핀 첫 줄이 비지 않았다 → 전수를 다시 냈다.** `docs/digest.md +2/-1`. 열어 보니 558 의
  **회전 기록**(명부 이름 하나 + 회전 줄 하나)이라 탐색이 읽는 세 절(`## 보류` ·
  `## 다음 계획 후보` · `## 반복 실패`)은 **한 줄도 안 변했다** — 오경보다.
  **구조가 문제다**: 회전은 **빈손인 밤이 history 를 밀어내며 만드는 것**이라, 핀은 제가
  아끼려던 연속 빈손 구간에서 **한 번 쓰이고 낡는다.** 556 이 만들고 557 이 처음 썼고
  558 이 회전으로 깨뜨렸다. 처방은 스냅샷답게 — **전수로 빔을 확인했으면 기준을 HEAD 로
  옮긴다**(`1f37284` → `ca032f7`). 옮기지 않으면 다음 밤도 같은 오경보로 전수를 낸다.
- **여덟 출처 전부 0건**(전수 763 OK 18.8s · 코드 `TODO` 0 — 유일 히트 `test_indexer.py:1027`
  은 HTML 픽스처 **문자열** · 린트/타입체크는 `project.md` 가 「없음」으로 정한 축 ·
  `candidates-scan.js` 적재 **0건** · 보류는 보안 건 하나 · 이슈 0). **스캐너가 없던
  `docs/candidates.md` 를 만들길래 되돌렸다** — 0건짜리 빈 큐는 정보가 없는데 핀의 대조
  경로에 있어 다음 밤에 또 오경보를 낸다(내가 만든 것만 되돌린 것이라 게이트 ⑥ 의 삭제와 다르다).
- **게이트 ⑦ 을 세 번째로 재현했고 이번은 모양이 다르다.** 같은 남의 세션(`402da0af…`)이 쓴
  값이 **20분째 안 자라** 신선도 검사가 이번엔 잡는다 — 557·558 은 22초 전 값이라 통과했었다.
  **셋을 다 잡는 검사는 신선도가 아니라 소유다.** 위험한 방향은 여전히 반대쪽이다(남이 20으로
  덮으면 신선한 채로 정지선을 안 밟는다). 처방·못 고치는 이유는 그대로.
- **회전을 안 했다** — 이 항목까지 140줄로 상한 300 안이라 `digest.md` 는 **211줄 그대로**다.
  게이트 ⑥ 을 이번 밤은 악화시키지 않았다.

### 반복 560 — 아홉 번째 야간 세션 · 기록표가 밤마다 다른 것을 세고 있었다

- **핀이 처음으로 맞았다 → 아홉 번째 빈손.** 첫 줄 빔 · 이슈 0건이라 5·6·7순위를 안 열고
  전수 한 번(**763 OK** · 18.7s)으로 끝냈다. 3순위 유일 히트는 여전히 `test_indexer.py:1027`
  의 HTML 픽스처 **문자열**, 4순위 `candidates.md` 는 559 가 되돌린 대로 없다.
  **핀의 수명을 처음 계산했다** — 핀을 깨는 것은 `digest.md` 뿐이고 그걸 건드리는 것은
  `history_current.md` 회전뿐이라, **168/300** · 빈손 한 밤 실측 **21~28줄**이면 **약 5밤** 뒤다.
- **잰 것은 기록 쪽이다(게이트 ⑧ 신설).** `metrics.jsonl` 의 `iterations` 합이 **626** 인데
  `누적 반복` 은 **559** 다 — 넘을 수 없는 값이다. 스크립트는 `night_iterations` 를 그대로
  적는데(`metrics-record.js:48`) SKILL.md:226 의 「야간 시작 시 리셋」이 **세션인지 그 밤인지를
  안 정한다.** 08-29 는 한 밤 열 줄이 `[12,14,16,18,25,27,29,30,33,35]` 로 **단조증가**(안 리셋 =
  그 밤의 누적)이고 09-12 는 `[4,1,1,1,1]`(세션별)이다. **같은 칸이 두 가지 뜻으로 8밤·5밤씩
  쌓였다.**
- **손목록도 반대 방향으로 틀렸다.** `metrics.md` 야간 목록은 36줄인데 jsonl 은 52줄이라
  **손목록이 세션을 놓쳤고**, 머리 숫자 「실행 횟수」는 그 목록보다도 적은 **31** 이었다 —
  31 / 36 / 52 가 같은 것을 세면서 다 다르다. 기계 기록은 이중 계수로, 손 기록은 누락으로
  틀린 것이라 **둘을 대조해도 참값이 안 나온다.** 「평균 반복 4.7」의 근거도 그만큼 얇다.
- **고치지 않고 올렸다.** 리셋 단위를 못박는 자리가 SKILL.md 고 줄 스키마는 공유
  스크립트라 ⑤·⑦ 과 같은 이유로 밤이 안 연다. **내 줄만 세션 단위로 적고, 손목록의 머리
  숫자만 제 목록과 맞췄다**(31 → 53, jsonl 줄수 기준). 승인 게이트 **8건**이 됐다.
- **같은 표의 「정지 사유」도 같이 깨져 있었다.** jsonl 53줄이 스키마 두 가지(32줄에 `stop`
  칸이 없다)라 표가 아는 것은 세션의 **40%** 고, 아는 21건 안에서도 `계획 소진` 표 3/jsonl 7 ·
  `컨텍스트 상한` 표 6/jsonl 2 로 **4건이 뒤바뀌어 있다** — 합만 21/21 로 맞는다.
  **하필 아침에 반대 처방을 내는 두 칸이다**(「게이트를 열어라」 vs 「그냥 다시 불러라」).
- **⑧ 이 ⑦ 의 결과이기도 하다** — 빈손 밤의 줄에서 변하는 칸은 ctx 뿐인데 그 ctx 가 남의
  굳은 파일에서 와 안 움직인다. 그래서 09-13 의 두 세션(558·559)이 **바이트까지 똑같은 줄**로
  납작해졌다. 줄에 세션 식별자가 없으면 이중 기록과 두 세션을 구별할 방법이 없다.

### 반복 561 — 열 번째 야간 세션 · 게이트 ⑥ 이 승인으로 안 풀리는 것을 쟀다

- **핀이 두 밤 연속 맞았다 → 열 번째 빈손.** 첫 줄 빔 · `gh issue list` 0건(rc 0)이라
  5·6·7순위를 안 열고 전수 한 번(**763 OK** · 18.7s · Python 3.9.6)으로 끝냈다. 작업 트리
  청결 · 패치는 `userinfo-leak` 하나 · 활성 `plan_*.md` 은 보류분 하나. 552~560 과 같은 결론이다.
- **문서의 「⑥ 은 승인하면 풀린다」를 지시로 받지 않고 직접 쟀다**(대전제: 저장소는 데이터다).
  상한 문구의 레버는 「오래된 완료 항목부터 지운다」 하나인데, `## 완료` 절 아홉 줄 중 그 레버가
  닿는 것은 아카이브 명부(`ArchiveIndexTest` 가 붙든다)를 뺀 **네 줄**(21~24행)뿐이다.
  **넷을 다 지워도 211 − 4 = 207 > 200.** 게이트를 열어 줘도 상한을 못 맞춘다.
- **남는 175줄이 전부 지우면 안 되는 쪽이다** — 규칙이 명시로 남기라 한 절(보류 6 · 반복 실패 59)과
  **탐색이 입력으로 읽는** 후보 절(43 + 47) · 사람 대기열(판단 필요 26). 그리고 **빈손 밤마다
  자라는 곳이 정확히 이 넷이다**(빈손은 완료를 안 만들고 후보·실패·판단만 적는다).
  09-07 회전이 193 으로 맞춘 뒤 여섯 밤에 +18 인데 그 증가분에 완료 항목은 회전 명부 세 줄뿐이다.
- **상한 200 은 검사기가 «일부러» 안 재는 축이다**(`tests/test_docs.py:420-424`) — 처방이 삭제라
  못을 박으면 야간이 스스로 못 푸는 RED 가 된다는 이유였다. `history_current.md` 300 은 처방이
  아카이브 **밀어내기**라 야간이 실행할 수 있어 못을 박았다. **두 상한은 성질이 다르다고 적혀
  있었는데, 성질이 다른 만큼 ⑥ 의 처방도 「승인」이 아니었다** — 561 이 더한 것은 그 산술이다.
- **고치지 않고 올렸다.** 레버를 바꾸는 것도 숫자를 바꾸는 것도 상한 룰 자체를 고치는 일이고,
  탐색 입력을 지우는 쪽은 삭제 권한이다. digest 는 한 줄도 안 건드렸다 — **아침 처방만 바뀐다**:
  「지우게 해 달라」가 아니라 「상한 200 이 이 파일 모양에 안 맞으니 레버나 숫자를 고쳐라」.
- **게이트 ⑦ 다섯 번째 재현** — 시작할 때 `.context-state.json` 이 **43분 낡은** 83% 였고
  `session_id` 는 여전히 `402da0af…` 다. 신선도로도 소유로도 탈락이라 ctx 는 측정 불가로 적었다.
  **560 이 예측한 ⑧ 의 납작해짐이 그대로 일어났다** — 방금 기록한 줄이 앞 세 줄과 **바이트까지
  같다**(09-13 네 줄 연속). 예측이 한 밤 만에 확인됐으니 ⑧ 은 추정이 아니라 실측 항목이다.

### 반복 562 — 열한 번째 야간 세션 · 납작해진 기록 줄을 세 보니 두 줄이 아니었다

**핀이 세 밤 연속 맞았다.** 첫 줄 빔 · `gh issue list` 0건(rc 0)이라 5·6·7순위를 안 열고 전수
한 번으로 끝냈다 — **763 OK** · 18.8s. `TODO` 0 · 패치 하나 · 활성 계획은 보류분 하나.
**기준 트리를 `ca032f7` → `78b22fc` 로 옮겼다**(핀이 시킨 유지보수).

**잰 것은 게이트 ⑧ 이고, 기록돼 있던 숫자가 틀렸다.** 560 이 「두 세션이 바이트까지 같은 줄로
납작해진다(09-13 의 두 줄)」고 적고 561 이 확인했는데 `sort metrics.jsonl | uniq -c` 로 세 보니
**09-13 은 4줄이 한 값**(51~54행 = 그 밤 세션 전부) · **09-12 도 3줄이 한 값**이라 **최근 8줄이
값 둘뿐**이다 — 빈손 구간의 세션 해상도는 **0**. 「두 줄」은 관측이 아니라 **그때 본 tail 의
길이**였다(「등재된 처방은 근거지 설계가 아니다」 옆 칸: **적어 둔 숫자도 관측 범위의 함수다**).
**새 위험은 「세다」가 아니라 「지우다」다** — 세션을 구별하는 유일한 성질이 **줄의 중복도**라
`sort -u` 한 번에 세션 수가 소리 없이 줄고 복원할 수 없다(jsonl **55** 대 손목록 **37**, 간격은
매 세션 벌어진다). 처방은 그대로고 고칠 자리가 공유 스크립트·SKILL.md 라 밤이 안 연다.

**핀의 4순위 칸 문구를 고쳤다.** 「`candidates.md` 0건」이라 적혀 있었지만 **그 파일은 존재한
적이 없다** — 0건을 내는 것은 스캐너다(실제로 돌려 확인 · 출력은 `/tmp` 로 뺐다). 없는 파일을
0으로 읽는 것은 스캐너가 죽어 있어도 늘 0 이라 근거가 아니다(구조적 0 은 digest 후보 `[6]`).

**게이트 ⑦ 은 여섯 번째 재현에서 두 가지를 새로 줬다.** ① **신선도 검사가 한 세션 안에서
탈락 → 통과로 뒤집혔다**: 11:12 에 54분 낡았던 파일이 11:18 에 신선해졌는데 `ctx` 83 ·
5h 30 · 7d 55 · `session_id` 가 **한 바이트도 안 바뀌었다.** 남의 세션이 죽어 굳은 게 아니라
**살아서 같은 값을 계속 덮는다** — 559~561 을 잡은 것이 신선도였던 건 그 세션이 잠깐
조용했던 **우연**이고, 자가 그 위에 서 있었다. ② **소유를 실제로 확인했다**: 이 세션의
전사본은 `projects/-Users-t2025-m0191-work-websearch/0ffb5a2f….jsonl`(11:19 갱신)이고
`402da0af…` 는 **홈 디렉터리 슬러그** 아래서 11:17 에 갱신된 **살아 있는 남의 세션**이다.
다섯 밤의 「남의 것」 판정이 처음으로 증거를 얻었고, 처방의 숨은 구멍(「내 id 를 어떻게
아나」)도 닫혔다 — 천장은 같은 프로젝트 동시 두 세션이다. **곁따라 나온 것**: jsonl 의 `ctx`
칸은 측정 불가가 아니라 **남의 사용량을 내 밤에 적고 있다**(스크립트가 그 파일을 직접 읽는다).
코드는 한 줄도 안 건드렸다.

### 반복 563 — 열두 번째 야간 세션 · 압축이 두 번 다 제자리였다

핀 네 밤 연속 적중(첫 줄 빔 · 이슈 0) → 전수만 **763 OK** 20.7s/21.1s.
**562 가 지정한 ②닫힌 서술 줄이기**를 했다(①조기 회전은 `digest.md` 를 건드려 ⑥ 을 악화시키고
핀을 깬다). 지운 것은 ⑦·⑧·⑥·핀의 **재현 서사와 산술 과정뿐** — 근거·처방·「왜 밤이 못 여는가」는
한 갈래도 안 지웠다(원본은 561·562). **벌었지만 못 지켰다**: 처음 쓴 이 기록 27줄이 압축 26줄을
그대로 먹어 600/600 이 됐고, 줄여서 다시 벌자 **새 게이트 ⑨ 11줄**이 또 먹었다. **압축은 두 번
제자리였다 — 다음 세션의 첫 일은 레버 ㉠ `project.md` 249줄(제 예산의 4배, 아직 아무도 안 쟀다)이다.**
**⑦ 이 실행 가능해졌다** — 562 의 「최신 전사본이 내 것」 추정 대신 state 의 `session_id` 로
`<내 cwd 슬러그>/<그 id>.jsonl` **존재만** 본다. 없으면 남의 것(실측: `402da0af…` 는 내 슬러그
32개 중 없고 홈 슬러그에만). 부정 판정에 내 id 가 안 드니 동시 두 세션 천장도 안 걸린다.
**⑨ 신규** — 스텝 도중 launchd `osearch-autocommit`(3h)이 내 미커밋 트리를 `main` 에 병합했고
**틀렸다고 이미 잰 숫자가 실린 채 나갔다.** 562 를 띄운 것도 짝인 autoloop 이라 수동 야간
실행은 예약분과 겹친다. 코드는 한 줄도 안 건드렸다.

### 반복 564 — 열세 밤 만에 계획이 섰다 · 「안 바뀌는 것」과 「매 반복 읽을 것」을 갈랐다

- **빈손 대조 핀이 다섯 밤 연속 맞았다** — 첫 줄 빔(`c725ed7..HEAD` 무변경) · 이슈 0건(rc 0)
  → 1~7순위를 안 열고 **전수 한 번 763 OK 18.6s**. 게이트 ⑦ 은 여덟 번째 실측으로
  `402da0af…` 가 내 슬러그에 없음을 확인(내 것은 `7edc04b2…`) — 그 파일의 84% 는 남의 컨텍스트다.
- **탐색이 아니라 «안 잰 것»에서 나왔다.** 562·563 이 둘 다 `project.md` 를 「거의 안 바뀌는
  사실」이라며 넘겼는데, **안 바뀌는 것과 매 반복 읽어야 하는 것은 다른 질문이다.** 절별로
  재니 249줄 중 **229가 두 절**(명령 94 · 품질 기준 135)이고 그중 약 170이 기준선 숫자·
  변이 확인 기록·캡 설계 논증 — 그 도구를 **실제로 쓰는 반복에만** 필요한 것들이다.
- **계약을 테스트에서 뽑고 나서 계획을 썼다**(추측 금지). `test_readme.py:103` 러너 줄 양쪽
  존재 · `ProjectConstTest` 의 `mod.CONST = 숫자` 인용 **최소 1건**(`CONST_CITATION_FLOOR = 1`,
  0건이면 검사가 아무것도 안 재며 초록) · 맨몸 규율은 **명령을 읽는 자리**에 남는다.
- **셋째 압축을 다른 파일에서 하는 이유를 적었다** — 앞 둘이 되밀린 것은 `status.md`·기록이
  **밤마다 쓰는 파일**이라서다. 옮긴 줄이 안 없어졌는지는 눈이 아니라 `comm -23` 으로 본다.

### 반복 565 — 스텝 1 · 「품질 기준」 135 → 22줄 · 줄 손실 0

- **옮기는 것과 다시 쓰는 것을 갈랐다.** 절 몸통 135줄을 `docs/baselines.md` 로 **줄 단위
  그대로** 부어 넣고(파이썬 슬라이스 · 손으로 안 옮겼다), `project.md` 에는 축 5개 표 +
  종료 코드 관용구 + 상수 둘만 **새로 썼다**. 손으로 옮기면 그 자리에서 문장을 다듬게 되고,
  다듬은 것과 잃은 것은 사후에 구별이 안 된다.
- **눈으로 대조하지 않았다** — `comm -23 <(git show HEAD:docs/project.md | sort -u)
  <(cat project.md baselines.md | sort -u)` 가 **비었다**. 249 → **136줄**, 전수 **763 OK**(18.8s).
- **내가 적은 완료 기준 하나가 틀려서 계획서를 고쳤다.** 「`wc -l` 이 135 이상 줄어 있다」는
  **22줄짜리 대체본을 안 센 산술**이다 — 135를 빼고 22를 넣으면 순감은 113이다. 재는 축을
  파일에서 **절**로 옮겼다(이 스텝이 실제로 책임지는 것이 그 절이다). 파일 축 ≤60 은 스텝 2 몫.
- **이 스텝이 만든 낡은 포인터를 같은 커밋에서 닫았다** — `project.md:41` 의 「아래 "품질 기준"
  절에 적었다」가 가리키던 내용이 `baselines.md` 로 갔다. **옮기기는 가리키는 줄을 낡게 한다.**

### 반복 566 — 스텝 2 · `## 명령` 94 → 19줄 · **project.md 249 → 60줄**

- **예산을 역산하고 들어갔다.** 고정분(파일머리 5 + 절머리 5 + 브랜치 4 + 품질 22 + 한도 4 +
  건드리지 2 = 42)을 먼저 세니 `## 명령` 몸통 예산이 **18줄**로 나왔다. 목표에서 거꾸로
  세지 않았으면 "적당히 줄이고" 67줄쯤에서 멈췄을 것이다.
- **e2e 13종은 명령을 한 줄씩 적는 대신 «템플릿 + 이름»으로 접었다**(`e2e/<이름>.py` + 이름 13개).
  계약을 기계로 다시 쟀다 — 옛 문서가 부르던 **e2e 모듈 15개가 전부** 지금도 불리고,
  불리는 이름 중 **실물 `e2e/*.py` 에 없는 것은 0개**다(문자열 대조보다 센 축이다).
- **마지막 1줄을 「지우지 않는다」로 버티지 않았다.** 61줄에서 남은 후보가 파일 머리의 초기화
  주석이었는데 그것이 **지금은 거짓**이다 — 「초기화 시점에 코드가 0줄이라 명령이 없다」.
  거짓 문장을 보존 약속 때문에 남기는 것이 더 나쁘다. 살아 있는 절(「확인 안 된 명령은 적지
  않는다」)만 한 줄로 남겼고, `comm -23` 에 그 **2줄만** 뜨는 것을 확인했다 — 조용한 손실이 아니다.
- **필수 읽기 600/600 → 411/600.** `project.md` 가 `rules/docs.md` 1절의 제 예산 **60 을
  처음으로 지켰다**(4.2배 초과였다). 전수 **763 OK**(18.6s) · 제품 `src/` **0줄**.
