# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**회전 명부는 `digest.md` 의 `## 완료` 절 «아카이브 명부» 줄이 정본이다.**
여기 있던 스물한 회전의 서술(233줄)은 그 줄과 내용이 겹쳤고, 검사가 강제하는 명부도
그쪽 하나뿐이라(`tests/test_docs.py` 의 `ArchiveIndexTest`) **개발 9(반복 269)가 이
자리에서 지웠다** — 회전으로는 300줄 상한을 못 맞추던 세 반복(309 → 372 → 418줄)의
원인이 이 명부였다. **지운 것은 머리말이지 반복 기록이 아니다** — 항목은 여전히
append 전용이고 수정·삭제 금지다. 각 회전의 사유는 `digest.md` 의 같은 줄에, 원문은
`history_<NNN>.md` 에 그대로 있다.

### 반복 414 — 계획 탐색 (YELLOW · 근거 0)

- **8개 출처를 전부 확인했고 열 수 있는 근거가 0 이다**: 실패 테스트 0(전수 647 OK ·
  e2e 21/21) · 린트/타입 수단 없음 · 코드 `TODO` 0(잡힌 1건은 `tests/test_indexer.py:779`
  의 **fixture 문자열**) · `candidates.md` 부재 · 보류 0 · 반복 실패 잔여 1건은 룰 문장으로
  닫힌 절차 항목 · `gh issue list` 빈손.
- **후보는 있지만 전부 여는 조건이 안 찼다** — `[9]` p95 배분(실물 분포가 먼저) · `[7]`
  비ASCII robots(도달 불가) · `[7]` 커서 페이지네이션(**공개 API 갈림길 — 사람 몫**) ·
  `[6]` [R71-3](표에 열을 더하는 날) · `[5]` `_IMPLIED_END` 101건(표를 고치는 날) ·
  `[5]` 예산 500 리터럴(사양 숫자가 움직이는 날). `[8]` 숨은 텍스트는 계획 51 이 닫았다.
- **`discover.md` 2절대로 계획을 만들지 않았다** — 조건이 안 찬 후보를 여는 것은 근거가
  아니라 취향이고, 3절이 밤에 금지한 부류다. 무인 모드라 질문 없이 **보류 후 정지**.
- **결과**: 커밋한 것은 문서뿐 · `src/`·`e2e/` **0줄** · `data/crawl.db` **무개봉** ·
  `main` 직접 커밋 0.
- **다음**: 야간 종료. 사람 몫 셋 — `digest.md` 216줄 정리 · 다음 계획 방향 ·
  `loop/index-e2e-verdict` 병합 여부. 보고서 `docs/reports/night_2026-09-07.md`.

### 반복 415 — 계획 72 `digest-rotate-guard` 수립 (GREEN · 사용자 지시)

- **근거는 측정값**: `digest.md` 216줄 > 상한 200(`rules/docs.md` 3절). 반복 414 가
  「사람 결정 대기」로 올린 첫째를 사용자가 결정했다 — 닫힌 기록에 한한 삭제 위임.
- **처방 넷 중 셋이 실측으로 떨어졌다.** A(느슨한 백틱 세기)는 계획 70 이 이미 반증.
  **B(하한을 실측치에 붙인다)는 오늘 반증** — 후보 포인터 **10개가 10개 다 취소선**이라
  삭제 정책이 모집단을 영구히 0 으로 만든다. 「실측치」가 0 이면 하한 0 이고, 그것은
  그 못이 막으라고 세워진 조용한 초록 자체다. B 는 후보 절이 append-only 라는 전제
  위에 있었고 그 전제가 오늘 틀렸다. **C(역방향 — `index.md` 완료 슬러그가 후보 절에
  없어야)는 오늘 처음 재서 반증** — 회전 뒤에도 오탐 4건이고 그중 `indexer` 는 제품
  모듈 이름과 계획 슬러그가 같은 글자다.
- **시뮬레이션이 사이 반복의 빨간 저장소를 미리 보여줬다**: 취소선 18줄을 지운 사본에서
  전수 647 중 **정확히 1건**(`test_pointer_extractor_still_bites`, `0 not >= 9`)만 실패.
  다른 축은 전부 조용해 회전이 밟는 자리가 하나임이 확정됐다. 사본은 `/tmp` 라
  `docs/digest.md` 는 216줄 그대로 원복했다.
- **덤**: 후보 절의 「화면에 안 보이는 텍스트가 «근거 문단» 으로」 항목이 취소선 없이
  살아 있는데 `plan_hidden-passage` 는 `완료` 다 — 문구가 「…가 앞 절반을 연다」라
  엄격 포인터가 안 문다. 계획 70 이 잠복이라 등재한 드리프트의 **실물 표본**이다.
  다만 뒷 절반은 진짜 열려 있어 닫힌 기록이 아니라 이 계획은 손대지 않는다.
- **다음**: 설계 phase — 남은 처방 D 의 **값**을 가른다. 58 에 붙이는 것은 오늘 사고의
  반복이라 그 자체가 갈림길이다.

### 반복 416 — 계획 72 설계 (GREEN · `design_digest-rotate-guard.md`)

- **갈림길은 하나였다** — 회전은 안 갈린다(지울 줄이 실측으로 확정). 갈리는 것은
  하한 못의 **모집단과 값**이다.
- **탐침 둘이 갈랐다**(커밋 안 함 · 원복함). ① 코드 상수 `CANDIDATE_HEAD` 변이 →
  **4건 RED**(합성 `StrikeGapTest` 셋 + 실물 하나) ② **실물 문서**의 첫 후보 절 머리만
  드리프트 → **1건 RED**, `test_pointer_extractor_still_bites` **하나뿐**.
  **②가 D3(못을 지운다)을 반증한다** — 합성 넷은 문서 드리프트에 전부 조용했다.
  상수와 실물 문서를 잇는 자가 이 못 하나다. 「축을 옮겨야지 지우면 안 된다」의 자리.
- **결정 D5 — 못을 후보 절 «머리 개수» 위에 세우고 하한 2.** 가정 탐침도 참이다:
  머리 수가 현재 2 · **회전 후에도 2**(거짓 RED 없음) · 머리 하나만 드리프트하면 **1**(RED).
- **버린 이유**: A 오탐 21 · B 회전 뒤 실측치 0 · C 오탐 4 · **D1(목록 줄, 값 58)은
  오늘 사고의 반복**(목록 줄은 회전이 지우는 쪽이다 76→58) · **D2(값 1)는 머리 하나
  드리프트를 못 문다**(탐침 2 상태에서 목록 줄은 여전히 1 이상이라 조용) · D3 위 참조.
- **천장을 적고 나간다**: 이 못은 「후보 절이 살아 있다」를 재지 「그 안의 줄이 규약대로
  적혔나」는 안 잰다. 문구 드리프트는 문구가 아니라 **규약**을 바꿔야 닫힌다 — 범위 밖.
- **다음**: 개발 1/2 — `candidate_lines` 분리 + `CANDIDATE_HEAD_FLOOR = 2`. TDD 로 RED 먼저.

### 반복 417 — 계획 72 개발 1/2 (GREEN · 못을 옮겼다)

- **`STRIKE_POINTER_FLOOR = 9` 를 빼고 `CANDIDATE_HEAD_FLOOR = 2` + `candidate_heads()`**
  를 세웠다. 전수 647 → **648 OK rc 0**(못 하나를 빼고 못 하나 + 합성 갈래 하나를
  더했다). `README.md` 의 건수 줄도 함께 맞췄다 — `test_readme` 가 그 자리를 잡는다.
- **다섯 판으로 밟았다**(커밋 안 함 · 전부 원복): ① 실물 첫 절 머리 드리프트 **RED**
  ② 실물 둘째 절 머리 드리프트 **RED** ③ **회전 시뮬레이션 OK — 거짓 RED 없음**
  ④ `startswith("## ")` 로 넓히는 변이 **RED**(합성이 죽인다) ⑤ 하한 `2`→`0` + 드리프트
  **OK, 생존**.
- **⑤ 는 천장이다.** 하한 상수를 0 으로 내리면 못이 이빨을 잃고 아무도 안 운다.
  형제 `VERDICT_ROW_FLOOR` 가 똑같이 갖는 성질이고, 닫으려면 «못을 지키는 못» 이 되어
  순환한다. 스텝 2 가 digest 를 여는 김에 후보로 등재한다.
- **설계 6절에서 하나를 덜어냈다** — 거기 적힌 `candidate_lines` 분리는 소비자가
  `candidate_pointers` 하나뿐이라 안 만들었다. D5 의 둘째 소비자는 줄이 아니라 머리를
  센다. 구현 하나짜리 추상은 다음 편집이 갈라 놓을 짐이다. 설계가 D1/D2 모양을
  가정한 채 쓰인 자리라 개발이 실물에서 걷어냈다.
- **다음**: 개발 2/2 — `digest.md` 회전(취소선 18줄 + `## 완료` 요약 7줄, 회전 기록 1줄 추가).

### 반복 418 — 계획 72 개발 2/2 (GREEN · 회전 216 → 193줄)

- **상한 200 을 처음으로 실제로 맞췄다.** 앞 두 회전이 못 맞춘 이유(지울 완료 항목이
  여덟 줄뿐 · 무인 모드는 삭제를 안 한다)를 사용자가 이 건에 한해 열어 줬다.
  지운 것은 **닫힌 기록뿐** — 후보 두 절 취소선 **18줄** + `## 완료` 계획별 요약
  **7줄**(계획 63~69). 더한 것은 회전 기록 1줄 + 새 후보 1줄.
- **안 건드린 것**: 아카이브 명부 · 회전 기록 2줄 · 보류 · 반복 실패 · **열린 후보 58줄**
  · `index.md` · `plan_history_*`(원본이 거기 남아 있다는 것이 삭제의 전제다).
- **회전이 실물 갈래를 하나 열었다.** 계획 70 이 등재한 후보 줄에 규약대로 포인터를
  달았더니 실물 포인터가 **1개**가 됐고, 그 계획이 `진행` 이라 `strike_gap` 의
  **「진행 중이면 안 문다」 오탐 축이 실물에서 처음 밟혔다**(결과 `None`). 지금까지
  합성에만 표본이 있던 갈래다.
- **위임 범위를 과장한 문구를 같은 반복에서 고쳤다.** 회전 기록 초안이 「회전 정책이
  한 칸 넓어졌다」라고 적었는데 사용자 위임은 **이 건 한 번**이다. `rules/docs.md`
  3절 문장은 안 바뀌었고 다음 회전은 다시 사람에게 묻는다고 고쳐 적었다.
- **전수 648 OK rc 0.** 다음: 테스트 phase.

### 반복 419 — 계획 72 테스트 2/2 (GREEN · 갭 탐색 + 변이 1판 · history 회전)

- **전수 648 OK rc 0.** 새 테스트 0 — 이 phase 의 물음은 「못을 옮긴 것이 단언을
  낮춘 것은 아닌가」(`test.md` 6절)였고 말 대신 변이로 쟀다.
- **변이 탐침**: `STRIKE_POINTER` 를 `열었다` → `개봉했다` 로 무력화 → **3건 RED**
  (`StrikeGapTest` 의 `test_only_candidate_list_lines_are_counted` ·
  `test_pointer_to_an_unlisted_plan_is_a_gap` · `test_unstruck_closed_candidate_is_a_gap`).
  **옛 `STRIKE_POINTER_FLOOR` 는 그 축의 유일한 파수꾼이 아니었다** — 제거는 낮춘
  것이 아니라 옮긴 것이라는 설계 주장이 실측으로 맞다. 커밋 안 함 · 원복 확인.
- **갭 2건 등재, 8점 이상 0건**: [7] `rules/docs.md` 3절 문서 상한(200·300)을 재는
  자가 0개 — **오늘 사고 자신**이고, 8점이 아닌 이유는 처방이 무인 루프를 교착시켜서다
  (상한 초과 RED 를 야간이 스스로 못 푼다). [4] `candidate_heads` 접두 일치가
  「후보…」로 시작하는 절이 하나 더 생기면 드리프트를 가린다.
- **`history_current.md` 회전** — 갭 [7] 을 재다 **352줄**(상한 300)을 발견했다.
  반복 404~413 을 `history_072.md` 로 밀어내 **121줄**. 계획 72 는 이것을 범위 밖으로
  적어 뒀지만 회전은 계획이 아니라 매 반복의 기록 의무라 했고, **커밋을 나눴다.**
  `digest.md` 는 갭 2줄 + 회전 기록 1줄을 더해 **196줄**(상한 아래).
- **다음**: 리뷰 phase. 볼 자리 셋을 status 에 적었다.

### 반복 420 — 계획 72 리뷰 2/2 (GREEN · 6건 중 5건 확증 · 승인 필요 0)

- **백지 패스를 별도 세션에 넘겼다** — diff 와 수정 파일만 주고 계획·설계·상태 문서는
  주지 않았다. 넷을 받아 셋 확증, 하나 기각(README 건수 대조는 같은 커밋 안에 있다 —
  필터된 diff 만 본 오탐).
- **핵심 확증**: 못을 절 머리로 옮기며 **실물 포인터 모집단을 재는 자가 0**이 됐다.
  별도 세션이 `candidate_pointers` 를 `return []` 로 죽여 `StrikeSyncTest` 2건이
  그대로 통과함을 실측했다. 코드 변이는 합성 셋이 물지만 **문서 드리프트는 아무도 안 문다.**
  옛 후보 `[5]` 는 이제 없는 상수를 표제로 들고 있어 그 축을 못 진다 — 새 후보 `[6]`
  으로 오늘의 말로 다시 열었다. `[5]` 는 e2e 에서 취소선을 받고, **그때 열린 위험이
  함께 묻히지 않게 하는 것이 `[6]`** 이다.
- **패스 B 둘**: 설계 6절 이탈(`candidate_lines` 미구현)이 설계 문서에 없었다 → 인용
  블록으로 적었다(문단은 안 지웠다 — 설계가 어디서 틀렸는지가 기록이다). 계획서의
  「`index.md` 무접촉」이 문자 그대로는 거짓이었다 → **자기 행 등재·스텝 칸 갱신은
  규약**이고 지키는 것은 「남의 행 무접촉」이라고 정정. 실측: `index.md` diff 는 자기 행 한 줄.
- **낡은 주석 1건**: `STRIKE_POINTER` 실측이 회전으로 낡았다(11자리/9 → 2자리/1) → 갱신하고
  **이 수는 회전이 흔든다**를 적었다. 못을 거기 세우면 안 되는 이유 자체다.
- 전수 **648 OK rc 0** · `digest.md` **197줄** · 다음은 e2e(마지막 스텝).

### 반복 421 — 계획 72 e2e 2/2 (DONE · 사본 5막 · 초록 2 · 빨강 2)

- **저장소 밖 사본에서 5막**(`git archive HEAD`). 워킹트리 무변경, 저장소는 읽기만.
  ① 마감 편집 리허설(`index.md` 완료 + e2e 판정 + 후보 `[5]` 취소선) → **648 OK rc 0**.
  ② 후보 첫 절 머리 드리프트 → **RED 1건** `test_candidate_heads_still_found`
  (`1 not greater than or equal to 2`). 회전 전에는 이 편집이 조용한 초록이었다.
  ④ `index.md` 만 완료로 바꾸고 취소선 안 달기 → **RED 1건**
  `test_closed_candidates_are_struck_through`. **①과 ④가 짝이다** — 같은 마감 편집을
  취소선 유무만 갈라 두 번 했고 하나는 초록, 하나는 빨강이다. 가드가 이 파이프라인에서
  **실제로 도달한다**는 뜻이고 계획 70 이 세운 자가 실물 마감 편집 위에서 처음 울었다.
  ③ 지워진 26줄의 엄격 포인터 **10개** 원본 대조 → `index.md` 10/10 · `plan_history_*`
  10/10. 삭제의 전제가 실측으로 성립한다. ⑤ 후보 두 절 정적 실측 → 목록 62줄 ·
  **취소선 0줄** · 포인터 1(진행 중인 자기 자신) · `strike_gap` None.
- **새 e2e 스크립트 0** — `src/`·`e2e/` 0줄이라 3절 면제. `ls e2e/*.py` 는 **21** 그대로.
- **안 잰 것을 「통과」로 안 적었다**: 경량·성능·디자인은 **해당 없음**(제품 코드 0줄),
  린트·타입체크는 이 저장소에 설정이 없어 **검증되지 않음**.
- **다음**: 계획 72 **DONE**. 아카이브가 막 ① 의 문안을 그대로 옮긴다 — 취소선은
  「상수가 사라졌다」지 「문구 드리프트가 닫혔다」가 아니고, 살아남은 축은 `[6]` 이 든다.

### 반복 422 — 계획 72 아카이브 · 정지 (사용자 지시 완료)

- `plan_digest-rotate-guard.md` → `plan_history_058.md`,
  `design_digest-rotate-guard.md` → `design_history_058.md`.
  `index.md` 행 `완료`·e2e 칸 `통과`. `digest.md` 완료 요약 1줄 + 후보 `[5]` 취소선.
- **취소선의 뜻을 좁게 적었다** — 「표제의 상수가 사라졌다」이지 「문구 드리프트가
  닫혔다」가 아니다. e2e 막 ④ 대로 이 취소선은 **기제가 강제**하므로(안 달면 전수 RED)
  뜻을 안 적으면 열린 위험이 그 밑에 묻힌다. 살아남은 축은 후보 `[6]` 이 든다.
- **정지.** `night <할 일>` 은 준 것만 하고 멈춘다 — 새 계획을 탐색하지 않았다.
  야간 지표: 반복 415~422(8회) · RED 0 · 재시도 0 · 계획 1 완료 · 커밋 7.
- **사람 몫 셋**: ① digest 회전 위임은 이 건 한 번이었고 지금 **198줄**로 여유가 두 줄이다
  — 다음 회전은 사람이 연다. ② 후보 `[7]` 문서 상한을 재는 자 0개(①이 먼저 정해져야
  못을 세운다). ③ 후보 `[6]` 실물 포인터 모집단을 재는 자 0개(규약 변경이라 별건).

### 반복 422 후기 — 정지 사유 어휘 정정 (기록만, 반복 카운터 무증가)

- `metrics.jsonl` 이 `"stop":"사용자 지시 완료"` 로 나갔는데 스크립트가 모르는 말이었다.
  `계획 소진` 으로 접으려다 **되물렀다** — 소진(정말 없다)·막힘(못 연다)과 아침 처방이
  달라, 접어 세면 이 표가 답하려는 「승인 게이트가 병목인가」가 흐려진다.
- `metrics.md` 정지 사유 표에 **`지시 완료`** 칸 신설(`탐색 막힘` 과 같은 취급 — 표가
  스크립트 `STOP_REASONS` 6칸보다 넓고, 경고는 오타용이지 새 칸 금지용이 아니다).
- status.md 커밋 수 오기 정정(**7 → 10**, `main..HEAD` 실측). ctx 는 statusLine 이
  되살아나 **77** 실측으로 갱신(정지 시점의 85 는 멈춘 파일 위의 추정치였다).


### 반복 423 — 야간 탐색 · 짧은 경로 1건 · 컨텍스트 정지

- **탐색 8순위 전수.** 1~3 은 명령 한 번씩이라 직접 봤다 — 실패 0(648 OK) · 린트 설정 없음 ·
  코드 `TODO` 0(유일한 히트는 `tests/test_indexer.py:779` 의 **fixture 문자열**이라 근거 아님).
  4 파일 없음 · 5 보류 0(주석뿐) · 8 이슈 트래커 없음. `docs/patches/` 도 없어 되살아날 근거 0.
- **높은 점수 후보 넷은 전부 「여는 조건」이 오늘 안 섰다** — `[9]` 실물 분포 없음(+성능
  최적화라 야간 금지) · `[8]` 남은 뒷부분은 `_INLINE_TAGS` 가 색인 경로와 공유라 재색인 ·
  `[7]` 문서 상한은 야간이 그 RED 를 못 푼다 · `[6]` 은 표에 열을 더하는 날이 조건이다.
- **짧은 경로로 낡은 곁가지 하나를 고쳤다.** 후보 `[7]`(robots 비ASCII)이 「함께 열면 싸다」고
  가리킨 `_fetch_robots` 의 `resp.read()` 무상한 건은 **이미 닫혀 있었다** — `MAX_ROBOTS_BYTES
  = 512_000`(`robots.py:14`), `git log -S` 로 계획 `robots-read-cap`(`eb6697e`) 확인.
  취소선 + 닫은 근거를 부기했고 줄 수는 **198 유지**. 「기록된 답을 실행 전에 다시 재라」의
  다섯 번째 적용이다 — 이번엔 처방이 아니라 **곁가지의 전제**가 낡았다.
- **정지: 컨텍스트.** 시작이 이미 79% 였고 탐색 끝에 83~84%. 5h 30% · 7d 45% 는 여유.
  전수 **648 OK rc 0** · `src/`·`e2e/` 0줄.

### 반복 424 — 짧은 경로 · 후보 `[5]` 를 실측하고 닫았다

- **여는 대신 쟀다.** 후보 `[5]` 의 「스위트 벽시계의 68% 가 테스트 하나」는 2026-09-01
  값(462건 11.67초)이다. 오늘 재니 대상 단독 **8.060초** · 전체 648건 **15.923초** ·
  1건만 뺀 647건 **7.725초** → **50.6%**. 절대값 8초는 그대로고 분모만 커졌다.
- **거래를 양쪽 다 쟀더니 손해라 닫았다.** e2e 로 옮겨 버는 것은 스텝당 8초(한 스텝의
  실비용은 LLM 왕복이라 잡음)이고, 치르는 것은 「락을 실제로 기다린다」는 **행동** 검증이
  계획당 한 번으로 주는 것이다. 항목이 대체재로 지목한 `test_every_connection_waits_the_
  same_thirty_seconds` 는 **값**만 고정한다 — 대체가 아니다.
- **일반화 한 줄을 남겼다**: 비율로 놀란 항목은 절대값으로 다시 잰다. 분모는 저장소가
  자라기만 하는 축이라 비율은 저절로 내려간다.
- `digest.md` 한 줄 교체, 줄 수 **198 유지**. 전수 **648 OK rc 0** · `src/`·`e2e/` 0줄.

### 반복 425~428 — 계획 73 `realweb-corpus` · 실물 코퍼스로 후보 셋을 판정했다

- **범위 없는 크롤러를 보내기 직전에 잡았다.** 계획서에 「`src/` 를 안 건드린다」고 적고
  착수했는데 `links.extract`→`frontier.add` 에 **범위 개념이 없었다** — 위키백과 한 장의
  외부 링크가 수백 개라 `--max 400` 은 4 사이트가 아니라 임의의 200여 도메인을 받아 온다.
  옵트인 `--same-site` 를 `Frontier.add` **한 자리**에 넣었다(기본값 무변 · 테스트 5건).
- **실물 400장 / 109초 / rc 0 · 전부 200 · 외부 0건.** 색인 361문서(39건은 위키백과
  편집 폼의 `noindex` — **처리가 실물에서 처음 발동해 옳게 걸렀다**).
- **셋 다 열렸고 둘이 뒤집혔다.** `[9]` `/search` 는 총 p95 의 **35.9%**(「대부분」이라던
  판정이 틀렸고, 예산의 8.7% 라 지금 문제도 아니다) · `[8]` 비-본문 **58.9%** 지만 주범은
  숨은 텍스트가 아니라 **목차·내비·제목**(기록된 처방으로 안 잡힌다) · `[6]` 이름표는
  실패의 **88.4%** 를 회수하는 축이라 **지울 것이 아니라 아직 안 쓴 것**이다.
- **틀린 방향이 서로 달랐다** — `[9]` 는 문제를 과대평가했고 `[6]` 은 자산을 쓰레기로 봤다.
  둘 다 원인은 하나: **합성 64문서가 `p` 중심이고 질의어를 문서마다 수천 번 반복한다.**
  「측정 하네스가 재지 않는 세계가 곧 버그가 숨는 곳」의 세 번째 실증이고, 이번엔 그
  하네스가 코퍼스 자체였다.
- 전수 **653 OK rc 0** · `docs/e2e/realweb-corpus/result.md` 94줄 · digest 199줄(상한 200).

### 반복 429~433 — 계획 74 `passage-prefer-body` · 전제가 깨졌고 자를 바꿨다

- **내가 계획 73 에서 낸 88.4% 가 틀렸다.** 재던 스크립트는 캡 없이 파싱하고 「담기만」
  하면 셌는데, 실물 선택기는 35,000자만 파싱하고 점수로 고른다. 같은 자로 재면 회수
  가능 43건 중 **3건** · 점수로 이기는 것 **0건**. 정정 커밋 `48c8548` 로 digest 와
  계획 73 e2e 결과를 고쳤다 — **「여는 조건」을 재는 자도 실물 코드와 같아야 한다.**
- **규칙 넷을 다 재고 하나도 안 골랐다.** 블라인드 라벨(규칙 이름을 가리고 블록만 판정)
  이라 「내 규칙이 이기는 라벨」을 못 만들었고, 그래서 넷이 전부 무의미하다는 결론이 났다.
- **진짜 자리는 캡이었다.** `MAX_PASSAGE_HTML` 35,000 **바이트** 대 실물 중앙값 159,008
  → **92% 가 잘리고** 남은 것은 `<head>`·내비·목차. 자를 **태그**로 바꿨다
  (`MAX_PASSAGE_TAGS = 3_000` · `_clip_by_tags()` 1.13MB 에 0.70ms = 파싱의 1%).
  근거 **73→96건** · 본문 `p` **41.1→81.2%** · `/passages` p95 43→**138ms**(예산 27.6%) ·
  **최악 모델 10건은 350→120ms 로 좁아졌다.** 전수 **655 OK rc 0**.

### 반복 434 — 계획 75 `project-const-drift` 착수 · 탐색이 실측에서 근거를 냈다

- **후보 목록이 아니라 오늘 실측이 근거다.** 8순위 전수는 1~5·8 전부 0건이고(655 OK ·
  린트 없음 · 코드 `TODO` 0 · `candidates.md` 없음 · 보류 0 · 이슈 없음) 6·7 의 열린
  후보는 여는 조건이 안 섰다. 그런데 **계획 74 의 상수 변경을 코드 쪽에서 확인하다가**
  `docs/project.md:188` 이 `indexer.MAX_PASSAGE_HTML` **= 35,000자**라고 적어 둔 것을 봤다 —
  실제는 **2,000,000** 이고 자르는 자는 `MAX_PASSAGE_TAGS = 3_000` **태그**다.
- **세 번째라 규율에서 기계로 옮긴다.** `git log -- docs/project.md` 의 마지막 커밋은
  계획 58(`724d8c8`)이고, 계획 57·58 은 이 자리를 손으로 맞췄다(`e6f375c` 「기록 자리
  둘을 오늘 값으로 맞춘다」). **아무도 `project.md` 를 입력으로 읽지 않는다** —
  `test_docs.py` 에 gap 검사가 넷 있는데 상수 인용을 재는 것만 없다. digest `[6]`
  「문서를 입력으로 읽는 검사가 한 벌은 있어야 한다」가 이 자리를 이미 지목해 뒀다.
- 스텝 3(판정 함수 → 실문서에 물림 → 문단 갱신). 설계 생략(트리거 미해당 · `src/` 0줄).
  **파생 숫자는 안 잰다** — 상수 값이 닻이고, 닻이 움직이면 사람이 문단을 다시 읽는다.
- `src/` 0줄. 전수 **655 OK rc 0**.

### 반복 435 — 계획 75 스텝 1/3 · `const_gap` 을 세우고 변이 4종으로 이빨을 확인했다

- **`docs/project.md` 의 `` `mod.CONST` = 숫자 `` 인용을 코드의 정수 리터럴과 대조한다.**
  `step_gap`·`strike_gap`·`verdict_gap` 과 같은 모양(순수 함수 + 갈래 테스트)이라
  고를 갈림길이 없었다 — 저장소에 있는 패턴을 그대로 썼다.
- **임포트가 아니라 소스를 읽는다.** 인용 대상은 `e2e/` 에도 살 수 있고 그쪽은 임포트에
  부작용이 있다. 문서가 인용하는 것도 대입문의 리터럴 그 자체다. 천장은 주석에 적었다 —
  정수 리터럴만 읽고, 식으로 바뀌면 **조용히 통과시키지 않고 「못 읽었다」로 보고한다**.
- **갈래 7건 중 셋이 「안 물어야 하는 것」이다** — `design_check.PAIRS`(값 없음) ·
  `fetcher.MAX_BYTES`(2MB)(괄호 표기) · 줄바꿈+굵게 낀 실물 꼴. 값 없는 인용까지 물면
  문서가 상수를 못 부르게 된다. 그리고 살아 있는 상수(`indexer.MAX_PASSAGE_TAGS`)로
  재는 갈래를 일부러 넣었다 — 가짜 이름만으로 재면 **코드에서 값을 읽어 오는 배선**은
  안 재게 된다.
- **변이 4종이 전부 죽는다**(`PYTHONPYCACHEPREFIX` 를 매번 새로 줬다 · 심기 전
  `count(old)==1` 로 원문 존재를 단언하고 원복은 무조건 돌렸다): 판정 무력화 1건 ·
  `= 숫자` 요구 제거 1건 · 없는 상수 묵인 1건(**깨끗한 실패가 아니라 `TypeError` 로
  죽는다** — 물긴 물지만 메시지가 곱지 않다) · 쉼표 무시 3건.
- **README 의 「단위 655건」이 그 자리에서 울었다** — 655 → **662건**. `readme-commands`
  계획이 세운 가드가 또 값을 냈다.
- `src/` 0줄. 전수 **662 OK rc 0**.
