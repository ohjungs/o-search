# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**반복 97~122 는 `history_006.md`, 123~128(계획 020 `deadline` 전체)은
`history_007.md`, 계획 21~24(짧은 경로 3건 + 보류 패치 소진, 2026-08-29 14:20~17:40)는
`history_008.md` 로 밀려났다** (2026-08-29 회전 3회).
**계획 25~29 는 `history_009.md`, 계획 30·31(짧은 경로) + 반복 152 탐색 +
계획 33 `clock-injection` 의 계획·설계·개발 1~4 는 `history_010.md` 로 밀려났다**
(2026-08-30 회전, 393줄 → 139줄. 상한 300줄을 넘겨 놓고 두 반복을 갔다).
**계획 33 의 마지막 네 반복(개발 5 · 테스트 1 · 리뷰 1 · e2e 1)은 `history_011.md`**
(2026-08-30 회전, 322줄 → 210줄. 상한을 넘긴 채 반복 두 번을 또 갔다 — 스텝 3 이
"미결(문서 위생)" 로 넘긴 것을 스텝 4 가 닫았다).
계획 33 의 전 phase 서술은 `plan_history_020.md` 의 `## 기록` 에 그대로 있다.
**계획 34 `graceful-interrupt` 의 앞 여덟 반복(계획 1 · 설계 1 · 개발 1 세 번 · 2 · 3 · 4)은
`history_012.md`** (2026-08-30 회전, 300줄 → 85줄. **상한에 정확히 닿아 넘기기 전에 돌렸다** —
앞 두 번은 넘긴 채 반복을 더 갔다).
**계획 34 의 마지막 세 반복(테스트 1 · 리뷰 1 · e2e 1)은 `history_013.md`**
(2026-08-31 회전, 307줄 → 244줄. 상한 300 을 넘긴 그 반복에서 바로 돌렸다 —
`digest ## 반복 실패` 가 지적한 "회전이 두 반복 늦었다" 를 안 되풀이한다).
**계획 35 `deadline-stop` 전체 여섯 반복(계획 · 개발 1 · 테스트 2 · 리뷰 · e2e)은
`history_014.md`** (2026-08-31 회전, 336줄 → 188줄. 상한 300 을 넘긴 채 테스트 반복
하나를 더 갔고 **리뷰가 그것을 잡았다** — 회전은 phase 룰이 아니라 문서 위생이라
`status.md` 의 "집안일 미결" 로만 있었다. 완료된 계획 하나를 통째로 미는 경계로 잘랐다).

**계획 36 `signal-budget-cover` 전체 두 반복과 계획 37 `indexer-interrupt` 전체 여섯 반복은
`history_015.md`** (2026-08-31 회전, 318줄 → 96줄. **계획 38 리뷰가 잡아서 돌렸다** —
테스트 스텝이 305줄인 것을 알고 `status.md` 에 적어 둔 채 그대로 갔다. 393·322·336·318,
네 번째 지각이다. 완료된 계획 둘을 통째로 미는 경계로 잘랐다).

**계획 38 `digest-rotate` 전체 다섯 반복과 계획 39 `indexer-lock` 전체 다섯 반복은
`history_016.md`** (2026-09-01 회전, 299줄 → 145줄. **계획 40 리뷰가 제때 돌렸다** —
테스트 반복이 "다음 반복이 회전한다" 고 적어 둔 그대로다. 393·322·336·318 은 지각이었고
이번은 아니다. 완료된 계획 둘을 통째로 미는 경계로 잘랐다 — 계획 40 을 여는 반복 198 부터 남는다).

**계획 40 `exit-code-contract` 전체 여섯 반복(198~203)은 `history_017.md`** (2026-09-01
회전, 291줄 → 새 항목을 넣기 전에 돌렸다. **테스트 phase 가 자기 손으로 돌린 첫 회전이다** —
205 가 "다음 반복이 회전 자리다" 라고 적어 둔 그대로다. 완료된 계획 하나를 통째로 미는 경계).

---

## 반복 204 — 계획 41 `crawl-db-guard` 설계 1/1 (2026-09-01)

- phase: 설계 · signal GREEN · `src/`·`tests/` **0줄**
- 한 것: `docs/design_crawl-db-guard.md` — **대안 C**. `crawl()` 이 `Store(db_path)`
  **한 줄만** `except (sqlite3.Error, OSError)` 로 감싸고 `crawl.py` 의 도메인 예외
  `StoreOpenError` 를 던진다. `main()` 이 그것만 잡아 안내 한 줄 + **rc 1**.
  제품 diff 는 `crawl.py` 한 파일이고 `store.py`(B)·`README.md` 는 0줄이다.
- 검증: 단위 **462건 OK 11.990초** · `data/crawl.db` sha256 무변경 · `digest` 50항목·200줄.
- 배운 것:
  ① **탐침이 계획의 표를 두 칸 넓혔다 — 다섯째 상황·셋째 던지는 줄.** 다른 크롤이
  쓰고 있으면 `store.py:24`(`execute(SCHEMA)`)가 **30.68초** 뒤 `database is locked` 를
  던진다. 199 는 *값*을, 203 은 *예외 타입*을, 여기서는 ***던지는 줄***을 안 세었다 —
  **같은 실패가 세 반복 연속이고 매번 한 축씩 더 미세해졌다.** 일반화: 열거의 완전성은
  "무엇을 세었나" 가 아니라 **"세는 축을 누가 정했나"** 의 문제다. 축은 코드가 정한다.
  → 처방도 그 모양으로 바꿨다. 줄 번호를 열거하지 않고 **생성자 호출 한 줄 전체**에
  그물을 건다. 넷째 던지는 줄이 생겨도 자동으로 덮인다.
  ② **가장 짧은 그물이 가장 넓은 그물이었다.** A(`main` 이 `crawl(...)` 통째)는 한 줄
  더 짧은데, 크롤 도중의 `store.upsert`(`crawl.py:315`, 메인 스레드)가 락에 걸려 죽는
  것(실측 30.6초)까지 *"DB 를 열 수 없다"* 로 오진하고 `수집 N 페이지` 도 삼킨다.
  **ponytail 사다리는 "짧은 diff" 가 아니라 "높은 칸" 을 고르라고 한다** — 여기서 높은
  칸은 사다리 2번(이미 있는 관용구 `NoUsableSeedsError` 재사용)이었고 그것이 C 다.
  ③ **못 재는 갈래는 만들지 않는다.** 락 안내를 `indexer` 처럼 가르는 안은 `Store` 의
  `timeout=30`(한도 밖) 때문에 검사 하나가 **30.6초** — 단위 전체(11.9초)보다 길다.
  버린 이유를 값으로 적고 여는 조건(timeout 을 손대는 계획)을 함께 남겼다.
  ④ **계획의 잠정 판정이 뒤집히지 않고 확인됐다** — 7절 "새 e2e 0개" 가 맞다.
  `Store` 는 `crawl()` 의 첫 줄이라 네트워크 전에 던지고, RED 2번은 "부모가 일반
  파일" 이라 `chmod` 조차 없다(둘 다 실측 재현). **잠정으로 적어 두면 확인이 한 줄로
  끝난다** — 203 이 [B1] 을 받아 미정 대신 잠정을 적은 것이 여기서 값을 냈다.
- **한도 밟았다 — 러너를 `| tail -4` 로 감쌌다. 다섯 번째다.** `status.md` 한도에서 그
  조항을 **읽은 그 반복에** 밟았다(맨몸 재실행으로 462 OK 확인). `digest ## 반복 실패`
  를 4 → **5회**(뒤의 넷이 연속). 199 는 "적어 놓고 밟았다", 203 은 "다음 반복에 또",
  204 는 "읽은 그 반복에". **문장·한도·경고 세 형태가 다 실패했다** — 남은 것은 기제뿐인데
  훅이 저장소 밖이라 루프가 못 고친다. 사람이 열어 줘야 닫히는 항목이다.
- 다음: 개발 1/1. RED 3건 → `crawl.py`. 단위 462 → **465건**. 변이 M1·M2 가 각각
  **하나씩만** 죽여야 한다.


## 2026-09-01 12:15 | crawl-db-guard | 개발 1/1 | 시도0
- 한 일: 설계 계약을 글자 그대로 구현했다. `src/websearch/crawl.py` **한 파일 · 실질 8줄**
  — `import sqlite3` · `class StoreOpenError` (`NoUsableSeedsError` 옆, docstring 에 "환경이라
  rc 1, 명령줄 오류 2 와 가른다") · `crawl()` 의 `Store(db_path)` 를 `except (sqlite3.Error,
  OSError)` 로 감싸 `from err` 로 다시 던지기 · `main()` 에 `except StoreOpenError` → rc 1.
  문구는 `indexer.py:264` 와 글자까지 같다. `tests/test_crawl.py` 에 `TestUnopenableDb` 3건.
  `README.md:66` 은 `단위 462건` → `465건` **한 줄**(rc 1 칸은 계획 40 이 이미 참으로 만들었다).
- 결과: 단위 462 → **465건 OK 11.732초** · e2e `crawl_e2e`(수집 15) · `indexer_e2e` 통과 ·
  `data/crawl.db` 무변경(mtime 08-29) · `digest` 50항목 **정확히 200줄**.
  **TDD 0절대로 갔다** — 세 테스트를 먼저 쓰고 `AttributeError: module 'websearch.crawl' has
  no attribute 'StoreOpenError'` 로 **RED 를 눈으로 본 뒤** 구현했다.
  실제 CLI 실측(임시 cwd 4곳): 네 상황 전부 **rc 1 · stderr 1줄 · 트레이스백 없음**.
  `DB 를 열 수 없다: data/crawl.db — file is not a database` / `unable to open database file`
  (쓰기 불가·디렉터리) / `[Errno 17] File exists: 'data'`. **전엔 14~16줄이었다.**
- 배운 것:
  ① **변이가 설계의 예측 한 칸을 정정했다.** M1(`OSError` 제거)→2번만 · M2(`return 1`→`2`)
  →3번만 은 설계표 그대로인데, **M3(`try` 통째 제거)는 "셋 다" 가 아니라 1·2번만** 죽인다.
  3번이 `crawl.crawl` 을 목으로 갈아 **`main` 의 번역만** 재기 때문이다 — 그물과 번역은
  다른 축이라 같은 변이에 함께 죽을 이유가 없었다. 셋 다 사망이라 결론은 안 바뀌지만,
  **설계가 예측한 변이표도 실측 대상이다**(203 이 "잠정을 적어 두면 확인이 한 줄" 이라
  적은 것의 반대 방향 사례 — 적어 둔 것이 틀리면 정정이 한 줄이다).
  ② **RED 셋이 같은 줄에서 죽었다.** 클래스가 없으니 세 테스트가 전부 `AttributeError` 다 —
  "실패를 봤다" 는 섰지만 **셋이 서로 다른 것을 재는지는 그 시점에 증명되지 않았다.**
  그것을 증명한 것은 RED 가 아니라 **변이 셋**이다(각각 다른 테스트를 죽였다).
  TDD 0절의 "실패를 눈으로 본다" 는 필요조건이고, 충분조건은 변이 쪽에 있다.
- **또 밟았다 — 러너를 `| tail -5` 로 감쌌다. 여섯 번째다.** README 를 고친 뒤 **재확인**
  실행에서 나왔다(첫 실행은 맨몸이었고 465건 중 README 1건 FAIL 을 그것으로 봤다).
  맨몸 재실행으로 465 OK 확인. `digest ## 반복 실패` 를 5 → **6회**(뒤의 다섯이 연속).
  **새 사실 한 칸**: 앞 다섯과 달리 이번 파이프는 첫 실행이 아니라 **"이미 봤으니 끝만
  보면 된다" 는 재확인**에서 나왔다 — 조항을 아는 것과 무관하게 *두 번째 실행*이 방아쇠다.
- 다음: 테스트 1. 붙일 곳을 찾는다 — 그물(`sqlite3.Error` 갈래)·번역(rc)·문구 동일성
  (`indexer.py:264` 와 글자까지)이 각각 몇 건에 붙들려 있는지, 특히 **문구 동일성은
  지금 아무도 안 잰다**(양쪽을 따로 고치면 조용히 갈린다).


## 2026-09-01 12:30 | crawl-db-guard | 테스트 1/1 | 시도0

- 한 일: ① **전체를 먼저 돌렸다** — 단위 465건 OK(기준선), `data/crawl.db` sha256 대조.
  ② 갭 탐색 6카테고리를 스텝의 완료 기준 위에 훑었다. 8점 이상은 **하나**다 —
  개발이 넘긴 **문구 동일성**(`crawl.py:178` 과 `indexer.py:264` 가 글자까지 같아야 한다는
  설계 계약을 **아무 테스트도 안 잰다**). ③ `tests/test_crawl.py` 의 `TestUnopenableDb` 에
  `test_wording_is_identical_to_indexer` **한 건**을 붙였다 — 같은 비 DB 파일에 `crawl.crawl`
  이 던진 `StoreOpenError` 의 문자열과 `indexer.main` 이 stderr 에 찍은 한 줄이 **같아야**
  한다. 제품 `src/` **0줄**, `README.md` 는 건수 줄(465→466) 하나. ④ 변이 둘.
- 결과: 단위 465 → **466건 OK 11.700초** · e2e 회귀 `crawl_e2e`(수집 15, 페이지 요청 15,
  최소 간격 1.001s)·`indexer_e2e`(3문서·증분 0문서) rc 0 · `data/crawl.db` sha256
  **무변경**(`85c96744…5bda18`) · `digest.md` 50항목·200줄 · `history_current.md`
  291 → 129줄 회전(198~203 → `history_017.md`).
  변이: **M4**(`crawl.py` 문구를 "DB 열기 실패" 로) → 466 중 **새 검사 하나만** 사망 ·
  **M5**(`indexer.py:264` 만 같은 식으로) → 역시 **그 하나만** 사망.
- 배운 것:
  ① **되돌리기를 `&&` 로 이으면 변이가 성공한 순간 되돌리기가 건너뛰어진다.** 첫 M5 는
  466 OK 로 **거짓 생존**을 냈다 — `run M4 && revert` 에서 러너가 실패(=변이 성공)로 1 을
  내는 바람에 revert 가 안 돌았고, `crawl.py` 가 M4 인 채 `indexer.py` 까지 같은 문구로
  바뀌어 **둘이 다시 같아졌기** 때문이다. 손으로 두 파일을 찍어 보고서야 보였다.
  digest `[8]`("변이가 실제로 **심겼는지** 먼저 단언한다")의 형제다 — **직전 변이가
  되돌려졌는지도 단언한다.** 변이가 잘 죽을수록 사본이 오염된다는 것이 고약한 지점이다.
  ② **`indexer.py:264` 의 문구도 아무도 안 붙들고 있었다**(`grep` 전수: `tests/`·`e2e/` 0건).
  검사 하나가 두 모듈의 문구를 동시에 못박는다 — M5 가 그것을 실물로 보였다.
  ③ 8점 미만이라 안 메운 것 셋은 **전부 이미 문서에 근거가 있다**: 락 갈래(검사 하나가
  30.6초, 설계 3번 답) · `Store` 가 시드 검사보다 먼저 열려 rc 1 이 rc 2 를 이기는 순서
  (설계 "받아들인 순서") · 크롤 도중 `store.upsert` 실패는 여전히 트레이스백(설계 1번 답).
  **새 `digest` 항목을 열지 않았다** — 열린 항목 50 을 그대로 둔다.
- **한도 밟았다 — 러너를 `| grep -E` 로 감쌌다. 일곱 번째다.** 방아쇠가 앞 여섯과 또 다르다:
  재확인도 망각도 아니고 **"이건 제품이 아니라 변이 사본의 기준선이라 가벼운 실행"** 이라는
  판단이었다(맨몸 재실행으로 466 OK 확인). `digest ## 반복 실패` 를 6 → **7회**.
- 다음: 리뷰 1. 새 검사가 재는 축이 셋(그물·번역·문구)으로 갈렸는지, 그리고 회전이
  제때 돌았는지를 본다.


## 2026-09-01 13:10 | crawl-db-guard | 리뷰 1/1 | 시도0

- 한 일: 세 커밋(`ccb5932`·`1b7b9c4`·`b490d7c`)을 백지 → 대조 두 패스로. **제품 수정 0줄.**
- 결과: **머지 가능.** 패스 A 막는 지적 0 — 그물이 `Store(` 한 줄만 덮고(전수 호출 1곳),
  두 타입이 안 겹치며 **`BaseException` 을 안 삼켜** rc 130 이 무사하고, `StoreOpenError` 가
  `ValueError` 와 상속이 안 겹쳐 except 순서에 안 걸린다. **rc 는 한 값도 안 바뀌었다 —
  안 잡힌 예외가 이미 1 이었다. 바뀐 것은 stderr 의 모양뿐이다.** 버린 지적 1건(락 갈래에서
  두 CLI 화면이 갈린다 — 주석이 약속한 건 `indexer.py:264` **그 줄**이라 항상 참이고,
  설계 물음 3 이 근거와 여는 조건까지 적어 둔 기보류 중복이다). 단위 **466건 OK 11.887초**
  (맨몸) · `data/crawl.db` sha256 무변경 · `digest` 50항목 200줄.
- 배운 것: **리뷰가 주울 것이 없는 반복이 가능하다.** 앞 스텝이 넘긴 둘(변이 절차 결함 ·
  파이프)이 이미 `digest [8]` 2회와 `## 반복 실패` 7회로 들어가 있었다 — `## 반복 실패` 가
  네 번 적은 *"리뷰 없는 phase 에서는 조용히 어긋난다"* 의 **반대 사례**다. 숫자 대조도
  `index.md`/`metrics`/`README` 셋 다 맞았고, README 의 466 은 애초에 손이 아니라
  `test_readme` 가 `discover` 로 센 값이다 — **검사로 옮긴 숫자는 리뷰의 일이 아니게 된다.**
- 다음: e2e 1. 새 e2e 0개(설계 확정)라 **회귀 전수**다 — 19종 rc 0, 그중 `crawl()` 을 직접
  부르는 12개가 새 예외로 안 깨지는가. 통과하면 41 DONE(`[6]` 닫아 49).


## 2026-09-01 13:15 | crawl-db-guard | e2e 1/1 | 시도0

- 한 일: 계획 7절이 적어 둔 **반증 신호를 직접 쟀다** — 진짜 CLI 를 프로세스로 띄워 네 상황을
  임시 디렉터리에서 재측정하고, `e2e/*.py` 19종을 맨몸으로 하나씩 돌렸다. 제품 **0줄**.
- 결과: 네 상황 전부 **rc 1 · stderr 한 줄 · Traceback 0회**(착수 실측 14~16줄) · 대조군 rc 0 →
  **판정 유지, 새 e2e 0개**(탐침은 스크래치패드에만 둔다 — 단위 셋이 같은 넷을 이미 붙든다).
  **19종 rc 0**, 그중 `crawl()` 직접 호출 12종이 새 예외에 안 걸렸다 — 그물이 한 줄이라는
  설계 결정의 실물 확인이다. 단위 **466건 OK 11.910초** · 품질/성능 무변(ko 100%·en 95% ·
  p95 8.74ms · 열림 10.25/s) · `crawl.db` sha256 무변경 · `digest` **49항목** 200줄.
- 배운 것: **`index.md` 41 행에 리뷰 phase 요약 줄이 통째로 없었다** — 반복 207 이 `status.md`
  에만 적었다. `## 판단 필요` 의 *"어긋나는 것은 늘 `index.md`"* 네 번째이고, 이번엔 스텝 칸이
  아니라 phase 줄이다. **처방(스텝 칸 대 개발 커밋 수 검사)이 이 변종을 못 잡는다.**
- 다음: 계획 41 DONE. 푸시 + PR 을 열고, 다음 반복은 계획 탐색이다.
- **한도 밟았다 — DONE 커밋 뒤에 러너를 `| tail -4` 로 감쌌다. 여덟 번째다.** 방아쇠가 또
  다르다: **Stop 훅이 "검증을 안 돌렸다" 고 막자 급히 답하려던 조급함**이다 — 검증을 요구하는
  기제가 검증을 새게 만들었다(맨몸 재실행 **466 OK 11.848초**). `digest ## 반복 실패` 7 → **8회**.

## 반복 209 — 계획 42 `docs-citation-guard` 계획 1/1 (2026-09-01)

- 한 일: 계획 41 DONE 뒤 **새 계획을 탐색해서 열었다**(`discover.md` 1~8순위를 순서대로).
  브랜치 `loop/docs-citation-guard` 를 `53332e0` 기점으로 팠고 계획서
  `docs/plan_docs-citation-guard.md` 를 썼다. 코드 **0줄**.
- 결과: **1~5순위 전부 0건 · 6순위 1건 · 8순위 0건.** 단위 **466건 OK 11.934초**(맨몸,
  파이프 0) · 린터는 저장소에 없다(ruff·flake8·mypy·pyflakes 미설치) · `TODO`/`FIXME`/`HACK`
  `src`·`tests`·`e2e` 전수 **0** · `docs/candidates.md` 없음 · `gh issue list` 비어 있음 ·
  `crawl.db` sha256 무변경 · `digest` 49항목 200줄 · `history_current` 201줄(회전 미달).
- 배운 것 ①: **계획 41 과 같은 자리로 돌아왔다 — 탐색 막힘 신호다.** 1~4순위는 전부 "기계가
  만드는 신호"인데 이 저장소에는 그 기계가 없다(린터 없음 · 큐 없음 · `TODO` 안 쓰는 관례 ·
  스위트는 항상 초록). 남은 우물이 `digest.md` 하나이고 그 49항목 대부분은 게이트가 닫혀 있다
  (승인 대기 · 도달 불가 · "실물에서 보이면" · "다른 계획을 열 때 함께"). **루프가 고칠 것이
  아니라 사람이 볼 신호**라 계획서 2절과 `status.md` 에 적어만 뒀다.
- 배운 것 ②: **7순위 파이프 8회는 저장소 안에 고칠 몫이 없다.** digest 가 적어 둔 처방
  (`make test`·별칭으로 러너 호출을 굳힌다)이 사다리 1번에서 멈춘다 — 검증을 세는 기제가
  저장소 밖이라 감싼 명령을 러너로 인정하는지 알 수 없고(인정 못 하면 **새는 자리가 하나 는다**),
  `make test | tail -4` 도 똑같이 되며, 여덟 방아쇠가 매번 다른 것은 이것이 명령 조립이 아니라
  **호출자의 규율** 문제라는 뜻이다. **처방을 실행 전에 다시 잰다**(`digest [7]`)의 네 번째 적용.
- 배운 것 ③: **착수 실측이 근거를 한 칸 넓혔다.** `digest [6]` 은 줄번호 인용이 "죽는다"고
  적었는데 살아 있는 3곳 중 둘은 죽은 게 아니라 **남의 항목을 가리킨다**(가리킨 줄은
  오늘 `[7]`, 가리키려던 `[6]` 은 `## 판단 필요` 로 내려갔다). 그리고 계획 38 리뷰가 "살아 있는
  문서 3곳을 고쳤다"고 적은 **그 조사가 전수가 아니었다** — `index.md` 22·357 과 `digest.md` 80
  이 남아 있었다. 이 계획이 검사를 심는 이유가 정확히 그것이다.
- 다음: 개발 1/1 — `tests/test_docs.py` 에 검사 한 건(RED 3건 확인) → 세 줄을 이름 인용으로
  교정 → **467건 OK** → 변이 M1~M3.

## 반복 210 — 개발 1/1 `docs-citation-guard` (2026-09-01)

- **RED 는 3건이 아니라 13건이었다.** 계획서가 센 3곳(`index.md` 22·358 · `digest.md` 80) 외에
  **반복 209 의 계획 phase 자신이 10곳을 새로 만들었다**(계획서 6 · `status.md` 2 · `index.md` 1 ·
  `history_current.md` 1). 손으로 센 전수가 다음 편집까지만 참이라는 이 계획의 논지를 **계획서가 직접 증명했다.**
- 진짜 인용 3줄은 이름 인용으로(둘은 `[7]` 을 가리키고 있었다), 형태를 말하는 10곳은
  `digest.md:<156>` 자리표시자로. `digest.md` **200줄·49항목 무변**. `README.md` 건수 한 줄은
  기존 `test_readme` 가 강제해 함께 고쳤다(계획서 5절 밖 파일, YELLOW 아님).
- 변이 M1~M3 전부 죽음. M2 는 아카이브가 5개가 아니라 **8개 파일 27줄**임을 드러냈고,
  M3 은 자리표시자 표기 15줄을 잡아 숫자 요구가 형태를 좁힌다는 증거가 됐다. 단위 **467건 OK**.


## 2026-09-01 13:55 | docs-citation-guard | 테스트 1/1 | 시도0

- 한 일: ① 전체를 먼저 맨몸으로 돌려 기준선(467건 OK)을 잡았다. ② 갭 탐색 6카테고리.
  8점 이상 **둘**을 `tests/test_docs.py` 한 파일에서 메웠다(**+10줄**, 검사 메서드는 안 늘려
  건수 467 무변 — 사다리 2번, 기존 `CITATION` 을 넓혔다). ③ RED 11건을 눈으로 본 뒤 교정.
  ④ 변이 둘. 제품 `src/` **0줄**, `README.md` **0줄**.
- 결과: 단위 **467건 OK 12.002초**(맨몸, 파이프 0) · `data/crawl.db` sha256 무변경
  (`85c96744…5bda18`) · `digest.md` **200줄 · 49항목**(한 글자도 안 건드렸다) ·
  고친 문서 4개(`plan_docs-citation-guard` 6줄 · `status` 2 · `index` 1 · `history_current` 1).
  변이 **M6**(계획서 31행을 `N행` 꼴로 되돌림) → 새로 넓힌 가지만 사망 ·
  **M7**(`glob("*.md")`→`"*.mdx"`) → 새 가드만 사망, 같은 실행에서 `DocHeadTest ... ok`.
- 배운 것:
  ① **금지 형태를 좁게 적으면 그 정규식이 탈출구의 설계도가 된다.** 개발이 심은 검사는
  `` `digest.md:<줄번호>` `` 만 봤고, 더 자주 쓰는 `` `digest.md` <줄번호>행 `` 은 통과했다.
  그래서 커밋 `8546f3d` 이 반복 209 항목의 인용을 **콜론 꼴 → 한국어 꼴로 옮겨 적고 "고쳤다"**
  로 셌다 — 가리키는 줄은 여전히 `digest` 의 `[7]` 이라 오인용이 살아 있었다. **탈출까지
  한 커밋 걸렸고 탈출한 사람이 저자 자신이다.** 210 이 "계획 phase 가 10곳을 만들었다" 를
  본 것과 같은 자리인데, 이번엔 *검사를 심은 손*이 검사를 피해 갔다.
  ② **형제 테스트가 이미 막아 둔 함정이 옆 테스트에는 뚫려 있었다.** `DocHeadTest` 는
  "경로를 잘못 잡으면 빈 문자열 위에서 조용히 통과한다" 를 주석까지 달아 막았는데
  `DocCitationTest` 는 "훑은 것 0건" 과 "위반 0건" 을 구분하지 못했다. M7 이 그 차이를 실물로
  보였다 — 머리 검사는 여전히 `ok` 였다. **가드는 파일 단위로 복사되지 않는다.**
  ③ **공백 한 칸이 참·거짓을 갈랐다.** `` `index.md` 41 행 `` 은 줄 41 이 아니라 **계획 41 의
  행**이다(index.md 의 41번째 줄은 `plan_search-api` 서술이다). `행` 앞 공백을 안 받는 쪽으로
  좁혀 그 오탐을 뺐고, 덕분에 얼린 기록 한 줄을 안 고쳤다.
  ④ **마감 문서를 쓰는 손도 똑같이 걸렸다.** ①을 서술하려고 금지 표기를 예시로 적었더니
  최종 실행이 `status.md` 4줄과 이 항목 1줄을 잡았다(자리표시자로 바꿔 초록). 계획 phase 10곳 ·
  개발 phase · 테스트 phase — **세 phase 연속으로 그 phase 자신의 산문이 위반을 만들었다.**
  검사가 없었으면 세 번 다 아무도 못 봤다. 이것이 "손으로 센 전수는 다음 편집까지만 참" 의
  세 번째 실물이다.
- **넘긴 것 — 검사와 `append 전용. 수정·삭제 금지.` 가 충돌한다.** 초록으로 만들려면
  `history_current.md` 를 고쳐야 하고 개발과 이번 스텝이 각각 한 줄씩 고쳤다. 회전이 그 항목을
  아카이브로 밀면 제외 규칙에 걸려 못 고치므로 **지금은 창이 열려 있어서 안 보일 뿐**이다.
  `status.md` `## 판단 필요` 에 두 선택지와 함께 적었다. `digest` 항목은 안 열었다(49 유지).
- **파이프 없음.** 러너를 세 번 다 맨몸으로 불렀다(기준선 · 변이 둘 · 최종). `## 반복 실패` 는
  8회에서 안 늘었다.
- 다음: 리뷰 1/1 — 넓힌 경계의 방어 가능성, 위 룰 충돌, 계획서에서 줄번호를 지운 것이 기록
  손실인지.
