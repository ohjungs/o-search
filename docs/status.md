---
signal: GREEN
phase: 테스트
step: 1
attempt: 0
iteration: 232
updated: 2026-09-02
ctx: 55
night_iterations: 91
night_red: 0
night_retries: 0
plan: docs-record-guard
---

# 현재 상태

**계획 45 `docs-record-guard` 테스트 1/1 완료.**
빠뜨린 것은 셋이었고 **셋 다 "설계가 잡는다고 적었지만 아무도 안 잰" 자리**다 —
새 검사 둘이 **자기 자신을 못 재고 있었다.** 실물 문서가 이미 초록이라 검사를
넓히는 변이 셋이 전부 살아남았다. 합성 픽스처로 재는 단언 둘을 더했다.
제품 `src/` 0줄 · 단위 477 → **482건 OK** · 새 파일 0개(아카이브 1개는 회전 산출물).
집안일로 **회전을 돌렸다**(318 → 149줄 · `history_023.md`).
**다음 반복은 리뷰 phase.**

## 방금 한 것 (2026-09-02 · 테스트 1/1)

**설계 변이표(V1~V8)는 개발이 소진했으니 표 밖을 쟀다.** 표의 변이는 전부 *데이터*
(문서)를 건드리는 것이었다. 이번엔 *검사 코드*를 넓히는 변이를 심어 봤고, **셋이
그대로 살아남았다.**

| 변이 | 무엇을 넓혔나 | 전 | 후 |
|---|---|---|---|
| M1 | `indexed` 에서 경계 매칭 `(?<![A-Za-z_])` 삭제 | **초록 (산다)** | RED |
| M2 | `ITER_ROW` 를 `^\| 반복[^\|]*\| ([0-9]+) \|` 로 확대 | **초록 (산다)** | RED |
| M3 | 검사 범위를 `## 완료` 절 → `digest.md` 전체 | **초록 (산다)** | RED |

**셋 다 설계가 갈림길을 걸어 고른 결정이다.** M3 은 갈림길 A 의 결론 자체이고,
M1 은 설계가 *"V7 이 설계의 값을 재는 변이"* 라고 적은 그 경계 매칭이며, M2 는
계약표의 *"이웃 세 행은 안 문다"* 줄이다. **셋 다 손으로 한 번 재고 그것으로
끝이었다** — 스위트에는 그 사실을 붙드는 단언이 하나도 없었다.

**왜 안 잡혔나 — 데이터가 이미 초록이라서다.** 개발 2/2 가 명부를 넣은 순간
`digest.md` 는 **어느 쪽으로 재도 구멍 0** 이 됐다. 범위를 파일 전체로 넓혀도
명부에 22개가 다 있으니 0, 경계를 빼도 0. `metrics.md` 도 정확한 행이 이웃들보다
**먼저** 나와서 정규식을 넓혀도 첫 매치가 그대로다. **린트형 검사는 데이터가
초록일 때 자신을 못 잰다** — 계획 42 가 적어 두고 `CitationPatternTest` 로 닫은
그 자리이고, 이번이 같은 수법의 세 번째 적용이다.

**처방도 같은 파일에 이미 있었다.** `CitationPatternTest` 가 `CITATION` 상수를
CAUGHT/NOT_CAUGHT 리터럴로 붙드는 그 모양 그대로, 검사의 판단을 모듈 수준
이음매로 꺼내고 합성 텍스트로 쟀다. 새 추상은 없다 — 이미 있는 관용구를 한 번 더 썼다.

- `ITER_ROW`·`ITER_LINE` — 정규식 둘을 모듈 상수로. `IterationSyncTest` 는 이제
  이 상수를 쓴다.
- `done_section(digest_text)` — `## 완료` 절 본문. 절이 없으면 `None`(호출부가 실패).
- `indexed(name, section)` — 명부가 이름으로 싣고 있나. 접두어에 가려진 것은 안 친다.

**새 단언 둘 (5건).**

- `IterationPatternTest` — 합성 표에서 **이웃 세 행을 일부러 앞에 두고** 정확한
  행만 물리는지 잰다. 넓힌 정규식은 `| 반복 상한 | 0 |` 의 `0` 을 집어 죽는다.
  `status.md` 쪽은 `night_iterations: 90`(같은 프런트매터의 실제 이웃)을 안 무는 것을 잰다.
- `ArchiveMatchTest` — 합성 `digest` 하나로 셋을 동시에 잰다. 실려야 하는 것 하나,
  **안 실려야 하는 것 셋이 각각 다른 이유**다: `## 반복 실패` 의 **신고문**(범위) ·
  `plan_history_003.md` 에 가려진 이름(경계) · `## 보류` 절(범위). 절 이름이 바뀌면
  `None` 이 오는 것도 함께 잰다.

**변이로 되돌아가 확인했다** — M1·M2·M3 을 다시 심으니 각각 **새 단언만** 죽는다
(M1 → `test_not_indexed[history_003.md]` · M2 → `test_only_the_exact_row_matches` ·
M3 → `test_not_indexed[history_001.md, history_004.md]`). 겹치는 검사가 없는 진짜
구멍이었다.

**스텝 1 의 가드가 첫 실사용에서 울었다.** `metrics.md` 를 232 로 고치고 `status.md`
를 아직 안 고친 채 전체를 돌리자 `IterationSyncTest` 가 **`'232' != '231'`** 로
죽었다. 어제 만든 검사가 **오늘 그것을 만든 반복의 기록을 잡았다** — 계획 30 의 README
카운트 가드가 하루 만에 값을 증명한 것과 같은 모양이다. README 카운트 가드는
**사흘 연속** 울었다(`(477, 19) != (482, 19)` → 482 로 정정).

**안 한 것.** 명부에 있는데 실물이 없는 이름(역방향)은 안 잰다 — 오타는 지금도 RED 이고
(진짜 이름이 안 잡힌다) 아카이브는 삭제 금지 문서라 사는 변이가 없다. 중요도 4 로 보고
`digest` 에도 안 남겼다.

## 한도 (매 반복 확인)

- `main` 직접 커밋 금지 · `--no-verify` 금지 · 외부 네트워크 금지 ·
  `docs/specs/` 읽기만 · `data/crawl.db` 무변경(sha256 확인) · 의존성 추가 금지(stdlib 만).
- **러너 파이프 — 이번 반복 1회.** 첫 실행에서 `| tail -5` 를 썼고 **판정 줄 대신
  테스트가 찍은 표준출력이 나와** 결과를 못 봤다(`project.md` 가 경고한 그 배치 그대로).
  즉시 맨몸으로 다시 돌렸고 **이후 전 실행이 맨몸**이다. 연속 카운트는 또 0 에서 시작한다.
- **변이는 제자리 편집이었다** — `.mutation-lock` 을 걸고 했고 되돌린 뒤 지웠다.
  되돌리기를 `&&` 로 잇지 않았고 매 실행 `python3 -B` 였다(계획 45 개발이 밟은 거짓 초록).
  작업 트리에 `.mutation-lock` 없음.
- 이번 반복 최종 실측(맨몸): `PYTHONPATH=src python3 -m unittest discover tests`
  **482건 OK 11.9초** · `src/` **0줄** · 새 파일 0개 · 의존성 0 ·
  `data/crawl.db` sha256 `85c96744…` 무변경.
- **집안일 하나 — 회전을 돌렸다.** 이 기록을 붙이니 `history_current.md` 가 **318줄**로
  상한 300 을 넘어, **그 자리에서** 계획 45 의 앞 네 반복(228~231)을 `history_023.md`
  로 밀었다(318 → **149줄**). **네 회전 연속 지각 0.** 진행 중인 계획을 가르는 경계라
  완료 계획을 통째로 미는 평소 경계와 다르다(계획 33·34 회전과 같은 꼴).
- **이번 회전이 계획 45 가 만든 새 의무를 처음 졌다** — `digest.md` 명부에
  `history_023.md` 를 더했다. 안 더하면 `ArchiveIndexTest` 가 RED 다. **규약이 문서에만
  있던 어제와 갈리는 첫 회전**이고, 검사가 실제로 강제하는 것을 이 반복이 실물로 봤다.
  압축 줄은 200줄 상한 때문에 **기존 회전 줄에 이어 붙였다** — `digest.md` **200줄 유지** ·
  완료 항목 삭제 0 · **새 색인 구멍 0** · 아카이브 **23개**.

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-01 확인)

`origin/main` 은 `494313b`(PR #5 병합). 계획 43·44 의 커밋들(`bc40ea9`~`79952ee`)이
아직 안 갔다. **남은 것 (사람 결정)**: 그것들을 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 — 둘이 열려 있다 (하나가 닫혔다)

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
   offset 이 0 이 되는 날 검사기가 멈추고, 짝은 그때 열릴 계획의 일이다.
3. ~~기록 3파일 불일치~~ — **계획 45 가 열었다** (테스트까지 왔다).
