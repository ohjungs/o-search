---
signal: GREEN
phase: 리뷰
step: 2/2
attempt: 0
iteration: 444
updated: 2026-09-07
ctx: 51
night_iterations: 11
night_red: 0
night_retries: 0
plan: history-cap-guard
---

## 현재 상태

**계획 76 테스트 phase 완료 — 갭 1건 찾아 닫았다.** 전수 **672건 OK**
(`PYTHONPATH=src python3 -m unittest discover -b -s tests`, 18초 — 전체).

**찾은 갭(중요도 6): 축 이름 단언이 «처방 문장» 으로 만족됐다.** `cap_gap` 의 처방이
「오래된 **항목**부터 ... 한 **줄**로 압축한다」라, `assertIn("줄", gap)` ·
`assertIn("항목", gap)` 은 **어느 축이 물었든 참**이었다(실측: 항목 축만 넘긴 메시지에도
`"줄"` 이 있다). 이름은 축을 보는 것 같은데 아무 메시지나 통과하던 단언이다.
`"줄 수 N > M"` 통째로 바꾸고 반대 축 부재까지 단언했다. **8점 미만인데 이번에 처리한
이유**: 고치는 것이 4줄인데 등재하는 것이 그보다 길고, 지금 digest 는 201줄이라
한 줄이 더 는다. 라벨 변이 3종(소실·축약·뒤바뀜)이 이제 각각 **2건씩** 죽인다
(전에는 `test_both_axes` 하나만 물었다 — 스위트가 공백은 아니었고 개별 단언이 헐거웠다).

## 닫힌 판단

- **`digest.md` 는 201줄이다** — 반복 440 의 회전 기록 한 줄이 상한 200 을 1줄 넘겼다.
  무인 모드는 삭제를 안 하므로 **무엇을 버릴지는 아침에 사람이 정한다**(야간 보고서).
  2026-09-07 의 삭제 위임은 그 한 건 한정이었고 `rules/docs.md` 3절 문장은 그대로다.
- **`.context-state.json` 이 낡았다**(반복 442 실측 22분) — statusLine 이 꺼진 것으로
  보고 `SKILL.md` 대로 **반복 상한에만 의존**한다. 마지막 값은 51/30/45 로 전부 여유.
