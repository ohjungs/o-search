---
signal: GREEN
phase: 개발
step: 1/1
attempt: 0
iteration: 335
updated: 2026-09-05
ctx: 45
night_iterations: 156
night_red: 2
night_retries: 0
plan: passage-cost-axis 계획 57 (테스트 phase 완료 · 다음은 기록 자리 둘 동기화)
---

# 현재 상태

**계획 57 테스트 phase 를 닫았다 — 갭 탐색이 «직전 스텝이 잡는 힘을 줄였다» 를 찾아
그 자리에서 메웠다.** 고친 파일은 `tests/test_indexer.py` 하나(단언 1줄 + 주석 8줄)이고
새 테스트 메서드 **0개**라 단위 건수 **605** 가 그대로다.

## 무엇을 찾았나 — 8점 하나, 그 자리에서 메웠다

한도를 `500/3` → `500` 으로 펴면서 ① 이 허용하는 `캡 × PASSAGE_LIMIT` 이 오늘의
**+8.2% → +50.4%** 로 넓어졌다. 변이 **M4(캡 35,000 → 50,000, +43%)** 를 저장소 밖
`src` 사본에 심으니 이 테스트도 **전수 605 도 `OK · rc 0`** — 옛 단언에서는
`220ms > 167ms` 로 죽던 변이인데 저장소 전체가 눈이 멀어 있었다.

그래서 같은 테스트에 **①-b 상수 동결** 한 줄을 세웠다.

```
①   MAX_PASSAGE_HTML/1000 × 0.95 × PASSAGE_LIMIT ≤ 500      (오늘 332.5ms · 예산의 67%)
①-b MAX_PASSAGE_HTML × PASSAGE_LIMIT ≤ 35,000 × 10          ← 새로 세운 줄
②   min(3회 파싱) / 캡kB ≤ 1.60 ms/1000자                     (오늘 0.948)
```

**한도 500 은 «재파싱 몫» 이 아니다.** 사양 성능 5 의 500ms 는 `GET /passages`
**한 요청 전부**의 예산이고 그 안에 성능 4 의 검색 몫 300ms 가 같이 산다
(`docs/specs/concept.md:127-129`). 재파싱 하나가 100% 를 먹어도 되는 한도라
**몫을 다시 긋는 것은 설계 판단**이고, 이번 스텝은 «올리는 문만 닫아» 그 판단을 사람
앞으로 보냈다 — 내리는 변이(캡 28,000)는 그대로 통과시킨다.

## 검증 — 변이 다섯이 각각 다른 단언을 죽인다

전부 **저장소 밖 `src` 사본**에 심었다(제품 `src/` 는 한 글자도 안 고쳤다).

| 변이 | 결과 |
|---|---|
| M1 `extract_blocks` 에 `sleep(0.06)` | ② `2.914 ms/1000자 > 1.6` · FAILED rc=1 |
| M2 `MAX_PASSAGE_HTML` 100,000 | ① `950ms > 500` · FAILED rc=1 |
| M3 `PASSAGE_LIMIT` 20 | ① `665ms > 500` · FAILED rc=1 |
| **M4 `MAX_PASSAGE_HTML` 50,000** | **①-b `500000 > 350000` · FAILED rc=1** (그전엔 전수 605 초록) |
| M5 `MAX_PASSAGE_HTML` 28,000 (내리는 변이) | `OK` rc=0 — 조이는 방향은 안 막는다 |

- 전수 `Ran 605 tests · OK` · rc 0 · **13.5초**(직전 13.6 — 새 단언이 산술이라 0초).
- 문서 가드 4종(`IterationSyncTest`·`ArchiveIndexTest`·`DocCitationTest`·
  `tests/test_readme.py`) `Ran 8 tests · OK` · rc 0.
- `src/` · `e2e/` · `README.md` · `docs/specs/` **무접촉** · `data/crawl.db` sha256
  앞뒤 동일(`85c96744…5bda18`) · 재색인 0 · 스키마 0 · 새 의존성 0.
- 원격 `loop/passage-cost-axis` = `c09c545` (HEAD 와 같다 · `gh` 조회 0).

## 다음 스텝 — 완료 기준 6 이 아직 거짓이다

계획 4절 완료 기준 6 은 «세 기록 자리의 숫자가 서로 같다» 인데 **둘이 낡았다**:

| 자리 | 지금 적힌 것 | 테스트가 적는 것 |
|---|---|---|
| `src/websearch/indexer.py:41~64` 주석 | 0.44 · 154ms · 예산의 31% · «배정치 1/3 은 모양 배수를 흡수하는 자리» | 0.95 · 332.5ms · **67%** · «흡수할 배수가 없다» |
| `docs/project.md:185~192` | 같음 | 같음 |

**정면으로 모순이다**(중요도 7). 고칠 자리가 `src/` 라 이번 스텝의 지시 범위 밖이어서
손대지 않았다. 다음 개발 스텝이 두 자리를 오늘 값으로 맞추면 완료 기준 6 이 참이 되고,
그 다음이 리뷰 phase 다. 계수가 0.118 → 0.352 → 0.44 로 **세 번 낡은** 그 구멍이
바로 이 세 자리가 안 묶여 있던 것이다.

## 남긴 천장 (`digest ## 다음 계획 후보 (테스트 phase 갭)` 에 등재)

- **[7] 재파싱 몫의 예산 배정을 아무도 안 그었다** — 최악 332.5ms + 검색 몫 300ms 는
  632ms 로 사양 밖인데 그 덧셈을 하는 자리가 0곳이다. ①-b 가 문만 닫아 뒀다.
- **[5] 1.4286~1.60 밴드** — 한가한 기계에서 파서가 1.51~1.69배 느려지면 ②·① 둘 다
  초록인데 실제로는 예산 밖이다. 부하에서는 잡힌다(같은 회귀가 1.77 로 나온다).

## 규율 위반 1건

문서 가드 4종을 `2>&1 | tail -20` 에 넣어 rc 를 잃었다 — **러너 규율 누적 36회**.
판정 줄을 보고도 맨몸으로 즉시 재실행해 `rc=0` 을 확인했다. 방아쇠는
「출력이 길 것 같다」이고 `digest [9]` 의 25회짜리 재발과 같은 뿌리다.

## 사람 결정 대기

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1).
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 0 일 때만.
3. **반응형 360px 미검증** — 브라우저가 없어 저장소의 아무도 안 그려 봤다.
4. **3시간 자동 스냅샷 잡을 루프 작업 중에도 세울 것인가**(반복 328 의 사고).
   RED 중간을 덮치면 깨진 상태가 원격에 올라간다. `.mutation-lock` 안이 있다.
   반복 334 에서 **RED 가 아닌 자리에서 실제로 한 번 일어났다**(커밋이 둘로 갈렸다).
5. **재파싱에 예산 얼마를 줄 것인가** — 위 [7]. 캡·`PASSAGE_LIMIT` 을 올리려는 판단이
   생기는 날 ①-b 가 사람을 이 자리로 데려온다.
