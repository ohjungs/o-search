---
signal: GREEN
phase: 탐색
step: 0/0
attempt: 0
iteration: 462
updated: 2026-09-08
ctx: 38
night_iterations: 2
night_red: 0
night_retries: 0
plan: null
---

## 짧은 경로 — `recrawl-blocker-remeasure` (완료)

- 근거: 컨셉 기능 5(*"재크롤: 색인된 문서는 30일 이내에 재방문해 갱신·삭제를 반영한다"*)와
  실물의 격차. 저장소가 `index.md` 에 적어 둔 「`recrawl` 은 루프가 열 수 없다 — 스키마
  변경 + 마이그레이션」이 **계획 77·78 이후로 낡았는지**를 실물로 잰다
- 할 일: 세 가지를 실측하고 기록만 고친다(`index.md`·`digest.md`). 제품 0줄
- 완료 기준: 결함 재현 · 실물 `fetched_at` 커버리지 · 워터마크 루프의 집합을 숫자로 남기고 전수 675 OK
- 이미 한 것: **전부 끝났다.** ① `pages.html` 을 갈고 재색인 → 새 본문 **0건**·옛 본문 1건
  ② 실물 `data/crawl.db` 400행이 `fetched_at` **400/400**(30일 초과 0건) ③ 갱신 반영은
  `indexer.py:243-260` 워터마크 루프가 이미 읽는 집합이라 스키마 무변경.

## 사람 결정 대기 — 재크롤을 열지 말지 (착수 안 함)

스키마·마이그레이션 축은 **0** 이 됐지만, 남은 것은 사양이 숫자를 안 준 **정책 셋**이다:
① 실패 상태(4xx·5xx·`status 0`)도 다시 받나 ② 404·410 의 삭제 반영이 `docs` 행만인가
`pages` 행까지인가(후자는 데이터 삭제라 야간 금지) ③ 재크롤이 링크를 다시 추출해
프런티어를 넓히나(크롤 예산·간격 축). **야간은 넘겨짚지 않는다** — 아침 결정 항목.

## 브랜치 기점

`loop/recrawl-blocker-remeasure` 는 `main` 이 아니라 **`loop/like-scan-verdict` 위에서 땄다**
— 같은 밤의 두 짧은 경로가 `status.md`·`metrics.md`·`history_current.md` 의 **같은 줄**을
순서대로 고쳐서, `main` 기점으로 갈라 두면 아침에 둘을 병합할 때 기록이 충돌한다.
버릴 때는 **둘을 함께 버린다**(뒤가 앞에 딸린다 — git 룰 1절의 사슬 대가를 알고 진 것이다).

## 다음

탐색 8순위 전수 후 착수 가능한 근거 0. 1~3순위 0건(전수 675 OK · 코드 `TODO` 0 · 린터
없음) · 4·5순위 대상 없음 · 6순위 후보는 오늘 하나 닫았고(`[7]`) 나머지는 여는 조건
미충족(두 `[4]` 는 「깨지는 날이 고칠 때」라고 스스로 적어 뒀다) 또는 승인 대기.
