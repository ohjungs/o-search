---
signal: GREEN
phase: 개발
step: 1
attempt: 0
iteration: 199
updated: 2026-09-01
ctx: 47
night_iterations: 65
night_red: 0
night_retries: 0
---

# 현재 상태

**계획 40 `exit-code-contract` 설계 완료 — 대안 A 를 골랐다.**
계획서 `docs/plan_exit-code-contract.md` · 설계서 `docs/design_exit-code-contract.md` ·
브랜치 `loop/exit-code-contract`(기점 `064e8a5`).
**계획 39 까지 전부 DONE·아카이브 완료** — 39 의 계획서는 `docs/plan_history_025.md`.
이 스텝의 `src/`·`tests/`·`e2e/` diff 는 **0줄**이다(설계 문서·기록만).
계획 34~37 은 PR #2 로 `main` 에 병합됐다(`main` 최신 `e0890c8`) — 38·39·40 의
`main` 병합은 사람이 정한다.

## 방금 한 것 (2026-09-01 · 설계 1)

### ① 대안 A — 환경 오류는 **rc 1**, 명령줄 오류는 **rc 2**

`indexer.main` 의 환경 오류 네 갈래(`indexer.py:242`·`:248`·`:253`·`:262`)를
`rc 2` → **`rc 1`** 로 내린다. 제품 diff 는 **한 파일 4줄**, `serve.py`·`crawl.py` 는 0줄.

가른 것은 **잡히지 않은 예외가 이미 rc 1** 이라는 사실이다. 파이썬이 처리 안 된 예외를
1 로 끝내므로 rc 1 은 저장소가 고른 값이 아니라 **런타임이 이미 내고 있는 값**이고,
손으로 처리한 갈래만 2 로 갈라져 나왔다. `serve.py:329-333` 이 그 근거를 적어 뒀고
(*"안 잡을 때도 1 이었다"*), `test_indexer.py:230` 주석은 색인 쪽 **고치기 전 동작이
`트레이스백 + rc=1`** 이었다고 남겼다. B(`serve` 를 2 로)를 고르면 **처리한 환경 오류 2 ·
처리 못 한 것 1** 이라는 틈이 영구히 남는다 — 새 `except` 를 안 단 갈래마다 계약이 갈린다.
**버린 축은 변경 크기다**(A 7단언 vs B 제품 1줄). 작은 쪽을 안 골랐다.

### ② 설계가 계획의 근거 하나를 **뒤집었다**

**계획 2-1 ② "`serve` 의 rc 1 을 고정하는 단언이 0건" 은 거짓이다.**
`tests/test_serve.py:890-899` `test_bind_failure_is_reported_instead_of_crashing` 이
`assertEqual(rc, 1)` 로 값을 붙들고 **docstring 에 규칙까지 적어 뒀다**. 계획의
`grep "serve.main"` 이 못 본 것은 그 단언이 헬퍼 `call()`(`:792`)을 거쳐 부르기 때문이다.
→ **B 의 비용은 0곳이 아니라 1곳 + 기록된 근거 삭제**다. 계획서 3절 비교표의 그 칸이 틀렸다.
**일반화: 호출을 grep 해 커버리지를 세면 헬퍼 뒤의 단언이 통째로 안 보인다.**

계획 2-1 ①(`crawl` 은 이미 지킨다)은 **참이었다** — `crawl.main` 의 반환값은 `{0,2,130}`
뿐이고, `crawl.py:318` 의 `return 1` 은 `main` 이 아니라 `_store_result`(`:274`)의
"이 페이지를 주웠나" 값이라 종료 코드가 아니다.

### ③ 재시도 축(C)은 **보류** · 스텝은 **2 → 1개로 합쳤다**

C(락만 별도 코드)는 **종료 코드를 보고 재시도하는 호출자가 저장소에 0곳**이라 YAGNI 다.
여는 조건은 "그런 호출자가 실제로 하나 생길 때" 이고, 오늘의 구분자는 stderr 문구다
(락만 *"DB 가 잠겨 있다 …"*, 계획 39 가 세운 갈래).

계획 스텝 1("값을 바꾸기 전에 오늘의 값을 못박는다, `src` 0줄")은 **전제가 무너졌다** —
오늘의 rc 는 이미 전부 단언이 붙어 있다(위 ②가 마지막 빈자리로 지목한 `serve` 환경 오류
1 도 붙어 있었다). 못박을 자리가 없어 별도 스텝이 0줄짜리 반복이 되므로 **개발 1스텝**으로 합쳤다.

## 다음 스텝 (개발 1 · 마지막 스텝)

**TDD.** 먼저 단언 7곳을 뒤집어 **RED(462건 중 7건 실패)** 를 보고, 그다음
`indexer.py` 4줄과 `README` 계약 표를 넣어 GREEN.

| 파일 | 자리 | 무엇 |
|---|---|---|
| `tests/test_indexer.py` | `:235`·`:505`·`:510`·`:586`·`:600`·`:615` | 6단언 2→1 · `..._and_rc_2` 이름도 함께 |
| `e2e/tokenizer_e2e.py` | `:153` | 옛 색인 CLI rc 2→1 |
| `src/websearch/indexer.py` | `:242`·`:248`·`:253`·`:262` | `return 2` → `return 1` + 주석의 값 갱신 |
| `README.md` | `:28` 아래 | 계약 표 4줄(0·1·2·130). `단위 462건` 숫자는 **안 변한다** |

**안 움직이는 것**: `test_indexer.py:502`·`:514`(인자) · `test_crawl.py` 의 rc 2 단언 7건 ·
`test_serve.py` 의 rc 1·rc 2 단언 전부 · `indexer` 의 130.
**아카이브(`plan_history_*`·`history_*`)는 안 고친다** — 21·24·26·27·29 의 "트레이스백
대신 rc 2" 서술은 그때의 기록이고, 현재 계약이 사는 곳은 `README.md` 하나다.

변이 **M1**(`indexer.py:242` 만 2 로 되돌리기)이 **새 단언 하나만** 죽여야 한다.
e2e 는 `tokenizer_e2e.py` 가 필수다(rc 단언이 거기 있다).

**곁가지**: 계획 39 리뷰 중 자동 스냅샷 훅이 만든 `9f034eb` 는 그대로 둔다
(저장소 밖 훅) — `digest.md ## 반복 실패` 의 스냅샷 훅 항목에 **네 번째 사례**다.

## 한도 (넘으면 RED)

- `data/crawl.db` 실물·스키마 무변경 (sha256 `85c96744…5bda18`).
  탐침은 임시 디렉터리에서만, `cwd` 도 거기다 — 색인 경로가 cwd 기준이다.
- 단위 **462건**이 하나라도 줄면 RED · e2e **19종**이 줄면 RED.
- `docs/digest.md` 열린 항목 **50**(`grep -c '^- \['`)이 줄면 RED · 191줄(상한 200).
  `[7]` 은 계획 40 이 DONE 될 때만 닫는다(그때 49).
- 제품 diff 는 설계가 지목한 **`src/websearch/indexer.py` 한 파일**이다.
  `crawl.py`·`serve.py` 가 바뀌면 RED.
- 변이는 `.git` 없는 스크래치패드 사본에서만 심는다(`## 반복 실패` 3회 항목).
- 의존성 추가 금지(stdlib 만) · `docs/specs/` 읽기만 · `--no-verify` 금지 ·
  `main` 직접 커밋 금지 · 외부 네트워크 금지 · 도메인당 요청 간격 1초 이상 · robots 준수.
