# e2e 결과 — 계획 68 `spec-citation-anchor` (반복 397 · 2026-09-06)

**판정: 통과 — 전수 `Ran 628 tests in 15.748s` · `OK` · rc 0 · 실물 e2e 스크립트 2개
실행(둘 다 rc 0) · 대조군 3판(V0 성한 원본 + 변이 2판) 전부 기대대로 → DONE.**
**예산을 먼저 못박고 들어왔다** — 이 저장소는 변이 샌드박스 안에서 에이전트가 두 번
정지한 자리다(`digest ## 반복 실패` 「리뷰 예산」). **도구 호출 20회 이내 · 전수 1회 ·
변이 2판**으로 상한을 정하고 그 안에서 기록·커밋까지 갔다. 실제 소요는 변이 3판(V0 포함)이
**한 번의 호출 · 0.03초 × 3** 이었다 — 사본 편집을 파이썬 한 스크립트에 묶어 샌드박스
안에서 대화가 왕복하지 않게 했다.

## 0. 이 phase 가 실제로 물은 것

계획 68 은 `src/` 를 **0줄** 고쳤다. 그래서 사용자 시나리오가 아니라 산출물 자체를 묻는다 —
**「사양이 움직였을 때 저장소가 거짓 초록을 내지 않는가」**. 계획 67 e2e(반복 392)가 같은
질문을 이미 한 번 샀으므로(U1 `failures=12`), 오늘의 값은 **같은 변이에서 숫자가 얼마나
올랐는가**다. 그것이 계획 68 이 산 것의 크기다.

곁들여 `status.md` 가 남긴 회귀 질문 하나 — 「`design_check.py`·`perf_crawl.py`·
`quality_eval.py` 의 바뀐 문자열이 **실행 경로에서 그대로 도는지**」 — 를 1절에서 실행으로
답한다. 계획 67 e2e 는 이 자리를 「소비자가 누구인지 세어서」 정적으로 면제했는데,
이번 diff 는 **`print` 라벨 하나와 `assert` 메시지 하나가 진짜 런타임 문자열**이고 후자는
**따옴표 종류가 바뀌면서 `%` 포맷 인자와 같은 식 안에 들어 있다.** 정적 판독으로 「돌 것이다」
라고 적는 대신 오늘은 **두 스크립트를 실물로 돌렸다.**

## 1. 실행 경로 — 바뀐 문자열 다섯 줄이 실제로 도는가

`git diff 274c0eb^ HEAD -- e2e/` 는 **일곱 줄**이고 그중 실행에 닿는 것은 둘이다.
docstring 넷(`design_check.py:2` · `perf_crawl.py:1`·`:151` · `quality_eval.py:3`)과
주석 하나(`perf_crawl.py:253`)는 안 돈다 — 그래도 import 로 파싱은 확인했다.

### 1-1. `e2e/design_check.py` — `print` 헤더 (실행, 매번 뜬다)

```
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 e2e/design_check.py
컨셉 디자인 4축 (docs/specs/concept.md:50-54 — "화면은 둘뿐" 이하)
…
4축 전부 통과.
rc=0
```

큰따옴표 → 작은따옴표 전환(계획 2절이 고른 모양)이 실물 stdout 에서 **의도한 그대로**
찍힌다. 안쪽 `"화면은 둘뿐"` 이 이스케이프 없이 진짜 큰따옴표로 서 있다. 4축 판정
자체도 전부 통과라 **판정 로직에 부작용 0**이다.

### 1-2. `e2e/perf_crawl.py` — 통과 경로 (실행)

```
PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=src python3 e2e/perf_crawl.py
[열림] … 초당 10.22문서 … [차단] … [예외] …
e2e 통과: 3시나리오(열림·차단·워커예외) 전부 통과
rc=0
```

### 1-3. `e2e/perf_crawl.py:275` — **실패할 때만 뜨는 `assert` 메시지**

여기가 오늘 유일하게 위험했던 자리다. 이 줄은 **작은따옴표로 돌면서 `%` 포맷의 첫
조각이 됐고**, 통과하는 실행에서는 **한 번도 렌더되지 않는다.** 인자 개수가 어긋나 있어도
초록으로 나간다는 뜻이다. 그래서 소스를 손으로 다시 적지 않고 **`ast` 로 그 `Assert`
노드의 `msg` 를 통째로 뽑아 그대로 컴파일·평가**했다(`rate=3.21`, `TARGET_RATE=5.0`,
`DOMAINS=12`, `LATENCY=0.4`).

```
초당 3.21문서 — concept.md:44 "초당 5문서 이상 지속" 기준 5.0 미달. 도메인 12개를
순차로 받으면 1/0.4s = 초당 2.5문서가 상한이다
```

**포맷 인자 다섯이 그대로 맞고 앵커 문구가 메시지 안에 산다.** 암묵 문자열 연결이 `%`
보다 먼저 묶이는 것(따옴표를 갈면서 깨질 수 있던 자리)도 이 렌더가 실행으로 답한다.

### 1-4. import — docstring 다섯 줄

`python3 -c "import quality_eval,design_check,perf_crawl"` → `IMPORT_OK`.
`quality_eval.py` 는 `data/crawl.db` 와 질의집합이 있어야 `main()` 이 도는 스크립트라
**오늘 안 돌렸다** — 이 계획이 그 파일에서 고친 것은 3행 docstring 한 줄뿐이고
실행 경로에 없다. **안 돌린 것을 「통과」로 적지 않는다.**

## 2. 전수 — `Ran 628` · `OK` · rc 0 (1회)

```
PYTHONDONTWRITEBYTECODE=1 PYTHONPYCACHEPREFIX=$(mktemp -d) PYTHONPATH=src \
  python3 -m unittest discover -b -s tests
Ran 628 tests in 15.748s
OK
rc=0
```

맨몸으로 돌렸다 — 파이프·리다이렉션 **0회**(`docs/project.md` 「명령」 절 규율).
`README.md:104` 의 「단위 628건」과 일치하고 「e2e 시나리오 21종」 ↔ `ls e2e/*.py` **21**.

## 3. 대조군 — 실물 사본 3판 (변이 2판)

저장소(`src`·`tests`·`e2e`·`docs`·`README.md`)를 `mktemp -d` 아래로 복사하고 **복사본만**
파이썬 줄 편집으로 고친 뒤(`sed` 0회 — `digest [8]` 의 BSD `sed` 거짓 초록 대응) 그 안에서
`python3 -m unittest -b tests.test_docs` 를 진짜 서브프로세스로 돌렸다. 매 판
`PYTHONDONTWRITEBYTECODE=1` · `PYTHONPYCACHEPREFIX=$(mktemp -d)`. 끝난 뒤 저장소
`git status --porcelain` **빈손**.

| # | 편집 | 판정 줄 | 뜻 |
|---|---|---|---|
| V0 | 손 안 댐 (성한 원본) | `Ran 31 tests in 0.025s` · `OK` · **rc 0** | 오탐 0 |
| **V1** | 사양 `concept.md` 20행 뒤에 항목 한 줄을 끼움 — 21행 이하 주소가 통째로 한 칸 밀린다 (반복 392 `U1` 과 같은 자리) | `Ran 31` · **`FAILED (failures=20)`** · rc 1 | **12 → 20** |
| **V2** | `e2e/perf_crawl.py` 1행 `concept.md:44` → `:45` (계획 착수 탐침의 `M1` — 계획 전에는 **`OK` 로 살아 나갔다**) | `Ran 31` · **`FAILED (failures=1)`** · rc 1 | 구멍이 닫혔다 |

### 3-1. V1 — 이 계획이 산 것의 크기가 여기 있다

**같은 변이가 어제 12건, 오늘 20건을 죽인다.** 반복 392 의 `U1` 과 삽입 자리가 같고
(20행 `1. 검색창에 질의 입력 → 관련도순 결과 목록(제목, URL, 스니펫 3줄 이내,` 뒤),
`tests/test_docs.py` 의 검사 수만 30 → 31 로 늘었다(새 의무 단언 하나).

죽은 자리를 인용 주소로 접으면 **18자리 중 17이 죽고 1이 산다.**

- **주소 축**(`test_spec_citations_point_at_real_lines`) 5건 —
  `tests/test_serve.py:914`·`:925`·`:955` · `e2e/design_check.py:2`·`:444`
- **문구·값 축**(`test_spec_quotes_match_cited_lines`) 15건 —
  값 5(`design_check.py:33`·`:34`·`:36` · `perf_crawl.py:53` · `quality_eval.py:34`) ·
  문구 10(`perf_crawl.py:1`·`:151`·`:253`·`:275` · `quality_eval.py:34` ·
  `test_quality_eval.py:153` · `test_serve.py:918`·`:925`·`:936`·`:955`)

**계획 68 이 앵커를 심은 열두 자리 중 여덟이 이 목록에 새로 들어왔다** —
`perf_crawl.py:1`·`:151`·`:253`·`:275` · `quality_eval.py:34`(문구 축) ·
`test_serve.py:918`·`:936`·`:955`. 계획 전이라면 이 여덟은 **주소가 빈 줄이 아니기만
하면 조용히 살아 나갔다.** 계획 1절이 「열한 자리가 조용히 산다」고 적은 문장이
**오늘 실물 변이에서 반대로 뒤집힌 것**을 봤다.

**살아남은 하나는 `e2e/quality_eval.py:3` 이다** — `concept.md:22-23` 범위 인용이고,
21행 뒤에 한 줄이 끼면 옛 22행 내용이 23행으로 가는데 **23은 아직 범위 안**이다.
반복 395 가 72조합을 정적으로 재서 「생존은 정확히 범위 인용 넷의 구조적 성질」이라고
적은 것과 **글자 그대로 같다** — 오늘 사양 쪽에서 미는 방향으로 실행으로 되샀고,
정적 판독이 맞았다. 처방은 이미 `digest.md` 「다음 계획 후보」 `[5]` 에 있다
(문구 축을 범위 전체가 아니라 **시작행**에서 찾기). **이 phase 에서 안 고쳤다** —
계획 68 의 범위 밖이고 다음 계획의 입력이다.

### 3-2. V2 — 완료 기준 3 을 실행으로 되샀다

계획 2절의 착수 탐침 `M1` 이 **계획 전 `Ran 627` `OK` rc 0 으로 생존**하던 그 편집이다
(앵커가 없는 자리라 45행이 빈 줄만 아니면 통과했다). 오늘 같은 편집이

```
FAIL: test_spec_quotes_match_cited_lines (tests.test_docs.SpecCitationTest) [e2e/perf_crawl.py:1 문구]
FAILED (failures=1)
```

로 **죽는다.** 실패 라벨에 고칠 파일·줄·축이 통째로 찍혀 화면만 보고 어디를 고칠지 안다.
개발 phase(반복 394)가 산 판정을 오늘 **성한 실물 사본**에서 되샀다.

## 4. 완료 기준 — 오늘 실행으로 다시 쟀다

| # | 기준 | 실측 | 판정 |
|---|---|---|---|
| 1 | 심은 직후 RED 11건(자리가 2절 목록과 같다) | 반복 394 가 샀다(실측은 **12건** — 계획의 11 은 `quality_eval.py:34` 를 값 앵커로만 세어 하나 적게 잡은 것) · 오늘 재실행 없음 | 충족(과거 phase) |
| 2 | 정정 뒤 전수 `OK` rc 0 · 627 → **628** · `README.md` 함께 | 2절 `Ran 628` `OK` rc 0 · `README.md:104` 「단위 628건」 | 충족 |
| 3 | **`M1` 이 이제 죽는다** | **V2 `failures=1`** · rc 1 · 라벨 `e2e/perf_crawl.py:1 문구` | 충족(오늘 실행으로) |
| 4 | 표본 셋 `+1` 밀림이 자리마다 1건 이상 죽는다 | 반복 394 `V2`(`failures=2`) · 반복 395 가 **72조합 전수**로 넓혀 68/72 사망 · 오늘 V1 이 사양 쪽에서 17/18 사망 | 충족 |
| 5 | 오탐 0 — 성한 트리에서 안 운다 | **V0 `Ran 31` `OK` rc 0** · 전수 `Ran 628` `OK` rc 0 · e2e 스크립트 둘 다 rc 0 | 충족 |
| 6 | 추출기를 죽이는 변이가 **새 단언에서** RED | 반복 394 `V3` `failures=14`(새 단언 13 + `MIN_CHECKS` 1) · 반복 396 이 정적으로 재확인 · 오늘 안 샀다(예산) | 충족(과거 phase) |
| 7 | 사양·제품 무변 | `git diff --stat 274c0eb^ HEAD -- src/ docs/specs/ data/` **빈손** rc 0 · `data/crawl.db` sha256 `85c96744…5bda18` **무변**(계획 65·66·67 e2e 가 적은 값과 한 자도 안 다르다) · 재색인 0 · 스키마 0 · 새 의존성 0 | 충족 |

## 5. e2e 가 잡은 것 — 0건, 다만 기록을 하나 정정한다

diff 의 주장을 실물과 다시 대조해 어긋난 곳이 없었다. 정정 하나:

- **계획서 2절·3절의 「앵커 없음 11」은 12 이다.** `e2e/quality_eval.py:34` 는 값 앵커가
  있어 「앵커 있음 7」에 들어갔는데, 문구 축에서도 새로 세어져 오늘 V1 에서 값·문구
  **두 라벨로 각각** 죽는다. 개발 phase(반복 394)가 이미 「RED 12」로 적었고 리뷰(396)도
  「인용 12자리 정정」이라 적었다 — 계획서 숫자만 갱신이 안 된 것이다. 판정에는 영향이
  없다(정정 자리 집합은 같다).

**안 산 것 — 그리고 그 사유.**

1. **e2e 21종 개별 재실행 0회.** 이번에 돌린 것은 diff 가 실행 경로를 건드린 **둘**
   (`design_check.py`·`perf_crawl.py`)뿐이다. 나머지 19종은 이 계획의 diff 가 닿지 않았다
   (`git diff … -- e2e/` 가 세 파일 일곱 줄). 안 돌린 것을 통과로 적지 않는다.
2. **새 e2e 파일 0개.** 만들면 `unittest discover -b -s tests` 와 겹쳐 「1회만」이 깨진다
   (`rules/e2e.md` 1절). `ls e2e/*.py` 는 어제와 같은 **21**.
3. **완료 기준 6(추출기 죽이는 변이)은 오늘 안 샀다.** 반복 394 가 실행으로, 396 이
   정적으로 두 번 산 판정이고 변이 예산 2판을 V1·V2 에 썼다. V1 이 더 새로운 축
   (사양 쪽 · 계획 전후 숫자 비교)이라 그쪽을 골랐다.
4. **범위 인용 처방(`digest [5]`)은 안 건드렸다** — 계획 68 의 범위 밖. 오늘 V1 이 그
   자리를 **사양 쪽 변이에서 처음 재현**했으니 다음 계획의 근거가 하나 더 굵어졌다.

## 6. 한도 — 지킨 것

- **러너 규율 위반 0회.** 전수 1회 · e2e 스크립트 2회 · 사본 3판을 전부 맨몸으로 돌렸고
  판정 줄(`Ran … / OK / FAILED / rc`)을 매번 눈으로 확인했다. `2>&1`·`>/dev/null` **0회**.
- **편집은 저장소 밖에서만** — `mktemp -d` 사본, 파이썬 줄 편집(`sed` 0회).
  끝난 뒤 `git status --porcelain` **빈손**. 실물 `src/`·`tests/`·`e2e/`·`README.md`·
  `docs/specs/` 는 이 phase 에서 **0줄** 움직였다.
- **예산** — 도구 호출 20회 이내 · 전수 1회 · 변이 2판. 변이 3판(V0 포함)을 **한 호출**에
  묶어 샌드박스 안에서 대화가 왕복하지 않게 했다(정지 사고 2회의 자리).
- `data/crawl.db` 무변경 · 재색인 0 · 스키마 0 · 새 의존성 0(stdlib) · `docs/specs/` 읽기만.
- 원격 `loop/passage-cost-band` 위에 오늘 커밋을 쌓는다. `main` 직접 커밋 0 ·
  PR #7 무접촉(`gh` 호출 **0회**) · `--no-verify`·`--force`·`--amend`·`rebase` **0회**.
  병합은 사람 몫이다.
- 도구 산출물 없음 — 브라우저 도구를 쓰지 않는 프로젝트라 `test-results/` 에 해당하는
  경로가 없다. 위 표의 판정 줄이 실행 출력 전부다.
