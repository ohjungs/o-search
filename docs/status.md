---
signal: GREEN
phase: 개발
step: 3
attempt: 0
iteration: 253
updated: 2026-09-02
ctx: 60
night_iterations: 100
night_red: 0
night_retries: 0
plan: passage-api
---

# 현재 상태

**개발 스텝 2 가 초록이다 — 다음은 스텝 3(측정기 `e2e/passage_eval.py`).** 설계서
`docs/design_passage-api.md` · 계획서 `docs/plan_passage-api.md` · 브랜치
`loop/passage-api` · 기점 `6e99031` · 스텝 3개 중 **2개 완료**.

**`GET /passages` 가 섰고, 계약은 새로 만들지 않고 물려받았다.** `do_GET` 의 404 줄을
`not in ("/search", "/passages")` 로 넓히고 **try 사다리 한 벌을 두 경로가 나눠 쓴다** —
갈래마다 `try` 를 두면 400·500·503 판정이 **세 벌**이 되고 계획 47 이 한곳에 모은 것이
다시 흩어진다(스텝 1 의 `passages()` 가 `search()` 를 부른 것과 같은 이유다). 그래서
`/search` 의 200 조립은 `else` 안에 **그대로 있고** 화면 경로(`_do_html`)는 손도 안 댔다.
제품 순증 **24줄**(주석 포함) · 새 파일 0 · 의존성 0.

**계약 셋을 단언으로 못박았다.** 최상위 키는 `{"version","query","passages"}` **뿐이고
`has_next`·`page` 는 없다**(페이지네이션을 안 열었으므로 따라갈 손잡이를 안 준다) ·
문단 수는 **서버 상수** `PASSAGE_LIMIT = 10` 이라 `&limit=50` 이 안 먹는다 ·
`page != 1` 은 **400**(받되 거절한다) · 문단은 `MAX_PASSAGE = 2000`자에서 자른다.
문단 목록은 `indexer.passages()` 결과와 **URL·position·text 까지 같은 것**을 단언한다 —
서버가 문단을 다시 고르면 그 자리에서 죽는다.

**변이 M6~M8 셋 전부 RED 를 눈으로 봤다**(`.mutation-lock` 걸고, 원복은 **초록이 아니라
`git diff` 로** 확인 — 기준 패치를 떠 놓고 매번 diff-of-diffs 로 잔여물 0 을 봤다 ·
**커밋된 변이 0**). **M6**(`page != 1` 거절 삭제) 2건 — 실패 출력이 *"page=2 에 1페이지와
똑같은 문단 목록"* 을 그대로 찍어, 설계가 «조용히 무시하면 소비자가 같은 문단을 영원히
받는다» 로 적은 문장이 실물이 됐다 · **M8**(자르기 삭제) 1건(`5999 != 2000`) ·
**M7**(`_send` 안 쓰고 직접 JSON) 2건.

**M7 이 단언 하나를 더 낳았다 — 그 변이는 `version` 만이 아니라 `nosniff` 도 같이 잃는다.**
기존 `test_responses_carry_nosniff_and_html_carries_a_csp` 가 경로 **셋만** 돌아서 새
진입점의 보안 헤더를 재는 단언이 **0건**이었다. `/passages` 를 그 목록에 더하고 **변이가
살아 있는 동안 RED 를 본 뒤** 원복했다(사다리 적용 제외 — 신뢰 경계다).
**`/search`·화면 무변경은 기존 스위트가 대조군이다** — `TestSearchEndpoint`·
`TestScreenMatchesJson`·`TestSchemaVersion` 이 전부 초록 그대로고, `search_api_e2e.py`
가 프로세스 밖에서 한 번 더 확인했다(rc 0 · p95 2.09ms).

**탐색 1~7순위는 착수 가능 0건**(2026-09-02 실측): 단위 **506건 OK(12.431초)** ·
린터 없음 · `TODO`/`FIXME`/`HACK` 이 `src`·`tests`·`e2e` 전수 **0건** ·
`docs/candidates.md` 없음 · `docs/patches/` 없음 · `digest.md ## 다음 계획 후보` 의 열린
항목은 전부 **자기 조건을 적어 둔 것**(다른 계획에서 · 실물이 보이면 · 도달 불가 ·
사람이 정할 갈림길) · `## 판단 필요` 는 전원 **승인 대기**. 그래서 **8순위(사양)** 에서
잡았고, 그 자리는 `index.md` 의 확장 분할 10번이 *"계획 47 뒤"* 로 못박아 둔 자리다.

## 직전 계획 (47 — DONE)

**계획 47 `db-open-atomic` — e2e 통과. DONE.** 계약(400·404·500·503·`version`)이 이제
**README 가 가르치는 형태**(상대 경로 `data/crawl.db`)와 **배포가 실수하는 형태**(빈
`DB_PATH`) 위에서 재진다.

**단위 506건 OK(12.380초) · `e2e/*.py` 19종 전수 rc 0 · 제품 `src/` 0줄 · 새 e2e 파일 0개.**
계획서·설계서는 `plan_history_033.md`·`design_history_033.md` 로 옮겼고, 결과는
`docs/e2e/db-open-atomic/result.md` 에 있다.

## 계획 47 이 한 것 (2026-09-02 · e2e phase 1/1)

**새 파일을 만드는 대신 기존 둘의 «형태» 를 바꿨다.** 계획과 리뷰가 닫은 두 구멍
(TOCTOU · 빈 `db_path`)을 재는 단언이 **단위 여덟·e2e 두 곳 모두 `tempfile` 의 절대
경로**였는데, README 의 세 명령은 전부 상대 경로다. 절대 경로는 단위 506건이 전수로
밟으므로, e2e 를 **아무도 안 밟던 형태**로 옮기는 것이 더 짧고 더 많이 잰다.

| 어디 | 무엇을 더했나 | 실측 |
|---|---|---|
| `search_api_e2e.py` | 서버를 `cwd=tmp` 에서 **`data/crawl.db`** 로 띄운다 | ①~⑥ 이 통째로 그 형태 위에서 — 200 · 400 · 404 · 501 · 치우면 **503** · 되돌리면 200 |
| 〃 | ⑦ **빈 `DB_PATH` 서버** (`serve "" --port 0`) | **503** (가드가 없으면 `200 {"results": []}`) |
| `indexer_e2e.py` | ⑤ README 형태 질의 · 없는 상대 경로 | rc 0 정답 URL · **rc 1** + `DB 파일이 없다` · **빈 파일 생성 0** |

**변이 둘 전부 RED**(`.mutation-lock`). **R1**(`file:`→`file://`): `indexer_e2e` rc 1
(`invalid uri authority: data`) · `search_api_e2e` rc 1(**`q=김치 → 500`**) · 단위 1건 사망.
**G1**(빈 경로 가드를 `if False` 로): `빈 DB 경로인데 200 다`. 원복은 **초록이 아니라
`git diff -- src/`(0줄)** 로 확인했고, 되돌린 뒤 rc 0 을 다시 봤다. **커밋된 변이 0.**

**리뷰가 넘긴 측정에 답했다 — `urllib.request` 는 계획 후보가 아니다.** CLI 시작
**33.4ms** 중 `import urllib.request` 몫 **19.9ms**(맨몸 9.7ms) · `indexer_e2e` 총 2.89초에서
CLI 9회 = 약 **6%**. 「경량」 축에 **시작 시간 예산이 없고**(메모리·의존성이 그 축이다)
서버는 한 번만 import 하므로 p95 에 안 얹힌다. *"숫자가 뒤집으면 후보"* 가 안 뒤집혔다.

**레이스는 e2e 로 안 벌렸다** — `connect` 직전에 파일을 지우는 훅은 프로세스 밖에서 걸
수 없고, 진짜 경합은 마이크로초라 재현하려면 flaky 한 반복 실행이 된다. 단위
`TestDbOpenIsAtomic` 여덟 건이 결정적으로 잰다(ponytail 1번). 사유는 result.md 7절.

## 한도 (매 반복 확인)

- `main` 직접 커밋 금지 · `--no-verify` 금지 · 외부 네트워크 금지 ·
  `docs/specs/` 읽기만 · `data/crawl.db` 무변경 · 의존성 추가 금지(stdlib 만).
- **러너 호출에는 출력 조작을 아무것도 붙이지 않는다 — 파이프·`grep`·`tail`·
  `2>&1`·`2>/dev/null` 전부. 다른 명령과 같은 호출에 태우지도 않는다.**
  **이번 반복(개발 2) 러너 호출 6회 · 출력 조작 0회 · 명령 잇기 0회 — 네 반복 연속
  깨끗하다.** 단위 전수 5회(RED 1 · GREEN 1 · 변이 3)와 `search_api_e2e.py` 1회가 전부
  맨몸·단독으로 돌았다(최종 **544건 OK · 12.749초**). 변이 기준 패치는 러너가 아니라
  `git diff` 이고 **저장소 밖(스크래치패드)** 에 떴다 — 제품·테스트 트리에 파일이
  생기지 않았고 `.mutation-lock` 도 지웠다.
- 이번 반복 실측: 제품 `src/` **+24줄**(`serve.py` 하나, 주석 포함) ·
  테스트 **+141줄**(단위 **530 → 544**, 새 14건) · 새 파일 **0개** · e2e **0줄** ·
  의존성 0 · 변이 **3회**(전부 되돌림 · **`git diff` 로 확인** · 커밋된 변이 0) ·
  `data/crawl.db` **무변경** · 스키마 무변경 · `docs/specs/` 무변경.
- 기준선: 단위 **544건** · `e2e/*.py` **19종**(이번엔 `search_api_e2e.py` 1종만 돌렸다 —
  rc 0 · p95 **2.09ms**, 전수는 e2e phase 몫) · 품질 ko **20/20** · en **19/20** ·
  처리량 [열림] **10.22/s** · 디자인 **4축 통과**.
- **README 카운트 가드가 울었고 따랐다** — `530 → 544`(`README.md` 67행, 두 반복 연속).
  e2e 는 **19종 그대로**(스텝 3 이 20종으로 만든다).
- `history_current.md` 는 **286줄 → 회전 없음**(상한 300 — **다음 반복은 회전이다**).
  아카이브 **31개** · `digest.md` **198줄**(상한 200 — 넣으려면 닫힌 항목 자리를 만든다).
- **개발은 `digest.md` 를 안 건드렸다** — 새 명부 줄도 새 후보도 없다(198줄 그대로).

## 다음 — 개발 phase (계획 48 스텝 3, 마지막)

**활성 계획 `passage-api` · 설계서 `docs/design_passage-api.md` 가 계약을 확정했다.**

- **스텝 3 — 두 숫자를 잰다.** 새 파일 `e2e/passage_eval.py`: 사양 기능 8(문단의 **90%**
  가 질의어 또는 그 2-gram 을 포함) · 성능 5(p95 **500ms**). **종료 코드 0/1/2** 로
  판정한다(2 = 측정 불능). `e2e/quality_eval.py` 는 **한 글자도 안 고친다**(recall 숫자가
  매달린 도구다) · 코퍼스·질의 JSON 은 **동결 그대로** 읽고 새로 하는 것은 **HTML 포장**
  뿐이다(본문을 문장 단위 `<p>` 로 — 색인 본문이 64/64 바이트 동일함은 스텝 1 에서 실측).
- **숫자를 둘 낸다** — 정확도(≥90%)만으로는 기본값이라 **채택률**(매치 블록을 못 찾아
  안 낸 문서의 비율, 실측 99.5%)을 옆에 놓는다. 성능은 **HTTP 왕복**으로 재고
  (`--port 0`), 3000문서 기준이라 사양의 100만 문서와 다르다는 주의를 함께 적는다.
- 검증: 그 측정 명령을 `docs/project.md` 명령 절에 실행 확인 날짜와 함께 적고,
  README 도 갱신한다. **e2e 19 → 20종이 되므로 README 카운트가 또 움직인다.**
- 스텝 3 이 끝나면 개발 phase 완료 — 다음은 테스트 phase(설계 변이 표 M1~M8 전수).
- **설계가 재색인·스키마 변경을 안 골랐다** — 계획 5절의 보류 조건에 안 걸린다.
- 남은 확장 둘: **속도 제한**(사람이 시점을 정한다) · `recrawl`(원래 사양의 마지막).
- `digest.md ## 다음 계획 후보` 의 열린 `[4]` 들은 그대로다 — 다음 탐색이 재서 고른다.

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-02 확인)

`origin/main` 은 `494313b`(PR #5 병합). 계획 43·44·45·46·**47** 의 커밋들(`bc40ea9` 이후)이
아직 안 갔다. **남은 것 (사람 결정)**: 그것들을 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).
**리뷰 커밋 `fe8b6a4` 가 안 올라가 있던 것은 이번 반복이 함께 푸시했다.**

## 사람이 정할 것 — 넷이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, `~/.claude/skills/loop-harness/rules/docs.md` 에는 그 예외가 없다.
   **저장소 밖이라 루프가 못 고친다.**
4. **«러너 호출을 다른 것과 섞지 않는다» 를 문장이 아니라 훅으로 옮기는 일.**
   재발 **17회**. 저장소 쪽 자리(`docs/project.md ## 명령`)에 이미 적혀 있는데도 났고,
   남은 자리는 `~/.claude/skills/loop-harness/rules/` 라 **저장소 밖이라 루프가 못 고친다.**
   **이번 반복이 0 으로 끝냈다고 항목이 닫히지는 않는다** — 28회 중 0 은 한 표본이고,
   ⑭⑮⑯(출력 조작)을 끊자 ⑰(명령 잇기)이 났던 것이 «문장을 조여도 옆으로 샌다» 의
   근거다. 닫으려면 **판정 명령을 단독 실행으로 강제하는 PreToolUse 검사**가 필요하다.

**사양이 사람에게 남긴 것 셋** (`docs/specs/concept.md` 의 `## 사람이 정할 것`):
속도 제한 시점(가정 IP 당 분당 60회) · JSON 검색과 근거 문단의 분할(가정 쪼갠다 —
**계획 46 이 그 가정을 따랐고 닫았다**) · 사양 숫자들이 초안이라는 것(90% · 500ms ·
60회/분). 첫째와 셋째는 아직 열려 있다.
