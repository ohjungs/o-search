# 기록 (현재)

### 반복 564 — 열세 밤 만에 계획이 섰다 · 「안 바뀌는 것」과 「매 반복 읽을 것」을 갈랐다

- **빈손 대조 핀이 다섯 밤 연속 맞았다** — 첫 줄 빔(`c725ed7..HEAD` 무변경) · 이슈 0건(rc 0)
  → 1~7순위를 안 열고 **전수 한 번 763 OK 18.6s**. 게이트 ⑦ 은 여덟 번째 실측으로
  `402da0af…` 가 내 슬러그에 없음을 확인(내 것은 `7edc04b2…`) — 그 파일의 84% 는 남의 컨텍스트다.
- **탐색이 아니라 «안 잰 것»에서 나왔다.** 562·563 이 둘 다 `project.md` 를 「거의 안 바뀌는
  사실」이라며 넘겼는데, **안 바뀌는 것과 매 반복 읽어야 하는 것은 다른 질문이다.** 절별로
  재니 249줄 중 **229가 두 절**(명령 94 · 품질 기준 135)이고 그중 약 170이 기준선 숫자·
  변이 확인 기록·캡 설계 논증 — 그 도구를 **실제로 쓰는 반복에만** 필요한 것들이다.
- **계약을 테스트에서 뽑고 나서 계획을 썼다**(추측 금지). `test_readme.py:103` 러너 줄 양쪽
  존재 · `ProjectConstTest` 의 `mod.CONST = 숫자` 인용 **최소 1건**(`CONST_CITATION_FLOOR = 1`,
  0건이면 검사가 아무것도 안 재며 초록) · 맨몸 규율은 **명령을 읽는 자리**에 남는다.
- **셋째 압축을 다른 파일에서 하는 이유를 적었다** — 앞 둘이 되밀린 것은 `status.md`·기록이
  **밤마다 쓰는 파일**이라서다. 옮긴 줄이 안 없어졌는지는 눈이 아니라 `comm -23` 으로 본다.

### 반복 565 — 스텝 1 · 「품질 기준」 135 → 22줄 · 줄 손실 0

- **옮기는 것과 다시 쓰는 것을 갈랐다.** 절 몸통 135줄을 `docs/baselines.md` 로 **줄 단위
  그대로** 부어 넣고(파이썬 슬라이스 · 손으로 안 옮겼다), `project.md` 에는 축 5개 표 +
  종료 코드 관용구 + 상수 둘만 **새로 썼다**. 손으로 옮기면 그 자리에서 문장을 다듬게 되고,
  다듬은 것과 잃은 것은 사후에 구별이 안 된다.
- **눈으로 대조하지 않았다** — `comm -23 <(git show HEAD:docs/project.md | sort -u)
  <(cat project.md baselines.md | sort -u)` 가 **비었다**. 249 → **136줄**, 전수 **763 OK**(18.8s).
- **내가 적은 완료 기준 하나가 틀려서 계획서를 고쳤다.** 「`wc -l` 이 135 이상 줄어 있다」는
  **22줄짜리 대체본을 안 센 산술**이다 — 135를 빼고 22를 넣으면 순감은 113이다. 재는 축을
  파일에서 **절**로 옮겼다(이 스텝이 실제로 책임지는 것이 그 절이다). 파일 축 ≤60 은 스텝 2 몫.
- **이 스텝이 만든 낡은 포인터를 같은 커밋에서 닫았다** — `project.md:41` 의 「아래 "품질 기준"
  절에 적었다」가 가리키던 내용이 `baselines.md` 로 갔다. **옮기기는 가리키는 줄을 낡게 한다.**

### 반복 566 — 스텝 2 · `## 명령` 94 → 19줄 · **project.md 249 → 60줄**

- **예산을 역산하고 들어갔다.** 고정분(파일머리 5 + 절머리 5 + 브랜치 4 + 품질 22 + 한도 4 +
  건드리지 2 = 42)을 먼저 세니 `## 명령` 몸통 예산이 **18줄**로 나왔다. 목표에서 거꾸로
  세지 않았으면 "적당히 줄이고" 67줄쯤에서 멈췄을 것이다.
- **e2e 13종은 명령을 한 줄씩 적는 대신 «템플릿 + 이름»으로 접었다**(`e2e/<이름>.py` + 이름 13개).
  계약을 기계로 다시 쟀다 — 옛 문서가 부르던 **e2e 모듈 15개가 전부** 지금도 불리고,
  불리는 이름 중 **실물 `e2e/*.py` 에 없는 것은 0개**다(문자열 대조보다 센 축이다).
- **마지막 1줄을 「지우지 않는다」로 버티지 않았다.** 61줄에서 남은 후보가 파일 머리의 초기화
  주석이었는데 그것이 **지금은 거짓**이다 — 「초기화 시점에 코드가 0줄이라 명령이 없다」.
  거짓 문장을 보존 약속 때문에 남기는 것이 더 나쁘다. 살아 있는 절(「확인 안 된 명령은 적지
  않는다」)만 한 줄로 남겼고, `comm -23` 에 그 **2줄만** 뜨는 것을 확인했다 — 조용한 손실이 아니다.
- **필수 읽기 600/600 → 411/600.** `project.md` 가 `rules/docs.md` 1절의 제 예산 **60 을
  처음으로 지켰다**(4.2배 초과였다). 전수 **763 OK**(18.6s) · 제품 `src/` **0줄**.

### 반복 567 — 리뷰+e2e · 계획 97 마감 · **리뷰가 내 diff 의 사실 오류를 잡았다**

- **[리뷰]** 급한 하나는 **내가 넣은 것**이다 — 「종료 코드는 다섯이 같은 관용구다(0/1/2)」인데
  `perf_search` 는 `return 0` 하나뿐, `crawl_delay_e2e` 는 `sys.exit` **0건**이라 둘 다 2 를 못 낸다.
  옛 문서는 `design_check` 한 줄에만 붙였다 — **넓힌 것이 이번 diff** 다. 셋으로 정정.
- **옮기기는 «옮겨진» 줄도 낡게 한다**(565 교훈의 뒷면) — `baselines.md` 의 자기모순·자기참조·
  `project.md` 와 중복 10줄을 정정(245→234). 사라진 14줄 전부 의도임을 `comm -23` 으로 재확인.
- **[e2e]** 문서를 **기계로 실행**했다 — `project.md` 에서 13개 이름을 뽑아 조립해 돌려 **13/13 rc=0**.
  접힌 3줄이 옛 13줄과 같음을 증명한다. 다만 **성공 경로만 밟아 위 오류를 못 봤다.**
- **마감** `plan_history_072.md` · 잔여 셋은 계획 밖 파일이라 `status.md` 에 박았다 ·
  **이 기록을 20→9줄로 줄였다**(563 이 지목한 레버) · 필수 읽기 **600/600 → 483/600** · 전수 **763 OK**.

### 반복 568 — 짧은 경로 · **옮긴 자리를 무는 자가 안 따라갔다**

- 계획 97 리뷰가 남긴 ㉠. `ProjectConstTest` 가 사는 이유는 docstring 이 스스로 적어 뒀다 —
  「**그 자리가 세 번 낡았기 때문이다**」. **그 자리(캡 설계 논증)가 97 에서 `baselines.md`
  로 이사했는데 무는 자는 `project.md` 만 읽고 있었다.** `const_gap` 을 두 파일 루프로.
- **음성 대조가 증거다** — `baselines.md` 의 `FRESH_DAYS` 를 30→99 로 변이하니 새 검사는
  잡고 **옛 검사는 `None`**(생존). 복구 확인(diff 빔). 오늘은 같은 상수가 두 파일에 다 남아
  우연히 덮이던 것이라, `project.md` 를 더 줄이면 조용히 통과할 자리였다.
- **백지 패스가 내 diff 에서 둘을 잡았다** — 클래스 docstring 이 아직 「`project.md` 가」로
  단정한 것 · 파라미터 이름 `project_text`. 메시지도 파일을 받아 찍게 고쳤다(`[R71-2]` 와 같은 자리).
- **회전**: 기록이 295/300 이라 반복 **552~563**(빈손 열둘 전체)을 `history_084.md` 로
  밀어냈다(295→57). 대가는 `digest.md` 210→**212** — 게이트 ⑥ 이 두 줄 더 나빠졌다.
- 전수 **763 OK** · 제품 `src/` **0줄** · 필수 읽기 **270/600**.

### 반복 569 — 짧은 경로 · 코드가 찍는 포인터가 이사를 따라갔다

- 계획 97 리뷰가 남긴 ㉡. `perf_search.py:124` 는 **매 실행** 「docs/project.md 품질 기준과
  비교할 것」을 찍는데 비교할 기준선은 97 이 `baselines.md` 로 옮겼다. `perf_crawl.py:156` 도 같다.
- **테스트가 안 보는 방향이다** — 문서가 코드를 인용하는 것은 `const_gap` 이 물지만
  (568 이 그 범위를 넓혔다) **코드가 문서를 가리키는 것은 무는 자가 없다.** 그래서 실측으로 봤다:
  `기준선 — p95 1.00ms (예산 300ms 의 0.3%). 예산은 docs/project.md, 기준선은 docs/baselines.md`
- 예산(300ms)은 `project.md` 표에 남았고 기준선만 갔으므로 **둘 다 적었다.**
  `passage_eval.py:35` 의 `docs/project.md` 는 **명령**을 가리켜 그대로 뒀다 — 명령은 안 옮겼다.
- 전수 **763 OK** · `perf_crawl` 3시나리오 rc=0([차단] 10.23/s · 최소 간격 1.003s) · 제품 `src/` **0줄**.

### 반복 570 — 계획 phase · 계획 98 `e2e-roster` 착수 · **갈림길을 측정으로 닫았다**

- **탐색 여덟 출처는 또 비었다** — 전수 763 OK · 코드 `TODO` 0(잡힌 한 건은 `test_indexer.py:1027`
  픽스처 HTML 안의 예시) · 린터 없음 · `gh issue` 0건 · 4순위 스캐너는 이 저장소에 존재하지 않는다.
  5~7순위의 열린 8점은 전부 게이트에 묶여 있었다.
- **근거는 1순위 스윕 «자신의 빈 칸»에서 나왔다** — 「테스트를 돌린다」가 1순위인데
  `e2e/*.py` 22개 중 **5개가 한 번도 안 돌았다**. 돌리는 것은 판단이 아니라 측정이라 밤이 할 수 있다.
- **다섯 전부 rc=0**: `crawl_politeness`(A 예외 뒤 2.01s) · `pagination_ui`(4축) · `recrawl`(간격
  1.009s · 404 묘비) · `retry_interval`(https 5.01s) · `url_normalize`(표기 10 → 문서 6).
- **그 결과가 ㉢ 의 갈림길을 지웠다** — 「왜 안 도는지를 적는다」 갈래는 다섯이 다 도는 이상
  근거가 없다. 남은 등재는 사람의 선택이 아니라 사실 기록이다. **밤이 못 여는 것과 밤이 안 잰
  것은 다르다** — 564 가 `project.md` 에서 배운 자리와 같다.
- **등재만 하면 `digest.md` 반복 실패 최상단(「문서에만 적고 재는 자를 안 만든다」 2회)을 세 번째로
  밟는다.** 그래서 2스텝: ①`e2e_roster_gap` + `E2eRosterTest` 로 RED 를 보고 ②`project.md` 13→18종.

### 반복 571 — 개발 스텝 1/1 · 명부와 무는 자를 한 스텝에서 닫았다

- **계획을 고쳤다: 스텝 둘 → 하나.** 쪼갠 대로 가면 `dev.md` 0절 TDD 주기가 두 커밋에 걸쳐
  끊기고 **②실패를 본 뒤 ③구현 전에 빨간 트리를 커밋해 남긴다.** 게이트 ⑨ 는 그 트리가
  `main` 으로 가는 경로가 실재함을 이미 보였다(`d74ed2f`). 내가 그 경로를 만들 이유가 없다.
- **RED 를 눈으로 봤다** — `project.md 가 e2e/ 의 5개를 이름으로 안 부른다 — crawl_politeness_e2e ·
  pagination_ui_e2e · recrawl_e2e · retry_interval_e2e · url_normalize_e2e`. 그 뒤 등재해 **769 OK**.
- **모집단을 디스크로 잡은 것이 이 자의 전부다.** 문서에서 출발해 「적힌 이름이 실재하나」를
  재는 자는 `test_readme.py` 가 이미 갖고 있었고 **그 방향은 빠뜨린 것을 구조적으로 못 본다** —
  실제로 763건이 다섯이 빠진 상태로 전부 초록이었다.
- **부분 문자열로 세지 않는다** — `interrupt_e2e` 는 `indexer_interrupt_e2e` 의 부분 문자열이라
  실재하는 함정이다(`E2E_WORD` 로 낱말 단위). 갈래 테스트가 그 자리를 붙든다.
- **기존 자가 내 diff 를 물었다** — `test_readme.py` 의 (단위, e2e) 대조가 763→769 를 잡아
  README·`project.md` 양쪽 숫자를 고쳤다. **음성 대조**: `recrawl_e2e` 를 지우니 새 자가 그
  이름 하나를 물었고 원복 후 초록. `project.md` 는 이름 줄을 접어 **예산 60줄을 그대로 지켰다.**

### 반복 572 — 테스트 스텝 · 변이 10판으로 새 자의 이빨을 쟀고 둘이 살아 둘을 닫았다

- **변이 10판 중 8이 죽고 2가 살았다**(`PYTHONDONTWRITEBYTECODE=1` **와** `PYTHONPYCACHEPREFIX`
  둘 다 준 채로 전수). 죽은 것: 부분 문자열로 세기 · 판정 삭제 · 첫 하나만 찍기 · `glob` 오타로
  모집단 비우기 · 하한 못 뽑기는 **살았다** · 낱말에서 밑줄 빼기 · `stem`→`name` · `E2E_DIR` 이사.
- **산 둘을 그 자리에서 닫았다**(단언은 하나도 안 낮췄다 · 새 단언 셋):
  ① **`doc` 인자가 죽은 칸이었다** — 메시지의 `%s` 를 `"project.md"` 로 못박아도 아무도 안 물었다.
  이 저장소가 이미 밟은 자리다(`[R568-2]` — `const_gap` 이 `baselines.md` 의 오류를
  「project.md 가」로 귀속했다). **형제 `const_gap` 도 같은 구멍이다** — 실물이 두 문서를
  도는데 살아 있는 문서는 갭을 안 내서 귀속 축을 아무도 안 밟는다.
  ② **`sorted` 가 안 재지고 있었다** — 풀어도 초록이라 같은 결함이 실행마다 다른 줄을 낼 수 있었다.
- **하한 못(`E2E_ROSTER_FLOOR` 1→0)은 살았고, 그것이 이 자의 정직한 값이다.** 갈래 픽스처를
  **디스크에서 조립**하기 때문에 모집단이 비면 갈래가 **먼저** 죽는다(M4·M8 이 실측) —
  못은 「아무것도 안 잰다」를 막는 **유일한** 방벽이 아니라 원인을 이름으로 찍는 몫만 한다.
  `const_gap` 쪽 못과 다르다(그쪽 픽스처는 디스크를 안 읽어 못이 유일하다). 그래도 안 뺀다 —
  실패 메시지가 「모집단이 비었다」를 직접 말하는 값이 못 값보다 크다.
- **기존 자가 또 물었다** — 새 테스트 1건에 `test_readme.py` 가 769→770 을 잡아
  `README.md`·`project.md` 숫자를 고쳤다. 전수 `PYTHONPATH=src python3 -m unittest discover -b tests`
  → **770 OK**(18.6s). 8점 미만 갭 둘은 `digest.md` 에 등재(라벨 「18종」 미측정 · 도우미 모듈 구분).

### 반복 573 — 리뷰 스텝 · 백지 패스가 자기 diff 에서 넷을 물었다

- **[R98-1] 내가 실측 없이 숫자를 올렸다** — 571 이 `project.md:13` 을 「약 19초」에서
  **「약 25초」**로 고쳤는데 오늘 실측은 `18.5·18.6·18.8·18.9s` 다. 25 는 이웃 문서의
  `perf_crawl` 소요(`baselines.md:217`·`crawl-throughput/result.md:50`)와 같은 값이다 —
  건수를 고치며 옆 숫자를 딸려 온 것으로 읽힌다. **19 로 되돌렸다.**
  이 파일은 루프가 매 반복 읽는 곳이고, 이 저장소가 세운 자들이 전부 이 부류를 막으려 산다.
- **[R98-2] 하한 못 주석이 572 가 거짓으로 잰 문장을 그대로 들고 있었다** — 「`const_gap` 이
  인용 0건에서 그런 것과 같다」. 같지 않다: 여기는 갈래 픽스처가 디스크에서 조립돼
  **갈래가 먼저 죽고**, 못만 1→0 으로 내리는 변이는 산다. 측정한 값을 주석에 앉혔다.
- **[R98-3] 천장 하나가 안 적혀 있었다** — 이 자는 **자리를 안 잰다.** 이름이 문서 어디에
  낱말로 서 있기만 하면 등재로 쳐서 「안 돌리는 것들」 목록에 적어도 조용해진다.
  자리를 재려면 명부 줄을 파싱해야 하고 그러면 줄 모양을 바꿀 때마다 거짓 RED 다 — 안 한다.
- **[R98-4] 대조 패스: 계획서 「하지 않을 것」의 `README.md` 가 실물과 어긋났다** —
  건수 가드가 **두 번**(763→769→770) 강제했다. **계획 76 이 같은 자리에서 같은 것을
  놓쳤다**(`[R76-2]`). 「e2e 종수는 안 건드린다」로 좁히고 단위 건수는 강제임을 박았다.
- 넷 다 **단언 무변 · 제품 `src/` 0줄** 이라 자동 수정. 전수 **770 OK**(18.8s).

### 반복 573 뒤 — 게이트 ⑨ 가 **두 번째로 실제 피해를 냈다** (기록만)

- 리뷰 커밋을 쓰려는 순간 `git commit` 이 **「커밋할 것이 없다」**를 냈다. launchd
  `autocommit` 이 13:57 에 내 미커밋 트리를 통째로 `자동 스냅샷 2026-09-13 13:57 —
  미커밋 작업 보존`(`55a57db`)으로 커밋하고 **원격까지 밀었다.** 563(`d74ed2f`) 이후 두 번째다.
- **이번엔 `main` 이 안 다쳤다**(4bed2e1 그대로) — 563 과 다른 점은 그것뿐이고, 원인·주기·
  자리는 같다. 트리는 내 것과 **바이트까지 동일**했다(`git diff` 공집합) — 잃은 것은 메시지다.
- 메시지를 되찾으려 `--amend` 했고(`e31d115`) **`--force-with-lease` 푸시가 거부됐다.**
  뒤이어 원격에 맞추려는 `reset --soft` 도 거부됐다. **그래서 지금 로컬과 원격이 갈라져 있다** —
  트리는 같고 커밋 해시만 다르다.
- **결과: `scripts/merge-to-main.sh` 가 첫 줄 `git push -q origin "$BRANCH"` 에서 막힌다**
  (non-fast-forward). 병합하려면 **브랜치 강제 푸시 한 번에 대한 승인**이 필요하다 — 게이트 ⑪.
- **이 자리는 밤이 못 연다.** 자동 커밋 주기를 고치는 것도(사용자 기계의 예약 작업),
  승인 없이 원격 역사를 다시 쓰는 것도 밤의 몫이 아니다. 네 findings 자체는
  `status.md`·`metrics.md`·`digest.md` 에 전부 남아 있어 메시지 유실로 사라진 것은 없다.

### 반복 574 — e2e 스텝 · 시나리오 1 이 첫 판에 실패했고 그것이 수확이었다

- **시나리오 1(명부를 기계로 실행)이 첫 판에 이름을 19개 뽑았다.** 열아홉째가
  `E2eRosterTest` — **571 이 그 줄에 넣은 포인터**다(`rc=2` · 그런 파일이 없다).
  사람에겐 유익한 한 마디인데 **그 줄은 기계가 읽는 줄**이라 유령 이름을 만들었다.
- **고친 것은 시나리오가 아니라 문서다**(`e2e.md` 7절). 포인터를 클래스에서 **파일**로
  바꿨다(`` `E2eRosterTest` `` → `` `test_docs.py` ``) — 점이 든 토큰은 맨 식별자 정규식에
  안 잡힌다. 추출기를 좁히는 길은 **자를 통과시키려고 자를 깎는 것**이라 안 갔다.
- **재실행 → 18/18 rc=0**(합 약 3분). 570 이 다섯을 처음 돌려 잰 값이 재현됐다
  (`crawl_politeness` 5.1s · `recrawl` 6.8s · `retry_interval` 15.7s · `url_normalize` 5.1s ·
  `pagination_ui` 11.9s) — **등재가 사실 기록이었다는 것이 두 번째 실행으로 확인됐다.**
- **시나리오 2(음성 대조) 통과** — 명부에서 `recrawl_e2e` 를 지우니 전수가 **1건만**
  실패하며 그 이름을 짚었고, 되돌리니 770 OK. 무는 자리가 하나로 모여 있다.
- **새 계약이 하나 생겼고 재는 자는 없다** — 「`**e2e N종**` 항목의 맨 식별자 백틱 토큰은
  정확히 명부다」. `e2e_roster_gap` 은 **자리를 안 재서** 못 문다. `digest.md` `[6]` 에
  합쳤다 — **한 줄이 둘을 함께 닫는다**(뽑은 이름 수 == 라벨 == `glob` 개수).

### 반복 575 — 계획 phase · 막힌 병합을 셋째 길로 풀고 계획 99 를 세웠다

- **게이트 ⑪ 이 사람 몫이 아니었다.** 적혀 있던 갈래 둘(강제 푸시 · 되감기)은 **둘 다
  히스토리를 고친다**는 같은 가정 위에 있었는데, 자동 스냅샷 `55a57db` 와 내 `e31d115` 는
  **트리가 바이트까지 같다**(`git diff` 빈손). 그냥 병합했다 — 충돌 셋은 내 쪽이 상위집합이라
  `--ours`, 결과 트리가 `fb1ac33` 과 **0줄 차이**임을 확인하고 커밋(`a9a14eb`).
  `merge-to-main.sh` 가 전수 **770 OK** 위에서 밀어 계획 98 이 `main` 에 들어갔다(`b8f0bb1`).
- **탐색 여덟 출처 중 일곱이 0건.** 1~4·8 은 명령으로 0/N(전수 770 OK 18.7s · 코드 `TODO` 0 ·
  이슈 0) · 5 는 보류가 보안 건 하나 · 7 은 `## 반복 실패` 세 항목의 처방이 전부 이미 섰다.
  **6순위 `[6]`** 하나가 남았고 **여는 조건을 다시 판정**했다 — 적힌 조건은 라벨 축의 것이고
  **유령 축은 574 에 이미 발동**했다(`E2eRosterTest` `rc=2`).
- **계획서를 쓰기 전에 처방을 실물에 대봤다.** digest 의 「수 == 라벨 == `glob` 개수」는
  실측에서 **18 == 18 ≠ 22** 로 **틀렸다** — `glob` 이 측정 넷을 포함한다. 닫을 등식을
  둘로 고쳐 계약 3 에 적었다(`## 반복 실패` 의 「등재된 처방은 근거지 설계가 아니다」 적용).

### 반복 576 — 개발 스텝 1/1 · 문서에서 디스크로 가는 자를 세웠다

- **`e2e_label_gap` + 갈래 7 + 실물 1.** 방향이 `e2e_roster_gap` 과 **반대**다 — 그쪽은
  디스크→문서(빠진 이름), 이쪽은 문서→디스크(**유령**)와 **라벨 드리프트**다.
  세 RED 를 눈으로 봤다: 유령이 이름을 찍고 · 라벨이 **두 수를 함께** 적고 · 앵커 실종이
  「항목을 못 찾았다」로 죽는다. 정상 픽스처와 실물은 `None`.
- **하한 못을 안 박았고 그 이유가 이번엔 정직하다** — 추출이 통째로 죽어 이름이 0개가 되면
  라벨(18)과 안 맞아 **그 자리에서 빨개진다.** 모집단이 문서 **안**에 있어서 그렇고,
  모집단이 디스크인 `e2e_roster_gap` 쪽은 못이 필요했다(계획 98 변이 실측).
- **계약 5 가 예고한 대로 건수 가드가 물었다**(770 → 778). 같은 숫자를 든 `project.md:13`
  은 기계가 안 보지만 함께 낡는다 — 낡은 채 두면 **[R98-1] 을 내가 다시 심는 것**이라
  둘 다 778 로 맞추고 계획서의 「하지 않을 것」 범위를 **명부 항목**으로 좁혀 다시 적었다.
- 전수 **778 OK** 18.7s. 제품 `src/` **0줄**.

### 반복 577 — 테스트 스텝 · 변이 8판이 전부 죽었고 구멍은 하나였다

- **이빨 실측 8판, 전부 잡힘.** 유령·라벨·맨 식별자 필터·앵커 실종·`doc` 칸·판정 순서 ·
  **이름 추출 전멸**(M7) · 형제 판정 무력화(M8). 계획 98 에서 살아남았던 `doc` 칸 변이는
  이번엔 실물 1건이 문다.
- **M7 이 주석을 측정으로 바꿨다** — 하한 못을 안 박은 근거(「이름 0개면 라벨과 어긋나
  그 자리에서 빨개진다」)가 실제로 실물 테스트를 죽였다. 못은 여전히 필요 없다.
- **구멍 하나: 빈 명부 0종은 0 == 0 이라 조용하다.** 못을 더 박는 대신 **모집단이 디스크인
  형제가 문다**는 것을 테스트로 쟀다 — M8 이 그 테스트만 죽인다. 「둘이 서로를 대체하지
  않는다」가 주석에서 내려왔다.
- 건수 가드가 **두 반복 연속** 물었다(778 → 779) — `README.md:104` · `project.md:13`.
- 전수 **779 OK** 18.7s.

### 반복 578 — 리뷰 스텝 · 같은 호출을 두 번 하던 갈래를 조였다

- **[R99-1](자동 수정)** `test_a_dotted_token_is_not_a_name` 이 「정상」 픽스처와 **완전히
  같은 호출**이었다 — 축이 하나 늘어난 척만 하고 재는 것은 같다. 라벨을 1 로 조여 세웠고,
  새 변이 **M9**(「슬래시·공백만 막고 점은 허용」)가 그 갈래를 죽이는 것을 확인했다.
  테스트 **수는 그대로**라 README 건수는 안 건드렸다.
- **패스 A 를 다른 세션에 못 넘겼다** — 이 세션에 Agent 도구가 없다(`review.md` 0절의
  「넘길 수단이 없으면 순서대로 두 패스」). 구조가 아니라 **규율로 돈 리뷰**라고 적어 둔다.
- 렌즈 3(git 히스토리): 574 가 유령을 문서 수정으로 닫으며 **기댄 성질**이 「점 든 토큰은
  이름이 아니다」였고, 이 자가 그것을 그대로 못박는다 — 되살리는 버그가 아니라 잠그는 쪽이다.
- 변이 누계 **9판 전부 잡힘** · 전수 **779 OK**.

### 반복 579 — e2e 스텝 · 유령을 심어 보고 되돌렸다 · 계획 99 DONE

- **시나리오 1 통과.** 명부에 `roster_ghost_e2e` 를 심자 전수가 **그 이름을 찍으며**
  빨개졌다(`Ran 779 · FAILED 1`). 같은 명부로 러너가 조립한 명령은 `rc=2` 까지만 말한다 —
  **앞 관문이 원인을, 뒷 관문이 증상을** 말한다. 원복 후 `project.md` diff 0 · 전수 OK.
- **시나리오 2(음성 대조) 통과.** 유령을 심은 채 판정만 끄니 **실물 1건이 rc=0**, 전수는
  **갈래만** 죽었다. 실물만 두면 판정을 끄는 변이가 초록으로 지나가고, 갈래만 두면 살아
  있는 문서가 낡아도 조용하다 — 이 저장소가 둘 다 두는 이유의 실측이다.
- 계획서를 `plan_history_074.md` 로 옮기고 `index.md` 를 완료·통과로 닫았다. 신호 **DONE**.

### 반복 580 — 병합 관문이 RED 를 냈다 · 자기 상태 문서를 무는 자가 일했다

- `merge-to-main.sh` 첫 판이 **전수 RED 로 푸시를 막았다** — `plan: null` 로 닫으면서
  `step` 을 1/1 로 둬 `StepSyncTest` 가 「계획 없이 스텝만 흘렀다」를 냈다. 초기값 `0/0`.
- 이어 `IterationSyncTest` 가 `metrics.md 579 ≠ status.md 580` 을 냈다. **둘 다 이 저장소가
  자기 기록을 못 믿게 되는 자리**고, 사람이 아니라 자가 잡았다.
- 로컬 `main` 에 남은 미푸시 병합 커밋은 `git branch -f main origin/main` 으로 되돌렸다
  (원격은 그대로 `b8f0bb1`). 되돌린 뒤 계획 브랜치에서 고쳐 다시 민다.

### 반복 581 — 계획 phase · 이 밤의 셋째 계획을 digest 6순위에서 뽑았다

- 빈손 대조 핀을 `064fe56` 으로 옮기고 1~4·8순위를 다시 쟀다: 실패 테스트 0 · 타입/린트
  도구 없음 · **코드** TODO 0(유일한 `TODO` 는 테스트 픽스처 HTML 안의 문자열) ·
  `docs/candidates.md` **파일 자체가 없다**(후보 `[6]` 이 적어 둔 「구조적으로 0건」의 실물) ·
  열린 이슈 0.
- 6순위에서 `[7]` **비ASCII 호스트가 robots 관문을 예외로 뚫는다**를 집었고 **오늘 다시
  쟀다** — `RobotsCache().allowed("http://한글.invalid/…")` 가 `UnicodeEncodeError` 로
  죽는다(퓨니코드는 정상). 등재된 처방을 그대로 옮기지 않고 실물에 대보는 것은 계획 99 가
  등식 하나를 틀리게 물려받을 뻔한 뒤로 고정 절차다.
- **도달 가능성까지 쟀다**: 씨앗도 링크도 `urls.normalize` 를 거쳐 퓨니코드가 되므로 오늘
  크롤 경로에서는 **도달 불가**다. 그래서 계획서에 「지금 죽는 버그」로 안 적고 **관문이
  입력 하나에 죽는다**로 적었다. 설계는 생략(트리거 0 — 공유 함수 `except` 한 줄).

### 반복 582 — 개발 스텝 1/1 · `except` 에 낱말 하나를 더해 관문을 닫았다

- **RED 를 먼저 봤다** — 비ASCII 둘이 `putheader` 의 latin-1 인코딩에서 죽었다(소켓 열기
  **전**이라 네트워크를 안 탄다). 퓨니코드 대조군은 그때도 통과했다.
- **고친 자리는 공유 함수 한 곳** — `_fetch_robots` 의 `except` 에 `UnicodeError`.
  호출부마다 가드를 두면 다음 호출부가 또 샌다(계약 4). 값은 599(차단)로, 바로 위
  `except ValueError` 가 적어 둔 판단과 같다.
- **관문에서 퓨니코드로 고쳐 통과시키지 않았다** — 그건 `urls` 의 일이고, 관문이 제 손으로
  주소를 바꾸면 조용히 더 후해진다. 크롤 윤리 축에서 틀린 방향이다.
- 건수 가드가 **세 반복째** 물었다(779 → 782). 전수 **782 OK** · 제품 `src/` **+8줄**(주석 7).

### 반복 583 — 테스트 스텝 · 살아남은 변이 하나가 갭을 정확히 가리켰다

- **변이 5판**: M1(`UnicodeError` 제거) 잡힘 · M2(599→404, 차단이 아니라 허용) 잡힘 —
  **값이 「차단」인지까지** 물린다 · **M3(`except Exception` 확대) 살아남음** ·
  M4(비ASCII base 빈 본문) 살아남았으나 **도달 불가 변이**라 등가로 기록 ·
  M5(비ASCII 통째 차단, 과차단) 잡힘.
- **M3 이 가리킨 갭을 메웠다** — 넓힌 `except` 는 갈래를 전부 통과시키면서 `TypeError`
  까지 599 로 접는다. 관문은 조용히 초록인데 크롤은 0건이 되는 모양이라, `urlopen` 이
  `TypeError` 를 던질 때 **그대로 올라오는지**를 재는 갈래를 세우고 M3 을 재측정해 잡았다.
- **경계 하나 더**: ASCII 호스트 + 한글 **경로**는 안 막는다(robots 왕복은 base 만 쓴다).
  M5 가 그 갈래를 죽이는 것으로 이빨을 확인했다.
- 건수 가드가 **네 반복째** 물었다(782 → 784). 전수 **784 OK**.
