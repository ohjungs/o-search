# 아카이브 — 계획 51 `hidden-passage` 의 리뷰 2·개발 3 (반복 295·296)

`history_current.md` 에서 회전으로 밀려난 원문이다. append 전용 · 수정·삭제 금지.

## 2026-09-03 21:30 | hidden-passage | 리뷰 2 | 시도0

- 한 일: 패스 A(백지·diff 만) 뒤 패스 B(계획·설계·README·과거 코멘트). 지적 **2건**
  (승인 필요 1·자동감 1) · 버린 후보 **3** · 제품 `src/` **0줄** · **패스 B 첫 값**([R51-4]).
- **[R51-3] `p` 가지의 술어가 «열린 집합» 이라 숨김이 풀린다 — medium · 승인 필요.**
  개발 2 가 재사용한 `tag not in _INLINE_TAGS and tag not in _NON_BLOCK_TAGS` 는
  **여집합**이라 모르는 태그가 전부 `<p>` 를 닫는다 — 브라우저는 **닫힌 목록**만 닫고
  커스텀 요소는 phrasing 이라 안 닫는다. `<p hidden>숨은<my-widget>속</my-widget>또 숨은
  김치찌개` 가 스텝 1 뒤 `[]` → HEAD 는 숨은 텍스트를 낸다(`amp-img`·`slot`·`datalist`·
  `nobr` 도). **표준 태그 127개 전수**로 과잉 **19**·미달 **0**·커스텀 요소는 **전부 과잉
  (무한 집합)**. **실제 DB 로 3/4 유출**(대조군 `<div hidden>` 0/1)이라 완료 기준 1 「0/5」
  와 README 계약 문장이 거짓이다. 설계 3절 천장은 «덜 낸다» 방향이라 이 «더 낸다» 를 안
  덮는다 — [R51-1] 수리의 거울상.
- **[R51-4] `indexer.py:41~46` 의 성능 계수가 낡았다 — low.** 「최악 0.352ms/1000자 → 10건
  123ms(예산 25%)」가 HEAD 에선 **0.439 → 153.5ms · 배정치의 125% · 예산의 31%** 다.
- **성능 회귀는 «값을 한다» 로 판정했다.** 기사 11.31 → **14.40ms(+27.3%)** 는 재현되지만
  **실물 p95 는 1.51 → 1.54ms**(예산의 0.006%)이고 +29% 는 **캡 최악에서만** 난다 — 산 것은
  숨은 텍스트 **5/5 → 0/5**, 잃은 것 0, +29% 의 **87%가 스텝 1**(이 스텝은 +2.9%p).
- 결과: 단위 **588건 OK** rc 0(13.445 · 마감 13.495초 · 맨몸·단독) · `passage_eval` 통과 · 러너 3회 전부
  맨몸·단독 · 출력 조작 0 · PR 0 · RED 0. **다음: 개발 3.** 교훈은 리뷰 1·[T2-1]·[R51-3]
  이 **한 뿌리**라 `digest ## 반복 실패` 에 **한 항목(3회)** 으로 합쳤다.

## 2026-09-03 22:20 | hidden-passage | 개발 3 | 시도0

- 한 일: **리뷰 2 의 반려 2건을 닫았다.** [R51-3] 은 «열린 집합» 술어를 **지우고**
  `_IMPLIED_END["p"]` 에 파싱 알고리즘의 "close a p element" 목록(49개)을 그대로 옮겨
  닫힌 집합으로 만들었다 — `_implied_end` 스태틱메서드를 **통째로 삭제**해
  `_INLINE_TAGS`·`_NON_BLOCK_TAGS` 가 숨김 판정에 **입력으로 아예 안 들어간다**(제품
  순증 0줄에 가깝고 파일 1개). 리뷰가 준 ②(`if "-" in tag` 가드)를 안 쓴 이유는 그것이
  커스텀 요소만 깁고 **표준 태그 과잉 29 를 그대로 남기기** 때문이다.
- **TDD — RED 를 먼저 봤다**: `Ran 588 tests` → `FAILED (failures=6)`
  (extract 의 subTest 5개 + `test_hidden_text_never_becomes_the_passage`, 예:
  `AssertionError: '김치찌개 김치찌개' != '김치찌개는 배추로 만든다.'`), 구현 뒤
  `Ran 588 tests in 13.418s / OK` rc 0. 단언 자리는 **가장 깊은 모양**이다 —
  `indexer.passages()` 를 임시 DB 에서 부르는 행을 새로 넣었다.
- **전·후를 둘 다 제품 코드에서 쟀다.** 표준 태그 **135개 전수**(리뷰의 127 목록 + 폐기
  요소 8): 과잉 **29 → 0** · 미달 **0 → 0** · 커스텀 요소(`my-widget`·`amp-img`·
  `ion-icon`) **전부 과잉 → 없음**. 임시 DB 유출 **5/6 → 0/6**(대조군 `<div hidden>`
  전·후 0). `search` 는 리뷰가 과잉으로 셌지만 **현행 명세는 `<p>` 를 닫는 쪽**이라
  넣었다 — 빼면 브라우저가 보여 주는 텍스트를 지우는 «덜 낸다» 쪽 오류다(더 나쁜 방향).
- **[R51-4] 를 다시 쟀다** — 캡 35,000자 · 30회 중앙값 · 세 모양: 1,000자당 최악
  기점 0.323 · HEAD 0.409 · 이번 수정 **0.413**. 계수를 **0.44**(리뷰 2 실측 0.439 를
  **낮은 쪽으로 안 내렸다**)로, 최악을 **154ms · 예산의 31%** 로 적었다. **리뷰는 이것을
  «서술이라 스위트가 안 잡는다» 고 했는데 실제로는 `tests/test_indexer.py` 가
  `0.352`·`500*0.25` 를 박아 두고 있었다** — 서술만 고쳤으면 거짓 초록이 남는다. 배정치를
  **25% → 1/3** 로 다시 그었고(캡·`PASSAGE_LIMIT` 무변) 기록 자리는 `indexer.py:41~51`
  주석 · 같은 단언의 주석 · `docs/project.md`. 도로 25% 로 조이려면 캡을 28,000 으로
  내려야 하고 그것은 «긴 문서 뒷부분 근거» 를 자르는 별개 판단이라 범위 밖으로 적었다.
- **이미 초록이던 축을 옛 `extract.py` 를 임시 모듈로 띄워 나란히 재확인했다** — 숨은
  텍스트 **0/6** · 본문 **6/6** · 코퍼스 64문서 블록 **253 차이 0**(통짜 64블록도 0) ·
  불변식 깨진 문서 0 · `tests/` HTML 리터럴 **342개 중 달라진 6개는 전부 이번 fixture** ·
  음성 6종 6/6 · [R51-1] 형제 누출 4종 · 변이 ⑤/T2-1/T2-2 자리 그대로 죽는다.
- **변이 10개 중 8개 사망**: ①`p` 항목 통째 삭제(5) · ②**열린 집합 복원(6 — extract 5 +
  `passages` 1)** · ③div(1) · ④li(1) · ⑤`while`→`if`(3) · ⑥tr(1) · ⑦br 추가(1) ·
  ⑧slot 추가(1). **살아남음 ⑨caption 삭제 · ⑩search 삭제** — 49개 집합에서 원소 하나를
  빼는 변이라 전부 죽이려면 단언이 구현 리터럴을 그대로 되뇌게 된다(동어반복). 보상
  통제는 **다시 돌릴 수 있는 135태그 전수 대조**이고 8점 미만으로 남긴다.
- 결과: 단위 **588건 OK** rc 0(13.479초 · 맨몸·단독) · `passage_eval` 통과(블록 253 ·
  정확도 100.0% · p95 **1.51ms**) · 제품 파일 2개(`extract.py`·`indexer.py`) · 새 의존성 0 ·
  `e2e/`·`docs/specs/`·`data/crawl.db` 무변 · `.mutation-lock` 되돌림 True(`git diff` 확인) ·
  PR 무접촉. `README.md:66` 계약 문장은 **다시 참**이고 단위 588 · e2e 20 이라 README 무변.
- 다음: **테스트 3** — 스위트 밖 축(코퍼스·`quality_eval`)을 다시 재고, 살아남은 변이
  ⑨⑩ 이 가리키는 «집합 원소 하나» 자리에 갭 탐색을 건다.

