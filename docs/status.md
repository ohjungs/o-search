---
signal: GREEN
phase: e2e
step: 1/1
attempt: 0
iteration: 354
updated: 2026-09-06
ctx: 60
night_iterations: 171
night_red: 2
night_retries: 1
plan: index-step-sync 계획 60 (리뷰 1/1 완료 — 자동 수정 1 · 승인 필요 0 · 다음은 e2e phase)
---

## 현재 상태

**리뷰 phase 를 돌았다 — 반려 없다.** 백지 패스(패스 A)를 별도 세션에 넘겼다
(`rules/review.md` 0절 — 준 것은 `tests/test_docs.py` diff 와 그 파일뿐, 계획·설계·상태
문서는 안 줬다). 받은 4건을 코드에서 다시 확인해 **2건을 살리고 2건을 버렸다.**
자동 수정 1건을 적용했고 승인 필요 항목은 **0건**이다. 전수 **614건 OK · rc 0**
(맨몸 · 회전 후 재실행 포함 3회). 제품 `src/` 는 **0줄**, 새 파일은 회전 아카이브 하나다.

## 리뷰 결과 — 4건 중 2건 채택

**[자동 수정] `step_gap` 의 「행이 없다」 메시지가 두 원인을 뭉갠다** (신뢰도 85).
`STEP_ROW` 는 스텝을 **넷째 칸**으로 못박는데(`^\| plan_%s \| [^|]* \| [^|]* \| (N/M) \|`),
`index.md` 의 열 모양이 바뀌면 행이 멀쩡히 있어도 매치가 없어지고 그때 나가던 말은
"계획 커밋과 등재 커밋이 갈렸다" 였다 — 읽는 사람을 엉뚱한 수리로 보낸다.
메시지를 「등재가 빠졌거나 표의 열 모양이 바뀌었다(스텝을 넷째 칸으로 가정한다)」로
넓혔다. **갈래를 쪼개지는 않았다** — 원인을 가르는 둘째 정규식은 자기 단언을 또 요구하고,
오늘 얻는 것은 메시지 한 줄과 같다.

**[범위 밖에 등재] 닫힌 계획의 행은 두 번 다시 안 본다** (신뢰도 85).
D 가 대조하는 것은 `status.plan` 이 지금 가리키는 **한 행**뿐이다(실물 37행 중 1행).
4회 재발한 결함은 «계획이 살아 있는 동안» 뒤처지는 것이라 다음 커밋에서 잡히지만,
계획을 닫는 **마지막 한 번의 쓰기**는 아무도 안 붙든다. 설계서
「범위 밖 (digest 후보로)」에 여는 조건과 함께 적었다 — digest 는 200/200 이라
완료 항목을 또 지우는 값이 아니고, 이 자리가 그 용도로 있는 절이다.

**버린 것 둘.** ① 「계획 행이 `index.md` 밖으로 회전해 나가면 오진한다」 — **거짓이다.**
회전하는 것은 `plan_<slug>.md` 문서지 `index.md` 의 행이 아니고, 계획 1
(`plan_crawler-core`)의 행이 오늘도 표 첫 줄에 있다(`rules/docs.md` 4절이 「한 줄 추가」만
말한다). ② 「`"null"` 이 매직 리터럴이라 하네스가 표기를 바꾸면 상시 적색」 —
오늘 템플릿(`~/.claude/skills/loop-harness/templates/status.md`)이 `step: 0/0` 을 그대로
쓰는 것을 확인했고, 코드 주석이 이미 출처를 적고 있다. 신뢰도 80 미만으로 버린다.

## 착수 전 세 물음, 답

status.md 가 남긴 셋을 코드로 확인했다. ① **검사 순서는 안 바뀌었다** — 옛 코드도
`assertIsNotNone(s)` 를 `p` 보다 먼저 했고(`49bd4e2`), 새 코드도 `step:` 을 먼저 문다.
보이는 실패 순서가 같다. ② **합성 `INDEX` 는 실물과 안 어긋난다** — 실물 계획 행 37/37 이
`STEP_ROW` 에 물리고, 스텝 칸이 `N/M` 이 아닌 행은 0개다. 실물은 6열에 비고 안쪽에
`|` 가 섞이지만 정규식이 넷째 칸에서 멈추므로 무해하다(다만 그 «넷째 칸» 가정이 위
자동 수정의 대상이다). ③ **계획 52 는 다른 곳에 남아 있다** — `plan_history_038.md`·
`design_history_038.md`·`docs/e2e/focus-ring-combinator/result.md`·`index.md` 52번 행이
전부 실재하고, `digest.md` 안에도 취소선 후보 `[5]` 로 이름이 남아 있다.

## 회전 — append 전에 했다

`history_current.md` 가 293/300 이라 오래된 것부터 **`history_062.md`** 로 밀었다:
계획 59 `endtag-cut-cover` 구간(반복 345~349) **131줄**, 원본 그대로다.
`history_current.md` 는 162줄이 됐고 이번 반복 기록을 그 위에 얹었다.
`digest.md` **아카이브 명부**에 `history_062.md` 를 등재했다(`ArchiveIndexTest` 통과).
**digest 는 200줄 그대로다** — 계획 59 의 압축 줄은 반복 350 이 마감하면서 이미
`## 완료` 에 넣어 뒀으므로 새로 더할 것이 없었고, 명부는 한 줄 안에서 늘어난다.

## 다음

**e2e phase.** 제품 `src/` 0줄 · 고친 것은 `tests/test_docs.py` 의 실패 **메시지 한 줄**과
문서뿐이라 `rules/e2e.md` 3절 면제 판정이 유력하다 — 프로세스 밖에서 달라지는 동작이
0이다. 볼 것은 ① 그 면제가 실제로 서는가(단위 614건과 e2e 21종 전수 rc 0 으로 재고,
새 e2e 파일은 0개 예정) ② 계획 60 마감 준비 — 완료 기준 대조와 `index.md` 계획 60 행을
`진행 1/1 → 완료 1/1` 로 닫는 것.

## 한도

- 병합은 사람 몫이다 — 계획 57·58·59·60 의 커밋이 `loop/passage-cost-band` 에 쌓여 있고
  `origin/main` 무접촉 · PR 0(만들지도 조회하지도 않았다). 착수 시 원격 브랜치는
  `b8e8371` 로 HEAD 와 같았다(`git ls-remote origin loop/passage-cost-band`).
- **러너 규율 위반 0회 — 누적 38 유지.** 전수 세 번 다 맨몸으로 돌리고 판정 줄과 `rc` 를
  눈으로 봤다(`Ran 614 tests · OK · rc 0`). 파이프는 `grep`·`sed` 쪽에만 썼다.
- 문서 한도 — `history_current.md` **회전 후 162줄** · `digest.md` 200/200 유지.
  아카이브는 `history_062.md` 까지다.
- 변이는 계속 **메모리에서** 걸고(소스를 문자열로 치환해 로드된 모듈에 다시 exec)
  `PYTHONPYCACHEPREFIX=$(mktemp -d)` 를 함께 준다 — 저장소 파일과 `data/crawl.db` 무접촉.
- 만진 파일은 `tests/test_docs.py`(메시지 1줄) · `docs/` 뿐이다. `src/`·`e2e/`·
  `README.md`(건수 614 이미 맞다)·`docs/specs/`·`data/crawl.db` 무접촉 · 재색인 0.
