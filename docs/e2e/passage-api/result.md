# e2e 결과 — 계획 48 `passage-api` (2026-09-02 · 반복 274)

브랜치 `loop/passage-api` · 기점 `6e99031` · **새로 만든 e2e 파일 0개**.
기존 하나에 더했다: `e2e/search_api_e2e.py`(+38 −2). 제품 `src/` **0줄**.

러너 호출 **29회 · 명령 잇기 0회 · 출력 조작 1회** — 그 1회를 3절 끝에 적었다.
`digest ## 반복 실패` 의 최다 재발 항목을 **28번째로 밟았고**, 이번엔 밟은 자리가
「전체 스위트의 두 번째 실행」이라 판정 줄 대신 크롤 로그 세 줄만 화면에 남았다.
**판정은 그 호출 위에서 내리지 않았다** — 맨몸으로 다시 돌려 `Ran 576 tests OK` 를 읽었다.

## 1. 선행 관문 셋 (`rules/e2e.md` 1절)

| 관문 | 명령 | 결과 |
|---|---|---|
| 전체 검증 | `PYTHONPATH=src python3 -m unittest discover tests` | **576건 OK**(맨몸 2회 — 13.433 · **13.418초**) · 실패 0 |
| 린트·타입체크 | `docs/project.md` 에 없다 | 해당 없음(있다고 쓰지 않는다) |
| CI | `.github` **부재** | 해당 없음 (1절 2항) |
| 품질 기준 | 아래 4절 | 전 축 기준선 유지 |

## 2. 이번 e2e 가 답한 것 — **`/passages` 의 계약을 프로세스 밖에서 아무도 안 쟀다**

계획 48 은 세 스텝으로 함수(`indexer.passages`) · 배선(`serve.do_GET`) · 자
(`e2e/passage_eval.py`)를 세웠다. 그런데 `passage_eval` 이 때리는 것은 **200 한 갈래뿐**이다
— 정확도(사양 기능 8)와 p95(성능 5)를 재려고 만든 자라 계약의 나머지(400·404·500·503·
`version`)를 한 번도 안 본다. 단위 576건은 전부 `make_server()` 를 **스레드로** 부르고
`tempfile` **절대 경로**를 쓴다. 즉 계획 48 이 만든 엔드포인트는 **진짜 CLI 프로세스가
README 형태의 상대 경로(`data/crawl.db`)로 뜬 자리에서 한 번도 안 밟혔다.**

계획 47 이 `/search` 에 대해 정확히 이 자리를 열어 뒀으므로(같은 파일 ①~⑦),
**갈래를 하나 더 띄우지 않고 그 서버에 `/passages` 를 얹었다**(ponytail 2번 — 이미 있는 것).

| # | 시나리오 | 어디서 | 실측 |
|---|---|---|---|
| 1 | `GET /passages?q=김치` | ⑧ | **200** · `version` 1 · 문단마다 `position`(정수 ≥0) · `title` · `url` · 질의어를 담은 `text` |
| 2 | `q` 없음 | ⑧ | **400** · 트레이스백 0 |
| 3 | `page=2` | ⑧ | **400** — 페이지를 안 나눈다는 것이 조용한 무시가 아니라 값이다(설계 갈림길 2) |
| 4 | POST | ⑧ | **501** |
| 5 | 색인을 치우면 | ⑧ | **503**(`/search` 와 같은 값·같은 순간) · 되돌리면 200 |
| 6 | **손상된 DB** | ⑨ | `/search`·`/passages` **둘 다 500** · 본문에 경로·트레이스백 0 · `version` 1 |
| 7 | **대조군** | 3절 | 변이 둘 전부 **RED** |

**6 이 이 반복의 값이다.** 계획 47 의 result.md 5절이 *"500 은 한 번도 안 났다"* 로
닫혀 있었다 — 재시도해도 안 낫는 상태(500)와 색인을 다시 돌리면 낫는 상태(503)가
갈리는 자리인데, 저장소 밖에서 그 둘을 갈라 본 자리가 **0개**였다. 이제 진짜 프로세스가
진짜 파일에서 `DatabaseError('file is not a database')` 를 만나 500 을 낸다(서버 로그가
`/search 실패` · `/passages 실패` 로 **어느 문이 열렸는지**까지 적는 것도 같이 확인했다).

## 3. 대조군 — 제자리 변이 둘, 전부 죽었다 (`.mutation-lock`)

| 변이 | 제품에 심은 것 | e2e 판정 | 단위 판정 |
|---|---|---|---|
| **M1** | `serve.py` 의 `_send(500, …)` → `_send(503, …)` | rc 1 — `손상 DB 인데 /search?q=김치 → 503 (기대 500)` | **사망 8건** (`TestBrokenDbStays500` 3 · `TestPassagesBrokenDb` 1 · `TestProgrammingErrorStays500` 2 · 스키마 버전 2) |
| **M2** | `serve.py` 의 `"position": pos` → `str(pos)` | rc 1 — `position` 이 `'0'` 이다 | **사망 2건** (`TestPassagesEndpoint`) |

**둘 다 단위가 먼저 죽인다 — 이 e2e 는 눈먼 자리를 메운 것이 아니라 형태를 바꿔 잰다.**
그렇게 적는 것이 정직하다. 다만 **한 자는 M2 에 완전히 눈이 멀었다**: 같은 변이 아래서
`e2e/passage_eval.py` 가 **rc 0 · 정확도 100.0% · p95 1.43ms** 로 통과한다. 정확도를 재는
자는 `text` 만 읽고 계약의 **필드 타입**은 안 본다 — 반복 267 이 세운 눈금(*"e2e 숫자가
안 움직이는 것은 «안전» 이 아니라 «그 축을 안 쟀다» 일 수 있다"*)이 여기서 한 번 더 맞았다.
**품질을 재는 자와 계약을 재는 자는 다른 자다.**

되돌림은 초록이 아니라 **`git diff -- src/` 가 0줄인 것**으로 확인했고, 되돌린 뒤 같은
e2e 를 다시 돌려 rc 0 을 봤다. `.mutation-lock` 은 심기 전에 걸고 되돌린 뒤 지웠다.
**커밋된 변이 0.**

**출력 조작 1회(28번째 재발).** 전체 스위트의 두 번째 실행에 `2>&1 | tail -3` 을 붙였다.
방아쇠는 27번과 같은 «이미 초록일 것 같은 실행» 이고, 결과도 같았다 — 크롤 로그가
`tail -3` 을 채워 `Ran …/OK` 가 화면 밖으로 밀렸다. 즉시 맨몸으로 다시 돌렸고
그 값(13.418초)이 위 표의 숫자다. **문장으로는 스물여덟 번 못 막았다** — `digest` 의
`[9]`(`unittest -b`)가 여전히 이 자리의 유일한 뿌리 처방이다.

## 4. 회귀 — `e2e/*.py` **20종 전수 rc 0** · 품질·성능 축 기준선 유지

`crawl` 15p/최소 1.005s · `crawl_delay` 2.01s/1.00s · `crawl_politeness` 2.01s/1.00s/1.00s ·
`deadline` 8p/2.1s · `design_check` **4축 통과** · `domain_key` 4회 2.01s·robots 1회 ·
`indexer` · `indexer_interrupt` rc 130/2000행 · `interrupt` rc 130 10.0s · `noindex` 4→2문서 ·
`non_ascii` 3행 · `pagination_ui` 11문서 · **`passage_eval`** · `perf_crawl` · `perf_search` ·
`quality_eval` · `retry_interval` 5.01s · **`search_api`**(⑧⑨ 추가) · `tokenizer` 6문서 ·
`url_normalize` 표기 10 → 문서 6.

- 근거 문단: 정확도 **100.0% (398/398)** ≥ 90% · 채택률 **99.5%**(398/400) ·
  p50 1.29ms · p95 **1.40ms** ≤ 500ms (예산의 **0.3%**) — 기준선 그대로
- 검색 품질: ko **20/20 (100%)** · en **19/20 (95%)** · 1위 39 · 미검출 1(`loaf`) ·
  매치 평균 **14.0** 최소 11 최대 28 — 기준선 그대로
- 성능: `perf_search` p95 **8.90ms** < 예산 300ms (**3.0%**) · `search_api_e2e` 안의
  500문서 측정 p95 **2.09~2.14ms**
- 처리량: [열림] **10.22/s** · [차단] **10.22/s** · [예외] 최소 간격 1.002s ·
  문서집합 sha1 `541d455a…c078654b` — 계획 47 과 같은 값
- 디자인 4축: 외부 서브리소스 0 · JS **0 B** · 대비 16짝 전부 통과 · 360px 초과 0건

`data/crawl.db` sha256 `85c96744…5bda18` **무변경**(e2e 는 전부 임시 디렉터리에서 돈다).
제품 `src/` **0줄** · 새 파일 0(문서 제외) · 의존성 0(stdlib 만) · 스키마·마이그레이션 없음 ·
`docs/specs/` 무변경 · `TODO`/`FIXME`/`HACK` **0건** · `.mutation-lock` **0**.
**README 카운트 무변경** — e2e 파일을 안 늘렸으므로 «단위 576건 · e2e 20종» 이 그대로 맞다
(`tests/test_readme.py` 가 576건 안에서 붙든다).

## 5. 웹 UI 8단계 (`rules/e2e.md` 4절) — 브라우저 없는 API 라 셋만 해당

| 단계 | 판정 |
|---|---|
| 3 폼(잘못된 값) | `/passages` 의 `q` 없음 · `page=2` → **400 JSON**, 트레이스백 0 |
| 5 상태(빈·에러·넘침) | 색인 상실 **503** · **손상 DB 500** · 빈 `DB_PATH` 503 · 없는 경로 404 · POST 501 |
| 6 콘솔(5xx) | 정상 경로에서 **5xx 0건**. 500 은 손상 DB 갈래와 변이 M1 아래서만 났다 |
| 1·2·4·7·8 | 해당 없음 — 브라우저를 안 띄운다. 화면 축은 `tokenizer_e2e`·`pagination_ui_e2e`·`design_check` 가 판다. `/passages` 는 **화면에 안 붙는다**(계획 5절이 범위 밖으로 못박은 것) |

이슈 분류(5절) **0건**.

## 6. 도구 산출물

Playwright 같은 도구를 안 쓴다 — 전부 stdlib 스크립트라 **증거는 표준출력 한 줄**이고
`docs/project.md ## 명령` 의 각 항목이 그 줄의 정본이다. 남는 파일이 없어
`.gitignore` 에 더할 것도 없다(`rules/e2e.md` 6절 — 도구가 안 남기면 적을 경로도 없다).

## 7. 안 만든 것과 그 사유

- **새 `e2e/*.py` 0개.** `/passages` 를 위한 서버를 따로 띄우면 프로세스가 하나 늘고
  재는 것은 같다 — 계획 47 이 열어 둔 서버가 이미 README 형태로 떠 있고, 계약은 그
  서버에 경로 하나를 더 때리는 것으로 전부 재진다(ponytail 2번).
- **정확도·지연을 여기서 다시 안 쟀다.** `passage_eval.py` 가 동결 코퍼스 64문서·질의 40개로
  판정한다. 같은 숫자를 15문서 코퍼스에서 또 내면 **기준선이 둘**이 되고 비교가 끊긴다.
- **`position` 의 값을 e2e 가 단언하지 않는다** — 이 코퍼스는 문서마다 `<p>` 가 하나라
  정답이 늘 0 이다. 자가 못 가르는 축이라 «정수이고 음수가 아니다» 까지만 잰다.
  값의 정확성은 단위 `TestPassagesEndpoint` 와 19행 골드 표가 판다(반복 271).
- **`MAX_PASSAGE_HTML`(35,000자) 캡을 e2e 로 안 밟았다.** 밟으려면 2MB 문서를 만들어야
  하고, 그러면 이 스크립트의 총 시간이 계약 검사가 아니라 파일 생성에 쓰인다.
  캡의 천장(«잘린 뒤의 문단은 근거로 안 나온다»)은 단위가 결정적으로 잰다.

## 8. 남긴 것

- **PR 은 안 열었다 · `main` 병합도 안 했다** — 사람 몫이다. `bc40ea9`~HEAD 가
  `origin/main` 에 없는 것은 계획 43부터 그대로다. **웹 UI 의 *Update branch* 는 누르지 않는다.**
- `digest.md ## 다음 계획 후보` 로 넘긴 것: `[R7-1]` 이름표 소비자 0곳(리뷰 7) ·
  `[6]` 보일러플레이트 빈도의 실물 데이터 · `[9]` `unittest -b`(이번 반복이 28번째 표본을 더했다).
- 설계서 **갈림길 6 은 열린 채로 둔다** — 닫는 조건이 실물 데이터이고 저장소가 못 잰다.
- 사람이 정할 것 넷(`--line` 대비 · `--focus` 짝 · 회전 규약이 저장소 밖 룰과 갈린 것 ·
  한도를 훅으로 옮기는 것)은 이번 반복이 안 건드렸다.

**판정: 계획 48 의 e2e 통과 — 사양이 요구한 근거 문단이 진짜 CLI 프로세스 위에서
200·400·404·500·503·`version` 을 전부 맞게 낸다. 계획 48 DONE.**
