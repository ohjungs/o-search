# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**회전 명부는 `digest.md` 의 `## 완료` 절 «아카이브 명부» 줄이 정본이다.**
여기 있던 스물한 회전의 서술(233줄)은 그 줄과 내용이 겹쳤고, 검사가 강제하는 명부도
그쪽 하나뿐이라(`tests/test_docs.py` 의 `ArchiveIndexTest`) **개발 9(반복 269)가 이
자리에서 지웠다** — 회전으로는 300줄 상한을 못 맞추던 세 반복(309 → 372 → 418줄)의
원인이 이 명부였다. **지운 것은 머리말이지 반복 기록이 아니다** — 항목은 여전히
append 전용이고 수정·삭제 금지다. 각 회전의 사유는 `digest.md` 의 같은 줄에, 원문은
`history_<NNN>.md` 에 그대로 있다.

## 반복 393 — 계획 68 `spec-citation-anchor` (계획 phase · 스텝 0/1 · 시도 0)

- **연 것**: `digest ## 다음 계획 후보 (테스트 phase 갭, 8점 미만)` 의 `[6]`
  「`concept.md:<N>` 인용 18건 중 11건은 «주소가 빈 줄이 아니다» 말고 아무도 안 잰다」.
  계획 67 이 어제 같은 파일의 같은 축을 닫아 여는 조건이 왔다.
- **착수 탐침 3판 + 무변이 대조군**(저장소 밖 `mktemp -d` 사본 · 워킹트리 무변경 ·
  매 판 `PYTHONPYCACHEPREFIX=$(mktemp -d)` · 심은 뒤 `diff -u`): M0 `Ran 627` `OK` rc 0
  (오탐 0) · **M1** `e2e/perf_crawl.py` 1행 `:44`→`:45` **생존**(`OK`) — 구멍이 오늘도
  살아 있다 · **M2 양성 대조** `e2e/design_check.py` 33행 `:51`→`:50` `FAILED (failures=1)`
  로 배선 증명 · **M3 처방** 같은 밀림 + 문구 조각 동행 → `FAILED (failures=1)`.
  **처방이 실제로 재는 것을 등재 전에 확인했다** — 계획 65·66·67 이 「서술은 옳고 처방은
  추정」으로 세 번 연속 걸렸던 자리를 이번엔 먼저 샀다.
- **정적으로 센 것**: 앵커 있음 7(문구 2 · 값 5) · 없음 11. 문자열 안 인용 3자리는
  이스케이프가 성립하지 않고 작은따옴표 전환이 성립한다(정규식 실측).
- **설계 생략** — 트리거는 「파일 3개 이상」 하나뿐이고 갈릴 자리는 탐침이 골랐다.
- **범위**: `src/`·`tests/`·`e2e/`·`README.md` 0줄 · `docs/specs/` 무변 ·
  `data/crawl.db` 무변 · 재색인 0 · 스키마 0 · 새 의존성 0 · PR #7 무접촉(`gh` 0회) ·
  전수 4회 · `--no-verify`·`--force`·`--amend`·`rebase` 0회 · `main` 직접 커밋 0회.
- 산출물 `docs/plan_spec-citation-anchor.md` · `index.md` 68번 행.
- 다음: **개발 phase 1/1** — 앵커 단언을 심어 RED 11건을 기록한 뒤 열한 자리를 고친다.

## 반복 394 — 계획 68 `spec-citation-anchor` (개발 phase · 스텝 1/1 · 시도 0)

- 한 일: `tests/test_docs.py` `SpecCitationTest` 에 `test_every_citation_carries_an_anchor`
  를 심고 `_anchors(line, pos)` 헬퍼를 뽑아 **대조 축과 의무 축이 같은 추출기를 보게** 했다
  (갈라 놓으면 한쪽 추출기가 죽는 날 다른 쪽이 조용히 통과한다).
  `test_spec_quotes_match_cited_lines` 는 헬퍼를 쓰도록만 바꿨고 단언은 무변이다.
- **RED 는 11 이 아니라 12 였다.** 계획서 2절의 「앵커 있음 7 · 없음 11」이 이름으로
  나열한 자리는 **열둘**이고 심은 단언이 문 자리도 그 열둘과 글자 그대로 같았다.
  「7·11」은 `e2e/quality_eval.py:34`(`TOP_N` — 문구 `"상위 10건"` 과 값 `10` 을 **둘 다**
  가진 줄)를 문구 축·값 축에서 한 번씩 센 오산이다. 실제는 **6 + 12 = 18**.
  **계획서의 숫자만 틀렸고 목록은 맞았다** — 완료 기준 1 은 숫자를 정정해 충족으로 닫는다.
- 정정 12자리. 조각은 「그 줄에만 있는 최소 길이」: `:50-54`→`"화면은 둘뿐"` ·
  `:51`→`"JS 번들 50KB"` · `:50`→`"검색 홈(검색창 하나)"`·`"결과 페이지(검색창 + 결과 리스트)"` ·
  `:53`→`"키보드만으로 검색·결과 이동"` · `:59`→`"크롤 윤리(robots·간격)"` ·
  `:44`→`"초당 5문서 이상 지속"` · `:22-23`→`"한국어 질의 20개·영어 질의 20개"`.
  문자열 안 3자리(`test_serve.py:918`·`design_check.py:444`·`perf_crawl.py:275`)는
  계획 2절대로 **바깥 따옴표를 작은따옴표로** 돌렸다 — 착수 탐침이 고른 그 모양이 그대로 섰다.
- **줄 수를 보존했다.** 네 자리를 두 줄로 쪼갰다가 전부 되돌렸다 — 파일이 한 줄이라도
  밀리면 `docs/`·계획서가 대는 `e2e/design_check.py:33` 같은 **소스 주소가 통째로 썩는다.**
  79자 상한은 이 저장소에 없다(기존 최장 101자). 여섯 파일 전부 삽입 = 삭제다.
- 변이 **3판 + 무변이 대조군**(저장소 밖 `rsync` 사본 · `tests.test_docs` 31건만 ·
  매 판 `PYTHONPYCACHEPREFIX=$(mktemp -d)`): **V0** `OK` 오탐 0(기준 5) ·
  **V1** `perf_crawl.py` 1행 `:44`→`:45` **`failures=1`** — 착수 탐침 M1 이 `OK` 로
  생존하던 밀림이 이제 죽는다(기준 3 · 4 의 docstring 표본) ·
  **V2** `test_serve.py` 주석 `:53`→`:54` **와** 문자열 안 `:51`→`:52` 를 한 판에
  **`failures=2`** — 표본 셋이 자리마다 1건씩 죽었다(기준 4) ·
  **V3** `PHRASE` 를 작은따옴표 꼴로 **`failures=14`** = 새 단언 13 + `MIN_CHECKS` 1(기준 6).
- **`MIN_CHECKS` 는 6 그대로 둔다.** 대조 건수는 5→**19**(문구 14·값 5)로 뛰지만 하한을
  올리면 손으로 관리할 숫자가 는다. V3 이 판정을 실측으로 샀다 — 같은 편집에서
  **새 단언이 13번, 하한이 1번** 운다. 「상위 호환」이 «먼저 그리고 더 크게 운다» 로 섰다.
  0건 수집 초록은 `MIN_HITS 14` 가 앞에서 막는다(새 단언은 수집된 인용마다 도는 자다).
- 안 산 것: 조각이 **자리마다** 이웃 줄에서 다시 찾아지지 않는지는 표본 셋으로만 샀다.
  열둘 전수 `±1` 밀림은 변이 예산(3판) 밖이라 다음 phase 의 입력으로 남긴다 —
  계획 7절이 적은 유일한 진짜 위험이 그것이다.
- 결과: 전수 **`Ran 628 tests` · `OK` · rc 0**(2회) · 완료 기준 **7/7** ·
  `src/` **0줄** · `docs/specs/` 무변 · `data/crawl.db` 무변 · 재색인 0 · 스키마 0 ·
  새 의존성 0 · PR #7 무접촉(`gh` 0회) · `README.md:104` 627→628.
- 다음: **테스트 phase.** 앵커가 **있기만 하면** 통과하는 것이 남은 구멍이다.

## 반복 395 — 계획 68 `spec-citation-anchor` (테스트 phase · 스텝 1/1 · 시도 0)

- **계획 7절의 유일한 진짜 위험을 자리마다 실측했다 — 변이 0판.** 개발 phase 가 표본
  셋으로만 산 「어느 조각도 이웃 줄에서 다시 찾아지지 않는다」(완료 기준 4)를 **정적으로**
  마저 쟀다. 밀림 생존은 파일을 고쳐 봐야 아는 것이 아니라 `_anchors`·`_has_number` 와
  세 단언의 판정식을 그대로 돌려 **계산되는 값**이다 — 저장소 밖 사본도 `sed` 도 안 썼다.
- **인용 18자리 × 밀림 넷(`±1`·`±2`) = 72조합 중 68이 죽고 4가 살았다.** 살아남은 넷은
  `tests/test_serve.py:914 -2` · `e2e/design_check.py:2 -2` · `e2e/design_check.py:444 -2` ·
  `e2e/quality_eval.py:3 -1` 이고, **넷이 곧 이 저장소의 범위 인용 전부**다
  (`concept.md:50-54` 셋 · `22-23` 하나). **단일행 인용 열넷은 0/56 — 하나도 안 산다.**
- **원인은 조각 길이가 아니라 범위다.** `50-54` 를 `-2` 밀면 `48-52` 라 문구가 있는 50행이
  **아직 범위 안**이고, `22-23` 을 `-1` 밀면 `21-22` 라 22행이 그대로 남는다. `cited` 가
  범위 전체를 이어 붙이므로 뒤로 미는 밀림을 범위가 삼킨다. 계획 7절이 걱정한
  「`"50KB"` 같은 짧은 조각이 이웃 줄에도 있다」는 **72조합 어디서도 안 일어났다** —
  걱정한 축은 무죄고 안 본 축이 열려 있었다.
- **고치지 않았다.** 처방(문구 축을 범위 전체가 아니라 시작행에서 찾기 — 오늘 넷의 앵커는
  모두 범위 첫 줄에 있다)은 테스트 phase 의 몫이 아니라 다음 계획의 입력이다.
  `digest.md` 「다음 계획 후보」에 `[5]` 로 등재했다.
- 전수 **`Ran 628 tests` · `OK` · rc 0**(1회, 맨몸) · 건수 무변이라 `README.md:104` 도
  `628` 그대로다 · `src/` 0줄 · `tests/`·`e2e/` 0줄 · `docs/specs/` 무변 · `data/crawl.db` 무변.
- 다음: **리뷰 phase.**
- 집안일: 이 항목을 붙이니 `history_current.md` 가 **306줄(상한 300)** 이라 계획 67 의
  여섯 항목(반복 387~392)을 `docs/history_069.md` 로 회전하고 `digest.md` 아카이브
  명부에 등재했다(306 → **111줄**).

## 396 · 리뷰 · 계획 68 `spec-citation-anchor` 1/1 · GREEN

- **판정 GREEN.** 백지 패스(diff 만)와 대조 패스(계획·CLAUDE.md) 둘 다 막는 것 0건.
  브랜치 전체 diff 는 `README.md` 1줄 · `tests/test_docs.py` +45-9 · 인용 12자리 정정
  (`tests/test_serve.py`·`e2e/design_check.py`·`e2e/perf_crawl.py`·`e2e/quality_eval.py`)
  · 나머지는 문서다. `src/` 0줄 · `docs/specs/` 무변 · `data/crawl.db` 무변 · 새 의존성 0.
- **핵심 판정 — `_anchors` 공유는 옳다.** 대조 축은 「옮겨 적은 것이 **있는** 인용」만
  재고 의무 축은 「**모든** 인용이 뭔가 갖는가」를 재는데, 둘이 같은 추출기를 보므로
  추출기가 죽으면 의무 축이 **먼저** 운다. 갈랐으면 의무 축이 자기 추출기로 "앵커 있음" 을
  선언하고 대조 축은 조용히 0건이 되는 구멍이 열린다. 정적으로 확인했다 — `PHRASE` 가
  죽으면 문구 전용 **12자리**가, `CONST`/`NUM` 이 죽으면 값 전용 **4자리**가 앵커 0 이 되어
  새 단언이 문다. 거짓 초록(수집 0건)은 `_citations` 안의 `MIN_HITS 14` 가 이미 막는다.
- **실측(정적, 변이 0판): 인용 18건 전부 앵커를 갖는다**(앵커없음 0). 대조 건수는
  **19**(문구 14 · 값 5)로 계획 전 7 에서 올랐다.
- **정보성 1 — `MIN_CHECKS 6` 은 이제 사실상 잉여다.** 실측 19 대비 하한이 6 이라
  13건이 조용히 사라져도 안 운다. 다만 「전부 사라짐」은 새 의무 단언이 먼저 물고,
  값을 손으로 관리할 자리를 늘리지 않기 위해 계획 5절이 **명시적으로 올리지 않기로**
  했다. 지금 손대면 직교 편집이라 **안 고친다** — 하한을 없앨지/올릴지는 다음 계획 몫.
- **정보성 2 — 밀림 생존 넷은 범위 인용의 구조적 성질이다.** 재실측에서 생존한 자리는
  `test_serve.py:914`·`design_check.py:2`·`design_check.py:444`(전부 `50-54`, `-1`·`-2`)과
  `quality_eval.py:3`(`22-23`, `-1`) 넷뿐이고 **정확히 범위 인용 넷**이다. 나머지 14 는
  ±1·±2 에서 전부 죽는다. 처방은 `digest.md` `[5]` 에 이미 등재돼 있다.
- **정보성 3 — 공백 한 칸짜리 문구(`" "`)는 `PHRASE` 를 통과하고 `assertIn` 도 통과한다.**
  실물 0건이고 막으려면 최소 길이를 손으로 관리해야 해서 적어만 둔다.
- `print` 라벨(`design_check.py:444`)이 큰따옴표를 갖게 됐지만 그 문자열을 긁는 파서는
  없다(참조처는 `docs/project.md`·`docs/e2e/search-ui/result.md` 산문뿐).
- 전수 **`Ran 628 tests` · `OK` · rc 0**(맨몸) · `README.md:104` 의 `628` 과 일치 ·
  `metrics.md` 반복 396 = `status.md` iteration 396(`IterationSyncTest`).
- 다음: **e2e phase.** 코드 diff 가 테스트·주석뿐이라 e2e 는 회귀 확인이 몫이다.

## 397 · e2e · 계획 68 `spec-citation-anchor` 1/1 · DONE

- **판정 통과 → DONE.** 전수 **`Ran 628 tests in 15.748s` · `OK` · rc 0**(맨몸 1회) ·
  실물 e2e 스크립트 **2개** 실행(`design_check.py`·`perf_crawl.py`, 둘 다 rc 0) ·
  실물 사본 대조군 **3판**(V0 성한 원본 + 변이 2판). 산출물은
  `docs/e2e/spec-citation-anchor/result.md`.
- **이 계획의 크기를 «같은 변이의 숫자 차이» 로 샀다.** 사양 `concept.md` 20행 뒤에 항목
  한 줄을 끼우는 변이(V1)는 계획 67 e2e(반복 392 `U1`)에서 **12건**을 죽였다. 오늘 같은
  자리 같은 편집이 **20건**이고, 인용 주소로 접으면 **18자리 중 17 사망 · 1 생존**이다.
  **새로 죽는 여덟이 정확히 계획 68 이 앵커를 심은 자리다** — `perf_crawl.py:1`·`:151`·
  `:253`·`:275` · `quality_eval.py:34`(문구 축) · `test_serve.py:918`·`:936`·`:955`.
  계획 1절의 「이 자리들은 조용히 산다」가 실물 변이에서 뒤집혔다.
- **V2 — 착수 탐침 `M1` 이 죽는다(완료 기준 3).** 계획 전 `Ran 627` `OK` rc 0 으로 살아
  나가던 `e2e/perf_crawl.py` 1행 `:44`→`:45` 가 오늘 `failures=1` · 라벨
  `[e2e/perf_crawl.py:1 문구]`. 개발 phase 의 판정을 성한 실물 사본에서 되샀다.
- **생존 1건은 구조적이다** — `e2e/quality_eval.py:3`(`concept.md:22-23`). 21행 뒤에 한 줄이
  끼면 옛 22행이 23행으로 가는데 **23이 아직 범위 안**이라 산다. 반복 395 가 72조합을
  정적으로 재서 적은 「생존은 범위 인용의 구조적 성질」이 **사양 쪽으로 미는 방향에서
  실행으로 확인됐다.** 처방은 이미 `digest [5]`(문구 축을 범위 전체가 아니라 시작행에서
  찾기) — 계획 68 범위 밖이라 **안 고쳤다.**
- **실행 경로 회귀 — `status.md` 가 남긴 질문에 실행으로 답했다.** `design_check.py` 의
  `print` 헤더가 바뀐 그대로 찍히고(안쪽 `"화면은 둘뿐"` 이 이스케이프 없이 선다) 4축 전부
  통과 · `perf_crawl.py` 3시나리오 전부 통과. **위험했던 자리는 `perf_crawl.py:275` 의
  `assert` 메시지 하나**다 — 작은따옴표로 돌면서 `%` 포맷 첫 조각이 됐는데 **통과하는
  실행에서는 한 번도 렌더되지 않아 인자가 어긋나도 초록으로 나간다.** 손으로 다시 적지 않고
  `ast` 로 그 `Assert` 노드의 `msg` 를 뽑아 그대로 컴파일·평가했다 — 포맷 인자 다섯이 맞고
  앵커 문구가 산다. **「초록이라 안 도는 문자열」이 e2e 의 사각이라는 표본을 하나 남긴다.**
- **계획서 숫자 정정 하나** — 2·3절의 「앵커 없음 11」은 실측 **12** 다
  (`e2e/quality_eval.py:34` 가 값 앵커를 가져 「있음 7」에 들어갔지만 문구 축에서도 새로
  세어진다). 개발(394)·리뷰(396)는 이미 12 로 적었고 정정 자리 집합이 같아 판정 무영향.
- **안 산 것**: e2e 21종 개별 재실행 **0회**(diff 가 닿은 둘만 돌렸다 — 안 돌린 것을 통과로
  적지 않는다) · 새 e2e 파일 **0개**(`unittest discover` 와 겹쳐 「1회만」이 깨진다) ·
  완료 기준 6(추출기 죽이는 변이)은 반복 394·396 이 두 번 산 판정이라 예산 2판을 V1·V2 에
  썼다 · `quality_eval.py` `main()` 은 `data/crawl.db` 와 질의집합이 필요하고 이 계획이
  고친 것은 3행 docstring 뿐이라 안 돌렸다.
- **범위**: `git diff --stat 274c0eb^ HEAD -- src/ docs/specs/ data/` **빈손** rc 0 ·
  `data/crawl.db` sha256 `85c96744…5bda18` **무변**(계획 65·66·67 e2e 와 같은 값) ·
  재색인 0 · 스키마 0 · 새 의존성 0 · `README.md:104` 「628건」·「21종」 실측 일치 ·
  사본 변이 뒤 저장소 `git status --porcelain` **빈손**.
- **예산** — 도구 호출 20회 이내 · 전수 1회 · 변이 2판. 변이 3판(V0 포함)을 **한 호출**에
  묶어 샌드박스 안에서 대화가 왕복하지 않게 했다(이 저장소가 두 번 정지한 자리다).
- 집안일: 계획서를 `docs/plan_history_054.md` 로 아카이브하고 `digest.md` 「완료」 명부와
  `index.md` 68번 행을 갱신했다.
- 다음: **계획 phase.** 계획 68 은 DONE 이고 다음 계획은 아직 없다.

## 2026-09-06 · 반복 399 · 계획 69 `noindex-entity-prefilter` — 계획 phase

- 브랜치 `loop/noindex-entity-prefilter` 를 `origin/main`(`7fcd669`)에서 땄다.
  계획서 `docs/plan_noindex-entity-prefilter.md` · 스텝 1개(개발 1/1) · 설계 없음.
- 근거는 **사용자 지시**(「소스 편중을 깨라」)와 `digest` 후보 `[4]`,
  그리고 `src/websearch/extract.py:202-203` 이 스스로 적어 둔 천장 주석이다.
  계획 60~68 아홉 계획이 `src/` 를 0줄 고쳤고, 이 계획은 제품 2줄에 착지한다.
- **착수 탐침이 근거를 오늘 다시 쟀다**: `name="&#114;obots"`·`name="&#x72;obots"`
  둘 다 `is_noindex()` **False** 인데 `_MetaRobotsParser` 단독은 `['noindex']`·
  `['none']` 을 본다 — 막는 것은 파서가 아니라 사전 필터 한 줄이다.
  정상 문서는 True(오탐 아님) · `&#` 판별자는 엔티티 문서에 있고 `&amp;` 문서에 없다.
- **처방을 갈아 끼웠다** — 후보가 적어 둔 「필터 제거」는 모든 페이지를 한 번 더
  파싱한다. 채택안은 필터를 `&#` 까지 넓히는 것(제품 2줄)이고, 대안 셋을 계획서
  4절 표에 남겼다.
- **구멍이 두 자리**인 것을 못박았다: `extract.is_noindex()`(색인 진입)와
  `indexer.py:176` 의 `WHERE p.html LIKE '%robots%'`(이미 색인된 것 제거).
- 탐침이 **다른 후보의 서술을 하나 뒤집었다** — `digest [8]` 의 `<nav>` 인라인
  연접은 링크 2개에서 내비 5점 대 본문 6점으로 **본문이 이긴다**.
- 이번 반복은 문서만 고쳤다 — `src/`·`tests/`·`e2e/`·`README.md` **0줄**.
- 다음: **개발 phase 1/1.**
