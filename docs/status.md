---
signal: GREEN
phase: 계획
step: 0/0
attempt: 0
iteration: 423
updated: 2026-09-07
ctx: 84
night_iterations: 1
night_red: 0
night_retries: 0
plan: null
---

## 현재 상태

**야간 탐색 1회. 계획을 새로 열지 않고 짧은 경로 한 건으로 끝냈다 — 컨텍스트 상한에 걸려 정지한다.**
전수 **648 OK rc 0**.

## 짧은 경로

- **근거**: `digest.md` 후보 `[7]`(robots 비ASCII 예외 누수)의 곁가지 문장
  「`robots.py:_fetch_robots` 의 `resp.read()` 무상한 건과 함께 열면 싸다」가 **낡았다** —
  `src/websearch/robots.py:14` 에 `MAX_ROBOTS_BYTES = 512_000` 이 이미 있고
  `git log -S` 로 계획 `robots-read-cap`(`eb6697e`)이 닫은 것을 확인했다.
- **할 일**: 그 곁가지에 취소선 + 닫은 계획·커밋·상수를 부기. `docs/digest.md` 한 줄만.
- **완료 기준**: 전수 648 OK · `digest.md` 줄 수 198 유지(상한 200).
- **이미 한 것**: 전부. 편집·검증·기록 완료.

## 탐색 결과 (8순위 전수)

| 순위 | 출처 | 결과 |
|---|---|---|
| 1 | 실패 테스트 | **0건** — 648 OK |
| 2 | 타입·린트 | **해당 없음** — 이 저장소에 설정이 없다 |
| 3 | 코드 `TODO`/`FIXME`/`HACK` | **0건** — 유일한 히트 `tests/test_indexer.py:779` 는 HTML 주석 **fixture 문자열**이지 실행 코드가 아니다 |
| 4 | `docs/candidates.md` | **파일 없음** |
| 5 | `digest.md` 보류 | **0건** — 절이 주석뿐이다 |
| 6 | `digest.md` 후보 | 다수. 아래 참조 |
| 7 | `digest.md` 반복 실패 | 새로 여는 근거 없음 |
| 8 | 이슈 | 저장소에 이슈 트래커 없음 |

`docs/patches/` **없음** — 되살아날 근거 0.

## 높은 점수 후보를 왜 안 열었나 (실측한 것만)

- **`[9]` `/passages` p95 의 대부분이 `/search` 몫** — 항목이 스스로 적은 **여는 조건이
  「실물 분포가 먼저다」**이고 오늘 실물 분포가 없다. 성능 최적화라 야간 금지 목록이기도 하다.
- **`[8]` 숨은 텍스트가 근거 문단으로 나온다** — 앞 절반은 계획 51 `hidden-passage` 가 닫았고,
  남은 뒷부분(`<nav>` 인라인 연접)은 `_INLINE_TAGS` 가 **색인 경로와 공유**라 손대면 재색인이다
  → 야간 금지(스키마·마이그레이션 성격).
- **`[7]` `rules/docs.md` 문서 상한을 재는 자 0개** — 반복 422 가 이미 사람 몫으로 올렸다.
  상한 초과를 RED 로 만들면 **야간이 그 RED 를 스스로 못 푼다**(삭제 금지).
- **`[7]` robots 비ASCII 예외 누수** — 항목이 「지금은 도달 불가」라고 적었고 실제로
  `test_robots_and_store_never_see_non_ascii_url` 이 순서 밀림을 막는다. 여는 조건은
  「`robots.py` 를 직접 부르는 새 호출처」이고 오늘 없다. **곁가지 하나만 낡아서 그것을 고쳤다.**
- **`[6]` `index.md` 다섯째 칸 헤더 확인자 0개** — 여는 조건이 「표에 열을 실제로 더하는 날」이다.

## 정지 사유

**컨텍스트.** 시작 시점에 이미 **79%** 였고(직전 세션의 마감 서술을 물려받았다) 탐색을
마쳤을 때 **83~84%** 다. 상한 85 를 넘기기 전에 스텝을 **끝내고** 기록을 남긴다 —
중간에 끊기면 다음 반복이 무엇을 이어받을지 알 수 없다. 5h **30%** · 7d **45%** 로
한도는 여유가 있다. **컨텍스트 정지는 실패가 아니다.**

## 사람 몫 — 반복 422 가 올린 셋이 그대로다

1. **`digest.md` 회전** — 지금 **198줄**, 상한 200 에 여유 두 줄. 다음 등재 한두 줄이면 넘는다.
   무인 모드는 삭제를 못 하므로 **다음 회전은 사람이 연다.**
2. **후보 `[7]`** — 문서 상한(200·300)을 재는 자 0개. 1번이 먼저 정해져야 못을 세운다.
3. **후보 `[6]`** — 실물 후보 절의 포인터 모집단을 재는 자 0개. 규약 변경이라 별건.

## 검증 (맨몸 · 리다이렉션 0)

```
PYTHONDONTWRITEBYTECODE=1 PYTHONPYCACHEPREFIX=$(mktemp -d) PYTHONPATH=src python3 -m unittest discover -b -s tests
Ran 648 tests in 15.9s · OK · rc 0
```

`src/`·`e2e/` **0줄** · `docs/specs/` 무접촉 · `data/crawl.db` 무개봉 · 바깥 네트워크 0.
