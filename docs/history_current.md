# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**회전 명부는 `digest.md` 의 `## 완료` 절 «아카이브 명부» 줄이 정본이다.**
여기 있던 스물한 회전의 서술(233줄)은 그 줄과 내용이 겹쳤고, 검사가 강제하는 명부도
그쪽 하나뿐이라(`tests/test_docs.py` 의 `ArchiveIndexTest`) **개발 9(반복 269)가 이
자리에서 지웠다** — 회전으로는 300줄 상한을 못 맞추던 세 반복(309 → 372 → 418줄)의
원인이 이 명부였다. **지운 것은 머리말이지 반복 기록이 아니다** — 항목은 여전히
append 전용이고 수정·삭제 금지다. 각 회전의 사유는 `digest.md` 의 같은 줄에, 원문은
`history_<NNN>.md` 에 그대로 있다.

## 2026-09-03 17:35 | hidden-passage | 설계 1 | 시도0

- 한 일: **`docs/design_hidden-passage.md` 를 세워 갈림길 넷을 닫고 `VERSION` 을 판정했다.**
  제품 `src/` **0줄** — 시제품은 `extract._BlockParser` 를 **상속**해 만들었고(탐침 파일은
  저장소 밖 scratchpad), 판정을 `extract` 모듈에 꽂아 `indexer.passages()` 를 진짜 임시 DB
  위에서 불렀다. `git diff` 빈 출력으로 원복이 아니라 **애초에 안 고친 것**을 확인했다.
- **결론: A(블록 파서 전용 술어).** `_NON_BLOCK_TAGS` 선례를 따라 `_BlockParser` 만 읽는
  `_is_hidden(tag, attrs)` 로 다섯 모양의 텍스트를 블록에 안 담는다. 색인 경로·스키마·
  `data/crawl.db` 무변경, 되돌리기는 커밋 하나 revert(플래그를 안 쓰는 사유도 적었다).
- **B 를 버린 이유가 계획서가 쓴 것보다 셌다.** 계획서는 「가장 짧지만 재색인」이라 적었는데,
  실제로 `_SKIP_TAGS` 는 **태그 이름만** 보는 집합이라 다섯 중 넷(속성 둘 · 인라인 스타일 둘)을
  **아예 못 잡는다** — 가장 짧아 보이던 안이 가장 적게 닫는다. C 는 감점이라 밀도가 높으면
  여전히 이겨 완료 기준 1번(0/5)을 보장 못 하고, 블록 튜플 폭까지 바꿔야 한다.
- **가장 위험한 가정을 깼고, 거짓이었다**(설계 룰 3-2). «시작 태그에서 열고 종료 태그에서
  닫는 스택이면 충분하다» → **종료 태그가 없는 요소가 문서 나머지를 통째로 삼킨다.**
  `<img aria-hidden="true">`(장식 아이콘) · `<hr hidden>` · `<input hidden>` ·
  `<br style="display:none">` **4/4 에서 3블록 → 1블록**. 개발로 넘기지 않고 설계 안에서
  `_VOID_TAGS` 가드(HTML void 14개)를 넣어 다시 쟀다 — 4/4 무해.
- 결과: 다섯 모양 각 **2블록 → 1블록**(숨은 블록 1 → 0 · 본문 문단 5/5 생존) ·
  `indexer.passages()` 가 고른 숨은 텍스트 **5/5 → 0/5**, 본문 문단 **0/5 → 5/5** ·
  숨은 블록만 매치인 문서의 문단 **1건 → 0건**. **오탐 방향은 세 자로 쟀다** —
  평가 코퍼스 64문서(블록 253) 차이 **0** · `tests/` 의 HTML 리터럴 263개(블록 298) 차이 **0** ·
  음성 6종(`aria-hidden="false"`·`font-size:0.9em`·`font-size:10px`·`class="hidden-md"`·
  `display:block`·문단 안 인라인 숨김) **6/6 안 문다**.
- **`font-size:0` 이 이 판정에서 오탐이 가장 가까운 자리다** — 부분문자열로 보면
  `font-size:0.9em`(보이는 작은 글씨)을 문다. 뒤에 숫자·점이 오면 안 물게 정규식으로 못 박고,
  그것을 부분문자열로 되돌리는 변이가 새 단언을 죽이도록 개발에 넘겼다.
- **`VERSION` 은 안 올린다.** 사양 기능 9 는 «필드 삭제·뜻 변경» 인데 필드 0개 삭제,
  `position` 의 정의(«결과의 순번») 무변이고 바뀌는 것은 **값**(실측 1 → 0)이다. 방향이
  계약을 어기던 쪽에서 지키는 쪽이고, 같은 API 안에 선례가 있다 — 계획 48 의
  `MAX_PASSAGE_HTML` 캡도 같은 질의의 결과를 바꿨지만 `VERSION` 은 1 그대로였다.
  대신 「조용한 변경」이 안 되게 `README.md` 의 `/passages` 절에 한 줄 적는 것을 계약에 넣었다.
- 불변식 `" ".join(blocks) == extract_text()[1]` 에는 **세 번째 예외**를 적는다(숨김 있는
  HTML). 정상 HTML 에서는 그대로다 — 코퍼스 64문서 전수 **True**.
- 결과: 단위 **579건 OK** rc 0(맨몸·단독 1회) · 제품 `src/` 0줄 · `e2e/`·`docs/specs/`·
  `data/crawl.db` 무변경 · 새 의존성 0 · 러너 규율 위반 **0**(열세 반복 연속).
- 다음: **개발 1/1** — `src/websearch/extract.py` 에 `_VOID_TAGS`·`_ZERO_FONT`·`_is_hidden`
  과 `_BlockParser` 의 메서드 셋, `tests/test_extract.py` 에 새 단언(설계 6절 목록).
  TDD 로 RED 를 먼저 보고, 변이는 좁아지는 쪽 5 · 넓어지는 쪽 3 을 양방향으로 심는다.

## 2026-09-03 18:20 | hidden-passage | 개발 1 | 시도0

- 한 일: **설계 A 를 그대로 심었다** — `extract.py` 에 `_VOID_TAGS`(HTML void 14개) ·
  `_ZERO_FONT` · `_is_hidden(tag, attrs)` 와 `_BlockParser` 의 메서드 셋(`handle_starttag`
  에서 열고 `handle_endtag` 에서 `_open` 과 **같은 관용구**로 닫고, `handle_data` 에서
  **애초에 안 담는다**). **TDD — RED 를 먼저 봤다**: 새 단언만 `FAILED (failures=14)`,
  구현 뒤 `Ran 585 tests / OK` rc 0(맨몸·단독). 제품 순증 **약 45줄**, 파일 1개.
- **설계가 지목한 무변경 지점을 지켰다** — `_SKIP_TAGS`·`_INLINE_TAGS`·`_NON_BLOCK_TAGS`·
  `_normalize`·`_TextParser` 는 `git diff` 에 **한 줄도 안 나온다.** 그래서 색인 본문·
  스키마·`data/crawl.db` 가 구조적으로 안 움직이고 재색인이 0 이다.
- **변이 8건을 양방향으로 심어 전부 RED 를 눈으로 봤다**(`.mutation-lock` 걸고 매번 원복,
  마지막에 `git diff -- src/` 로 확인 · 커밋된 변이 0). 좁아지는 쪽 5 는 다섯 모양을
  각각 지우니 **그 모양의 단언만** 빨개졌다(각 2~4건, 전부 새 단언). 넓어지는 쪽 셋 —
  ⓐ `_is_hidden` 이 늘 True → **기존 `TestExtractBlocks` 가 무너진다**(오탐이 이 계획의
  제일 위험이라는 것을 스위트가 증명한다) · ⓑ `_ZERO_FONT` → `"font-size:0" in style`
  → 음성 단언 `font-size:0.9em` **하나만** 죽는다 · ⓒ `_VOID_TAGS` 가드 제거 → void
  4모양 전부 «앞 문단만 남는다». 설계가 예고한 자리와 죽은 자리가 정확히 같다.
- **계약이던 문서 두 줄도 같은 커밋이다** — `README.md ## 품질 기준` 아래에 «`/passages` 는
  화면에 안 보이는 텍스트를 근거로 내지 않는다» 를 적고(`VERSION` 을 안 올리는 대신 조용한
  변경이 안 되게 한 것 · 설계 5절), 단위 건수를 **579 → 585** 로 고쳤다(`test_readme.py`
  자신이 그 숫자를 잰다 — 안 고치면 첫 실행이 RED 다).
- 결과: 단위 **585건 OK**(13.476초) rc 0 · 새 단언 **6건**(extract 5 · indexer 1) ·
  `e2e/`·`docs/specs/`·`data/crawl.db` 무변경 · 새 의존성 0(`re` 는 stdlib) ·
  러너 호출 **11회 전부 맨몸·단독**(변이 8회는 `python3 -B` — 선례 `history_042`).
- 다음: **테스트 1/1** — 완료 기준의 「그대로여야 할 것」 중 스위트 밖 축(코퍼스 64문서
  블록 목록 차이 0 · `passage_eval` 정확도·p95 · `quality_eval` ko/en)을 실제로 재고,
  갭 탐색으로 새 단언이 못 잡는 자리를 찾는다.

## 2026-09-03 17:35 | hidden-passage | 테스트 1 | 시도0

- 한 일: 전체 `Ran 587 tests / OK` rc 0(`PYTHONPATH=src python3 -m unittest discover
  -b -s tests` — 맨몸·단독). 스위트 밖 축을 전부 다시 쟀고, 갭 탐색이 **직전 개발이
  심은 코드의 결함 하나(8점)** 를 찾아 같은 반복에서 닫았다.
- **결함 — 성긴 스택에 «같은 관용구» 를 그대로 옮긴 것.** `_hidden` 은 **숨긴 태그만**
  담는데 `handle_endtag` 는 이름으로 찾아 잘랐다. 그래서 숨김 컨테이너 **안의 같은 이름
  자식**이 닫히면 바깥 숨김이 함께 풀려 그 뒤가 샜다. 실측 —
  `<div hidden><p>숨은</p><div>속</div><p>숨은 김치찌개</p></div>` → 뒤 문단이 블록으로
  나왔다(다섯 모양 중 넷 + 인라인 `<span>` 까지 5건). `<div hidden>` 안의 `<div>` 는
  모달·드롭다운·탭 패널의 **기본형**이라 실물에서 가장 흔한 중첩이고, 새는 방향이
  «숨은 텍스트가 근거로 나간다» 쪽이라 완료 기준 1번(0/5)을 실물에서 무너뜨린다.
  `_open` 이 같은 관용구로 안전한 것은 **그쪽이 빈틈없는 스택이기 때문**이다.
- **고친 방식 — 스택을 빈틈없게 만든다**(제품 순증 6줄, 파일 1개). `_els` 가 열려 있는
  **모든 비-void 요소**를 `(태그, 조상 중 하나라도 숨김인가)` 로 담고 시작 태그에서
  숨김을 **전파**해 `handle_data` 는 꼭대기만 O(1)로 본다. `_VOID_TAGS` 가드는 그대로다.
- **살아 있던 변이도 하나 잡았다.** 닫기를 「꼭대기가 맞을 때만 pop」으로 바꾸니 587건이
  **전부 통과**했다. 갈리는 자리는 안 닫힌 자식(`<div hidden><p>숨은</div><p>본문</p>`)
  으로, 브라우저가 `</div>` 에서 대신 닫는 **실물의 정상형**인데 꼭대기만 보면 숨김이
  문서 끝까지 고정돼 **뒤의 본문이 조용히 사라진다** — 계획서 8절이 제일 큰 위험으로
  적은 오탐 방향이다. 단언을 더해 죽였다.
- **TDD** — 두 단언 다 먼저 넣어 RED 를 눈으로 봤다(`['숨은 김치찌개', '보이는 김치찌개']
  != ['보이는 김치찌개']` 5건 · 꼭대기 pop 변이에서 `[] != ['보이는 김치찌개']` 3건).
  새 단언 **2건**, 단위 **585 → 587**(`README.md` 동기 — `test_readme` 가 강제한다).
- **변이 4건 전부 RED**(`.mutation-lock` · 매번 원복 · `git diff -- src/` 로 확인 ·
  커밋된 변이 0): 성긴 스택 되돌리기 **6실패** · 조상 전파 제거 **18실패** ·
  꼭대기만 pop **3실패** · `_VOID_TAGS` 가드 제거 **4실패** · 숨김 늘 True 대량 실패.
- **스위트 밖 축 — 기준선 그대로다.** `passage_eval` 블록 **253** · 정확도 **100.0%** ·
  채택률 **99.5%** · p95 1.49ms · `quality_eval` ko **20/20** · en **19/20** · 1위 39 ·
  매치 평균 14.0(최소 11) · `perf_search` p95 8.77ms · `design_check` rc 0 ·
  `perf_crawl` 10.20/10.23/s · 문서집합 sha1 `541d455a…c078654b` **무변** ·
  `data/crawl.db` sha256 `85c96744…75bda18` **무변**.
- **오탐을 자 셋으로 다시 쟀다** — 코퍼스 64문서를 통짜(64블록)와 문장 `<p>` 포장
  (**253블록**) 두 형태로 「판정 켜고/끄고」 비교해 달라진 문서 **0** · 불변식 깨진 문서
  **0**; `tests/` HTML 리터럴 315개 중 달라진 **9개는 전부 이 계획이 새로 넣은 숨김
  fixture** 였다. `extract` 를 지나는 e2e 둘(`indexer_e2e`·`search_api_e2e`) rc 0.
- 갭 8점 미만 셋(중복 `style` 속성 · 자기닫음 표기 · `font-size:00` — 전부
  `html.parser` 가 브라우저와 갈리는 자리)은 한 항목으로 `digest` 에 등재. 격리·flaky·
  보안·동시 실행 카테고리는 0건이다(파서는 호출마다 새로 만들어져 공유 상태가 없고,
  신뢰 경계인 크롤 HTML 은 방향이 «덜 낸다» 이며 `/passages` 는 JSON 전용이다).
- 다음: **리뷰 1/1** — 이 반복이 고친 자리가 곧 리뷰가 볼 자리다(성긴 스택이 두 곳
  더 있는지, `_open` 과 `_els` 두 스택을 합칠 수 있는지).

## 2026-09-03 18:40 | hidden-passage | 리뷰 1 | 시도0

- 한 일: 리뷰 1/1 — **반려**. 지적 **2건, 둘 다 승인 필요**(자동 수정 0). 제품 `src/`
  **0줄** · 테스트 0건. 패스 A(백지)를 먼저 끝내고 계획·설계를 그 뒤에 열었고, 착수·마감
  두 번 다 `Ran 587 tests / OK` rc 0(맨몸·단독).
- **[R51-1] 숨김이 형제로 샌다 — 정상 HTML 에서 보이는 문단이 사라진다.** `_is_hidden` 이
  참인 요소가 **선택적 종료 태그**(`p`·`li`·`td`·`th`·`tr`·`option`·`dt`·`dd`)면 브라우저는
  다음 형제에서 닫지만 `html.parser` 는 안 닫고, `_els` 가 그 상태를 전파해 **형제까지**
  숨김이 된다. 실측 — `<ul><li hidden>숨은<li>보이는 김치찌개</ul><p>뒤 문단</p>` →
  `['뒤 문단']` · `<p hidden>숨은<p>보이는 김치찌개</p>` → `[]` · `<p hidden>숨은<div>보이는
  </div>` → `[]` · `<table><tr><td hidden>숨은<td>보이는</table>` → `[]`.
  **설계 3절의 천장이 안 덮는다** — 거기 적힌 것은 «**안 닫힌** 숨김 컨테이너»(깨진 입력)
  인데 위 넷은 명세가 종료 태그 생략을 허용하는 **유효한 HTML** 이다. 직전 반복이 죽인
  변이(「꼭대기가 맞을 때만 pop」)의 **거울상**이고, 방향은 계획서 8절이 제일 큰 위험으로
  적은 오탐 쪽이다.
- **[R51-2] 닫는 태그마다 리스트를 새로 만든다 — 최악 11.5배.** `handle_endtag` 의
  `names = [t for t, _h in self._els]` 가 닫는 태그마다 돌고 `_els` 는 이제 **모든 비-void
  요소**를 담아 비용이 깊이²다. 캡(35,000자) 안 최악 `<b>` 5,000중첩에서 기점 **14.7ms →
  168.5ms**(위키/CMS 모양은 13.4 → 16.8ms 로 거의 안 움직인다). `PASSAGE_LIMIT` 10이면
  **1.7초**로 사양 성능 5 의 500ms 예산과 `project.md` 배정치 123ms 를 넘는다. 입력은
  크롤 HTML 이라 신뢰 경계 밖이다. **리스트를 안 만들고 제자리에서 뒤에서부터 찾으면
  17.8ms** 로 기점 수준을 회복한다(동작 무변).
- **단위 587건은 둘 다에 눈이 멀어 있다** — 근거가 스위트가 아니라 리뷰의 탐침
  (`bench.py`·`probe.py`·`variant.py`, 저장소 밖 임시 디렉터리 · 커밋 안 함)이다.
- **패스 B 신규 지적 0건.** 설계 계약(다섯 모양·`_is_hidden` 시그니처·`VERSION` 1 유지·
  README 한 줄)과 코드가 일치하고, 「안 건드린다」로 못박은 다섯은 diff 에 한 줄도 없다.
  버린 후보 **2건**(`font-size:0.0em` 미탐 · `_els` 메모리 상한 — 캡이 막는다).
- **직전 반복이 남긴 세 물음에 답했다**: 성긴 스택은 더 없고(`_skip_depth`·`_in_title` 은
  다른 축) · `_open` 과 `_els` 는 못 합치며(합치면 이름표가 샌다, 계획 48 리뷰 6 `[R6-2]`) ·
  「같은 관용구」로 넘어간 자리가 하나 더 있었다 — 관용구와 함께 **비용**도 옮겼는데 담는
  것이 블록 태그에서 전 요소로 넓어진 차이를 아무도 안 쟀다(그것이 [R51-2]).
- 다음: **개발 2** — 두 지적만 고친다. [R51-1] 은 위 네 줄을 단언으로 먼저 넣어 RED 를
  보고, [R51-2] 는 동작이 안 바뀌므로 변이와 실측 표로 잡는다.

## 2026-09-03 19:40 | hidden-passage | 개발 2 | 시도0

- 한 일: **리뷰 51 의 반려 2건을 둘 다 닫았다.** `src/websearch/extract.py` **한 파일** ·
  제품 순증 **약 21줄** · 새 파일 0 · 새 의존성 0. 단위 **587 → 588건 OK** rc 0
  (맨몸·단독, 13.494초).
- **[R51-1] 암묵적 닫기 — TDD 로 RED 를 먼저 봤다.** 리뷰 표의 네 줄을 단언으로 넣어
  `FAILED (failures=5)` 를 눈으로 보고 구현했다. `_IMPLIED_END`(종료 태그 생략이 허용된
  일곱 — `li`·`dt`·`dd`·`tr`·`td`·`th`·`option`)와 `_BlockParser._implied_end()` 를 넣고
  `handle_starttag` 이 시작 태그마다 **스택 꼭대기를 필요한 만큼 pop** 한다. **`p` 만 값이
  아니라 판정이다** — 같은 이름뿐 아니라 아무 블록 시작 태그로나 닫히므로 부모가 이미 쓰는
  술어(`tag not in _INLINE_TAGS and tag not in _NON_BLOCK_TAGS`)를 **읽기만** 한다.
  새 집합을 만들지 않았고, 그래서 「안 건드린다」로 못박은 다섯이 `git diff` 에 한 줄도 없다.
- **꼭대기만 본다는 것이 수리의 절반이다** — 닫히는 것은 형제뿐이고 숨긴 요소의 **자식**은
  숨김 그대로여야 한다. 중첩 목록·중첩 표·문단 안 인라인 셋을 넓어지는 쪽 가드로 같이 넣었다.
  결과: `<ul><li hidden>숨은<li>보이는 김치찌개</ul><p>뒤 문단</p>` → `['뒤 문단']` 이
  `['보이는 김치찌개', '뒤 문단']` 로, `<p hidden>`+`<p>`/`<div>`·`<td hidden>` 형제 셋은
  `[]` → 본문이 나온다. 리뷰 표의 넷에 셋을 더했다(`<tr hidden>` 걸러진 표의 행 ·
  `<option hidden>` 필터 드롭다운 · `<li hidden>` 안의 **안 닫힌 `<p>`**).
- **[R51-2] 리스트를 안 만든다 — 고치기 전·후를 둘 다 쟀다.** `names = [t for t, _h in
  self._els]` 를 제자리 역방향 `for` 로 바꿨다. 캡 35,000자 안 최악(5회 최소):
  `<b>` 5,000중첩 **169.0 → 18.1ms** · `<div>` 3,181중첩 **93.4 → 21.8ms** ·
  위키/CMS 모양 16.7 → 16.2ms. `PASSAGE_LIMIT` 10건이면 **1.7초 → 181ms** 로 사양 성능 5
  의 500ms 예산 안이고 리뷰가 적은 기점 수준(14.7ms)에 붙는다.
- **변이 여덟을 양방향으로 심어 전부 눈으로 봤다**(`.mutation-lock` · 매번 원복 ·
  `python3 -B` · 끝나고 `git diff -- src/` 로 **원복을 확인**했다, 「초록이다」로 확인하지
  않았다 · 커밋된 변이 0). ① 암묵적 닫기 삭제 → 실패 6(전부 새 단언) · ② `_implied_end`
  늘 True → 실패 **25**(기존 숨김 단언 전부 + `test_indexer` — 오탐이 제일 위험이라는 것을
  스위트가 다시 증명한다) · ③ `p` 가지를 `tag == "p"` 로 → 실패 2 · ④ `_IMPLIED_END` 를
  빈 것으로 → 실패 4 · ④' `tr`·`option` 줄만 삭제 → 실패 2 · ⑥ 역방향 → 정방향 스캔 →
  실패 6(성긴 스택 고장이 되돌아온다) · ⑦ `break` 삭제 → 실패 6.
- **⑤ 가 이번 반복의 값이다 — `while` 을 `if` 로 줄여도 588건이 전부 초록이었다.** 리뷰
  표의 넷은 전부 **한 번만 pop 하면 되는** 모양이라 반복문이 안 재졌다. 갈리는 자리는
  `<ul><li hidden>숨은<p>여전히 숨은<li>보이는</ul>`(안 닫힌 `<p>` 위의 `<li>` — 한 시작
  태그가 **둘을 겹쳐 닫는다**)이고 실물 목록의 기본형이다. 단언을 더해 죽였다.
  **반려를 고치는 반복에서도 「리뷰가 준 케이스만 넣으면 새 줄이 안 재진다」** 가 참이다.
- **⑧ (리스트 재생성으로 되돌리기)은 초록으로 살았다** — 동작이 같으므로 예상대로다.
  그래서 그 줄을 붙드는 것은 단언이 아니라 실측 표다(되돌리면 170.4ms 로 돌아간다).
  **스위트가 성능 축에 눈이 멀어 있다는 리뷰의 말이 실측으로 확인됐다.**
- **회귀 — 「그대로여야 할 것」을 전부 다시 쟀다.** 숨은 텍스트 **0/5** · 본문 **5/5**
  (실제 임시 DB 로 `indexer.passages()`) · 평가 코퍼스 64문서를 통짜(64블록)와 문장 `<p>`
  포장(**253**블록) 두 형태로 **직전 커밋의 `extract.py` 와 대조**해 달라진 문서 **0** ·
  불변식 깨진 문서 0 · `tests/` HTML 리터럴 326개 중 달라진 **8개는 전부 이번 반복이 넣은
  숨김 fixture**(이름으로 전수 확인) · 음성 6종 **6/6 안 문다** · `e2e/passage_eval.py`
  통과(블록 253 · 정확도 100.0% · p95 1.51ms) · `data/crawl.db` sha256 무변 ·
  `e2e/`·`docs/specs/` 무변 · `VERSION` **1** 유지.
- **남긴 천장 하나** — 안 닫힌 인라인 태그가 `<p>` 위에 얹히면 암묵적 닫기가 못 닿는다
  (`<p hidden>숨은<b>x<div>보이는</div>` → `[]`). 방향이 「덜 낸다」 쪽이고 깨진 입력이라
  설계 3절의 기존 천장 안이다. 닫으려면 인라인까지 훑어야 하고 그것이 방금 [R51-2] 로
  없앤 깊이² 비용을 되살린다 — **안 연다.**
- 결과: 단위 **588건 OK**(13.494초) · 새 단언 **1건**(subTest 9개 — 양성 7 · 음성 3) ·
  `README.md` 단위 건수 **587 → 588** · 러너 호출 **15회 전부 맨몸·단독** · 러너 출력 조작
  **0회** · RED 0 · 재시도 0.
- 다음: **테스트 2** — 스위트 밖 축을 다시 재고 갭 탐색. 겨눌 두 자리는 **변이 ⑧ 이
  초록이었다**(성능 축을 붙드는 단언이 스위트에 없다)와 **`_implied_end` 의 `p` 가지가
  읽는 두 집합**이 바뀌면 숨김 판정도 같이 움직인다는 새 결합이다.

## 2026-09-03 20:40 | hidden-passage | 테스트 2 | 시도0

- 한 일: **전체 스위트 588건 OK rc 0**(13.455초 · 맨몸·단독)에서 시작해 갭 탐색을 돌렸다.
  직전 반복이 겨누라고 적은 두 자리에서 **변이 셋이 살아 있었고 둘이 8점**이라 같은 반복에서
  닫았다. 제품 `src/` **0줄** · 고친 파일 **1개**(`tests/test_extract.py` **+15줄**) ·
  새 파일 0 · 새 의존성 0 · **단언 메서드 수 588 유지**(둘 다 기존 표 주도 테스트의
  **행**이라 `README.md`·`test_readme.py` 는 무변경이다).
- **[T2-1] `p` 가지가 읽는 술어 둘 중 하나만 재고 있었다 (8점).** `_implied_end` 의 `p`
  가지는 `tag not in _INLINE_TAGS and tag not in _NON_BLOCK_TAGS` 인데 **뒤 절을 지우는
  변이가 588건 전부 초록**이었다. 기존 케이스가 `<b>`(인라인) 하나뿐이라 앞 절만 재진다.
  갈리는 자리는 `<p hidden>숨은<br>여전히 숨은<img src=k.jpg>또 숨은 김치찌개` — `<br>`·
  `<img>` 는 인라인이 아니지만 문단 경계도 아니라 `<p>` 를 **안 닫아야 한다**. 닫으면 그
  뒤가 숨김에서 풀려 **숨은 텍스트가 근거로 샌다**(완료 기준 1번 「0/5」의 반대 방향).
  직전 반복이 남긴 「`p` 가지가 두 집합에 결합돼 있다」 가 결합이 아니라 **구멍**이었다.
- **[T2-2] 암묵적 닫기의 `while` 이 두 번 이하만 재지고 있었다 (8점).** 개발 2 가 ⑤
  (`while`→`if`)를 죽였는데 **한 칸 아래**(「최대 두 번만 pop」)가 다시 초록이다 — 새로
  넣은 겹쳐 닫기 케이스가 마침 **두 번**짜리였다. 셋을 겹쳐 닫는 자리는
  `<table><tr hidden><th>숨은<p>여전히 숨은<tr><td>보이는</table>`(`p`→`th`→`tr`)이고
  걸러진 표의 행은 실물의 기본형이다. **같은 행이 `_IMPLIED_END` 의 `th` 값을 처음 쓴다** —
  `th` 줄을 지우는 변이도 588건 전부 초록이었다(값이 표에 있는데 아무 케이스도 안 밟았다).
  한 행으로 변이 둘을 같이 죽였다.
- **변이 넷을 심어 새 단언 전·후를 둘 다 봤다**(`.mutation-lock` · 매번 원복 ·
  `python3 -B` · 끝나고 `git diff -- src/` 로 **원복 확인** · 커밋된 변이 0):
  ⓐ `del self._els[i:]` → `[i:i+1]`(닫을 때 자손을 안 자른다) **실패 1** — 살아 있진
  않지만 여유가 한 단언뿐이다(아래 8점 미만) · ⓑ `p` 가지의 `_NON_BLOCK_TAGS` 절 삭제
  **초록 → 실패 1** · ⓒ `while` 을 최대 2회로 **초록 → 실패 1** · ⓓ `_IMPLIED_END` 의
  `th` 줄 삭제 **초록 → 실패 1**.
- **스위트 밖 축을 전부 다시 쟀다 — 기준선 그대로다.** `passage_eval` 블록 **253** ·
  정확도 **100.0%**(398/398) · 채택률 **99.5%** · p95 **1.52ms**(기준선 1.51) ·
  `quality_eval` ko **20/20** · en **19/20** · 1위 **39** · 매치 평균 **14.0**(최소 11) ·
  `perf_search` p95 **8.78ms**(기준선 8.97) · `search_api` p95 **2.10ms**(기준선 2.11) ·
  `perf_crawl` 10.24/10.21/s · **문서집합 sha1 `541d455a…c078654b` 무변** ·
  `data/crawl.db` sha256 `85c96744…75bda18` **무변** · 숨은 텍스트 **0/6** · 본문 **6/6**
  (실제 임시 DB 로 `indexer.passages()`, `svg><title>` 포함).
- **성능 표를 다시 쟀고, 정상 문서 축을 새로 넣었다**(캡 35,000자 · 5회 최소 · 세 판을
  같은 입력으로). `<b>` 5,000중첩 **17.7ms**(개발 2 가 적은 18.1) · `<div>` 3,181중첩
  **21.8ms**(21.8) · 위키/CMS **16.5ms**(16.2) — **표는 오늘도 참이다.** 새로 잰 정상 문서
  둘: 기사 모양(제목·문단·링크·목록·표) 기점 `d5367fa` **9.5 → 12.2ms**(+29%) · 문단만
  기점 **5.1 → 6.0ms**(+18%). **암묵적 닫기 몫은 그중 8%**(고치기 전 11.3 → 12.2), 나머지는
  숨김 판정이다. `PASSAGE_LIMIT` 10건이면 **122ms** 로 사양 성능 5 의 500ms 예산 안이고
  블록 수는 세 판이 **전부 같다**(1800·921) — 정상 문서에서 동작은 안 움직였다.
- 결과: 단위 **588건 OK** rc 0(13.458초) · **e2e 20종 전수 rc 0**(제품 `src/` 를 고친 첫
  계획이라 전수로 돌렸다) · 새 단언 행 **2** · 새 e2e 0 · RED 0 · 재시도 0.
- 다음: **리뷰 2.** 남긴 것은 8점 미만 하나(ⓐ 의 여유가 한 단언뿐 — `<div hidden><span>속
  </div><p>보이는</p>` 처럼 **암묵적으로 못 닫는** 자손이 안 재진다)이고 `digest` 에
  등재했다. 성능 축은 이번에도 **단언이 아니라 표**가 붙든다 — 벽시계 단언은 스위트를
  깨지기 쉽게 만들고, 회귀를 실제로 잡은 것은 두 반복 연속 이 표였다.

## 2026-09-03 21:30 | hidden-passage | 리뷰 2 | 시도0

- 한 일: 패스 A(백지·diff 만) 뒤 패스 B(계획·설계·README·과거 코멘트). 지적 **2건**
  (승인 필요 1·자동감 1) · 버린 후보 **3** · 제품 `src/` **0줄** · **패스 B 첫 값**([R51-4]).
- **[R51-3] `p` 가지의 술어가 «열린 집합» 이라 숨김이 풀린다 — medium · 승인 필요.**
  개발 2 가 재사용한 `tag not in _INLINE_TAGS and tag not in _NON_BLOCK_TAGS` 는
  **여집합**이라 모르는 태그가 전부 `<p>` 를 닫는다 — 브라우저는 **닫힌 목록**만 닫고
  커스텀 요소는 phrasing 이라 안 닫는다. `<p hidden>숨은<my-widget>속</my-widget>또 숨은
  김치찌개` 가 스텝 1 뒤 `[]` → HEAD 는 숨은 텍스트를 낸다(`amp-img`·`slot`·`datalist`·
  `nobr` 도). **표준 태그 127개 전수**로 과잉 **19**·미달 **0**·커스텀 요소는 **전부 과잉
  (무한 집합)**. **실제 DB 로 3/4 유출**(대조군 `<div hidden>` 0/1)이라 완료 기준 1 「0/5」
  와 README 계약 문장이 거짓이다. 설계 3절 천장은 «덜 낸다» 방향이라 이 «더 낸다» 를 안
  덮는다 — [R51-1] 수리의 거울상.
- **[R51-4] `indexer.py:41~46` 의 성능 계수가 낡았다 — low.** 「최악 0.352ms/1000자 → 10건
  123ms(예산 25%)」가 HEAD 에선 **0.439 → 153.5ms · 배정치의 125% · 예산의 31%** 다.
- **성능 회귀는 «값을 한다» 로 판정했다.** 기사 11.31 → **14.40ms(+27.3%)** 는 재현되지만
  **실물 p95 는 1.51 → 1.54ms**(예산의 0.006%)이고 +29% 는 **캡 최악에서만** 난다 — 산 것은
  숨은 텍스트 **5/5 → 0/5**, 잃은 것 0, +29% 의 **87%가 스텝 1**(이 스텝은 +2.9%p).
- 결과: 단위 **588건 OK** rc 0(13.445 · 마감 13.495초 · 맨몸·단독) · `passage_eval` 통과 · 러너 3회 전부
  맨몸·단독 · 출력 조작 0 · PR 0 · RED 0. **다음: 개발 3.** 교훈은 리뷰 1·[T2-1]·[R51-3]
  이 **한 뿌리**라 `digest ## 반복 실패` 에 **한 항목(3회)** 으로 합쳤다.
