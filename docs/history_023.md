# 반복 기록 아카이브 023 — 계획 45 `docs-record-guard` 의 앞 네 반복 (반복 228~231)

<!-- 수정·삭제 금지. history_current.md 에서 밀려난 것. 요약은 digest.md 에 있다. -->

## 2026-09-01 23:55 | docs-record-guard | 계획 1/1 | 시도0

- 한 일: **계획 45 `docs-record-guard` 를 열었다** — 계획서 `docs/plan_docs-record-guard.md` ·
  브랜치 `loop/docs-record-guard`(기점 `79952ee`). 근거는 탐색 **7순위**(`digest ## 반복 실패`)
  이고 항목 둘이 *"같은 파일·같은 꼴이라 한 계획으로 묶인다"* 고 스스로 적어 둔 것이다:
  `반복`↔`iteration` 불일치(4회 재발)와 아카이브 색인 구멍.
- 탐색 1~8순위 전수(계획서 2절 표): 1 **0건**(단위 475 OK 11.923초, 맨몸) · 2 해당 없음 ·
  3 **0건** · 4 파일 없음 · 5 **0건** · 6 후보 다수(1등 `[6]` 은 파일·축이 달라 안 연다) ·
  7 **여기서 잡았다** · 8 해당 없음. 중복 확인: 활성 계획 0 · `docs/patches/` 없음 ·
  `test_docs.py` 를 건드린 38·42 는 두 불변량을 다룬 적 없다.
- **착수 탐침이 근거를 세 군데 정정·확대했다**(전부 메모리 사본 · 저장소 무수정):
  ① `digest` 항목의 처방을 **글자 그대로** 구현하면 구멍이 4개인데 항목이 적은 수는 5개다 —
  차이 `history_001.md` 는 `digest ## 반복 실패` 의 색인 구멍 항목 본문 — **구멍을
  신고하는 그 문장 자신**에만 나온다.
  **신고문이 신고 대상을 초록으로 만든다.** ② 살아있는 문서 6개 전수 대조에서
  **`history_002.md`·`history_005.md` 는 어디에도 없는 진짜 고아**다(항목은 003 만 봤다).
  ③ 순진한 substring 은 `plan_`/`design_` 접두에 가려진다 — `history_020.md` 는 단독 2회·
  접두 2회라 단독만 지우면 순진한 검사는 **통과**, 경계 검사는 RED(잠복 거짓 초록).
- 결과: 검사 A 변이 V1·V2(과거 재발 ④·② 재현) 둘 다 **잡힌다**, V3(값 없음)은 설계로 넘긴다.
  `src/` **0줄** · 의존성 0 · `data/crawl.db` 무변경.
- **러너 파이프 1회 — 그리고 이번엔 진짜 RED 를 가렸다(⑬).** 계획서를 다 쓰고 스위트를
  재확인하며 `... discover tests 2>&1 | tail -3` 을 돌렸고, `tail -3` 이 잡아온 것은
  크롤 로그 세 줄이라 **판정 줄도 종료 코드도 안 보였다.** 맨몸으로 다시 돌리니
  **FAILED (failures=1)** 이었다. 방아쇠는 ⑥⑨⑪⑫ 와 같은 *"이미 초록일 것 같은 재확인"*
  이지만 **결과가 다르다 — 앞의 열둘은 전부 초록을 가렸고 이번은 빨강을 가렸다.**
  파이프를 안 걷었으면 RED 를 커밋했다. `digest` 처방(러너 호출을 별칭·`make test` 로
  굳히기)의 근거가 한 단계 세졌다.
- **그 RED 가 계획 42 의 검사였다 — 가드가 실사용에서 발화했다.** `DocCitationTest` 가
  이 계획서의 `digest.md`·`index.md` **줄번호 인용 두 곳**을 잡았다. append 전용 문서를
  줄번호로 가리키면 회전 한 번에 죽는다는 바로 그 규칙을, **이 계획서가 어겼다.**
  이름 인용으로 고쳐 GREEN(475건 OK 11.963초, 맨몸). 정규식이 못 잡았지만 같은 죄인
  두 곳도 함께 고쳤다 — 검사를 통과시키는 게 아니라 규칙을 지키는 것이 목적이다.
- **곁가지 — 계획서를 쓰는 행위가 자기 근거를 지웠다.** 위 ②의 고아 둘을 계획서와 이 기록에
  적은 직후 다시 재니 **고아 0** 이다(포인터가 `plan_docs-record-guard.md` 와
  `history_current.md` 로 생겼다). 둘 다 임시다 — 계획이 DONE 되면 계획서는
  `plan_history_031.md`(아카이브)가 되고 이 기록은 다음 회전에 밀린다. ①의
  "신고문이 신고 대상을 초록으로 만든다" 가 **실시간으로 재현된 것**이고, 계획서
  갈림길 A 의 대안 3(살아있는 문서 어디든)을 실측으로 죽였다.
- 집안일: **회전을 돌렸다.** `history_current` 이 기록을 붙여 **306줄**이 되자 상한 300 을
  넘긴 그 자리에서 바로 계획 44 여섯 반복을 `history_022.md` 로 밀었다(306 → 107줄).
  `digest.md` 는 **200줄 유지** — 새 줄을 안 만들고 기존 `history_021.md` 회전 줄에 이어
  붙여 **완료 항목을 하나도 안 지웠다**(새 색인 구멍 **0** · 고아 **0** 재확인).
  `## 반복 실패` 가 네 번 적은 *"회전이 늦는다"* 를 안 되풀이했고 **계획 phase 가 자기 손으로
  돌린 첫 회전**이다.
- 다음: **설계 phase.** `design.md` 1절 *"대안이 2개 이상 갈림"* 에 걸린다 — 갈림길 둘
  (검사 범위를 어디로 자르나 · 색인이 어디에 사나). `digest.md` 가 **정확히 200줄**이라
  색인 완전성과 압축 상한이 정면으로 부딪히는 것이 본체다.

## 2026-09-02 00:15 | docs-record-guard | 설계 | 시도0

- 한 일: `docs/design_docs-record-guard.md` 를 썼다. 계획서 5절의 갈림길 둘을 닫는다.
  **① 범위 = `digest.md` 의 `## 완료` 절만.** 전체를 보면 구멍이 4개인데 001 을 초록으로
  만드는 것이 **구멍을 신고하는 그 문장 자신**(`## 반복 실패` 의 *"회전을 돌리면서 그
  아카이브의 `digest` 압축 줄을 안 남긴다"* 항목)이다 — 거짓 초록 계열의 네 번째다.
  절만 보면 정직하게 **5개**. 대안 2 의 값싼 위험(절 이름이 바뀌면 빈 텍스트 위에서
  조용히 통과)은 이 파일이 이미 두 번 쓴 수법으로 막는다 — **절을 못 찾으면 실패**.
  **② 색인 = `## 완료` 절 머리의 "아카이브 명부" 한 줄.**
- **설계의 본체는 두 룰의 정면 충돌이었다.** `rules/docs.md` 는 *"200줄을 넘으면 완료
  항목부터 지운다"*, 새 불변량은 *"모든 아카이브 이름이 완료 절에 있어야 한다"* —
  완료 항목을 지우면 색인 구멍이 생긴다. 실제로 **003 이 그렇게 사라졌다**(계획 44
  테스트 반복이 상한을 맞추려 지운 회전 안내가 유일한 003 포인터였다).
  **역할을 쪼개 풀었다**: 이름 색인은 명부 1줄, 판단 재료 요약은 완료 항목들. 분리되면
  완료 항목 삭제가 다시 안전하다. 명부는 회전 1회당 **약 18자**만 늘고 줄 수는 영원히 1.
  넣을 1줄은 가장 오래된 완료 항목 1줄을 지워 만드는데, 실측으로 그 두 항목
  (`cooldown-burn`(011)·`normalize-gaps`(019))은 **맨 아카이브 이름을 하나도 안 갖고
  있어** 삭제가 공짜다. 기각한 대안 셋: 회전 줄에 이어 붙이기(이름이 어느 줄에 사는지가
  암묵이라 003 을 잃은 경로 그대로) · `index.md` 로 옮기기(`digest` 가 색인을 자처하는
  계약을 깬다) · `rules/docs.md` 수정(**이 저장소 밖**이다 — 대신 명부 규약을 `digest.md`
  자신의 머리 주석에 적어 저장소 안에서 자족하게 한다).
- **설계 탐침이 계획서 실측을 두 군데 확대했다**(전부 메모리 · 저장소 무수정):
  ① **대안 3(살아있는 문서 어디든)이 완전히 죽었다** — 오늘 구멍 0 인데 002·005 를
  이름으로 붙드는 자리가 `index.md` 의 계획 45 행 · `history_current.md` 의 이번 기록 ·
  `status.md` 셋뿐이고 **셋 다 이 계획이 직접 쓴 문장**이다. 계획서가 "임시 포인터 둘" 로
  적은 것을 셋으로 늘려 다시 확인한 셈이다.
  ② **접두 오인 자리가 8곳**이다(`digest.md` 3 · `history_current.md` 1 · `index.md` 4).
  가장 뚜렷한 것은 **`index.md` 의 `history_022.md`** — 접두 1회 · 맨 이름 **0회**라
  순진 매칭이면 **오늘 이미 거짓으로 잡고 있다.** 잠복이 아니라 현행이다.
- 결과: 설계서 4절에 두 검사의 계약(값 추출 정규식 · 눈먼 자리 차단 4개)과 5절에 변이
  **V1~V8** 을 박았다. **V7(명부의 `history_019.md` → `plan_history_019.md`)이 이 설계의
  값을 재는 변이다** — 오늘은 순진/경계 두 매칭의 구멍 목록이 우연히 같아서, 이 변이가
  없으면 경계 매칭을 넣을 이유를 아무도 못 본다(계획 42 의 *"데이터가 초록일 때 린트형
  검사는 자신을 못 잰다"* 그 자리).
  맨몸 실측 `PYTHONPATH=src python3 -m unittest discover tests` **475건 OK · 11.927초** ·
  `src/` **0줄** · 의존성 0 · `data/crawl.db` sha256 무변경.
- **러너 파이프 0 — 맨몸 1회.** 직전 반복이 `2>&1 | tail -3` 으로 **빨강을 가린** 바로
  다음 반복이라, 설계서를 다 쓴 뒤의 재확인도 파이프 없이 돌렸다. 방아쇠였던 *"이미
  초록일 것 같은 실행"* 이 정확히 이 자리였다. 연속 카운트 1.
- **계획 42 의 검사가 이번엔 조용했다.** 설계서를 쓸 때부터 `digest.md`·`index.md`·
  `history_current.md` 를 **항목 이름으로만** 가리켰다 — 직전 반복은 같은 자리에서
  두 곳을 잡혔다. 규칙을 알고 쓰면 처음부터 초록이라는 것을 한 번 확인했다.
- 다음: **개발 스텝 1** — `tests/test_docs.py` 에 `IterationSyncTest`. 설계서 4절 표가
  계약이고 변이 V1~V4 를 눈으로 RED 로 본 뒤 되돌린다. 스텝 2 는 의존이 없어 순서를
  바꿔도 된다.

## 2026-09-02 00:25 | docs-record-guard | 개발 1/2 | 시도0

- 한 일: `tests/test_docs.py` 에 `IterationSyncTest` **한 메서드 24줄**(docstring 포함).
  `metrics.md` 의 `^\| 반복 \| ([0-9]+) \|` 와 `status.md` 의 `^iteration: ([0-9]+)$`
  를 `re.M` 으로 뽑아 같은지 본다. 설계서 4절 표 그대로다. 제품 `src/` **0줄** ·
  새 파일 0 · 의존성 0. 곁가지로 `README.md` 의 단위 수 475 → 476 **한 줄**.
- **TDD 순서를 실제로 밟았다 — 오늘 데이터가 이미 초록이라 변이로 RED 를 만들었다.**
  검사는 린트형이고 `반복`·`iteration` 은 지금 둘 다 229 라, 그냥 쓰면 초록에서
  출발한다(계획 42 가 *"데이터가 초록일 때 린트형 검사는 자신을 못 잰다"* 고 적은
  그 자리). 그래서 **테스트를 먼저 쓰고, 통과를 확인하기 전에 변이 넷을 심어 빨강을
  눈으로 본 뒤** 되돌리고 초록을 봤다. 네 메시지 전부 실물로 읽었다:
  - **V1** `metrics.md` 반복 229 → 230 → `'230' != '229'` · *반복 번호가 어긋났다 —
    metrics.md `반복` 230 ≠ status.md `iteration` 229*
  - **V2** `status.md` iteration 229 → 228 → `'229' != '228'` · 같은 문구의 반대편
  - **V3** `metrics.md` 표 머리를 `| 반복 회차 |` 로 → `unexpectedly None : metrics.md
    에서 | 반복 | <수> | 행을 못 찾았다` — **조용한 통과가 막혔다**(눈먼 자리 확인)
  - **V4** `status.md` 의 `iteration` 줄 삭제 → `unexpectedly None : status.md 에서
    iteration: <수> 줄을 못 찾았다`
  V3·V4 가 이 검사의 값이다. 값 비교만 있으면 `None == None` 위에서 초록이 된다.
- **`.mutation-lock` 을 걸고 했고 되돌림을 실측했다** — 변이 넷은 전부 제자리 편집이라
  자동 스냅샷이 커밋할 위험이 있는 자리다(`digest` 가 5회 재발로 적어 둔 것). 넷을
  되돌린 뒤 `git status --short` 가 `docs/` 에 대해 **완전히 비었고**(바이트 단위 복원),
  락 파일을 지웠다. 되돌리기를 `&&` 로 잇지 않았다.
- 결과: 맨몸 실측 `PYTHONPATH=src python3 -m unittest discover -s tests`
  **475건 OK 11.968초 → 476건 OK 12.010초** · `data/crawl.db` sha256 `85c96744…`
  **무변경** · `git diff --stat` = `README.md` 1줄 · `tests/test_docs.py` 24줄, 그뿐.
- **README 카운트 가드가 하루 만에 또 값을 냈다.** 테스트 +1 을 넣자
  `test_verification_counts_match_reality` 가 `(475, 19) != (476, 19)` 로 즉시 울었다 —
  **자기 발등을 찍은 것이 아니라 남이 심은 가드에 정직하게 걸린 것**이고, 이번 계획이
  하려는 일(사람 규율을 스위트로 옮기기)이 왜 값이 있는지를 같은 반복 안에서 보여줬다.
- **자기 발등은 따로 확인했다.** 이 반복의 기록이 `반복`·`iteration` 을 **둘 다 230**
  으로 올리므로 새 검사가 자기 기록에 즉시 걸린다. 두 곳을 함께 올려 초록을 확인했다.
- **러너 파이프 0회 — 연속 2회.** 여섯 번 다 맨몸이다(전체 2회 · 단일 클래스 4회).
  파이프·`grep`·`head`·`tail`·리다이렉션 없이 돌렸다.
- 다음: **개발 스텝 2** — `ArchiveIndexTest` + `digest.md ## 완료` 절 머리의 아카이브
  명부 한 줄. 의존은 없다. 설계서 4절 표가 계약이고 변이 V5~V8 을 눈으로 RED 로 본다.
  `digest.md` 가 정확히 200줄이라 아카이브 이름이 없는 가장 오래된 완료 항목을 지워
  1줄을 비우고 넣는다. **V7(`history_019.md` → `plan_history_019.md`)이 설계가 재려는
  변이**다 — 순진한 substring 이면 살아남는다.

## 2026-09-02 00:40 | docs-record-guard | 개발 2/2 | 시도0

- 한 일: `tests/test_docs.py` 에 `ArchiveIndexTest` **34줄**(다섯째 검사) — 아카이브
  `sorted(DOCS.glob("history_[0-9]*.md"))` 전부가 `digest.md` 의 `## 완료` 절에
  **경계 매칭 `(?<![A-Za-z_])` + `re.escape(이름)`** 으로 있는가. 절은 `## 완료` 줄부터
  다음 `^## ` 직전까지 잘라 쓴다. 그리고 `docs/digest.md` 의 그 절 머리에 **아카이브
  명부 한 줄**(`history_001.md`~`history_022.md` 22개). 제품 `src/` **0줄**.
- **RED 를 먼저 봤다** — 검사만 넣고 돌리니 설계 기준선 그대로 **구멍 5개**
  (`history_001.md`·`002`·`003`·`005`·`019`)가 실패 메시지에 이름으로 찍혔다.
  그 뒤 명부를 넣어 초록. TDD 1~4 를 순서대로 밟았다.
- **200줄 상한은 순증 0 으로 지켰다.** 명부 1줄이 **가장 오래된 완료 항목**
  (`plan_cooldown-burn` (011) · 2026-08-27)의 자리를 그대로 차지한다. 그 항목은
  아카이브 이름을 하나도 안 갖고 있어 삭제가 새 구멍을 안 만들고(설계 3절 실측),
  내용은 `index.md` 의 `cooldown-burn` 행이 그대로 갖고 있다. `digest.md` **200줄 유지**.
- **명부를 회전에서 지키는 규약도 순증 0 으로 넣었다** — 머리 주석의 "상한 200줄…"
  줄을 제자리에서 고쳐 *"보류·반복실패와 `## 완료` 첫 줄의 아카이브 명부는 남긴다 —
  회전은 새 `history_<NNN>.md` 이름을 명부에 더한다"* 로. 설계가 대안 4 를 기각하며
  적은 *"명부 규약을 `digest.md` 자신의 머리 주석에 적어 저장소 안에서 자족하게 한다"*
  를 줄을 안 늘리고 이행했다.
- **변이 V5~V8 넷 다 RED 를 눈으로 봤다**(`.mutation-lock` 걸고, 되돌림 확인).
  - **V5** 명부에서 `history_002.md` 삭제 → `[] != ['history_002.md']`
  - **V6** 절 이름을 `## 완료된 것` 으로 → `'## 완료' not found in [...]` (눈먼 자리 b)
  - **V7** 명부의 `history_019.md` → `plan_history_019.md` → **RED.** 같은 변이에서
    순진한 substring 은 구멍 0 으로 **살아남는다**(같은 파일 안에서 대조 실측).
    설계가 이 검사를 산 이유가 여기 하나뿐이고, 실제로 그 하나가 값을 냈다.
  - **V8** `DOCS` 를 없는 경로로 → `[] is not true : 아카이브를 못 찾았다` (a·c)
- **거짓 초록을 하나 더 밟았고 그 자리가 변이 되돌림 검증 자체였다.** V8 되돌린 뒤
  스위트가 계속 빨갰다: 소스는 `docs` 인데 런타임 `DOCS` 는 `nope` 였다. 원인은
  이 맥의 시스템 `python3`(3.9)이 `sys.pycache_prefix` 를
  `~/Library/Caches/com.apple.python` 으로 잡는 것 — **저장소 안에 `__pycache__` 가
  아예 없어서 지워도 소용이 없다.** 변이와 되돌림이 같은 초에 일어나고 `"docs"`↔`"nope"`
  가 **같은 바이트 수**라 pyc 무효화 조건(mtime 초 + 크기)이 둘 다 안 걸렸다.
  **sha256 이 원본과 일치하는데도 실행은 변이본이었다** — 파일 해시로 되돌림을 증명하는
  기존 절차가 못 잡는 자리다. 처방: 테스트 파일 제자리 변이는 **`python3 -B`** 로 돌리고,
  이상하면 `sys.pycache_prefix` 아래 저장소 경로를 지운다. 재실측은 그렇게 다시 했다.
- 결과: 맨몸 실측 `PYTHONPATH=src python3 -m unittest discover tests`
  **476건 OK → 477건 OK 11.790초** · `data/crawl.db` sha256 `85c96744…` **무변경** ·
  의존성 0 · `git diff --stat` = `tests/test_docs.py` 34줄 · `docs/digest.md` 2줄 ·
  `README.md` 1줄.
- **README 카운트 가드가 이틀 연속 울었다** — `(476, 19) != (477, 19)`, 477 로 정정.
- 다음: **테스트 phase.** 설계 5절 변이표는 소진했으니 표 밖을 찾는다 — 이름이 명부
  아닌 곳(회전 줄·`## 반복 실패`)에만 있을 때 · 절이 두 번 나올 때 · 회전이 새 아카이브를
  만들고 명부를 안 고칠 때. `.mutation-lock` 과 `python3 -B` 를 함께 쓴다.

