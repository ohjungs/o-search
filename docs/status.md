---
signal: GREEN
phase: 개발
step: 8
attempt: 0
iteration: 266
updated: 2026-09-02
ctx: 62
night_iterations: 111
night_red: 1
night_retries: 0
plan: passage-api
---

# 현재 상태

**리뷰가 계획 48 을 네 번째로 반려한다 — 이번엔 코드가 아니라 설계 전제가 틀렸다.**
활성 계획 `passage-api` · 설계서 `docs/design_passage-api.md` · 브랜치 `loop/passage-api` ·
기점 `6e99031`. **단위 565건 전부 초록**(개발 7 의 564 + 이번 리뷰가 심은 1) ·
`e2e` 문단 경로 5종 rc 0 · 제품 `src/` **0줄**(리뷰는 테스트·문서만 고쳤다).

## 이번 반복이 한 것 (리뷰 4 — 반려)

**① 재확인 넷은 전부 통과했다 — 개발 7 의 주장은 실측으로 참이다.**

| 잰 것 | 결과 |
|---|---|
| `close()` 의 `rawdata` 규칙 (15모양 실측) | **옳다.** `<` 로 시작하는 잔여물은 예외 없이 마크업 내부(주석·선언·PI·CDATA·속성값·미완성 태그)고, 평문 잔여물(잘린 엔티티)은 `<` 로 시작하지 않는다. `script`·`style` 은 `cdata_elem` 이 세워져 `goahead(1)` 이 **애초에 안 흘린다** |
| 색인 경로 불변 | **바이트 동일.** `extract.py` 는 삭제 0줄 순수 추가고, 코퍼스 **3,424건**(실물 + 퍼즈 3천)에서 `extract_text`·`is_noindex` 불일치 **0건** |
| 불변식 `" ".join(blocks) == body` | 3,424건 중 **정상 입력에서 갈린 것 0건**. 갈린 1,836건은 전부 파서 잔여물 또는 제어문자 — docstring 의 예외 ①② 그대로다 |
| `/search` 몫 판정 | **옳다.** `search`·`_connect`·`_bigrams`·`_page_hits`·`_parse`·`_results`·`_send_html` 일곱이 기점과 **바이트 동일**이라 그 비용은 물려받은 것이다 → `digest` 후보로 등재해 닫았다 |

**② 변이 넷 중 셋은 죽고 하나가 살아남았다** (`.mutation-lock` 아래 · 전부 원복 · `git diff -- src/` 0줄):

| 변이 | 무엇 | 결과 |
|---|---|---|
| Y1 넓힘 | 꼬리를 안 버린다 | **4건 죽음** — 개발 7 의 주장 그대로 |
| Y2 좁힘 | 꼬리를 무조건 버린다 | **1건 죽음** — 잘린 엔티티 |
| Y3 | 동점이면 뒤 블록이 이긴다 (`>` → `>=`) | **1건 죽음** — 계약의 동점 규칙은 고정돼 있다 |
| **Y4** | **500 로그가 경로를 안 찍는다**(개발 7 이 고친 그 줄을 통째로 되돌린다) | **565건 전부 초록 — 살아남았다** |

Y4 가 산 이유는 기존 단언이 `"search 실패"` 라 **`"/search 실패"` 의 부분문자열**이어서다.
개발 7 이 그 줄을 고친 이유(«어느 문이 열렸는지가 500 의 유일한 흔적에 있어야 한다»)를
재는 단언이 **0건**이었다. 자동 수정으로 닫았다 — 앞의 `/` 까지 단언하고,
`/passages` 가 터졌을 때 `/search` 라고 안 적는지를 재는 테스트를 하나 심었다(**565건**).

**③ 반려 사유 — 「블록」이 「문단」이 아니다** (설계서 «갈림길 6» 에 적었다).
갈림길 1 의 전제 *"부모가 블록 태그마다 그 자리를 이미 안다"* 가 **절반만 참이다**.
`extract.py:7` 이 `<br>` 을 *"줄바꿈이므로 일부러"* `_INLINE_TAGS` 에서 뺐기 때문에
`<br>` 도 `_separate()` 를 부른다 — 색인엔 **공백 하나**, 블록엔 **문단 경계**다.

| 입력 | 나오는 근거 |
|---|---|
| `<nav><a>김치찌개</a></nav><article><p>김치찌개 만드는 순서를…</p></article>` | **`'김치찌개'`**(4자) — 동점이고 내비가 항상 앞이라 «먼저 나온 것» 이 이긴다 |
| `<p>오늘은<br>김치찌개<br>내일은 된장찌개를 끓인다</p>` | **`'김치찌개'`**(4자) — `<br>` 이 만든 블록이 **점수로** 이긴다(동점이 아니다) |

**④ 이 결함을 잡을 수 있는 측정기가 저장소에 하나도 없다 — 코퍼스가 마크업이 아니라 평문이라서다.**
`e2e/quality/corpus.json` 64건은 `url·lang·title·body` 뿐이고 `passage_eval.build_index`
가 **문장마다 `<p>` 하나**로 감싸 HTML 을 만든다. 그래서 코퍼스에 `<br>` **0건** ·
`nav`/`ul`/`li`/`h*` **0건**, 블록 최소 길이 **129자**, 실물 근거 399건 중 20자 미만
**0건**이다. 정확도 100% · 채택률 99.5% 는 **한 문장짜리 `<p>` 위에서만** 잰 값이고,
`data/crawl.db` 도 2문서 × 66자라 실물이 이 자리를 덮은 적이 없다.
**개발 8 은 손잡이를 고르기 전에 잴 자부터 정한다.**

**⑤ 백지 패스가 처음으로 phase 결과를 바꿨다.** 지적 6건 → 확증 3 · 기각 3 · 고친 줄 0.
확증 둘(위 ③)이 반려의 근거다. 기각 셋은 결이 앞 네 측정과 같다 — 성능 지적은 **다섯 번
중 다섯 번** 틀렸고(이번 것은 설계서가 이미 적어 둔 `/search` 몫의 기보류 중복),
«캡을 바꾸면 `position` 이 조용히 바뀐다» 는 **재 보니 없었다**(잘림은 뒤만 지우므로 앞
블록 번호는 cap 35,000·200·120·60 에서 전부 같다). 나머지는 주석 밀도·변수명이라 억제 항목이다.

**⑥ 보안 훑기 — 새로 연 구멍은 없고, 물려받은 것 하나를 등재했다.**
`/passages` 는 JSON 전용(`json.dumps` · `nosniff`)이고 화면 경로는 이 diff 를 안 지나
**XSS 표면이 0** 이다. `SELECT html FROM pages WHERE url = ?` 는 파라미터 바인딩이다.
다만 `<template>`·`aria-hidden`·`display:none`·`hidden`·0px 폰트의 **안 보이는 텍스트가
근거 문단으로 나온다**(실측). 색인에는 원래 있던 것이라 이 diff 의 결함은 아니지만,
**고르는 규칙이 밀도를 적극 선호해 키워드를 채운 숨은 블록이 근거 자리를 확정적으로 이긴다** —
`_SKIP_TAGS` 는 `_TextParser` 와 공유라 고치면 재색인이므로 `digest` 후보로 넘겼다.

**⑦ 회전은 돌렸지만 상한을 못 맞췄다.** 개발 7 을 `docs/history_041.md` 로 밀어
343 → 309줄(스물한 회전 연속 지각 0). **항목을 다 밀어도 300 아래로 안 내려간다** —
머리의 회전 명부가 233줄이다. 아래 «사람이 정할 것» 5번으로 올렸다.

## 넘긴 것 — 개발 8 이 볼 것

1. **먼저 고를 것은 손잡이가 아니라 코퍼스다** (설계서 갈림길 6). 세 안(동점 키에 길이 ·
   `_BlockParser` 에서 `br` 만 경계에서 빼기 · 블록 최소 길이)은 무엇을 골라도 **나은지 잴
   자가 지금 없다**. `<br>`·`nav`·목록이 든 fixture 를 `passage_eval` 에 넣는 것이 먼저다.
2. **동점 규칙만 고치면 절반만 닫힌다** — `<br>` 쪽은 동점이 아니라 점수로 이긴 것이다.
   뿌리는 `_separate()` 를 «블록 태그» 로 좁히는 쪽에 있다.
3. **자동 수정 둘은 이미 들어갔다** — 500 로그 단언(테스트 +1, 565건)과 설계서 계약 절의
   낡은 한 줄(개발 6 의 폐기된 되감기 규칙을 아직 적고 있었다).
4. `e2e` 는 문단 경로 5종만 돌렸다(`passage_eval` · `quality_eval` · `search_api_e2e` ·
   `indexer_e2e` · `design_check`). **20종 전수는 e2e phase 의 일이다.**

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-02 확인)

`origin/main` 은 PR #6 으로 계획 44~47 까지 받았다. 계획 48 의 커밋들은 브랜치에만 있다.
**이번 반복도 `origin/loop/passage-api` 로 푸시했다** — 푸시는 루프의 정상 절차다
(강제 푸시만 금지).
**남은 것 (사람 결정)**: 계획 48 이 끝난 뒤 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 — 다섯이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, `~/.claude/skills/loop-harness/rules/docs.md` 에는 그 예외가 없다.
   **저장소 밖이라 루프가 못 고친다.**
4. **«러너 호출을 다른 것과 섞지 않는다» 를 문장이 아니라 훅으로 옮기는 일.**
   재발 **19회 — 이번 반복이 다시 밟았다**(Y2 변이 아래 러너를 `2>&1 | tail -20` 으로 돌렸다.
   판정 줄은 얻었지만 그 형태가 정확히 금지된 것이라 **변이를 심어 둔 채 맨몸으로 다시 돌렸다** —
   결과는 같았다). 0 으로 끝낸 반복이 열 번 쌓인 직후에 났다는 것이 이 항목의 요지다:
   **연속 기록은 문장이 훅을 대신한다는 증거가 아니다.** 저장소 쪽 자리(`docs/project.md ## 명령`)에 이미 적혀 있는데도 났고,
   남은 자리는 `~/.claude/skills/loop-harness/rules/` 라 **저장소 밖이라 루프가 못 고친다.**
   0 으로 끝낸 반복이 쌓이는 것이 항목을 닫지는 않는다 — 닫으려면
   **판정 명령을 단독 실행으로 강제하는 PreToolUse 검사**가 필요하다.

5. **`history_current.md` 를 회전으로는 더 이상 300줄 아래로 못 내린다.**
   이번 회전(개발 7 → `docs/history_041.md`)이 상한을 못 맞춘 **첫 사례**다 — 343 → 309줄이고,
   **항목을 하나도 안 남기고 다 밀어도** 머리의 회전 명부가 **233줄**이라 안 내려간다.
   스물한 번의 회전 서술이 그 자리에 쌓였고 내용은 `digest.md ## 완료` 의 회전 줄과 겹치는데,
   **검사(`ArchiveIndexTest`)가 강제하는 명부는 `digest.md` 의 `## 완료` 절 «아카이브 명부» 줄 하나뿐**이다.
   그 파일은 룰이 **append 전용**이라 루프가 못 줄인다 — 줄이려면 사람이 룰을 고치거나
   («명부는 `digest.md` 가 갖고 `history_current.md` 는 항목만 남긴다») 계획으로 열어야 한다.

**사양이 사람에게 남긴 것 셋** (`docs/specs/concept.md` 의 `## 사람이 정할 것`):
속도 제한 시점(가정 — IP 당 분당 60회) · JSON 검색과 근거 문단의 분할(가정 — 쪼갠다,
**계획 46 이 그 가정을 따랐고 닫았다**) · 사양 숫자들이 초안이라는 것(90% · 500ms ·
60회/분). 첫째와 셋째는 아직 열려 있다.
