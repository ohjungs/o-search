# 기록 (현재)

### 반복 584 — 리뷰 스텝 · 두 패스가 하나를 물었고, 기록 상한이 먼저 빨개졌다

- **[R100-1](자동 수정)** 비ASCII 갈래 둘은 `_fetch_robots` 를 **가짜로 안 바꾸고** 진짜
  `urlopen` 을 탄다 — 인코딩에서 먼저 죽는 것이 재는 값이라 그게 맞다. 다만 파이썬이
  호스트를 퓨니코드로 먼저 바꾸게 되면 **같은 테스트가 조용히 DNS 로 나간다**(`project.md`
  한도 위반). `socket.getaddrinfo` 그물을 씌웠고, ASCII 호스트로 **그물이 실제로 무는지**
  까지 대봤다(장식이 아님을 확인).
- **패스 B 는 빈손이고 그게 값이다** — 계약 4(공유 함수 한 곳)를 실물로 다시 쟀다:
  `known_delay` 는 `_delays.get` 뿐이라 네트워크를 안 타고, `delay` 는 `_parser` 를 거쳐
  같은 `except` 로 모인다. 「하지 않을 것」 셋(`urls.normalize` 개조 · 관문 안 퓨니코드 변환 ·
  호출부별 가드)도 diff 에 0줄.
- **전수가 RED 를 냈는데 원인이 diff 가 아니라 기록이다** — `HistoryCapTest`, 항목 21 > 20.
  **줄이 아니라 항목 상한에 걸린 첫 회전**이라 564~573(+「573 뒤」)을 `history_085.md` 로
  밀어냈다(295 → 129줄). 회전 뒤 전수 **784 OK**.
- 제품 `src/` **0줄** · 테스트 수 무변(784).

### 반복 585 — e2e 스텝 · 시나리오 2종 통과 · 계획 100 DONE

- **새 e2e 파일을 안 만들었다** — 같은 축(`non_ascii_e2e`)의 **넷째 시나리오**로 넣었다.
  파일을 더하면 `project.md` 「e2e 18종」과 README 「22종」이 함께 낡는데, 재는 것은 같다.
- **시나리오 1 통과.** 한글 호스트 씨앗은 `urls.normalize` 가 `xn--bj0bj06e.invalid` 로
  바꾼 뒤에야 프런티어에 든다 — **계약 2「도달 불가」의 실물 확인**이다(행 0 · 종료 0 ·
  로컬 3행과 요청 집합 무변). 우회해 관문에 직접 넣으면 `allowed` **False** · `delay` None.
- **그물을 `AssertionError` 로 놓은 것이 핵심이다** — `gaierror` 로 놓으면 관문이 그것을
  599 로 접어 「네트워크를 탔는데도 False」가 조용히 통과한다. 자식 프로세스에도 같은 그물을
  깔았다(죽은 호스트 판정이 이 기계의 DNS 로 나가면 `project.md` 한도 위반이다).
- **시나리오 2(음성 대조) 통과 — 죽는 자리가 둘로 갈렸다.** e2e 는 `rc=1`(`putheader` 의
  latin-1 · 소켓 열기 **전**), 전수는 **errors=2**(`TestNonAsciiHost` 둘)뿐이다. 단위는
  **원인**을, e2e 는 **증상이 사용자 경로까지 나온다**를 말한다. 2건뿐인 것도 측정이다 —
  관문에 비ASCII 를 직접 넣는 갈래가 이 저장소에 그 둘뿐이라는 뜻이다.
- 계획서를 `plan_history_075.md` 로 옮기고 `index.md` 를 완료·통과로 닫았다. 신호 **DONE**.

### 반복 586 — 짧은 경로 · `-b` 가 삼키던 경고를 처음 벗겨 봤다

- **탐색 여덟 출처가 전부 0건이었다** — 전수 784 OK · 코드 `TODO` 0 · 이슈 0 ·
  `candidates.md` 없음 · 보류는 보안 건 하나(밤 금지) · 후보 절은 여는 조건 전부 미충족.
  품질 4축도 전부 통과(ko 20/20 · en 19/20 · 근거 100.0% · p95 8.96ms · 디자인 4축) ·
  **e2e 18종 전부 rc=0**. 빈손으로 닫는 대신 digest `[4]` 가 「영영 안 보인다」고 등재한
  축을 **처음 쟀다** — `-W always` 로 벗기니 `ResourceWarning` **12건**이 나왔다.
- **`DeprecationWarning` 0건이 더 큰 소식이다.** 파이썬 3.9.6 에서 이 저장소가 부르는
  것 중 사라질 예정인 API 가 없다는 뜻이고, 이것은 이 밤 전에 아무도 몰랐다.
- **12건은 전부 한 파일**(`test_merge_script.py:34,39`)의 `open(...).write(...)` 다. 파일
  객체에 이름이 없어 `__del__` 이 닫는다 — CPython 에서는 결과가 같지만 **닫히는 시점이
  GC 에 달려 있고 바로 다음 줄이 그 파일을 `git commit` 으로 읽는다.** 한 줄씩 `write_text`.
- 빨강을 눈으로 보고(12) 고치고 다시 봤다(**0**). 전수 **784 OK** · 제품 `src/` **0줄**.
- **파이프를 두 번 붙였다** — `quality_eval` 과 전수. `project.md` 가 금지한 그것이고
  digest 최다 반복 실패(**16회**)의 열일곱·열여덟 번째다. 둘 다 즉시 맨몸 재실행했다.

### 반복 587 — 재탐색은 빈손이다 · 정지 사유 「탐색 막힘」

- 반복 586 을 `main` 에 넣고(`6a1af58` · 전수 784 OK) digest `[4]` 의 여는 조건을
  「열렸고 같은 반복이 닫았다」로 갱신했다 — 12건 → 0건 · `DeprecationWarning` 0건.
- **여덟 출처 재탐색이 전부 0건이다.** 새로 한 것은 **등재된 여는 조건 셋을 실물에
  대 본 것**이다 — e2e 도우미 0건(22/22 진입점) · `fetcher.py:52` 는 여전히 non-2xx
  본문을 `None` 으로 버린다(신규 색인 루프가 `status` 를 안 보는 자리가 안 열렸다) ·
  제품 CSS 에 괄호 셀렉터 0곳(정적 CSS 파일 자체가 없다). **셋 다 아직 닫혀 있다.**
- 확인은 값이 있다 — 「안 열었다」와 「안 봤다」는 다르고, 등재 항목들은 열리는 날을
  지목해 두었지 **그날이 왔는지 재는 자는 없다.**
- `discover.md` 2절대로 상상해서 계획을 만들지 않고 **YELLOW 로 멈춘다.** 이 밤의
  성과는 계획 1건(짧은 경로 `test-file-leak`)이다.

### 반복 588 — 계획·설계 스텝 · 앞 밤이 닫은 자리에서 근거를 찾았다

- **여덟 출처는 이번에도 0건이다** — 전수 784 OK · 코드 `TODO` 0(유일한 `test_indexer.py:1027`
  은 테스트 픽스처 문자열) · 이슈 0 · `candidates.md` 없음 · 보류는 보안 건 하나(밤 금지).
- **다른 것은 등재 항목을 다시 읽은 것이다.** digest `[4]` 는 반복 586 이 「단위 쪽은
  닫혔다」로 갱신해 뒀는데, 그 항목이 말하는 축의 **e2e 절반은 아무도 안 쟀다.**
  「안 열었다」와 「안 봤다」가 다르다는 앞 반복의 말이 이번엔 계획을 냈다.
- **뚜껑이 둘이었다.** e2e 는 `-b` 를 안 쓰는데도 경고가 안 보인다 — 파이썬이
  `ResourceWarning` 을 **기본으로 무시**한다. 반복 586 이 연 것은 둘 중 하나였다.
- 벗겨 재니 **30건 · 18종 중 16종**. **전부 rc=0 이다** — 종료 코드는 누수를 본 적이 없다.
- `tracemalloc` 으로 할당 지점을 받아 원인을 둘로 갈랐다(소켓 24 · 파이프 6). 설계는
  대안 셋을 비교해 **A(진입점마다 한 줄)** 를 골랐다 — 저장소가 이미 셋에서 그 패턴이다.

### 반복 589 — 개발 스텝 1 (가드) · 내가 만든 가드가 거짓 초록이었다

- TDD 로 `tests/test_e2e_fd.py` 를 먼저 세웠다. **첫 판이 빨갛지 않고 초록이었다** —
  `\bHTTPServer\(` 가 `ThreadingHTTPServer` 안에서 안 맞는다(`g`·`H` 가 둘 다 낱말 문자라
  경계가 없다). **0개를 세면서 17파일이 새는 위를 통과했다.**
- 잡은 것은 같이 넣어 둔 `test_the_guard_has_something_to_measure` 다. 없었으면 이 계획은
  **가드를 세웠다고 적고 아무것도 안 재는 자를 남겼을 것**이다. 그래서 그 단언이 남는다.
- 고치니 의도한 빨강이 나왔다 — 서버 17파일 · 파이프 5파일을 **이름으로 지목**한다.
- `test_readme.py` 가 즉시 물었다(단위 784 → 788). README 숫자를 함께 고쳤다.

### 반복 590 — 개발 스텝 2 (소켓 24건 → 0) · 개수 세기를 버렸다

- `shutdown()` 뒤에 `server_close()` 를 18곳에 놓았다. **새 개념 0개** — `design_check.py`
  ·`passage_eval.py`·`perf_search.py` 셋이 이미 그 패턴이었다.
- **가드를 개수 비교에서 있고/없고로 바꿨다.** `deadline_e2e` 는 서버를 루프에서 만들고
  루프에서 닫는다(만든 자리 2 · 닫는 자리 1) — 개수를 요구하면 **옳게 고친 파일이 빨개진다.**
  대리 측정의 정밀도를 올리려다 거짓 빨강을 만들 뻔했고, 실물이 그것을 막았다.
- 실측 `unclosed <socket` **24 → 0** · 18/18 rc=0.

### 반복 591 — 개발 스텝 3·4 (파이프 6건 → 0) · 리뷰 · e2e · 계획 101 DONE

- `wait()` 뒤에 `stdout.close()` 를 여섯 자리에 놓았다. **자식이 끝난 뒤에** 닫아야
  종료 중인 자식이 EPIPE 를 안 맞는다. `terminate()`+`wait()` 는 `communicate()` 와 다르다.
- **완료 측정: `ResourceWarning` 30 → 0 · `DeprecationWarning` 0 · 18/18 rc=0 · 단위 788 OK.**
  품질 4축 회귀 없음(ko 20/20 · en 19/20 · 근거 100.0% · p95 8.57ms · 디자인 4축).
- **명부 밖 넷을 더 쟀다** — `project.md` 의 「18종」은 측정 도구 넷을 안 센다. 그 넷도 같은
  종료 경로를 쓰고 `perf_crawl` 은 실제로 새던 17파일 중 하나였다. **22/22 · RW 0 · DW 0.**
- **변이 둘로 가드의 배선을 확인했다** — 심은 뒤 **실제로 지워졌는지 먼저 단언**하고 돌렸다.
  둘 다 잡혔고 되돌린 뒤 `git status --short` 빈 출력.
- `deadline_e2e` 간헐은 **등재분 그대로**(3판 중 1실패 · 간격 0.916초 · 기록된 0.932·0.949
  ·0.942 와 같은 단언 같은 자리). 그 파일의 변경은 시나리오가 다 끝난 뒤 `finally` 의 한
  줄이라 시나리오 2 의 간격 측정에 닿지 않는다. **고치지 않고 재기만 했다.**

### 반복 592 — 마감 · 게이트 ⑫ 의 값을 재다 자기 오등재를 되받았다
- 계획 101 이 `main` 에 들어간 뒤 「`-W always` 를 상시로 달면 얼마인가」를 쟀다. **답: 공짜다.**
  같은 조건 연달아 두 판 — 평소 `-b` **21.229초** / `-W always` **20.877초** · 둘 다 `rc=0` · 788 OK · 26% cpu.
  `-W` 쪽 경고는 **0건**이라 두 로그 차이가 「Ran 788 tests in …」 숫자 한 줄뿐이다.
- **먼저 읽은 숫자는 전부 틀렸다.** 「16분 18초 · 0% cpu」를 세 번 읽고 「`-W` 가 48배 느리다」로 등재했다가,
  `-W` **없는** 평소 전수가 같은 자리에서 또 16분을 찍는 것을 보고 뒤집었다 — 값은 명령이 아니라 **셸이 멈춘 시간**이었다.
- 그동안 unittest 자신의 타이머는 세 판 내내 **20~21초**를 찍고 있었다. 그것을 1순위로 읽었으면 첫 판에 끝났다.
- `digest.md`: 후보 `[7]` 을 사실대로 교체하고, **벽시계 함정**을 반복 실패(3회)에 등재했다. 코드 변경 0줄.

### 반복 593 — 빈손 밤 · 「테스트 순서 의존」 축을 처음 쟀다

- **여덟 출처 0건**(전수 788 OK · `TODO` 0 · 이슈 0 · 큐 없음 · 보류는 보안 건). digest 후보에
  남은 것은 **사람 판단 · 이미 닫힌 것 · 저장소 밖** 셋뿐이라 **계획을 만들지 않았다**.
- `loaf`←`loaves` `[6]` 을 열려다 접었다 — 스테머가 **FTS5 내장 `porter`** 라 우리 코드가 아니고,
  여는 길이 질의 확장 + 불규칙 복수 사전(설계 갈림 · 랭킹 경로)이며 합격선은 이미 넘어 있다.
- **쟀다**: 788건을 평평하게 펴 씨앗 1·2·3 으로 섞어 세 판 — **전부 788 OK**(20.5·20.5·20.7초).
  섞임을 먼저 단언했다(같은 자리 0~1/788). digest `[5]` 의 「잠복」에 값으로 등재. 코드 **0줄**.

### 반복 594 — 계획 phase · 빈손 두 밤을 컨셉으로 끊었다 · 계획 102 `serve-rss`

- **여덟 출처는 이번에도 전부 0건이다**(전수 **788 OK** · 코드 `TODO` 0 — 유일한 히트
  `tests/test_indexer.py:1027` 은 픽스처 본문이다 · 이슈 0 · `candidates.md` 없음 ·
  보류는 보안 건 하나). digest 후보도 앞 밤 판정이 맞았다 — 열어 본 것들이 **여는 조건**을
  스스로 적어 뒀고 그날이 아직 아니다(`[4]` `is_noindex` 자르기는 `<body>` 안 `meta` 를
  놓치는 **미탐**이라 우선순위 최상단 축을 깎는 거래). `[6]` 143 은 **이미 닫혀 있었다** —
  계획 99 의 `E2E_LABEL_ITEM` 이 라벨과 명부 길이를 묶었다(낡은 후보였다).
- **끊은 길은 계획 77 이 쓴 그 길이다** — 여덟 출처가 막힌 밤엔 `docs/specs/concept.md`
  를 자로 댄다. 대니 **경량 2** 가 나왔다: 「검색 서버 상주 메모리 2GB 이하(판정: RSS 측정)」
  인데 **이 저장소에 RSS 를 재는 코드가 0줄**이고 `project.md` 품질 기준 표 다섯 줄에도 없다.
  컨셉이 **판정 방법까지 적어 둔 축**을 아무도 안 돌린다 — digest `[8]` 의 「측정 하네스가
  재지 않는 세계가 곧 버그가 숨는 곳」이 그대로 성립하는 자리다.
- **위험은 «오늘 크다» 가 아니라 «색인을 따라 큰다» 다.** SQLite 는 디스크에 사니 RSS 는
  평평해야 정상이고, 평평하지 않으면 2GB 천장이 규모에 비례해 온다 — 그리고 그 사고는
  100만 문서에 닿는 날 **처음** 보인다. 「2단계까지 단일 머신」 전제가 이 축 위에 서 있다.
- **설계는 다음 반복으로 넘긴다** — 트리거 둘(새 파일 · 대안 2개 이상)에 걸린다. 다만
  갈래는 계획서에 이미 적었다: `ru_maxrss` 는 **peak 이지 상주가 아니고** 빌드 할당이 섞이며,
  `perf_search.py` 에 세 줄 더하는 재사용 길은 그쪽이 서버를 **같은 프로세스 스레드**로
  띄워 같은 오염을 물려받는다. 남는 길은 **진짜 진입점을 자식으로 띄우고 밖에서 `ps`** 다.
- **기록 회전**: append 가 항목 21을 만들기 직전이라 574~583 을 `history_086.md` 로
  밀어냈다(254줄 20항목 → 126줄 10항목). 코드 **0줄**.

### 반복 595 — 설계 phase · 갈래 셋 중 C · 탐침이 덤으로 축의 답을 줬다

- **`docs/design_serve-rss.md`**. 버린 둘이 왜 **틀린 숫자**를 내나가 요점이다: `ru_maxrss` 는
  **peak 이지 상주가 아니고** 빌드 할당이 섞인다(실물보다 크게 나와 「2GB 안전」을 뒤집어 읽게
  한다) · `perf_search.py` 재사용은 서버를 **같은 프로세스 스레드**로 띄워 같은 오염을 물려받는다.
  **재사용할 것은 파일이 아니라 함수였다** — `build_index`·`query_paths` 를 임포트한다.
- **탐침(커밋 안 함)으로 가장 위험한 가정을 먼저 깼다** — 자식 `serve` 의 포트와 현재 RSS 가
  밖에서 읽힌다. `--port 0` 의 stdout 첫 줄은 `serve.py:473` 이 **측정 스크립트용으로 이미
  만들어 둔** 자리다.
- **덤으로 축의 답이 나왔다**: 500문서 **17,008 KB** · 4,000문서 **17,008 KB** — 8배 색인에
  띄운 직후 RSS 가 **바이트 단위로 같다**. 질의 뒤 증가분도 960 → 1,152KB(8배에 192KB).
  원인은 코드에 이미 있었다 — `serve.py:222` 가 요청마다 `indexer.search(db_path, …)` 를 불러
  **연결을 상주시키지 않는다**(`serve.py:8` 「3% 라 아낄 것이 없다」).
- **그래서 값은 「오늘의 숫자」가 아니라 「기울기를 지키는 자」다** — 연결을 상주시키거나 캐시를
  얹는 날 이 측정이 평평하지 않게 된다. 그날 전엔 아무도 안 본다는 것이 지금 상태다.
- **리미터는 안 끈다** — 자식이 진짜 진입점이라 `RATE_LIMIT 60`/분이 켜져 있고 `serve.main` 에
  끄는 갈래가 없다. 적게 쏘는 쪽(5종 × 소수 반복)이 실물 그대로다. 코드 **0줄**.

### 반복 596 — 개발 스텝 1·2 · 축이 평평하다는 것이 실측됐다 · 벽시계 함정 5회째

- **`e2e/perf_memory.py` 를 세웠다**(측정 5종째). 자식으로 `python3 -m websearch.serve
  <db> --port 0` 을 띄우고 stdout 첫 줄에서 실제 포트를 읽은 뒤 밖에서 `ps -o rss=` 로
  **현재** RSS 를 읽는다. `build_index`·`query_paths` 는 `perf_search` 에서 임포트했다 —
  색인 만드는 코드를 두 벌로 안 만든다.
- **실측(기준선)**: 500문서 뜬 직후 16.6MB · 질의 뒤 17.7MB / 4,000문서 16.6MB · 17.9MB.
  **뜬 직후끼리 차이 +0.0KB** · 기울기 **0.0503 KB/문서** · 100만 외삽 **66.8MB = 예산
  2,048MB 의 3.3%**. **경량 2 는 여유가 아니라 구조로 지켜지고 있다** — `serve.py:222` 가
  요청마다 DB 를 열어 색인을 상주시키지 않는다.
- **「좋은 숫자」가 「잴 필요 없었다」는 뜻이 아니다.** 값은 회귀 감지다 — 연결을 상주시키거나
  캐시를 얹으면 기울기가 평평하지 않게 된다. 그날 전엔 아무도 안 봤다.
- **새 자가 의미 있게 실패하는지 세 판으로 확인했다**(dev 0절 — 산출물이 검사인 스텝):
  ① 리미터 초과(5종×12=60) → `rc=2` ② 규모 역전 → `rc=2` ③ 소스 변이로 자식을 죽이니
  (`--port 99999`) 「포트 줄을 안 냈다」 → `rc=2`. **셋 다 1 이 아니라 2 다** — 재지 못한
  것을 실패로 안 적는 `project.md` 관용구 그대로다.
- **가드 둘이 제 일을 했다** — 새 파일을 넣자 `E2eRosterTest`(명부에 `perf_memory` 없음)와
  `test_readme`(22 ≠ 23)가 **즉시 빨개졌다**. 등록하고 초록. 전수 **788 OK**(25.1s).
- **벽시계 함정 5회째**: 전수가 400초 상한에 걸렸는데 같은 로그 안에서 unittest 자신은
  **25.135s · rc=0** 을 찍고 있었다. 명령은 성공했고 **셸만 멈췄다** — 기록대로 1순위로
  unittest 타이머를 읽어 첫 판에 끝냈다.
