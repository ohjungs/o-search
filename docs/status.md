---
signal: DONE
phase: e2e
step: 1
attempt: 0
iteration: 227
updated: 2026-09-01
ctx: 55
night_iterations: 87
night_red: 0
night_retries: 0
plan: null
---

# 현재 상태

**계획 44 `focus-ring-presence` DONE — e2e 1/1 완료, 새로 만든 e2e 파일 0개.**
브랜치 `loop/focus-ring-presence` · 결과 `docs/e2e/focus-ring-presence/result.md` ·
계획서 `docs/plan_history_030.md` · 설계서 `docs/design_history_030.md`.
**활성 계획이 없다 — 다음 반복은 계획 phase(탐색)다.**

## 방금 것 (2026-09-01 · e2e 1/1)

**단위가 구조적으로 못 밟던 자리를 e2e 가 밟았다.** 단위는 `check_contrast` 를 **함수로**
부르고 `unmeasurable` 리스트만 본다 — 설계 `## 갈림길 A` 의 결론(`ValueError` → **종료 2**)은
`main()` 의 `return 2` → `sys.exit(main())` 을 거쳐야 참이 되는데 아무도 그 경계를 안 밟았다.
링을 죽인 CSS 를 제품(`src/websearch/serve.py`)에 심고 **진짜 프로세스**로 쟀다.

| 심은 것 | 실측 |
|---|---|
| 기준선(무변경) | **rc 0** · 요약 줄 `포커스 링 규칙 1개 · outline var(--focus) · offset 2px` · 라이트 `--focus` **3.56:1** |
| `outline:3px solid var(--focus)` → `outline:none` | **rc 2** · `측정 불능 1건: outline 이 색 토큰을 안 쓴다 (none)` |
| `outline` 선언 통째 삭제 | **rc 2** · `측정 불능 1건: outline 을 정하는 규칙이 0개 — 캐스케이드를 바이트로 못 정한다` |

**`3.56:1` 이 화면에 없다** — 둘 다 `[3]` 절이 16행 → **14행**(텍스트 7짝 × 2맵)으로 줄고
요약 줄도 사라진다. 무효가 된 숫자를 **안 찍는 것**이 계획의 요구였고 표준출력에서 참이다.
`.mutation-lock` 을 걸고 했고 끝나고 지웠다. 되돌리기는 `&&` 로 안 이었다 —
작업 트리 깨끗 · `design_check.py` **rc 0** 복귀 확인.

**선행 관문 셋**(`rules/e2e.md` 1절): 단위 **475건 OK 11.892초** · 린터·타입체커 **없음**
(`docs/project.md` 가 `없음` 이라 "검증되지 않았다" 로 적는다) · **CI 없음**(`.github` 부재).

**회귀 — 전부 맨몸, 한 번에 하나씩.**

- `e2e/*.py` **19종 전수 rc 0**
- 품질: ko **20/20 (100%)** · en **19/20 (95%)** · 순위 1위 39 · 매치 평균 **14.0**(최소 11)
- 성능: `perf_search` p95 **8.90ms**(예산 300ms 의 3.0%) · `search_api` p95 2.13ms ·
  [열림] **10.22/s** · [차단] **10.22/s**
- `data/crawl.db` sha256 **무변경**(`85c96744…5bda18`) · 제품 `src/` **0줄** · 의존성 0

**새 e2e 0개** — 이 계획이 만든 것은 검사기의 눈 하나이고 걸리는 자리는 "진짜 프로세스의
종료 코드" 하나인데 `design_check.py` **자신이 그 프로세스**다. 위 표가 그것을 CSS 변이로
재면 끝이라 파일을 만들면 19종이 20종이 될 뿐이다(계획 41·42·43 과 같은 판정 · ponytail 1번).

## 다음 반복이 할 것 — 계획 phase (탐색)

`rules/discover.md` 1절 1~8순위 전수. **6순위 우물의 1순위 후보는 이미 서 있다** —
아래 `## 사람이 정할 것` 3번(기록 3파일 불일치)과 `digest ## 반복 실패` 의 아카이브 색인
구멍 5개는 **`tests/test_docs.py` 검사 한 줄로 같은 파일·같은 꼴**이라 한 계획으로 묶인다.
`digest [6]`(살아남는 새 변이 넷)은 승인이 필요하다.

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-01 확인)

`origin/main` 은 `494313b`(PR #5 병합)이고 그 트리는 `bc40ea9` 의 부모와 같다.
**남은 것 (사람 결정)**: `bc40ea9`(반복 221 기록) 와 이 계획의 커밋들을 `main` 으로
보내는 PR. 무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 — 셋이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다(리뷰가 1.2707 을 손으로 재확인).
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, **검사기가 매 실행 offset > 0 을 확인한다**(실측 2px, 요약
   줄에 찍힌다 — 이 반복이 0 이 되는 갈래에서 rc 2 를 실측했다). 짝을 지금 늘리면 rc 1 이라
   안 늘린다. offset 이 0 이 되는 날 검사기가 멈추고, 짝은 그때 열릴 계획의 일이다.
3. **기록 3파일 불일치가 5회째 재발** — 처방은 `tests/test_docs.py` 에 검사 한 줄
   (`metrics.md` 의 누적 반복 수와 `status.md` 의 `iteration` 이 어긋나면 죽는다).
   `digest` 의 아카이브 색인 구멍(5개)과 같은 파일·같은 꼴이라 묶어서 한 계획이다 —
   다음 계획의 1순위 후보. **개발 스텝을 여는 일이라 승인 필요.**

## 한도 (매 반복 확인)

- `main` 직접 커밋 금지 · `--no-verify` 금지 · 외부 네트워크 금지 ·
  `docs/specs/` 읽기만 · `data/crawl.db` 무변경(sha256 확인) · 의존성 추가 금지(stdlib 만).
- **검증은 맨몸으로 — 이 반복은 러너 파이프 0회다. 연속 카운트 1.** 러너를 **20번**
  불렀고(단위 1 + e2e 19 + 변이 실행 3) 어디에도 파이프·리다이렉션을 안 붙였다.
  e2e 반복은 러너를 가장 많이 부르는 자리라 표본이 가장 크다. **두 반복 더 0회면
  항목을 접는다** — 앞 두 반복이 연속으로 깼으므로 이번 1회로는 못 접는다.
  ※ 문서를 잘라 읽는 데는 `grep`·`head`·`tail` 을 썼다. 항목의 정의는 *"러너를 파이프로
  감싼다"* 이고 러너가 아닌 것에는 판정 줄도 종료 코드도 없다. 세지 않되 적어는 둔다.
- **변이 검증은 `.mutation-lock` 을 걸고 했고 끝나고 지웠다.** 되돌림 확인:
  작업 트리 깨끗 · 검사기 rc 0 · 되돌리기를 `&&` 로 잇지 않았다.
- 이번 반복 최종 실측(전부 맨몸): `PYTHONPATH=src python3 -m unittest discover tests`
  **475건 OK 11.892초** · `PYTHONPATH=src python3 e2e/design_check.py` **rc 0 · 4축 전부 통과** ·
  `e2e/*.py` **19종 rc 0**.
- **집안일 둘을 이 반복이 닫았다.** `history_current.md` **293 → 269줄** 회전
  (`history_021.md` 신설 — 계획 43 의 마지막 반복. 자기 항목을 넣기 전에 돌렸다) ·
  `docs/digest.md` **200줄 유지**(새 줄 둘을 넣느라 가장 오래된 완료 항목 둘을 지웠다 —
  `tokenizer`(010)·`url-normalize`(018). **아카이브 포인터는 하나도 안 지웠다** —
  `## 반복 실패` 의 색인 구멍 항목이 지목한 실패를 되풀이하지 않으려고 지운 것을
  회전 줄에 이름으로 적어 뒀고, 결과는 `index.md` 11·19번 행이 그대로 갖고 있다).
  지금 `history_current.md` **283줄** · `digest.md` **200줄** · 열린 항목 **50**.
