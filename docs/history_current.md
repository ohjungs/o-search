# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**반복 97~122 는 `history_006.md`, 123~128(계획 020 `deadline` 전체)은
`history_007.md`, 계획 21~24(짧은 경로 3건 + 보류 패치 소진, 2026-08-29 14:20~17:40)는
`history_008.md` 로 밀려났다** (2026-08-29 회전 3회).
**계획 25~29 는 `history_009.md`, 계획 30·31(짧은 경로) + 반복 152 탐색 +
계획 33 `clock-injection` 의 계획·설계·개발 1~4 는 `history_010.md` 로 밀려났다**
(2026-08-30 회전, 393줄 → 139줄. 상한 300줄을 넘겨 놓고 두 반복을 갔다).
**계획 33 의 마지막 네 반복(개발 5 · 테스트 1 · 리뷰 1 · e2e 1)은 `history_011.md`**
(2026-08-30 회전, 322줄 → 210줄. 상한을 넘긴 채 반복 두 번을 또 갔다 — 스텝 3 이
"미결(문서 위생)" 로 넘긴 것을 스텝 4 가 닫았다).
계획 33 의 전 phase 서술은 `plan_history_020.md` 의 `## 기록` 에 그대로 있다.
**계획 34 `graceful-interrupt` 의 앞 여덟 반복(계획 1 · 설계 1 · 개발 1 세 번 · 2 · 3 · 4)은
`history_012.md`** (2026-08-30 회전, 300줄 → 85줄. **상한에 정확히 닿아 넘기기 전에 돌렸다** —
앞 두 번은 넘긴 채 반복을 더 갔다).
**계획 34 의 마지막 세 반복(테스트 1 · 리뷰 1 · e2e 1)은 `history_013.md`**
(2026-08-31 회전, 307줄 → 244줄. 상한 300 을 넘긴 그 반복에서 바로 돌렸다 —
`digest ## 반복 실패` 가 지적한 "회전이 두 반복 늦었다" 를 안 되풀이한다).
**계획 35 `deadline-stop` 전체 여섯 반복(계획 · 개발 1 · 테스트 2 · 리뷰 · e2e)은
`history_014.md`** (2026-08-31 회전, 336줄 → 188줄. 상한 300 을 넘긴 채 테스트 반복
하나를 더 갔고 **리뷰가 그것을 잡았다** — 회전은 phase 룰이 아니라 문서 위생이라
`status.md` 의 "집안일 미결" 로만 있었다. 완료된 계획 하나를 통째로 미는 경계로 잘랐다).

**계획 36 `signal-budget-cover` 전체 두 반복과 계획 37 `indexer-interrupt` 전체 여섯 반복은
`history_015.md`** (2026-08-31 회전, 318줄 → 96줄. **계획 38 리뷰가 잡아서 돌렸다** —
테스트 스텝이 305줄인 것을 알고 `status.md` 에 적어 둔 채 그대로 갔다. 393·322·336·318,
네 번째 지각이다. 완료된 계획 둘을 통째로 미는 경계로 잘랐다).

**계획 38 `digest-rotate` 전체 다섯 반복과 계획 39 `indexer-lock` 전체 다섯 반복은
`history_016.md`** (2026-09-01 회전, 299줄 → 145줄. **계획 40 리뷰가 제때 돌렸다** —
테스트 반복이 "다음 반복이 회전한다" 고 적어 둔 그대로다. 393·322·336·318 은 지각이었고
이번은 아니다. 완료된 계획 둘을 통째로 미는 경계로 잘랐다 — 계획 40 을 여는 반복 198 부터 남는다).

**계획 40 `exit-code-contract` 전체 여섯 반복(198~203)은 `history_017.md`** (2026-09-01
회전, 291줄 → 새 항목을 넣기 전에 돌렸다. **테스트 phase 가 자기 손으로 돌린 첫 회전이다** —
205 가 "다음 반복이 회전 자리다" 라고 적어 둔 그대로다. 완료된 계획 하나를 통째로 미는 경계).

**계획 41 `crawl-db-guard` 전체 다섯 반복(204~208)은 `history_018.md`** (2026-09-01
회전, 364줄 → 221줄. **e2e phase 가 마감 작업으로 돌렸다** — 300줄 상한을 64줄 넘긴
채로 개발 2/2 가 지나갔다. 완료된 계획 하나를 통째로 미는 경계. 이 회전이
`DocCitationTest` 의 아카이브 제외 규칙을 **실물로 시험한 첫 사례**다 — 계획 42
7절 반증 신호 ②가 예고한 그 자리이고, 새 이름이 규칙과 맞아 스위트는 초록이다).

**계획 42 `docs-citation-guard` 전체 여섯 반복(209~214)은 `history_019.md`** (2026-09-01
회전, 292줄 → **설계 반복이 자기 항목을 넣기 전에 돌렸다** — 넣었으면 300줄을 넘겼다.
계획 41 회전이 64줄 지각한 바로 다음 회전이고, 이번엔 앞에서 잘랐다. 완료된 계획 하나를
통째로 미는 경계).

**계획 43 `focus-contrast` 의 앞 여섯 반복(215~220)은 `history_020.md`** (2026-09-01
회전, 293줄 → 70줄. **e2e 반복이 자기 항목을 넣기 전에 돌렸다** — 넣었으면 311줄이다.
계획 42 회전과 같은 자리에서 같은 순서로 돌았고, 두 번 연속 지각이 없다. 완료된 계획
하나를 통째로 미는 경계).

---

## 2026-09-01 17:05 | focus-contrast | e2e 1/1 | 시도0

- 한 일: **계획 43 의 e2e phase 를 돌고 DONE 으로 닫았다. 새로 만든 e2e 파일 0개.**
  `rules/e2e.md` 1절의 선행 관문 셋(전체 검증 · CI · 품질 기준)을 먼저 통과시키고,
  계획서 3절 스텝 1 의 완료 기준을 그대로 실행했다. 결과는
  `docs/e2e/focus-contrast/result.md`.
- 결과(전부 맨몸 · 파이프 0회): 단위 **473건 OK 11.628초** · **`e2e/*.py` 19종 rc 0** ·
  `design_check.py` `[3]` 절 **16행**(비텍스트 2행 — 라이트 `--focus` **3.56:1** / 기준
  3.0 · 다크 11.14:1) · ko 20/20 100% · en 19/20 95% · p95 **8.85ms**(예산 300ms 의 2.9%) ·
  [열림] 10.24/s · [차단] 10.23/s · `data/crawl.db` sha256 무변경(`85c96744…5bda18`).
  린터·타입체커 없음, **CI 없음**(`.github` 부재) — `rules/e2e.md` 1절 2항 해당 없음.
- **새 e2e 0개의 근거를 진짜 응답 바이트로 확인했다**(계획 41·42 와 같은 판정 자리).
  제품 diff 는 CSS 색값 하나이고, e2e 가 사는 네 자리 중 "실제 응답 바이트" 는
  `design_check.py` 가 이미 로컬 서버(포트 0)를 세워 덮는다 — 만들면 19종이 20종이
  될 뿐이다(ponytail 1번).
- **탐침이 형제 구멍 하나를 새로 쟀다 — 계획 범위 밖이라 고치지 않고 `digest` 로 넘긴다.**
  응답 CSS 에서 포커스 링 규칙을 통째로 지운 뒤 `check_contrast` 를 돌리면 **기준 위반
  0건 · 측정 불능 0건**이다. 토큰은 여전히 선언돼 있으니 검사기는 3.56:1 을 찍고 rc 0 을
  낸다 — **아무에게도 안 보이는 링을 초록으로 재는 자리**다. 계획 43 이 닫은 것은
  *어느 이름을 보나*(토큰 커버리지)이고, 이것은 *그 토큰을 쓰는 규칙이 살아 있나* 다.
  계획 13 → 43 으로 이어진 같은 계보의 다음 칸이다.
- 다음: 계획 43 **DONE**. 계획서 `plan_history_029.md` · 설계서 `design_history_028.md`.
  **PR 은 안 연다** — `## 판단 필요`(PR #4 미병합, `origin/main` 기점 아님)가 그대로다.
  브랜치는 `origin/loop/focus-contrast` 로 푸시했다.

## 2026-09-01 17:25 | focus-ring-presence | 계획 1/1 | 시도0

- 한 일: **계획 44 를 열었다.** 사용자가 승인 게이트를 열어 앞 반복이 *"개발 스텝을
  여는 일이라 승인 필요"* 로 닫아 둔 후보(포커스 링의 **존재**를 아무도 안 잰다)가
  풀렸다. `rules/discover.md` 1절 1~8순위를 전수로 돌고, 착수 탐침으로 후보의
  처방을 다시 쟀다(`digest` 의 *"기록된 답을 실행 전에 다시 재라"*).
  계획서 `docs/plan_focus-ring-presence.md` (`phase: 설계`).
- 결과(전부 맨몸 · 파이프 0회): 1순위 **473건 OK 11.754초**(실패 0) · 3순위 코드
  `TODO`/`FIXME`/`HACK` **0건** · 4순위 `docs/candidates.md` **파일 없음** ·
  5순위 `digest ## 보류` **0건** · 8순위 `gh issue list` **0건** ·
  6순위 열린 **21건 중 착수 가능 1건** · 7순위 `## 반복 실패` **3건 전부 판정**.
  **착수 탐침이 구멍을 다섯 배로 넓혔다** — 링을 죽이는 변이 **6종 전부**가
  기준 위반 0 · 측정 불능 0 으로 살아남는다(규칙 삭제 · `outline:none` ·
  `var(--focus)`→`var(--line)` · `:focus-visible`→`:focus` · `outline-offset`
  제거 · `outline-offset:0`). `digest` 항목은 **첫째 하나만** 쟀다. 심긴 것을
  먼저 단언했다. 제품 코드 **0줄** · `data/crawl.db` 무변경.
- **두 가지가 후보의 진술을 뒤집었다.** ① **처방이 한 칸 틀렸을 수 있다** — 항목은
  *"없으면 종료 2"* 라 적었는데 같은 파일의 형제 관용구(`check_mobile` 의
  `overflow-wrap:anywhere`)는 **종료 1** 이다(실측 `fail=1 unm=0`). 계획 41 에서
  물려받은 처방이 자리도 타입도 틀렸던 것과 같은 형태다. ② **`outline-offset` 을
  건드리는 변이는 `NONTEXT_PAIRS` 주석이 명시한 짝 선택의 근거를 무효로 만든다** —
  `status.md ## 사람이 정할 것` 의 2번(`--focus` on `--bg-button` 1.45:1)이
  가정에서 실측 대상으로 바뀐다. 그래서 **`rules/design.md` 1절 "대안이 2개 이상
  갈림" 에 걸렸고 짧은 경로가 아니다**(4-1 다섯 중 둘이 거짓).
- **기점 전제가 낡아 있었다** — 다시 재니 `origin/main` 은 `494313b`(PR #5 병합)이고
  계획 42·43 의 코드 산출물이 둘 다 `main` 에 있다. `origin/main` 의 트리는
  `bc40ea9` 의 부모와 **같은 해시**다. 그래서 기점을 `bc40ea9` 로 잡았다 —
  `main` 기점이면 반복 221 기록 넷이 되돌아가 아래 재발 항목을 여섯 번째로 만든다.
- 다음: **설계 1/1**. 갈림길 둘(종료 코드 · 무엇을 자국으로 삼나)을 세 출발점에서
  풀고, 가정 *"응답 바이트의 자국으로 '링이 보인다'를 대리할 수 있다"* 를 깬다.
  거짓이면 개발로 가지 않는다.

## 2026-09-01 17:45 | focus-ring-presence | 설계 1/1 | 시도0

- 한 일: 설계서 `docs/design_focus-ring-presence.md`. **후보 자국 3종 × 변이 11종**을
  탐침으로 갈랐다(계획의 6종에 V7 뒤 규칙 `outline:none` · V8 뒤 규칙
  `outline-width:0` · V9 같은 규칙 안 `outline-width:0` · V10 **다크 블록 안**
  `outline:none` 을 더했다). 제품 코드 **0줄**, 탐침은 저장소 밖 스크립트라
  되돌릴 것이 없다. `data/crawl.db` 무변경.
- 결과(맨몸 · 러너 파이프 0회): `PYTHONPATH=src python3 -m unittest discover tests`
  **473건 OK 11.698초** · `PYTHONPATH=src python3 e2e/design_check.py` **4축 전부 통과**.
  후보별 실측 — **A `"var(--focus)" in css`(digest 처방) 3/11** ·
  **B 규칙을 읽는다 9/11** · C 짝을 CSS 에서 파생은 판정만 맞고 메시지가 틀린다.
- **선택은 B.** A 는 문자열이 *어디에* 있는지 안 봐서 `body{color:var(--focus)}` 만
  남아도 통과하고, 못 잡는 V5·V6 이 하필 `NONTEXT_PAIRS` 주석의 전제를 깨는 변이다.
  **C 를 버린 근거는 실측이다** — V3 에서 rc 2 는 나오는데 메시지가
  `짝도 제외도 없는 색 토큰: --focus` 라 *"링이 `--line` 으로 그려진다"* 를 못 말한다.
  **판정만 맞고 진단이 틀린 검사기는 야간에 아무도 못 고친다.**
- **갈림길 A(종료 코드)는 2 다.** 형제 `check_mobile` 이 1 인 것(재확인 `fail=1 unm=0`)과
  갈리는 이유를 적었다 — 거기서는 [4]번 축의 다른 숫자가 그대로 참인데, 여기서는
  **[3]번 축이 찍은 3.56:1 자체가 무효**가 된다. **1 과 2 를 가르는 것은 고장의 크기가
  아니라 "찍은 숫자가 아직 참인가" 다.**
- **가정을 깼고 참이었다 — 그리고 조건으로 승격시켰다.** 자국이 캐스케이드를 대리하려면
  `outline` 을 정하는 규칙이 하나뿐이어야 하고(실측 1개), 그 가정을 깨는 V7·V8·V10 이
  **규칙 수 세기 하나에 셋 다** 걸린다. 주석으로 적을 것을 검사로 올렸다.
- **일부러 안 잡는 둘에 사유를 적었다**: V4(`:focus`)는 키보드에 링이 그대로 보이므로
  이 축이 재는 것이 안 깨진다 · V9 는 지금 CSS 에 0곳이고 잡으려면 한 규칙 안의 선언
  순서를 해석해야 한다. 둘 다 `ponytail:` 천장으로 남긴다.
- **곁에서 실물 하나** — `NO_PAIR["--line"]` 의 *"(다크도 1.08:1)"* 이 **어느 짝도
  아니다**. 여섯 짝 실측: 라이트 page/input 1.34 · button 3.86, 다크 page 1.37 ·
  input 1.27 · button 4.88. 사유가 계약인 파일이라 개발 스텝에서 **숫자 하나만** 고친다
  (판단 보류 문장은 안 건드린다). `status.md`·계획서의 *"라이트에서 1.08:1"* 도 같은 오기.
- 다음: **개발 1/1**. `focus_rule(css)` 약 15줄 + 호출 한 줄 + 출력 한 줄, 단언 7건.
  계약은 설계서 `## 계약` 9항. `src/websearch/serve.py` 0줄이고 `design_check.py` 는
  계속 rc 0 이어야 한다.
