# 회전 아카이브 062 — 계획 59 `endtag-cut-cover` (반복 345~349)

<!-- 원본 그대로. 수정·삭제 금지. 회전 사유와 압축은 digest.md `## 완료` 에 있다. -->

## 반복 345 — 계획 58 마감 → 계획 59 `endtag-cut-cover` 탐색 (2026-09-05)

- 한 일: **계획 58 아카이브.** `plan_passage-cost-band.md` → **`plan_history_044.md`** ·
  `design_passage-cost-band.md` → **`design_history_044.md`**(`git mv` · 내용 무변경).
  `index.md` 의 계획 58 줄을 **완료**(완료 기준 8/8 · e2e 21종 rc 0 · 기준선 회귀 0)로 닫고
  `digest.md` `## 완료` 에 등재 · 후보 `[5]`(예산 정각과 상한 사이의 창)를 **닫힘**으로 표시했다.
  회전은 **불필요**(항목 6개 · 142줄 → 상한 20회/300줄 안).
- 결과: **탐색 1~5순위 실측 0건** — 전수 `Ran 605 tests in 13.922s` · `OK` · rc 0 ·
  린터/타입체커 설정 파일 없음 · `TODO` 가 `src`·`tests`·`e2e` 에 1건인데 파서 입력 문자열 안 ·
  `docs/candidates.md`·`docs/patches/` 없음 · `digest ## 보류` 0건 · 열린 이슈/PR 0.
- 결과: **6순위에서 더 높은 점수가 전부 여는 조건에 막혀 있다**(실물 코퍼스 · 재색인 · 사람 결정)
  → `[5]`「닫는 태그가 자손을 잘라내는 자리를 붙드는 단언이 하나뿐이다」를 골라 **계획 59
  `endtag-cut-cover`**. 착수 탐침을 **메모리 변이**로 다시 쟀다 — `del self._els[i:i+1]` 은
  `failures=1`(오직 `안 닫힌 li` subTest), 형제 넷은 2·6·6·16건이라 여유 충분. 가르는 모양은
  **`_IMPLIED_END` 에 없는 자손이 안 닫힌 때**(`<div hidden><span>속</div><p>보이는</p>` →
  원본 `[('p','보이는')]` vs 변이 `[]`)이고 오늘 그 모양을 밟는 테스트는 **0건**이다.
- 다음: **개발 1/1.** 고치는 파일은 `tests/test_extract.py` 하나 · 제품 `src/` 0줄 ·
  subTest 라 단위 건수 605 무변(`README.md` 무접촉). 설계는 생략(트리거 0 · 대안이 산출물을
  안 가른다). 이번 반복이 만진 것은 `docs/` 뿐이다.

## 반복 346 — 계획 59 개발 1/1 (2026-09-05) | 시도1

- 한 일: **앞 에이전트가 같은 스텝에서 변경 0건으로 죽은 자리를 처음부터 다시 돌았다**(재시도
  1회차 · `attempt: 1`). 긴 명령 하나에 매달리지 않도록 쪼갰다 — 변이 측정은 `test_extract.py`
  만(0.004s), 전수는 마지막에 한 번(13.8s).
- 한 일: `tests/test_extract.py` `test_an_unclosed_child_does_not_pin_a_hidden_region_open`
  목록에 **`("안 닫힌 span", "<div hidden><span>숨은</div><p>보이는 김치찌개</p>")`** 를 더하고,
  루프 뒤에 **반대 방향** 한 줄(`<section><span>가</section><p>나</p>` → `["가", "나"]`)을 붙였다.
  **+12줄 · 파일 1개 · 제품 `src/` 0줄 · `README.md` 무접촉**(subTest 라 건수 605 무변).
- 결과: **변이 M-a(`del self._els[i:i+1]`)가 1 → 2건으로 죽는다** — 더하기 전
  `failures=1`(오직 `안 닫힌 li`), 더한 뒤 `failures=2` 이고 늘어난 것이 `안 닫힌 span` 이다.
  **변이는 저장소 파일이 아니라 메모리에서 걸었다**(`mock.patch.object` 로 `handle_endtag` 교체 ·
  반복 328·334·341 과 같은 방식) — 심고 되돌리는 사이에 죽어도 작업 트리가 안 더러워진다.
- 결과: **새 단언은 `_IMPLIED_END` 표 축에 안 얹혀 있다** — 표에서 `li` 줄을 지우는 변이에서
  죽는 것은 `test_an_optional_end_tag_does_not_hide_the_next_sibling` 의 `li`·`li 안의 안 닫힌 p`
  둘뿐이고 새 subTest 는 초록이다(완료 기준 4). 오늘의 유일한 칸(`안 닫힌 li`)이 왜 얇았는지가
  여기서 보인다 — 그 칸은 `li` 줄이 `{"li"}` 라서 «아무도 대신 안 닫아 준다» 를 **우연히** 만족했고,
  같은 목록의 `안 닫힌 p`·`다른 컨테이너`는 뒤따르는 `<p>` 가 대신 닫아 줘서 변이를 못 잡는다.
- 결과: 전수 `Ran 605 tests in 13.819s` · `OK` · rc 0(맨몸 · 리다이렉션 0).
  `git status --short` 는 `M tests/test_extract.py` 하나 + `docs/`.
- 다음: **테스트 phase 0/1.** 남은 완료 기준은 6번(e2e 21종 전수)뿐이고 e2e phase 몫이다.
  러너 규율 위반 0회(누적 37).

## 2026-09-05 23:10 | endtag-cut-cover | 테스트 1/1 | 시도0

- 한 일: **계획 59 테스트 phase.** 이 계획은 제품 `src/` 0줄이라 룰 3절(갭 탐색)만 돌았다.
  `_BlockParser.handle_endtag`·`handle_starttag` 의 줄마다 변이를 하나씩 걸고 전수를
  돌려 「죽이는 단언이 몇 건인가」를 셌다 — 변이 6종 전부 `mock.patch.object` 로
  **메모리에서만** 걸었고 저장소 파일은 안 건드렸다(하네스는 스크래치패드 `gap59.py`).
- 결과: 무변이 대조군 `Ran 605 tests in 13.765s` · **OK** · rc 0(failures 0 · errors 0).
  변이별 죽는 건수 — G1 `if tag in self._open` 가드 삭제 **45건**(실패 6 + 에러 39:
  열린 적 없는 닫는 태그가 `ValueError`) · G2 `_open` 꼭대기만 버리기 2건 ·
  G3 `_open` 자르기 off-by-one 3건 · **G4 `_open` 을 앞에서부터 찾기 1건** ·
  G5 암묵적 닫기 `while` → `if` 8건 · **G6 암묵적 닫기에서 `_open` 안 걷기 1건**.
- 결과: **새 갭 1건 · 4점 — 8점 미만이라 안 고쳤다.** G4·G6 이 계획 59 가 어제 닫은
  `_els` 쪽과 같은 「한 칸」 모양인데, `extract_blocks()` 의 `(태그, 본문)` 중 **태그를
  읽는 곳이 저장소에 0곳**이다(`src/websearch/indexer.py:334`·`e2e/passage_eval.py:93`
  둘 다 `_tag` 로 버린다 · 읽는 것은 `tests/test_extract.py` 뿐). 이름표가 틀려도
  사용자에게 보이는 오류가 안 나므로 `digest.md` `## 다음 계획 후보 (테스트 phase 갭,
  8점 미만)` 에 `[4]` 로 등재했다. 여는 조건은 「태그를 처음 읽는 소비자가 생길 때」.
- 결과: 완료 기준 3 재확인(어제 더한 `안 닫힌 span` subTest 와 반대 방향 한 줄이 원본에서
  초록) · **단언을 낮춘 곳 0**(개발 diff 가 `+12줄` 순수 추가) · 고친 파일 **0개**(문서만).
- 다음: **리뷰 phase 0/1.** 남은 완료 기준은 6번(e2e 21종 전수 rc 0 · 기준선 회귀 0)뿐이다.
  러너 규율 위반 **0회**(누적 37).

## 2026-09-05 23:35 | endtag-cut-cover | 리뷰 0/1 | 시도0

- 한 일: **계획 59 리뷰 phase.** 대상은 이 계획의 diff — 개발 커밋 `7396117` 의
  `tests/test_extract.py` **+12줄**(제품 `src/` 0줄)과, 앞 에이전트가 리뷰 실측 중
  작업 트리에 남긴 주석 3줄. 짧은 경로라 패스 A(백지)·패스 B 를 한 세션에서 순서대로
  돌았다(`rules/review.md` 0절 마지막 줄). 변이는 전부 `mock.patch.object` 로
  메모리에서만 걸었다(스크래치패드 · 저장소 파일 무변경 · 반복 328·334·341·346 과 같은 방식).
- 결과: **[R59-1] 확증 — 개발 커밋이 심은 주석이 표를 거꾸로 읽었다(자동 수정).**
  「뒤따르는 `<p>` 시작 태그가 남은 `<p>`/`<li>` 를 암묵적으로 닫아 준다」고 적었는데
  `_IMPLIED_END["li"]` 는 `{"li"}` 라 `<p>` 가 `<li>` 를 **못 닫는다**. 자르기 변이
  (`del self._els[i:i+1]`)를 걸고 네 칸을 직접 재니 `안 닫힌 p` **초록** ·
  `안 닫힌 li` **빨강** · `안 닫힌 span` **빨강** · `다른 컨테이너` 초록이다 —
  주석이 「p 도 li 도 대신 닫힌다」로 읽히면 **`안 닫힌 li` 칸이 왜 변이를 잡는지**가
  거꾸로 설명된다. 앞 에이전트의 작업 트리 수정이 그 방향을 맞게 고친 것이라 그대로 받았다.
- 결과: **[R59-2] 확증 — 「표를 어떻게 고치든 잡는다」는 실측보다 한 칸 넓었다(자동 수정).**
  표에서 **어느 줄을 지워도** 새 칸은 잡는다(`li` 줄 삭제 + 자르기 변이에서 `안 닫힌 span`
  빨강 · 완료 기준 4 재현). 그러나 표에 **`span` 줄을 더하면** 뚫린다(`_IMPLIED_END`
  에 `span={"p"}` 를 얹으면 같은 변이가 **초록**). 명세에 `</span>` 생략이 없어 실물에서
  안 일어나는 편집이라 결함이 아니라 **천장**이고, 주석을 그 조건으로 좁혔다.
  반복 319 의 «천장 주석이 «여기는 안전하다» 를 주장하면 그 주장도 실측 대상이다» 가
  같은 파일에서 값을 낸 자리다.
- 결과: **기각 2건(80점 미만).** ① 「개발이 더한 `반대 방향` 한 줄이 죽은 단언 아닌가」 —
  **재 보니 살아 있다**: 주석이 지목한 모양(안 닫힌 자손이 있으면 모아둔 글을 버리고 닫는다)
  을 변이로 심으니 위 네 칸은 **전부 초록**인데 그 줄만 빨강이라, 「이 줄이 없으면 그 변이가
  위 단언들을 전부 통과한다」가 글자 그대로 맞았다(같은 변이가 파일 전체로는 19건을 죽이지만
  주석의 주장 범위는 이 테스트 안이다). ② 「`digest [5]` 에 취소선을 아직 안 그었다」 —
  이 저장소의 닫기 표기는 e2e 결과를 함께 다는 마감 몫이라 리뷰의 지적이 아니다.
- 결과: 고친 파일 **`tests/test_extract.py` 하나**(주석만 · 단언·입력·코드 **무변**) ·
  제품 `src/` 0줄 · `README.md`(단위 605건)·`docs/specs/`·`data/crawl.db` 무접촉.
  전수 `Ran 605 tests in 13.846s` · `OK` · rc 0(맨몸 · 리다이렉션 0).
- 결과: **주석 수정 두 건이 리뷰 커밋이 아니라 `97c3057`「자동 스냅샷 …」에 실렸다** —
  저장소 밖 훅이 작업 트리를 스냅샷해 커밋하고 원격까지 밀었다(`git ls-remote origin
  loop/passage-cost-band` = `97c3057`). 내용은 온전하고(위 두 수정 그대로) 히스토리
  재작성·`--amend`·강제 푸시는 하드 제약이라 **그대로 두고** 이 반복의 기록을 그 위에
  쌓는다. 다음 반복이 `git log` 에서 계획 59 의 코드 변경을 찾을 때 볼 커밋은 `97c3057` 이다.
- 다음: **e2e phase 0/1.** 승인 필요 항목 0건이라 개발 phase 복귀 없이 넘어간다.
  남은 완료 기준은 6번(e2e 21종 전수 rc 0 · 기준선 회귀 0)뿐이다.
  러너 규율 위반 **0회**(누적 37).

## 2026-09-05 23:45 | endtag-cut-cover | e2e 1/1 · **DONE** | 시도0

- 한 일: **계획 59 e2e phase.** ① `rules/e2e.md` 3절 면제 판정 — 계획이 바꾼 것은
  `tests/test_extract.py` 단언 +12줄과 주석뿐이고 제품 `src/` 0줄이라 **프로세스 밖에서
  달라지는 동작이 0** 이다. 새 단언이 지키는 실물 계약은 `e2e/hidden_passage_e2e.py` 가
  이미 재고 있어 **새 시나리오 0개**(만든 e2e 파일 0 · 고친 e2e 파일 0). ② e2e 21종 전수를
  맨몸으로. ③ 완료 기준 2·3·4 를 **메모리 변이**로 다시 재기(하네스는 스크래치패드
  `e2e59_mut.py` · `mock.patch.object` 로 `_BlockParser`·`_IMPLIED_END` 교체 · 저장소 무변경 ·
  `PYTHONDONTWRITEBYTECODE=1`+`PYTHONPYCACHEPREFIX=$(mktemp -d)`).
- 결과: **21종 전부 `rc 0` · 기준선 회귀 0건.** 정확도 100.0%(=) · `/passages` p95 1.51ms
  (1.52) · `/search` p95 8.80ms(8.81 · 기록 9.11) · ko 20/20 · en 19/20 · 매치 14.0/11/28(=) ·
  크롤 [열림] 10.24/s · [차단] 10.24/s(반복 344 는 10.21) · 숨은 텍스트 0/5 · 디자인 4축
  (JS 0 B · 4.87:1). **움직인 기준선이 없어 `docs/project.md` 수치는 한 줄도 안 고쳤다.**
- 결과: **완료 기준 6/6 충족.** 전수 `Ran 605 tests in 13.789s` `OK` rc 0 · 무변이 대조군
  `605 failures=0`(기준 3 오탐 0) · 변이 M-a `failures=2` 로 늘어난 칸이 **`안 닫힌 span`**
  (기준 2 · 착수 탐침은 1) · **`_IMPLIED_END` 에서 `li` 줄을 지우는 변이는 새 subTest 를 못
  죽인다**(기준 4 — 죽는 둘은 전부 `test_an_optional_end_tag_does_not_hide_the_next_sibling`
  의 `li`·`li 안의 안 닫힌 p` 다). 표를 건드리는 변이와 자르기를 죽이는 변이가 **서로 다른
  칸을 죽인다**는 것이 계획이 주장한 「축이 다르다」의 실측 증거다.
- 결과: 고친 파일은 `docs/` 뿐이다(`docs/e2e/endtag-cut-cover/result.md` 신규 · `status.md` ·
  이 파일 · `metrics.md`). `src/`·`tests/`·`e2e/`·`README.md`·`docs/specs/` 무접촉 ·
  `data/crawl.db` sha256 `85c96744…5bda18` 무변 · 재색인·스키마·새 의존성 0.
- 다음: **계획 59 DONE.** 다음 반복은 계획 phase — 계획 59 마감(계획서 아카이브 회전 ·
  `digest.md` 후보 `[5]` 취소선 · `## 완료` 절에 한 줄)과 새 후보 탐색. 병합은 사람 몫이라
  `loop/passage-cost-band` 에 쌓아만 둔다(`origin/main` 무접촉 · PR 0).
  러너 규율 위반 **0회**(누적 37).

