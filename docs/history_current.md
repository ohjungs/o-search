# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**회전 명부는 `digest.md` 의 `## 완료` 절 «아카이브 명부» 줄이 정본이다.**
여기 있던 스물한 회전의 서술(233줄)은 그 줄과 내용이 겹쳤고, 검사가 강제하는 명부도
그쪽 하나뿐이라(`tests/test_docs.py` 의 `ArchiveIndexTest`) **개발 9(반복 269)가 이
자리에서 지웠다** — 회전으로는 300줄 상한을 못 맞추던 세 반복(309 → 372 → 418줄)의
원인이 이 명부였다. **지운 것은 머리말이지 반복 기록이 아니다** — 항목은 여전히
append 전용이고 수정·삭제 금지다. 각 회전의 사유는 `digest.md` 의 같은 줄에, 원문은
`history_<NNN>.md` 에 그대로 있다.

## 2026-09-02 21:20 | passage-api | e2e 1 | 시도1

- 한 일: **e2e 20종 전수 + `/passages` 를 프로세스 밖으로 처음 꺼냈다.** 새 e2e 파일은
  안 만들고 `e2e/search_api_e2e.py`(+38 −2)에 갈래 둘을 얹었다 — ⑧ `/passages` 계약,
  ⑨ 손상 DB. 제품 `src/` **0줄**. 결과는 `docs/e2e/passage-api/result.md`.
- **왜 새 파일이 아니라 그 파일인가.** 계획 48 이 만든 엔드포인트를 재는 자가 셋인데
  셋 다 같은 자리를 안 본다: `passage_eval.py` 는 **200 한 갈래뿐**(정확도·p95 전용),
  단위 576건은 `make_server()` 를 **스레드**로 부르고 `tempfile` **절대 경로**를 쓴다.
  계획 47 이 열어 둔 서버가 이미 **README 형태의 상대 경로**로 떠 있어서, 경로 하나를
  더 때리는 것으로 계약 전체가 재진다(ponytail 2번 — 프로세스를 더 띄우면 재는 것은 같다).
- **손상 DB 500 을 저장소 사상 처음 밟았다.** 계획 47 의 result.md 5절이 *"500 은 한 번도
  안 났다"* 로 닫혀 있었다 — 재시도해도 안 낫는 상태(500)와 색인을 다시 돌리면 낫는
  상태(503)를 프로세스 밖에서 가른 자리가 0개였다. 이제 진짜 파일에
  `not a sqlite database` 를 써서 `/search`·`/passages` 둘 다 **500 · `version` 1 ·
  경로·트레이스백 유출 0** 을 확인한다. 서버 로그가 **어느 문이 열렸는지**까지 적는다.
- **대조군 둘, 전부 RED.** M1 `_send(500,…)`→`_send(503,…)` · M2 `"position": pos`→`str(pos)`.
  **둘 다 단위가 먼저 죽인다(8건 · 2건)** — 이 e2e 는 눈먼 자리를 메운 것이 아니라 형태를
  바꿔 잰다. 그렇게 적는 것이 정직하다. **다만 `passage_eval` 은 M2 에 완전히 눈이 멀었다**
  (rc 0 · 정확도 100.0% · p95 1.43ms). **품질을 재는 자와 계약을 재는 자는 다른 자다** —
  반복 267 의 눈금(«숫자가 안 움직이는 것은 그 축을 안 쟀다는 뜻일 수 있다»)이 또 맞았다.
  원복은 초록이 아니라 **`git diff -- src/` 0줄**로 확인했다. 커밋된 변이 0 · `.mutation-lock` 0.
- 결과: 단위 **576건 OK**(맨몸 2회 13.433·13.418초) · e2e **20종 전수 rc 0** ·
  정확도 **100.0%(398/398)** · p95 **1.40ms** · recall ko 20/20·en 19/20 · 처리량 10.22/s ·
  디자인 4축 통과 · `perf_search` p95 8.90ms · `data/crawl.db` sha256 무변경 ·
  README 카운트 무변경(e2e 파일을 안 늘렸다 — 20종 그대로).
- **러너 규율: 밟았다(28회째).** 두 번째 전체 스위트에 `2>&1 | tail -3` 을 붙여 판정 줄이
  크롤 로그에 밀렸다. 즉시 맨몸 재실행해 `Ran 576 tests OK` 를 읽었고 **판정은 그 호출 위에서
  안 내렸다.** 방아쇠·형태·자리가 전부 기존 칸이라 **새로 보이는 것이 없다는 것이 이 표본의
  값**이다 — 문장은 소진됐고 남은 것은 `digest [9]`(`unittest -b`)뿐이다.
- **회전은 안 돌렸다** — 이 항목을 붙이고 세어 상한 300 아래다.
- 다음: **계획 48 DONE.** 계획·설계서를 `plan_history_034.md`·`design_history_034.md` 로
  아카이브했고 `index.md` 확장 분할 10번과 `digest ## 완료` 에 등재했다. **PR 은 사람 몫이라
  안 열었다.** 다음 반복은 계획 탐색이다 — 갈림길 6 은 열린 채로 남고(닫는 조건이 실물
  코퍼스) `digest` 의 `[6]`·`[9]` 가 후보로 서 있다.

## 2026-09-02 21:35 | focus-rule-scope | 계획 1 | 시도1

**계획 49 착수 — 후보 다섯을 세우고 하나를 골랐고 하나를 실측으로 버렸다.** 제품 `src/` **0줄**·
새 테스트 0건, 산출물은 `docs/plan_focus-rule-scope.md` 하나. 기점 `18d485f` · 브랜치
`loop/focus-rule-scope`(`main` 은 계획 48 이 없어 거기서 따면 첫 커밋이 RED).

**고른 것 — `focus_rule` 의 at-rule 위치·셀렉터 극성.** 출처는 digest 의 «승인 필요» 로 막혀
있던 항목이고, 처방을 믿지 않고 다시 쟀다 — `serve._render` 의 실제 CSS 를 변이시켜
`mutated != css` 를 먼저 단언하고 `check_contrast` 에 먹였다. ⓐ 다크 `@media` 이동 ⓑ `@media
print` ⓒ `:not(:focus-visible)` ⓓ `:focus-within` **넷 다 `fail=0 unmeasurable=0`** 이면서
«포커스 링 규칙 1개 · offset 2px»·`라이트 3.56:1 OK` 를 찍는다. 대조로 통째 삭제는 죽는다.

**버린 것 — 「`passage_eval` 이 응답 계약을 안 본다」.** 1순위로 들고 갔으나 **계약을 재는
자가 따로 있었다** — `e2e/search_api_e2e.py` 의 `/passages` 블록이 `position` 타입·`title`·
`url` 접두를 단언하고 계획 48 의 M2 를 죽인 것이 그 단언이다. 더하면 중복 커버다. **눈멂과
분업은 다르다 — 「이 도구가 안 잰다」를 쓰기 전에 「그럼 누가 재나」를 먼저 grep 한다.**
**남은 셋**: `unittest -b`(약 90줄 → **4줄**, `Ran 576 in 13.453s / OK` — 다음 짧은 경로, 근거는
계획서 5절) · 락 테스트 단독 **8.038초**/전체 13.453초 · `pages` 없는 DB 에서 `/search` 200·
`/passages` 500(뒤 둘은 여는 조건이 아직 안 왔다).
**설계로 보냈다 — 트리거 「대안이 갈린다」** 하나. at-rule 위치를 닫는 셋이 닫는 범위가 다르다:
깊이 세기 · `token_maps` 재사용(`@media print` 를 못 닫는다) · 최상위 강제. 계획서에서 고르면
측정이 아니라 취향이다. **짧은 경로**: 49 는 해당 없음(2스텝·트리거), B 는 해당.

**러너 규율 재발 1건.** 첫 전체 실행에 붙인 `2>&1 | tail -4` 가 화면에 내놓은 것은 판정이 아니라
크롤 로그 넷이었다 — `OK` **아래로** 출력이 더 흐르므로 파이프를 붙이면 판정이 잘리는 게 아니라
아예 안 보인다. 맨몸으로 다시 돌려 `Ran 576 tests in 13.552s / OK` 확인. 출력 조작 3회·명령
잇기 3회 — **어긴 것은 어긴 것이다.** PR 은 안 열었고 *Update branch* 도 안 눌렀다.

## 2026-09-02 22:00 | focus-rule-scope | 설계 1 | 시도1
- 한 일: **후보 셋을 말로 비교하지 않고 넷을 실제로 만들어 변이 12종에 먹였다**
  (`docs/design_focus-rule-scope.md`). 제품 응답 바이트에서 CSS 를 뽑고 변이마다
  `mutated != css` 를 먼저 단언했다. 후보 구현은 전부 스크래치패드 탐침 안에 있어
  **저장소 변이 0** — `.mutation-lock` 이 필요 없었다(`git status` clean).
  계획서에 없던 후보 A′(A 를 20줄로 줄인 것)와 오탐 후보 F1~F4 를 더해 잰 것이 갈림을 갈랐다.
- 결과: **C(최상위 요구)를 골랐다.** 계획서가 설계에 넘긴 물음(「다크 전용 CSS 를 영구히
  거절하는 것이 옳은가」)은 갈림의 자리가 아니었다. **A 계열이 거짓 초록을 다시 만든다** —
  prelude 블랙리스트(`prefers-color-scheme|print`)가 **모르는 조건을 안전으로 분류**해
  `@media (forced-colors:active)`(F3)·`@layer`(F4) 안의 조건부 링을 종료 0 으로 통과시켰다.
  계획 44 가 죽인 고장과 같은 형태다. 화이트리스트로 뒤집으면 통과하는 at-rule 이 0개가
  되어 그것이 곧 C 다. **B 는 계획서 예상대로 ⓑ(`@media print`)에서 종료 0** — 짧아도
  닫으라는 것을 안 닫는다. C 는 9줄이고 ⓐⓑⓒⓓ 넷을 전부 종료 2 로 만들면서
  V1·V10·V11 을 한 칸도 안 움직였다. 정상 CSS 진단 줄 무변(`라이트 --focus on --bg-page
  3.56:1 / 기준 3.0 OK`).
- 검증(전부 맨몸·단독): 단위 **576건 OK**(13.479초) · `PYTHONPATH=src python3
  e2e/design_check.py` **종료 0**(4축 전부 통과). 러너 출력 조작 **0회** · 명령 잇기 **0회**.
- 다음: **개발 1** — 조건 5(셀렉터 극성, 6줄)를 `focus_rule` 에 넣고 ⓒⓓ 를 종료 2 로
  만든다. 스텝은 계획서의 둘 그대로다(쪼개지도 합치지도 않았다).

## 2026-09-02 22:15 | focus-rule-scope | 개발 1 | 시도1
- 한 일: 설계서 3절의 **조건 5(셀렉터 극성)** 를 `e2e/design_check.py` 에 넣었다(+12 −2,
  주석 빼면 **3줄** — 정규식 `NOT_RE`·`FOCUS_RE` 와 조건 한 줄). `:not(…)` 안을 지운 뒤
  `:focus`/`:focus-visible` 이 **낱말로** 남아야 한다. 기존 `":focus" not in selector` 를
  대체했다. `tests/test_design_check.py` 의 변이 표에 ⓒⓓ 를 `subTest` 로 더했다(+9).
  제품 `src/` **0줄** · 공개 인터페이스 변경 0(반환·예외 모양 그대로).
- 결과: **RED 를 눈으로 보고 갔다** — 구현 전 ⓒ(`:not(:focus-visible)`)와 ⓓ(`:focus-within`)
  둘 다 `unmeasurable` 0건·종료 0 으로 살아남으면서 무효가 된 `3.56:1 / 기준 3.0 OK` 를
  화면에 찍었다(`FAILED (failures=2)`). 계획 44 가 죽이러 온 고장의 형태 그대로다.
  **새 조건의 세 부분이 각각 짐을 진다** — 탐침에서 부분마다 하나씩 무력화하니
  M1(`:not()` 벗기기 제거)에 ⓒ 가, M2(낱말 경계 `(?![\w-])` 제거)에 ⓓ 가, M3(옛 부분
  문자열 가드)에 둘 다 종료 0 으로 살아났다. **오탐 0** — 정상 CSS 와
  `:not(.sb-x):focus-visible` 은 네 갈래 전부에서 종료 0. 종료 코드는 `dc.main()` 을
  실제로 돌려 확인했다(제품 CSS 를 ⓒ 로 태우니 **종료 2**). 탐침은 모듈 속성만 갈아끼워
  저장소 파일을 안 고쳤다 — `python3 -B` · `.mutation-lock` 불필요 · **커밋된 변이 0**
  (`git status` 는 두 파일만 M, 변이 잔재 검색 0건).
- 검증(단위는 맨몸·단독): 단위 **576건 OK**(13.472초 · 건수 무변 — 둘 다 기존 테스트의
  `subTest`) · `PYTHONPATH=src python3 e2e/design_check.py` **종료 0**(4축 전부 통과) ·
  e2e 20종·README 무변경 · `data/crawl.db`·`docs/specs/` 무변경.
- **러너 규율 1회 (누적 30회).** `design_check.py` 를 맨몸으로 돌려 4축과 `4축 전부 통과.`
  까지 본 **뒤에** `> /dev/null; echo "종료코드=$?"` 를 붙였다. **방아쇠가 새것이다** —
  29회의 「출력이 길다」가 아니라 **「종료 코드를 눈으로 못 봤다」**. 실제로는 맨몸 호출이
  이미 증명하고 있었다(러너 rc≠0 이면 Bash 툴이 `Exit code N` 을 먼저 띄우고, 이 반복의
  RED 도 그렇게 왔다). 안 했어도 아는 것이 같았다 — **판정 줄을 못 봤다고 느낄 때 덧붙일
  것은 명령이 아니라 앞 출력을 다시 읽는 일이다.**
- 다음: **개발 2** — 조건 6(at-rule 위치, 3줄)으로 ⓐⓑ 를 종료 2 로. 링 규칙 앞의 `{`·`}`
  개수가 같아야 한다는 것 하나이고, 설계서가 못박은 대로 at-rule 화이트리스트는 안 만든다.
