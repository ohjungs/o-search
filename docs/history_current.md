# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**반복 97~122 는 `history_006.md`, 123~128(계획 020 `deadline` 전체)은
`history_007.md`, 계획 21~24(짧은 경로 3건 + 보류 패치 소진, 2026-08-29 14:20~17:40)는
`history_008.md` 로 밀려났다** (2026-08-29 회전 3회).
**계획 25~29 는 `history_009.md`, 계획 30·31(짧은 경로) + 반복 152 탐색 +
계획 33 `clock-injection` 의 계획·설계·개발 1~4 는 `history_010.md` 로 밀려났다**
(2026-08-30 회전, 393줄 → 139줄. 상한 300줄을 넘겨 놓고 두 반복을 갔다).
**계획 33 의 마지막 네 반복(개발 5 · 테스트 1 · 리뷰 1 · e2e 1)은 `history_011.md`**
(2026-08-30 회전, 322줄 → 210줄. 상한을 넘긴 채 반복 두 번을 또 갔다 — 스텝 3 이
"미결(문서 위생)" 로 넘긴 것을 스텝 4 가 닫았다).
계획 33 의 전 phase 서술은 `plan_history_020.md` 의 `## 기록` 에 그대로 있다.
**계획 34 `graceful-interrupt` 의 앞 여덟 반복(계획 1 · 설계 1 · 개발 1 세 번 · 2 · 3 · 4)은
`history_012.md`** (2026-08-30 회전, 300줄 → 85줄. **상한에 정확히 닿아 넘기기 전에 돌렸다** —
앞 두 번은 넘긴 채 반복을 더 갔다).
**계획 34 의 마지막 세 반복(테스트 1 · 리뷰 1 · e2e 1)은 `history_013.md`**
(2026-08-31 회전, 307줄 → 244줄. 상한 300 을 넘긴 그 반복에서 바로 돌렸다 —
`digest ## 반복 실패` 가 지적한 "회전이 두 반복 늦었다" 를 안 되풀이한다).
**계획 35 `deadline-stop` 전체 여섯 반복(계획 · 개발 1 · 테스트 2 · 리뷰 · e2e)은
`history_014.md`** (2026-08-31 회전, 336줄 → 188줄. 상한 300 을 넘긴 채 테스트 반복
하나를 더 갔고 **리뷰가 그것을 잡았다** — 회전은 phase 룰이 아니라 문서 위생이라
`status.md` 의 "집안일 미결" 로만 있었다. 완료된 계획 하나를 통째로 미는 경계로 잘랐다).

**계획 36 `signal-budget-cover` 전체 두 반복과 계획 37 `indexer-interrupt` 전체 여섯 반복은
`history_015.md`** (2026-08-31 회전, 318줄 → 96줄. **계획 38 리뷰가 잡아서 돌렸다** —
테스트 스텝이 305줄인 것을 알고 `status.md` 에 적어 둔 채 그대로 갔다. 393·322·336·318,
네 번째 지각이다. 완료된 계획 둘을 통째로 미는 경계로 잘랐다).

**계획 38 `digest-rotate` 전체 다섯 반복과 계획 39 `indexer-lock` 전체 다섯 반복은
`history_016.md`** (2026-09-01 회전, 299줄 → 145줄. **계획 40 리뷰가 제때 돌렸다** —
테스트 반복이 "다음 반복이 회전한다" 고 적어 둔 그대로다. 393·322·336·318 은 지각이었고
이번은 아니다. 완료된 계획 둘을 통째로 미는 경계로 잘랐다 — 계획 40 을 여는 반복 198 부터 남는다).

**계획 40 `exit-code-contract` 전체 여섯 반복(198~203)은 `history_017.md`** (2026-09-01
회전, 291줄 → 새 항목을 넣기 전에 돌렸다. **테스트 phase 가 자기 손으로 돌린 첫 회전이다** —
205 가 "다음 반복이 회전 자리다" 라고 적어 둔 그대로다. 완료된 계획 하나를 통째로 미는 경계).

**계획 41 `crawl-db-guard` 전체 다섯 반복(204~208)은 `history_018.md`** (2026-09-01
회전, 364줄 → 221줄. **e2e phase 가 마감 작업으로 돌렸다** — 300줄 상한을 64줄 넘긴
채로 개발 2/2 가 지나갔다. 완료된 계획 하나를 통째로 미는 경계. 이 회전이
`DocCitationTest` 의 아카이브 제외 규칙을 **실물로 시험한 첫 사례**다 — 계획 42
7절 반증 신호 ②가 예고한 그 자리이고, 새 이름이 규칙과 맞아 스위트는 초록이다).

**계획 42 `docs-citation-guard` 전체 여섯 반복(209~214)은 `history_019.md`** (2026-09-01
회전, 292줄 → **설계 반복이 자기 항목을 넣기 전에 돌렸다** — 넣었으면 300줄을 넘겼다.
계획 41 회전이 64줄 지각한 바로 다음 회전이고, 이번엔 앞에서 잘랐다. 완료된 계획 하나를
통째로 미는 경계).

---

## 2026-09-01 15:20 | focus-contrast | 계획 1/1 | 시도0

- **계획 43 `focus-contrast` 를 열었다.** 브랜치 `loop/focus-contrast` · 기점 `96eea95` ·
  계획서 `docs/plan_focus-contrast.md` · **설계 트리거 걸림**(대안 2개 이상 갈림).
- **탐색을 1~8순위 전수로 돌렸고 1~5순위가 세 반복 연속 0건이다** — 단위 469건 OK
  11.689초 · `design_check` rc 0 · `quality_eval` rc 0 · 린터 없음 · 코드 `TODO` 0
  (매치 2건은 둘 다 `\uXXXX` 라는 문자열) · `candidates.md`·`scripts/` 없음 ·
  `digest ## 보류 (승인 대기)` 절은 주석뿐 · `gh issue list` 빈 출력.
- **6순위를 세 번째로 열지 말지를 전수로 판정했고 답은 "안 된다" 였다** — 두 후보 절의
  열린 항목 **21건 전부 게이트가 닫혀 있다**(도달 불가 6 · 다른 계획을 열 때 함께 5 ·
  천장으로 받아들인 값 4 · 사람이 정할 갈림길 6). 7순위 3건은 전부 저장소 밖이고
  계획 42 가 같은 판정을 이미 실측으로 냈다. **그래서 사양으로 내려갔다** —
  `docs/specs/concept.md` 디자인 3축이 근거고, 1~8순위 밖에서 근거를 잡은 첫 계획이다.
- **근거는 추측이 아니라 저장소 자신의 자로 쟀다.** 라이트 `--focus` `#f97316` on
  `--bg-page` `#ffffff` = **2.80:1**(`design_check.contrast`), 다크는 11.14:1 로 통과.
  WCAG 2.1 SC 1.4.11 비텍스트 3:1 미달이 **라이트에서만** 있다. `token_maps` 가 이 토큰을
  양쪽 맵에서 실제로 파싱하는 것까지 확인했다 — 새 축은 기존 파서 위에 얹힌다.
- **왜 211 반복 동안 안 걸렸나가 이 계획의 진짜 내용이다.** `check_contrast` 의 커버리지
  강제가 `startswith("--fg-")` 로 걸러서 `--focus` 는 `declared` 에 들어가지도 않는다 —
  검사기가 토큰의 존재를 모른 채 "4축 전부 통과" 를 낸다. **초록불이 근거 없이 켜져
  있던 자리**이고, 계획 13 리뷰가 닫은 눈먼 자리 4곳의 **형제**다(그때는 *어느 맵을
  보나*, 이번은 *어느 이름을 보나*). 그래서 목표가 색 하나가 아니라 **접두어 허용을
  명시적 제외로 뒤집는 것**이다 — 이름이 무엇이든 짝이 없으면 종료 2.
- 검증: 계획서를 넣은 뒤 단위 **469건 OK 11.882초**(맨몸) — 계획 42 의
  `DocCitationTest`·`CitationPatternTest` 가 이 계획서를 통과시켰다. `src/` **0줄**.
- **파이프 ⑪⑫** — 탐색 첫 실행에 `| tail -20`, 계획서 확인 실행에 `| tail -6`.
  둘 다 맨몸으로 재실행해 값을 다시 받았다. 계획 42 가 "규율로 막은 것이라 다음
  반복이 또 셀 자리" 라고 적었고, **바로 다음 반복이 두 번 셌다.**
- 다음: 설계 1/1 — 임계값(3:1 vs 4.5)과 색값을 스스로 고른다. **A 를 고르면 B 가 정해진다.**

## 2026-09-01 15:45 | focus-contrast | 설계 1/1 | 시도0

- 한 일: `docs/design_focus-contrast.md` — **갈림길 셋을 다 닫았다.** 임계값
  `MIN_CONTRAST_NONTEXT` **3.0**(WCAG 2.1 SC 1.4.11) · 색 **`#ea580c`**(2.80 → **3.56:1**) ·
  강제는 접두어 허용에서 **전 토큰 + 이름·사유를 적은 제외 상수**(`NO_PAIR`)로 뒤집는다.
  집안일로 회전 1회(`history_019.md` 신설). `src/`·`tests/`·`e2e/` **0줄**.
- **왜 3.0 인가**: 사양이 적은 4.5 는 **텍스트** 기준이다. 비텍스트에 4.5 를 걸면 표준에도
  사양에도 없는 요구라 앞으로 비텍스트 토큰마다 오탐이 나고, **오탐을 내는 검사는 결국
  제외 목록으로 도망친다** — 이번에 뒤집는 그 문을 스스로 여는 셈이다. 상수 하나 아끼는
  대가로 검사기의 신뢰를 판다.
- **왜 `#ea580c` 인가**: `#c2410c`(5.18)는 수치는 통과지만 **버튼색과 같아** 링이
  `outline-offset` 의 2px 흰 틈 하나로만 구별된다 — 포커스 링의 일은 대비가 아니라
  **어디 있는지**다. `#9a3412`(7.31)는 3:1 에 두 배 넘는 과잉이고 오렌지가 갈색으로
  넘어간다. 팔레트 한 칸(orange-500 → 600)이 3:1 을 **19% 여유**로 넘는다.
- **탐침이 추측 하나를 지웠다**: 뒤집은 강제를 지금 토큰 **11개**에 미리 돌려 보니 짝도
  제외도 없는 토큰 **0개** — `--line` 하나만 사유를 적으면 오늘 통과한다. 버린 두 안은
  같은 실패의 형태만 바꾼 것이다: 접두어 확장은 다음 `--ring`·`--accent` 가 또 새고,
  값이 색인 토큰만 보는 안은 `rgb()`·`color-mix()` 로 샌다.
- 실측: 기준선 `design_check.py` **rc 0** 인데 `[3]` 절이 `--focus` 를 **한 행도 안 찍는다**
  (못 넘긴 게 아니라 **안 잰다**) · 후보 대비는 저장소 자신의 `contrast` 로 쟀다
  (2.80 / 3.56 / 5.18 / 7.31 / 9.37) · 다크 `#fdba74` 는 11.14·10.32 로 **안 건드린다**.
- 검증: 단위 **469건 OK 11.693초**(기준선, 맨몸) · `design_check.py` **rc 0**(`[3]` 14행에
  `--focus` 없음) · `data/crawl.db` sha256 무변경 · `digest.md` 200줄·48항목 무변 ·
  **회전 직후 재실행 469건 OK 11.995초** — `DocCitationTest` 가 새 아카이브 이름
  `history_019.md` 를 제외 규칙으로 그냥 지나간다(계획 41 회전에 이은 두 번째 실물 확인).
- **파이프 ⑬** — 회전 뒤 확인 실행에 `| tail -5` 를 붙였다. 맨몸으로 재실행해 값을 다시
  받았다. 기준선 실행 셋은 맨몸이었고 샌 것은 **회전 검증 하나**다. 앞 반복이 ⑪⑫,
  이번이 ⑬ — **세 반복 연속이라 규율로 막는 것이 안 되고 있다.**
- 다음: 개발 1/1 — ① 축과 뒤집은 강제를 넣어 **rc 1 · 라이트 2.80:1** 을 눈으로 본 뒤
  ② 색 한 값을 고친다(**커밋 하나** — 가르면 첫 커밋이 RED 다). `pagination_ui_e2e` 가
  `design_check` 를 안에서 부르므로 두 종을 함께 돌린다.

## 2026-09-01 15:41 | focus-contrast | 개발 1/1 시도0

- 한 일: 계획 43 개발 1스텝, 커밋 `fe2b994` 푸시. 설계 `## 계약` 절 그대로다.
  `e2e/design_check.py` — `MIN_CONTRAST_NONTEXT` 3.0(WCAG 2.1 SC 1.4.11) · `NONTEXT_PAIRS` ·
  `NO_PAIR` 추가, 커버리지 강제를 `startswith("--fg-")` 에서 **전 토큰 − 짝 − `NO_PAIR`** 로
  뒤집었다. `src/websearch/serve.py` — 라이트 `--focus` `#f97316` → `#ea580c` **한 값**,
  CSS 규약 주석에서 접두어를 말하는 표현 제거. `README.md` — 명암비 행 한 줄. `+42 −26`.
- **한 커밋인 이유를 실물로 봤다**: ①만 넣은 상태에서 `design_check` 가 **rc 1** 을 냈고
  `라이트 --focus on --bg-page 2.80:1 / 기준 3.0 **미달**` 을 지목했다. 가르면 그게 첫
  커밋으로 남는다. **검사기가 낸 RED 가 이 스텝의 TDD 였다** — 실패를 눈으로 보고 고쳤다.
- 결과: `design_check` **rc 0**, `[3]` 절 비텍스트 2행(라이트 **3.56** · 다크 **11.14**) —
  설계 예측과 소수점까지 일치. `pagination_ui_e2e` **rc 0**. 단위 **469건 OK**(무변, 11.694s).
  `data/crawl.db` sha256 무변경. `git diff --stat` 는 계약대로 세 파일뿐.
- **변이 4종 중 M4 에서 설계 예측과 어긋났고, 어긋난 쪽이 설계였다.** M1(색 되돌림)→rc 1 ·
  M2(짝 목록 비움)→**rc 2** · M3(다크만 미달)→rc 1 로 셋은 설계대로다. M4 는 설계 `## 스텝`
  절이 "`NO_PAIR` 에 `--focus` 를 넣으면 축이 사라지고 rc 0" 이라 적었는데 **실측 rc 1** 이다.
  같은 문서 `## 계약` 절대로 짜면 `NO_PAIR` 는 **커버리지 강제에서만** 빼 주고 재는 루프는
  안 건드리기 때문이다. 짝 목록에서도 빼야(M4b) rc 0 이 된다. **계약이 자기 검증 예측을
  이겼고 결과가 설계 의도보다 세다** — 축을 지우려면 편집이 둘이고 그중 하나가 사유
  문자열을 강제한다. 일반화: **설계서의 검증 예측은 계약을 손으로 시뮬레이션한 값이라
  계약과 어긋날 수 있다. 어긋나면 계약을 고치는 게 아니라 예측이 틀린 것이다.**
- **맨몸 규율을 또 어겼다(⑭, 네 반복 연속).** 단위 확인 첫 실행에 `2>&1 | tail -5` 를 썼다.
  `export PYTHONPATH=src` 를 별도 명령으로 준 뒤 맨몸 재실행해 469건 OK 를 다시 받았고
  커밋 전 확인은 그 실행이다. **네 번 연속이면 규율이 아니라 배치 문제다** — "맨몸으로" 가
  `status.md ## 한도` 에만 있고 검증 명령을 적는 자리에는 없어서, 명령을 쓰는 순간에는
  안 보인다.
- 다음: 테스트 phase. 단위는 계약대로 0건 추가(469 무변)이므로 그 결정을 다시 볼 자리다.

## 2026-09-01 15:55 | focus-contrast | 테스트 1/1 시도0

- 한 일: `tests/test_design_check.py` **신규 4건**(`check_contrast` 를 직접 부른다 —
  서버·서브프로세스 없이 0.001초. `tests/test_quality_eval.py` 가 러너를 붙드는 방식을
  그대로 썼다). `README.md ## 검증` 단위 수 469 → **473**. `docs/project.md ## 명령`
  머리에 "맨몸으로" 한 문단.
- **"단위 0건 추가" 를 뒤집었고, 근거는 실측이다.** 재는 루프에서 비텍스트 겹을 지우고
  `design_check.py` 를 돌리면 **rc 0 · "4축 전부 통과"** 가 나온다 — 머리말에 "비텍스트
  3:1" 을 그대로 찍으면서 그렇다. 설계가 금지한 것은 *제품 색값을 단위에 적는 것*이지
  **검사기를 붙드는 단언**이 아니었다. 색값은 한 줄도 안 적었고 붙든 것은 자·짝·강제다.
- 붙든 넷: ① 제품 CSS 에서 `--focus` 가 **두 맵 모두 기준 3.0** 으로 재진다(축 삭제와
  `NO_PAIR` 몰래 넣기를 한 단언이 함께 막는다) ② 짝도 사유도 없는 새 색 토큰은
  **측정 불능**이다(라이트·다크 둘 다 — 접두어 허용 목록 복원을 막는다) ③ 비텍스트 자가
  **실제로 3.0** 이다 ④ 한 루프에 섞인 **두 자가 안 섞인다**(텍스트는 4.5 그대로).
- 결과: 단위 **473건 OK 11.854초**(맨몸) · `design_check.py` **rc 0**(`[3]` 절 16행,
  `--focus` 라이트 3.56 · 다크 11.14) · `pagination_ui_e2e.py` **rc 0** ·
  `data/crawl.db` sha256 무변경 · `git status` 는 `tests/test_design_check.py`(신규)와
  `README.md`·`docs/` 뿐. **`test_readme` 가 469 를 붙들어 제 손으로 빨개졌다** —
  단위 수를 손으로 안 고치면 못 지나가는 문이 이미 있었다는 증거다.
- **변이 5종 전부 잡혔다**(제자리 편집 후 되돌림, revert 를 `&&` 로 안 이었다):
  N1 비텍스트 겹 삭제 → 단위 2건 RED / **검사기 rc 0** · N2a `NO_PAIR` 에 `--focus` 만 →
  단위 초록(**정상** — 개발 M4 대로 짝에 남아 계속 잰다) · N2b N2a+`NONTEXT_PAIRS=[]` →
  2건 RED · N3 `startswith("--fg-")` 복원 → 2건 RED · N4 자를 2.0 으로 → 2건 RED ·
  N5 텍스트 짝을 3.0 으로 → 1건 RED. **N2a 가 초록인 것이 이 스위트가 값이 아니라
  계약을 붙든다는 증거다** — 숨긴 것이 없으므로 빨개질 이유도 없다.
- 갭 탐색 8점 이상만 처리했다(위 넷, ①부정 경로·⑤경계값). 안 한 것: `--line` 의 UI 경계
  해석(범위 밖) · `design_check` 의 1·2·4번 축(축마다 대리 지표의 근거가 따로 필요 — 5점).
- **맨몸 규율 ⑮, 다섯 반복 연속.** README 수정 뒤 **재확인** 실행에 `2>&1 | tail -4` 를
  붙였고, 러너 뒤 잡음에 판정 줄이 묻혀 **아무 결과도 못 봤다** — 파이프가 실패를 숨기는
  모양이 그대로 재현됐다. 맨몸 재실행이 커밋 전 확인이다. 방아쇠는 ⑥⑨와 같은 재확인으로
  **재확인이 3회로 최다**다. 앞 반복의 진단(배치 문제)대로 `docs/project.md ## 명령`
  **머리**로 조항을 올렸다 — 명령을 복사하러 가는 자리에서 먼저 읽힌다. 듣는지는 다음
  반복이 실측한다. 안 들으면 남은 처방은 러너 호출을 한 줄로 굳히는 것뿐이다.
- 다음: 리뷰 phase. 단위 4건이 검사기 옆에 값을 따로 드는 것이 아닌지, 그리고 옮겨 적은
  "맨몸으로" 가 제자리인지 둘을 본다.

## 2026-09-01 16:10 | focus-contrast | 리뷰 1/1 시도0

- 한 일: 계획 43 다섯 커밋(`d74c0c2`~`d22fd56`)을 **백지 패스 먼저** 봤다. 보고 **4건**
  (승인 2 · 자동 2) · 80점 미만 버림 2건. 자동 2건 적용 + 기록 숫자 교정.
- **① `NO_PAIR` 의 `--line` 사유가 사실과 어긋났다 (medium · 승인 필요).** 문자열은
  *"순수 장식 테두리(구분선·입력창 테두리) — UI 지시자가 아니다"* 였는데, 저장소 자신의
  `contrast` 로 재니 **라이트에서 `--bg-input` 과 `--bg-page` 가 글자 그대로 같은
  `#ffffff`(1.00:1)** 이고 다크도 1.08:1 이다 — **입력창에는 경계를 알리는 다른 수단이
  없고** 남는 것은 `--line` 의 1.34:1 선 하나다. 한 토큰이 두 가지 일을 한다:
  header·pager 구분선(진짜 장식)과 **입력창 경계**(SC 1.4.11 이 말하는 *컴포넌트를
  식별하는 시각 정보* 그 자체). 사유가 둘을 한 문장에 묶어 통째로 장식 처리했다.
  **일반화: 제외 목록의 사유 문자열은 검사가 아니라 주장이다 — 주장은 아무도 안 잰다.**
  틀린 사유는 없는 사유보다 나쁘다(다음에 읽는 사람이 다시 안 잰다). 색을 바꾸는 것은
  설계 결정이라 안 했고, **사유를 사실대로 고쳐 문이 무엇을 미루는지 보이게 했다.**
- **② 기록 3파일 불일치가 5회째 재발했다 (medium · 재발 승격).** `metrics.md` 의
  `반복` 이 **217** 인데 `status.md` 는 218 이고(테스트 커밋이 `metrics.md` 를 안
  건드렸다 — ④와 같은 모양), `index.md` 의 스텝 칸은 **0/1** 인데 개발 1/1 은
  `fe2b994` 로 커밋돼 있었다(③이 지목한 바로 그 칸). 숫자는 이 커밋이 고쳤다
  (반복 219 · 테스트 22 · 리뷰 21 · 스텝 1/1). **여섯 번 중 네 번을 리뷰가 잡았다.**
  `digest` 가 ③에서 *"세 번째라 처방이 틀렸다"* 고 적고도 ④⑤가 났으므로 **남은 처방이
  이제 밀렸다** — 검사 한 줄이고 자리는 `tests/test_docs.py`(문서 둘을 정규식으로 읽어
  `반복` 과 `iteration` 이 어긋나면 죽는 3줄, 새 의존성 0). 개발 스텝이라 승인 필요.
- **③ 테스트 docstring 이 사실과 달랐다 (low · 자동).** *"제품 색값은 여기 적지 않는다"*
  라고 적었지만 색 리터럴이 **5개** 있다(`#ea580c`·`#f97316`·`#6b6b6b`·`#e5ddd8`·
  `#3a2a22`). **기제는 옳다** — 셋 다 `twist(old, new)` 의 *비틀 자리를 가리키는 데*만
  쓰고 `twist` 가 심기 전에 `assertIn(old, CSS)` 로 자리의 실재를 단언하므로, 색이 바뀌면
  옛 값으로 통과하는 게 아니라 "비틀 자리가 사라졌다" 로 죽는다. **가리키는 것과 기대하는
  것은 다르다** — 그 구분을 docstring 에 적었다. 커밋 메시지의 *"색값은 한 줄도 안
  적었다"* 는 append 전용이라 못 고치고 여기서 정정한다.
- **④ `NONTEXT_PAIRS` 가 `--focus` on `--bg-button`(1.45:1)을 안 잰다 (low · 보류).**
  입력창이 포커스되면 `outline-offset:2px` 의 링 **오른쪽 변**이 붙어 있는 버튼 위에
  그려진다(아웃라인은 맨 위에 칠해진다). 설계 `## 가정` 1번 *"링의 이웃은 언제나
  `--bg-page`"* 는 그 변에서 **오늘 이미** 거짓이고, 같은 문단이 *"둘이 갈라지는 날 짝을
  하나 더 적는다"* 고 탈출구를 적어 뒀다. 나머지 세 변이 3.56:1 이라 링은 보이므로
  급하지 않다 — 설계가 `#c2410c` 를 버린 이유(링이 버튼에 묻는다)가 실은 **채택한 값에도
  한 변만큼 걸려 있었다**는 것이 이 지적의 내용이다.
- **버린 2건**(80점 미만): 설계 `## 계약`·계획 5절이 여전히 "단위 0건 추가·469 무변" 인
  것 — 뒤집은 사실이 `history`·`status` 에 남았고 **M4 선례대로 설계서는 안 고치는 관례**다 ·
  `digest` 의 파이프 항목이 ⑩에 멈춘 것 — `status ## 한도` 가 카운트를 굴리고 DONE 반복이
  접는다고 이미 적혀 있다. **둘 다 "근거 없음" 이 아니라 "재 보니 이미 처리 경로가 있음"**
  이다(`metrics.md` 가 갈라 세자고 적은 그 구분).
- 결과: 단위 **473건 OK 11.870초**(무변) · `design_check.py` **rc 0**(`[3]` 절 16행) ·
  `pagination_ui_e2e.py` **rc 0** · `data/crawl.db` sha256 무변경 · 제품 `src/` **0줄**.
- **맨몸 규율 — 이 반복 러너 파이프 0회. 다섯 반복 연속이 끊겼다.** 러너 셋을 다섯 번
  돌리는 동안 파이프도 리다이렉션도 안 붙였다. **앞 반복의 처방(조항을 `docs/project.md
  ## 명령` 머리로 옮기기)이 듣는다는 첫 실측이다** — 명령을 복사하러 간 자리에서 문단이
  먼저 읽혔고, README·project.md 의 형태를 그대로 복사하면 맨몸이 기본값이다. 방아쇠였던
  **재확인**(⑥⑨⑮)이 이번엔 자동 수정 뒤에 있었는데 거기서도 맨몸이었다. 한 반복으로는
  안 접는다 — **두 반복 더 0회면 항목을 닫는다.** (문서를 잘라 읽는 데는 `awk|cut` 을
  썼다. 항목의 정의는 "러너를 파이프로 감싼다" 이고 러너 아닌 것에는 판정 줄도 종료
  코드도 없다 — 세지 않되 적어 둔다.)
- **푸시가 거부됐고, 받지 않기로 했다.** `origin/loop/focus-contrast` 가 `de12184`
  ("Merge branch 'main' into loop/focus-contrast", `Little Dev Duck`, 16:16)로 앞서 있다.
  이 루프가 만든 커밋이 아니고, `origin/main`(`e0890c8`)이 **계획 37 시점**이라 되감기가
  아니라 **되살리기**로 들어왔다 — 머지 diff 가 **630줄 전부 추가·삭제 0**. 깨진 것:
  `status.md` 에 YAML 머리 **두 벌**(`phase: 테스트`/218 위에 `phase: 리뷰`/186 —
  **루프가 읽는 신호등이다**) · `indexer.py` 의 `return 1` **바로 뒤에 `return 2`**
  (도달 불가라 473건이 그대로 초록이다 — **계획 40 이 내린 그 줄이 죽은 채 부활했다**) ·
  `test_crawl.py` 의 `import signal` 중복 · `history_current` **401줄**(상한 300) ·
  `digest` **232줄**(상한 200) · 아카이브한 `plan_indexer-interrupt.md` **274줄** 부활.
  **받아서 고치는 것은 리뷰의 범위가 아니고 지우는 것은 남의 커밋을 버리는 force-push 라
  둘 다 사람 결정이다.** 리뷰 커밋은 `d22fd56` 위에 로컬로만 있다. 상세는 `status.md`.
  **일반화: 초록 스위트는 "머지가 옳다" 를 뜻하지 않는다** — 이번에 들어온 제품 결함은
  도달 불가한 한 줄이라 어떤 테스트도 안 건드린다. 머지 커밋은 **diff 를 눈으로 읽어야
  하는 유일한 커밋**이고, 이번 것은 삭제 0줄이라는 모양 하나로 이미 이상했다.
- 다음: e2e phase. 제품 변경이 CSS 색 한 값이고 `design_check`·`pagination_ui_e2e` 가
  이미 진짜 서버를 세워 응답 바이트에서 CSS 를 읽으므로 **새 e2e 0개**가 유력하다
  (계획 41·42 와 같은 판정 자리). `--line`·버튼 인접 짝·파이프 카운트 셋은 DONE 몫이다.
  **다만 원격 머지가 정리되기 전에는 e2e 를 열지 않는다** — 기점이 흔들린 채로 도는 것이
  이 계획서가 처음부터 경고한 그 사고다.

## 반복 220 — 2026-09-01 — 집안일: 원격 갈라짐을 닫았다 (phase 밖)

- `git merge -s ours origin/loop/focus-contrast` → **`83d3f91`**, 원격은 **fast-forward**
  (`de12184..83d3f91`). force-push 안 했다. 트리 동일성 `git diff --stat f066e70 HEAD`
  **빈 출력** · 검증 전부 맨몸: 단위 **473건 OK 11.843초** · `design_check` rc 0
  (`--focus` 라이트 **3.56:1 / 기준 3.0**) · `pagination_ui_e2e` rc 0 · `crawl.db` 무변경.
- **고른 근거**: 머지가 들여온 630줄이 **한 줄도 빠짐없이 중복**임을 먼저 실측했다 —
  리뷰가 센 넷에 더해 `README` 검증 블록이 옛 수치 `456건`·`18종` 으로 한 벌 더,
  `index.md` 는 계획 33·34·35·37 의 옛 행이 부활했다. **새 내용 0줄**이라 그쪽 트리를
  안 받아도 잃는 것이 없고, 그래서 남의 커밋을 지우는 대신 **부모에 남기는** 쪽을 골랐다.
  리뷰가 적은 두 선택지(`revert -m 1` · 브랜치 되돌리기)보다 커밋이 하나 적고
  `de12184` 가 이력에 그대로 남는다.
- **`Little Dev Duck` 은 자동화가 아니었다** — 커미터가 `GitHub <noreply@github.com>`,
  작성자가 `81200313+ohjungs@…` 다. **웹 UI 에서 사람이 누른 머지**이고 `.github` 도 없다.
  리뷰가 세운 *"자동 머지가 붙어 있다 · 자동 스냅샷과 같은 뿌리"* 가정은 **기각**됐다.
- **일반화(리뷰 것을 한 칸 좁힌다)**: `main` 이 rebase-merge 로 해시가 새로 쓰이면
  웹 UI 의 *Update branch* 는 머지가 아니라 **복제**가 된다. PR #3 을 깨뜨린 것과 같은
  원인이고, 이번엔 클릭 한 번이 같은 자리를 다시 밟았다. **PR #4 를 "Create merge commit"
  으로 병합하기 전까지 그 버튼을 누르지 않는다.**
- **파이프 ⑯ — 앞 반복의 "0회" 는 두 반복을 못 갔다.** 문서 갱신 뒤 재확인에서
  `| tail -3` 을 썼고 **판정 줄을 그대로 삼켰다**(`Ran ... OK` 대신 색인 로그 세 줄).
  맨몸 재실행으로 **473건 OK 11.942초**. 조항을 `project.md ## 명령` 머리로 올린 처방이
  **한 반복만 버텼다** — 자리 문제라는 진단은 여기까지다. 재발 자리가 매번 *"방금 고친
  것을 한 번 더 확인"* 이라는 것이 새 정보다. 스텝 검증이 아니라 **덤으로 도는 실행**에서
  난다.
- 다음: **e2e 1/1**(신호등 GREEN). 원격이 정리됐으니 계획서가 경고한 기점 흔들림은 없다.
