---
signal: GREEN
phase: 리뷰
step: 7
attempt: 0
iteration: 272
updated: 2026-09-02
ctx: 68
night_iterations: 114
night_red: 1
night_retries: 0
plan: passage-api
---
# 현재 상태

**초록불이 가리고 있던 구멍 셋을 변이로 찾아 검사 3건으로 막았다. 제품 결함은 0건이다.**
활성 계획 `passage-api` · 설계서 `docs/design_passage-api.md` · 브랜치 `loop/passage-api` ·
기점 `6e99031`. **단위 576건 전부 초록**(맨몸 단독 실행 확인, 13.4초) · 제품 `src/` **무변경**
(`git diff -- src/` 빈 것으로 확인) · 새 파일 0 · 의존성 0 · 스키마·마이그레이션 없음 ·
`docs/specs/`·`data/crawl.db` 무변경 · `.mutation-lock` **0**.
`passage_eval` 정확도 **100.0%(398/398)** · 채택률 99.5% · p50 1.29ms · p95 **1.42ms**(예산 500ms) ·
`quality_eval` 한국어 20/20 · 영어 19/20(합격선 80%).

## 이번 반복이 한 일 (테스트 5)

**갭 탐색이다 — 코드가 아니라 검사를 쟀다.** 제품 `src/` 는 한 줄도 안 건드렸다.

**개발 10 이 남긴 셋을 먼저 확인했다.** ① 동점 키 삭제는 이미 9건이 잡는다(재현 완료)
② **리뷰 5 가 「길이」를 반려한 사유는 표에 그대로 살아 있다** — `_UNMATCHED_SHAPES` 4행이
`assertNotEqual(want, today)` 로 «아직 틀린다» 를 못박고 있고, 맞게 되는 날 그 검사가 빨개져
다음 사람이 표를 고치게 된다 ③ `extract.py` 재탐침에서 **새 구멍 하나**가 나왔다(아래 B).

**변이 다섯 — 둘 즉사, 셋 생존.** 전부 `.mutation-lock` 을 걸고 맨몸·단독으로 돌렸고, 원복은
초록이 아니라 **`git diff -- src/`** 로 확인했다. **커밋된 변이 0.**

| 변이 | 자리 | 판정 | 구멍 |
|---|---|---|---|
| A | `extract.py` 스택 push 를 `super()` 앞으로 | 사망(2) | — |
| **B** | `extract.py` `del self._open[i:]` → `[i]` | **생존** | 바깥이 닫힐 때 안쪽의 안 닫힌 태그를 안 걷으면 뒤 텍스트가 **끝난 태그의 이름표**를 단다 |
| **C** | `serve.py` `"title": title` → `""` | **생존** | `/passages` 의 `title` **값**을 아무 검사도 안 쟀다 |
| **D** | `indexer.py` 2-gram 바늘 삭제 | **생존** | 띄어쓰기 변형 문서의 모든 블록이 0점 → **문단이 통째로 사라진다** |
| E | `indexer.py` 동점 키를 「길이」로 되돌리기 | 사망(9) | — |

**B 는 실물 HTML 의 기본형이다** — `<li>`·`<p>` 는 종료 태그가 선택이라 `<ul><li>가<li>나</ul>다`
가 흔하다. **텍스트는 맞고 이름표만 틀려서 눈으로는 안 보인다.**
**D 는 사양을 직격한다** — `/search` 는 주면서 `/passages` 는 빈손이 되는 모양이라
«근거 채택률» 이 조용히 깎인다.
**C 는 조용한 품질 손실이다** — 소비자가 근거를 인용할 때 사람에게 보이는 것은 URL 이 아니라
제목이다. 200 검사는 키 **집합**만 보고, 위임 검사는 url·position·text 만 비교하고 있었다.

**검사 3건으로 셋을 죽였다** — 전부 **RED 를 먼저 보고** 넣었다.
`test_closing_an_outer_tag_also_sweeps_the_unclosed_tags_inside_it`(마지막 블록의 이름표가 `""`) ·
`test_title_is_the_document_title_not_a_placeholder`(url → title 사전 전체 비교) ·
`test_a_spacing_variant_document_still_gets_a_passage`(`김치 찌개` 문서가 `김치찌개` 질의에 문단을 낸다).
**573 → 576건**, `README.md` 의 카운트도 같이 올렸다(`test_readme` 가 강제한다).

**제품 결함은 0건이다.** 생존한 셋은 전부 **검사의 구멍**이지 코드의 버그가 아니었다 —
세 자리 모두 맞게 동작하고 있었고, 다만 틀려도 아무도 안 잡는 상태였다. 리뷰에 넘길 결함이 없다.

## 넘긴 것 — 갈림길 6 은 여전히 열려 있다

**닫는 조건은 갈림길 5 의 캡과 같다: 실물 크롤 코퍼스다.** 손잡이 셋(점수만·이름표·길이)
중 어느 것도 19행을 다 못 맞추고, **19행 표도 내가 만든 자라 정답표가 아니다.**
`data/crawl.db` 는 2문서 × 66자, e2e 코퍼스는 한 문장짜리 `<p>` 뿐이라
«보일러플레이트가 `<p>` 로 싸이는 빈도» 를 저장소가 못 잰다.
**설계서 갈림길 6 은 열린 채로 두고, 오늘의 선택과 그 이유를 거기 적었다.**

**범위는 그대로 못박혀 있다** — 인라인 연접(`김치찌개된장찌개`) · 갈림길 5 의 캡 ·
`_SKIP_TAGS` 를 색인 경로에서 빼는 것은 **계획 48 에서 다시 열지 않는다**(`digest` 후보).
다음은 **리뷰 7** 이다. 그 뒤 **e2e 20종 전수로 보내 DONE 으로 닫는다.**

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-02 확인)

`origin/main` 은 PR #6 으로 계획 44~47 까지 받았다. 계획 48 의 커밋들은 브랜치에만 있다.
**이번 반복도 `origin/loop/passage-api` 로 푸시했다** — 푸시는 루프의 정상 절차다
(강제 푸시만 금지).
**남은 것 (사람 결정)**: 계획 48 이 끝난 뒤 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 — 넷이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, `~/.claude/skills/loop-harness/rules/docs.md` 에는 그 예외가 없다.
   **저장소 밖이라 루프가 못 고친다.**
4. **«러너 호출을 다른 것과 섞지 않는다» 를 문장이 아니라 훅으로 옮기는 일.**
   **재발 26 → 27회 — 이번 반복도 한 번 밟았다.** 변이 다섯의 판정은 전부 맨몸·단독으로
   돌려 놓고, **마지막 전수 확인에서** `2>&1 | tail -4` 를 붙였다. 판정 줄이 잘려 화면에
   크롤 로그 네 줄만 남았고, 즉시 맨몸으로 다시 돌려 `Ran 576 tests ... OK` 를 읽었다.
   **방아쇠는 27번째도 「출력이 길다」다** — 직전 반복이 밟은 자리를 알고도 같은 자리에서
   같은 손이 나왔다. 크롤 로그가 화면을 덮는 것이 이 스위트의 상수고, 그것을 가리려는
   손이 매번 판정 줄까지 같이 가린다. **문장으로는 스물일곱 번 못 막았다.**
   처방은 이미 판정해 뒀다: `unittest` 의 **`-b`** 가 방아쇠를 뿌리에서 없앤다
   (실측 약 90줄 → 5줄, 거른 테스트 0, 새 코드 0). `digest.md ## 다음 계획 후보` 에
   **`[9]`** 로 등재돼 있다. 단서 — `-b` 는 테스트가 직접 낸 stdout/stderr 만 버퍼링하므로
   하위 프로세스가 fd 로 직접 쓰는 로그는 샌다. 판정 명령을 단독 실행으로 강제하는
   PreToolUse 검사는 여전히 **저장소 밖 몫**이다.

**사양이 사람에게 남긴 것 셋** (`docs/specs/concept.md` 의 `## 사람이 정할 것`):
속도 제한 시점(가정 — IP 당 분당 60회) · JSON 검색과 근거 문단의 분할(가정 — 쪼갠다,
**계획 46 이 그 가정을 따랐고 닫았다**) · 사양 숫자들이 초안이라는 것(90% · 500ms ·
60회/분). 첫째와 셋째는 아직 열려 있다.
