# e2e 결과 — 계획 44 `focus-ring-presence` (2026-09-01 · 반복 227)

브랜치 `loop/focus-ring-presence` · 기점 `bc40ea9` · 새로 만든 e2e 파일 **0개**.
전부 맨몸 실행이다 — 파이프·리다이렉션 **0회**(러너 기준).

## 1. 선행 관문 셋 (`rules/e2e.md` 1절)

| 관문 | 명령 | 결과 |
|---|---|---|
| 전체 검증 | `PYTHONPATH=src python3 -m unittest discover tests` | **475건 OK 11.892초** · 실패 0 |
| 린트·타입체크 | `docs/project.md` 에 **없음** | 해당 없음(있다고 쓰지 않는다) |
| CI | `.github` **부재** | 해당 없음 — 1절 2항 |
| 품질 기준 | 아래 4절 | 전 축 무변 |

## 2. 이 계획의 e2e 시나리오 — **프로세스 경계에서 rc 2 를 처음 밟았다**

단위는 `check_contrast` 를 **함수로** 부르고 `unmeasurable` 리스트만 본다. 설계
`## 갈림길 A` 의 결론(*"잴 것이 없어진 검사는 조용히 통과하면 안 된다" → 종료 **2***)은
`main()` 의 `return 2` → `sys.exit` 를 거쳐야 참이 되는데 **아무도 그 경계를 안 밟았다.**
링을 죽인 CSS 를 제품(`src/websearch/serve.py`)에 심고 진짜 프로세스로 잰다.

`.mutation-lock` 을 걸고 했고 끝나고 지웠다. 되돌리기는 `&&` 로 잇지 않았다.

| # | 심은 것 (제품 CSS) | 기대 | 실측 |
|---|---|---|---|
| 기준선 | 없음 | rc 0 | **rc 0** · `포커스 링 규칙 1개 · outline var(--focus) · offset 2px` · 라이트 `--focus` **3.56:1** |
| M1 | `outline:3px solid var(--focus)` → `outline:none` | rc 2 | **rc 2** · `측정 불능 1건: outline 이 색 토큰을 안 쓴다 (none)` |
| M2 | `outline` 선언을 통째로 삭제(`{outline-offset:2px}` 만 남김) | rc 2 | **rc 2** · `측정 불능 1건: outline 을 정하는 규칙이 0개 — 캐스케이드를 바이트로 못 정한다` |

**`3.56:1` 이 화면에 없다** — M1·M2 둘 다 `[3]` 절이 **14행**(텍스트 7짝 × 2맵)이고
비텍스트 2행과 요약 줄이 사라진다. 무효가 된 숫자를 안 찍는 것이 계획의 요구였고,
그것이 **프로세스 표준출력에서** 참이다. 텍스트 7짝은 양쪽 다 그대로 잰다(설계대로).

되돌린 뒤 `git status` 깨끗 · `design_check.py` **rc 0**(요약 줄 복귀 · 3.56:1 복귀).

## 3. 회귀 — `e2e/*.py` **19종 전수 rc 0**

`crawl` 15p/최소 1.001s · `crawl_delay` 2.01s/1.00s · `crawl_politeness` 2.01s/1.00s/1.01s ·
`deadline` 8p/2.1s · `design_check` 4축 통과 · `domain_key` 4회 2.01s·robots 1회 ·
`indexer` · `indexer_interrupt` rc 130/2000행 · `interrupt` rc 130 10.0s ·
`noindex` 4→2문서 · `non_ascii` · `pagination_ui` 11문서 · `perf_crawl` ·
`perf_search` · `quality_eval` · `retry_interval` 5.01s · `search_api` · `tokenizer` ·
`url_normalize` 표기 10 → 문서 6.

## 4. 품질·성능 축 — 무변

- 검색 품질: ko **20/20 (100%)** · en **19/20 (95%)** · 순위 1위 39 · 미검출 1(`loaf`) ·
  매치 평균 **14.0** 최소 11 최대 28 — 기준선 그대로
- 성능: `perf_search` p95 **8.90ms**(예산 300ms 의 3.0%) · `search_api` p95 2.13ms
- 처리량: [열림] **10.22/s** · [차단] **10.22/s** · 문서집합 sha1 `541d455a…c078654b`
- 디자인 4축: 외부 서브리소스 0 · JS **0 B** · 최저 대비 **3.56:1**(비텍스트 기준 3.0) ·
  360px 초과 고정폭 0건
- `data/crawl.db` sha256 **무변경** (`85c96744…5bda18`)

## 5. 8단계 체크리스트 — 해당 없음

브라우저 자동화가 없다(의존성 추가 금지). `design_check.py` 가 로컬 서버(포트 0)의
**실제 응답 바이트**로 1(시각·대리)·5(빈 상태)·6(외부 요청 0)·7(360px 대리)을 덮고,
2·3·4 는 `pagination_ui_e2e`·`search_api_e2e`·`tokenizer_e2e` 가 화면 HTML 로 덮는다.
**못 잡는 것은 그대로다** — 브라우저 레이아웃·페인트, 실제 키보드 포커스의 시각적 결과.
이 계획이 좁힌 것은 그 대리 지표의 **전제**(링이 그려지긴 하는가)이지 대리 자체가 아니다.

## 6. 새 e2e 파일 0개 — 근거

제품 `src/` 는 **0줄** 변경이고 이 계획이 만든 것은 검사기의 눈 하나다. e2e 가 사는
자리 중 걸리는 것은 "진짜 프로세스의 종료 코드" 하나인데 `design_check.py` 자신이
그 프로세스다 — 위 2절이 그것을 CSS 변이로 재면 끝이고, 새 파일을 만들면 19종이
20종이 될 뿐이다(계획 41·42·43 과 같은 판정 · ponytail 1번).

## 7. 남은 것

- **`digest [6]`(새 변이 넷 — at-rule 위치·셀렉터 극성)은 이 계획이 안 연다.** 설계
  `## 범위 밖` 이 명시했고 승인이 필요한 개발 스텝이다.
- **PR 은 안 연다** — `status.md ## 기점`(PR #5 는 병합됐으나 이 브랜치는 `main` 기점이
  아니다)이 그대로다. 사람 결정.
