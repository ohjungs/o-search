---
signal: GREEN
phase: DONE
step: 1
attempt: 0
iteration: 197
updated: 2026-09-01
ctx: 44
night_iterations: 64
night_red: 0
night_retries: 0
---

# 현재 상태

**계획 39 `indexer-lock` 리뷰 끝(개발 1·2 + 테스트 1 + 리뷰 1/1 완료)** — 계획서 `docs/plan_indexer-lock.md`.
브랜치 `loop/indexer-lock`(기점 `bf78d02`, `loop/digest-rotate` 에서 팠다).
**계획 38 까지 전부 DONE·아카이브 완료.** 리뷰 지적 **5건 · 제품 동작 지적 0건 · 자동 수정 1**
(테스트 한 건). 단위 461 → **462건 OK** · e2e 19종 그대로 · `src/` 는 리뷰에서 **0줄**.
**다음은 DONE 처리** — 계획서를 `plan_history_025.md` 로 아카이브하고 `index.md` 를 완료로.
계획 34~37 은 PR #2 로 `main` 에 병합됐다(`main` 최신 `e0890c8`) — 38·39 의
`main` 병합은 사람이 정한다.

## 방금 한 것 (2026-09-01 · 리뷰 1/1)

**백지 패스를 먼저 끝내고 대조 패스를 열었다. 물음 셋에 전부 실측으로 답했다.**

### ① rc 2 충돌 — 결론은 유지, 근거는 틀렸다

**바꾸지 않는다.** 다만 앞 스텝이 적은 근거("이 저장소에서 rc 2 는 이미 관례다")는
**전수 조사가 아니었다** — `serve.py:329-333` 이 정반대 규칙을 주석으로 적어 뒀다:
EADDRINUSE·EACCES 를 두고 *"사용자 입력이 아니라 환경이라 usage(2)가 아니고 … rc 1"*.
즉 이 저장소의 관례는 **하나가 아니라 둘**이고, `indexer`(21·24·26·27·29 + 39)와
`crawl.py:380` 은 환경 오류를 rc 2 로, `serve` 는 rc 1 로 낸다.

바꾸지 않는 이유는 그대로다 — 세 모듈의 계약과 `e2e/tokenizer_e2e.py:153`(옛 색인 rc 2)을
한꺼번에 건드리는 일이고 계획 39 의 한도(제품 diff 는 `indexer.py` 한 파일) 밖이다.
39 는 있던 관례를 **따랐지 만들지 않았다**. `digest.md ## 다음 계획 후보` 로 넘겼다(세 CLI 종료
코드 통일 + "락은 재시도하면 풀리는 오류인데 손상 DB 와 같은 값" 축).

### ② 락 e2e — 붙이지 않는다 (계획서 7절 유지)

7절이 정한 반증 신호("단위로는 못 재는 자리가 나온다")는 **울렸다**(아래 ③). 그런데
**e2e 도 그 자리를 못 잰다** — 못 재는 이유가 계층이 아니라 물리다(WAL 에서 읽기는
락에 안 막힌다. 실측: 락을 쥔 채 `--query` **0.02초 rc 0**, `timeout` 을 지운 사본에서도 같다).
30초를 넘기는 갈래는 정의상 **벽시계 30초 이상**을 써야 재지는데, 그것을 e2e 로 옮기면
e2e 19종에 35초가 붙는다. 30초 이하 갈래는 이미 단위가 **진짜 프로세스 둘·진짜 8초 락**으로
잰다 — e2e 가 더하는 것은 argv·서브프로세스 경계뿐이고 그건 `indexer_e2e.py` 가 이미 덮는다.

**대신 값을 하나 쟀다: 그 8초 테스트가 단위 스위트의 68% 다**(단독 8.04초 / 전체 11.67초).
루프는 스위트를 매 스텝 돌리고 e2e 는 계획당 한 번이라, "느린 것은 e2e 로" 가 맞는 방향일 수
있다. 지금 옮기지 않는 이유는 옮기면 **행동 검증이 계획당 한 번으로 줄기** 때문이다 —
후보로 남긴다(`digest.md`).

### ③ `_doc_count`(136)·`search`(181) 의 `timeout=30` — 진짜 구멍이었고 닫았다

**변이 M6**(그 둘의 `timeout=30` 만 지우고 `index_pages`(78) 는 남긴다) → **461건 전부 초록**.
**변이 M7**(셋 다 지운다) → `test_index_waits_out_a_write_lock_instead_of_dying` **하나만** 사망.
즉 락 테스트는 78 만 고정하고 나머지 둘은 아무도 안 붙들고 있었다 — `digest.md` `[7]`
("변이 검사는 값을 지웠을 때로 잡아야 한다")이 경고한 그 모양 그대로다.

행동으로는 못 잰다(위 ②). 그래서 **값을 고정했다** —
`TestCli.test_every_connection_waits_the_same_thirty_seconds` 가 `sqlite3.connect` 를 감싸
세 연결이 넘긴 `timeout` 을 전부 모아 `{30}` 인지 본다(단위 461 → **462건**, 0.009초).
M6 를 다시 심으니 이제 **이 테스트가 죽는다**.

## 리뷰 지적 (5건 · 제품 동작 0건)

| # | 등급 | 지적 | 처리 |
|---|---|---|---|
| 1 | medium | `_doc_count`·`search` 의 `timeout=30` 을 고정하는 단언 0건(M6 초록) | **수정** — 테스트 1건 추가 |
| 2 | medium | 락 테스트 하나가 단위 스위트 벽시계의 68%(8.04/11.67초) | 후보로 (digest) |
| 3 | medium | `## 보류` 의 rc 2 근거가 `serve.py` 를 안 봤다 — 관례는 둘이다 | 후보로 (digest) |
| 4 | low | 락 안내가 락 주인을 크롤로 단정한다("크롤이 끝난 뒤") — 다른 `indexer`·외부 프로세스일 수 있다 | 안 고침 — 복구 행동("다시 돌린다")은 어느 경우든 같고 문구 다듬기는 억제 대상(`severity.md` 4절) |
| 5 | 정보 | `except sqlite3.DatabaseError` 는 우리 SQL 버그(`no such module: fts5` 등)도 "DB 를 열 수 없다" 로 덮어 트레이스백을 잃는다 | 수용 — `%s` 로 **원문을 그대로** 붙이므로 진단은 남는다. 같은 파일의 `StaleIndexError` 관용구와 같은 거래 |

**80점 미만으로 버린 것 2건**: ① `"locked" in str(e)` 문자열 매칭 → `e.sqlite_errorcode` 는
**Python 3.11+** 이고 이 저장소는 stock macOS **3.9.6** 이라 대안이 없다(`digest [4]` 와 같은 자리).
② `finally: db.close()` 가 `connect` 자체의 실패를 못 덮는다 → `db =` 대입이 실패하면 `try` 에
들어가지도 않는다. 둘 다 확인하고 버렸다.

## 이번 계획이 하려는 일

**크롤이 도는 중에 색인을 돌리면 5초 만에 트레이스백이 난다.**
`store.py:22` 는 `sqlite3.connect(path, timeout=30)` 이고 주석이 이유까지 적어 뒀는데
(`실측에서 1,700문서째에 크롤을 죽인 게 이거다`, 계획 8), `indexer.py:78`·`136`·`181` 은
`sqlite3.connect(db_path)` 로 **timeout 을 안 줬다** — 기본 5.0초다. 같은 DB 에 붙는 두
모듈이 락을 **6배 다르게** 기다렸다. 계획 8 이 store 쪽에서 닫은 것의 **형제 구멍**이다.
그리고 `indexer.main` 에는 `sqlite3` 예외 갈래가 없어 rc **1** + 트레이스백이 됐다.

개발 두 스텝이 둘 다 닫았다 — 락 8초·20초 rc 0(스텝 1) · 락 35초 rc 2 · 비 DB 파일
rc 2 · 트레이스백 0줄(스텝 2). 테스트 phase 가 `--query` 진입점의 빈 자리를 메웠고,
리뷰가 **나머지 두 연결의 값**을 고정했다.

## 다음 스텝 (DONE 처리)

계획서를 `plan_history_025.md` 로 아카이브 · `index.md` 39행을 완료로 · e2e 없음(7절 유지).
**곁가지 하나**: 자동 스냅샷 훅이 리뷰 중 `9f034eb 자동 스냅샷` 을 만들어 새 테스트가
그 커밋에 실려 갔다(원격에도 올라갔다). 저장소 밖 훅이라 그대로 두고, 리뷰 커밋은
문서와 README 를 싣는다 — `digest.md` 의 스냅샷 훅 관찰(3회)에 네 번째 사례다.

## 한도 (넘으면 RED)

- `data/crawl.db` 실물·스키마 무변경 (sha256 `85c96744…5bda18` — 이번 스텝에서 대조함).
  탐침은 임시 디렉터리에서만, `cwd` 도 거기다 — 색인 경로가 cwd 기준이다.
- 단위 **462건**(리뷰에서 +1)이 하나라도 줄면 RED · e2e 19종이 줄면 RED.
- `docs/digest.md` 열린 항목 **50**(`grep -c '^- \['`)이 줄면 RED · 191줄(상한 200).
- 제품 diff 는 `src/websearch/indexer.py` **한 파일**이다. 다른 `src/` 파일이 바뀌면 RED.
- 변이는 `.git` 없는 스크래치패드 사본에서만 심는다(`## 반복 실패` 3회 항목).
- 의존성 추가 금지(stdlib 만) · `docs/specs/` 읽기만 · `--no-verify` 금지 ·
  `main` 직접 커밋 금지 · 외부 네트워크 금지 · 도메인당 요청 간격 1초 이상 · robots 준수.
