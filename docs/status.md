---
signal: DONE
phase: e2e
step: 0/0
attempt: 0
iteration: 520
updated: 2026-09-09
ctx: 62
night_iterations: 6
night_red: 0
night_retries: 0
plan: null
---

## 계획 90 `live-plan-gap` — 버려진 계획을 아무도 안 물었다

`docs/plan_history_069.md`(아카이브). 브랜치 `loop/live-plan-gap`.

**이 문서 자신이 근거였다.** 오늘 밤이 읽은 `status.md` 는 frontmatter 가
`plan: backoff-recovery · step 2/2` 인데 본문은 **계획 85 `iter-third-witness`** 를
설명하며 넉 계획 전에 끝난 스텝을 시켰다. 실측:

```
index.md   | plan_iter-third-witness | 진행 | … | 2/3 |   ← 계획 행 68개 중 유일한 「진행」
본문 마지막 변경  f2e8e37 (계획 85)  · 86·87·88·89 는 frontmatter 세 줄만 고쳤다
전수 737 OK rc 0                                          ← 아무도 안 문다
```

`iter-third-witness` 는 개발 2/3 에서 **버려졌다.** 그 뒤 `f2e8e37` 이 **같은 번호 85
를 다시 써서** 다른 계획을 마감했고 네 계획이 그 위를 지났다. 문서 가드 여덟 벌은
전부 `status.plan` 이 가리키는 행만 봐서 **뒤에 남겨진 행은 시야 밖**이다.

## 이번 반복 — 리뷰 phase · 백지 패스가 커밋 자신의 위반을 잡았다

패스 A 를 별도 세션에 넘겨 8건을 받았고 **일곱을 실측으로 확인해 자동 수정**했다.
`src/` 무변, 단언 무변 또는 강화, 판정 4/4/1/4 그대로, 전수 **747 OK rc 0**.

**[R90-1] 이 무겁다.** 앞 반복이 쓴 docstring 이 「비고 칸이 다른 행을 **인용하는**
습관은 이 저장소에 있다」고 단정했는데 실물 `docs/index.md` 에 그 꼴은 **0건**이다
(백틱 안에 파이프를 무는 줄은 34개다). 실재하는 것은 **머리 문구** 인용 습관이고
(계획 76 · 반복 516) 표 행 인용은 그 습관이 표로 번지는 날의 이야기다 — 예방이라고
고쳐 적었다. **바로 그 반복이 digest 에 등재한 「주장은 참인 범위까지만 적는다」를
그 커밋 자신이 어겼다.** 등재가 다음 반복을 안 막는다는 것을 하루 안에 봤다.

**[R90-2·3·4]** 새 테스트가 `replace` 사슬로 조립한 픽스처를 **찍어 보니 마지막 칸이
안 닫힌 깨진 행**이었고(`| 통과 | ` 로 줄이 끝난다), 주석이 「같은 행」이라 한 둘은
꼬리 칸이 서로 달라 「차이는 줄 위치뿐」이 엄밀히는 거짓이었다. 리터럴 `PROMOTED` 로
낮춰 셋을 한 번에 닫았다 — 산문 한 글자에 매달린 조립이 사라졌다.

**[R90-5]** 앵커 변이를 실제로 무는 것은 새 테스트가 아니라 `INDEX` 에 낀 `QUOTED`
**한 줄**이다(분해 실측: 테스트를 빼도 3건이 죽고, `QUOTED` 를 빼면 변이가 도로 산다).
테스트가 재는 것은 반대편 — 「앵커가 산 채로 진짜 행까지 놓치지는 않는다」다.

8점 미만 1건 등재: `PLAN_ROW` 의 다른 소비자 `strike_gap` 은 앵커를 못박는 픽스처가
없어 지금은 `LiveGapTest` 가 **대신 죽어 준다**(상수 공유).

## 앞 반복 — 테스트 phase · 넓어지는 변이 둘이 살아 있었다

`rules/test.md` 갭 여섯을 `live_gap` 과 새 테스트 아홉에 대고 훑었다. **⑥ 커버리지
갭에서 둘이 나왔고 둘 다 실측으로 살아남는 것을 봤다** — 형제 가드는 정규식을 못박는
`*PatternTest` 를 갖는데(`StepPatternTest`·`IterationPatternTest`·`ArchivePatternTest`)
`LIVE_ROW`·`LIVE_MARK` 에는 그것이 없었다.

```
A `^` 앵커 삭제        745건 중 0건 죽음 ★생존★  →  픽스처 추가 후 4건 죽음
B 슬러그를 `.+` 로     3건 죽음 (이미 물고 있었다)   →  4건
C 상태 칸을 `in` 으로   745건 중 0건 죽음 ★생존★  →  1건 죽음
D `LIVE_MARK` 를 딴 말  3건 죽음                     →  4건
```

닫은 방법은 **새 가드가 아니라 픽스처 두 줄**이다. A 는 비고 칸이 다른 행을
**인용하는** 꼴(`옛 행은 | plan_ghost | 진행 | … | 였다`)을 `INDEX` 에 넣어 닫았다 —
머리 문구를 산문에 인용하는 습관은 이 저장소에 있고(계획 76 의 `HISTORY_ENTRY_HEAD`
가 같은 자리에서 나왔다, 바로 아래 절이 그 실물이다) 앵커가 죽으면 인용을 행으로 세어
**거짓 RED** 를 낸다. C 는 「진행」을 **품는** 표기(`진행(재개)`)를 넣어 닫았다.

**C 는 내가 「여기서 그것을 잰다」고 docstring 에 적어 둔 자리다.** `보류(미완)` 은
「진행」을 안 품어 부분 문자열 비교로도 통과하는데, 그 케이스를 가리키며 부분 문자열
변이를 잰다고 썼다. **주장은 그 주장이 참인 범위까지만 적는다** — digest `## 반복 실패`
에 등재했다. 전수 **747 OK rc 0**(745 → +2), `README.md` 단위 건수도 같이 갱신했다
(`ReadmeCommandsTest` 가 즉시 빨개져 강제했다 — 하루 전 계획이 세운 가드다).

8점 미만으로 남긴 것: 열이 줄어 `LIVE_ROW` 가 행을 통째로 건너뛰는 쪽은 `live_gap` 이
안 물고 `verdict_gap` 에 기대는데 **그 기댐을 재는 자가 0개다**(digest 후보 등재).

## 반복 517 — 개발 2/2 · 변이 3종이 각각 죽는다

```
① `진행` 필터 삭제 (모든 행을 본다)         → 5건 죽음
② 대조를 자기비교로 (`slug != slug`)         → 3건 죽음   ← 조용한 초록 방향
③ 판정 통째 삭제 (`return None`)             → 4건 죽음
```

**변이가 실제로 심어졌는지를 먼저 단언했다**(digest `[8]` — BSD sed 가 조용히 무시한
적이 있다). 앵커가 유일한지도 단언하는데 **③ 이 거기서 걸렸다** — `p =
PLAN_SLUG.search(status_text)` 가 `step_gap` 과 겹쳐 두 자리라 docstring 끝줄까지
붙여 좁혔다. `PYTHONDONTWRITEBYTECODE=1 PYTHONPYCACHEPREFIX=$(mktemp -d)` 를 매 판 새로
줬고(`project.md`) 매 판 뒤 원복을 파일 비교로 확인했다.

`digest ## 반복 실패` 에 등재했다 — 일반화는 **「지금 도는 것」만 재는 자는 «도는 것이
바뀌는 순간» 뒤에 남은 것을 구조적으로 못 본다**이고, 문서 가드 여덟 벌이 전부 그
모양이었다.

## 반복 517 이 반복 516 의 사고를 되돌렸다 — 본문 세 절이 커밋에서 사라져 있었다

반복 516 이 이 파일을 `t.index("## 다음 스텝")` 으로 잘라 편집했는데, **그 머리 문구가
바로 위 산문에 인용돼 있어** 슬라이스가 거기서 시작해 세 절을 통째로 삼켰다. 전수
745 OK 였다 — 문서 가드는 이 파일의 절 구성을 안 본다. **이 파일은 짧으니 앞으로
통째로 다시 쓴다**(긴 문서는 `done_section` 처럼 줄 단위 일치로 자른다).

## 반복 515 — 개발 1/2 · 자를 세우고 이빨을 실물 다섯 트리로 쟀다

`live_gap` + `LivePlanSyncTest`(실물) + `LiveGapTest`(갈래 7 → **9**). `PLAN_ROW` 에
슬러그 자리만 열어 재사용했다(`LIVE_ROW`) — 표의 열 모양이 바뀌는 날 둘이 함께 움직인다.

**이빨의 증거는 합성 픽스처가 아니라 정정 전 실물 다섯 트리다**: `f2e8e37`(버려진 그
커밋)·`ba83bc2`(86)·`6ea7f10`(87)·`78f323c`(88)·`1b79a74`(89) 가 전부
`iter-third-witness` 를 이름으로 신고하고 지금 트리는 `None` 이다.
**거짓 RED 도 과거 전체로 쟀다** — 슬러그 관례가 선 뒤(`3f7d1f8`, 계획 60) 137커밋 중
빨강 5건이고 5건 전부 그 결함이라 오탐 0이다.

## 반복 520 — e2e 2/2 · 새 e2e 0개 · 사전결함 2건을 갈라냈다

`docs/e2e/live-plan-gap/result.md`. **면제를 「해당 없음」으로 넘기지 않고 쟀다** — `git diff --stat be0ddd5 HEAD -- src/ e2e/` 가 빈손이라 제품 경로가 어제와 같다.
대신 실물 트리 복사본에 **사람이 낼 편집 일곱**을 걸어 `live_gap` 이 물 것을 물고
(E2·E3·E6) 안 물 것에 조용한지(E1·E4·E5)를 양방향으로 확인했다. **E3 이 오늘 밤 실제로
있었던 사고이고 그것이 이 계획의 존재 이유다.**

**실패 2종은 이 계획의 회귀가 아니다 — 가정하지 않고 쟀다.** 처음엔 「내가 돌린 동시
부하 탓」으로 짚었는데 부하를 없애고 단독으로 재니 그대로 실패해 **가설을 폐기했다.**
코드가 같은 과거 트리 셋을 워크트리로 세워 재니 같은 띠였다 — `perf_crawl` [차단]
8.88~9.11(기준선 9.0) · `deadline_e2e` 는 `be0ddd5` 에서도 0.880s 로 갈린다.
기준선을 내리는 것은 「통과시키려고 시나리오를 낮추는 것」이라 `rules/e2e.md` 7절이
금지한다 — **digest `[8]`·`[6]` 으로 등재하고 아침 판단으로 넘겼다.**

판정 칸에 맨 「통과」를 안 적은 이유도 이것이다(`통과(사전결함 2건)`). 계획 87·88·89 는
세 줄 다 「통과」인데 그중 `be0ddd5` 가 오늘 여기서 빨간 판을 냈고, **그 세 줄이 무엇을
근거로 적혔는지는 오늘 재지 않았다. 재지 않은 것을 적지 않는다.**

## 다음 반복 — 계획 탐색 (DONE)

계획 90 은 닫혔다. 계획서는 `plan_history_069.md`, `plan: null`.
다음 탐색이 먼저 볼 곳은 **오늘 밤이 실측으로 채운 digest 후보 `[8]`** 이다 —
e2e 축 하나가 기준선 아래로 내려앉은 채 최근 네 계획을 지나왔다.

## 아침 할 일 — 어제와 같은 하나

**패치 적용 판단** — `docs/patches/userinfo-leak-refuse-credentials.patch` (382줄 ·
구현 + 단위 + e2e · `git apply --check` 통과). 밤이 안 댄 이유는 「보안 — 항상 보류」다.
적용하면 `urls.normalize` 가 netloc 에 `@` 를 든 URL 에 `None` 을 준다(018 의 「보존」을
뒤집는다). 되돌리려면 `normalize` 안의 조건 한 줄을 지운다.

## 한도

`digest.md` **223줄**(상한 200). 무인은 이 파일을 회전하지 않는다(계획 76 이 그은 선).
`history_current.md` 264줄(상한 300).
