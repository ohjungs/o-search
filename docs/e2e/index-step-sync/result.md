# e2e 결과 — 계획 60 `index-step-sync` (반복 355 · 2026-09-06)

**판정: 통과 — 21종 전수 `rc 0` · 기준선 회귀 0 · 완료 기준 6/6 → DONE.**

## 0. 면제 — 새 e2e 를 안 만든다 (`rules/e2e.md` 3절)

계획이 만진 것은 `tests/test_docs.py`(+165줄)와 `docs/` 뿐이고 제품 `src/` 는 **0줄**이다
(`git diff 61e82e7 HEAD --name-only` 가 `README.md`·`docs/`·`tests/test_docs.py` 열 개만
낸다 · `README.md` 는 단위 건수 줄 한 줄). 세운 것은 **기록 문서 둘을 서로 대조하는
가드**라 크롤러·색인·서버·화면 어디도 안 지난다 — **프로세스 밖에서 달라지는 동작이 0**
이다. e2e 가 낼 값이 「안 달라졌다」의 확인뿐이므로 시나리오를 새로 만들지 않는다.
만든 e2e 파일 **0** · 고친 e2e 파일 **0**(이 `result.md` 는 3절이 허용하는 세 곳 중 하나).

**대신 확인할 것이 하나 있다** — 새 검사는 `docs/status.md` 와 `docs/index.md` 를 읽으므로
**이 커밋 자신이 그 검사의 첫 실사용 대상**이다. 아래 2절 M2·M3 가 그것을 실물 복사본
위에서 양방향으로 다시 쟀다(저장소 파일 무접촉).

## 1. e2e 21종 — 전부 `rc 0`

맨몸 실행이다(리다이렉션·파이프 0). 세 개짜리 표기는 오늘 실측값이다.

| # | 시나리오 | rc | 오늘 실측 |
|---|---|---|---|
| 1 | `crawl_e2e.py` | 0 | 수집 15 · 차단 0 · 최소 간격 1.002s |
| 2 | `indexer_e2e.py` | 0 | 3문서 색인 · 증분 0문서 · 없는 상대경로 rc 1 |
| 3 | `noindex_e2e.py` | 0 | 4페이지 중 2문서 · 뒤늦은 noindex 제거 |
| 4 | `search_api_e2e.py` | 0 | 15문서 · 10+5건 겹침 0 · 400/404/501/503/500 · p95 **5.73ms** |
| 5 | `crawl_delay_e2e.py` | 0 | `Crawl-delay:2` 최소 2.01s · 하한 1.00s |
| 6 | `non_ascii_e2e.py` | 0 | 한글 경로 3행 · 서로게이트 시드 1건 건너뜀 |
| 7 | `hidden_passage_e2e.py` | 0 | 숨은 텍스트 **0/5** · 본문 문단 **5/5** · 오탐 대조군 4종 |
| 8 | `design_check.py` | 0 | 4축 통과 · JS **0 B** · 최저 대비 **4.87:1**(포커스 3.56:1) |
| 9 | `tokenizer_e2e.py` | 0 | 화면(HTML)에서 복합어·어순·굴절·AND · 503→재색인 복구 |
| 10 | `domain_key_e2e.py` | 0 | 같은 서버 간격 2.01×3 · robots 1회 · 대조군 1.00 |
| 11 | `deadline_e2e.py` | 0 | 대조군 16 / CLI 8페이지 2.2s · **버려진 응답 0건** |
| 12 | `interrupt_e2e.py` | 0 | SIGINT 10.0s rc 130 · DB 0행 · 두 번째 Ctrl-C rc -2 |
| 13 | `indexer_interrupt_e2e.py` | 0 | 재구축 중단이 옛 2,000행 보존 · 재실행 복구 |
| 14 | `crawl_politeness_e2e.py` | 0 | 예외 뒤 2.01s · 대조군 1.00s · 재시도 1.00s |
| 15 | `pagination_ui_e2e.py` | 0 | 다음을 따라가 doc00 · 마지막에 다음 없음 |
| 16 | `retry_interval_e2e.py` | 0 | https 재시도 5.01s · 대조군 1.01s |
| 17 | `url_normalize_e2e.py` | 0 | 표기 10개가 문서 6개로 |
| 18 | `perf_crawl.py` | 0 | [열림] **10.20/s** · [차단] **10.20/s** · [예외] 간격 1.000s |
| 19 | `perf_search.py` | 0 | p50 1.49ms · **p95 9.01ms** (예산 300ms 의 3.0%) |
| 20 | `quality_eval.py` | 0 | ko **20/20** · en **19/20** · 매치 평균 14.0 · 최소 11 · 최대 28 |
| 21 | `passage_eval.py` | 0 | 정확도 **100.0%** · 채택률 99.5% · p95 **1.52ms** |

## 2. 기준선 대조 — 회귀 **0**

괄호는 직전 전수(반복 349 · 계획 59)의 값이다.

| 축 | 기준 | 오늘 | 직전 |
|---|---|---|---|
| 근거 문단 정확도 | ≥90% | **100.0%** | 100.0% |
| `/passages` p95 | ≤500ms | **1.52ms** | 1.51ms |
| `/search` p95 | ≤300ms | **9.01ms** | 8.80ms |
| 품질 ko / en | ≥80% | **20/20 · 19/20** | 20/20 · 19/20 |
| 매치 수 평균/최소/최대 | 기록 | **14.0 / 11 / 28** | 14.0 / 11 / 28 |
| 크롤 [열림] / [차단] | 5.0 / 9.0 | **10.20 / 10.20** | 10.24 / 10.24 |
| 디자인 JS / 최저 대비 | 50KB / 4.5:1 | **0 B / 4.87:1** | 0 B / 4.87:1 |
| 숨은 텍스트 문단 | 0/5 | **0/5** | 0/5 |

**움직인 기준선이 없어 `docs/project.md` 의 수치는 한 줄도 안 갱신했다.** 계획이 제품
코드를 0줄 고쳤으니 기대한 결과 그대로다 — 표의 값은 판정이 아니라 **그 기대가 참임을
보인 증거**다. `p95` 두 줄의 소수점 흔들림(1.51→1.52 · 8.80→9.01)은 예산의 0.3%·3.0%
안쪽이고 스크립트 자신의 합격선이 통과 판정을 냈다.

## 3. 완료 기준 6/6 — 오늘 전부 다시 쟀다

| # | 기준 | 실측 | 판정 |
|---|---|---|---|
| 1 | 전수 `OK` · rc 0 · 건수 606 이상 | `Ran 614 tests in 16.098s` · `OK` · rc 0 (맨몸) | 충족 |
| 2 | `index.md` 스텝 칸을 비트는 변이에서 새 검사가 죽는다 · 되돌리면 초록 | M2(`1/1`→`0/1`) `StepSyncTest failures=1`, 메시지가 **`0/1 ≠ 1/1`** 을 적는다 · 무변이 대조군 `run=1 failures=0` | 충족 |
| 3 | `status.md` 의 `step` 을 비트는 변이에서도 죽는다 | M3(`step: 1/1`→`0/1`) `failures=1`, 메시지가 **`1/1 ≠ 0/1`** — 방향이 반대라 **한쪽만 재는 검사가 아니다** | 충족 |
| 4 | 정규식을 넓히는 변이에서 패턴 단언이 죽는다 | M4a(슬러그를 안 본다) **5건 사망**(`test_absent_slug_matches_nothing`·`test_row_is_picked_by_exact_slug`·`StepGapTest` 둘·`StepSyncTest`) · M4b(접두 일치) **3건 사망** · 무변이 대조군 `run=9 failures=0` | 충족 |
| 5 | `README.md` 단위 건수 줄이 새 건수와 맞는다 | `README.md` 의 러너 줄이 `단위 614건` · `tests/test_readme.py` 가 전수 안에서 초록 | 충족 |
| 6 | 제품 `src/` 0줄 · 스키마·재색인·새 의존성 0 · `docs/specs/`·`data/crawl.db` 무접촉 | `git diff 61e82e7 HEAD -- src/ e2e/ docs/specs/ data/` **빈손** · `README.md` 만 +1/-1 · `data/crawl.db` sha256 `85c9674…5bda18` 무변 · `git status --short` 빈손 | 충족 |

**덤으로 반복 353 의 갈래 변이 6종을 다시 쟀다** — G1 null 기대값 비틀기 · G2 null 갈래
삭제 · G3 대조를 자기비교로 · G4 「행이 없다」 가드 삭제 · G5·G6 「`step:`/`plan:` 줄이
없다」 가드 삭제. **6/6 이 오늘도 죽는다**(각각 단언 1건). 테스트 phase 가 `step_gap` 을
순수 함수로 뺀 판단이 하루 뒤에도 유효하다는 확인이다.

## 4. 한도 — 지킨 것

- **러너 규율 위반 0회(누적 38 유지).** 전수와 e2e 21종을 전부 맨몸으로 돌리고 판정 줄과
  `rc` 를 눈으로 봤다. `grep` 은 결과 파일을 읽는 쪽에만 썼다.
- **변이는 저장소 밖에서 걸었다** — 문서 변이(M2·M3)는 `docs/` 를 임시 디렉터리로 복사해
  비틀고 `test_docs.DOCS` 를 그쪽으로 돌렸고, 소스 변이(M4·G1~G6)는 `test_docs.py` 원문을
  문자열로 치환해 메모리에서 `exec` 했다(`e2e60_mut.py` · 스크래치패드 · 커밋 안 함).
  원문 존재를 `count(...) == 1` 로 먼저 단언해 **거짓 초록**을 막았다.
- `PYTHONDONTWRITEBYTECODE=1` 과 `PYTHONPYCACHEPREFIX=$(mktemp -d)` 를 함께 줬다
  (`docs/project.md` 의 macOS pycache 처방).
- `data/crawl.db` 무변경 · 재색인 0 · 스키마 0 · 새 의존성 0 · `docs/specs/` 읽기만.
- 원격 `loop/passage-cost-band` 위에 오늘 커밋을 쌓는다. `origin/main` 무접촉 ·
  PR 무접촉(만들지도 조회하지도 않았다) — 병합은 사람 몫이다.
