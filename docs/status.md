---
signal: YELLOW
phase: 계획(탐색)
step: -
attempt: 0
iteration: 152
updated: 2026-08-29
ctx: 21
night_iterations: 35
night_red: 0
night_retries: 0
---

# 현재 상태

**탐색 막힘으로 정지.** `src` **0줄** · 열린 계획 0 · 보류 패치 0 · RED 0.
단위 **431건 OK** · e2e **17종 전부 rc=0** — 이 반복에서 **전수 재실행한 실측**이다
(직전 두 반복은 `src` 무변경이라 일부만 돌렸으므로, 전수 초록은 여기서 처음 확인했다).
브랜치 `loop/readme-perf-audit` = `main` = `origin/main` = `f888518`.

> **프런트매터를 복원했다.** 위 필드 전부가 비어 있었다(`---` 뒤 빈 줄). `night_iterations`·
> `night_red`·`night_retries` 는 정지 조건 검사와 `metrics-record.js` 가 **직접 읽는**
> 필드라, 없는 동안 야간 정지 조건이 근거 없이 돌고 있었다. `night_iterations` 는
> 세션값이 아니라 **밤 누적**이다(jsonl 기록분 33 + 반복 151 + 152 = 35).

## 반복 152 — 탐색만 했고 계획을 열지 않았다

**`discover.md` 1절 8개 출처를 순서대로 실측했다.**

| 출처 | 결과 |
|---|---|
| 1 실패하는 테스트 | **0건** — 단위 431 OK · e2e 17종 전부 rc=0 (전수 실행) |
| 2 미반영 리뷰 지적 | **0건** |
| 3 `TODO`/`FIXME`/`HACK` | **0건** — `src`·`tests`·`e2e` grep 히트 2건은 둘 다 설명 주석 |
| 4 `candidates.md`·스캔 스크립트 | **이 저장소에 없다** |
| 5~7 `digest` 후보·판단필요·반복실패 | 항목은 남아 있다 — 아래 |

**`SKILL.md` 4-1 이 여기서 걸렸다.** 짧은 경로 **연속 3건**(30·31·32)의 근거를 보니
**셋 다 같은 출처**였다: 30 = `status` 다음 계획 후보 `[6]` · 31 = `digest` `[4]` ·
32 = `digest` `[6]`. 전부 **미룬 후보 목록**(5순위)이다. 룰대로 "탐색이 막힌 것이니 정지".

**출처 1~4 가 실측 0건이라는 것이 그 판정을 뒷받침한다** — 저장소가 스스로 새 근거를
못 내고 있고, 루프가 미리 적어 둔 목록만 갉아먹고 있었다. "연속 3건" 이 아니라
**셋이 한 우물에서 나왔다는 것**이 신호다.

## 남은 후보를 분류했다 — 왜 정지가 맞는지의 근거

**(a) 사람 승인 대기 — 무인 모드가 손대지 않는다**
`[8]` indexer 증분이 갱신을 반영 안 함(**스키마 변경**) · `[high]` `store.has` 가 상태
불문 스킵 = 재크롤 정책(**사람 판단**) · URL userinfo 가 `pages.url` PK 이자 검색 결과에
렌더됨(**보안 경계**) · `[5]` `X-Robots-Tag` http-equiv(**스키마**) · `loop/*` 병합 여부.

**(b) 도달 불가 — 고치면 오히려 정상값을 거절한다**
`[4]` `int_max_str_digits`(파이썬 3.9.6, 유지 판단 완료) · `[4]` `quality_eval` 의
`limit=100` 절단(실물 최대 28건) · `[4]` `fixture_defects` 가 ko·en 만 셈(코퍼스 동결이라
`ja` 질의가 생길 수 없다). 셋 다 **지금 짜면 죽은 코드**다.

**(c) 짧은 경로가 아니라 정식 계획 크기 — 설계 트리거에 걸린다**
`[6]` `tests/test_crawl.py` 의 가짜 시계 이음매(**9곳**, digest 가 직접 "자기 계획
크기다" 라고 적어 둔 건) · `[4]` 간격 시계가 발신이 아니라 pop 시각에서 시작 · `[7]`
요청 사이 색인 변경 시 OFFSET 페이지네이션 드리프트. 셋 다 40줄을 넘고 대안이 갈려
**야간 자동 적용 기준 밖**이다(패치로 남기려 해도 설계 결정이 먼저다).

**즉 짧은 경로로 집을 것은 실제로 소진됐다.** 남은 것은 승인 대기거나, 도달 불가거나,
사람이 옆에 있을 때 설계부터 시작할 계획이다. `계획 소진` 이 아니라 **`탐색 막힘`** 이다 —
할 일이 없는 게 아니라, 무인 모드가 혼자 열어도 되는 일이 없다.

## 사람이 정해야 할 것 (아침에 볼 것)

1. **브랜치 병합** — `loop/readme-perf-audit`(커밋 3건, 푸시 완료)을 `main` 에 남길지.
   지금 `main` 과 같은 커밋이라 실질 차이는 없다.
2. **(c) 셋 중 무엇을 열지** — 하나를 열어 주면 다음 밤은 정식 경로로 돈다.
   가장 값이 커 보이는 것은 `[6]` 가짜 시계 이음매다(저장소가 초 단위 간격을 단언하는
   근거가 전부 거기 걸려 있다).
3. **(a) 의 스키마·보안 건**을 열지 — 열면 무인 금지가 풀린다.

## 미결

`data/crawl.db` 는 URL 을 `pages.url` PK 로 쓴다 · CI 없음 · `docs/specs/` 는 동결이다.
