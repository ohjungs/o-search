# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**반복 97~122 는 `history_006.md`, 123~128(계획 020 `deadline` 전체)은
`history_007.md`, 계획 21~24(짧은 경로 3건 + 보류 패치 소진, 2026-08-29 14:20~17:40)는
`history_008.md` 로 밀려났다** (2026-08-29 회전 3회).
**계획 25~29 는 `history_009.md`, 계획 30·31(짧은 경로) + 반복 152 탐색 +
계획 33 `clock-injection` 의 계획·설계·개발 1~4 는 `history_010.md` 로 밀려났다**
(2026-08-30 회전, 393줄 → 139줄. 상한 300줄을 넘겨 놓고 두 반복을 갔다).
**계획 33 의 마지막 네 반복(개발 5 · 테스트 1 · 리뷰 1 · e2e 1)은 `history_011.md`**
(2026-08-30 회전, 322줄 → 210줄. 상한을 넘긴 채 반복 두 번을 또 갔다 — 스텝 3 이
"미결(문서 위생)" 로 넘긴 것을 스텝 4 가 닫았다).
계획 33 의 전 phase 서술은 `plan_history_020.md` 의 `## 기록` 에 그대로 있다.
**계획 34 `graceful-interrupt` 의 앞 여덟 반복(계획 1 · 설계 1 · 개발 1 세 번 · 2 · 3 · 4)은
`history_012.md`** (2026-08-30 회전, 300줄 → 85줄. **상한에 정확히 닿아 넘기기 전에 돌렸다** —
앞 두 번은 넘긴 채 반복을 더 갔다).
**계획 34 의 마지막 세 반복(테스트 1 · 리뷰 1 · e2e 1)은 `history_013.md`**
(2026-08-31 회전, 307줄 → 244줄. 상한 300 을 넘긴 그 반복에서 바로 돌렸다 —
`digest ## 반복 실패` 가 지적한 "회전이 두 반복 늦었다" 를 안 되풀이한다).
**계획 35 `deadline-stop` 전체 여섯 반복(계획 · 개발 1 · 테스트 2 · 리뷰 · e2e)은
`history_014.md`** (2026-08-31 회전, 336줄 → 188줄. 상한 300 을 넘긴 채 테스트 반복
하나를 더 갔고 **리뷰가 그것을 잡았다** — 회전은 phase 룰이 아니라 문서 위생이라
`status.md` 의 "집안일 미결" 로만 있었다. 완료된 계획 하나를 통째로 미는 경계로 잘랐다).

**계획 36 `signal-budget-cover` 전체 두 반복과 계획 37 `indexer-interrupt` 전체 여섯 반복은
`history_015.md`** (2026-08-31 회전, 318줄 → 96줄. **계획 38 리뷰가 잡아서 돌렸다** —
테스트 스텝이 305줄인 것을 알고 `status.md` 에 적어 둔 채 그대로 갔다. 393·322·336·318,
네 번째 지각이다. 완료된 계획 둘을 통째로 미는 경계로 잘랐다).

**계획 38 `digest-rotate` 전체 다섯 반복과 계획 39 `indexer-lock` 전체 다섯 반복은
`history_016.md`** (2026-09-01 회전, 299줄 → 145줄. **계획 40 리뷰가 제때 돌렸다** —
테스트 반복이 "다음 반복이 회전한다" 고 적어 둔 그대로다. 393·322·336·318 은 지각이었고
이번은 아니다. 완료된 계획 둘을 통째로 미는 경계로 잘랐다 — 계획 40 을 여는 반복 198 부터 남는다).

**계획 40 `exit-code-contract` 전체 여섯 반복(198~203)은 `history_017.md`** (2026-09-01
회전, 291줄 → 새 항목을 넣기 전에 돌렸다. **테스트 phase 가 자기 손으로 돌린 첫 회전이다** —
205 가 "다음 반복이 회전 자리다" 라고 적어 둔 그대로다. 완료된 계획 하나를 통째로 미는 경계).

**계획 41 `crawl-db-guard` 전체 다섯 반복(204~208)은 `history_018.md`** (2026-09-01
회전, 364줄 → 221줄. **e2e phase 가 마감 작업으로 돌렸다** — 300줄 상한을 64줄 넘긴
채로 개발 2/2 가 지나갔다. 완료된 계획 하나를 통째로 미는 경계. 이 회전이
`DocCitationTest` 의 아카이브 제외 규칙을 **실물로 시험한 첫 사례**다 — 계획 42
7절 반증 신호 ②가 예고한 그 자리이고, 새 이름이 규칙과 맞아 스위트는 초록이다).

**계획 42 `docs-citation-guard` 전체 여섯 반복(209~214)은 `history_019.md`** (2026-09-01
회전, 292줄 → **설계 반복이 자기 항목을 넣기 전에 돌렸다** — 넣었으면 300줄을 넘겼다.
계획 41 회전이 64줄 지각한 바로 다음 회전이고, 이번엔 앞에서 잘랐다. 완료된 계획 하나를
통째로 미는 경계).

**계획 43 `focus-contrast` 의 앞 여섯 반복(215~220)은 `history_020.md`** (2026-09-01
회전, 293줄 → 70줄. **e2e 반복이 자기 항목을 넣기 전에 돌렸다** — 넣었으면 311줄이다.
계획 42 회전과 같은 자리에서 같은 순서로 돌았고, 두 번 연속 지각이 없다. 완료된 계획
하나를 통째로 미는 경계).

**계획 43 의 마지막 반복(e2e 1/1 · 반복 221)은 `history_021.md`** (2026-09-01 회전,
293줄 → 269줄. **계획 44 e2e 반복이 자기 항목을 넣기 전에 돌렸다** — 넣었으면 300줄에
닿는다. 완료된 계획 하나를 통째로 미는 경계이고, 계획 43 은 이 회전으로 다 밀렸다).

---

**계획 44 `focus-ring-presence` 전체 여섯 반복(계획 · 설계 · 개발 · 테스트 · 리뷰 · e2e)은
`history_022.md`** (2026-09-01 회전, 306줄 → 107줄. **상한 300 을 넘긴 그 반복에서
바로 돌렸다** — 계획 phase 가 자기 손으로 돌린 첫 회전이다. `digest.md` 압축 줄은 200줄
상한 때문에 새 줄을 더하지 않고 **기존 회전 줄에 이어 붙였다**(`## 완료` 의 2026-09-01
`history_021.md` 줄) — 완료 항목은 하나도 안 지웠으므로 **새 색인 구멍 0** 이다).

**계획 45 `docs-record-guard` 의 앞 네 반복(계획 · 설계 · 개발 1/2 · 개발 2/2)은
`history_023.md`** (2026-09-02 회전, 318줄 → 140줄. **테스트 반복이 자기 항목을 넣고
상한을 넘긴 것을 보고 그 자리에서 돌렸다.** 진행 중인 계획을 가르는 경계라 완료 계획을
통째로 미는 평소 경계와 다르다 — 계획 33·34 회전과 같은 꼴이고, 나머지 서술은
`plan_docs-record-guard.md` 와 이 계획이 DONE 될 때 남길 아카이브가 갖는다.
**이번 회전은 새 의무를 하나 지고 돈 첫 회전이다** — 계획 45 자신이 만든
`ArchiveIndexTest` 때문에 `digest.md` 의 아카이브 명부에 `history_023.md` 를 더하지
않으면 스위트가 RED 다. 규약이 문서에만 있던 어제와 달리 오늘은 검사가 강제한다).

## 2026-09-02 00:50 | docs-record-guard | 테스트 1/1 | 시도0

- 한 일: `tests/test_docs.py` 에 **자기 자신을 재는 단언 둘**(`IterationPatternTest` ·
  `ArchiveMatchTest`, 5건)을 더하고, 두 검사의 판단을 모듈 이음매 넷으로 꺼냈다
  (`ITER_ROW`·`ITER_LINE` 상수 · `done_section()` · `indexed()`). 제품 `src/` **0줄** ·
  새 파일 **0개**.
- **설계 변이표(V1~V8)는 개발이 소진했으므로 표 밖을 쟀다.** 표의 여덟은 전부
  *데이터*(문서)를 건드리는 변이였다. 이번엔 **검사 코드를 넓히는** 변이를 심었고
  **셋이 그대로 살아남았다** — 셋 다 *"설계가 잡는다고 적었지만 아무도 안 잰"* 자리다.
  - **M1** `indexed` 에서 경계 매칭 `(?<![A-Za-z_])` 삭제 → **초록.** 설계가
    *"V7 이 설계의 값을 재는 변이"* 라고 적은 바로 그 결정이 무방비였다.
  - **M2** `ITER_ROW` 를 `^\| 반복[^|]*\| ([0-9]+) \|` 로 확대 → **초록.**
    계약표의 *"`| 반복 수 |`·`| 반복 상한 |`·`| 평균 반복 |` 은 안 문다"* 줄이 무방비였다.
  - **M3** 검사 범위를 `## 완료` 절 → `digest.md` **전체** → **초록.**
    갈림길 A 의 결론 자체다. 설계서 한 절을 통째로 쓴 결정을 아무도 안 재고 있었다.
- **왜 안 잡혔나 — 개발 2/2 가 데이터를 초록으로 만들어서다.** 명부를 넣은 순간
  `digest.md` 는 **어느 쪽으로 재도 구멍 0** 이 됐다(범위를 넓혀도 22개가 다 있고,
  경계를 빼도 0). `metrics.md` 도 정확한 행이 이웃보다 **먼저** 나와 넓힌 정규식의
  첫 매치가 그대로다. **린트형 검사는 데이터가 초록일 때 자신을 못 잰다** — 계획 42 가
  적어 두고 `CitationPatternTest` 로 닫은 그 자리의 **세 번째 적용**이다.
  설계 5절도 *"오늘은 순진 매칭과 경계 매칭의 구멍 목록이 우연히 같다"* 로 이 사실을
  **적어 두었는데**, 적어 둔 것과 붙들어 둔 것은 다르다.
- **처방은 이미 이 파일에 있었다** — 새 수법을 만들지 않고 `CitationPatternTest` 의
  모양(공유 상수 + CAUGHT/NOT_CAUGHT 리터럴)을 그대로 한 번 더 썼다. 검사와 픽스처가
  **같은 코드 경로**를 봐야 변이가 죽는다(복사본은 드리프트해서 변이를 살린다).
- **합성 픽스처가 실물이 못 만드는 상황을 만든다.**
  - `IterationPatternTest` — 이웃 세 행을 **일부러 앞에 둔** 표. 넓힌 정규식은
    `| 반복 상한 | 0 |` 의 `0` 을 집어 `'232' != '0'` 으로 죽는다. `status.md` 쪽은
    같은 프런트매터의 실제 이웃 `night_iterations: 90` 을 안 무는 것을 잰다.
  - `ArchiveMatchTest` — 합성 `digest` 하나로 셋을 동시에. 안 실려야 하는 셋이 **각각
    다른 이유**다: `## 반복 실패` 의 **신고문**(범위) · `plan_history_003.md` 에 가려진
    이름(경계) · `## 보류` 절(범위). 절 이름이 바뀌면 `None` 인 것도 함께 잰다.
- **되돌아가 재확인했다** — M1·M2·M3 을 다시 심으니 각각 **새 단언만** 죽는다
  (M1 → `test_not_indexed[history_003.md]` · M2 → `test_only_the_exact_row_matches` ·
  M3 → `test_not_indexed[history_001.md, history_004.md]`). 겹치는 검사가 없는 진짜
  구멍이었고, 넷 중 어느 것도 남의 단언에 얹혀 죽지 않는다.
- **스텝 1 의 가드가 첫 실사용에서 울었다.** `metrics.md` 를 232 로 고치고 `status.md`
  를 아직 안 고친 채 전체를 돌리자 `IterationSyncTest` 가 **`'232' != '231'`** 로 죽었다.
  **어제 만든 검사가 그것을 만든 계획 자신의 기록을 잡았다** — 계획 30 의 README 카운트
  가드가 하루 만에 값을 증명한 것과 같은 모양이다.
- **안 더한 것 (중요도 4, `digest` 에도 안 남긴다)**: 명부에 있는데 실물이 없는 이름
  (역방향 검사). 오타는 지금도 RED 이고(진짜 이름이 안 잡힌다) 아카이브는 삭제 금지
  문서라 사는 변이가 없다. 단언을 위한 단언이 된다.
- 결과: 맨몸 실측 `PYTHONPATH=src python3 -m unittest discover tests`
  **477건 OK → 482건 OK 12.036초** · `data/crawl.db` sha256 `85c96744…` **무변경** ·
  의존성 0 · 제품 `src/` **0줄** · 새 파일 0개.
- **README 카운트 가드가 사흘 연속 울었다** — `(477, 19) != (482, 19)`, 482 로 정정.
- **러너 파이프 1회** — 첫 실행에서 `| tail -5` 를 썼고 **판정 줄 대신 테스트가 찍은
  표준출력**이 나와 결과를 못 봤다(`project.md` 가 경고한 그 배치 그대로, 방아쇠도
  그대로 "이미 초록일 것 같은 실행"이었다). 즉시 맨몸 재실행했고 **이후 전 실행이 맨몸**이다.
- 변이는 제자리 편집이라 `.mutation-lock` 을 걸고 했고, 매 실행 **`python3 -B`**,
  되돌리기를 `&&` 로 안 이었다. 끝나고 지웠다(작업 트리에 없음).
- **집안일 — 회전을 이 반복에서 돌렸다.** 이 기록을 붙이니 **318줄**로 상한 300 을
  넘어 그 자리에서 계획 45 의 앞 네 반복(228~231)을 `history_023.md` 로 밀었다
  (318 → **149줄**). **네 회전 연속 지각 0.** 진행 중인 계획을 가르는 경계라 완료
  계획을 통째로 미는 평소 경계와 다르다(계획 33·34 와 같은 꼴).
- **이 회전이 계획 45 가 만든 새 의무를 처음 졌다** — `digest.md` 명부에
  `history_023.md` 를 더했다. 안 더하면 `ArchiveIndexTest` 가 RED 다. 어제까지 이
  규약은 `digest.md` 머리 주석의 **문장**이었고 오늘부터는 **검사**다. 압축 줄은 200줄
  상한 때문에 기존 회전 줄에 이어 붙여 `digest.md` **200줄 유지** · 완료 항목 삭제 0 ·
  **새 색인 구멍 0** · 아카이브 **23개**.
- 다음: **리뷰 phase.**

## 2026-09-02 01:05 | docs-record-guard | 리뷰 1/1 | 시도0

- 한 일: `git diff 79952ee..HEAD`(다섯 커밋 · `tests/test_docs.py` +167줄과 문서)를
  **백지 패스 먼저** 보고, findings 를 적은 뒤 계획·설계를 열어 대조했다.
  **판정 통과 · 지적 3건 보고 · 고침 0건 · 제품 동작 지적 0건**(`src/` 가 이 계획 내내 0줄).
- **[A1] 백지 패스가 잰 것 — `ArchiveIndexTest` 가 보는 것은 "명부"가 아니라 `## 완료`
  절 전체다** (중요도 3). 그 절에는 명부 한 줄 말고 회전 압축 줄 20여 개가 산문으로
  들어 있고 그것들도 이름을 적는다. `indexed()` 를 직접 불러 명부와 나머지를 갈라 재니
  **아카이브 23개 중 18개는 산문에도 이름이 있어** 명부에서 그 이름만 지워도 초록이고,
  **명부가 유일한 포인터인 것은 다섯(001·002·003·005·019)** — 계획 45 가 닫은 그 다섯과
  정확히 같다. 설계 갈림길 B 의 결론(*"명부 한 줄이 색인이다"*)은 **다섯에만 붙들려 있다.**
- **변이 R1 로 확인했다** — 명부 줄 통째 삭제 → **RED**, 실패 목록이 그 다섯을 정확히
  찍는다. 설계 V1~V8(데이터)도 테스트 M1~M3(검사 코드 확대)도 **명부 줄의 존재 자체**는
  안 쟀다. 그 축 하나만 심었다.
- **그래도 안 고쳤다.** 검사가 지키는 불변량은 *"이름으로 찾을 수 있나"* 이고 산문
  포인터도 그것을 참으로 만든다. 18개의 명부 항목은 산문이 회전으로 지워지는 순간
  RED 가 되어 그때 자기를 되찾는다(자가 치유). **명부 줄만 따로 재는 단언은 불변량이
  아니라 구현을 붙든다** — 테스트 phase 가 역방향 검사를 중요도 4 로 버린 것과 같은 이유다.
- **[A2] `IterationPatternTest.TABLE` 첫 행 `| phase | 반복 수 |` 는 장식이다**
  (중요도 4, 안 고침). `^\| 반복` 앵커라 어떤 확대 변이로도 안 걸린다 — 그 행은
  `| phase` 로 시작한다. M2 를 실제로 죽이는 것은 2·3행이다. docstring 의 *"이웃 세 행"*
  중 재는 것은 **둘**. 지우면 실물 `metrics.md` 의 배치를 덜 닮아 그대로 뒀다.
- **[B1] 대조 패스 — 갈림길 B 가 만든 규약이 저장소 안에만 있다** (중요도 3, 못 고친다).
  *"회전은 명부에 이름을 더한다"* 는 `digest.md` 주석에 있고, 회전을 지시하는
  `rules/docs.md` 는 **저장소 밖**이라 여전히 *"완료 항목부터 지운다"* 만 적는다.
  잊으면 RED 가 가르쳐 주므로 실동작은 안전하다. `status.md` 의 `## 사람이 정할 것` 4번.
- **계약은 전수 이행됐다** — 설계 4절 표를 한 줄씩 대조: 정규식 둘이 계약표 글자 그대로 ·
  눈먼 자리 셋(glob 빈손 · `digest` 부재 · 절 부재)에 가드 전부 · 매치 `None` 은 비교 전
  실패 · 갈림길 A 대안 2 · 갈림길 B 대안 1 · 경계 매칭 존재. 한도도 전부: `src/` 0줄 ·
  새 파일 0 · 의존성 0 · `digest.md` 200줄 · `data/crawl.db` 무변경 · 고친 파일 2개.
- 결과: 맨몸 실측 `PYTHONPATH=src python3 -m unittest discover -s tests`
  **482건 OK 11.847초** · 변이 R1 되돌린 뒤 재실행해 초록을 눈으로 확인 ·
  `.mutation-lock` 없음 · `git status` 깨끗 · README 카운트 가드 무변(482 그대로).
- **러너 파이프 0회.** 직전 두 반복이 연속으로 `| tail` 에 판정 줄을 잃은 자리라
  전 실행을 맨몸으로 돌리고 눈으로 읽었다. 변이 실행의 `-k` 는 셸 파이프가 아니라
  `unittest` 자신의 인자다.
- 다음: **e2e phase.**

## 2026-09-02 01:30 | docs-record-guard | e2e 1/1 | 시도0

- 한 일: **계획 45 는 제품이 아니라 기록 규율을 고쳤으니, e2e 질문을 "사용자 흐름이
  도나" 가 아니라 **"진짜 절차가 이 가드를 통과하는가"** 로 잡고 절차 셋(회전 · 반복
  기록 · 계획 완료)을 실제로 밟았다. 증거는 `docs/e2e/docs-record-guard/result.md`.
- **새 `e2e/*.py` 0개** — 검사는 `read_text` 와 `re.search` 뿐이라 프로세스로 잴 것이
  없다(계획 41·42 와 같은 판정). **잴 것이 하나 더 있었지만 그것은 `.py` 가 아니었다** —
  절차는 사람이 밟는 순서지 프로그램이 아니라, 스크립트로 굳히면 회전 절차의 사본을
  하나 더 만드는 것이 된다(ponytail 1번).
- **회전이 이번 반복에 안 걸렸다**(194줄·기록 2회, 상한 300/20). 그래서 회전 절차 셋은
  **`.git` 없는 스크래치패드 전체 사본**에서 밟았다 — `digest` 의 "제자리 변이를 자동
  스냅샷이 커밋한다" 처방 그대로다. 사본이 실물과 같은 판정을 내는 것부터 확인했다(P0 482건 OK).
- **P1 — 회전하고 명부를 안 고치면 RED.** `history_024.md` 를 만들고 명부를 그대로 두니
  482건 중 **`ArchiveIndexTest` 하나만** 죽고 실패 목록이 `['history_024.md']` 를 정확히
  찍었다. 명부에 이름을 더하니 GREEN(P2)이고 `digest.md` 는 **200줄 그대로**다(한 줄에
  이어 붙으니 줄 수가 안 는다). 계획 8절이 *"다음 회전이 RED 를 내면 그것은 설계대로
  동작한 것"* 이라 적은 자리를 실제 회전으로 확인했다.
- **P3 — 설계 3절의 정면 충돌이 실제로 풀렸다.** 아카이브 넷(020·021·022·023)의 산문
  포인터를 통째로 갖고 있는 완료 항목 **한 줄을 지웠는데 GREEN**이다. 명부가 넷을 그대로
  붙들었다. `rules/docs.md` 의 *"완료 항목부터 지운다"* 와 새 불변량이 더는 안 부딪히고,
  계획 43 의 회전이 색인 구멍을 만들던 사고가 **구조적으로 안 난다.**
- **L1 — 가드가 이번 반복 자기 기록을 잡았다.** `status.md` 만 234 로 올리고 돌리자
  `IterationSyncTest` 가 **`'233' != '234'`**. 재발 항목 ④와 같은 모양이고 **리뷰가 없는
  phase 라 예전이면 조용히 어긋난 채 갔을 자리**다. `metrics.md` 도 올려 GREEN(L2).
  어제 테스트 반복에 이어 **이틀 연속 실사용 발화**다.
- **L3 — 계획서를 아카이브로 옮긴 뒤 고아를 전수 재측정했다.** 계획서 2-4 절이 잰
  *"계획서를 쓰는 행위가 고아 판정을 지웠다"* 는 임시 상태였고, `plan_history_031.md`·
  `design_history_031.md` 로 옮겨 그 임시 포인터를 실제로 없앴다. **살아있는 문서 6개로
  복귀 · 아카이브 23개 · 고아 0.** `## 완료` 절에서 **명부가 유일한 포인터인 것은
  다섯**(001·002·003·005·019, 리뷰 [A1] 숫자 그대로)이고, 그중 **살아있는 문서 어디에도
  다른 포인터가 없는 것은 `history_002.md`·`history_005.md` 둘**이다 — 착수 시점의 진짜
  고아 둘이고, **지금 그 둘을 붙들고 있는 것은 명부 한 줄뿐이다.** 001·003·019 를 아직
  갖고 있는 `history_current.md` 의 포인터는 다음 회전에 밀려난다(계획서가 예고한 그대로).
- 결과: 맨몸 실측 — 단위 **482건 OK**(선통과 11.738초 · 최종 11.901초) · `e2e/*.py`
  **19종 전부 rc 0** · 품질 4축 전부 기준선(ko 20/20 · en 19/20 · p95 8.86ms ·
  처리량 10.23·10.25/s · 디자인 4축 통과 최저 4.87:1) · `data/crawl.db` sha256
  `85c96744…` **무변경**(시작·끝 두 번) · 제품 `src/` **0줄** · README 카운트 가드 무변(482·19).
- **러너 파이프 1회 — 연속 0 이 끊겼다.** 사본 기준선 P0 **첫 실행에 `| tail -60`** 을
  붙였다. 방아쇠는 항목이 적어 둔 그대로 *"이미 초록일 것 같은 실행"*(사본을 막 만들고
  무변경을 확인하는 자리)이다. 판정 줄은 살았지만 **테스트 표준출력이 뒤에 붙어 창 안에
  들어온 운**이라 실패로 센다. 즉시 맨몸 재실행했고 이후 전 실행(사본 4 · 실물 3 ·
  e2e 19)이 맨몸이다. `status.md` 의 접기 관찰 카운트는 **0 으로 되돌아간다.**
- 제자리 편집 창(L1 의 의도된 RED)에는 `.mutation-lock` 을 걸었고 L2 GREEN 뒤 지웠다.
- **계획 45 DONE.** 계획서·설계서를 031 로 아카이브, `index.md` 45번 행 완료,
  `status.md` `plan: null`. 회전 안 걸려 명부 갱신 없음(아카이브 23개 그대로).
- 다음: **계획 phase (탐색).**

## 2026-09-02 01:49 | json-contract | 계획 1/1 | 시도0

- 한 일: 탐색 **1~8순위 전수** 후 **계획 46 `json-contract` 수립**
  (`docs/plan_json-contract.md`). 브랜치 `loop/json-contract`, 기점 `3d766be`.
  **제품 코드 0줄 · 테스트 0줄** — 계획서와 기록만 썼다.
- **1~4순위 착수 가능 0건**: 단위 482건 OK · `python3 -m compileall -q src/ tests/ e2e/`
  출력 0줄 · 코드에 `TODO`/`FIXME`/`HACK` **0건** · `docs/candidates.md` 없음
  (`scripts/` 디렉터리 자체가 없다).
- **5~7순위의 열린 넷은 전부 항목 자신이 조건을 적어 뒀다** — 스위트 벽시계 68%
  건은 *"락 e2e 를 여는 계획에서 함께 판단"*, `futures.wait(timeout=None)` 건은
  *"값이 낮아 미뤘다"*, 중단 e2e 여유 건은 *"올리면 목표가 흐려진다"*(고치지 않는 것이
  답), `robots` 비ASCII 건은 *"지금은 도달 불가 · 새 호출처가 생길 때가 고칠 때"*.
  네 건 다 조건이 아직 안 왔다.
- **근거는 8순위 사양에서 나왔다.** `docs/specs/concept.md` 의 `# 확장: 다른 시스템이
  쓰는 검색` 절(2026-09-01 사용자 지시)이 미구현 다섯을 열어 뒀고, 그중 셋이 **이미
  있는 코드 위에서 거짓**이라 새 기능이 아니라 결함이다. **8순위가 1~7순위보다 강한
  근거인 드문 경우**이고, 사용자가 방향을 사양에 적는다는 `CLAUDE.md` 의 규약 그대로다.
- **실측 넷 (저장소 무수정)**: ① `grep -rn "503" src/` **0건** — 사양 디자인 5 가
  요구한 상태 코드가 코드에 한 글자도 없다. ② `grep -rn "StaleIndexError" src/` 는
  `indexer.py` 4곳인데 **`serve.py` 0곳** — `do_GET` 의 `except Exception` 이
  `FileNotFoundError`(DB 없음)와 함께 500 으로 접는다. ③ `grep -n "version"` 이
  `serve.py`·`test_serve.py` 양쪽 **0건**(사양 기능 9). ④ `tests/test_serve.py` 의
  `def test_` **90건 전수**에 화면 응답과 JSON 응답을 맞대 보는 단언 **0건**
  (사양 기능 6 의 판정). 구조상 참이지만(둘 다 `_page_hits`) 붙들려 있지는 않다.
- **`TestMissingDb` 의 독스트링이 *"500 이 맞지만"* 이라고 적어 두고 그 값을 못박고
  있다** — 틀린 코드가 아니라 **사양이 뒤집은 판정**이다. 고쳐야 할 자산이지 고장이 아니다.
- **착수 탐침이 사양 밖의 것을 하나 더 냈다.** `indexer.search` 의 *"빈 목록을 내면
  «결과 0건» 과 구분되지 않는다 — 원인이 다르니 소리를 낸다"* 주석이 드리프트 갈래에만
  붙어 있고, 바로 위 *"아직 색인 전"* 갈래는 `return []` 라 `serve` 가 200 으로 내보낸다
  (`TestUnindexedDb` 가 그 값을 단언 중). 이 저장소가 네 번 닫은 *"실패가 성공으로
  위장된다"* 계보(계획 21·26·29·37)와 같은 모양인데, **여기서는 200 이 옳을 수도 있어**
  (서버는 멀쩡하고 색인만 안 돌았다) 지금 정하지 않고 **설계 갈림길 D** 로 넘겼다.
- **이미 조사된 자리를 다시 열지 않았다.** `digest.md ## 다음 계획 후보` 의 *"`serve.do_GET`
  의 넓은 `try` 를 고치기 전에 쟀다"* 항목은 2026-08-29 야간에 무죄 판정을 받았고
  (유출 0 · `KeyboardInterrupt` 안 삼킴 · `src` 0줄), 이번에 묻는 것은 그 안전성이
  아니라 *"그 안에서 «색인이 없다» 만 따로 꺼내야 하나"* 라 축이 다르다. 넓은
  `except Exception` 은 500 의 자리로 그대로 남는다고 계획서에 못박았다.
- **막혀 있던 후보 하나의 길이 열리는 것을 확인했지만 열지 않았다.** `digest.md
  ## 판단 필요` 의 *"`has_next` 가 «더 있다» 와 «상한에 닿았다» 를 한 bool 로 합친다"*
  항목이 **"`/search` 응답 스키마 변경이라 사용자 확인 후보"** 로 멈춰 있다 — 버전이
  없어 스키마를 못 건드리고, 스키마를 못 건드려 버전도 안 생기던 자리다. 계획 46 의
  `## 하지 않을 것` 에 명시적으로 넣어 범위 밖으로 고정했다.
- **설계 트리거 걸림** — `rules/design.md` 1절의 셋(공개 인터페이스 변경 500→503 ·
  응답 필드 추가 · 대안이 2개 이상 갈림). 갈림길 넷을 계획서 5절에 적어 설계로 넘겼다:
  A 버전을 어디에 두나(본문 필드 · `/v1/search` · 미디어 타입 · 헤더) · B 503 을 어디서
  가르나(`serve` 한 갈래 · `indexer` 공통 예외 · 선검사) · C 화면(`/`)도 503 인가 ·
  D 미색인 DB 도 «색인이 없다» 인가.
- **스텝 셋에 의존 엣지가 없다**(예외 갈래 · 응답 조립 · 테스트뿐). 그러나 `serve.py`·
  `test_serve.py` 를 겹쳐 건드리므로 **순차로 돈다**(`rules/plan.md` 3-1 의 worktree
  분할 조건 ①이 거짓). **임계경로는 스텝 1** — 제품 diff 가 가장 크고 갈림길이 셋 걸린다.
- 결과: 맨몸 실측 **1회** — `PYTHONPATH=src python3 -m unittest discover -s tests`
  **482건 OK**(11.729초). 파이프 0회이므로 **접기 관찰 연속 카운트가 1 부터 다시 선다**
  (앞 반복이 *"이미 초록일 것 같은 실행"* 에 `| tail -60` 을 붙여 0 으로 되돌아갔다).
  제품 `src/` **0줄** · `tests/` **0줄** · `data/crawl.db` 무변경(읽지도 않았다) ·
  README 카운트 가드 무변(482 · 19종) · `.mutation-lock` 없음(제자리 변이 0).
- 기록 갱신: `status.md`(반복 235 · `plan: json-contract` · `phase: 설계`) ·
  `metrics.md`(반복 235 · 계획 phase 26→27) · `index.md`(계획 46 행 추가 + **사양 분할
  꼬리말 정정** — *"`recrawl` 이 유일하게 남은 미구현 사양"* 이 확장 절 때문에 거짓이 돼
  확장 분할 9·10·11 을 적었다). 회전 미달이라 아카이브 23개 그대로 · 명부 갱신 없음.
- 다음: **설계 phase** — `docs/design_json-contract.md`. 갈림길 넷을 `rules/design.md`
  3-1 대로 **서로 다른 출발점**(최소 · 정공법 · 되돌리기 우선)에서 낸다.
