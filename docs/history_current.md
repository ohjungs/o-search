# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**회전 명부는 `digest.md` 의 `## 완료` 절 «아카이브 명부» 줄이 정본이다.**
여기 있던 스물한 회전의 서술(233줄)은 그 줄과 내용이 겹쳤고, 검사가 강제하는 명부도
그쪽 하나뿐이라(`tests/test_docs.py` 의 `ArchiveIndexTest`) **개발 9(반복 269)가 이
자리에서 지웠다** — 회전으로는 300줄 상한을 못 맞추던 세 반복(309 → 372 → 418줄)의
원인이 이 명부였다. **지운 것은 머리말이지 반복 기록이 아니다** — 항목은 여전히
append 전용이고 수정·삭제 금지다. 각 회전의 사유는 `digest.md` 의 같은 줄에, 원문은
`history_<NNN>.md` 에 그대로 있다.

## 2026-09-03 00:05 | runner-quiet | 계획 1 | 시도0
- 한 일: **계획 50 `runner-quiet` 를 탐색해 열었다.** 탐색 1~5순위는 0건이다
  (단위 578건 OK · 린터 없음 · `TODO`/`FIXME`/`HACK` 0 · `docs/candidates.md` 없음 ·
  `digest ## 보류` 비어 있음 · `docs/patches/` 없음 · 활성 `plan_*.md` 0 —
  `status.md` 가 `plan: null`). 6순위 `digest ## 다음 계획 후보` 에서 **`[9]`
  (8점, `unittest -b`)** 를 집었다. 직전 반복이 이어받을 것 넷 중 넷째로 적어 둔 것이고,
  `status.md ## 러너 규율` 의 **누적 30회** 가 그 근거다. 중복 검사: `index.md` 의
  계획 슬러그 27개·`digest ## 완료`·활성 계획·패치 어디에도 러너 규율 건이 없다.
  계획서 `docs/plan_runner-quiet.md` 를 썼고 브랜치 `loop/runner-quiet` 를
  `373cafd`(계획 49 DONE)에서 땄다.
- 결과: **계획 phase 가 근거를 재확인하고 후보가 남긴 「확인할 것 하나」까지 닫았다.**
  맨몸·단독 4회로 쟀다. ① `-b` 없이 `Ran 578 tests in 13.416s` · `OK` · 화면 약 90줄.
  ② `-b` 붙여 `Ran 578 tests in 13.547s` · `OK` · 화면 **5줄** · 거른 테스트 **0건** ·
  시간 차 없음. ③ 「별도 프로세스·스레드는 `-b` 를 안 탈 수 있다」는 **샌 것 0줄**이다 —
  서브프로세스 셋은 원래 `PIPE`/`capture_output` 이고(`test_indexer.py:1098` ·
  `test_quality_eval.py:77` · `test_passage_eval.py:45`), `http.server` 스레드 로그는
  `sys.stderr` 를 호출 시점에 찾아 교체분을 탄다. ④ 임시 스위트로 «실패는 안 삼킨다» 확인 —
  통과 테스트의 마크 둘은 안 나오고 실패 테스트의 마크 둘은 `Stdout:`/`Stderr:` 로 나온다.
  **새로 보이는 것 하나: 맨몸이어도 `OK` 가 화면의 마지막이 아니다.** `unittest` 판정은
  stderr(무버퍼)로, 테스트 `print()` 는 stdout(블록 버퍼)으로 나가서 남은 버퍼가 `OK`
  **뒤에** 늦게 플러시된다(① 에서 `수집 0 페이지`·`1 문서 색인` 이 그 자리에 나왔다).
  「출력이 길다」를 계속 만든 물리적 원인이 그것이고, 계획서 5절 A-2 가 그것을
  **완료 기준으로** 세웠다. 천장도 적었다 — `-b` 는 **초록 실행에서만** 판정을 마지막
  줄로 만든다(④ 에서 `FAILED` 뒤에도 늦은 플러시가 붙었다. 그건 봐야 할 것이라 무해하다).
  스텝 **1개** · 건드릴 파일 **3개**(`README.md:69` · `docs/project.md:14` ·
  `tests/test_readme.py`) · 제품 `src/` **0줄** · 새 파일 0 · 새 의존성 0.
  **설계는 트리거 「3개 이상 파일」에 걸리지만 넘기지 않았다** — 유일한 갈림길
  «`-b` 냐 얇은 래퍼냐» 를 계획 48 리뷰 6 이 실측으로 이미 닫아 `digest [9]` 에 숫자째로
  남겼고, 여기서 설계서를 쓰면 `design.md 2절` 이 경계하는 「없는 결정을 지어내는」
  자리가 된다. 사유는 계획서 6절에 그대로 적었다.
- 러너 규율: **이번 반복 0회 (누적 30회).** 단위 2회·임시 스위트 1회를 전부 맨몸·단독으로
  돌렸다. 파이프·리다이렉션·`;` 잇기 0회, `> /dev/null` 0회. **여섯 반복 연속 0회다.**
  화면이 90줄이라 이번에도 `| tail` 이 손에 잡혔지만, **그 손을 없애자는 것이 이 계획이라**
  안 붙였다. **러너가 아닌 곳에서 파이프 1회는 적어 둔다** — 마감의
  `git push` 에 `| tail -4` 를 붙였다. 계수에는 안 넣지만(러너가 아니고 가려질 판정 줄이
  없다) «0회» 를 «파이프를 한 번도 안 썼다» 로 읽으면 거짓이다.
- 마감: **회전했다 — 337줄 → 69줄.** 자기 항목을 붙인 뒤 세어 상한 300 을 37줄 넘긴 것을
  보고 그 자리에서 돌렸다(스물다섯 회전 연속 지각 0). 직전 반복이 «다음 반복이 자기
  항목을 붙이면 넘을 자리다» 라고 예고한 그 자리이고, 예고가 그대로 맞은 **두 번째**
  사례다. 계획 49 가 DONE 이라 경계가 깨끗해 계획 48 의 e2e 1(274)과 계획 49 전체
  (275~281) 여덟을 통째로 `history_045.md` 로 밀었다 — 남은 것은 이 항목 하나다.
  `digest ## 완료` 의 아카이브 명부에 `history_045.md` 를 더하고 회전 사유를 같은 줄에
  이어 붙였으므로 **새 색인 구멍 0**(`ArchiveIndexTest`). `digest ## 다음 계획 후보` 의
  `[9]` 에는 «계획 50 으로 열었다» 를 달아 다음 탐색이 같은 것을 다시 집지 않게 했다
  (`discover.md` 5절). digest 는 **191줄**로 상한 200 안이다.
- 다음: **개발 1/1.** TDD 로 간다 — 먼저 `tests/test_readme.py` 의 `ReadmeCommandsTest` 에
  «`README ## 검증` 의 `unittest discover` 줄이 `-b` 를 갖는가» 단언을 더해 **RED 를 눈으로
  확인**하고, 그 다음 `README.md:69`·`docs/project.md:14` 에 `-b` 를 넣고, 마지막에
  `README.md` 의 `단위 578건` 을 **579** 로 고친다(같은 커밋 — `test_readme.py` 자신이
  그 숫자를 실제 스위트로 대조한다).

## 2026-09-03 01:10 | runner-quiet | 개발 1 | 시도0
- 일: TDD 순서 그대로 갔다. ① `tests/test_readme.py` 에
  `test_guided_runner_commands_buffer_output` 을 더하고 **RED 를 눈으로 봤다**
  (`FAILED (failures=2)` — 새 단언 하나와, 아직 578 이던 개수 단언 하나).
  ② `README.md` 의 `## 검증` 블록을 `discover -b -s tests` 로, `docs/project.md ## 명령`
  의 테스트(전체)를 `discover -b tests` 로 고쳤다(두 줄은 형태가 서로 다르고 그대로 뒀다
  — 검사는 `-b` 만 본다). 실행 확인 날짜도 오늘로 갱신했고, `-b` 가 **통과한** 테스트의
  출력만 삼킨다는 두 줄을 명령 바로 아래 붙였다. **맨몸 규율 문장은 안 지웠다** —
  `-b` 는 파이프의 대체재가 아니라 파이프를 붙일 이유를 없애는 것이다.
  ③ 두 형태를 각각 맨몸·단독으로 돌려 둘 다 `Ran 579 tests` · `OK` 를 읽고
  ④ `README.md` 의 `단위 578건` 을 **579** 로 고쳤다(같은 커밋). 제품 `src/` **0줄**,
  건드린 파일 3개(`+24 −3`).
- 결과: **579건 OK** · rc 0 · 화면 **5줄**(13.461초 / 13.440초) · 거른 테스트 **0건** ·
  시간 차 ±0.5초 안. **`OK` 가 화면의 마지막 줄이다** — 계획 phase 가 잰 「`OK` 뒤의 늦은
  stdout 플러시」가 **0줄**이 됐다. 새 단언은 **두 문서를 함께** 붙든다: 메모리 exec 탐침
  (스크래치패드, 저장소 파일 무편집)으로 사본에서 `-b` 를 **한쪽씩만** 지워 보니
  대조군 GREEN · README 쪽 삭제 **RED** · project.md 쪽 삭제 **RED** 였다. 한쪽만 고치고
  다른 쪽을 놔두는 것이 이 검사가 막으려던 실패다. **커밋된 변이 0**, `git diff` 는
  의도한 3개 파일뿐. 완료 기준 중 「사라진다」 3줄과 「그대로다」 6줄이 닫혔다.
- **적어 두는 것 하나**: RED 확인용 러너 한 번에 `-k ReadmeCommandsTest` 를 붙였다.
  출력을 자르는 손이 아니라 러너 자신의 필터라 판정 줄은 그대로 나왔지만 「전체를 돌려라」
  와는 다르다 — 그래서 그 뒤 전체를 두 번 맨몸으로 돌렸다. **부수 발견**: `-k` 는
  `defaultTestLoader.testNamePatterns` 를 세팅해서 `test_verification_counts_match_reality`
  안의 내부 `discover()` 까지 걸러 개수가 (5, 20) 으로 나온다. **개수 단언은 전체 실행에서만
  참이다** — 필터를 걸고 그 테스트만 보면 항상 빨갛다. 천장으로 적어 둔다.
- 다음: **테스트 1/1.** 남은 완료 기준은 「실패했을 때 그 테스트의 stdout·stderr 가 둘 다
  보인다」 하나다. 제품 코드 한 줄을 깨서 빨갛게 만들고 `Stdout:`/`Stderr:` 블록을 눈으로
  읽는다. `-b` 의 천장(빨간 실행에는 `FAILED` 뒤 늦은 플러시가 남는다)도 같은 자리에서 본다.

## 2026-09-03 02:20 | runner-quiet | 테스트 1 | 시도0
- 한 일: **남은 완료 기준 하나를 진짜 실패로 닫았다.** ① 대조군을 맨몸·단독으로 돌려
  `Ran 579 tests in 13.458s` · `OK` · rc 0 · 화면 5줄을 읽었다. ② 「실패 시 출력」은
  메모리 탐침으로는 못 재서(진짜 스위트에서 진짜 실패가 나야 한다) `.mutation-lock` 을
  걸고 제자리 변이 1건으로 갔다 — `src/websearch/crawl.py` 의
  `crawl(args, max_pages, …)` → `max_pages + 1`(오프바이원). **그 자리를 고른 이유는
  한 테스트가 두 스트림을 다 쓰기 때문이다**: `test_max_zero_is_rejected_like_workers_and_deadline`
  은 `--max 0` 거절 두 건을 **stderr** 로, 대조군 `--max 1` 의 `수집 0 페이지` 를
  **stdout** 으로 낸 뒤 마지막 줄에서 `crawl()` 에 넘어간 값을 잰다. ③ 갭 탐색 6범주.
  ④ 기록 문서 다섯을 갱신했다.
- 결과: **`FAILED (failures=3)` · rc 1**, 그리고 그 테스트의 실패 보고서에
  `Stdout: 수집 0 페이지` 와 `Stderr: --max 는 1 이상의 숫자 하나를 받는다` 가 **둘 다**
  붙었다(`AssertionError: 2 != 1`). 파급 3건 중 나머지 둘(`=`/공백 형태 인자 전달)은
  stderr 를 안 내서 `Stdout:` 만 붙는다 — **`-b` 는 그 테스트가 실제로 쓴 스트림만 낸다.**
  **천장도 눈으로 봤다**: 빨간 실행은 `FAILED` 뒤로 늦은 `Stdout:` 세 덩이가 더 붙는다 —
  unittest 가 실패한 테스트의 버퍼를 보고서에 넣고 **진짜 스트림에도 되비추는데**
  (`_mirrorOutput`) 되비친 stdout 이 블록 버퍼라 판정 뒤에 늦게 나온다. 초록 실행의
  0줄이 목표였으므로 무해하다. **원복은 초록이 아니라 `git diff` 로 확인**했다 —
  변경 0줄 · 미추적 0개(`.mutation-lock` 삭제 확인) · **커밋된 변이 0** · 새 테스트 0건.
- 갭 탐색: **8점 이상 0건.** 부정 경로는 개발 phase 변이 2건과 `assertTrue(lines, …)` 가
  덮고, **형제 호출부를 전수로 봤다** — `unittest discover` 를 적은 파일은 많아도
  **살아 있는 안내 문서는 `README.md`·`docs/project.md` 둘뿐**이고 나머지는 전부
  아카이브·기록(수정 금지)이라 가드 범위가 맞다. 넘긴 것 셋(`digest` 새 `[5]`):
  `RUNNER_LINE` 이 산문 반례 줄까지 문다 · `BUFFERED` 가 동치인 `--buffer` 장형을 RED 로
  본다(둘 다 거짓 RED 방향이라 무해) · `-k` 아래 개수 단언이 항상 RED(개발 phase 부수
  발견을 영구 자리로 옮겼다).
- 러너 규율: **이번 반복 0회 (누적 30회) — 여덟 반복 연속 0회.** 러너 2회 전부 맨몸·단독,
  변이 실행은 `python3 -B`. 파이프·리다이렉션·`;`·`&&` 잇기 0회. **러너가 아닌 곳에서
  `;` 4회**(`ls` 셋 이은 것 · `grep`+`cat` · `git diff`+`git status`), 파이프·`&&` 는 0회 —
  계수에는 안 넣지만 «0회» 를 «한 번도 안 이었다» 로 읽으면 거짓이라 남긴다.
- 마감: `status.md`(반복 **284** · phase 리뷰) · `metrics.md`(반복 284 · 테스트 **33**) ·
  `index.md` 50번 행 · `digest.md` 191 → **192줄** · 이 항목. `README.md` 는 무변경 —
  단위 579건 · e2e 20종이 그대로 참이다. 회전 없다(**146줄** · 3회).
- 다음: **리뷰 1/1.** 볼 자리 셋 — ① 새 단언이 두 문서를 붙드는 폭이 맞나(오탐 방향 둘)
  ② `-b` 가 실패를 안 삼킨다는 실측이 두 벌인데 더 볼 축이 있나 ③ 저장소 밖 룰 파일을
  안 건드린다는 경계가 계속 옳은가.
