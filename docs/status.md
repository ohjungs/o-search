---
signal: YELLOW
phase: 탐색
step: 1/1
attempt: 0
iteration: 484
updated: 2026-09-09
ctx: 58
night_iterations: 2
night_red: 0
night_retries: 0
plan: throughput-shape
---

## 다음 반복이 읽을 것 — 탐색이 근거 0 으로 멈췄다

반복 483 이 8순위를 전부 돌고 **근거 0** 으로 정지했다. 1~3순위 0건(전수 708 OK · 코드
`TODO` 0 · 패치 0) · 4순위 `docs/candidates.md` 없음 · 5순위 보류 절 비어 있음 ·
6순위는 아래 반복 482 가 위쪽 둘을 재서 닫았고 남은 것은 **사람 몫이거나 여는 조건이
안 열린 것**(`index.md` 다섯째 칸 못은 항목 자신이 「그 전에 넣으면 ponytail 1번」이라
적어 뒀다) · 7순위 반복 실패는 훅이 이미 선 자리 · 8순위 이슈 0건.

**사양 축도 쟀다**: 검색 품질 ko **20/20** · en **19/20**(합격선 80%) 통과. 남은 미검출
1건은 `loaf ← loaves` 하나이고 후보 `[6]` 이 「예외 사전이 필요해 처음부터 뺐다」로
적어 둔 그 자리다 — 축이 통과 중이라 열지 않았다.

**사람이 정할 것 하나를 올린다 — 색인 규모 1단계(10만 문서).** 컨셉 기능 4 의 유일한
미달 축이고, 81계획을 거쳐 기계(재크롤·예산·중단·압축·워터마크·윤리 하한)는 다 섰다.
다만 이것은 **실물 사이트에 밤새 대량 요청을 보내는 일**이라 무인이 혼자 열 자리가
아니다(컨셉 우선순위에서도 4순위 · 「규모는 조급할 이유가 없다」). 시드 폭과 예산을
사람이 정해 주면 그다음은 루프가 돈다.

## 짧은 경로 (반복 482 · 완료)

- **근거**: digest 후보 `[6]` 「실물 후보 절의 «포인터 모집단»을 재는 자가 0개가 됐다」.
  야간 탐색이 1~3순위를 실제로 돌려 **0건**(전수 708 OK · 코드 `TODO` 0건 ·
  `docs/patches/` 0건)이라 6순위로 내려온 자리다.
- **할 일**: 처방(「포인터 모집단에 못을 박는다」)을 **먹여 보고 판정**한다.
  `docs/digest.md` 만 건드린다.
- **완료 기준**: 못을 세울지 접을지가 **실측 숫자로** 갈리고, 그 숫자가 후보 줄에 남는다.
  전수 708 이 그대로 초록.
- **이미 한 것**: 셋 다 쟀고 후보 두 줄을 고쳤다. 전수 708 OK. 커밋까지.

## 처방이 반증됐다 — 못을 세우지 않는다

세 번 쟀고 세 번 다 처방과 갈렸다.

1. **코드 드리프트는 이미 물린다.** `candidate_pointers` 를 `return []` 로 죽이니
   `tests.test_docs` 가 **3건 RED** 다. 후보가 적어 둔 「`StrikeSyncTest` 2건이 통과」는
   참이지만 **전수는 안 조용했다** — 합성 `StrikeGapTest` 가 옆에서 문다.
2. **문구 드리프트는 오늘 이미 나 있다.** `[7] 화면에 안 보이는 텍스트` 줄이
   「계획 51 `hidden-passage` 가 앞 절반을 **연다**」라서 `STRIKE_POINTER` 에 안 물린다.
3. **그 자리를 물게 하면 거짓 RED 다.** 정규식을 `(?:가|이) … 연다` 까지 넓혀 실물에
   먹이니 `strike_gap` 이 「`plan_hidden-passage` 는 `완료` 인데 취소선이 없다」로 울었는데,
   그 항목은 **뒷부분이 아직 열린 다축 항목**이라 취소선이 없는 것이 맞다.
   어휘 못(`닫혔다` ⇒ 취소선)도 히트 7건 중 **2건이 거짓 RED**.

**근본 원인은 문구가 아니다 — 취소선은 줄 단위인데 항목은 다축이다.** 한 계획이 항목의
절반만 닫는 것이 이 파일의 정상 상태라, 포인터를 정확히 읽어도 줄 단위 판정으로는 못 간다.
기계로 가려면 **형식 변경**(항목을 축 단위로)이 먼저고, 계획 76 이 그은 선대로 **사람 몫**이다.

## 함께 고친 것 — `[9]` 의 머리가 본문보다 두 날 늦었다

`[9] /passages 총 p95` 는 본문이 2026-09-07 에 이미 「지금은 문제가 아니다」로 닫혔는데
**머리의 9점이 그대로**라 탐색이 이 파일에서 가장 먼저 읽는 줄이었다 — **오늘 밤이 실제로
그것부터 읽었다.** 취소선을 그었다. 다시 여는 조건은 본문에 그대로 있다.

**digest 줄 수는 205 로 무변이다** — 항목이 한 줄씩이라 덧붙여도 안 늘고, 회전은 안 했다.
