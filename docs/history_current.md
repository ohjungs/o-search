# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**반복 97~122 는 `history_006.md`, 123~128(계획 020 `deadline` 전체)은
`history_007.md`, 계획 21~24(짧은 경로 3건 + 보류 패치 소진, 2026-08-29 14:20~17:40)는
`history_008.md` 로 밀려났다** (2026-08-29 회전 3회).
**계획 25~29 는 `history_009.md`, 계획 30·31(짧은 경로) + 반복 152 탐색 +
계획 33 `clock-injection` 의 계획·설계·개발 1~4 는 `history_010.md` 로 밀려났다**
(2026-08-30 회전, 393줄 → 139줄. 상한 300줄을 넘겨 놓고 두 반복을 갔다).
**계획 33 의 마지막 네 반복(개발 5 · 테스트 1 · 리뷰 1 · e2e 1)은 `history_011.md`**
(2026-08-30 회전, 322줄 → 210줄. 상한을 넘긴 채 반복 두 번을 또 갔다 — 스텝 3 이
"미결(문서 위생)" 로 넘긴 것을 스텝 4 가 닫았다).
계획 33 의 전 phase 서술은 `plan_history_020.md` 의 `## 기록` 에 그대로 있다.
**계획 34 `graceful-interrupt` 의 앞 여덟 반복(계획 1 · 설계 1 · 개발 1 세 번 · 2 · 3 · 4)은
`history_012.md`** (2026-08-30 회전, 300줄 → 85줄. **상한에 정확히 닿아 넘기기 전에 돌렸다** —
앞 두 번은 넘긴 채 반복을 더 갔다).
**계획 34 의 마지막 세 반복(테스트 1 · 리뷰 1 · e2e 1)은 `history_013.md`**
(2026-08-31 회전, 307줄 → 244줄. 상한 300 을 넘긴 그 반복에서 바로 돌렸다 —
`digest ## 반복 실패` 가 지적한 "회전이 두 반복 늦었다" 를 안 되풀이한다).

---

## 2026-08-30 21:0x | 35 deadline-stop | 계획 1 | 시도0 (코드 0줄 · 새 계획을 열었다)

- **탐색.** 열린 계획 0 · 보류 0 · `docs/patches/` 없음 · `TODO|FIXME|HACK` **0건** ·
  `candidates.md` 없음 · 단위 447건 OK(착수 전 직접 실행, 3.25초) · e2e 기준선 유지.
  출처 1~4 가 비어 **5순위(`digest.md ## 다음 계획 후보`)**로 내려갔다.
- **후보를 넷 세웠다.** ① `[5]` `--deadline` 만료가 `stop` 을 안 쓴다 ② 사양 분할 8번
  `recrawl` ③ `[6]` 랭킹 fixture(bm25 정렬이 한 번도 시험되지 않았다) ④ `[6]` `_fetch_one`
  위치 인자 순서 단언. **②는 3순위(사양 미구현)라 ①보다 위인데 열지 않았다** — `pages`·
  `docs` 스키마 변경이자 기존 `data/crawl.db` 마이그레이션이고 `status.md` 한도가
  "실물·스키마를 안 건드린다" 로 못 박아 뒀다(digest `[8]`·`[high]` 도 "사람 승인 필요").
  **아래 `## 사용자 판단` 으로 올린다.** ④는 짧은 경로 크기고 ①에 딸린 축이라 미뤘다.
- **①을 골랐고 근거를 실측으로 다시 세웠다.** `--deadline 2` · 안 답하는 서버 ·
  `Crawl-delay: 30` → **70.08초**, 서버 수신 `t=0.05 / 30.05 / 60.06`.
  **예산 만료(t=2) 뒤에 요청이 두 건 더 나갔다** — digest 가 적은 "꼬리가 붙는다"가
  아니라 `--deadline` 계약("새 요청을 안 던진다")의 직접 위반이다. **항목의 등급을
  올릴 근거를 착수 탐침이 만들었다.**
- **처방이 절반만 맞았다(digest `[7]` 의 네 번째 사례).** `[5]` 는 "`main()` 이
  `stop.set()` 하는 몇 줄이 전부" 라고 적었는데, `.git` 없는 사본에서 갈라 재니
  `stop.set()` 만 **70.08초** · `futures.wait` 자르기만 **70.07초** · **둘 다 10.05초·요청 1건**.
  한 줄씩은 **0초를 회수한다.** 이유는 계획서 2절 — 메인 루프가 `timeout=None` 으로
  워커를 기다려 예산 검사 자리에 못 오고, 깨어나도 워커의 재시도 잠은 `stop` 없이 안 접힌다.
  **계획 34 의 "깨우기와 접기는 나눌 수 없다" 가 예산 축에서 되풀이됐다.**
- **커버 구멍이 곧 이 계획의 본체다.** 세 줄 심은 사본에서 단위 **447건 OK** ·
  `deadline_e2e`·`interrupt_e2e`·`crawl_delay_e2e` **rc=0**. 고치기 전과 후가 똑같이
  초록이라 **새 단언 없이 넣으면 검증되지 않은 변경**이고 되돌려도 조용하다.
- **설계는 생략했다** — 트리거 4개(데이터 형태·공개 계약 파괴·의존성·보안) 무해당.
  digest 가 "뜻이 갈린다, 그 선택부터 정할 것" 이라 남긴 갈림길은 **실측이 지웠다**:
  `stop` 이 접는 것은 *아직 안 보낸 재시도*뿐이고 *이미 나간 응답*은 그대로 줍는다(그
  10초가 소켓 읽기다). 그래서 결론이 저장소의 두 문장에서 연역된다 — "재시도도
  요청이다"(`crawl.py:56`) + "예산이 다하면 새 요청을 안 던진다"(`crawl.py:126`).
- 만든 것: `docs/plan_deadline-stop.md`(스텝 2개·완료 기준·변이 4종·"하지 않을 것" 7항목),
  브랜치 `loop/deadline-stop`(`loop/graceful-interrupt` `eef1b50` 에서). 스크래치패드 사본 정리 완료.
- 다음: **개발 스텝 1**. 제품 ≈8줄이지만 rc 판정(`crawl.py:366`)을 같이 안 가르면
  예산 만료가 **rc 130** 이 되어 공개 계약이 깨진다 — 한 커밋이다.

## 2026-08-30 21:50 | deadline-stop | 개발 1 | 시도0

- 한 일: 예산 만료를 **중단 신호에 이었다**. `src/websearch/crawl.py` 세 자리 —
  ① 만료 분기에서 `stop.set()`(`interrupted` 판정 **뒤**), ② `futures.wait` 의
  timeout 을 남은 예산으로 자르기, ③ `main()` 에 신호 전용 `signaled` 이벤트를 두고
  rc 판정을 `stop.is_set()` → `signaled.is_set()` 으로 가르기. 제품 11줄(주석 제외 7줄).
  단언 3개 추가(`tests/test_crawl.py` `TestBudgetFoldsRetries` 2건 +
  `TestCliTurnsSigintIntoTheSignal` rc 0 1건) · README 건수 447 → 450.
- **TDD 순서를 지켰다** — 세 단언을 먼저 쓰고 **3건 실패를 눈으로 봤다**
  (`False is not true` / `2.01초 not less than 1.0` / `130 != 0`). 그 다음 구현.
- **한 줄씩은 안 듣는다는 계획서 실측이 단언 설계를 갈랐다.** 벽시계로 재면 M1·M2 가
  같이 죽어 어느 쪽이 없는지 못 가린다 → ① 은 **떠 있는 요청이 없는 자리**(쿨다운 대기,
  가짜 시계)에서 신호만 보고, ② 는 `concurrent.futures.wait` 를 감싸 **잠 자체의 벽시계
  길이**를 잰다(주입한 시계는 그 자리를 못 본다 — digest `[7]`). 그래서 변이가 갈린다.
- 결과: 단위 **450건 OK**(447 → +3, 3.5초) · **e2e 18종 전부 rc=0**(합계 155초) ·
  변이 4종 **각각 새 단언 하나씩만** 죽었다(`.git` 없는 사본, `sed` 안 씀):
  M1 `stop.set()` 삭제 → `stop 이 서 있다` / M2 자르기 삭제 → `잠이 예산을 안 넘는다`
  (2.01초) / M3 rc 를 `stop` 으로 → `rc 0` / M4 판정을 `stop.set()` 뒤로 →
  `예산 소진 보고`(`중단 — 1페이지에서 멈춘다` 로 바뀐다).
- **탐침 재실측**(계획서 2절과 같은 자리, `--deadline 2` · 안 답하는 서버 ·
  `Crawl-delay: 30`): 종료 **70.08 → 10.05초** · 서버 수신 **3건 → 1건**(t=0.05) ·
  **rc 0** · `예산 2초 소진 — 0페이지에서 멈춘다`. 남은 10.05초는 소켓 10초(계획 23 의
  의도한 자리) — 계획서 5절대로 안 건드렸다.
- 다음: **테스트 스텝 2**(e2e). 계획서 4절대로 `e2e/deadline_e2e.py` 에 안 답하는 서버 +
  `Crawl-delay 30` 시나리오를 붙이고 `--control` 로 불능 가드를 확인한다.

## 2026-08-30 22:2x | deadline-stop | 테스트 스텝2 | 시도0 (e2e +1시나리오)

- `e2e/deadline_e2e.py` 시나리오 3: 안 답하는 서버(`Crawl-delay: 30`) 하나에
  `--max 5 --deadline 2` 를 CLI 서브프로세스로. **재는 것은 크롤러 내부가 아니라 서버가
  받은 요청 로그**다 — 서버 수신 **1건** · **10.1초** · rc **0** · `수집 0 페이지` ·
  `예산 2초 소진` · DB **0행**. 파일 9.7 → 20.3초(늘어난 10초가 곧 소켓 타임아웃).
- **변이 M1·M2 각각에서 `요청 3건 · 80초` 로 죽었다**(`.git` 없는 사본). 둘이 **같은
  단언으로** 죽는 것은 중복이 아니다 — 가르는 것은 스텝 1 의 단위고, e2e 는 밖에서 본
  계약 하나만 본다. 건수를 단언 앞자리에 두고 시간을 뒤에 뒀다(건수가 늘면 시간도 죽지만
  반대는 아니다 — 느린 기계에서 시간만 흔들린다).
- **`--control` 가드는 파일 전체로는 안 걸린다** — 시나리오 0 이 먼저 rc 2 로 죽어
  새 가드가 실행조차 안 된다. 시나리오 3 만 따로 돌려 `안 답하는 서버가 받은 페이지 요청
  표본이 0개다` · **rc 2** 를 눈으로 확인했다.
- `HANG_CRAWL_DELAY = 30` 은 `MAX_DELAY`(30) **이하여야 한다** — 넘기면 `_fetch_one` 이
  `retries=0` 을 넘겨(crawl.py:107) 잴 대상이 통째로 사라진다. 주석에 박아 뒀다.
- 검증: 단위 **450건 OK**(무회귀·추가 0) · e2e **18종 rc=0**(합계 164초). README 숫자는
  그대로다(파일 수 18 · 단위 450 불변). 다음: **리뷰**.

## 사용자 판단 (루프가 열지 않는다)

- **`recrawl`(사양 분할 8번)이 유일하게 남은 미구현 사양이다** — `concept.md` 기능 5
  "색인된 문서는 30일 이내에 재방문해 갱신·삭제를 반영한다". 지금은 `store.has` 가
  상태 불문 스킵이라 같은 DB 로 재실행하면 **시드부터 스킵돼 0으로 끝나고**(digest `[high]`),
  `indexer` 증분(`url NOT IN docs`)이 갱신을 **아예 반영 못 한다**(digest `[8]`, 실측).
  `pages`/`docs` 스키마 변경 + 기존 `data/crawl.db` 마이그레이션이라 루프가 못 연다.
  같은 수술로 묶이는 것: 018 이 남긴 "옛 열쇠 행" 통합(digest `[high]`).
  **사양 분할의 나머지는 전부 완료라, 승인 없이는 3순위 출처가 영구히 비어 있다.**

## 2026-08-30 22:5x | deadline-stop | 테스트 | 시도0 (갭 1건 · 단위 +1)

- 한 일: 갭 탐색 6카테고리를 스텝 1·2 의 diff 세 자리에 돌렸다. **8점 하나만 처리** —
  `crawl.py:248` 의 `min(wait_for, left)` 에 단언이 없었다. 스텝 1 의 wall-clock
  테스트는 도메인이 하나라 `seconds_until_ready(exclude=busy)` 가 `0.0` → `or None`
  이라 **`min` 가지를 영영 안 밟는다**. `tests/test_crawl.py`
  `test_a_cooling_sibling_domain_never_extends_the_budget_wait` 추가: 답하는 형제
  b.com(쿨다운 5초·큐 안 비움) + 안 답하는 a.com, 예산 0.3초. 6·5·4점 3건은 digest 로.
- 결과: 단위 450 → **451건 OK**(3.48초) · e2e **18종 전수 rc=0**(161초, 파일 무변경).
  변이 2종이 **각각 새 단언 하나만** 죽인다(`min`→`max` · 자르기 삭제, 둘 다 2.01초).
  변이 전 기준선은 450건 전수 통과 — **살아남은 변이가 곧 구멍이었다**(digest `[7]`).
  README 450 → 451(`test_readme.py` 가 즉시 FAILED 로 잡았다). 린터·타입체커 없음.
- **사고**: 3시간 자동 스냅샷 `a9202b6` 이 작업 중 트리를 커밋·**푸시**했다 — digest
  `## 반복 실패` 3회째. 변이는 사본에만 심어 안 샜지만 README 갱신 전이라 그 커밋은
  RED 다. 원격에 이미 갔으므로 전례(`06bc289`)대로 되감지 않고 앞으로 커밋해 덮었다.
- 다음: **리뷰**. 새로 볼 자리 하나 — 예산 자르기가 `:239`·`:248` 두 벌이다.

## 2026-08-30 23:1x | deadline-stop | 리뷰 1/1 | 시도0 (지적 5 · 전부 자동 · 제품 동작 0건)

- 한 일: `fbbe8a3..HEAD` 를 **패스 A(백지) 먼저** 넘겼다(`rules/review.md` 0절).
  후보 8 · 80점 미만 버림 3 · 보고 5 · **고침 5, 승인 필요 0**. 제품(`crawl.py`) 수정
  2곳은 **둘 다 주석**이다 — `stop` 계약이 `set()` 까지 셋으로 넓어졌는데 독스트링은
  아직 읽기 둘이었고(`FakeStop` 이 우연히 `set()` 을 가져 451건이 전부 초록), `:241` 의
  "완료를 기다린다(계약 8)" 주석이 바로 아래 새 코드와 반대를 말했다. 나머지 셋은 문서:
  `docs/index.md` `0/2 → 2/2`, `metrics.md` `반복 175 → 178`(+개발·테스트·리뷰 칸),
  `digest.md` `[4]` 에 `HANG_LIMIT 12.0` 의 얇은 여유(예산 2+소켓 10 **정확히 그 합**).
- **B1·B2 가 같은 뿌리라 `## 반복 실패` 에 2회로 올렸다** — 계획 34 리뷰가 `1abc058`
  에서 같은 것을 잡았다(`index.md 0/4` 인데 개발 4/4 커밋됨). 뿌리는 **기록 파일이
  셋인데 스텝 커밋은 `status.md` 만 본다**. 둘 다 *리뷰가* 잡았다 = 리뷰 없는 phase
  에서는 조용히 어긋난 채로 간다. 처방 후보: `반복`≠`iteration` 이면 죽는 검사 한 줄.
- 버린 3건: `interrupted` 참일 때 `stop.set()` 중복 호출(무해·가드가 더 길다) · 같은
  `stop` 으로 `crawl()` 재호출(그런 호출자가 저장소에 없다) · 독스트링이 가리키는
  `design_graceful-interrupt.md` 부재(diff 밖 · 아카이브 관례).
- 결과(고친 뒤 재실행): 단위 **451건 OK** rc 0(3.58초, 무회귀) · 계획이 건드린 e2e 3종
  개별 **전부 rc=0** — `deadline_e2e`(시나리오 3 수신 **1건**·**10.1s**·rc 0·DB 0행) ·
  `interrupt_e2e`(SIGINT 뒤 10.0s rc 130 · 2번째 Ctrl-C rc -2) · `crawl_delay_e2e`(2.01s/1.00s).
  **린터·타입체커 없음**(`docs/project.md`) — 검증은 이 두 줄이 전부다.
  계획 5절 "하지 않을 것" 7항목·6절 한도 전부 대조 통과. 보안 지적 0.
- 다음: **e2e phase**(`docs/e2e/deadline-stop/` 작성 · 18종 전수 + `--control` 재확인).

## 2026-08-30 23:4x | 35 deadline-stop | e2e 1 | 시도0 (코드 0줄 · 계획 DONE)

- **만든 파일은 `docs/e2e/deadline-stop/result.md` 하나.** 재는 수단(시나리오 3)은 테스트
  스텝 2 가 이미 커밋했고(`f7a1300`) 이 phase 는 **밖에서 다시 재고 기록으로 옮긴 것**이다
  (`rules/e2e.md` 5절). 전체 검증 선통과: 단위 **451건 OK**(3.598초) · 린터·타입체커 없음 ·
  CI 없음(`.github` 부재).
- 검증: **e2e 18종 전수 rc=0**(합계 164초). 실측 **[3] 서버 수신 1건 · 10.1초 · rc 0 ·
  `수집 0 페이지` · `예산 2초 소진` · DB 0행**(착수 탐침 3건·70.08초) · [0] 대조군 16페이지 ·
  [1] 8페이지·간격 최소 1.00s · [2] 버려진 응답 0건. **시나리오 3 만 따로 `--control` →
  rc 2**(`표본이 0개다`) — 파일 전체로는 시나리오 0 이 먼저 죽어 이 가드가 실행조차 안 된다.
- **계획 5절 "하지 않을 것" 7항목을 밖에서 다시 대조했다** — 소켓 10초 무변경(그 10.1초가
  그것이다) · 예절 1.00 / 2.01 / 1.004s · `deadline=None` 16페이지 그대로 · SIGINT rc 130 ·
  두 번째 Ctrl-C rc -2 · DB 무접촉(전부 임시 디렉터리·로컬 서버). **품질 4축·처리량 전부
  기준선 그대로**(recall 100/95% · p95 9.29ms · JS 0 B · 4.87:1 · 10.26/10.29 docs/s).
  **처리량 ±0.01/s 가 "무예산 경로는 바이트 무변경" 의 밖에서 잰 증거다.**
- `deadline_e2e` 가 9 → **19초**다. 늘어난 10초는 회귀가 아니라 **재려고 낸 비용**이다 —
  시나리오 3 이 소켓 타임아웃을 실제로 태운다. 전수 합계도 154 → 164초.
- 다음: **계획 35 DONE.** 새 계획 탐색은 다음 반복이 한다(`main` 병합은 사람 판단).

## 2026-08-30 23:5x | 36 signal-budget-cover | 탐색+계획 | 시도0 (코드 0줄 · 짧은 경로)

- **탐색: 출처 1~4 가 실측 0건**(단위 451 OK · 린터/타입체커 없음 · 코드 `TODO` 0 —
  `ponytail:` 7건은 전부 천장 주석 · `candidates.md`·`patches/` 없음). 6순위로 내려가
  `digest.md` 후보 넷을 갈랐고 `[6]`(예산+SIGINT 겹침 rc 무측정)만 남았다 — `[5]` 는
  e2e 시나리오 3 이 밖에서 이미 덮고, `[4]` 둘은 지우면 즉사하거나 값이 곧 계약이다.
- **착수 탐침이 계획의 모양을 바꿨다** — 겹침을 실측하니 rc 는 두 순서 다 **130**,
  문구는 먼저 온 쪽(`중단` / `예산 2초 소진`)이다. 계약이 이미 참이라 `src` 0줄이고
  **본체는 코드가 아니라 단언**이다. digest 가 "기제상 자명" 이라 적어 점수를 낮춘
  항목인데, 자명한 것을 흔드는 변이 둘이 **451건을 전부 통과한다** — 자명함은 커버가 아니다.
- **탐침이 한도를 스쳤다**: `crawl.main` 은 db 인자가 없어 기본 `data/crawl.db` 가
  **cwd 기준**인데 탐침을 `cwd=저장소` 로 돌렸다. 실물 무변경 확인(크기·mtime 그대로 ·
  `-wal` 0바이트 · `git status` 비었다). `status.md` 한도에 `cwd` 조항을 더했다 —
  `e2e/interrupt_e2e.py:129` 는 이 함정을 이미 주석으로 적어 두고 있었다.
- 다음: **개발 스텝 1**(`tests/test_crawl.py` `TestCliTurnsSigintIntoTheSignal` 에 겹침
  단언 1건 · 452건 · 변이 M1·M2 가 새 단언만 죽이는지). 집안일 `digest.md` 220줄 미결.

## 2026-08-31 00:1x | 36 signal-budget-cover | 개발 1/1 | 시도0 (DONE · `src` 0줄)

- 한 일: `tests/test_crawl.py` 의 `TestCliTurnsSigintIntoTheSignal` 에
  `test_a_signal_wins_over_an_expired_budget_in_either_order` 1건(두 순서를 `subTest`).
  가짜 `crawl` 이 핸들러를 직접 부르고 `kwargs["stop"].set()` 을 **순서만 바꿔** 두 번
  돈다 — 클래스가 이미 쓰는 관용구 그대로다(진짜 `os.kill` 은 SIG_DFL 뒤라 테스트를 죽인다).
  `README.md` 단위 건수 451 → 452.
- **TDD 는 변이가 대신했다** — 제품이 이미 옳아 RED 를 못 만든다. `.git` 없는 사본에서:
  **M1**(`signaled.set()` 을 `if not stop.is_set():` 로 감싸기) → `[만료 먼저]` **한
  갈래만** FAIL · **M2**(만료 갈래 이른 `return 0`) → **두 갈래 다** FAIL. 둘 다 나머지
  451건은 전부 통과한다 — 그것이 이 단언이 없던 이유다. 심기 전 `count(old)==1`.
- **M1 이 두 번째 subTest 의 존재 이유다.** 순서를 하나만 쟀으면 `signaled` 를 `stop`
  뒤에 숨기는 변이가 살아남는다. 처음 쓴 독스트링은 "각각 다른 변이가 걸린다" 였는데
  실측은 **M2 가 둘 다 죽인다** 였다 — 백지 리뷰에서 문장을 실측에 맞춰 고쳤다.
- 결과: 단위 451 → **452건 OK**(3.425초) · `src/` diff **0줄** · `interrupt_e2e` **rc 0**
  (SIGINT 뒤 10.0s/rc 130 · 두 번째 Ctrl-C rc -2) · `deadline_e2e` **rc 0**(시나리오 3
  수신 1건·10.1s·DB 0행). 제품 0줄이라 e2e 전수 대신 이 계약을 재는 둘만 돌렸다.
  `data/crawl.db` sha256 무변경.
- 다음: **계획 36 DONE.** 집안일 `digest.md` 222줄(상한 200)·`history_current` 273줄은
  미결로 넘긴다 — 삭제 전 `index.md`·`plan_history_*` 참조 확인이 선행이다.

## 2026-08-31 01:0x | 37 indexer-interrupt | 탐색+계획 | 시도0 (코드 0줄 · 평소 경로)
- 한 일: 계획 36 마감 확인(status DONE · `index.md` 36번 · digest 완료 항목 — 전부 있었다)
  뒤 탐색. **1~4순위 0건**(실패 테스트 0 · 보류 0 · `TODO|FIXME|HACK` **grep 0건** ·
  `candidates.md` 없음). 5순위 후보를 **믿지 않고 다시 쟀고 넷이 틀렸다**(`digest [7]`
  다섯 번째 사례): ① `_fetch_one` 위치 인자 무단언 `[5]`+`[6]` → **반증**(인자 삽입 변이
  세 자리가 각각 58·8·1건을 죽인다. digest 가 "조용해진다" 고 지목한 `floor`↔`sleep`
  자리가 8건이다). ② `[4]` 간격 시계가 pop 시각 → **011 이 닫았다**(`frontier.py:70`).
  ③ `[7]` OFFSET 페이지네이션 → 게이트("화면이 없다")가 013·015 로 열렸다. 실측 **중복 1
  ·누락 1**(문서 하나 증분 색인). ④ `[6]` `serve` 400 유출 → 16질의×2경로 **500 0건**.
  6순위 `## 반복 실패`("CLI 가 트레이스백을 낸다", 2회)로 내려가 `indexer` 를 쟀다.
- 결과: **착수 탐침이 후보보다 무거운 것을 냈다.** Python 3.9.6 `sqlite3` 은 DDL 을 암묵
  트랜잭션에 안 넣어, 스키마 재구축 중 SIGINT 가 **`docs` 6000행 → 0행**(DROP/CREATE 는
  커밋, INSERT 만 롤백)으로 끝난다 — 그 뒤 검색은 전부 `결과 없음` 이고 크롤 데이터 없음과
  **구별되지 않는다**(21·26·29 가 세 번 닫은 모양). 정상 색인 중 SIGINT 는 무변경이지만
  rc **-2** + `extract.py:60` 프레임 트레이스백. 맨 `sqlite3` 으로 뿌리도 확인했다.
  계획서 `docs/plan_indexer-interrupt.md`(2스텝 · 변이 6종 · 완료 기준 7개) · 설계 생략 ·
  **짧은 경로 아님**(다섯 중 "데이터와 무관" 이 거짓). 탐침은 전부 임시 디렉터리,
  `data/crawl.db` 안 지나감.
- 다음: 개발 스텝 1 — `indexer.py:88` `DROP` 앞 `db.execute("BEGIN")`, RED 는
  `extract.extract_text` 가 `KeyboardInterrupt` 를 던지게 만든다.

## 2026-08-31 01:0x | 37 indexer-interrupt | 개발 스텝1 | 시도0 (제품 1줄 · 단위 +1)

- 한 일: 재구축을 **한 트랜잭션으로 묶었다**. `src/websearch/indexer.py:88` `DROP TABLE docs`
  **앞**에 `db.execute("BEGIN")` 한 줄(+주석 3줄). 단언 1건 추가
  (`tests/test_indexer.py` `TestSchemaDrift.test_interrupted_rebuild_leaves_the_old_index_intact`):
  옛 정의 `docs` 2행을 심고 `mock.patch.object(indexer.extract, "extract_text",
  side_effect=KeyboardInterrupt)` 로 재구축 한복판을 끊은 뒤 **행수 2 · 정의 옛것 유지**를 잰다.
  **TDD 실패를 눈으로 봤다** — 고치기 전 `AssertionError: 0 != 2`(계획서가 예고한 그 모양).
- 변이(`.git` 없는 rsync 사본, 심기 전 `count(원문)==1` 단언): **M1**(`BEGIN` 삭제) →
  새 단언 **하나만** FAIL, 나머지 452건 전부 초록 — 이 자리를 재던 것이 정말 없었다.
  **M6**(`BEGIN` 을 `DROP` **뒤로**) → 같은 단언이 `no such table: docs` 로 ERROR.
  순서가 계약이라는 것이 값으로 박혔다. M2~M5 는 스텝 2 몫이라 안 심었다.
- 결과: 단위 452 → **453건 OK**(3.841초, 무회귀). README 검증 건수 452→453
  (`test_readme.py` 가 먼저 잡았다 — 세 번째 반복). e2e 개별 **전부 rc=0**:
  `indexer_e2e` · `search_api_e2e`(p95 2.22ms) · `tokenizer_e2e` · `pagination_ui_e2e` ·
  `noindex_e2e` · `quality_eval`(한 20/20 · 영 19/20) · `perf_search`(p95 9.26ms).
  **린터·타입체커 없음**(`docs/project.md`) — 검증은 단위+e2e 두 줄이 전부다.
  `data/crawl.db` sha256 `85c96744…` 무변경(8월 29 mtime 그대로).
- 탐침 재실측(임시 디렉터리, 6000문서·본문 3.5KB로 전건 색인 4.53초에 맞춘 뒤 2.0초에 SIGINT):
  **B 재구축 중단 → `docs` 6000행 유지 · 정의 옛것 유지**(고치기 전엔 6000→0). **완료 기준 2 충족.**
  **A 정상 색인 중단 → pages 6000 · docs 0 · integrity ok**(기준 3, 오늘과 동일).
  rc 는 아직 **-2** · Traceback 1회 — **기준 4 는 스텝 2 몫이다.** 성능: 재구축
  `BEGIN` 있음 4.63/4.54초 vs 없음 4.56/4.59초 — **회귀 없음**(전건 색인 경로는 이 분기를 아예 안 밟는다).
- **락 실측(계획 밖 확인, 보고만 한다)**: DB 는 WAL 이라 재구축 4.6초 동안 **읽기는 안 막힌다**
  (0.001초). 다만 커밋 전까지 읽는 쪽은 **옛 정의**를 보므로 `search()` 가 `StaleIndexError`
  를 낸다 — 고치기 전 같은 시점은 새 빈 테이블을 봐서 **조용히 0건**이었다. 이 저장소 관례
  ("조용한 0건은 구분되지 않는다")로는 나아진 쪽이라 **안 고쳤다.** 동시 쓰기 대기는
  2.99초 vs 2.97초로 **사실상 무변화**(`Store` `busy_timeout` 30초 안).
- 다음: **개발 스텝 2** — `indexer.main` 에 `except KeyboardInterrupt` 한 갈래, rc **130** ·
  안내 한 줄. 스텝 1 이 끝났으니 "색인은 바뀌지 않았다" 가 두 갈래 모두에서 참이다.
