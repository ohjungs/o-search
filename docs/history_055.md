# 아카이브 — 계획 52 `focus-ring-combinator` 전체 (반복 303~308)

`history_current.md` 에서 회전으로 밀려난 원문이다. append 전용 · 수정·삭제 금지.
계획 52 가 DONE 이라 경계가 깨끗해 계획·설계·개발·테스트·리뷰·e2e 여섯을 통째로
밀었다. 값은 `index.md` 52번과 `plan_history_038.md`·`design_history_038.md`·
`docs/e2e/focus-ring-combinator/result.md` 가 원본을 갖는다.

## 2026-09-04 13:48 | focus-ring-combinator | 계획 | 시도0

- 한 일: **계획 52 를 열었다.** 탐색은 `rules/discover.md` 순위대로 돌았고 **1~5순위가
  실측 0건**이라 6순위에서 멈췄다 — 단위 `Ran 593 tests · OK`(13.540초, 맨몸·단독) ·
  타입체커·린터 **설정 0개**(의존성 0) · `src/`·`tests/`·`e2e/` 전수 grep 의 유일한
  `TODO` hit 는 `tests/test_indexer.py` 의 **fixture HTML 주석 문자열**이라 코드가
  아니다 · `docs/candidates.md` 와 `scripts/` 는 이 저장소에 없다 · `digest ## 보류`
  는 빈 절이고 `## 판단 필요` 의 승인 항목은 **새 승인이 안 났다**.
- **중복 방지를 전수로 돌린 것이 오늘의 값이다.** `index.md` 51개 행과 `digest` 의
  「다음 계획 후보」를 대조하니 **점수가 가장 높은 [8] 「토크나이저가 못 잡는 세
  가지」가 사실상 이미 닫힌 항목**이었다 — 계획 7 이 적은 5/40 중 넷을 계획 11
  `tokenizer` 가 닫았고(`index.md` 의 `plan_tokenizer` 행: ko 20/20 · en 19/20) 남은
  하나는 별개 후보 [6] 이다. **취소선이 안 그어졌을 뿐**이라 그대로 골랐으면 계획
  하나를 통째로 중복 작업할 뻔했다. 나머지 후보는 전부 여는 조건 미도래(캡·상한·락
  e2e·실물 코퍼스·실물 검색 로그·`robots.py` 새 호출처·괄호 두 겹)다.
- 결과: **계획 52 `focus-ring-combinator`** — 6순위 후보 「`focus_rule` 의 셀렉터
  가드는 결합자 뒤를 안 본다」(계획 49 리뷰 1 등재). 계획서
  `docs/plan_focus-ring-combinator.md` 신규 · `index.md` **52번 줄 신설(진행 0/1)**.
- **계획 phase 가 셋을 직접 쟀다**(`PYTHONPATH=src:e2e`): ① 결합자 ` `·`>`·`+`·`~`
  뒤에 링을 그리는 셀렉터 **4/4 가 `('--focus', '2px')` 로 통과**(거짓 초록 재현) ·
  ② **후보에 없던 다섯째 갈래** — `a:focus-visible,.x` 도 통과한다(조건 5 가 쉼표
  목록을 조각으로 안 가른다. `.x` 는 링이 **항상** 그려지는 상자다) · ③ **물려받은
  처방이 절반이었다** — 후보가 적어 둔 `re.split(r"[ >+~]", part)[-1]` 은
  `a:focus-visible:not(.x + .y)` 를 **거절**해(괄호 안 결합자가 split 을 넘어뜨린다)
  계획 44·49 가 지킨 「오탐 0」을 깨는 첫 사례가 된다. `digest` 의 「기록된 처방은
  실행 전에 다시 재라」가 계획 41·44 에 이어 **세 번째**로 맞았다.
- 다음: **설계.** 트리거 하나가 걸린다 — 「대안이 2개 이상 갈림」. A 순진한 split ·
  B `INDIRECT_RE` 선지우기 · C 괄호 깊이 세기 셋 다 결합자 4종은 닫는데 **오탐/미탐
  집합이 다르다.** 셋을 다 짜서 같은 표에 먹이고 **갈리는 행**으로 고른다(계획 49
  교훈: 말로 고르면 갈림이 계획서 자리에 없다). 제품 `src/` **0줄** 계획이고 고치는
  것은 **재는 자**다 — 제품 CSS 에 결합자 뒤 링이 0곳이라 오늘의 3.56:1 은 참이다.
- 원격을 다시 읽었다: `git ls-remote origin loop/hidden-passage` = **`20ee8d5dd307…`**
  = 로컬 `HEAD`. 새 브랜치 `loop/focus-ring-combinator` 의 **기점은 `20ee8d5`**
  (`main` 아님 — `origin/main` 은 `687a159`(계획 47)이라 `README.md` 의 `단위 593건`·
  `e2e 21종` 단언이 거기서는 첫 커밋부터 RED 다). **PR #7 무접촉**(OPEN·미병합) ·
  `--no-verify`·`--force` **0회** · `docs/specs/`·`data/crawl.db` 무변경.
- **러너 규율 위반 1회를 적는다** — 계획 커밋 뒤 확인 실행에서 단위 러너에
  `| tail -4` 를 붙였다(판정 줄은 살아 있었지만 규율은 「맨몸」이다). 곧바로
  **맨몸·단독으로 다시 돌려** `Ran 593 tests · OK` 를 화면 마지막 줄로 봤다.
  나머지 호출은 전부 맨몸·단독이고 출력 조작 0 · 명령 잇기 0 이다. 계획 51
  리뷰 3 에 이어 **두 번째**라 `digest ## 반복 실패` 의 방아쇠에 가까워졌다.
  회전 없음(상한 안).

## 2026-09-04 14:35 | focus-ring-combinator | 설계 1/1 | 시도0

- 한 일: **설계서 `docs/design_focus-ring-combinator.md` 신규.** 제품 `src/` **0줄** ·
  코드 변경 0(설계는 계약만 적는다). 계획서가 「말로 고르지 않는다」고 못박은 대로
  **세 대안을 다 짜서 같은 25행 표에 먹였다** — 오늘의 조건 5 는 **8행 틀림**,
  후보가 물려준 순진한 처방 **6**, **A**(`INDIRECT_RE` 선지우기) **4**,
  **C**(정규식 lookahead) **4**, **B**(괄호·대괄호 깊이 세기) **0**. B 를 골랐다.
- **갈린 것은 결합자가 아니라 괄호 안이었다.** 셋 다 결합자 4종(` `·`>`·`+`·`~`)과
  쉼표 목록은 닫는다. 표를 가른 행은 여섯이고 전부 **정상 셀렉터를 거절하는 오탐** 쪽이다:
  `:not(.x + .y)` · `:is(.x + .y)` · `:is(.x, .y)` · `:not(.x, .y)` · `:is(.x, .y > .z)` ·
  `[class~=btn]`. **A 는 처방을 한 칸 민 것뿐이었다** — `:is()`/`:where()` 는 투명이라
  `INDIRECT_RE` 가 안 지우므로(계획 49 오탐 표가 붙들고 있는 바로 그 성질) 먼저 지워도
  `:is()` 안의 결합자·쉼표 앞에서 똑같이 넘어진다. C 는 결합자만 보고 **쉼표를 안 본다**.
- **후보에도 계획서에도 없던 행을 설계가 표를 짜다 찾았다** — `a:focus-visible[class~=btn]`.
  `[class~=btn]` 의 `~` 는 CSS **속성 연산자**인데 결합자와 같은 글자다. 셋 중 대괄호를
  세는 B 만 안 문다. 계획 phase 가 「다섯째 갈래(쉼표 목록)」를 찾은 데 이어
  **재 보면 항목이 또 늘어난다**는 것이 이 계획에서 두 번째다.
- **가장 위험한 가정을 깼고 거짓이었다** — B 첫 판은 꼬리 공백(`a:focus-visible `)에서
  **죽었다**. 마지막 compound 가 빈 문자열이 돼 오탐이 난다. 제품 셀렉터를 실제로
  뽑아 보니 `RULE_RE.group(1)` 이 앞 규칙의 개행을 달고 온다(`'\n.sb input:focus-visible,…'`).
  개발로 넘기지 않고 설계 안에서 **`part.strip()` 을 계약에 넣고** 다시 쟀다(25/25).
- **변이가 셋에서 넷으로 늘었다.** 계획서의 셋(마지막 compound·쉼표 가르기·결합자 한 글자)은
  **A·C 도 전부 통과시킨다** — 고른 안을 붙드는 자가 없다. 넷째 **「깊이 세기 제거
  (= 순진한 처방)」** 가 ★ 여섯 행을 전부 죽인다. 이것이 선택과 테스트를 잇는 자리다.
- **A·C 의 오탐은 오늘 안 터진다** — 제품 CSS 에 괄호도 대괄호도 0곳이다. 그래도 버린 이유는
  결과의 모양이다: 오탐 = **rc 2 → e2e 전수 RED**. 누가 `:is()` 나 속성 셀렉터를 처음 쓰는
  날 제품이 아니라 **재는 자 때문에** 빨개진다. 계획 44·49 가 지킨 「오탐 0」이 최대
  위험이라고 계획서 5절이 못박았으므로 **틀린 행 0 을 낸 안**을 골랐다.
- 기준선(코드 무변): 단위 **`Ran 593 tests · OK`**(13.453초, 맨몸·단독) ·
  `PYTHONPATH=src python3 e2e/design_check.py` **rc 0** · `[3]` 축 **16행** ·
  라이트 `--focus on --bg-page` **3.56:1** · `4축 전부 통과.`
- 천장은 그대로다 — CSS 파서 안 만든다 · `INDIRECT_RE` 의 **괄호 한 겹** 천장 무변
  (이 변경은 «어디를 볼까» 만 옮기고 «무엇을 지울까» 는 안 건드린다) · 별개 후보 [4]
  (두 겹 중첩)는 안 연다 · 백슬래시 이스케이프(`.a\,b`)는 안 읽는다(0곳).
- 다음: **개발 1/1** — `e2e/design_check.py` 에 `_top_level` 헬퍼 + 234행 `if` → 루프,
  `tests/test_design_check.py` 의 두 표에 행 **13개**(미탐 7 · 오탐 6) 추가. 파일 2개.
- **명령은 전부 맨몸·단독으로 돌렸다** — 파이프·리다이렉션 0. 계획 phase 가 남긴
  「러너 규율 위반 1회」의 회전은 없다(이번 반복 위반 0).
- `--no-verify`·`--force` **0회** · 훅 우회 0 · `docs/specs/`·`data/crawl.db` 무변경 ·
  **PR #7 무접촉**(OPEN·미병합) · `src/` **0줄**.

## 2026-09-04 15:20 | focus-ring-combinator | 개발 1/1 | 시도0

- 한 일: **설계 계약을 그대로 옮겼다.** `e2e/design_check.py` **+34줄**(헬퍼 `_top_level`
  22줄 + 조건 5 를 쉼표 조각 루프로 · 주석 포함) · `tests/test_design_check.py` **+40줄**
  (전부 기존 두 표에 붙인 행 — 새 테스트 메서드 0). 제품 `src/` **0줄** · 새 파일 0 ·
  새 의존성 0 · `focus_rule` 시그니처와 rc 계약·에러 메시지 문자열 무변.
- **RED 를 먼저 눈으로 봤다** — 미탐 7행(결합자 4종 · 공백 낀 `>` · 쉼표 목록 ·
  `:is(:focus-visible) .hint`)을 심고 `FAILED (failures=7)`. 구현 뒤 `Ran 8 tests · OK`.
  **새 오탐 8행은 심는 날부터 초록이었다** — 오늘의 조건 5 도 통과시키던 행이라
  RED 가 안 난다. 그 여덟을 붙드는 것은 단언이 아니라 **변이 ④** 다.
- **변이 넷 전부 죽는다**(`.mutation-lock` 아래 제자리 · `python3 -B` · 전부 원복 ·
  심기 전 `count(원문)==1` 단언): ① 마지막 compound → 조각 전체 `failures=6` ·
  ② 쉼표 가르기 제거 `failures=1` · ③ 결합자 집합에서 `~` 제거 `failures=1` ·
  ④ **깊이 세기 제거(= 순진한 처방) `failures=6`** — 설계서 ★ 여섯 행이 전부
  **오탐**으로 뒤집힌다. 넷째가 A·C 를 B 와 가르는 자리라 「깊이를 센다」가 장식이
  아님을 그것 하나가 증명한다(계획서의 셋만으로는 A·C 도 통과한다).
- **설계서 표를 구현된 코드에 다시 먹였다 — 틀린 행 0.** 다만 **행 수가 24다**:
  설계서는 「25행」으로 셌는데 `(4행)`·`(3행)` 묶음을 풀어 세면 24개다. 판정이 아니라
  **계수의 차이**라 고치지 않고 적어만 둔다(통과/거절 집합은 B 열과 한 행도 안 갈렸다).
- 결과: 단위 **`Ran 593 tests in 13.386s` OK**(맨몸·단독) — **건수 무변**이라
  `README.md` 의 `단위 593건`·`e2e 21종` 단언 그대로(문서 0줄) ·
  `e2e/design_check.py` **rc 0** · `[3]` 축 **16행** · 라이트 `--focus` **3.56:1** ·
  검사기를 서브프로세스로 부르는 **`e2e/pagination_ui_e2e.py` 도 rc 0**(호출처를 함께
  봤다) · `data/crawl.db`·`docs/specs/` 무변 · `--no-verify`·`--force` 0회 ·
  **PR #7 무접촉**(OPEN·미병합) · RED 1(의도된 것) · 재시도 0.
- **러너 규율을 두 번 어겼다.** 변이 ② 판정에서 `2>/tmp/nul`, 그 뒤 한 번 더
  `> /dev/null` 을 러너에 붙였다(**리다이렉션 2회**). 둘 다 맨몸으로 즉시 재실행해
  판정을 원문에서 읽었고 조작은 0회지만 규율은 0회가 아니다. 방아쇠는 둘 다
  «출력이 길어 줄이고 싶다» 였다 — 계획 50 이 `-b` 로 없앤 방아쇠와 **다른 쪽**이라
  기제로는 안 막힌다.
- **되돌리기를 잘못 골라 미커밋 구현을 통째로 날렸다.** 변이 ① 을 되돌리려고
  `git checkout -- e2e/design_check.py` 를 썼는데 HEAD 에는 구현이 없어 34줄이
  사라졌다(재작성해 복구 · 테스트 파일은 무사 · 잃은 것은 시간뿐). **커밋 전 단계의
  제자리 변이는 `git checkout` 이 아니라 스크래치패드 사본으로 되돌린다** — 이후
  변이 셋은 사본에서 `cp` 로 되돌렸다. `.mutation-lock` 은 걸었지만 그 락이 막는 것은
  「변이가 커밋되는 것」이지 「원복 수단이 틀린 것」이 아니다.
- 위 둘을 `digest ## 반복 실패` 에 적었다 — 러너 규율은 ⑳ 로(계획 phase 의 1회를
  합쳐 **28 → 31회**), 되돌리기 교훈은 같은 항목 안에. `index.md` 의
  `plan_focus-ring-combinator` 행은 `0/1 → 1/1`,
  `metrics.md` 반복 304→305 · 개발 72→73.
- 원격을 다시 읽었다: 개발 커밋 **`5f86d08e9a2f…`** 를 푸시하고
  `git ls-remote origin loop/focus-ring-combinator` = **`5f86d08e9a2f…`** = 로컬 `HEAD`.
- 다음: **테스트 1/1** — 갭 탐색(조건 5 가 새로 읽는 자리의 이웃)과 e2e 전수 21종.

## 2026-09-04 16:10 | focus-ring-combinator | 테스트 1/1 | 시도0

- 한 일: **갭 탐색이 초록 아래 살아 있던 변이 하나를 찾았다.** `_top_level` 의
  가드절 `max(0, depth - 1)` 을 `depth - 1` 로 지우고 전 스위트를 돌렸더니
  **`Ran 593 tests · OK`** — 개발이 심은 변이 넷이 전부 다른 줄을 겨눠 이 줄만
  아무도 안 붙들고 있었다(룰 3절 ①가드절 + ⑥변경된 함수의 새 분기, 같은 자리).
  살아 있으면 안 열린 `)` 에서 깊이가 음수가 되고, 그 뒤의 결합자·쉼표가 전부
  «괄호 안» 으로 보여 셀렉터가 한 덩어리가 된다 — **이 계획이 닫으러 온 거짓 초록**이
  다른 문으로 돌아온다. 8점으로 매기고 이번 스텝에서 닫았다.
- **행 하나.** `tests/test_design_check.py` 미탐 표에 `(":focus-visible" →
  ":focus-visible) .hint", "포커스용이 아니다")` **+7줄**(그중 5줄 주석) ·
  새 테스트 메서드 0 · 새 파일 0 · 제품 `src/` **0줄** · `e2e/design_check.py`
  **0줄**(테스트 phase 는 검사기를 안 고쳤다) · 단위 건수 **593 무변**이라
  `README.md` 의 `단위 593건`·`e2e 21종` 단언 그대로다(문서 0줄).
- **RED 를 먼저 눈으로 봤다** — 가드를 지운 채 행을 넣고 `FAILED (failures=1)`.
  실패 출력이 증거다: 링을 `.hint` 에 그려 놓고 `라이트 --focus on --bg-page
  3.56:1 / 기준 3.0 OK` 를 그대로 찍는다. 가드를 되돌리자 `Ran 593 tests · OK`.
- **변이 다섯 전부 죽는다**(`.mutation-lock` 아래 제자리 · `python3 -B` · 심기 전
  `count(원문)==1` · 원복은 **스크래치패드 사본에서 `cp`** — 개발 phase 의 교훈을
  그대로 썼고 `git checkout` 은 한 번도 안 썼다): ① 마지막 compound 제거
  `failures=7`(개발 때 6 · 새 행이 일곱째) · ② 쉼표 가르기 제거 `failures=1` ·
  ③ `~` 제거 `failures=1` · ④ 깊이 세기 제거 `failures=6` · ⑤ **가드절 제거
  `failures=1`(심은 날엔 `OK` 였다)**. 되돌린 뒤 `git diff -- e2e/design_check.py`
  **0줄** · `.mutation-lock` 생성·삭제 확인 · 커밋된 변이 0.
- **나머지 다섯 카테고리는 0건** — ②경계값은 실측으로 닫았다(꼬리 쉼표 `a:focus-visible,`
  와 빈 조각 `,,` 둘 다 **거절** = 측정 불능이 맞다) · ③격리(`_top_level` 은 순수
  함수) · ④불안정(`sleep`·랜덤·순서 단언 0) · ⑤보안(신뢰 경계 아님 — 제 저장소의
  CSS 를 읽는다) · ⑦동시 실행(공유 상태 0).
- **8점 미만 둘을 `digest ## 다음 계획 후보 (테스트 phase 갭, 8점 미만)` 에 등재했다** —
  [5] 안 **닫힌** `(` 는 아직 거짓 초록이다(실측 `a:focus-visible:not(.x .y` 통과 ·
  깨진 CSS 라 제품에 실재 불가 · 막으려면 「끝에서 depth != 0 이면 거절」이 필요한데
  그것은 설계서 6절이 그은 «CSS 파서를 안 만든다» 선 위다) · [4] 문자열 안 홀괄호가
  안 무는 것은 `COMMENT_OR_STRING_RE` 를 **먼저** 돌리는 순서에 기댄 성질인데 그
  순서를 붙드는 단언이 0개다(실측 `[data-x="(a)"]` 통과 — 짝 맞는 괄호라 어차피 무해).
- **단언을 낮춘 곳 0** — 기존 표의 판정도 문구도 안 고쳤다. 더한 것은 행 하나뿐이다.
- 결과: 단위 **`Ran 593 tests in 13.488s` OK**(맨몸·단독) · `e2e/design_check.py`
  **rc 0** · `[3]` 축 **16행** · 라이트 `--focus` **3.56:1** · **`e2e/pagination_ui_e2e.py`
  rc 0** · `data/crawl.db`·`docs/specs/` 무변 · `--no-verify`·`--force` 0회 ·
  **PR #7 무접촉**(OPEN·미병합) · RED 1(의도된 것) · 재시도 0.
- **러너 규율 위반 0회.** 파이프·리다이렉션 0 — 변이 ①·④ 의 실패 리포트가 화면을
  채웠지만(그것이 앞 phase 두 번의 방아쇠였다) 자르지 않고 원문에서 판정을 읽었다.
  누적 31회는 그대로고 회전 없다. **러너 아닌 곳에 파이프 1회는 적어 둔다** —
  `git push … 2>&1 | tail -3`(진행 로그) · 원격 SHA 는 맨몸 `ls-remote` 로 따로 읽었다.
- 원격을 다시 읽었다: 테스트 커밋 **`872dfca23b31…`** 을 푸시하고
  `git ls-remote origin loop/focus-ring-combinator` = **`872dfca23b31…`** = 로컬 `HEAD`.
  `index.md` 의 `plan_focus-ring-combinator` 행에 테스트 phase 를 적었고,
  `metrics.md` 반복 305→306 · 테스트 37→38.
- 다음: **리뷰 1/1**.

## 2026-09-04 14:45 | focus-ring-combinator | 리뷰 1/1 | 시도0

- 한 일: 백지 패스(diff·수정 파일만)를 먼저 끝내고 패스 B 를 열었다. **보고 3건 ·
  전부 low · 자동 수정 3 · 승인 필요 0 · 반려 0** — 계획 52 는 반려 없이 e2e 로 간다.
- **[R52-1] 패스 A · 값이 있는 쪽** — `_top_level` 은 괄호를 **세기만 하고 안 읽으므로**
  이 계획이 닫은 미탐이 한 겹 안에서 그대로 산다. 리뷰가 셀렉터 7개를 직접 먹여 읽었다:
  `:is(a:focus-visible > .hint)` · `:is(a:focus-visible .hint)` ·
  `:where(a:focus-visible + .hint)` **셋 다 `('--focus', '2px')` 통과**(링은 `.hint` 에
  그려진다), 반대로 `:is(.x, a:focus-visible) .hint` 와 `a:focus-visible:has(.x) .hint`
  는 정상 거절. **회귀가 아니라 안 적힌 천장이다** — 설계서 4절이 「천장은 `ponytail`
  주석으로 코드에 남긴다」 고 적고 세 개를 적었는데 이 갈래가 그중에 없다.
  고치지 않고(재귀 = «CSS 파서를 안 만든다» 밖) `_top_level` 에 천장 5줄 · `digest`
  에 [5] 등재. **593건은 이 자리에 눈이 멀 수밖에 없다** — 통과가 정답인지 오답인지가
  어느 표에도 안 적혀 있었고, 그것을 적은 것이 이번 산출물이다.
- **[R52-2] 패스 A** — 오탐 표 docstring 「뒤 **여덟**」인데 실제 더한 행은 **아홉**이다
  (★ 여섯 + 앞 compound + 꼬리 공백 + 줄바꿈 낀 쉼표). 개발이 스스로 적어 둔
  「설계서 25행 / 실제 24행」과 같은 종류라 같은 방식으로 닫았다 → `아홉`.
- **[R52-3] 패스 B** — 계획서 완료 기준 5(«N > 593»)가 4절(«표에 **행**을 더한다 ·
  새 클래스·파일 0»)과 **모순**이다. `subTest` 행은 건수를 안 늘리고, 늘렸다면
  `README.md` 의 `단위 593건`·`tests/test_readme.py` 가 같은 커밋에서 RED 다.
  «593 무변» 이 옳은 값이라 계획서 5절에 취소선 + 사유를 적었다 — e2e phase 가 이
  기준으로 DONE 을 판정하기 전에 닫아야 하는 자리다.
- **버린 것 2건(80점 미만)**: 결합자 집합에 `\r`·`\f` 없음(도달 불가 · 저장소 0곳) ·
  문자열 지우기 순서 의존(테스트 phase 가 이미 `digest [4]` 로 등재 — 중복).
- 고친 것: `e2e/design_check.py` **주석 5줄**(실행 경로 0줄) · `tests/…` docstring
  **한 낱말** · `docs/plan_…` 한 줄 · `digest.md` 한 항목. 제품 `src/` **0줄** ·
  단언을 낮춘 곳 **0** · 새 파일 0.
- 결과: 단위 **`Ran 593 tests in 13.562s` OK**(맨몸·단독 · 건수 무변) ·
  `e2e/design_check.py` **rc 0** · `[3]` 축 **16행** · 라이트 `--focus` **3.56:1** ·
  `data/crawl.db`·`docs/specs/` 무변 · `--no-verify`·`--force` 0회 ·
  **PR #7 무접촉**(OPEN·미병합) · RED 0 · 재시도 0.
- **러너 규율: 가린 것 0회, 섞은 것 1회.** `design_check.py` 뒤에 `; echo "rc=$?"` 를
  한 번 붙였다 — 앞서 맨몸으로 돌려 전문을 읽은 뒤였고 판정도 안 잘렸지만, 형태가
  digest ⑭ 의 «러너를 다른 명령과 한 호출에 섞기» 라 적어 둔다. 누적 31 은 안 올렸다
  (그 계수는 «판정 줄을 가린 것» 이고 이번엔 안 가렸다).
- **원격을 다시 읽은 값이다** — 리뷰 커밋 `d83dd3e` 를 푸시하고
  `git ls-remote origin loop/focus-ring-combinator` 가
  `d83dd3ee8f6d89d8e226f16700e46d86e222d748` 로 같음을 확인했다(`56c522e..d83dd3e`).
  `git push`·`ls-remote` 에도 파이프·리다이렉션 **0회**.
- 다음: **e2e 1/1** — 새 e2e 문서 없이 **전수 21종 rc 0** 과 완료 기준 1~8(5번은
  위 정정판)로 계획 52 를 닫는다.

## 2026-09-04 17:05 | focus-ring-combinator | e2e 1/1 시도0

- 한 일: **계획 52 DONE.** 완료 기준 1~8 을 실측으로 판정하고
  `docs/e2e/focus-ring-combinator/result.md` 를 새로 만들었다(이 phase 가 만든 유일한
  파일 · 새 e2e 스크립트 **0** · 제품 `src/` **0줄**). 계획서·설계서를
  `plan_history_038.md`·`design_history_038.md` 로 아카이브했다.
- **개발·테스트가 못 쟀던 자리를 쟀다 — 제품 파일을 실제로 망가뜨렸다.** 앞 phase 들은
  `check_contrast()` 를 프로세스 **안에서** 불러 비틀린 CSS **문자열**을 먹였는데,
  이 계획이 겨눈 고장은 «단언이 아니라 **종료 코드**가 초록» 이던 것이라 프로세스
  경계에서 재야 진짜다. `src/websearch/serve.py` 의 링 셀렉터 꼬리만 바꿔
  `PYTHONPATH=src python3 e2e/design_check.py` 를 통째로 돌렸다.
- **망가뜨렸을 때 5/5 가 rc 2** — 결합자 ` `·`>`·`+`·`~` 넷과 쉼표 목록 `,.hint`.
  앞 두 조각(`.sb input:focus-visible`·`.sb button:focus-visible`)은 그대로 두었다:
  «한 조각만 어긋나도 거절» 이 계약이기 때문이다. **종료 코드보다 강한 증거는
  `[3]` 축의 모양이다** — 다섯 경우 모두 대비 표가 16행 → **14행**으로 줄고
  `--focus on --bg-page 3.56:1` 두 줄이 **사라진다**. 착수 때(반복 303)는 4/4 가
  `('--focus','2px')` 를 돌려주며 통과했으니, 이 계획이 닫으러 온 「아무도 못 보는 링
  위에서 3.56:1 을 찍는 것」이 실제로 안 찍히는 것을 여기서 봤다.
- **정상일 때 rc 0 · 16행 · 3.56:1 · `4축 전부 통과.`** 그리고 **오탐 쪽도 제품
  파일에서 쟀다** — 설계서가 세 대안을 가른 ★ 둘(`a:focus-visible:not(.x + .y)` ·
  `a:focus-visible[class~=btn]`)을 심으니 **둘 다 rc 0 · 16행 · 3.56:1**. 순진한
  `re.split` 처방이었다면 이 둘이 rc 2 = **e2e 전수 RED** 였다 — 「오탐 0」이 왜 최대
  위험이었는지가 프로세스 경계에서 눈에 보인다.
- **값이 하나 더 나왔다 — `_top_level` 의 두 호출처를 서로 다른 행이 붙들고 있다.**
  기준 7 을 인용으로 넘기지 않고 변이 ④(깊이 세기 제거)를 다시 심었는데, 처음에
  **안쪽 호출처만**(`_top_level(part.strip(), …)` → `re.split`) 되돌리니
  `FAILED (failures=4)` 였다. 남는 둘(`:is(.x, .y):focus-visible` ·
  `:is(.x, .y > .z):focus-visible`)은 **바깥 쉼표 가르기**(`_top_level(selector, ",")`)가
  붙들고 있고, 둘을 다 되돌려야 설계서·테스트 phase 가 셈한 **`failures=6`** 이 된다.
  「깊이 세기」가 한 덩어리가 아니라 **두 자리**이고 각각 증인이 있다는 뜻이다 —
  한쪽만 리팩터링으로 무르면 절반이 조용히 열린다. ①②③⑤ 는 개발 305·테스트 306
  실측을 인용했고 **여기서 다시 재지 않았다**(result.md 에 그렇게 적었다).
- 결과: 완료 기준 **1~8 전부 통과**. 단위 **`Ran 593 tests in 13.470s` OK**(맨몸·단독,
  선행 관문) · 원복 뒤 재실행 **13.500초 OK** · **593 무변**이라 `README.md` 의
  `단위 593건`·`e2e 21종` 단언 그대로(리뷰 1 이 정정한 기준 5 가 옳았다) ·
  **e2e 21종 전수 rc 0** · 품질 4축 무변(ko 20/20 · en 19/20 · 문단 정확도 100.0%
  p95 1.47ms · `perf_search` p95 8.85ms · 크롤 10.22/10.24 · 문서집합 sha1 무변).
- **`crawl_e2e` 최소 간격이 0.999s 로 찍혔다**(계획 49 때 1.001s). 하한 0.95 라 통과고
  이 계획과 무관한 **계측 오차**다 — 이 계획이 고친 것은 재는 자(`design_check.py`)
  하나뿐이라 크롤 간격에 닿지 않는다. 손댈 신호가 아니라 적어만 둔다.
- 범위: `git diff --stat 20ee8d5..HEAD` 에 `src/` **0줄** · `data/crawl.db` sha256
  `85c967…5bda18` 무변 · `docs/specs/` 무변 · 새 의존성 0.
- **`.mutation-lock` 을 두 번 썼고 원복은 둘 다 스크래치패드 사본이다**(제품 CSS 7회 ·
  `design_check.py` ④ 2회). `git checkout` **0회** — 개발 phase 가 산 교훈 그대로다.
  되붙인 뒤 `git status --short` 와 `git diff --stat` 이 **빈 출력**임을 확인했다.
- **러너 규율 위반 0회** — 파이프·리다이렉션·명령 잇기 **0**(`git push`·`ls-remote`
  포함). 변이 아래 실패 리포트가 길어도 자르지 않고 원문에서 읽었다. 누적 31 유지.
- 회전 없음(6항목 · 상한 20회/300줄 안). 푸시 뒤 `git ls-remote origin
  loop/focus-ring-combinator` 로 다시 읽은 원격 SHA 는 **`bfc4c17`** 로 로컬 HEAD 와 같다.
- 다음: **계획 탐색** — `plan: null`. 1~5순위는 계획 52 착수 때 0건이었고 6순위 후보는
  `digest ## 다음 계획 후보` 에 있다(대부분 여는 조건 미도래). **PR #7 무접촉**.
