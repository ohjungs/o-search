# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**회전 명부는 `digest.md` 의 `## 완료` 절 «아카이브 명부» 줄이 정본이다.**
여기 있던 스물한 회전의 서술(233줄)은 그 줄과 내용이 겹쳤고, 검사가 강제하는 명부도
그쪽 하나뿐이라(`tests/test_docs.py` 의 `ArchiveIndexTest`) **개발 9(반복 269)가 이
자리에서 지웠다** — 회전으로는 300줄 상한을 못 맞추던 세 반복(309 → 372 → 418줄)의
원인이 이 명부였다. **지운 것은 머리말이지 반복 기록이 아니다** — 항목은 여전히
append 전용이고 수정·삭제 금지다. 각 회전의 사유는 `digest.md` 의 같은 줄에, 원문은
`history_<NNN>.md` 에 그대로 있다.

## 2026-09-04 19:05 | passage-db-state | e2e | 1/1 시도0

- 한 일: **계획 53 을 프로세스 밖에서 다시 쟀다 — 임시 DB 둘에 실서버를 띄워 상태 코드를
  소켓에서 받았다.** 단위는 핸들러를 프로세스 **안**에서 부르는데 이 계획이 고친 것은 본문이
  아니라 **상태 코드**라 그 값은 밖에서 재야 참이다. 스크래치패드 탐침이 `store.upsert` +
  `indexer.index_pages` 로 1문서 DB 를 둘 만들고 한쪽만 `DROP TABLE pages` 한 뒤
  `python3 -m websearch.serve data/crawl.db --port 0` 으로 각각 띄워 5줄씩 때렸다.
  저장소의 `data/crawl.db` 는 **읽지도 않았다**.
- **결과 — `pages` 없는 DB 의 세 질의가 전부 503**(착수 반복 309 의 실측은 **500 / 200 `[]` /
  200 `[]`**): `q=김치찌개` **503** · `q=zzzznope` **503** · `q=%01` **503**. 본문은 셋 다
  고정 문구 `{"version":1,"error":"색인이 아직 준비되지 않았다"}` 라 **DB 경로가 안 샌다**
  (경로는 서버 로그에만 남는다). 같은 DB 의 `/search?q=김치찌개` **200 · 결과 1건**,
  화면 `/?q=김치찌개` **200** — 설계 갈림길 3(화면 사다리를 안 넓힌다)의 근거를 실서버로
  다시 확인했다. **정상 DB 대조군 다섯 줄은 전부 무변**(200·문단 1건 / 200 `[]` / 200 `[]` /
  200 / 200)이라 계획서 8절이 최대 위험으로 적은 **가드 오탐이 0** 이다. 탐침 rc **0**.
- **완료 기준 10 을 HTTP 표면에서 다시 죽였다** — `src/` 사본을 스크래치패드에 두고 심었다
  (`git checkout` 0회 · 저장소 무변). ① 503 튜플에서 `NoCrawlDataError` 제거 →
  **500/500/500**(탐침 rc 1) · ② `passages()` 가드 삭제 → **500 / 200 `[]` / 200 `[]`**
  (탐침 rc 1). **②가 이 phase 의 값이다** — 착수 때의 그 갈림이 HTTP 표면에 글자 그대로
  돌아온다. 개발·리뷰는 `failures=N` 으로 쟀는데 **소비자가 받는 세 값이 갈리는 것**을 본
  것은 여기가 처음이다. ①은 셋을 500 하나로 모으지만 이름이 틀렸다 — 두 변이가 서로 다른
  층을 잡는다.
- 완료 기준 6(무토큰·미매치에서 `OperationalError` 가 안 난다)은 in-process 로도 확인했다 —
  세 질의 모두 `NoCrawlDataError` 이고 `sqlite3.OperationalError` **0건**.
- 전수: **단위 599건 OK**(13.687초 · 맨몸·단독 · rc 0) · **e2e 21종 전수 rc 0**(새 e2e
  **0개** — 제품 diff 가 상태 코드 하나라 `e2e/` 에 더할 자가 없고, 이 phase 가 잰 것은
  스크래치패드 탐침이다) · `passage_eval` 정확도 **100.0%** · 채택률 **99.5%**(398/400) ·
  p95 **1.55ms**(예산 500ms 의 0.3%) · `quality_eval` ko 20/20 · en 19/20 ·
  `design_check` 4축 · `perf_search` p95 8.84ms · `perf_crawl` 3시나리오.
- 범위: 제품 diff **두 파일**(`indexer.py` +10 · `serve.py` +8/-2 · `a4a4da1..HEAD`) ·
  `e2e/` **0줄** · 새 파일은 결과 문서 하나 · `data/crawl.db` sha256 `85c967…5bda18` 무변 ·
  `docs/specs/` 무변 · **PR #7 무접촉**(조회도 안 했다). `pgrep -f websearch.serve` **0건**.
- **러너 규율 위반 2회**(숨기지 않는다) — 둘 다 **재확인 실행**이고 방아쇠가 같다.
  ① 변이 탐침의 재확인에 `2>&1 | tail -40` 을 붙여 변이 ① 구간이 잘렸다. ② 문서 갱신 뒤
  `test_docs`·`test_readme` 재확인에 `2>&1 | tail -30` 을 붙였다 — 16건이라 판정 줄은
  살아남았지만 그건 운이고 `rc` 는 파이프 뒤라 **안 찍혔다**. `digest ## 반복 실패` 의
  「러너의 판정 줄을 가린다」가 적은 방아쇠(«재확인 실행») 그대로다. **둘 다 맨몸으로 다시
  돌렸고**(`Ran 16 … OK · rc=0` · `Ran 599 tests in 13.585s … OK · rc=0`) **기록한 값은
  전부 맨몸 실행의 것**이다. 단위·e2e **전수**에는 위반 0회. **한 반복에 같은 방아쇠가 두 번
  당겨진 것은 처음이다** — 겨냥할 자리는 본 실행이 아니라 «이미 통과한 줄 알고 다시 돌리는
  실행»이다.
- 집안일 둘: ① `digest.md` 를 208 → 198줄로 회전했다(2026-09-01 이전 완료 항목 아홉 삭제 ·
  원본은 각 `history_<NNN>.md`). ② **아카이브 명부에 `history_055.md` 가 빠져 있었다** —
  직전 회전이 «명부에 더했다» 고 적었는데 실제로는 서술 줄에만 붙어 있었다. 명부에 넣었다.
  그 줄을 지웠다면 `ArchiveIndexTest` 가 RED 였을 자리다 — **오래된 항목을 지우기 전에
  잡혔다.** `digest` 의 `[5]`(«`pages` 없으면 500») 는 닫힘 표시했고, 남은 `[6]`
  (`html` 열만 없는 DB)은 **테이블 축만 닫혔다**는 뜻으로 그대로 둔다.
- 커밋 `aaeddce` 를 푸시했고 `git ls-remote origin loop/passage-db-state` 가
  `aaeddced568f788f8b7f70a4a996ac6ad026dc23` 으로 로컬 HEAD 와 같음을 확인했다.
- 결과 문서: `docs/e2e/passage-db-state/result.md`. **계획 53 DONE — 완료 기준 10/10.**
- 다음: **새 계획 탐색.** `digest ## 다음 계획 후보` 의 `[6]` 과 테스트 phase 갭 목록이
  후보다. 병합은 사람 몫이다(PR #7 이 OPEN 이고 그 뒤에 브랜치 셋이 쌓여 있다).

## 2026-09-04 20:02 | passage-html-column | 계획 0/1 | 시도0

- 한 일: **계획 54 `passage-html-column` 착수 — 탐색과 계획서 작성.** 계획 53 이 닫은 것은
  「`pages` 테이블이 통째로 없다」 한 축이라, `pages` 는 있는데 **`html` 열이 없는** DB 에서
  「DB 상태 판정이 질의 내용에 달렸다」가 그대로 열려 있는 것을 열었다. 계획서
  `docs/plan_passage-html-column.md`, 브랜치 `loop/passage-html-column`, 기점 `91731e4`.
- **탐색 1~5순위 실측 0건**: 단위 **599건 OK**(13.709초 · 맨몸·단독 · rc 0) · 린터·타입체커
  설정 **0개** · 코드 `TODO`/`FIXME`/`HACK` **0**(유일한 hit 는 `tests/test_indexer.py` 의
  fixture HTML 문자열 안이라 실행되는 코드가 아니다) · `candidates.md` 없음 ·
  `digest ## 보류` 살아 있는 항목 0. **6순위 `[6]` 채택.**
- **중복 방지(discover 5절)를 고르기 전에 5곳 전수로 돌렸다** — `index.md` 계획 행 30개 ·
  `digest ## 완료` · 활성 계획서(계획 53 하나, DONE) · 보류 · `docs/patches/`(없음).
  **함정 둘을 지나왔다**: ① 점수가 가장 높은 `[8]` 토크나이저는 **계획 11 이 이미 닫았는데
  취소선이 없다** — 그대로 믿었으면 완료된 것을 다시 여는 반복이었다(직전 탐색이
  `[6] focus_rule` 에서 겪은 자리와 같은 고장이 **같은 목록에 하나 더** 있었다는 뜻이다).
  ② 그 `[8]` 의 남은 갈래는 **재색인**이라 이 반복의 제약에 정면으로 걸린다.
- **후보의 서술과 처방을 실행 전에 다시 쟀다 — 여섯 번째 적용이고 이번엔 둘 다 맞았다.**
  임시 DB(`store.upsert` → `index_pages` → `pages` 를 `html` 없는 스키마로 재생성)로
  `indexer` 를 직접 불러 **500 / 200 `[]` / 200 `[]`** 을 재현했고(정상 DB 대조군은 문단 1건 /
  `[]` / `[]`), 같은 DB 의 `search()` 는 세 질의 다 무변이다. 처방 자리의
  `SELECT html FROM pages LIMIT 1` 은 **정상 DB 에서 OK(오탐 0)** · 고장난 DB 에서 **질의와
  무관하게** `OperationalError` 다. 순서도 쟀다: + 옛 색인 → 셋 다 `StaleIndexError` ·
  + 색인 전 → 셋 다 `[]` · `pages` 없음 → 셋 다 `NoCrawlDataError`(계획 53 의 수리가 선다).
  저장소의 `data/crawl.db` 는 **읽지도 않았다**.
- **처방을 그대로 받지는 않았다** — 후보가 «그날의 답» 으로 적어 둔 **500** 은 계획서가
  갈림길로 되돌렸다. 이 상태는 사람이 스키마를 손으로 고쳐야 닿고(후보 자신이 «8점이 아닌
  이유» 로 적은 그것), 오늘 살아 있는 손실은 **제품 주석 한 줄이 거짓** 이라는 것이다 —
  계획 53 설계·제품 주석의 *"500 이 맞는 이름이다"* 는 **이름에만 참이고 갈림을 안 봤다**.
  그래서 설계가 A(500) · B(503 `NoCrawlDataError`) · **C(제품 0줄 · 주석과 테스트 docstring
  만 교정)** 셋을 잰다. **ponytail 사다리 1칸을 계획서 안에 후보로 남긴 첫 표본이다.**
- **함께 닫힐 수 있는 것**: `digest` 테스트 phase 갭의 `[6]`(「`passages()` 의 판정 셋이 어느
  순서로 이기는지 붙드는 단언이 0개」)이 **여는 조건에 닿았다** — 설계가 500 을 고르면 그
  항목이 적은 «셋 중 하나가 503 아닌 코드로 갈리는 날» 이 그날이 된다. 503 을 고르면 그대로
  남는다. 한 계획이 후보 둘을 조건부로 흡수하는 모양이다.
- 기점 판단: `git ls-remote origin` 을 다시 읽어 원격 `main` 이 `687a1598…`(계획 47),
  `loop/passage-db-state` 가 `91731e47…`(= 로컬 HEAD)임을 확인하고 **후자를 기점**으로 잡았다.
  고칠 코드가 **계획 53 이 심은 가드 그 자체**라 `main` 에는 대상이 없고, `README.md` 의
  599건·21종 단언도 이 사슬에서만 참이다. **PR #7 은 조회도 안 했다.**
- 결과: 제품 `src/` **0줄**(탐침은 전부 임시 DB·스크래치패드 · `git checkout` 0회) ·
  `docs/specs/` 무변 · `data/crawl.db` 무변 · 스키마·재색인 0 · 새 의존성 0.
  러너는 **맨몸**으로만 돌렸다(파이프·리다이렉션 0회 — 직전 반복이 재확인 실행에서 두 번
  어긴 자리라 이번엔 재확인도 맨몸이다). 띄운 서버 **0개**.
- 커밋 `4d6c18e` 를 푸시했고 `git ls-remote origin loop/passage-html-column` 이
  `4d6c18e0a67778ee401258a21cb71fbdba8c1a41` 로 로컬 HEAD 와 같음을 확인했다.
- 다음: **설계 phase.** 갈림길 1(500 · 503 · 제품 0줄)과 갈림길 2(가드를 넓히는 세 방식)를
  표로 가른다. 특히 안 C 를 진지하게 재고, 고르면 그것도 정상 종료로 적는다.

## 2026-09-04 20:41 | passage-html-column | 설계 1/1 | 시도0

- 한 일: **설계 1/1 — `docs/design_passage-html-column.md`**(제품 `src/` **0줄** · 코드 변경 0).
  **결정: 제품 한 줄** — `passages()` 의 `sqlite_master` 가드 **직후**·`hits` 루프 **앞**에
  `db.execute("SELECT html FROM pages LIMIT 0")`. `serve.py`·`README.md`·스키마·새 예외 **전부 0**.
- **갈림길 1 → 안 A(500). 결정한 것은 「다시 돌리면 낫나」 를 직접 처방해 본 실측이다** —
  고장난 DB 에 `store.Store()` 재생성은 **열을 못 살리고**(`SCHEMA` 가 `IF NOT EXISTS`),
  이어지는 `upsert()` 는 `table pages has no column named html`, `index_pages()` 는
  `no such column: html` 로 죽는다. **크롤도 색인도 죽는 상태**라 503 «색인이 아직 준비되지
  않았다» 는 사양 5 와 `README.md` 의 «다시 돌리면 낫는 상태» 정의에 어긋나는 거짓말이고,
  503 을 본 인프라는 안 낫는 것을 영원히 두드린다. C(제품 0줄)는 거짓 주석만 참으로 바꿀 뿐
  「판정이 질의 내용에 달렸다」를 **세 번째로** 남긴다 — A 와의 차이가 한 줄이다.
- **갈림길 2 → `LIMIT 0`. 계획서가 후보로 적어 둔 `LIMIT 1` 이 넷 중 가장 비쌌다**(1000행·
  10,426바이트 html · 200회): `LIMIT 1` **0.0128 ms** 대 `LIMIT 0` **0.0016 ms** · `WHERE 0`
  0.0015 ms · 오늘의 `sqlite_master` 가드 0.0024 ms. **열 이름만 물으면 되는데 html 본문을 한
  행 통째로 읽어서다.** 넷 다 정상 DB 오탐 0 · 고장 DB `OperationalError`. `PRAGMA table_info`
  는 «없다» 를 우리가 예외로 번역해야 해 판정이 한 벌 는다(계획 47 역행)라 버렸다.
  **계획서 3절이 걱정한 「예외 메시지 문자열 비교」는 안 일어난다** — 테이블째 없는 DB 는
  위쪽 가드가 먼저 `NoCrawlDataError` 를 낸다. **순서가 문자열 비교를 대신한다.**
- **가장 위험한 가정을 깼다 — 제안 계약을 `src/` 밖에 복제해 일곱 상태 × 세 질의 전수.**
  정상 200·200·200(오탐 0) · `html` 열 없음 **500·200·200 → 500·500·500**(갈림 닫힘) ·
  `pages` 없음·옛 색인·둘 겹침 503 무변 · 고장난 DB 의 `/search` 세 질의 무변(2·0·0).
  우선순위도 무변(없는 DB → 옛 색인 → 창고 없음 → **열 없음 500** 이 맨 뒤). **알고 바꾸는
  행 하나**: 「`html` 열 없음 + 색인 전」 200 → 500 — 색인을 돌리면 낫는 게 아니라 죽는 DB 다.
- **대조군의 한계를 그대로 적는다** — `data/crawl.db` 사본은 `pages` 행이 **2건**뿐이라 다섯
  질의 전부 오늘·제안 0건으로 같았다. **오탐 0 을 이 코퍼스가 증명하지 못한다**; 붙드는 것은
  **599 OK**(13.352초 · 맨몸·단독 · 이번 반복 재측)와 개발이 더할 정상 DB 단언이다. 실코퍼스
  지연 증분 `-0.001 ms`(-0.32%, 잡음 안). 범위: `data/crawl.db` sha256 `85c96744…5bda18`
  **사본으로만 읽고 대조 완료** · `docs/specs/` 무변 · `e2e/` 0줄 · **PR #7 무접촉** ·
  띄운 서버 0개 · **러너 규율 위반 1회**(설계 문서 추가 뒤 첫 재확인에 `2>&1 | tail` 을 붙였다 —
  맨몸으로 다시 돌려 `Ran 599 tests` `OK` 를 끝까지 봤다. 방아쇠는 이번에도 «재확인 실행»이다).
  **다음: 개발 phase** — RED 는 새로 짓는 것이 아니라 `TestPassagesWithoutHtmlColumn` 을 뒤집는 것이다.
  커밋 `62f619f` 를 푸시했고 `git ls-remote origin loop/passage-html-column` 이
  `62f619f5488d6183b523753d45c99ada94dda5a6` 로 로컬 HEAD 와 같음을 **읽어서** 확인했다.
