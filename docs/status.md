---
signal: DONE
phase: e2e
step: 1/1
attempt: 0
iteration: 338
updated: 2026-09-05
ctx: 46
night_iterations: 159
night_red: 2
night_retries: 0
plan: passage-cost-axis 계획 57 (e2e 완료 · 완료 기준 7/7 충족)
---

# 현재 상태

**계획 57 을 닫는다 — e2e 21종 전수 `rc 0` · 기준선 회귀 0 · 완료 기준 7/7 을 오늘 다시
재서 충족했다.** 이 phase 가 한 일은 셋이다: 리뷰 findings 3건 처리, 기존 e2e 전수 실행,
변이 넷을 **낡은 캐시가 안 끼는 방식으로** 다시 재기. 증거는
`docs/e2e/passage-cost-axis/result.md` 에 있다.

## R57-1 은 리뷰가 제안한 0.97 이 아니라 **1.00** 으로 갔다

세 기록 자리의 `0.95` 는 「한가할 때 0.939~0.970」을 덮는 상한이라고 적었는데 부등호가
거짓이었다. 오늘 두 벌을 다시 재니 idle min-of-3 이 `0.973 0.974 0.941 0.946 0.950` /
`0.942 0.931 0.961 0.964 0.943` — **0.974 가 나와 리뷰가 제안한 0.97 도 깨진다.**
어제 표본과 합쳐 20표본 **0.917~0.974** 위로 올려 **1.00** 으로 못박았다.
① 은 이제 **350ms · 예산의 70%** 이고 상수 변이에는 오히려 **조여진다**(허용
`캡 × PASSAGE_LIMIT` 이 오늘의 +50.4% → **+42.9%**).

- **[R57-2]** `tests/test_indexer.py` 주석에서 이미 사라진 «배정치 1/3» 과 현재형 `0.44`
  서술을 걷어내고 오늘의 세계로 다시 썼다.
- **[R57-3]** `docs/design_passage-cost-axis.md` 2절 계약에 **①-b 를 추가**하고,
  「② 가 ① 의 리터럴을 붙든다」를 **「1.60 위에서만 붙든다 · 1.00~1.60 은 열린 창」**
  으로 정정했다.

## e2e 21종 — 전부 `rc 0` · 기준선(계획 56) 대비 회귀 0

정확도 **100.0%**(=) · `/passages` p95 **1.51ms**(1.53) · `perf_search` p95 **8.96ms**(8.95) ·
품질 ko **20/20**(=) · en **19/20**(=) · 크롤 **10.23/s**(10.30 · 기준 5.0) ·
디자인 4축 전부 통과. **새 e2e 파일 면제는 여전히 옳다** — 이 계획이 만진 것은 단언과
주석뿐이라 프로세스 밖에서 달라질 동작이 0이고, 안쪽 상한은 `e2e/passage_eval.py` 가
이미 실물로 재고 있다.

## 변이 넷을 다시 쟀다 (저장소 밖 사본 · 매 실행 새 캐시 접두어)

M1 파서 2배 지연 `FAILED`(2.154 > 1.60 · ②) · M2 캡 10만 `FAILED`(1000.0 > 500 · ①) ·
M3 `PASSAGE_LIMIT` 100 `FAILED`(3500.0 > 500 · ①) · M4 캡 50,000 `FAILED`(500000 > 350000
· **①-b** — ① 은 정확히 500.0 이라 초록). 원복 뒤 `diff -r` 무변 · 사본 삭제.

## 완료 기준 7/7

전수 `Ran 605 tests · OK · rc 0 · 13.837초` · M1 · M2·M3 · 스위트 증가 +0.18초 ·
README 단위 건수 일치 · 세 기록 자리 **1.00/350ms/70%** 동일(옛 숫자 잔여 0건) ·
`data/crawl.db` sha256 `85c96744…5bda18` 앞뒤 동일 · `docs/specs/` 무접촉 · 새 의존성 0 ·
스키마·재색인 0. 문서 가드 `tests/test_docs` + `tests/test_readme` `Ran 16 tests · OK · rc 0`.

## 프로세스 함정 하나를 기제로 옮겼다

리뷰가 medium 으로 올린 **macOS `sys.pycache_prefix`** 함정을 `docs/digest.md ## 다음 계획
후보` `[7]` 로 등재하고, 처방을 `docs/project.md ## 명령` 의 「변이 검사」 줄에 넣었다 —
**변이 실행마다 `PYTHONPYCACHEPREFIX=$(mktemp -d)`**. 이 phase 의 변이 넷이 그 방식으로
돌았고 넷 다 죽었다.

## 사람 결정 대기 (계획 57 과 무관하게 계속 열려 있다)

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1).
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 0 일 때만.
3. **반응형 360px 미검증** — 브라우저가 없어 저장소의 아무도 안 그려 봤다.
4. **3시간 자동 스냅샷 잡을 루프 작업 중에도 세울 것인가**(반복 328 사고). RED 중간을
   덮치면 깨진 상태가 원격에 올라간다. 반복 334 에서 RED 아닌 자리에서 실제로 일어났다.
5. **재파싱에 예산 얼마를 줄 것인가** — 캡 최악이 예산의 **70%** 라는 사실이 이제 세 자리에
   모두 적혀 있다. 캡·`PASSAGE_LIMIT` 를 내리는 것은 «긴 문서의 뒷부분 근거» 쪽 판단이다.
