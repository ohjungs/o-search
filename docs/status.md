---
signal: GREEN
phase: 계획
step: 0/0
attempt: 0
iteration: 422
updated: 2026-09-07
ctx: 77
night_iterations: 8
night_red: 0
night_retries: 0
plan: null
---

## 현재 상태

**계획 72 `digest-rotate-guard` 아카이브 완료. 사용자 지시가 준 일이 끝나 정지한다.**
`/loop-harness night <할 일>` 은 **준 것만 하고 정지**한다 — 새 계획을 탐색하지 않았다.
전수 **648 OK rc 0**.

## 아카이브가 옮긴 것

- `plan_digest-rotate-guard.md` → **`plan_history_058.md`**
- `design_digest-rotate-guard.md` → **`design_history_058.md`**
- `index.md` 행 → `완료` · e2e 칸 **`통과`** · 아카이브 이름 부기
- `digest.md` `## 완료` 에 계획 72 요약 1줄 · 후보 `[5]` 에 취소선

**취소선의 뜻을 좁게 적었다** — 「표제의 상수(`STRIKE_POINTER_FLOOR`)가 사라졌다」이지
「문구 드리프트가 닫혔다」가 아니다. e2e 막 ④ 가 보인 대로 이 취소선은 **기제가 강제**하는
것이라(안 달면 전수 RED) 뜻을 안 적으면 열린 위험이 그 밑에 묻힌다. 살아남은 축은
후보 `[6]` 이 오늘의 말로 들고 있고 `[5]` 의 닫힘 사유가 `[6]` 을 가리킨다.

## 사용자가 준 세 물음에 대한 답

1. **「완료 절만으로는 여덟 줄이라 산술이 안 맞는다」** — 맞다. 실측 **7줄**(아카이브
   명부와 회전 기록은 룰이 남기라 한 줄이다). 그것만으로는 216 → 209 라 상한을 못 맞춘다.
2. **「닫힌 후보도 정리 대상이다」** — 맞다. 취소선 **18줄**이 `index.md`·`plan_history_*`
   와 이중이었고, e2e 막 ③ 이 그 원본 **10/10** 을 양쪽에서 찾아 삭제의 전제를 세웠다.
   18 + 7 = 25줄 삭제 + 2줄 추가 → **216 → 193줄**.
3. **「그 정리가 계획 70 등재 항목의 여는 조건이고, 회전과 가드를 한 계획에서 닫아라」** —
   그렇게 했다. **다만 등재된 처방 B 는 반증됐다.** B 는 포인터 모집단이 append-only 라고
   가정했는데 실측이 **10/10 전부 취소선**이었다 — 회전이 그 모집단을 **0** 으로 만든다.
   답은 하한을 실측치에 붙이는 것이 아니라 **모집단을 옮기는 것**이었다
   (`CANDIDATE_HEAD_FLOOR = 2` — 회전 전후 둘 다 2, 어느 머리가 갈려도 1).

## 사람 몫 — 야간이 못 넘는 자리 셋

1. **digest 회전 위임은 이 건 한 번이었다.** `rules/docs.md` 3절 문장은 안 바뀌었다.
   지금 `digest.md` 는 **198줄**(상한 200)이고 **여유가 두 줄**이다 — 다음 반복의 등재
   한두 줄이면 다시 넘는다. 무인 모드는 삭제를 못 하므로 **다음 회전은 사람이 연다.**
2. **후보 `[7]`** — 문서 상한(200·300)을 재는 자가 **0개**다. 오늘 사고가 그 부재의
   실물 증거고, 못을 세우려면 1번이 먼저 정해져야 한다(상한 초과를 RED 로 만들면
   야간이 그 RED 를 스스로 못 푼다).
3. **후보 `[6]`** — 실물 후보 절의 포인터 모집단을 재는 자가 0개다. 처방은 후보 줄에
   계획 번호를 기계가 읽는 자리로 못 박는 **규약 변경**이라 digest 전체를 손대는 별건이다.

## 야간 지표

반복 **415~422**(8회) · RED **0** · 재시도 **0** · 계획 **1 완료** · 정지 사유
**지시 완료**. 커밋 **10**개, 전부 `loop/digest-rotate-guard`(`main` 직접 0).
`metrics.md` 에 **`지시 완료` 칸을 신설**했다(사유·스크립트 어휘 간극은 그 절에).

## 검증 (맨몸 · 리다이렉션 0)

```
PYTHONDONTWRITEBYTECODE=1 PYTHONPYCACHEPREFIX=$(mktemp -d) PYTHONPATH=src python3 -m unittest discover -b -s tests
Ran 648 tests in 15.9s · OK · rc 0
```

`src/`·`e2e/` **0줄** · `docs/specs/` 무접촉 · `data/crawl.db` 무개봉 · 바깥 네트워크 0.
