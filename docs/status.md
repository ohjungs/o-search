---
signal: GREEN
phase: 테스트
step: 1
attempt: 0
iteration: 218
updated: 2026-09-01
ctx: 60
night_iterations: 78
night_red: 0
night_retries: 0
plan: docs/plan_focus-contrast.md
---

# 현재 상태

**계획 43 `focus-contrast` 테스트 1/1 완료 — 단위 469 → 473건.**
브랜치 `loop/focus-contrast` · 설계서 `docs/design_focus-contrast.md` ·
계획서 `docs/plan_focus-contrast.md`.

## 방금 것 (2026-09-01 · 테스트 1/1)

**개발이 넘긴 "단위 0건 추가" 를 뒤집었다.** 설계가 금지한 것은 *제품 색값을 단위에
적는 것*이었지 **검사기를 붙드는 단언**이 아니었다. 그 자리가 비어 있으면 다음 반복이
축을 지워도 `design_check` 는 **rc 0 · "4축 전부 통과"** 를 찍는다 — 실측했다(N1).
`tests/test_design_check.py` **4건**(`check_contrast` 를 직접 부른다. 서버·서브프로세스
없이 0.001초). 제품 CSS 를 한 곳만 비튼 입력이고 비틀기가 심겼는지 먼저 단언한다.

| 테스트 | 붙드는 것 |
|---|---|
| `focus_ring_is_measured_against_the_nontext_floor` | 제품 CSS 에서 `--focus` 가 **두 맵 모두 기준 3.0** 으로 재진다 |
| `color_token_without_pair_or_reason_is_unmeasurable` | 짝도 사유도 없는 새 토큰 = **측정 불능**(라이트·다크 둘 다) |
| `nontext_pair_below_three_fails` | 비텍스트 자가 **실제로 3.0** 이다 |
| `text_pair_keeps_the_four_point_five_floor` | 한 루프에 섞인 **두 자가 안 섞인다** |

### 변이 5종 — 전부 잡혔다 (제자리 편집 후 되돌림, `&&` 로 안 이었다)

| | 심은 것 | 단위 | `design_check` |
|---|---|---|---|
| N1 | 재는 루프에서 비텍스트 겹을 지운다 | **2건 RED** | **rc 0 — 조용히 통과** |
| N2a | `NO_PAIR` 에 `--focus` 만 | 초록(정상) | — 짝에 남아 계속 잰다(개발 M4) |
| N2b | N2a **+** `NONTEXT_PAIRS = []` | **2건 RED** | 이것이 진짜 탈출구다 |
| N3 | `startswith("--fg-")` 허용 목록 복원 | **2건 RED** | rc 0 |
| N4 | `MIN_CONTRAST_NONTEXT` 를 2.0 으로 | **2건 RED** | rc 0(2.80 도 통과가 된다) |
| N5 | 텍스트 짝을 3.0 으로 잰다 | **1건 RED** | rc 0 |

**N1 이 이 스텝의 근거다** — 축을 통째로 지웠는데 검사기는 머리말에 "비텍스트 3:1" 을
그대로 찍으면서 rc 0 을 냈다. 그 상태를 빨갛게 만드는 것이 이제 저장소에 있다.

### 갭 탐색 — 8점 이상만 처리했다

위 넷이 8~9점(전부 ①부정 경로·⑤경계값). **처리 안 한 것**: `--line` 을 UI 경계로 볼지
(범위 밖, `digest` 후보는 DONE 반복이 한다) · `design_check` 의 나머지 3축(1·2·4번,
이번 계획 밖이고 축마다 대리 지표의 근거가 따로 필요하다 — 5점).

## 다음

- **리뷰 phase.** 볼 것 둘: ① 단위 4건이 **검사기 옆에 값을 따로 드는 것**이 아닌지
  (색값은 안 적었고 자·짝·강제만 붙들었다는 것이 근거다) ② `docs/project.md ## 명령`
  머리에 옮겨 적은 "맨몸으로" 가 제자리인지.
- `--line` 을 UI 컴포넌트 경계로 볼지(1.34:1)는 **범위 밖**이고 `digest` 후보로 넘기는
  것은 **DONE 반복이 한다**. 지금 넣으면 열린 항목 48·200줄 상한을 근거 없이 흔든다.

## 판단 필요 — PR 은 여전히 안 연다

**계획 42·43 둘 다 `origin/main` 기점이 아니다.** PR #4(계획 38~41)가 열려 있고
그 위에 42 가, 42 위에 43 이 쌓인다. 지금 PR 을 열면 PR #3 과 같은 사고가 난다.

**사람이 할 일: PR #4 병합 → 계획 42 체리픽 PR → 계획 43 체리픽 PR.**

## 한도 (매 반복 확인)

- `main` 직접 커밋 금지 · `--no-verify` 금지 · 외부 네트워크 금지 ·
  `docs/specs/` 읽기만 · `data/crawl.db` 무변경(sha256 `85c96744…5bda18` 확인) ·
  의존성 추가 금지(stdlib 만 — 이 반복도 stdlib 만 썼다).
- **검증은 맨몸으로.** (**15회 · 다섯 반복 연속이다**: README 를 고친 뒤 재확인 실행에
  `2>&1 | tail -4` 를 썼고, 출력이 러너 뒤 잡음에 묻혀 **판정 줄이 안 보였다** — 파이프가
  실패를 숨기는 그 모양이 그대로 나왔다. 맨몸 재실행 **473건 OK 11.854초**가 커밋 전
  확인이다. 방아쇠는 ⑥⑨와 같은 **재확인**이고, 이로써 재확인이 최다 방아쇠 3회다.)
  **앞 반복의 진단대로 배치를 고쳤다** — `docs/project.md ## 명령` 절 **머리**에 한 문단을
  올렸다. 조항은 `한도` 에도 남기되, **명령을 복사하러 가는 자리**에서 먼저 읽히게 했다.
  다음 반복이 이 배치가 듣는지를 실측한다(듣지 않으면 남은 처방은 러너를 한 줄로 굳히는 것).
- `docs/digest.md` 200줄 상한 · 열린 항목 **48**(이 반복은 안 건드렸다 — 위 항목의
  누적 횟수도 `digest` 는 ⑩에서 멈춰 있고 실제 카운트는 이 절이 굴린다) ·
  `history_current.md` 300줄 상한 — 지금 **165줄**이라 여유 있다.
