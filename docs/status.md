---
signal: GREEN
plan: null
phase: 계획
step: -
attempt: 0
iteration: 144
night_iterations: 27
night_red: 0
night_retries: 0
updated: 2026-08-29 21:20 (반복 144 · 27 crawl-unknown-arg 짧은 경로 DONE)
ctx: 65% / 200k
stopped: -
rules: 1411a37
mode: night (지시받은 것만 실행 · 다 하면 정지)
---

# 현재 상태

**26 `crawl-max-guard` · 27 `crawl-unknown-arg` DONE**(둘 다 짧은 경로).
열린 계획 없음 · 보류 패치 0건. 단위 **419건 OK** · e2e **17종 전부 rc=0**.
브랜치 `loop/crawl-max-guard`(기점 `d2337fb`) — 병합은 사용자 판단이다.

**설계는 둘 다 건너뛰었다** — 새 모듈 없음 · 파일 2개 · 데이터 구조 무관 · 되돌리기
쉬움 · 보안 무관. 계획 24(`serve-port-guard`)와 같은 부류라 같은 경로를 썼다.
집안일: `history_current.md` 293줄(상한 300)이라 **회전 먼저** 했다 — 21~24 를
`history_008.md` 로(293 → 176줄).

## 반복 143 — 26 `crawl-max-guard` (짧은 경로)

**`--max 0` 의 가드 비대칭을 닫았다.** 실측 `--max 0` → 요청 **0건** · `수집 0 페이지` ·
**rc 0**(`--workers 0`·`--deadline 0` 은 진작 rc 2). **rc 2 로 거절하는 쪽으로 정했다** —
가른 것은 대칭이 아니라 **결과의 모양**이다. `수집 0 페이지` rc 0 은 크롤이 아무것도
못 찾은 것과 **구별되지 않는 성공**이고, 이 저장소가 두 번 닫은 실패 유형이다(21 · 25).
**0 을 일괄 금지하지 않았다** — `serve --port 0` 은 "임의 포트" 라는 뜻이 있어 그대로
받는다. 하한은 파서가 아니라 **플래그의 뜻**이 정하므로 `flags.number_flag` 는 범위를
여전히 안 본다(설계 019 유지). 변이 3종 전부 잡힘 — **M3(하한 `< 2`)가 잡힌 것은
하한 자체를 재는 대조군(`--max 1`)을 넣었기 때문**이다.

## 반복 144 — 27 `crawl-unknown-arg` (짧은 경로)

**탐색(`discover.md` 1절)은 소진이 아니었다.** 1~4순위는 비었지만(`TODO` 2건은
`\uXXXX` 오탐) 6순위 후보 `[5]` 가 살아 있었다. **후보가 적어 둔 범위보다 넓었다** —
"중복 플래그" 가 아니라 **모르는 플래그 전부**가 시드로 샜다: `--maxx 3` 오타 하나로
크롤이 **기본값 100페이지를 조용히 돌고** rc 0 이었다. 뿌리는 `indexer`·`serve` 가
`len(args) != 1` 로 이미 막는 것을 **시드 개수가 가변인 `crawl` 만 셀 수 없던 것**이라
그 한 곳에 가드 한 벌(6줄). **변이 M4 가 "더 잡는" 쪽으로 살아남아** 대조군을 하나 더
낳았다 — 스킴 없는 시드까지 거절하는 것은 이 가드가 주장한 적 없는 다른 계약이다.

## 다음 계획 후보 (근거는 `digest.md`)

`[4]` **스킴 없는 시드가 rc 0 에 `unknown url type` 을 낸다**(27 이 변이 M4 로
드러내고 일부러 안 건드린 것 — 시드가 하나도 안 살아남으면 rc 1 이 26·21 과 같은 값이다) ·
`[5]` **README 가 없는 `python -m websearch.cli` 를 안내한다**(25 는 직교 편집이라
안 고쳤다) · `[4]` `--port 0` 단위 무커버 · `[4]` `int_max_str_digits`(3.9.6 이라
도달 불가, 유지 판단) · `[6]` `serve.do_GET` 의 넓은 `try`(확신 낮음).

## 열지 않는 것 (사용자 판단 대기)

`data/crawl.db` 재키잉/옛 열쇠 행 통합(마이그레이션) · URL userinfo 가 `pages.url` PK
이자 검색 결과에 렌더되는 것(보안 경계) · `recrawl`(`fetched_at` 스키마) ·
`X-Robots-Tag`(스키마) · `loop/*` 브랜치 병합 · 의존성 추가·업그레이드 · CI 도입 ·
`docs/specs/` 쓰기 · git 사용자 이메일 설정 변경.
