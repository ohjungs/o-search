---
signal: DONE
phase: e2e
step: 1/1
attempt: 0
iteration: 320
updated: 2026-09-04
ctx: 55
night_iterations: 142
night_red: 2
night_retries: 0
plan: passage-html-column # 계획 54 — e2e 1/1 통과 · 기준 9 통과 + 1 의도적 변경 · DONE
---

# 현재 상태

**계획 54 `passage-html-column` 이 끝났다.** 결과는 `docs/e2e/passage-html-column/result.md`,
설계서는 `docs/design_passage-html-column.md`, 계획서는 `docs/plan_passage-html-column.md`,
브랜치는 `loop/passage-html-column`.

## e2e 가 잰 것 — **실서버 다섯 대를 띄워 상태 코드를 소켓에서 받았다**

단위는 핸들러를 프로세스 안에서 부른다. 이 계획이 고친 것은 본문이 아니라 **상태 코드**라
그 값은 밖에서 재야 참이다. 임시 DB 를 `store.upsert` → `index_pages` 로 만들고 사본만
손으로 부순 뒤 각각 `python3 -m websearch.serve --port 0` 으로 띄웠다
(저장소 `data/crawl.db` 는 **읽지도 않았다**).

| DB 상태 | `/passages` 세 질의 | `/search?q=김치찌개` | 화면 |
|---|---|---|---|
| 정상 (대조군) | 200(문단 1건) · 200 `[]` · 200 `[]` | 200 · 1건 | 200 |
| **`html` 열 없음** | **500 · 500 · 500** (착수 500/200/200) | **200 · 1건** | **200** |
| `pages` 없음 | 503 · 503 · 503 | 200 · 1건 | 200 |
| 옛 색인 + `html` 열 없음 | 503 · 503 · 503 | 503 | 503 |
| `html` 열 없음 + 색인 전 | 500 · 500 · 500 | 200 · 0건 | 200 |

**둘째 행이 이 계획이다** — 같은 고장난 DB 가 질의에 따라 «우리가 터졌다» 와 «근거가 없다»
로 갈려 나가던 것이 한 값으로 모였다. **넷째 행이 우선순위를 실서버에서 붙든다**(옛 색인이
열 없음보다 앞선다 — 색인만 다시 돌리면 낫는 DB 를 500 으로 안 부른다). **정상 대조군
다섯 줄 무변**이라 계획서 8절의 최대 위험(가드 오탐)은 **0** 이다.

**500 본문이 안 흘리는 것 11종** — DB 절대경로·파일명·임시 디렉터리·`html`·`pages`·
`SELECT`·`LIMIT`·`OperationalError`·`no such column`·`Traceback`·`sqlite`. 같은 실행의
서버 stderr 에는 `/passages 실패: OperationalError('no such column: html')` 가 **3건 전부**
남는다 — 응답에는 없고 로그에는 있다(설계 6절 SRE 판정이 프로세스 밖에서 참).

## 변이 둘 — HTTP 표면에서 **동치**임을 처음 봤다

| 변이 | `q=김치찌개` | `q=zzzznope` | `q=%01` | 색인 전 | 정상 DB |
|---|---|---|---|---|---|
| ① 판정 줄 삭제 | 500 | **200 `[]`** | **200 `[]`** | 200×3 | 200×3 |
| ② 판정 줄을 `hits` 루프 **안**으로 | 500 | **200 `[]`** | **200 `[]`** | 200×3 | 200×3 |

착수 때의 갈림이 둘 다에서 글자 그대로 돌아온다. **새로 안 것**: ①과 ②는 열다섯 칸이 한
칸도 안 다르다(단위에서도 둘 다 `failures=5` 였다). 줄을 루프 안에 두는 것은 루프 자신의
`SELECT html … WHERE url = ?` 이 이미 하던 일이라 **줄이 없는 것과 같기 때문**이다.
「루프 앞」을 지키는 것은 둘을 **가르는** 단언이 아니라 **둘 다 RED 로 만드는** 단언 셋이다.

## 완료 기준 — **9 통과 · 1 의도적 변경**

기준 7(「`html` 열 없음 + 색인 전」은 여전히 200 `[]`)만 실측이 **500 셋**이다. 설계 3절이
일곱 상태 전수로 재고 4절이 **「알고 바꾸는 행 하나」로 명시**한 뒤 개발이 단위로 못박은
값이라, e2e 는 그것을 뒤집지 않고 **계획서와 실측이 다르다는 사실을 지우지 않는다**.
계획 54 의 목표(판정이 질의 내용에 안 달린다)는 **10/10** 이다.

## 전수

**단위 603 OK**(13.814초 · 맨몸·단독 · rc 0) · **e2e 21종 전수 rc 0 · 새 e2e 파일 0개** ·
`passage_eval` 정확도 **100.0%**(398/398) · 채택률 **99.5%** · p95 **1.51ms**(예산 500ms 의
0.3% — 계획 53 e2e 의 1.55ms 보다 **낮다**. 요청마다 SELECT 한 줄을 더했는데도 그래서
위험 3 은 «측정 불가» 로 닫힌다) · `quality_eval` ko 20/20 · en 19/20 · `design_check` 4축 ·
`perf_search` p95 8.80ms · `perf_crawl` 10.27/s. `data/crawl.db` sha256 `85c967…5bda18`
**무변** · `docs/specs/` 무변 · 제품 diff **한 파일**(코드 한 줄) · `e2e/` **0줄** ·
새 파일은 결과 문서 하나 · **PR 무접촉**(조회도 안 했다) · `pgrep -f websearch.serve` **0건** ·
**러너 규율 위반 0회**(직전 세 반복의 방아쇠였던 «재확인 실행» 도 전부 맨몸).

## 새 e2e 파일 판정 — **0개**

계획서 7절이 e2e phase 로 미뤄 둔 자리다. 이 고장은 **사람이 스키마를 손으로 고쳐야** 닿고
(`store`·`indexer` 어느 경로도 `html` 없는 `pages` 를 안 만든다), 프로세스 경계에서 재야 할
값은 이 phase 가 이미 쟀으며, 되돌아오는 회귀는 단위 둘이 매 실행 붙든다. 상시 e2e 로
만들면 21종이 22종이 되고 매 스텝 비용만 는다 — 계획 53(더 닿기 쉬운 테이블 축)도 같은
판정으로 0개였다.

## 행동

**다음은 새 계획 탐색**이다. `digest ## 다음 계획 후보` 에 리뷰 54 가 등재한 **`url` 축**
(열이 없으면 `q=김치찌개` 500 · 나머지 둘 200 `[]` — 같은 갈림이 그대로)과 테스트 phase 의
8점 미만 갭들이 있다. **원칙이 세 번(계획 47·53·54) 재발했다** — 네 번째가 나오면 자리를
또 넓히는 대신 **원칙을 기계로 만드는 것**을 먼저 검토한다.

## 문서 마감 — 다음 반복이 먼저 볼 것

`index.md` 54번은 **완료** · `digest ## 완료` 에 계획 54 가 있고 후보 `[6]` 은 취소선(자리를
만들려 가장 오래된 완료 항목 하나 = 계획 42 를 지웠다 — 교훈은 같은 파일 취소선 항목이,
원본은 `index.md` 42번·`plan_history_028.md` 가 갖는다 · **명부 무변** · digest **200줄**).
**`history_current.md` 가 300줄 — 상한에 정확히 닿았다.** 다음 반복이 자기 항목을 붙이면
넘으므로 **붙이기 전에 회전**한다(`history_057.md`). 계획 54 가 DONE 이라 경계가 깨끗하다.
계획서·설계서(`plan_passage-html-column.md`·`design_passage-html-column.md`)는 **이 phase 가
안 옮겼다** — 회전은 `plan_history_NNN.md`·`design_history_NNN.md` 짝으로 그때 함께 돈다.

## 사람 결정 대기

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1).
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 0 일 때만.
3. **반응형 360px 미검증** — 브라우저가 없어 저장소의 아무도 안 그려 본다.
4. **이 브랜치의 병합** — 직전 반복이 읽은 바로는 원격 `main` 이 `7db3acb` 이고 PR #7·#8 은
   사용자가 병합했다(계획 48~53 이 `main` 안). **이 반복은 PR 을 조회조차 안 했다.**
   `loop/passage-html-column` 의 병합은 사람 몫이고, `main` 직접 커밋은 하지 않는다.

## 정지 사유

없음 — 계획 54 **DONE**.
