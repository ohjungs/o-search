# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**반복 97~122 는 `history_006.md`, 123~128(계획 020 `deadline` 전체)은
`history_007.md`, 계획 21~24(짧은 경로 3건 + 보류 패치 소진, 2026-08-29 14:20~17:40)는
`history_008.md` 로 밀려났다** (2026-08-29 회전 3회).
**계획 25~29 는 `history_009.md`, 계획 30·31(짧은 경로) + 반복 152 탐색 +
계획 33 `clock-injection` 의 계획·설계·개발 1~4 는 `history_010.md` 로 밀려났다**
(2026-08-30 회전, 393줄 → 139줄. 상한 300줄을 넘겨 놓고 두 반복을 갔다).
**계획 33 의 마지막 네 반복(개발 5 · 테스트 1 · 리뷰 1 · e2e 1)은 `history_011.md`**
(2026-08-30 회전, 322줄 → 210줄. 상한을 넘긴 채 반복 두 번을 또 갔다 — 스텝 3 이
"미결(문서 위생)" 로 넘긴 것을 스텝 4 가 닫았다).
계획 33 의 전 phase 서술은 `plan_history_020.md` 의 `## 기록` 에 그대로 있다.
**계획 34 `graceful-interrupt` 의 앞 여덟 반복(계획 1 · 설계 1 · 개발 1 세 번 · 2 · 3 · 4)은
`history_012.md`** (2026-08-30 회전, 300줄 → 85줄. **상한에 정확히 닿아 넘기기 전에 돌렸다** —
앞 두 번은 넘긴 채 반복을 더 갔다).
**계획 34 의 마지막 세 반복(테스트 1 · 리뷰 1 · e2e 1)은 `history_013.md`**
(2026-08-31 회전, 307줄 → 244줄. 상한 300 을 넘긴 그 반복에서 바로 돌렸다 —
`digest ## 반복 실패` 가 지적한 "회전이 두 반복 늦었다" 를 안 되풀이한다).
**계획 35 `deadline-stop` 전체 여섯 반복(계획 · 개발 1 · 테스트 2 · 리뷰 · e2e)은
`history_014.md`** (2026-08-31 회전, 336줄 → 188줄. 상한 300 을 넘긴 채 테스트 반복
하나를 더 갔고 **리뷰가 그것을 잡았다** — 회전은 phase 룰이 아니라 문서 위생이라
`status.md` 의 "집안일 미결" 로만 있었다. 완료된 계획 하나를 통째로 미는 경계로 잘랐다).

**계획 36 `signal-budget-cover` 전체 두 반복과 계획 37 `indexer-interrupt` 전체 여섯 반복은
`history_015.md`** (2026-08-31 회전, 318줄 → 96줄. **계획 38 리뷰가 잡아서 돌렸다** —
테스트 스텝이 305줄인 것을 알고 `status.md` 에 적어 둔 채 그대로 갔다. 393·322·336·318,
네 번째 지각이다. 완료된 계획 둘을 통째로 미는 경계로 잘랐다).

---

## 2026-08-31 21:2x | 38 digest-rotate | 탐색+계획 | 시도0 (코드 0줄 · 평소 경로)
- 한 일: **계획 38 `digest-rotate` 개설**(`docs/plan_digest-rotate.md`, 2스텝 · 설계 생략 ·
  e2e 없음). discover 1~8순위를 전부 실측한 뒤(단위 456 OK · 코드 TODO 0건 · 후보는 전부
  "도달 불가·천장 수용·사람이 정할 갈림길") **집안일이 임계경로**라고 판정했다.
- 결과: `digest.md` **232줄·90,568바이트**(상한 200) · 회전 시뮬레이션 **187줄·57,241바이트**.
  덤으로 **머리 두 줄 손상**을 찾았다 — `b11fd40`(2026-08-27) 이후 H1 `# 아카이브 요약` 이
  리스트 항목 안에 있고, 그 사고로 018 이 닫은 `[5] 호스트 대소문자` 항목이 **4일째 열린
  후보로 보인다**(`urls.normalize('http://A.com/') -> 'http://a.com/'` 로 닫힘 확인).
- **여기서 배운 것 — 회전을 막고 있던 선행 조건이 이미 무너져 있었다.** "회전 전에 참조
  확인" 때문에 두 반복을 밀었는데, `digest.md:<줄번호>` 인용 8곳 중 **6곳이 오늘 빈 줄이나
  다른 항목**을 가리킨다(인용 당시엔 맞았다 — `git show 1abc058`). **미루는 비용이 지키려던
  것을 이미 먹었다.** 집안일을 미루면 그 근거도 같이 낡는다.

## 2026-08-31 21:4x | 38 digest-rotate | 개발 1/2 | 시도0 (문서 3줄 · 코드 0줄)
- 한 일: `b11fd40` 이 H1 `# 아카이브 요약` 을 리스트 항목 안으로 밀어 넣은 것을 되돌리고,
  그 사고로 머리에 붙어 있던 018 닫힘 문구를 `[5] 호스트 대소문자` 제자리(`:134`)로 옮겼다.
  **새로 쓴 문장은 0 이다** — 지운 줄과 옮긴 줄, 복구한 머리를 전부 `cksum` 으로 원본과 대조했다.
- 결과: 열린 항목 48 → **47**(고치기 전후 목록을 `diff` 로 대조 — `[5]` 하나만 빠졌다) ·
  232줄 그대로(옮기기라 순증감 0, 회전은 스텝 2) · 단위 456 OK · e2e rc 0 · DB sha256 무변경.
- 배운 것: **계획서의 완료 기준도 산수가 틀린다.** `grep -c '^  - \[원문\]'` 을 "오늘 값 +1" 로
  적었지만 깨진 머리의 둘째 줄이 이미 그 카운트 안에 있어 실제 값은 11 그대로다 — 기준으로 쓸
  숫자는 그것이 **무엇을 세고 있는지**까지 봐야 기준이 된다.

## 2026-08-31 21:5x | 38 digest-rotate | 개발 2/2 | 시도0 (문서 45줄 삭제 · 코드 0줄)
- 한 일: `docs/digest.md` 회전. 취소선 최상위 항목 27줄 + 딸린 `  - [원문]` 11줄 +
  `## 완료` 의 2026-08-25·26 행 7줄 = **45줄 삭제, 추가 0**. `## 반복 실패` 는 손대지
  않았다(머리말이 "보류·반복실패는 남긴다") — 그 안의 닫힌 CLI 취소선 1개도 남겼다.
- 결과: **232줄·90,566바이트 → 187줄·56,818바이트**(−36.4%, 상한 200 안) · 열린 항목
  **47 그대로** · `^- ~~` **1** · 반복 실패 **3항목** · 단위 456 OK · e2e rc 0 · DB 무변경.
- 배운 것: **사본 위 시뮬레이션의 바이트는 ±0.7% 로 읽는다.** 탐침 C 가 57,241 을 냈고
  실측은 56,818 이었다(기준선도 90,568 이 아니라 90,566). 줄 수·항목 수는 정확히 맞았다 —
  **셀 수 있는 것을 완료 기준으로 삼은 것이 옳았다.**
- 다음: 테스트 phase. **이 파일이 이 항목으로 305줄이 되어 상한 300 을 넘었다** — 다음
  반복의 첫 일이 `history_current.md` 회전이다(계획 38 의 "하지 않을 것"이라 여기서 안 한다).

## 2026-08-31 22:0x | 38 digest-rotate | 테스트 1/1 | 시도0 (테스트 39줄 · 제품 코드 0줄)
- 한 일: 개발 1 이 되돌린 사고에 재현 테스트를 붙였다 — `tests/test_docs.py` 하나가
  `digest.md`·`index.md`·`history_current.md` 의 **첫 줄이 H1 인지**만 본다. 갭 6종 중
  9점 하나만 메웠고, 상한(줄 수) 검사는 일부러 뺐다 — `history_current.md` 가 305줄로
  이미 상한 밖이라 넣는 즉시 RED 이고, 그 회전은 계획 38 의 "하지 않을 것"이다.
- 결과: 단위 456 → **457건 OK** · `b11fd40` 손상판을 그대로 주입하니 그 단언 하나가
  죽고 되돌린 뒤 `cksum` 동일 · e2e rc 0 · `digest.md` 한 글자도 안 바뀜 · DB 무변경.
  README `단위 456건` 을 457 로 고쳤다 — `test_readme.py` 가 안 고치면 즉시 RED 를 낸다.
- 배운 것: **아는데 미룬 것에는 테스트를 걸지 않는다.** 상한 초과는 `status.md` 가 두
  반복째 적어 온 보이는 문제고, 머리 손상은 4일·25커밋을 아무도 못 본 문제였다. 테스트가
  값을 내는 자리는 사람이 이미 보고 있는 곳이 아니라 **보고 있지 않은 곳**이다.
- 다음: 리뷰 phase. 상한 검사는 `history_current.md` 회전 뒤에 넣으면 공짜다.

## 2026-08-31 22:2x | 38 digest-rotate | 리뷰 1/1 | 시도0 (문서 5파일 · 제품 코드 0줄)

- 한 일: 백지 리뷰어를 **먼저** 별도 세션에 돌려 결과를 받고(머지 가능 · 지적 2건) 역할
  관문을 열었다. 지적 **6건 · 제품 동작 0건 · 4건 자동 수정**. `tests/test_docs.py` 를
  `b11fd40` 손상판 사본에 재판정(FAILED 1, rc 1) · 인용 13곳 전수 재측정 · 회전이 지운
  숫자가 `index.md` 에 살아 있는지 대조 · `docs/history_current.md` **318 → 97줄** 회전
  (`history_015.md`, 계획 36+37 여덟 반복).
- 결과: 단위 **457건 OK**(수정 뒤 재실행) · e2e 통과 · `digest.md` **189줄**(≤200) ·
  `grep -c '^- \['` 47 → **48**(후보 1줄 추가) · `crawl.db` sha256 무변경 · `src`·`e2e` 0줄.
- 배운 것: **백지 리뷰어는 `docs/` 를 못 보므로 문서 사이의 참조 붕괴를 구조적으로 못 잡는다.**
  이번 회전이 죽인 인용 2곳은 둘 다 `status.md` 자신에 있었고, 계획서 탐침 D 의 "8곳"
  목록에도 빠져 있었다 — **세는 사람이 자기가 서 있는 파일을 안 센다.**
- 다음: **DONE 처리** — 계획서를 `plan_history_024.md` 로, `index.md` 를 완료로.

## 2026-08-31 22:4x | 38 DONE + 39 indexer-lock | 탐색+계획 | 시도0 (문서만 · 코드 0줄)

- 한 일: 계획 38 마감(`git mv` → `plan_history_024.md` · 머리 DONE · `index.md` 완료) 뒤
  **옛 경로를 가리키던 세 곳을 전수 대조**했다 — `status.md`·`index.md` 는 고치고
  `history_current.md:52` 는 **그때의 기록**이라 안 고쳤다(`history_015.md` 에도
  `plan_indexer-interrupt.md` 인용이 그대로 있는 것이 선례다). 이어 discover 1~8순위를
  전부 실측하고 **계획 39 `indexer-lock`** 을 열었다(`docs/plan_indexer-lock.md`, 2스텝 ·
  설계 생략 · e2e 없음). 착수 탐침 10종을 임시 디렉터리에서 돌렸다.
- 결과: 1~6순위 **0건**(단위 **457건 OK** · 린터 없음 · 코드 `TODO` 0 · `candidates.md`·
  `patches/` 없음 · `## 보류` 비어 있음 · 후보는 전부 도달 불가·천장 수용·사람 몫).
  7순위 `## 반복 실패` 에서 나왔다. 실측 — 쓰기 락 20초 아래서 색인이 **5.02초 rc 1
  트레이스백**(첫 색인·증분 둘 다), 검색은 **0.02초 rc 0**(WAL). `crawl.db` sha256 무변경 ·
  `src`·`tests`·`e2e` **0줄**.
- 배운 것: **닫힘 표시된 반복 실패 항목이 여전히 근거다.** "CLI 가 트레이스백을 낸다" 는
  `indexer-cli-guard` 로 닫혔는데, 그 항목이 스스로 적어 둔 "탐침이 셋을 다 봤다" 는
  **인자만** 본 것이었다 — 락은 인자가 아니라 **환경**이라 다섯 계획 어디서도 안 쟀다.
  그리고 **처방을 사본에서 미리 재니 절반이었다**: `timeout=30` 은 8·20초 락을 회수하지만
  35초 락에서는 사본도 죽는다(30.04초 rc 1). `digest.md [7]`("한 줄만 넣으면 된다는 처방을
  의심하라")이 또 맞았고, 그래서 계획이 1스텝이 아니라 2스텝이다.
- 다음: 개발 1/2 — `indexer.py` 의 세 연결에 `timeout=30`. TDD 로 **8초 이상** 락을 쥐는
  테스트를 먼저 쓴다(0.3초 락은 기본 5초가 받아줘 인자를 지워도 통과한다).
  `index.md`·`metrics.md` 도 이번 커밋에서 함께 올렸다 — `## 반복 실패` 3회 항목이 그것이다.

## 2026-08-31 23:3x | 39 indexer-lock | 개발 1/2 | 시도0 (제품 3줄 · 테스트 +1)

- 한 일: `indexer.py:78·136·181` 의 `sqlite3.connect(db_path)` 를 `timeout=30` 으로.
  값은 `store.py:22` 가 계획 8 에서 고른 것을 그대로 따랐다(새로 정하지 않았다).
  테스트가 먼저다 — 하위 프로세스가 `BEGIN IMMEDIATE` 로 **8초** 락을 쥐고 `locked` 를
  찍으면 그 줄을 읽은 뒤에 `indexer.main()` 을 부른다(락을 실제로 쥔 것을 보고 시작한다).
- 결과: **RED 를 눈으로 봤다** — `sqlite3.OperationalError: database is locked` at
  `indexer.py:94`, 계획서 탐침 B·E 와 같은 자리다. 고친 뒤 단위 457 → **458건 OK**
  (11.9초, 늘어난 8초는 새 테스트의 락 대기다) · e2e 2종 rc 0 · `crawl.db` sha256 무변경 ·
  README 의 `단위 457건` 을 458 로(그 가드가 즉시 울었다) · `src` 는 3줄, 다른 파일 0줄.
- 배운 것: **변이를 "지우기" 로만 잡으면 값은 여전히 안 잠긴다.** M1(3곳 삭제)은 고치기 전
  상태와 바이트가 같아 RED 관찰 자체가 증거였지만, 그것만으로는 `timeout=1` 도 통과한다.
  **M2(30 → 3)** 를 사본에 심어 RED 를 따로 봤다 — 8초 락이 3초 timeout 을 못 넘는다.
  `digest.md [7]` 이 지적한 거짓 초록의 반대편이 여기다: **락 시간이 방어하려는 값보다
  길어야 그 값이 단언된다.** 사본은 `.git`·`*.db` 없이 `src`·`tests` 만 복사했고,
  `sed -i ''` 뒤 자리 3곳을 grep 으로 먼저 확인했다(BSD sed 거짓 초록, `digest.md [8]`).
- 다음: 개발 2/2 — `main` 에 `except sqlite3.DatabaseError` 갈래(rc 2 + 한 줄 안내).
  스텝 1 은 문턱을 5초에서 30초로 옮겼을 뿐이다. 락 35초는 오늘도 rc 1 트레이스백이고,
  `DatabaseError` 로 잡으면 "진짜 DB 가 아닌 파일" 형제 구멍이 같은 갈래에 덮인다.

## 2026-09-01 00:0x | 39 indexer-lock | 개발 2/2 | 시도0 (제품 9줄 · 테스트 +2)

- 한 일: `indexer.main` 에 `except sqlite3.DatabaseError` 갈래 —
  `NoCrawlDataError`·`StaleIndexError` 와 같은 자리·같은 관용구로 rc **2** + stderr 한 줄.
  락(`"locked" in str(e)`)이면 "크롤이 끝난 뒤 다시 돌린다", 아니면 원문 그대로.
  테스트가 먼저다 — `TestCli.test_lock_past_timeout_is_a_message_and_rc_2` 와
  `..._not_a_database_is_a_message_and_rc_2`.
- 결과: **RED 를 눈으로 봤다** — 둘 다 트레이스백이 그대로 샜다(`indexer.py:228`·`:67`).
  고친 뒤 단위 458 → **460건 OK**(11.7초) · e2e 2종 rc 0 · `crawl.db` sha256 무변경 ·
  README `단위 458건` → 460(가드가 또 즉시 울었다) · 제품 diff 는 `indexer.py` 한 파일.
  **진짜 35초 락을 탐침으로 따로 쟀다**: 30초 만에 rc 2 · 트레이스백 0줄 · 첫 줄이 복구법
  (고치기 전 30.04초 rc 1). 비 DB 파일도 실물로 rc 2 + `file is not a database` 원문.
  변이 M2(`except` 삭제) errors=2 · M3(rc 2 → 0) failures=2 · M4(락 분기 문구 제거)
  failures=1 — 사본은 `.git`·`*.db` 없이 `src` 만, 대조군 OK 를 먼저 확인했다.
- 배운 것: **단위에서 못 재는 시간은 탐침이 재야 한다.** timeout 30 을 넘기는 락은
  정의상 30초가 드는데 그 값을 단위 테스트에 넣으면 스위트가 30초 느려진다. 단위는
  같은 예외를 세워 **갈래**를 고정하고(변이 3개로 죽는 것을 확인), **경로**는 임시
  디렉터리 탐침이 한 번 실측했다. 갈래만 재고 경로를 안 재면 "예외가 실제로 저 자리에서
  나는가" 가 미검증으로 남는다 — 스텝 1 의 RED 가 `indexer.py:94` 였는데 스텝 2 의
  실제 락 예외는 `_doc_count` 쪽에서 먼저 났다. 자리는 예상과 달라진다.
- 다음: 테스트 phase. 락 e2e(동시 실행 시나리오)가 19종에 없다 — 붙일지가 그 물음이다.

## 반복 196 — 계획 39 `indexer-lock` 테스트 1/1 (2026-09-01)

- 한 일: 갭 탐색 네 자리 실측 → 진짜 갭 하나(`--query` + 비 DB 파일에 단언 0건)를
  테스트 한 건으로 닫았다. `src/` **0줄** — 동작은 이미 옳았고 없던 것은 증거뿐이다.
- 결과: 단위 460 → **461건 OK**(11.74초) · e2e 2종 rc 0 · `crawl.db` sha256 무변경 ·
  README `단위 460건` → 461. **변이 M5**(`DatabaseError` 갈래를 `query is None` 으로
  좁힘)가 65건 중 **새 테스트 하나만** 죽였다 — 색인 쪽 형제 셋은 초록.
  갭 아닌 셋도 근거를 남겼다: 두 사용자 예외는 MRO 가 `RuntimeError` 라 가려질 수
  없고(`KeyboardInterrupt` 는 `Exception` 하위도 아니다), 비 DB 색인 경로는 이미 실물
  테스트가 있고, rc 2 충돌은 39 가 만든 것이 아니라 21·24·26·27·29 와 `crawl.py:380`
  이 이미 쓰던 관례라 `## 보류` 로 넘겼다.
- 배운 것: **동작이 옳은 갈래에도 진입점마다 증거가 필요하다.** 개발은 티켓이 말한
  경로(색인)를 고쳤고 `--query` 도 우연히 같이 나았는데, 그것을 재는 단언이 없으니
  "우연히 나았다" 와 "설계상 그렇다" 가 구분되지 않았다. 가른 것은 실행이 아니라
  **변이**다 — 갈래를 티켓의 경로로 좁히는 변이는 기존 460건을 전부 통과한다.
  갭 탐색의 산출물이 꼭 새 동작일 필요는 없다. 여기서는 **좁아지지 못하게 만드는 못**이었다.
- 다음: 리뷰 phase. 물음 셋 — rc 2 충돌(보류) · 락 e2e 를 19종에 붙일지 ·
  `_doc_count`(136)·`search`(181) 의 `timeout=30` 은 아직 단위 단언이 없다.

## 반복 197 — 계획 39 `indexer-lock` 리뷰 1/1 (2026-09-01)

- 한 일: 백지 패스 먼저, 대조 패스 나중. 물음 셋을 전부 실측으로 답하고 테스트 1건
  (`test_every_connection_waits_the_same_thirty_seconds`)만 고쳤다. `src/` **0줄**.
- 결과: 단위 461 → **462건 OK** · e2e 2종 rc 0 · `crawl.db` sha256 무변경 · `digest.md`
  48 → **50항목·191줄**. **변이 M6**(`_doc_count`·`search` 의 `timeout=30` 만 삭제)이
  **461건을 전부 통과**했고 M7(셋 다)은 락 테스트 하나만 죽였다 — 새 테스트가 M6 를 죽인다.
- 배운 것: **"단위로 못 재니 e2e 로 올린다" 가 항상 답은 아니다** — WAL 에서 읽기는 락에
  안 막혀 그 두 대기값은 어느 계층에서도 관측되지 않는다. 관측 불가능한 방어값은 계층을
  올릴 것이 아니라 값을 못 박는다. 둘째: **"관례를 따랐다" 는 전수 조사여야 한다** —
  `serve.py:329-333` 이 환경 오류를 rc 1 로 내라고 적어 뒀다(결론은 살고 근거는 절반).
- 다음: DONE 처리(계획서 → `plan_history_025.md`, `index.md` 완료 표시). e2e 없음(7절).

## 반복 198 — 계획 39 DONE · 계획 40 `exit-code-contract` 개설 (2026-09-01)

- 한 일: 계획 39 를 DONE 으로 마감(`git mv` → `docs/plan_history_025.md` · `index.md` 완료)
  하고, discover 1~8순위를 돌려 6순위 `digest.md ## 다음 계획 후보` `[7]`(세 CLI 종료 코드
  계약이 갈렸다)로 계획 40 을 열었다. `docs/plan_exit-code-contract.md` · 브랜치
  `loop/exit-code-contract`(기점 `064e8a5`). `src/`·`tests/`·`e2e/` **0줄**.
- 결과: 1~5순위 전부 0건(단위 **462건 OK 11.883초** · 린터 없음 · 코드 `TODO` 0 ·
  `candidates.md`·`scripts/` 없음 · `## 보류` 비어 있음). 실측 rc — 없는 DB 2 ·
  비 DB 파일 2 · `serve --port 80` **1** · `file://` 시드 2 · 무인자 2.
  `crawl.db` sha256 무변경 · `digest.md` 50항목·191줄 그대로.
- 배운 것: **백지 패스가 남긴 후보의 세부도 백지 패스다** — `[7]` 이 "세 모듈짜리" 라고
  적었지만 `crawl.main` 에는 환경 오류 갈래가 **0개**이고, 환경으로 센 `NoUsableSeedsError`
  는 `crawl.py:25-32` 가 **사용자 입력 오류**라고 이유까지 적어 뒀다. 계획 39 가 "관례를
  따랐다는 주장은 전수여야 한다" 로 배운 것이 **그 리뷰가 낳은 후보에서 되풀이됐다.**
  둘째: 관례로 인용된 `serve` 의 rc 1 은 **단언이 0건**이다 — 39 리뷰의 M6 와 같은 모양이라
  계획 40 의 첫 스텝을 "값을 바꾸기 전에 오늘의 값을 못박는다"(`src/` 0줄)로 잡았다.
- 다음: 설계 1. 대안 셋(A 환경을 rc 1 로 · B `serve` 를 rc 2 로 · C 재시도 축을 별도 코드로)
  중 고르고 7절 e2e 를 되받아 적는다. `docs/design_exit-code-contract.md`.

## 반복 199 — 계획 40 설계 1 · 대안 A(환경 rc 1 / 명령줄 rc 2) (2026-09-01)

- 한 일: `docs/design_exit-code-contract.md` 를 쓰고 계획 6절의 물음 셋에 답했다.
  **A** 를 골랐다 — `indexer.main` 의 환경 오류 네 갈래를 rc 2 → **rc 1**(제품 diff 는
  `indexer.py` 4줄, `serve`·`crawl` 0줄). **C**(재시도 축을 별도 값으로)는 보류.
  `src/`·`tests/`·`e2e/` **0줄**.
- 결과: 단위 **462건 OK 11.783초**(기준선 유지) · e2e 생략(코드 0줄) · `crawl.db`
  sha256 무변경 · `digest.md` 50항목·191줄 그대로. 개발 스텝을 **2 → 1개로 합쳤다**.
- 배운 것: **호출을 grep 해 커버리지를 세면 헬퍼 뒤의 단언이 통째로 안 보인다** —
  계획이 "`serve` 의 rc 1 을 붙드는 단언 0건" 이라고 적은 근거는 `grep "serve.main"`
  이었는데, `tests/test_serve.py:890` 이 `assertEqual(rc, 1)` 로 값을 붙들고 docstring 에
  규칙까지 적어 뒀다(단언은 헬퍼 `call()`(`:792`)을 거쳐 부른다). 계획 39 가 "관례를
  따랐다는 주장은 전수여야 한다" 로 배운 것의 **측정 도구 판**이다 — 전수를 세려면
  호출 지점이 아니라 **값**(`rc, 1`)을 세야 했다. 그 정정이 대안 B 의 비용을 0곳에서
  1곳 + 기록된 근거 삭제로 올렸고, "오늘의 값을 못박는" 스텝 1 의 전제도 함께 무너뜨렸다.
  둘째: **가른 것은 크기가 아니라 틈이었다** — A 는 단언 7곳을 옮기고 B 는 제품 1줄인데,
  파이썬이 **잡히지 않은 예외를 이미 rc 1 로 끝내므로** B 를 고르면 처리한 환경 오류 2 ·
  처리 못 한 것 1 로 영구히 갈린다. 두 파일이 그 사실을 이미 적어 뒀다
  (`serve.py:329-333` "안 잡을 때도 1 이었다" · `test_indexer.py:230` "고치기 전엔
  트레이스백 + rc=1"). **저장소가 고른 값이 아니라 런타임이 내고 있던 값이 기준선이다.**
- 다음: 개발 1(마지막 스텝). TDD 로 단언 7곳(`test_indexer.py` 6 + `tokenizer_e2e.py:153`)을
  먼저 뒤집어 RED 를 보고 `indexer.py` 4줄 + `README` 계약 표로 GREEN. 변이 M1.
