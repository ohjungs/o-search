---
signal: GREEN
phase: 리뷰
step: 1
attempt: 0
iteration: 233
updated: 2026-09-02
ctx: 48
night_iterations: 92
night_red: 0
night_retries: 0
plan: docs-record-guard
---

# 현재 상태

**계획 45 `docs-record-guard` 리뷰 1/1 완료 — 판정 통과 · 지적 3건 보고 · 고침 0건.**
백지 패스를 먼저 회수했다. **제품 동작 지적 0건**(제품 `src/` 가 이 계획 내내 0줄이다).
백지 패스가 잰 것 하나가 값이 있었다 — **설계가 갈림길 B 에서 고른 "명부 한 줄이
색인이다" 는 아카이브 23개 중 다섯에만 붙들려 있다.** 나머지 18개는 `## 완료` 절의
산문 회전 줄에도 이름이 있어 명부에서 그 이름만 지우면 **초록**이다. 다만 불변량
자체("이름으로 찾을 수 있나")는 23개 전부에 대해 성립하고, **명부 줄 전체를 지우는
변이 R1 은 RED**(실패 목록이 계획 45 가 닫은 그 다섯을 정확히 찍는다 — 실측).
그래서 **고칠 것 없음**으로 닫고 기록만 남긴다.
단위 **482건 OK**(맨몸) · `src/` 0줄 · 새 파일 0개 · 의존성 0.
**다음 반복은 e2e phase.**

## 방금 한 것 (2026-09-02 · 리뷰 1/1)

**패스 A(백지)를 먼저 끝내고 findings 를 적은 뒤에 계획·설계를 열었다.**
대상은 `git diff 79952ee..HEAD` — 다섯 커밋, `tests/test_docs.py` +167줄과 문서.
제품 `src/` 는 0줄이라 렌즈 2·3 이 볼 것은 검사 코드 자신뿐이다.

### 패스 A — 백지에서 잰 것

**[A1] `ArchiveIndexTest` 가 보는 것은 "명부"가 아니라 `## 완료` 절 전체다** (중요도 3).
검사는 아카이브 이름이 그 절 **어디에든** 있으면 초록이다. 그런데 그 절에는 명부 한 줄
말고도 회전 압축 줄 20여 개가 산문으로 들어 있고, 그 산문들이 이름을 또 적고 있다.
`indexed()` 를 직접 불러 **명부 줄과 나머지를 갈라 쟀다**:

| 무엇 | 값 |
|---|---|
| 아카이브 | 23개 |
| 명부가 싣고 있다 | 23개 |
| **산문 회전 줄에도 이름이 있다** | **18개** (004·006~018·020~023) |
| **명부가 유일한 포인터** | **5개** — 001 · 002 · 003 · 005 · 019 |

**즉 계획 45 가 닫은 구멍 다섯이 정확히 "명부만이 붙들고 있는 다섯"이다.**
나머지 18개는 명부에서 이름 하나를 지워도 산문이 대신 초록을 만든다.
설계 갈림길 B 의 결론(*"명부 한 줄이 색인이다"*)은 **다섯에 대해서만 붙들려 있다.**

**변이 R1 로 확인했다 — 명부 줄 통째로 삭제 → RED.** 실패 메시지가 찍은 목록이
`history_001.md`·`002`·`003`·`005`·`019` **정확히 그 다섯**이다. 위 표의 예측 그대로다.

**그래도 고치지 않는다.** 검사가 지키기로 한 불변량은 *"아카이브를 이름으로 찾을 수
있나"* 이고 산문 포인터도 그 불변량을 참으로 만든다. 18개의 명부 항목은 산문이
회전으로 지워지는 순간 RED 가 되어 **그때 자기를 되찾는다**(자가 치유). 명부 줄만
따로 재는 단언은 *불변량이 아니라 구현*을 붙드는 것이고, 테스트 phase 가 역방향
검사를 중요도 4 로 버린 것과 같은 이유로 버린다.

**[A2] `IterationPatternTest.TABLE` 의 첫 행은 장식이다** (중요도 4, 안 고침).
`| phase | 반복 수 |` 는 `^\| 반복` 앵커에 어떤 확대 변이로도 안 걸린다 — 그 행은
`| phase` 로 시작한다. M2 를 실제로 죽이는 것은 2·3행(`| 반복 상한 | 0 |` 의 `0`)이다.
docstring 이 *"이웃 세 행"* 이라고 적었지만 재는 것은 **둘**이다. 오해를 부르지만
행을 지우면 실물 `metrics.md` 의 배치를 덜 닮는다 — 그대로 둔다.

### 패스 B — 계획·설계 대조

**계약은 전수 이행됐다.** 설계 4절 표를 한 줄씩 대조했다.

- `ITER_ROW`·`ITER_LINE` 이 계약표의 정규식 **글자 그대로**이고 모듈 상수다.
- 눈먼 자리 셋에 가드가 전부 있다 — glob 빈손(`assertTrue(archives)`) · `digest`
  부재(`is_file`) · 절 부재(`assertIsNotNone(section)`). 매치 `None` 도 비교 **전에** 실패.
- 갈림길 A = 대안 2(`## 완료` 절만), 갈림길 B = 대안 1(명부 한 줄). V7 이 재는
  경계 매칭 `(?<![A-Za-z_])` 존재.
- 한도: `src/` 0줄 · 새 파일 0개(`history_022`·`023` 은 회전 산출물) · 의존성 0 ·
  `data/crawl.db` 무변경 · `digest.md` **200줄**(상한 정확히) · 고친 파일 2개.
- `index.md` 45번 행 · `metrics.md` · `README.md` 482 전부 갱신돼 있다.

**[B1] 갈림길 B 가 만든 새 규약이 저장소 안에만 있다** (중요도 3, 이 계획이 못 고친다).
*"회전은 명부에 새 이름을 더한다"* 는 `digest.md` 머리 주석에 적혔지만, 회전을 실제로
지시하는 룰은 **저장소 밖** `~/.claude/skills/loop-harness/rules/docs.md` 이고 거기엔
여전히 *"넘으면 오래된 완료 항목부터 지운다"* 만 있다. 다만 **잊어도 RED 가 가르쳐
준다** — 개발 2/2 의 회전이 그것을 실물로 밟았다. 룰 파일을 고칠지는 사람 몫이라
아래 `## 사람이 정할 것` 에 넣는다.

### 안 한 것

- 명부 줄만 따로 붙드는 단언(A1) · `TABLE` 첫 행 제거(A2) — 위에 사유를 적었다.
- 변이는 R1 하나만 심었다. 설계 V1~V8 은 개발이, 검사 코드를 넓히는 M1~M3 은 테스트가
  이미 소진했다. R1 은 **둘 다 안 잰 축**(데이터도 정규식도 아니고 *명부 줄의 존재*)이라 쟀다.

## 한도 (매 반복 확인)

- `main` 직접 커밋 금지 · `--no-verify` 금지 · 외부 네트워크 금지 ·
  `docs/specs/` 읽기만 · `data/crawl.db` 무변경(sha256 확인) · 의존성 추가 금지(stdlib 만).
- **러너 파이프 — 이번 반복 0회.** 전 실행이 맨몸이다. 직전 두 반복이 연속으로
  *"판정 줄만 뽑으려고"* `| tail` 을 썼다가 둘 다 테스트 표준출력을 받아 결과를 못 본
  자리라, **판정이 필요하면 맨몸으로 돌리고 눈으로 읽었다.** 연속 카운트 1 부터 다시 센다.
  (변이 실행 한 번은 `unittest` 자신의 `-k` 필터를 썼다 — 셸 파이프가 아니라 러너 인자다.)
- **변이 R1 은 제자리 편집이었다** — `.mutation-lock` 을 걸고 심었고, `git checkout --`
  으로 되돌린 뒤 지웠다. 되돌리기를 `&&` 로 잇지 않았다. **고친 것이 `.md` 라
  `__pycache__` 함정은 이번엔 경로 밖**이지만, 되돌린 뒤 전수 482건을 맨몸으로 다시
  돌려 초록을 눈으로 확인했다. 작업 트리에 `.mutation-lock` 없음 · `git status` 깨끗.
- 이번 반복 최종 실측(맨몸): `PYTHONPATH=src python3 -m unittest discover -s tests`
  **482건 OK 11.847초** · `src/` **0줄** · 새 파일 0개 · 의존성 0 ·
  `data/crawl.db` **무변경**(`git diff --stat -- data/` 0줄) · `digest.md` **200줄**.
- **집안일 — 이번 반복은 회전 미달이다.** `history_current.md` 는 이 기록을 붙여도
  상한 300 아래다(직전 반복이 318 → 149줄로 밀었다). 다음 회전이 명부에 이름을 더할
  의무는 그대로다.

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-01 확인)

`origin/main` 은 `494313b`(PR #5 병합). 계획 43·44 의 커밋들(`bc40ea9`~`79952ee`)이
아직 안 갔다. **남은 것 (사람 결정)**: 그것들을 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 — 둘이 열려 있다 (하나가 닫혔다)

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
   offset 이 0 이 되는 날 검사기가 멈추고, 짝은 그때 열릴 계획의 일이다.
3. ~~기록 3파일 불일치~~ — **계획 45 가 열었다** (리뷰까지 왔다).
4. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   이제 *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, 회전을 지시하는 `~/.claude/skills/loop-harness/rules/docs.md` 에는
   그 예외가 없다. **저장소 밖이라 루프가 못 고친다.** 안 고쳐도 잊으면 RED 라
   실동작은 안전하고, 룰 문장을 맞출지는 사람이 정한다.
