# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**반복 97~122 는 `history_006.md`, 123~128(계획 020 `deadline` 전체)은
`history_007.md`, 계획 21~24(짧은 경로 3건 + 보류 패치 소진, 2026-08-29 14:20~17:40)는
`history_008.md` 로 밀려났다** (2026-08-29 회전 3회).

---

## 2026-08-29 23:55 | 30 readme-perf-audit | 짧은 경로 | 시도0

- README `## 품질 기준` 표 다섯 줄을 **표가 지목한 명령으로 직접 돌렸다**.
  전부 참이라 표는 한 글자도 안 고쳤다: recall@10 97.5%(ko 20/20 · en 19/20) ·
  p95 9.16ms · 수집 10.22/s · JS 0 B · 최저 명암비 4.87:1(`--fg-url` 라이트).
  **여유가 가장 좁은 축은 명암비(+0.37)와 recall(+17.5%p) 이다** — 나머지 셋은
  예산의 3% 대라 사실상 안 걸린다. 다음에 UI 색을 만지면 [3]축이 먼저 운다.
- **거짓은 표가 아니라 다섯 줄 아래 있었다.** `## 검증` 의 `단위 419건` 이 428 위에서
  초록이었다. 419 는 28 이 README 를 고치던 그 시각의 값이고, 같은 밤 29 가 6건을 더했다.
  **고친 날부터 낡는 숫자를 손으로 적어 둔 것**이라 다시 적는 것만으로는 또 낡는다.
- 그래서 28 이 만든 `tests/test_readme.py` — 이 저장소에서 문서를 입력으로 읽는
  유일한 검사 — 에 단언을 하나 얹었다. README 가 안내한 그 명령으로 직접 센다
  (`unittest.defaultTestLoader.discover` · `e2e/*.py` glob). 새 파일 0 · 새 추상 0.
- 변이 3종 전부 잡힘: `419`(원래 값) · `e2e 17종`→`16종` · `# 단위 429건` 주석 삭제.
  **BSD sed 를 안 썼다**(`digest [8]` 의 거짓 초록) — 치환도 원문 복원도 파이썬이
  하고 복원 여부를 스크립트가 단언한다.
- **안 한 것: README 표에 실측값 열을 박는 것.** 지금 그 열을 지킬 검사가 없어서,
  방금 고친 것과 같은 종류의 낡을 숫자를 하나 더 만드는 일이다. 실측 기준선은
  `project.md` 와 `status.md` 가 갖는다.
- 검증: 단위 428 → **429건 OK** · e2e **17종 전부 rc=0**(넷은 이 스텝의 측정 도구 자체).

## 2026-08-29 23:58 | 31 port-zero-cover | 짧은 경로 | 시도0

- `serve --port 0` 에 단위 커버가 0 이었다(digest `[4]`). `src` 는 **0줄** 고쳤다 —
  코드는 이미 옳았고 없던 것은 그것이 옳다는 증거다.
- **왜 이 한 줄이 위태로웠나**: `0` 의 뜻이 플래그마다 다르다. `--max`·`--workers`·
  `--deadline` 은 0 을 거절하고(26), `--port` 는 0 이 "아무 포트나" 다. 파서
  (`flags.number_flag`)는 **범위를 일부러 안 본다**(019 설계 결정) — 그래서 그 비대칭을
  지키는 자리는 `serve.main` 의 `port > 65535` **한 줄뿐**이었고, 그 줄을 조이다
  `port < 1` 을 같이 넣으면 단위 429건이 전부 초록이고 e2e 넷만 죽는다.
- 기존 `test_the_highest_real_port_still_serves` 의 **짝**을 놓았다. 새 파일도 새 클래스도
  없다 — 같은 `TestCliArgs`, 같은 `call()` 헬퍼(가짜 `make_server`, 그래서 회귀해도
  테스트가 매달리지 않는다).
- 변이 3종: ① 하한 `< 1` 추가 → **새 테스트 하나만** 죽음 ② `number_flag(...) or 8000`
  (0 을 기본값으로 삼킨다) → **새 테스트 하나만** 죽음 ③ 상한 제거 → 기존 둘이 죽음.
  ①②가 429건 중 그 한 건만 죽였다는 것이 "e2e 넷이 유일한 파수꾼" 이 **사실이었다**는 증거다.
- **30 이 붙인 이빨이 첫 실사용에서 울었다.** 테스트를 하나 더하자 README 의
  `단위 429건` 이 그 자리에서 FAILED 를 냈다 — 사람이 세어 고칠 일이 아니게 됐다.
- 검증: 단위 429 → **430건 OK** · `src` 무변경(`git diff -- src` 빈 출력)이라 e2e 는
  serve 를 쓰는 넷만 다시 돌렸다(전부 rc=0). 나머지 13종은 30 에서 rc=0 확인.

## 반복 151 — 32 `serve-500-log-cause` (짧은 경로)

**측정이 결론이었다.** `digest`/`status` 의 `[6]` "`serve.do_GET` 의 넓은 `try`"(확신
낮음)를 **고치기 전에 먼저 쟀다.** `_page_hits` 에 `AttributeError`·`TypeError`·
`MemoryError` 를 주입해 JSON·HTML 두 경로를 실제로 때렸다:

- 프로그래밍 오류까지 **500**, 응답 본문은 고정 문구, `Traceback` 유출 0.
- `BaseException` 이 아니라 `Exception` 이라 `KeyboardInterrupt` 는 **안 삼킨다**
  (실측: 응답 없이 연결 종료 = 정상). 종료가 500 으로 위장되지 않는다.
- `try` 는 `_parse`+`_page_hits` 만 덮고 **200 응답 렌더링은 `else` 에 있다** —
  넓다고 부른 그 `try` 는 이미 좁혀져 있었다.

**그래서 `src` 를 0줄 고쳤다.** `[6]` 은 코드 문제가 아니었다.

**대신 재는 동안 진짜 구멍이 테스트 쪽에서 나왔다.** 넓은 `try` 의 유일한 실위험은
"원인이 로그에만 있는데 그 로그를 아무도 단언하지 않는 것" 인데,
① 화면 경로(`_do_html`)의 500 은 **로그 단언이 아예 0건**이었고
② JSON 쪽 단언은 `"search 실패"` 라는 **접두어만** 봐서 `%r` 을 고정 문구로 바꿔도
초록이었다. 그 순간 500 은 **고칠 수 없는 500** 이 된다.

- 새 파일·새 클래스 없다. 기존 `TestMissingDb` 에 단언 한 줄, 기존
  `TestHtmlPathFailsSafely` 에 테스트 하나. 목도 안 썼다 — DB 없는 기존 픽스처가
  이미 `FileNotFoundError` 를 낸다.
- **단언 문자열을 처음에 `OperationalError` 로 잘못 적었고 테스트가 바로 잡아냈다**
  (실제 원인은 `FileNotFoundError`). 추측한 원인이 아니라 **찍히는 원인**을 못박았다.
- 변이 3종 전부 잡힘, 각각 **새 단언 하나씩만** 죽였다(= 그 자리에 파수꾼이 없었다는
  증거): ① 화면 `log_error` 통째 삭제 ② JSON `%r` → 고정 문구 ③ 화면 `%r` → 고정 문구.
  심기 전에 `count(old) == 1` 로 **원문이 실제로 있는지 먼저 단언**했고(`digest [8]`
  거짓 초록), 치환·복원은 파이썬이 했다(BSD `sed -i` 안 씀).
- 검증: 단위 430 → **431건 OK** · `src` 무변경(`git diff --stat` 이 `README.md`·
  `tests/test_serve.py` 둘뿐)이라 e2e 는 serve 를 쓰는 둘만 다시 돌렸다(rc=0).
  나머지는 30·31 의 17종 그대로.
- README `단위 430건` 이 **또 그 자리에서 FAILED** 를 냈다(28 이 만들고 30 이 이빨을
  붙인 검사). 두 번째 실사용이고 두 번 다 정확했다 — 431 로 고쳤다.

## 2026-08-29 | (계획 없음) | 계획(탐색) | 시도0

- **탐색만 하고 정지했다. `src` 0줄 · 새 계획 0.** `discover.md` 8개 출처를 순서대로
  실측: 실패 테스트 **0건**(단위 431 OK · e2e **17종 전수** rc=0 — 직전 둘은 `src`
  무변경이라 일부만 돌렸으므로 전수 초록은 여기서 처음 봤다) · 미반영 리뷰 지적 0 ·
  `TODO`/`FIXME` 0(grep 히트 2건은 둘 다 설명 주석) · `candidates.md` 는 이 저장소에 없음.
- **`SKILL.md` 4-1 이 걸렸다.** 짧은 경로 연속 3건(30·31·32)의 근거가 **셋 다 같은
  우물**이었다 — `status`/`digest` 의 **미룬 후보 목록**(5순위). 룰대로 정지.
  **"연속 3건" 이 신호가 아니라 "셋이 한 출처" 가 신호**라는 룰 문장 그대로의 사례다.
- **출처 1~4 가 0건이라는 실측이 그 판정을 뒷받침한다** — 저장소가 새 근거를 못 내고
  루프가 자기가 적어 둔 목록만 갉고 있었다.
- 남은 후보를 셋으로 갈라 status 에 근거로 남겼다: **(a) 승인 대기**(스키마 2 · 재크롤
  정책 · userinfo 보안 경계) · **(b) 도달 불가**(`int_max_str_digits` 3.9.6 · `quality_eval`
  `limit=100` 실물 28건 · `fixture_defects` ko·en, 코퍼스 동결이라 `ja` 가 생길 수 없다) ·
  **(c) 정식 계획 크기**(가짜 시계 이음매 9곳 · pop 시각 간격 · OFFSET 드리프트).
  **(b) 를 지금 짜면 죽은 코드고, (c) 는 40줄·설계 트리거를 넘어 야간 자동 적용 밖이다.**
  그래서 `계획 소진` 이 아니라 **`탐색 막힘`** 이라고 적었다.
- **곁가지로 실물 고장 하나를 찾았다: `status.md` 프런트매터가 통째로 비어 있었다**
  (`---` 뒤 빈 줄). `night_iterations`·`night_red`·`night_retries` 는 **정지 조건 검사와
  `metrics-record.js` 가 직접 읽는** 필드라, 없는 동안 야간 정지 조건이 근거 없이
  돌았다(스크립트는 빈 값을 조용히 받는다). 복원했다 — 밤 누적 35.
- 검증: 단위 **431건 OK** · e2e **17종 전부 rc=0**. 코드 변경 0이라 변이 검사 대상 없음.
- 다음: 정지. 사람이 (a) 를 열거나 (c) 중 하나를 골라 주면 정식 경로로 재개한다.

## 2026-08-29 | clock-injection (33) | 계획 | 시도0

- **반복 152 의 `탐색 막힘` 을 정식 경로로 열었다.** `docs/plan_clock-injection.md` 개설.
  근거는 `digest.md:156` `[6]` — digest 가 직접 "자기 계획 크기다" 라고 적어 둔 건이다.
  `src` **0줄**. 브랜치 `loop/clock-injection`, 기점 `de28dfb`(`main` 이 아니다 —
  그 커밋이 status 프런트매터 복원과 152 기록을 들고 있고 정지 조건이 그 필드를 읽는다).
- **문제를 다시 진술하면서 이름이 바뀌었다.** "테스트 중복 10곳" 이 아니라
  **`crawl.py` 가 시각 읽기는 인자(`now`)로, 잠들기는 모듈 전역(`time.sleep`)으로
  다룬다는 비대칭**이다. 그래서 테스트가 몽키패치 말고는 잡을 손잡이가 없다.
  중복은 증상이고 비대칭이 원인이다.
- **실측 정정 1 — 9곳이 아니라 10곳이다.** `digest.md:156` 이 9곳이라고 적어 뒀는데
  `grep -c 'crawl\.time\.sleep'` 이 **10**(65·622·672·742·787·843·948·1023·1096·1325).
  `e2e/` 에는 0곳. 계획 종료 시 digest 를 고친다.
- **실측 정정 2 — 잠드는 자리는 두 곳이고 서로 다른 스레드다.** `crawl.py:74` 는
  `before_send` 안이라 **워커**, `crawl.py:179` 는 **메인** 루프다. 스텝 2·3 을 나눈 이유.
  `_fetch_one` 의 제품 호출자는 `crawl.py:170` **하나뿐**이라 시그니처를 바꿔도 파장이 좁다.
  `frontier.py` 는 `now` 만 받고 잠들지 않으므로 범위 밖으로 못 박았다.
- **설계로 넘겼다** — 트리거 2개(공개 인터페이스 변경 · 대안 3갈래).
  `plan.md` 6-1 대로 **계획서 안에서 저울질하지 않고** 스텝을 "이음매" 라고만 써서
  어느 안을 골라도 말이 되게 뒀다. 세 출발점(최소/정공법/되돌리기 우선)은 status 에 적었다.
- **e2e 시나리오를 계획 시점에 확정했다.** 겉보기 동작이 안 바뀌는 리팩터라
  e2e 는 "달라진 게 없다" 를 증명하는 자리다 — 가짜 시계는 초록인데 진짜 대기가 사라지는
  것이 유일한 실제 위험이고, 그건 서버 시각을 재는 `crawl_delay_e2e.py`(예의) ·
  `perf_crawl.py`(반대쪽 실수: 대기가 늘어남) · `deadline_e2e.py`(메인 쪽 대기)만 본다.
- 검증: 단위 **431건 OK** 기준선 재확인. 코드 변경 0이라 변이 검사 대상 없음.
- 다음: `docs/design_clock-injection.md` 작성.

## 2026-08-29 | clock-injection (33) | 설계 | 시도0

- **대안 B(정공법)를 골랐다**: `sleep` 을 `now` 옆에 기본값 있는 인자로 추가한다.
  `_fetch_one(url, robots, now, floor, sleep=time.sleep)` ·
  `crawl(..., deadline=None, sleep=time.sleep)`. `src` **0줄**(설계는 문서만).
- **선택을 가른 것은 설계 중 잰 사실이다.** `mock.patch("websearch.crawl.time.sleep")` 은
  `crawl` 모듈만 건드리는 게 아니라 **stdlib `time` 모듈을 프로세스 전역·전 스레드로**
  갈아끼운다 — `mock` 이 따라가는 `websearch.crawl.time` 이 공유된 그 모듈 객체이기
  때문이다. 실측으로 확인했다(다른 스레드에서도 가짜, 빠져나오면 복구).
  **`crawl` 은 워커를 띄우므로** 그 안의 `fetcher`·`urllib`·`futures` 중 누구든
  `time.sleep` 을 쓰기 시작하면 가짜 시계가 조용히 흘러 간격 단언이 거짓이 된다.
  오늘 사고가 안 난 건 운이다.
- **그래서 이것은 크롤 윤리 축의 문제로 판정했다.** 컨셉 갈림길 1순위가
  `크롤 윤리(robots·간격)` 이고 도메인 1초는 "어기는 코드는 리뷰에서 RED" 인 전제
  조건인데, 그 전제를 재는 장치의 사정거리가 프로세스 전역이었다. 좁히는 쪽이 맞다.
- **버린 이유를 남겼다.** A(테스트 전용 가짜 시계 컨텍스트 매니저) = 중복은 줄지만
  **원인이 아니라 증상**을 고친다. 중단 신호는 여전히 막히고 전역 사정거리도 그대로다.
  C(시계 객체) = `now=` 쓰는 **19곳 전부**를 고치게 만드는데, `sleep` 까지 필요한 곳은
  크롤 경로뿐이고 `frontier.py` 는 잠들지 않는다 — 안 쓰는 짝을 들고 다니는 YAGNI.
  **정말 묶고 싶어지면 B 위에서 묶는 것이 지금 묶는 것보다 싸다**는 것이 근거다.
- **되돌리기 수단은 ② 덧붙이기만**(`design.md` 3절). 기본값이 곧 꺼진 플래그라
  아무도 `sleep=` 을 안 넘기면 오늘과 동일하다. 제품과 테스트를 따로 무를 수 있다.
- 계약 6개 중 함정 둘을 명시했다: **인자를 뒤에 붙일 것**(위치 인자 순서를 바꾸면
  `_fetch_one` 을 직접 부르는 테스트 7곳이 조용히 어긋난다) · **두 대기 자리가
  같은 하나를 쓸 것**(갈라 놓으면 한쪽만 가짜로 만들 수 있게 되어 원래 상태로 돌아간다).
- 검증: 문서만 바뀌어 코드 검증 대상 없음. 단위 431건 OK 그대로(`src` 무변경).
- 다음: 개발 스텝 1 — 실패 테스트 2건을 쓰고 빨간불을 눈으로 확인한다.

## 2026-08-29 | clock-injection (33) | 개발 스텝1 | 시도0

- **실패 테스트 2건을 먼저 썼다** (`tests/test_crawl.py` `TestSleepIsInjected`).
  `src` **0줄**. ① `crawl(..., sleep=fake)` 를 넘기면 `fake` 만 불리고 전역
  `time.sleep` 은 0회 ② `crawl`·`_fetch_one` 의 `sleep` 기본값이 `time.sleep`.
- **빨간불 원문**(단위 433건 중 이 2건만 실패):

  ```
  TypeError: crawl() got an unexpected keyword argument 'sleep'
  KeyError: 'sleep'
  ```

- **약한 빨강이라 탐침을 따로 돌렸다.** 둘 다 단언에 닿기 전에 시그니처에서 터진다 —
  그러면 "실패를 봤다" 가 단언이 옳다는 근거가 못 된다(`dev.md` 0절이 막으려는 바로 그것).
  같은 시나리오를 `sleep=` 없이 돌려 재니 **오늘 전역 `time.sleep` 이 `[1.0, 1.0]`
  2회** 불린다. ①의 "전역 0회" 는 오늘 거짓이고 스텝 2 뒤에 참이 되는, 살아 있는 단언이다.
- **시나리오를 워커 쪽으로 좁혔다.** 시드 1개·링크 없음·재시도 2회라
  `crawl.py:74`(워커)만 지나고 `crawl.py:179`(메인)는 안 지난다. 계획이 스텝 2의 완료
  기준을 "스텝 1 테스트가 초록" 으로 적어 뒀는데, 메인 쪽까지 지나면 스텝 3 전에는
  초록이 될 수 없어 그 기준이 거짓말이 된다.
- **전역 관찰법**: `mock.patch("time.sleep", wraps=time.sleep)` — 진짜에 위임하는
  **스파이**라 동작을 안 바꾼다. 설계가 문제 삼은 전역 패치는 진짜를 **가짜로 갈아끼우는**
  쪽이고, 여기서는 세기만 한다. 주입이 새면 "그래도 잤다" 가 그대로 보인다.
- **README 단위 건수 431→433.** `tests/test_readme.py` 가 README 의 숫자와 실제를
  맞추는 단언을 들고 있어 함께 움직인다(안 고치면 무관한 테스트가 빨개진다).
  직교 편집이 아니라 같은 변경의 일부다.
- 검증: 단위 **433건 · 실패 2건(의도된 TDD 빨강)**. `src` 무변경이라 변이 검사 대상 없음.
- 다음: 개발 스텝 2 — `_fetch_one` 에 `sleep=time.sleep` 을 **뒤에** 붙이고
  `crawl.py:74` 를 `sleep(remaining)` 으로, `crawl()` 이 `crawl.py:170` 에서 넘긴다.
  스텝 1 의 2건이 초록이 되고 나머지 431건은 그대로 초록이어야 한다.

## 2026-08-29 | clock-injection (33) | 개발 스텝2 | 시도0

- **워커 쪽 대기를 주입 지점으로 옮겼다.** `_fetch_one(url, robots, now, floor,
  sleep=time.sleep)` · `crawl.py:74` 가 `sleep(remaining)` · `crawl()` 이
  `deadline` 뒤에 `sleep=time.sleep` 을 받아 `crawl.py:170` 에서 그대로 넘긴다.
  diff 10줄(`src` 6 · `tests` 4). 스텝 1 의 TDD 빨강 2건이 초록이 됐다.
- **계획의 가정 하나가 거짓이었다.** 스텝 2 는 "몽키패치 10곳은 그대로지만 전역 패치가
  기본값도 잡으므로 초록" 이라고 적어 뒀는데, **기본 인자는 `def` 실행 시점에 한 번
  평가돼 `_fetch_one.__defaults__` 에 진짜 `time.sleep` 객체가 박힌다.** `mock.patch` 는
  `time` 모듈의 **속성**만 갈아끼우므로 이미 박힌 기본값에는 닿지 않는다. 실측으로 확인:
  `_fetch_one.__defaults__ == (<built-in function sleep>,)` 이고 패치 중에도 그대로다.
- **그래서 첫 실행에서 8건이 빨개졌다** (`TestRetriesKeepTheInterval` 3 ·
  `TestRetryUsesWhatTheFrontierKnows` 5). 가짜 시계가 안 흘러 간격이 `[0.0, 0.0]` 으로
  찍혔고 진짜로 자느라 전체가 129초 걸렸다. **이 빨강 자체가 주입이 실제로 동작한다는
  반증이다** — 안 옮겨졌다면 전역 패치가 계속 먹혀 아무 일도 안 일어났을 것이다.
- **최소 수정으로 닫았다**: 호출 지점 3곳(`:849`·`:954` 의 `crawl.crawl`, `:1030` 의
  `_fetch_one` 직접 호출)에 `sleep=ms` 를 넘긴다 — 이미 만들어 둔 그 가짜 시계 목이다.
  기대값도 시나리오도 안 건드렸다(계약 6). `mock.patch` 는 **남겨 뒀다**: 메인 쪽
  (`crawl.py:179`)이 아직 전역을 쓰므로 스텝 3 전에는 그게 필요하다.
- **스코프 이탈을 기록한다.** `tests/test_crawl.py` 는 스텝 2 의 "건드릴 파일"(`src` 만)
  **밖**이다. 대안이 없었다 — 기본값을 늦게 묶는 `sleep=None` 센티널 우회는
  **스텝 1 이 이미 고정한 `test_default_sleep_is_the_real_one` 을 깨고**, 스텝 4 가
  어차피 지울 임시 발판이다. 스텝 4(몽키패치 치환)의 일이 3줄 줄었다.
- 검증: 단위 **433건 전부 OK** (3.1초, `PYTHONDONTWRITEBYTECODE=1`). `_fetch_one` 을
  부르는 곳이 `crawl.py:170` 하나뿐임을 `src`·`e2e`·`tests` 전역 grep 으로 확인했다.
- 다음: 개발 스텝 3 — `crawl.py:179` 를 `sleep(...)` 으로. `crawl()` 이 이미 인자를
  들고 있어 **한 줄**이다. 그때 설계 계약 2("두 대기 자리가 같은 하나")가 완성된다.

## 2026-08-29 | clock-injection (33) | 개발 스텝3 | 시도0

- **메인 쪽 대기도 주입 지점이 됐다 — 설계 계약 2 완성.** `crawl.py:179` 의
  `time.sleep(wait if left is None else min(wait, left))` 를 `sleep(...)` 으로.
  `src` diff **1줄**. 이제 `grep -n 'time\.sleep' src/websearch/crawl.py` 는
  시그니처 기본값 2곳(`:33`·`:88`)만 남고 **호출은 0곳**이다. 두 대기 자리
  (`:74` 워커 · `:179` 메인)가 같은 하나를 쓴다.
- **스텝 2 와 똑같은 이유로 첫 실행이 멈췄다.** 기본 인자는 `def` 시점에 박히므로
  `mock.patch("websearch.crawl.time.sleep")` 가 `:179` 에 안 닿는다. 가짜 시계가
  안 흘러 `frontier.seconds_until_ready()` 가 영원히 1.0 이고, 진짜 `time.sleep` 으로
  **무한 루프**에 들어갔다(첫 테스트에서 120초 타임아웃). 스텝 2 는 8건이 빨개졌지만
  메인 루프는 **행**으로 나온다 — 같은 원인의 다른 얼굴이다.
- **최소 수정으로 닫았다**: 남은 `crawl.crawl()` 호출 7곳에 `sleep=ms` 를 넘긴다.
  이미 만들어 둔 그 가짜 시계 목이다. **기대값(`[1.0]`·`[5.0]`·`[1.0, 1.0]`)도
  시나리오도 한 글자도 안 건드렸다**(계약 6) — `tests` diff 는 전부 `sleep=ms` 추가다.
  `mock.patch` 줄은 스텝 4 의 일이라 그대로 뒀다(잔량 10곳).
- 검증: 단위 **433건 전부 OK** (3.27초). 스텝 2 가 빠뜨린 **e2e 를 이번에 전수 실행했다
  — 17종 전부 rc=0** (`crawl_delay`·`perf_crawl`·`deadline` 포함).
- **변이 검사(예비)**: M1(메인 대기 삭제) → 타임아웃 rc=124 로 **죽는다**.
  M2(`:179` 를 전역 `time.sleep` 으로 되돌리기) → **rc=0, 안 죽는다.**
  몽키패치 10곳이 아직 그 전역을 잡아 주기 때문이고, 이것이 바로 스텝 4 가 있는 이유다.
  **스텝 5 의 M2 는 스텝 4 뒤에 다시 재야 의미가 있다.**
- 다음: 개발 스텝 4 — `tests/test_crawl.py` 의 `mock.patch("websearch.crawl.time.sleep")`
  10줄을 지운다. `ms` 는 이제 전부 `sleep=` 으로 넘어가므로 `mock.Mock()` 로 바꾸면 된다.

## 2026-08-29 | clock-injection (33) | 개발 스텝4 | 시도0

- **몽키패치가 사라졌다.** `tests/test_crawl.py` 의
  `mock.patch("websearch.crawl.time.sleep") as ms` **10줄을 지우고**, 뒤따르던
  `ms.side_effect = lambda s: ...` 를 `ms = mock.Mock(side_effect=lambda s: ...)` 로
  바꿨다. 치환은 완전히 기계적이었다 — 10곳이 한 글자도 안 다른 같은 모양이라
  스크립트로 한 번에 돌렸다. 스텝 2·3 이 임시 발판으로 붙여 둔 `sleep=ms` 가
  이미 전부 자리에 있어서 **그 외에는 붙일 것이 없었다.**
- `ms` 를 평범한 함수가 아니라 `mock.Mock` 으로 둔 이유: `TestDeadline._elapsed` 가
  `ms.call_args_list` 로 흘러간 시간을 잰다(`test_crawl.py:1237`). 호출 기록이
  필요해서 목을 유지했다.
- 잔량 `grep -c 'crawl\.time\.sleep' tests/test_crawl.py` → **0**. 코드 10곳에 더해
  `TestSleepIsInjected` 독스트링 1줄도 과거형으로 고쳤다("한때 몽키패치했는데 …
  지금은 전부 `sleep=` 으로 넘긴다").
- **`src` diff 0줄** (스텝 3 커밋 `4e7feb0` 대비). `tests` diff **+16/-26**.
- 검증: 단위 **433건 전부 OK** (2.93초). **기대값·시나리오는 한 글자도 안 고쳤다**
  (계약 6) — diff 에서 치환 4종과 독스트링을 빼면 남는 줄이 없음을 grep 으로 확인했다.
  실행 시간이 그대로라는 것 자체가 **진짜 `time.sleep` 이 안 불렸다**는 증거다.
- **M2 재측정: 죽는다.** `crawl.py:179` 를 전역 `time.sleep` 으로 되돌리면
  **rc=124**(90초 타임아웃). 스텝 3 예비 측정은 rc=0 이었다 — 몽키패치 10곳이
  그 전역을 잡아 주고 있었기 때문이다. 그것이 사라지자 가짜 시계가 안 흐르고
  메인 루프가 무한히 잔다. **계획 33 이 노린 전환이 실제로 일어났다.**
- **사고 하나 — 변이를 작업 트리에서 재지 마라.** M2 를 제자리에서 재는 동안
  10분 행이 걸렸고, 그 사이 **자동 스냅샷 훅이 변이된 `crawl.py` 를 커밋해
  origin 에 푸시했다**(`0aac7b0` "자동 스냅샷 21:19"). `git checkout -- src` 가
  그 스냅샷을 복원해 변이가 되살아났다 — `git checkout 4e7feb0 -- src/websearch/crawl.py`
  로 되돌렸다. 재측정은 `src`·`tests` 를 스크래치패드에 복사하고 90초 타임아웃을
  걸어서 다시 했다. **스텝 5 는 처음부터 그렇게 돈다.**
- e2e 는 안 돌렸다: `src` diff 가 0줄이라 e2e 가 만지는 코드가 그대로다.
  스텝 3 에서 17종 전수 rc=0 을 이미 받아 뒀다.
- 다음: 개발 스텝 5 — 변이 3종(M1·M2·M3) 정식 판정. 스크래치패드 사본에서,
  `PYTHONDONTWRITEBYTECODE=1` 과 타임아웃을 붙여서.

## 2026-08-30 | clock-injection (33) | 개발 스텝5 | 시도0

- **변이 3종이 전부 죽었다 — 개발 phase 끝.** 스크래치패드 사본(`scratchpad/mut/`,
  `.git` 없음)에서 쟀고, 같은 사본의 기준선이 **433건 OK** 임을 먼저 확인한 뒤 잰 값이다.

  | 변이 | rc | 실패 |
  |---|---|---|
  | M1 — `before_send` 의 대기 블록(`crawl.py:71~74`) 통째 삭제 | **1** | 9건 |
  | M2 — `crawl.py:179` 를 전역 `time.sleep` 으로 되돌리기 | **124** (90초 타임아웃) | 행 |
  | M3 — `crawl.py:61` 의 `max` → `min` | **1** | 11건 |

- **M2 가 계획 33 의 판정이다.** 스텝 3 예비 측정에서는 rc=0 으로 살아남았다 —
  몽키패치 10곳이 그 전역을 잡아 주고 있었기 때문이다. 그것이 사라지자 가짜 시계가
  안 흐르고 메인 루프가 무한히 잔다. 계획서가 "여기서 안 죽으면 테스트가 여전히
  전역에 붙어 있는 것" 이라고 적어 둔 판정이 **이제 통과한다.**
- M1·M3 는 공통으로 `TestRetriesKeepTheInterval`·`TestRetryUsesWhatTheFrontierKnows`·
  `TestSleepIsInjected` 를 죽인다. M3 는 `TestRetriesKeepTheInterval` 의 상한 관련
  2건(`test_a_hair_over_the_cap_does_not_retry`·`test_unkeepable_interval_means_no_retry_at_all`)을
  더 죽여 11건이다.
- **M1 은 한 줄이 아니라 블록 4줄을 지웠다.** `sleep(remaining)` 만 지우면 빈 `if` 라
  문법 오류다. `digest.md:154` `[7]` 의 **"이 줄을 안 썼다면 무엇이 되는가"** 기준에
  맞춘 되돌리기는 `if sends:` 블록 삭제 — `before_send` 가 발신 시각만 적고 안 자는 상태.
- **스텝 4 의 사고는 재발하지 않았다.** 작업 트리는 내내 깨끗했고(`git status` 로 확인),
  변이는 전부 `.git` 없는 사본에서 일어났다. 각 실행에 `timeout 90` 과
  `PYTHONDONTWRITEBYTECODE=1` 을 걸었다 — M2 의 rc=124 가 그 타임아웃이다.
- **복구 기록**: 이 반복은 앞 세션이 스텝 4 도중 죽은 자리에서 시작했다. 코드는
  작업 트리에 살아 있었고 커밋만 안 됐다. 단위 433건 OK 를 먼저 받고 **스텝 5 를
  시작하기 전에 커밋했다**(`1c3fc85`) — 그 커밋이 스냅샷 `0aac7b0` 이 함께 넣어 버린
  `crawl.py:179` 변이의 되돌림도 포함한다.
- 다음: 테스트 phase. `test.md` 6절(단언을 낮추지 않았는지)이 이 계획의 핵심 관문이고,
  진짜 관문은 그 뒤 e2e 3종이다.

## 2026-08-30 04:4x | 33 clock-injection | 리뷰 1 | 시도0

- **리뷰 1건 (informational, 자동 수정). 패스 A 에서 나왔다 — 계획을 열기 전에.**
- **찾은 것**: 계약 클래스 `TestSleepIsInjected` 가 **누수 한 갈래를 구조적으로 못 본다.**
  `mock.patch("time.sleep")` 은 `time` 모듈의 **속성**을 갈아끼우는데, 기본값
  `sleep=time.sleep` 은 **def 시점에 진짜 함수 객체를 붙들고** 있어 패치가 안 닿는다.
  그래서 주입이 **기본값으로 새면** `global_sleep.call_count` 는 0 그대로다.
- **변이 M4 로 실측**(스크래치패드 사본): `crawl.py:171` `pool.submit` 에서 `sleep`
  인자만 뺀다 = `_fetch_one` 이 기본값(진짜 `time.sleep`)을 쓴다.

  | 무엇으로 재나 | M4 결과 | 소요 |
  |---|---|---|
  | `TestSleepIsInjected` (계약을 자처한 클래스) | **OK — 안 죽는다** | 0.003초 → **4.02초** |
  | 전 스위트 | FAILED, 7건 | 3초 → **130초** |

  둘 다 "진짜로 잤다" 는 같은 사실을 가리키는데, **계약 클래스는 그것을 초록으로 넘겼다.**
  계약이 안 지켜진 게 아니라 **자처한 자리가 아니라 옛 간격 테스트들이 지키고 있었다.**
- **digest 가 이미 적어 둔 자리다** — `[7]` "변이는 *이 줄을 안 썼다면 무엇이 되는가* 로
  고른다. **기본값이 있는 인자는 특히 위험하다(0 이 아니다)**". 개발 phase 의 변이 3종
  (M1 대기 블록 삭제 · M2 `:179` 전역 되돌리기 · M3 `max`→`min`)은 전부 **줄을 고치는**
  변이라 **인자를 빼는** 갈래가 비어 있었다. `[100]`("주장은 참인 범위까지만")도 걸린다:
  독스트링의 "주입이 샜을 때 '그래도 잤다' 가 보인다" 는 **모듈 속성 조회로 샐 때만** 참이다.
- **고친 것**: 새 테스트를 만들지 않고 **벽시계 단언 한 줄**을 더했다 —
  `assertLess(elapsed, 0.5, ...)`. mock 이 못 보는 것을 시간이 본다. 정상 0.003초 ·
  M4 4.04초라 여유가 양쪽으로 166배·8배다. `src` diff **0줄** · `tests` diff +13/-1.
- **판정은 2단계로 했다**(digest `[103]`): ① 변이가 실제로 심겼나 — `grep` 으로
  `:171` 에 `sleep` 이 없음을 먼저 확인 ② 새 단언이 죽이나 — **FAILED `4.04초 걸렸다`**.
- **크롤 윤리 — `sleep=lambda s: None` 으로 예절을 무력화할 수 있나: 반쪽만.** 실측:

  | 자리 | 정상 | `sleep=no-op` | 왜 |
  |---|---|---|---|
  | 워커(재시도, `:74`) | `[1.005, 1.005]` | **`[0.0, 0.0]`** | `sleep` 이 **유일한** 강제 |
  | 메인(URL 사이, `:179`) | `[1.001]` | `[1.0]` — 지켜짐 | `frontier.next()` 가 `now()` 로 재검사 |

  메인 쪽은 대신 **91만 회 공회전(벽시계 1.00s에 CPU 1.00s)** 이 된다 — 정상은 CPU 0.00s.
  **대조: `now=` 는 이번 변경 이전부터 두 자리를 다 뚫는다**(`now` 가속 시 간격 0.001초).
  즉 `sleep=` 이 연 표면은 **기존 `now=` 의 부분집합**이고, 둘 다 CLI 에서 도달 불가다
  (`main()` 이 `crawl(args, max_pages, workers=, deadline=)` 만 넘기고, 모르는 `-`
  인자는 rc=2 로 막힌다). **인프로세스 이음매지 신뢰 경계가 아니다.**
- 검증: 단위 **433건 OK**(3.36초, `discover -s tests` — 전체). 변이는 전부 `.git` 없는
  스크래치패드 사본에서 쟀다 — 작업 트리 `src` 는 이 스텝 내내 무변경(`git status` 확인).
- 다음: e2e phase (이 스텝에서는 넘어가지 않는다).

## 2026-08-30 04:2x | 33 clock-injection | 테스트 1 | 시도0

- **관문("낮춘 단언이 있나")을 기계로 닫았다**: `git diff de28dfb -- tests/test_crawl.py
  | grep -cE '^-.*assert'` = **0**. 지워진 단언 줄이 없고 `+` 쪽 `assert` 3줄은 전부
  새 `TestSleepIsInjected` 안이다. 431→433 은 **삭제 0 · 신규 2**.
  기대값 `[1.0]`·`[5.0]`·`[1.0, 1.0]` 은 diff 에 아예 없다 = 안 건드렸다.
- **갭 1건(⑤ 계약, 중요도 8)**: `test_injected_sleep_is_the_only_one_used` 가 워커 쪽
  대기(`crawl.py:74`)만 지나고 있었다 — 자기 독스트링이 그렇게 적어 두고도 아무도 안 읽었다.
  메인 쪽(`:179`)은 **행(rc=124)으로만** 지켜지는데, 행은 *시계에 의존하는* 대기일 때만
  난다. 고정 대기(`time.sleep(0.1)`)를 새로 심으면 행도 빨강도 없이 산다.
- **새 테스트를 만들지 않고 시나리오를 두 줄 넓혔다** — 시드를 같은 도메인 URL 둘로 바꾸니
  쿨다운에서 `frontier.next()` 가 None 을 줘 메인 루프가 `:179` 를 지난다. 단언은 그대로.
  `:179` 에 `time.sleep(0)` 을 심어 **옛 판본 OK(rc=0) / 새 판본 FAILED(rc=1)** 로
  갭의 실재와 소멸을 같은 변이 위에서 증명했다. `src` diff **0줄**.
- 결과: 단위 **433건 OK**(2.93초, `discover -s tests` — 전체). 대표 e2e 3종 rc=0
  (`crawl_delay`·`deadline`·`perf_crawl`) — 판정은 e2e phase 몫이고 여기서는 참고만.
- 다음: 리뷰 phase 1스텝. 볼 곳은 `e2e/` 호출 5곳이 `sleep=` 을 **안 넘기는 것이 의도**임이
  읽히는가다(진짜로 자야 하는 자리다).
