---
signal: GREEN
phase: DONE
step: 1
attempt: 0
iteration: 192
updated: 2026-08-31
ctx: 62
night_iterations: 61
night_red: 0
night_retries: 0
---

# 현재 상태

**계획 38 `digest-rotate` 리뷰 끝(개발 1·2 + 테스트 1 + 리뷰 1/1 완료)** — 계획서 `docs/plan_digest-rotate.md`.
브랜치 `loop/digest-rotate`(기점 `c2a115e`, `loop/indexer-interrupt` 에서 팠다).
계획 37 까지 전부 DONE·아카이브 완료. **`docs/digest.md` 는 232줄·90,566바이트에서
189줄로 내려왔고, 머리 손상을 막는 재현 테스트가 붙었다**(단위 456 → **457**).
설계 생략(계획서 6절) · e2e 없음(7절) · 제품 코드 0줄.
**다음은 DONE 처리** — 계획서를 `plan_history_024.md` 로 아카이브하고 `index.md` 를 완료로.
계획 34~37 은 PR #2 로 `main` 에 병합됐다(`main` 최신 `e0890c8`) — 이번 계획도
`main` 병합은 사람이 정한다.

## 이번 계획이 하려는 일

**탐색이 매번 통째로 읽는 `docs/digest.md` 가 상한을 넘겼고, 한 곳은 깨져 있다.**
① **232줄 · 90,568바이트**(머리말이 정한 상한 200) — `status.md` 가 두 반복 연속으로
"밀린다" 고 적은 집안일이다. ② **머리 두 줄이 깨져 H1 `# 아카이브 요약` 이 리스트 항목
안에 들어가 있다**(`b11fd40`, 2026-08-27 이후 25커밋·4일). ③ 그 사고로 닫힘 표시가
엉뚱한 자리에 붙어, **018 이 이미 닫은 `[5] 호스트 대소문자` 항목이 오늘도 열린 후보로
보인다** — 이번 탐색이 실제로 그것을 열린 후보로 읽었다.

2스텝이다: **스텝 1** 머리 복구 + 닫힘 표시를 `digest.md` 의 `[5] 호스트 대소문자를
정규화하지 않는다` 항목 제자리로,
**스텝 2** 취소선 항목·`[원문]` 하위줄·2026-08-25·26 완료 7줄 삭제. 순서가 있다
(스텝 1 이 만든 취소선이 스텝 2 의 삭제 대상이다). **제품 코드 0줄 · 설계 생략 · e2e 없음.**

## 탐색 phase 가 한 일 (2026-08-31)

- **discover 1~8순위를 전부 실측하고 나서 집안일을 골랐다.** 단위 456건 OK(rc 0) ·
  린터 없음 · 코드 `TODO`/`FIXME`/`HACK` **0건** · `candidates.md` 없음 · 보류는 승인
  대기(재시도 금지) · 후보는 전부 "도달 불가·천장 수용·사람이 정할 갈림길" · 이슈 없음.
- **값이 가장 큰 후보를 일부러 안 열었다** — `digest.md` 의 `[7] 게이트가 열렸다 ·
  크기를 쟀다`(페이지 넘김에
  중복 1건·누락 1건)은 `?page=N` 공개 API 를 커서로 바꾸는 갈림길이라 **항목 자신이
  "사람이 정할 갈림길" 이라고 적었다.** 승인 항목으로 그대로 둔다.
- **회전을 두 반복 막고 있던 선행 조건을 실측으로 해소했다** — `digest.md:<줄번호>`
  인용 8곳 중 **6곳이 오늘 빈 줄이나 다른 항목**을 가리킨다(인용 당시에는 맞았다:
  `git show 1abc058:docs/digest.md` 156줄). **줄번호 인용은 회전 전에 이미 죽어 있다.**
- 회전 시뮬레이션(사본): 취소선만 지우면 **194줄**(여유 6줄뿐이라 두 반복이면 재발),
  2026-08-25·26 완료 7줄까지 지우면 **187줄 · 57,241바이트**(−36%).

## 개발 스텝 1 (2026-08-31) — 머리 복구 + 닫힘 표시 제자리로

`docs/digest.md` 만 3줄 고쳤다(제거 3·추가 3). **옮긴 것뿐이고 새로 쓴 문장은 0** —
지운 머리 1줄과 `:134` 에 붙인 닫힘 문구가 `cksum` 으로 동일하고(981바이트), 복구된
머리 3줄은 사고 이전 판(`b11fd40~1`)의 머리 3줄과 동일하다(28바이트).
**열린 항목은 `[5]` 하나만 빠졌다** — 고치기 전후 목록을 `diff` 로 대조해 확인했다(48 → **47**).
`[5]` 가 018 에서 닫힌 근거: `docs/index.md:129`(18 `url-normalize` 완료 =
`plan_history_016`) · `plan_history_016.md:35`(호스트 소문자) · 실측
`urls.normalize('http://A.com/')` → `http://a.com/`.

## 개발 스텝 2 (2026-08-31) — 회전: 닫힌 것과 가장 오래된 완료를 지운다

`docs/digest.md` 에서 **45줄을 지웠다(추가 0)**. 셋뿐이다: ① 세 후보 섹션의 취소선
최상위 항목 **27줄** ② 그 바로 아래 딸린 `  - [원문]` 하위줄 **11줄** ③ `## 완료` 의
`- 2026-08-25`·`- 2026-08-26` 행 **7줄**(`docs/index.md` 표에 계획별로 이미 있어 중복).
**`## 반복 실패` 는 손대지 않았다** — 머리말이 "보류·반복실패는 남긴다" 이므로 그 안의
닫힌 CLI 항목(취소선 1개)도 남겼다. 계획서 4절 완료 기준 5개를 전부 대조했다.

## 테스트 스텝 1 (2026-08-31) — 머리 손상에 재현 테스트를 붙인다

**`tests/test_docs.py` 하나(단언 1건, 39줄).** `docs/digest.md`·`index.md`·
`history_current.md` 의 **첫 줄이 H1 인지**만 본다. 개발 1 이 되돌린 사고(`b11fd40` 의
append 편집이 H1 을 리스트 항목 안으로 삼킨 것)가 **4일·25커밋 무탐지**였고, 그 사고를
재는 검사가 저장소에 0건이었다(`tests/`·`e2e/` 전수 확인 — 문서를 입력으로 읽는 것은
`tests/test_readme.py` 뿐이고 그것은 README 만 본다).

**변이로 재판정했다** — `git show b11fd40:docs/digest.md` 를 그대로 덮어쓰니 이 단언
하나가 죽었고(`첫 줄이 H1 이 아니다 — '- ~~[5]~~ **닫혔다 …'`), 되돌린 뒤 `cksum` 동일·
`git status` 무변경을 확인했다. 사고를 실제로 일으킨 바이트로 재판정한 것이라
"이런 모양이면 죽겠지" 라는 추정이 아니다.

**형제 파일까지 함께 본다.** 셋 다 append 대상이고 H1 로 시작하므로 같은 사고가 그대로
재발한다 — 증상(digest) 이 아니라 원인(append 가 머리를 삼킨다) 자리에 걸었다.
`status.md` 는 프런트매터(`---`)로 시작해 대상이 아니다.
**제목 문구는 안 본다** — 이름 변경은 정당한 편집이고, 깨진 것은 문구가 아니라 구조다.

### 갭 탐색 (6종) — 중요도 8 이상만 이번 스텝에서 처리

| 갭 | 점수 | 처리 |
|---|---|---|
| 기록 문서 머리 손상을 재는 단언 0건 (탐색 입력이 조용히 오염된다) | **9** | ✅ 이번 스텝 |
| 줄 수 상한(200) 초과를 재는 단언 0건 | 6 | ❌ `digest.md` 후보로 — 아래 사유 |
| 닫힘 표시가 항목에서 떨어져 나간 것 | 5 | ❌ 산문 정규식이라 과적합 |
| `[원문]` 하위줄이 부모 없이 뜬 것 | 4 | ❌ 회전 때만 생기고 줄 수로 이미 보인다 |
| `## 반복 실패`·`## 보류` 항목 수 감소 | 4 | ❌ 완료 기준으로 매 스텝 대조 중 |
| 죽은 `digest.md:<줄번호>` 인용 | 3 | ❌ 계획 38 의 "하지 않을 것" |

**상한 검사를 일부러 안 넣었다** — ① `history_current.md` 가 지금 **305줄로 상한 300 초과**라
넣는 즉시 RED 고, 그 회전은 계획 38 의 "하지 않을 것"이다 ② 상한 초과는 이미
`status.md` 가 두 반복째 적어 온 **보이는** 문제다(놓쳐서 늦은 게 아니라 미뤄서 늦었다).
**아는데 미룬 것에 테스트를 걸면 무시되는 빨간불이 생긴다.**

## 리뷰 스텝 1/1 (2026-08-31) — 지적 6건 · 제품 동작 지적 0건 · 4건 자동 수정

**백지 패스를 별도 세션에 먼저 돌리고 그 결과를 받은 뒤에 역할 관문을 열었다**(계획 37
리뷰의 교훈 — 백지 결과가 리뷰 커밋 뒤에 도착하면 사후 처리가 된다). 백지 리뷰어는
`docs/` 를 안 읽고 `git diff c2a115e...HEAD` 와 `tests/test_docs.py`·`test_readme.py`
소스만 봤다. **판정 머지 가능**, 지적 2건(아래 1·2) — 둘 다 내 역할 패스와 독립적으로 겹쳤다.

| # | 지적 | 점수 | 처리 |
|---|---|---|---|
| 1 | `history_current.md` 가 이 diff 로 270 → **318줄**(상한 300 초과) | 100 | ✅ 회전 — `history_015.md` |
| 2 | 줄 수 상한을 재는 단언이 없다 | 95 | ❌ 판정: 옳은 보류(아래 사유) |
| 3 | 회전이 **살아 있던** `digest.md:<줄번호>` 인용 2곳을 죽였다 | 95 | ✅ 이름 인용으로 |
| 4 | 이 diff 가 `index.md` 에 **새 죽은 인용**을 하나 만들었다 | 90 | ✅ 이름 인용으로 |
| 5 | 계획 5절이 약속한 "인용 방식 후보를 `digest.md` 에 남긴다" 가 빠졌다 | 85 | ✅ 후보 1줄 추가 |
| 6 | 상한까지 여유 13줄은 실측 증가 속도로 한두 반복이다 | 85 | ❌ 회전은 끝났다 — 다음 회전 목표로 |

**3·4 가 이 리뷰의 값이다 — 백지 리뷰어는 `docs/` 를 못 봐서 구조적으로 못 잡는다.**
계획서 탐침 D 는 인용을 **8곳**으로 셌는데(`index.md` 1 · `plan_history_020` 4 ·
`plan_history_021`·`history_010`·`history_011`) **`status.md` 자신의 2곳을 빠뜨렸다.**
전수는 13곳이고, 회전 직전 `c2a115e` 에서 **살아 있던 것은 정확히 그 2곳**
(`status.md:33`→`:135` `[5] 호스트 대소문자`, `status.md:42`→`:178` `[7] 게이트가 열렸다`)
이며 **회전 뒤 0곳**이다. 나머지 11곳은 그전에 이미 죽어 있었다(`:144`·`:154`·`:156`·`:160`).
살아 있는 문서 3곳(`status.md` 2 · `index.md` 1)은 **줄번호를 빼고 항목 이름으로** 고쳤다.
아카이브 8곳은 `history_*.md`·`plan_history_*.md` 가 **수정·삭제 금지**라 그대로 두고
`digest.md ## 다음 계획 후보` 에 `[6]` 항목으로 올렸다(계획 5절이 약속한 그것 = 지적 5).

**지적 2 는 고치지 않는 것이 맞다.** 지금 넣으면 초록이 되지만(회전을 끝냈으므로),
`digest.md` 189/200 · `history_current.md` 97/300 이라 **여유가 비대칭**이다. 상한 검사는
회전을 자기 일로 보는 phase 가 생긴 뒤에 넣어야 무시되는 빨간불이 안 된다.

**회전이 지운 45줄은 복원 가능하다 — `git` 히스토리 말고 살아 있는 문서에 남아 있다.**
지운 `## 완료` 7줄의 실측 숫자를 전수 대조했다: `6.71ms`(004 p95 기준선) · `ko 85` · `en 90`
(006 품질) · `미검출 5 → 1`(010) 이 **전부 `docs/index.md` 계획 표에 그대로 있다**(그 외
`history_001`·`002`·`project.md` 에도). 취소선 항목의 본문도 `index.md` 의 계획별 행이
결과를 담는다. **재발 횟수와 날짜는 `## 반복 실패` 가 갖고 있고 그 절은 바이트 동일이다**
(3항목 무변경 대조 완료) — "처음 보는 실패다" 로 오판될 자리는 거기이고 거기는 안 지웠다.

**`tests/test_docs.py` 가 진짜 그 손상을 잡는지 직접 재판정했다** — 테스트 스텝의 보고를
믿지 않고 스크래치패드 사본에 `git show b11fd40:docs/{digest,index,history_current}.md` 를
그대로 깔고 돌렸다: `FAILED (failures=1)` · rc **1** · `doc='digest.md'` 서브테스트에서
`Regex didn't match: '^# \S' not found in '- ~~[5]~~ **닫혔다 …'`. 사고를 일으킨 실제
바이트에서 죽는다.

**SRE — 이 테스트가 앞으로 누구를 언제 막나.** 세 파일 다 H1 로 시작하는 것이 관례이고
(`# 아카이브 요약` · `# 계획 목록` · `# 최근 반복 기록`), 이번 `history_015.md` 회전에서
새로 쓴 `history_current.md` 머리도 그대로 H1 이라 **정당한 작업이 걸리는 경로가 안 보인다.**
제목 **문구**를 안 보므로 이름 변경도 안 걸린다. 파일이 없어지면 `assertTrue` 가 경로를
찍고 죽는다. **거짓 실패 위험 낮음 · 못 잡는 것은 2번째 줄 이하의 구조 붕괴와 상한 초과**
(후자는 지적 2 로 후보에 있다). **CSO — 문서와 테스트뿐이고 신뢰 경계·비밀·권한 변화 0.**

## 검증 (이번 반복에 직접 돌렸다)

- 단위 **457건 OK · rc 0**(3.8초) — 자동 수정 4건 **뒤에** 다시 돌린 값이다.
- `PYTHONPATH=src python3 e2e/indexer_interrupt_e2e.py` **통과**(19종, 6시나리오 출력 확인).
- 변이 재판정(스크래치패드 사본): `b11fd40` 손상판 → `FAILED(1)` rc 1. 사본은 전부 지웠다.
- 회전 무손실 대조: `history_015.md` 본문 == 원본 46~271줄, 잔여 == 원본 272줄~ (파이썬
  리스트 비교로 `True` 둘). 반복 12 = 아카이브 **8** + 잔여 **4**.
- `wc -l` `digest.md` **189**(≤200 ✓) · `history_current.md` **97**(≤300 ✓) ·
  `head -1` 셋 다 H1 ✓ · `grep -c '^- \['` **47 → 48**(후보 1줄 추가 — 감소가 RED 이지 증가는 아니다).
- 린터·타입체커는 이 저장소에 없다(`docs/project.md:10`) — 돌릴 것이 없다.
- `data/crawl.db` sha256 `85c96744…5bda18` **무변경**. `src/`·`e2e/` **0줄**.

## (아래는 테스트 스텝이 남긴 것 — 리뷰가 닫은 항목은 위 표에 있다)

- 단위 **456 → 457건 OK** — `PYTHONPATH=src python3 -m unittest discover -s tests`.
- 변이 재판정: `b11fd40` 손상판 주입 → `tests.test_docs` **FAILED(1)**, 되돌린 뒤 OK.
- `PYTHONPATH=src python3 e2e/indexer_interrupt_e2e.py` rc **0**(19종, 6시나리오 출력 확인).
- `README.md` `단위 456건` → **457건** — `test_readme.py::test_verification_counts_match_reality`
  가 강제한다(안 고치면 즉시 RED). e2e 19종은 그대로.
- 린터·타입체커는 이 저장소에 없다(`docs/project.md`) — 돌릴 것이 없다.
- `wc -l` **187**(≤200 ✓) · `grep -c '^- \['` **47** ✓ · `head -1` `# 아카이브 요약` ✓ —
  **`docs/digest.md` 는 이번 스텝에 한 글자도 안 바뀌었다**(회전은 개발 2 에서 끝났다).
- `data/crawl.db` sha256 `85c96744…5bda18` **무변경**. `src/`·`e2e/` **0줄**.

## 남은 것 (다음 반복이 읽을 것)

- **바이트는 예상보다 423 적다** — 계획서 탐침 C 는 57,241 을, 실측은 **56,818**을 냈다
  (기준선도 90,568 이 아니라 **90,566**이었다). 줄 수·항목 수 기준은 전부 맞았고
  바이트는 완료 기준이 아니다 — **사본 위 시뮬레이션의 바이트 값은 ±0.7% 로 읽는다.**
- ✅ **죽은 인용 — 리뷰가 살아 있는 3곳을 이름 인용으로 고쳤다.** 아카이브 8곳은
  수정·삭제 금지라 그대로이고 `digest.md ## 다음 계획 후보` `[6]` 으로 올라갔다.
- ✅ **`history_current.md` 318 → 97줄 회전 완료**(`history_015.md`, 계획 36+37 여덟 반복).
  회전은 `tests/test_docs.py` 아래에서 했다 — 새 머리도 H1 이라 457건 그대로 초록이다.
- **`digest.md` 여유가 189/200 = 11줄뿐이다 — 다음 회전은 더 깊게 가야 한다.** 실측 증가
  속도는 `b11fd40` 125줄(08-27) → `1abc058` 171줄(08-30) → 232줄(08-31)로 **하루 +46·+61**
  이다. 계획 3절 표도 "취소선만 지우면 여유 6줄이라 두 반복이면 다시 넘는다" 며 187 을
  골랐지만 **13줄도 같은 크기의 여유다.** 다음 회전 목표를 **150줄대**로 잡는다.
  이번엔 안 건드렸다 — 계획 38 은 "회전은 끝났다, 더 지우지 마라" 가 선이었다.
- **상한(줄 수) 검사는 여전히 후보다** — 지금은 두 파일 다 상한 안이라 넣으면 초록이지만
  여유가 11 대 203 으로 비대칭이라, 회전을 자기 일로 보는 phase 가 생긴 뒤에 넣는다.
- **회전 지각이 네 번째다**(393·322·336·318) **그런데 `## 반복 실패` 에 항목이 없다.**
  회전은 phase 룰이 아니라 문서 위생이라 아무 phase 도 자기 일로 안 본다 — 매번 리뷰가
  잡는다. 항목 추가는 계획 38 의 `## 반복 실패` 3항목 불변량에 걸려 여기서 안 했다.
- **보류 2건은 그대로 열려 있다**(`plan_history_023.md` 12-2) — ① `commit()` 뒤 도착한
  중단이 "색인은 바뀌지 않았다" 를 거짓으로 만든다 ② `--query` 도중 중단도 같은 안내를
  낸다. 둘 다 사용자 표면이라 자기 RED 가 필요하다.
- **후보 2건** — ① `sqlite3.connect` timeout 불일치(`indexer.py:78·136·181` 5초 대
  `store.py:22` 30초) ② `digest.md:<줄번호>` 인용 규약(아카이브 8곳을 고칠지, 규약을
  `docs.md` 에 못박을지 — `digest.md ## 다음 계획 후보` `[6]`).

## 한도 (넘으면 RED)

- `data/crawl.db` 실물·스키마를 안 건드린다. 탐침은 임시 디렉터리에서만.
- **열린 후보 항목이 하나라도 사라지면 RED**(`grep -c '^- \[' docs/digest.md` = **48** —
  리뷰가 인용 규약 후보 1줄을 더했다. 감소만 RED 다).
- `docs/digest.md` 의 `## 반복 실패`(3항목) · `## 보류` 항목 수 변경 금지.
- 기존 단언을 낮추지 않는다 · 단위 457건이 줄면 RED.
- 도메인당 요청 간격 1초 이상 · robots.txt 준수.
- 외부 네트워크 금지 · `docs/specs/` 읽기만 · `--no-verify` 금지 · `main` 직접 커밋 금지.
