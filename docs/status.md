---
signal: GREEN
phase: 리뷰
step: 2
attempt: 0
iteration: 178
updated: 2026-08-30
ctx: 44
night_iterations: 51
night_red: 0
night_retries: 0
---

# 현재 상태

**계획 35 `deadline-stop` — 스텝 1·2 + 테스트 + 리뷰 1/1 완료. 다음은 e2e phase.**
계획서 `docs/plan_deadline-stop.md`. 브랜치: `loop/deadline-stop`
(`loop/graceful-interrupt` `eef1b50` 에서 팠다). 계획 34 는 DONE·아카이브 완료.

**스텝 1 실측**: 탐침 재실측 종료 **70.08 → 10.05초** · 서버 수신 **3건 → 1건**(t=0.05) ·
rc **0**(`예산 2초 소진`). 단위 **450건 OK**(+3) · e2e **18종 rc=0** ·
변이 4종이 **각각 새 단언 하나씩만** 죽었다. 남은 10.05초는 소켓 10초 — 계획서 5절이
안 건드리기로 한 자리다.

**스텝 2 실측**: `e2e/deadline_e2e.py` 에 시나리오 3(안 답하는 서버 · `Crawl-delay: 30` ·
`--max 5 --deadline 2`, CLI 서브프로세스)을 붙였다. **서버가 받은 페이지 요청 1건** ·
**10.1초** · rc **0** · stdout `수집 0 페이지` · stderr `예산 2초 소진` · DB **0행**.
파일 rc=0, 9.7 → **20.3초**(늘어난 10초가 소켓 타임아웃 그 자체다). `--control` → **rc 2**
(시나리오 3 만 따로 돌려도 2 — 파일 전체로는 시나리오 0 이 먼저 죽어 새 가드가 안 걸린다).
변이 M1·M2 각각에서 **요청 3건 · 80초**로 죽었다. 단위 **450건 OK** · e2e **18종 rc=0**(164초).

**테스트 phase 실측**: 갭 탐색이 **형제 구멍 하나**를 찾았다 — `crawl.py:248` 의
`min(wait_for, left)` 가 **한 번도 안 불리고 있었다.** 스텝 1 의 wall-clock 테스트는
도메인이 하나뿐이라 `busy` 가 그것을 빼면 `seconds_until_ready()` 가 `0.0` 을 주고,
`or None` 때문에 늘 "예산을 그대로 쓴다" 가지로만 간다. 변이 `min`→`max` 가 단위
**450건을 전부 통과**했다(실측, `.git` 없는 사본). 단위 1건 추가 → **451건 OK**.
새 테스트에서 변이 2종(`min`→`max` · 자르기 삭제)이 **각각 새 단언 하나만** 죽인다
(둘 다 2.01초 — 형제의 5초 쿨다운을 기다린다). e2e **18종 rc=0**(161초, 무변경).
**이 저장소엔 린터·타입체커가 없다**(`docs/project.md`) — 검증은 unittest + e2e 가 전부다.

**리뷰 1/1 실측**: 대상 `fbbe8a3..HEAD`. `rules/review.md` 0절대로 **패스 A(백지)를
먼저 끝내고** 패스 B로 갔다. **후보 8 · 80점 미만 버림 3 · 보고 5 · 고침 5(승인 필요 0)**.
**제품 *동작* 지적은 0건이다** — 제품 파일 수정 2곳은 둘 다 주석/독스트링이다.
A1 `crawl()` 독스트링이 `stop` 계약을 아직 읽기 둘(`is_set`·`wait`)로만 적는데 `:205` 가
`stop.set()` 을 부른다 — `FakeStop` 이 우연히 `set()` 을 가져서 451건이 전부 초록이었다.
A2 `:241` 의 "완료를 기다린다(계약 8)" 주석이 바로 아래 새 코드와 반대를 말한다.
A3 `HANG_LIMIT 12.0` 은 예산 2 + 소켓 10 **정확히 그 합**인데 서브프로세스 기동부터
재서 여유가 1.9초다 — 진짜 단언은 건수라 흔들리면 **오탐 RED**(digest `[4]` 에 셋째로).
B1 `docs/index.md` 가 `0/2 · 미작성` 에 멈춰 있었다. B2 `metrics.md` 의 `반복` 이
**175 인데 status 는 177** — 반복 3회에 phase 칸이 1회만 올랐다.
**B1·B2 는 같은 뿌리라 `digest.md ## 반복 실패` 에 2회로 올렸다** — 계획 34 리뷰가
`1abc058` 에서 같은 것을 잡았다. 기록 파일이 셋인데 스텝 커밋은 `status.md` 만 본다.
고친 뒤 재검증: 단위 **451건 OK**(3.58초, rc 0) · 계획이 건드린 e2e 3종 개별
**전부 rc=0**(`deadline_e2e` 수신 1건·10.1s · `interrupt_e2e` SIGINT 뒤 10.0s rc 130 ·
`crawl_delay_e2e` 2.01s/1.00s).

**곁가지 사고**: 3시간 자동 스냅샷(`a9202b6`)이 작업 중인 트리를 커밋하고 **푸시까지**
했다 — digest `## 반복 실패` 의 그 항목이 **3회째**다. 이번엔 변이가 아니라 정상 작업이
잡혔고(변이는 사본에만 심었다), 다만 README 갱신 전이라 그 커밋 하나는 RED 다.
원격에 이미 갔으므로 전례(`06bc289`)대로 되감지 않고 **앞으로 커밋해** 덮었다.

기준선(착수 전 실측, 2026-08-30): 단위 **447건 OK** · e2e **18종 rc=0** ·
recall@10 100%/95% · 오탐 평균 14.0 · p95 9.30ms · JS 0 B · 최저 명암비 4.87:1 ·
처리량 10.25/10.28 docs/s.

## 이 계획이 여는 것

**`--deadline` 의 계약이 워커 안에서 깨진다.** 예산이 만료돼도 워커의 재시도는 나간다.
착수 탐침: `--deadline 2` · 안 답하는 서버 · `Crawl-delay: 30` →
**종료 70.08초**(예산의 35배)이고 서버가 받은 페이지 요청이 **3건**(`t=0.05 / 30.05 / 60.06`)이다.
**t=2 에 예산이 만료된 뒤 두 건이 더 나갔다** — "꼬리가 붙는다"가 아니라 계약 위반이다.
근거: `digest.md ## 다음 계획 후보` `[5]`.

**그 항목의 처방("`stop.set()` 몇 줄이 전부")은 절반만 맞았다** — 사본에서 갈라 재니
`stop.set()` 만은 **70.08초**(메인 루프가 `futures.wait(timeout=None)` 에 갇혀 예산
만료를 못 본다), `futures.wait` 자르기만도 **70.07초**(워커의 재시도 잠이 그대로),
**둘 다일 때만 10.05초 · 요청 1건**이다. 계획 34 가 신호 축에서 배운
"깨우기와 접기는 하나다" 가 예산 축에서 되풀이됐다.

**오늘의 447건과 e2e 18종은 이 변화를 하나도 안 잰다** — 사본에서 전부 초록이었다.
그래서 스텝 1·2 의 본체는 코드가 아니라 **그것을 재는 단언**이다.

## 다음

**리뷰** — 두 스텝과 테스트 phase 가 끝났다. 리뷰가 볼 자리: ① 예산 만료가 `stop` 을 세우는
것과 SIGINT 가 세우는 것이 `signaled` 로만 갈린다(rc 0 vs 130) — 세 번째 세우는 곳이
생기면 다시 섞인다. ② 남은 10초는 소켓 타임아웃이라 **의도한 자리**다(계획 23).
③ e2e 시나리오 3 에서 M1·M2 가 같은 단언으로 죽는다 — 가르는 것은 스텝 1 의 단위다.
④ 자르기가 **두 자리**(`crawl.py:239` 제출 전 잠 · `:248` 결과 대기)로 나뉘어 같은
관용구를 두 번 쓴다 — 테스트도 두 벌이 됐다. 합칠 자리인지 리뷰가 본다.

## 한도 (넘으면 RED)

- 도메인당 요청 간격 1초 이상 · robots.txt `Crawl-delay` 준수. **예산 만료 중에도 그렇다.**
  이 계획은 재시도를 *접는* 것이지 *빨리 보내는* 것이 아니다.
- `data/crawl.db` 실물·스키마를 안 건드린다. e2e·탐침은 임시 디렉터리에서만.
- 외부 네트워크 — 로컬 테스트 서버만.
- 기존 단언을 낮추지 않는다. 시간 상한을 올려 초록을 만드는 것은 실패다.
- `docs/specs/` 는 사용자 소유(읽기만) · `--no-verify` 금지 · `main` 직접 커밋 금지.
