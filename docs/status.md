---
signal: GREEN
phase: e2e
step: 1
attempt: 0
iteration: 273
updated: 2026-09-02
ctx: 72
night_iterations: 115
night_red: 1
night_retries: 0
plan: passage-api
---
# 현재 상태

**리뷰 7 통과 — 계획 48 의 검토를 닫고 e2e 로 보낸다. 제품 결함은 0건이다.**
활성 계획 `passage-api` · 설계서 `docs/design_passage-api.md` · 브랜치 `loop/passage-api` ·
기점 `6e99031`. **단위 576건 전부 초록**(맨몸 단독, 기록을 다 쓴 뒤 두 번 재서 13.447·13.517초) · 제품 `src/` 는
**주석 2곳만**(동작 0줄) · 새 파일 0 · 의존성 0 · 스키마·마이그레이션 없음 ·
`docs/specs/`·`data/crawl.db` 무변경 · `.mutation-lock` **0**.
`passage_eval` 정확도 **100.0%(398/398)** · 채택률 99.5% · p50 1.27ms · p95 **1.42ms**
(예산 500ms, rc 0 — 이번 반복에 다시 돌린 값이다).

## 이번 반복이 한 일 (리뷰 7)

**패스 A(백지)를 먼저 끝내고 계획·설계를 열었다.** 대상은 리뷰 6 이후의 diff
(개발 10 + 테스트 5 · `19317eb..81c3a5b` · `src/` 50줄 · `tests/` 158줄).

**세 가지를 남의 자로 다시 쟀다 — 셋 다 통과다.**

**① 19행 재측이 독립 탐침에서 글자 그대로 재현된다.** `passages()` 를 안 부르고 고르는
루프만 스크래치패드에서 다시 써서 쟀다: **점수만 11/19 · 「길이」 15/19 · 「이름표」 12/19.**
개발 10 과 리뷰 6 이 적은 숫자와 같다. 현행 규칙이 틀리는 4행도 `_UNMATCHED_SHAPES` 4행과
정확히 같고, 거기 적힌 «오늘의 오답» 문자열이 내 탐침 출력과 **4/4 일치**한다 —
**리뷰 5 가 「길이」를 반려한 반례 셋은 표에서 안 사라졌다.**
**새 사실 하나 — 겹쳐도 안 맞는다**: `(점수, p인가, 길이)` **13/19** ·
`(점수, 길이, p인가)` **15/19**. 뒤엣것이 「길이」와 같다는 것은 **이름표가 3순위로는 한 번도
안 걸린다**는 뜻이다. «조합을 안 써 봐서 그렇다» 는 다음 반복의 유혹을 여기서 닫는다.

**② `_UNMATCHED_SHAPES` 가드는 양방향이다 — 「나빠져도 초록」인 구멍이 아니다.**
`assertEqual(got, today)` 가 오늘의 오답을 **문자열로** 못박으므로 규칙이 좋아지면 그 행이
빨개지고, 조용히 **다른** 오답으로 바뀌어도 빨개진다. 실측으로 봤다 — 동점 키를 「이름표」로
바꾸자 `_SHAPES` 6건 + **`_UNMATCHED_SHAPES` 3건**이 함께 죽어 총 9건이다.

**③ 테스트 5 가 심은 검사 3건이 각각 자기 변이를 죽인다.** 다섯을 따로 심어 전수로 확인했다.

| 변이 | 판정 | 죽인 자리 |
|---|---|---|
| `extract.py` `del self._open[i:]` → `[i]` | **사망(1)** | `test_closing_an_outer_tag_also_sweeps_...` |
| `serve.py` `"title": title` → `""` | **사망(1)** | `test_title_is_the_document_title_not_a_placeholder` |
| `indexer.py` 2-gram 바늘 삭제 | **사망(1)** | `test_a_spacing_variant_document_still_gets_a_passage` |
| `indexer.py` 동점 키 → 「이름표」 | **사망(9)** | `_SHAPES` 6 + `_UNMATCHED_SHAPES` 3 |
| `extract.py` `_open[-1]` → `_open[0]` | **사망(4)** | 이름표 검사 4건 |

전부 `.mutation-lock` 을 걸고 제자리에서 심었고, 원복은 초록이 아니라
**`git diff -- src/` 가 빈 것**으로 확인했다. **커밋된 변이 0.**

## 찾은 것 — [R7-1] 이름표를 읽는 소비자가 하나도 없다

**medium · 신뢰도 95.** `extract_blocks` 가 내는 `(태그, 텍스트)` 의 **첫 원소를 읽는 제품
코드가 0곳**이다: 소비자는 `indexer.passages`(`_tag` 로 버린다)와 `e2e/passage_eval.py`
(`len()` 만) 둘뿐이고, `/passages` 응답 스키마는 `{url,title,position,text}` 라 태그 필드가
아예 없다. 위 ①의 «3순위로도 안 걸린다» 가 여기 붙는다 — 지금 안 읽힐 뿐 아니라
**이 표 위에서는 보탤 것도 0** 이다. 유지비는 `_open` 스택 · `handle_endtag` ·
`_flush` 의 이름표 줄 · 검사 3건이다.

**지우지 않았다.** 갈림길 6 이 실물 코퍼스로 열려 있고, 이름표를 지우면 그 갈림길을 재는
자에서 축 하나가 사라진다 — 계획 48 안에서 되돌릴 일이 아니라 **그 갈림길과 같은 문으로
닫힐 일**이다. `digest.md ## 다음 계획 후보` 에 **`[6]`** 으로 등재했다.

**대신 이 diff 가 들여온 거짓말 한 문장은 고쳤다.** `extract_blocks` docstring 이
*"소비자가 «이 텍스트가 무엇이었나» 를 볼 수 있게 같이 낸다"* 고 적고 있었는데 **그 소비자가
없다.** 그 문장을 실측으로 바꿨고, `indexer.py` 동점 키 주석에는 위 조합 측정(13/19 ·
15/19)을 한 줄 더했다. **동작은 0줄** — 단위 576건과 `passage_eval` 을 고친 뒤 다시 돌렸다.

## 넘긴 것 — 갈림길 6 은 여전히 열려 있다

**닫는 조건은 갈림길 5 의 캡과 같다: 실물 크롤 코퍼스다.** 손잡이 셋(점수만·이름표·길이)
중 어느 것도 19행을 다 못 맞추고, **리뷰 7 이 둘을 겹친 것 둘도 못 맞추는 것을 쟀다**
(13/19 · 15/19). **19행 표도 내가 만든 자라 정답표가 아니다.**
`data/crawl.db` 는 2문서 × 66자, e2e 코퍼스는 한 문장짜리 `<p>` 뿐이라
«보일러플레이트가 `<p>` 로 싸이는 빈도» 를 저장소가 못 잰다.
**설계서 갈림길 6 은 열린 채로 두고, 오늘의 선택과 그 이유를 거기 적었다.**
**리뷰는 여기서 닫힌다** — 손잡이를 더 돌려 보는 것은 계획 48 이 아니라 다음 계획이다.

**범위는 그대로 못박혀 있다** — 인라인 연접(`김치찌개된장찌개`) · 갈림길 5 의 캡 ·
`_SKIP_TAGS` 를 색인 경로에서 빼는 것 · [R7-1] 이름표 삭제는 **계획 48 에서 다시 열지
않는다**(`digest` 후보). 다음은 **e2e 20종 전수**이고, 통과하면 계획 48 을 DONE 으로 닫는다.

## 기점 — 원격 갈라짐은 닫혀 있다 (2026-09-02 확인)

`origin/main` 은 PR #6 으로 계획 44~47 까지 받았다. 계획 48 의 커밋들은 브랜치에만 있다.
**이번 반복도 `origin/loop/passage-api` 로 푸시했다** — 푸시는 루프의 정상 절차다
(강제 푸시만 금지).
**남은 것 (사람 결정)**: 계획 48 이 끝난 뒤 `main` 으로 보내는 PR.
무인 모드는 병합하지 않는다 — **이 반복도 PR 을 안 열었다.**
**웹 UI 의 *Update branch* 는 여전히 누르지 않는다** — `main` 이 rebase-merge 로 해시가
새로 쓰인 뒤의 그것은 머지가 아니라 복제다(PR #3 을 깨뜨린 원인).

## 사람이 정할 것 — 넷이 열려 있다

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
   `NO_PAIR` 사유가 판단 보류를 적어 두고 있다.
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1). 그 배경은 `outline-offset` 이
   0 일 때만 링의 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **회전 규약이 저장소 밖 룰 파일과 갈린다** (리뷰 [B1], 2026-09-02). `digest.md` 는
   *"`## 완료` 첫 줄의 명부는 회전이 지우지 않는다"* 를 자기 주석에 적고 검사로
   강제하는데, `~/.claude/skills/loop-harness/rules/docs.md` 에는 그 예외가 없다.
   **저장소 밖이라 루프가 못 고친다.**
4. **«러너 호출을 다른 것과 섞지 않는다» 를 문장이 아니라 훅으로 옮기는 일.**
   **재발 27회에서 안 늘었다 — 리뷰 7 은 안 밟았다**(러너 호출 8회 = 전수 7 + `passage_eval` 1,
   전부 맨몸·단독. 변이는 `python3 -B`). 직전 세 반복이 각각 한 번씩 밟았으니 **넷 만에
   처음 안 밟았다 — 한 번으로는 추세가 아니다.** 아래 직전 반복의 기록을 그대로 남겨 둔다.
   **직전(테스트 5)은 재발 26 → 27회였다.** 변이 다섯의 판정은 전부 맨몸·단독으로
   돌려 놓고, **마지막 전수 확인에서** `2>&1 | tail -4` 를 붙였다. 판정 줄이 잘려 화면에
   크롤 로그 네 줄만 남았고, 즉시 맨몸으로 다시 돌려 `Ran 576 tests ... OK` 를 읽었다.
   **방아쇠는 27번째도 「출력이 길다」다** — 직전 반복이 밟은 자리를 알고도 같은 자리에서
   같은 손이 나왔다. 크롤 로그가 화면을 덮는 것이 이 스위트의 상수고, 그것을 가리려는
   손이 매번 판정 줄까지 같이 가린다. **문장으로는 스물일곱 번 못 막았다.**
   처방은 이미 판정해 뒀다: `unittest` 의 **`-b`** 가 방아쇠를 뿌리에서 없앤다
   (실측 약 90줄 → 5줄, 거른 테스트 0, 새 코드 0). `digest.md ## 다음 계획 후보` 에
   **`[9]`** 로 등재돼 있다. 단서 — `-b` 는 테스트가 직접 낸 stdout/stderr 만 버퍼링하므로
   하위 프로세스가 fd 로 직접 쓰는 로그는 샌다. 판정 명령을 단독 실행으로 강제하는
   PreToolUse 검사는 여전히 **저장소 밖 몫**이다.

**사양이 사람에게 남긴 것 셋** (`docs/specs/concept.md` 의 `## 사람이 정할 것`):
속도 제한 시점(가정 — IP 당 분당 60회) · JSON 검색과 근거 문단의 분할(가정 — 쪼갠다,
**계획 46 이 그 가정을 따랐고 닫았다**) · 사양 숫자들이 초안이라는 것(90% · 500ms ·
60회/분). 첫째와 셋째는 아직 열려 있다.
