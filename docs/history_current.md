# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**회전 명부는 `digest.md` 의 `## 완료` 절 «아카이브 명부» 줄이 정본이다.**
여기 있던 스물한 회전의 서술(233줄)은 그 줄과 내용이 겹쳤고, 검사가 강제하는 명부도
그쪽 하나뿐이라(`tests/test_docs.py` 의 `ArchiveIndexTest`) **개발 9(반복 269)가 이
자리에서 지웠다** — 회전으로는 300줄 상한을 못 맞추던 세 반복(309 → 372 → 418줄)의
원인이 이 명부였다. **지운 것은 머리말이지 반복 기록이 아니다** — 항목은 여전히
append 전용이고 수정·삭제 금지다. 각 회전의 사유는 `digest.md` 의 같은 줄에, 원문은
`history_<NNN>.md` 에 그대로 있다.

## 2026-09-02 18:30 | passage-api | 개발 8 | 시도1

- 한 일: **리뷰 4 가 연 갈림길 6 을 닫았다 — 손잡이보다 «잴 자» 를 먼저 만들었다.**
  제품 `src/` 두 곳(`extract.py` 경계 인자 하나 · `indexer.py` 정렬 키 하나, 합 +24/-12행).
  `tests/test_extract.py`(+2건) · `tests/test_indexer.py`(+2건, 그중 하나가 여덟 모양
  골드 표) · `e2e/passage_eval.py`(문서주석) · `docs/design_passage-api.md`(갈림길 6 결정) ·
  `README.md`(565 → 569).
- **RED 를 먼저 봤다.** 코드보다 표를 먼저 썼다 — `tests/test_indexer.py` 의 `_SHAPES`
  여덟 행(모양마다 «사람이 인용할 문단» 을 적는다). 심자마자 **8행 중 5행이 빨갛다**
  (nav 보일러플레이트 · 제목이 질의어 그 자체 · `<br>` 로 줄만 나눈 문단 · `<br>` 문단이
  둘 중 하나 · 표 칸이 질의어 그 자체). `test_extract.py` 의 `<br>` 두 건도 빨갛다.
  **고치기 전 3/8 을 내 눈으로 읽었다.**
- **자가 안을 골랐다 — 세 안을 표에 태워 잰 값이다.** 현행 **3/8** · 안 1(동점 키에 길이)
  **6/8** · 안 2(`br` 만 경계에서 빼기) **5/8** · 안 3(블록 최소 길이 20자) **4/8** ·
  **안 1+2 = 8/8**. 둘은 서로 다른 결함을 닫으므로 **둘 다** 넣었다.
  **안 3 은 재고 나니 적극적으로 나쁘다** — 코퍼스가 통째로 그 모양인 «한 문장 `<p>`» 와
  «목록 항목이 진짜 답인 문서» 를 같이 버린다. 자를 안 만들었으면 이것을 골랐을 수도 있다.
- **자를 e2e 코퍼스가 아니라 단위 스위트에 뒀다.** 리뷰 4 는 «`<br>`·`nav`·목록이 든
  fixture 가 `passage_eval` 에 먼저 들어가야 한다» 고 넘겼는데, 코퍼스 JSON 은 계획의
  «안 고치는 것» 이라 못 건드리고 **e2e 는 e2e phase 에만 돌지만 단위는 매 반복 돈다**.
  결함을 네 반복 놓친 원인이 «축을 안 재는 자» 였으니 자는 자주 도는 자리에 둔다.
  `passage_eval.py` 의 문서주석이 이제 자기 눈먼 축 셋(`<br>` 0건 · `nav`/`ul`/`h*` 0건 ·
  블록 최소 길이 129자)을 적고 그 표를 가리킨다.
- **뿌리에서 고쳤다.** 부모 `_TextParser._separate(block=True)` 가 «여기가 **문단** 경계인가»
  를 넘기고 `_BlockParser` 만 그것을 읽는다 — 색인 경로는 인자를 안 읽는다.
  **실측으로 확인**(입력 4,128건 = 실물 128 · 퍼즈 4,000): 기점 `6e99031` 대비
  `extract_text` 불일치 **0건** · `is_noindex` 불일치 **0건**. 직전 커밋 `2e39590` 대비
  블록 출력이 달라진 문서 173건은 **전부 `br` 을 담고 있다**(무관한 변화 0건).
  불변식 `" ".join(blocks) == text` 위반은 628 → **617건**으로 줄었고 **새로 생긴 것 0건**이다.
- **변이 여섯 심어 여섯 다 죽었다.** Z1 `handle_starttag` 가 `<br>` 도 문단으로 넘긴다 → 4건 ·
  Z2 `handle_endtag` 쪽 → **1건** · Z3 `_BlockParser` 가 인자를 아예 안 읽는다(리뷰 4 로 회귀)
  → 4건 · Z4 경계 판정을 뒤집는다 → **23건**(`test_passage_eval` 의 G4·G6 가드까지 발화) ·
  Z5 동점 깨기를 없앤다 → 4건 · Z6 동점이면 짧은 쪽 → 4건.
  **Z2 가 1건뿐인 것이 새로 보인 것이다** — `<br/>` 자기폐쇄 표기는 `handle_startendtag` 가
  start 와 end 를 둘 다 부르므로 Z1 만으로도 `<br>` 은 잡히고, **endtag 자리를 따로 재는
  단언은 `<br/>`·`<br />` 한 건뿐**이었다. 두 자리를 각각 심어야 둘 다 재진다.
- 결과: **단위 569건 전부 통과**(565 + 4 · 회귀 0). `e2e` 문단 경로 5종 rc 0 —
  `passage_eval` 정확도 100.0%(398/398) · 채택률 99.5% · p95 **1.36~1.40ms**(실행 간 흔들림) ·
  `quality_eval` ko 20/20 · en 19/20 · `search_api_e2e` p95 2.11ms · `indexer_e2e` ·
  `design_check`. **수리 전후 숫자가 한 자리도 안 움직인다** — 그것이 이 반복의 핵심
  관측이다: 준비된 측정기는 이 축에 눈이 멀어 있고, 새 자만 3/8 → 8/8 로 움직였다.
- **닫지 않은 축을 적어 뒀다.** `<nav><a>김치찌개</a><a>된장찌개</a></nav>` 처럼 내비 링크가
  **둘 이상**이면 여전히 내비가 이긴다 — `<a>` 가 인라인이라 텍스트가 `'김치찌개된장찌개'` 로
  붙고 그 밀도가 본문을 **점수로** 이긴다. «블록 경계» 가 아니라 «보일러플레이트와 인라인
  연접» 이라 다른 축이고, 리뷰 4 가 올린 «안 보이는 텍스트가 근거로 나온다» 와 뿌리가 같다
  (`_SKIP_TAGS` 가 색인 경로와 공유 → 손대면 재색인). 설계서에 적고 `digest` 후보로 넘겼다.
- 실측: **러너 판정 호출을 이번 반복에 세 번 밟았다**(재발 19 → 22회). ① `design_check` 를
  `> /dev/null` 로 · ② 단위 러너에 `-k` 를 붙여 스위트를 걸러 · ③ **변이 Z3 아래에서
  `2>/dev/null`** — 셋 다 즉시 맨몸으로 다시 돌렸고 판정 줄을 눈으로 읽었다. ③ 은 잡음을
  줄이려던 손이고 `digest` 의 ⑮ 와 **형태가 글자 그대로 같다** — 그 항목을 읽고 나서
  같은 반복 안에서 다시 밟았다. **문장으로는 안 막힌다는 증거가 하나 더 늘었을 뿐이다.**
  변이 국면 러너 여덟 회 중 일곱이 맨몸·단독이었다. `.mutation-lock` 은 심을 때 만들고
  원복 뒤 지웠다 — 최종 **0**. 의존성 0 · 스키마·마이그레이션 없음 ·
  `docs/specs/`·`data/crawl.db` 무변경 · 새 파일 0.
- **`history_current.md` 가 두 반복 연속 상한을 못 맞춘다** — 회전은 개발 7 이 이미 돌렸고
  (`docs/history_041.md`) 남은 309줄 중 **머리의 회전 명부가 233줄**이라 항목을 다 밀어도
  300 아래로 안 내려간다. 룰이 append 전용이라 루프가 못 줄인다. `status.md` 의 «사람이
  정할 것» 5번에 그대로 둔다 — **고칠 사람이 정할 일이고, 여기서는 적고 지나간다.**
- 다음: **리뷰 5.** 볼 자리는 셋이다 — ① 골드 표가 «지금 동작» 을 베낀 것이 아니라 «사람이
  인용할 문단» 인가(내가 쓴 정답이라 내가 검토자가 아니다) · ② `block` 인자가 색인 경로에
  진짜로 안 닿는가(위 4,128건 밖의 반례) · ③ 동점 깨기가 만든 새 편향 — 길이를 선호하면
  **긴 보일러플레이트**(사이드바 통짜 블록)가 이길 자리가 있는가.

## 2026-09-02 19:05 | passage-api | 리뷰 5 | 시도0
- 한 일: 개발 8(`9ed460a`)의 diff 를 리뷰. **백지 패스를 먼저 끝냈다** — 계획서·설계서를 열기
  전에 diff 와 고친 두 파일(`src/websearch/extract.py`·`indexer.py`)만 보고 «같은 성질의
  형제가 더 있나» 하나만 물었다. 그것이 아래 둘을 냈고 둘 다 탐침으로 재현했다.
  **569건은 둘 다에 눈이 멀어 있다** — 이번 리뷰의 근거는 스위트가 아니라 실측이다.
  설계서 `docs/design_passage-api.md` 의 갈림길 6 에 «재개» 절을 붙이고, 개발 9 의 범위를
  거기서 못박았다. 제품 `src/` **0줄** · 테스트 0줄(RED 를 남기지 않는다 — 자를 고치는 것은
  개발 9 가 «먼저 RED 를 눈으로 본다» 의 재료이지 리뷰가 앞질러 심을 것이 아니다).
- 결과: **반려 — 계획 48 의 다섯 번째다. 새 축이 아니라 리뷰 4 가 연 축의 나머지 절반이다.**
  **[R5-1] high** — 안 2 는 «`_separate()` 를 블록 태그로 좁히기» 가 아니라 `<br>` 한 태그를
  뺀 것이다(`self._separate(tag != "br")`). `_separate()` 를 부르면서 문단 경계가 **아닌**
  태그는 `_INLINE_TAGS` 에서 빠진 phrasing 태그 전부와 `_SKIP_TAGS` 셋이 더 있다. 일곱 모양
  중 **여섯이 여전히 한 문단을 조각낸다** — `<script>`·`<style>`(문단 안 광고·트래킹) ·
  `<img>` · `<del>`/`<ins>`(넷으로 갈린다) · `<label>` · `<font>`. **개발 8 이 «고쳤다» 고
  적은 실패가 `<br>` 을 `<script>` 로 바꾸면 그대로 재현된다** — 내비 링크가 조각난 본문을
  이겨 근거로 나온다. 이 축은 **재색인 없이 닫힌다**: 설계서가 미룬 사유로 적은
  *"`_SKIP_TAGS` 는 색인과 공유라 손대면 재색인"* 은 `_INLINE_TAGS` 로 옮길 때만 참이고
  `block` 인자로는 거짓이다(`_TextParser._separate` 가 인자를 문법적으로 안 읽는 것이
  개발 8 이 4,128건으로 실측한 그 성질이다). 일곱 모양 전부 불변식
  `" ".join(extract_blocks(h)) == extract_text(h)[1]` **7/7 유지**.
  **[R5-2] high·회귀** — 안 1(동점이면 긴 쪽)이 **새 오답**을 만들었다. `status.md` 의
  재확인 3 이 물은 자리다. 본문 `<article>` **뒤에** 오는 긴 `<footer>`(질의어 1회)가
  동점에서 본문을 이긴다 — 같은 입력이 직전 커밋 `2e39590` 에서는 **맞았고** `9ed460a` 에서
  **틀려졌다**(기점 대조 실측). 사이드바가 **앞에** 오는 모양은 양쪽 다 틀리고 점수가
  갈리는 모양은 양쪽 다 맞으니, 안 1 은 이득이 아니라 **맞바꿈**이다. 원인은 손잡이가 아니라
  **자**다 — `_SHAPES` 여덟 행이 보일러플레이트를 **전부 본문 앞에** 둔다. 「길이」가 이득이
  되는 방향만 표에 있어 안 1 이 8/8 을 받았다. **개발 8 의 규율(자를 먼저)은 옳다** —
  이 반복이 결함을 잡은 이유이기도 하다. 틀린 것은 규율이 아니라 **표의 대칭성**이다.
  재확인 1(골드 표 6행의 정답 `'김치찌개'` 4자)은 **결함 아님으로 닫는다** — 그 문서에서
  점수가 붙은 블록이 그것 하나뿐이라 동점 규칙이 안 걸린다. 재확인 2(`block` 인자가 색인에
  닿나)도 **닫는다** — 위 7종 반례 전부 불변식 유지, 부모가 인자를 문법적으로 안 읽는다.
  재확인 4(인라인 연접)는 범위 밖 유지, 재확인 5(e2e 20종)는 e2e phase 몫.
- 실측: 러너 판정 호출 **1회 · 맨몸 단독** — `PYTHONPATH=src python3 -m unittest discover tests`
  → **569건 OK(13.420초)**. 재발 **22회에서 늘지 않았다**(직전 반복이 하나에서 셋을 밟은 뒤
  0 이다). 탐침 둘은 판정 러너가 아니라 별도 스크립트로 돌렸고 결과는 설계서 표에 있다.
  `.mutation-lock` **0**(만들지 않았다) · 의존성 0 · 스키마·마이그레이션 없음 · 새 파일 0 ·
  `docs/specs/`·`data/crawl.db` 무변경 · 제품 `src/` 0줄.
- **계획 48 을 끝낼 수 있나 — 끝낼 수 있다.** 저장소에서 가장 긴 계획이지만 미궁이 아니다:
  세 스텝은 전부 붙어 있고 사양의 두 숫자(90% · 500ms)는 이미 통과한다. 다섯 반려가 전부
  **같은 한 곳**(「블록이 문단인가」)으로 좁혀졌다. **개발 9 의 범위를 둘로 못박았다** —
  [R5-1] 태그 집합 · [R5-2] `_SHAPES` 에 방향을 짝지은 행. 인라인 연접 · 갈림길 5 의 캡 ·
  `_SKIP_TAGS` 를 색인에서 빼는 것은 **계획 48 에서 다시 열지 않는다**(`digest` 후보).
  **리뷰 6 은 그 둘의 재측만 하고 새 축을 열지 않는다.**
- 다음: **개발 9.** 개발 8 의 규율 그대로 — 위 두 표의 입력을 `_SHAPES` 에 먼저 심어 RED 를
  눈으로 본 뒤 고른다. 닫히면 **e2e 20종 전수로 보내 DONE 으로 닫는다.**

## 2026-09-02 19:20 | passage-api | 개발 9 | 시도1
- 한 일: **리뷰 5 가 못박은 둘만 닫았다 — 갈림길 6 이 닫혔다.** 제품 `src/` 두 곳
  (`extract.py` 의 `_NON_BLOCK_TAGS` 집합 + `_BlockParser` 가 블록마다 «자기를 낸 태그» 를
  붙인다 · `indexer.py` 의 동점 키 한 줄). `tests/test_extract.py`(+2건, 그중 하나가
  인라인 7모양 표) · `tests/test_indexer.py`(`_SHAPES` 8 → **11행** + 동점 단언 2건) ·
  `README.md`(569 → 571) · `docs/design_passage-api.md`(갈림길 6 결정) · `metrics.md` ·
  `index.md` · `status.md` · `digest.md`.
- **RED 를 먼저 봤다.** [R5-2] 의 방향 짝(뒤·긴 푸터 / 앞·긴 사이드바 / 긴 내비+문단 안
  `<script>`) 세 행을 `_SHAPES` 에 먼저 심었고 **3행이 즉시 빨갛다**. 인라인 7모양 표도
  먼저 심어 **7 중 6이 빨갛다**(script·style·img·del/ins·label·font). 고치기 전에 눈으로 읽었다.
- **표를 대칭으로 만들자 개발 8 이 고른 손잡이가 떨어졌다.** 11행 기준 실측:
  개발 8 그대로 **8/11** · [R5-1] 만 **6/11** · [R5-1]+길이(안 1 유지) **8/11** ·
  [R5-1]+`tag != "p"`(뒤집음) **5/11** · **[R5-1]+`tag == "p"` 채택 11/11**.
  안 1 은 여덟 행 표에서 6/8 을 받았는데 방향을 짝지으니 8/11 이다 — **자가 한쪽으로만
  눈금이 있었다는 리뷰 5 의 판정이 숫자로 확인된다.** 길이도 순서도 방향이 하나뿐이라
  네 방향을 동시에 못 지고, 그것을 가르는 이름표를 **HTML 이 이미 갖고 있다**(`<p>`).
  그래서 `extract_blocks()` 가 `(태그, 텍스트)` 를 내고 동점 키가 `(점수, 태그가 p 인가)` 다.
- **색인 경로는 한 글자도 안 바뀐다 — 다시 실측했다.** 기점 `6e99031` 대비 입력 **2,496건**
  (코퍼스 64문서 × 39모양 — 문단 안에 태그 33종을 하나씩 끼운 변주 + 안 닫힌 속성값·주석·
  `meta robots`): `extract_text` 불일치 **0건** · `is_noindex` **0건**. 불변식이 갈린 128건은
  **문서마다 정확히 둘**이고 둘 다 docstring 의 예외 ①(EOF 잔여 마크업)이다 — 정상 입력 0건.
- **변이 여섯 심어 여섯 다 죽었다.** N1(집합을 `{"br"}` 로 되돌린다) 7건 · N2(집합에 `p` 를
  섞는다) **23건** · N3(동점 규칙 제거) 6건 · N4(이름표 뒤집기) 7건 · N5(`>` → `>=`) **1건** ·
  N6(이름표를 부모 호출 **앞에서** 갈아 끼운다) **1건**. N5·N6 이 1건인 것은 그 자리만 재는
  단언이 각각 하나씩이기 때문이고, 없으면 아무도 못 본다 — 개발 8 의 Z2 관측과 같은 결이다.
- 결과: **단위 571건 전부 통과**(569 + 2 · 회귀 0, 13.4초). `e2e/passage_eval.py` rc 0 —
  정확도 **100.0%(398/398)** · 채택률 99.5% · p95 **1.38ms**. **e2e 20종 전수는 e2e phase 몫.**
- **`history_current.md` 를 300줄 아래로 되돌렸다(418 → 204줄).** 머리에 쌓여 있던 회전
  명부 233줄을 지웠다 — `digest.md` 의 `## 완료` «아카이브 명부» 줄과 내용이 겹치고
  **검사(`ArchiveIndexTest`)가 강제하는 것도 그쪽 하나뿐**이라 색인 구멍이 안 생긴다.
  **지운 것은 머리말이지 반복 기록이 아니다** — 항목은 여전히 append 전용·수정 삭제 금지고,
  그 규약을 두 파일의 머리 주석에 적었다. 세 반복 연속(309 → 372 → 418) «회전으로는 못
  맞춘다» 였던 것이 이것으로 닫힌다. **저장소 밖 룰 파일은 안 건드렸다** —
  `status.md` 의 «사람이 정할 것» 3번(회전 규약이 룰 파일과 갈린다)은 그대로 열려 있다.
- 실측: **러너 판정 호출을 이번 반복에 세 번 밟았다**(재발 22 → **25회**). ① 변이 국면 진입
  전 `2>/dev/null | tail -5` · ② 변이 N3 아래 `2>&1 | tail -40` · ③ 후보 측정에서
  `python3 -m unittest tests.test_indexer` 로 **모듈 하나만** 돌렸다(스위트 거르기).
  셋 다 맨몸·단독으로 다시 돌려 판정 줄을 눈으로 읽었다(①은 즉시, ②는 변이를 다시 심어
  571 중 6건 실패를 재확인, ③은 전체 스위트로 같은 3행 실패를 재확인). 방아쇠는 세 번 다
  **«출력이 길다»** 이고, 개발 8 이 세 번 밟은 그 자리다 — **두 반복 연속으로 셋씩이다.**
  이번 반복의 러너 호출은 열 번이고 그중 일곱이 맨몸·단독이었다. `.mutation-lock` 은 심을
  때 만들고 원복 뒤 지웠다 — 최종 **0**(`git status` 로 확인). 의존성 0 · 스키마·마이그레이션
  없음 · `docs/specs/`·`data/crawl.db` 무변경 · 새 파일 0 · 강제 푸시 없음.
- 다음: **리뷰 6.** 재측할 것은 둘뿐이다 — ① 태그 집합이 색인 경로에 안 닿는가(2,496건 밖의
  반례) · ② 이름표 동점 규칙이 11행 표 밖에서 새 편향을 만드는가(예: `<p>` 안에 든 광고
  문구가 진짜 문단을 이기는 자리). 닫히면 **e2e 20종 전수로 보내 DONE 으로 닫는다.**

## 2026-09-02 19:40 | passage-api | 리뷰 6 | 시도1
- 한 일: **리뷰 5 가 못박은 재측 둘만 했다. ①은 닫혔고 ②에서 반례가 나왔다 — 계획 48 의
  여섯 번째 반려다.** 새 축은 안 열었다(인라인 연접·갈림길 5 의 캡·`_SKIP_TAGS` 는 그대로
  범위 밖). 제품 `src/` **0줄** · 테스트 0건 · 새 파일 0 · 의존성 0.
- **패스 A(백지)를 먼저 끝냈다** — `6e09e1f` 의 diff 와 고친 두 파일만 열고 계획·설계는
  그 뒤에 열었다. 백지에서 나온 것이 아래 [R6-2] 이고, [R6-1] 은 패스 B 의 재측이 냈다.
- **[R6-1] high·회귀 — `key = (score, tag == "p")` 가 실물에서 가장 흔한 동점 모양을 통째로
  틀린다.** 11행 표는 **정답이 늘 `<p>` 이고 경쟁자는 한 번도 `<p>` 가 아니다** — `<p>` 대
  `<p>` 동점 행이 **0개**다. 방향을 짝지은 8행(정답이 `<div>`·`<li>`·`<td>`·`<section>`·
  `<blockquote>` 이거나 보일러플레이트가 `<p>` 인 것)을 세워 같은 자로 다시 쟀다:
  채택안이 **새 8행에서 1/8**, 합친 19행에서 **12/19** 로 **철회된 「길이」(15/19) 밑**이고
  대조군으로 세웠던 「뒤집음」(11/19) 바로 위다. **개발 9 가 적은 왼쪽 열 넷(6/11 · 8/11 ·
  11/11 · 5/11)은 내 탐침에서 글자 그대로 재현됐다** — 저울이 아니라 자를 바꾼 것이 맞다.
  뿌리는 **이름표가 «최내곽 태그»** 라는 것이다: `<footer><p>ⓒ…</p></footer>` 의 이름표가
  `p` 이고 `<aside><p>인기글</p></aside>` 도 `p` 다(실측) — **이름표는 컨테이너를 아예 못
  본다.** 게다가 `<p>` 대 `<p>` 동점은 `test_a_tie_between_two_paragraphs_goes_to_the_earlier_one`
  이 «앞이 이긴다» 로 **계약으로 굳혀** 두었고, 그 자리가 곧 본문 앞의 광고 문구·빵부스러기다.
  **원인이 리뷰 5 와 글자 그대로 같다** — 개발 9 는 «앞/뒤»·«길다/짧다» 는 짝지었지만
  **«정답이 `<p>` 인가» 는 안 짝지었고, 새 규칙이 읽는 축이 하필 그것**이다. 자체로는
  medium 이고 **같은 원인의 재발이라 high**(`severity.md` 5절).
- **[R6-2] low — 이름표가 새는 자리 둘** (백지 패스). `_BlockParser.handle_starttag` 은
  «부모가 먼저 이전 블록을 끊는다» 를 전제로 이름표를 부모 호출 **뒤에** 갈아 끼우는데,
  `title` 은 부모가 **안 끊고 early-return** 한다 → `<p>김치찌개 하나<title>제목</title>`
  이 `[('title', '김치찌개 하나')]` 다(`<p>` 안이었는데 이름표가 `title`). 그리고 `_tag` 는
  닫는 태그에서 **복원되지 않아** `<article><p>본문</p>꼬리 텍스트</article>` 의 꼬리가
  `('p', '꼬리 텍스트')` 다 — `<p>` 밖 텍스트가 `p` 이름표를 얻는다(동점을 **이기는** 방향의
  거짓양성이다). 둘 다 깨진·느슨한 HTML 에서만 나고 지금은 오답을 못 만들지만, 개발 10 이
  이 자리를 다시 만지므로 같이 적는다.
- **재측 ① 은 닫는다 — 태그 집합은 색인 경로에 안 닿는다.** 2,496건 **밖**에서 **8,844건**을
  기점 `6e99031` 과 문자 단위 대조: [A] 태그 31 × 모양 10(자기닫힘·**대문자**·감쌈·
  `data-y="a > b"`·안 닫힘·짝 없는 닫는 태그·**블록을 감쌈**·**자기 중첩**) × 틀 10 =
  3,100건 → 불일치 0 · [B] 코퍼스 8문서 얹기 744건 → 0 · [C] 무작위 5,000건(seed 48) → 0.
  `is_noindex` 도 전부 0. **샘플보다 강한 근거가 따로 있다** — `_TextParser._separate` 의
  본문은 `target.append(" ")` 두 줄이라 `block` 인자를 **문법적으로 안 읽는다.**
- **어느 안도 19행을 다 못 맞춘다(최고 15/19). 그래서 결론이 «다른 손잡이» 가 아니다** —
  이름표는 컨테이너를 못 보고, 길이·순서는 방향이 하나뿐이고, 셋을 겹쳐도 15/19 다.
  **닫는 조건은 갈림길 5 의 캡과 같다: 실물이다.** `data/crawl.db` 는 2문서 × 66자, e2e
  코퍼스는 한 문장짜리 `<p>` 뿐이라 «보일러플레이트가 `<p>` 로 싸이는 빈도» 를 저장소가 못
  잰다. **19행 표도 내가 만든 자라 정답표가 아니다** — «둘 다 한쪽으로 기운다» 의 증거다.
- **개발 10 의 범위를 둘로 못박는다.** ㉠ 동점 키를 「길이」로 되돌리고(15/19) 못 맞추는 행을
  표에 «못 맞춤» 으로 **분리해** 남긴 뒤 갈림길 6 을 실물까지 열어 둔다 · ㉡ 동점 키를 아예
  안 쓴다(9/19). **어느 쪽이든 새 손잡이는 안 만든다.** `_NON_BLOCK_TAGS` 와 `(태그, 텍스트)`
  계약은 **남긴다** — 재측 ①·인라인 7모양 표가 각각 값을 증명했다. 떨어지는 것은 **한 줄**이다.
- 실측: 러너 판정 호출 **2회 · 둘 다 맨몸 단독 · 전체 스위트** — `PYTHONPATH=src python3 -B -m
  unittest discover -s tests` → **571건 OK(13.432초)**. 재발 **25회에서 늘지 않았다**(개발 9 가
  셋을 밟은 뒤 0). 두 번째 호출은 아래 후보의 실측이고 `-b` 만 붙였다 — 테스트를 거르지도
  판정 줄을 가리지도 않으므로 이 항목에 안 센다. 탐침 둘은 판정 러너가 아니라 별도 스크립트다.
  `.mutation-lock` **0**(만들지 않았다) · 제품 `src/` **0줄** · 의존성 0 · 스키마·마이그레이션
  없음 · 새 파일 0 · `docs/specs/`·`data/crawl.db` 무변경 · 강제 푸시 없음.
- **재발 25회짜리 항목의 처방 후보를 판정했다 — 제안보다 위칸이 있다.** 개발 9 가 낸 것은
  «판정 줄을 마지막에 다시 찍는 얇은 래퍼» 였다. 진단은 옳다(방아쇠가 세 번 연속 «출력이
  길다»). 그런데 그 래퍼는 잡음을 **안 줄이고** 판정만 한 번 더 찍는다 — «가릴 이유» 는
  그대로 둔 채 «가려도 보인다» 를 만드는 처방이다. `unittest` 에 **이미 있는 `-b`** 가 방아쇠를
  뿌리에서 없앤다: 테스트의 stdout/stderr 를 버퍼링해 **실패한 것만** 낸다. 실측했다 —
  같은 571건이 **약 90줄 → 5줄**, `Ran 571 tests in 13.586s / OK` 그대로, 거른 테스트 0.
  **새 파일 0 · 새 코드 0 · 명령 문자열에 두 글자**다. `digest.md ## 다음 계획 후보` 에
  **`[9]`** 로 등재했다(대괄호는 순번이 아니라 **우선도 점수**다 — 이번 스텝에서 만들지
  않는다. 범위 밖이고 `project.md`·README 의 명령 줄을 건드리는 일이다).
  **단서를 함께 적었다**: `-b` 는 테스트가 직접 낸 stdout/stderr 만 버퍼링하므로 하위
  프로세스·다른 스레드가 fd 로 직접 쓰는 로그는 그대로 샌다. 그 계획이 열릴 때 잴 것이다.
- **회전을 돌렸다** — 이 항목을 붙이니 **312줄**이라 그 자리에서 리뷰 4 를
  `docs/history_042.md` 로 밀었다(**246줄**). 스물두 회전 연속 지각 0. 개발 8 은 남긴다 —
  개발 10 이 되돌릴 「길이」 규칙과 여덟 행 표를 만든 반복이고, 리뷰 5·6 이 둘 다 그것을
  판정한다. 명부는 `digest.md ## 완료` 의 «아카이브 명부» 줄에 더했다(새 색인 구멍 0).
- **`digest.md` 가 상한을 넘어 있었다 — 207 → 200줄.** 회전 명부·후보 등재를 붙이고 세니
  207 이었는데, `git show HEAD:docs/digest.md | wc -l` 이 **204** 라 내 편집 전에 이미
  넘어 있었다(개발 9 가 붙일 때 안 셌다). 룰대로 **오래된 완료 항목부터 일곱 줄**을 지웠다
  (`normalize-gaps(019)`·`number-flag(25)`·`crawl-max-guard(26)`·`crawl-unknown-arg(27)`·
  `readme-perf-audit(30)`·`port-zero-cover(31)`·`clock-injection(33)`). 일곱 줄 전부
  `history_` 이름을 안 담은 것으로 골랐으므로 **아카이브 명부와 `ArchiveIndexTest` 는
  무손상**이고, 원본은 각 `history_<NNN>.md` 와 `index.md` 의 계획 명부에 그대로 있다.
  보류·반복 실패·명부 줄은 안 건드렸다.
- **판정: 반려.** 승인 필요 1건(high)이 있으므로 phase 를 **개발**로, step 을 **10** 으로
  되돌린다. 자동 수정 1건(low)은 같은 자리를 만지므로 개발 10 이 함께 처리한다.
- 다음: **개발 10.** 위 ㉠·㉡ 중 하나. 닫히면 **e2e 20종 전수로 보내 DONE 으로 닫는다.**

## 2026-09-02 20:12 | passage-api | 개발 10 | 시도1

- 한 일: **리뷰 6 이 못박은 둘만 닫았다.** 제품 `src/` 두 곳(`extract.py` 의 `_tag` 필드 →
  `_open` 스택 · `indexer.py` 의 동점 키 한 줄, 합 +50/-21행) · `tests/test_extract.py`(+1건) ·
  `tests/test_indexer.py`(표 8 → 15행, `_UNMATCHED_SHAPES` 4행, +1건) · `README.md`(571 → 573).
  새 파일 0 · 새 손잡이 0 · `_NON_BLOCK_TAGS` 와 `(태그, 텍스트)` 계약은 남겼다.
- **[R6-1] ㉠ 을 골랐다 — 동점 키를 `(점수, 길이)` 로 되돌렸다.** 리뷰의 19행 재측을 내
  탐침으로 독립 재현했다: **점수만 11/19 · 이름표 12/19 · 길이 15/19** (㉡ 은 9/19).
  근거는 «더 낫다» 가 아니라 **«덜 틀린다»** 다. 처음 재니 이름표가 15/19 로 나와 길이와
  동률이었는데, 행마다 블록 점수를 찍어 보니 **새로 더한 여덟 중 넷이 동점에 도달하지도
  못하고 점수(5 대 4)에서 갈리고 있었다** — 질의어를 중복으로 심은 내 탓이다. 그것을
  걷어내 여덟을 진짜 동점으로 만드니 리뷰 숫자와 **글자 그대로** 같아졌다.
  **자를 의심하기 전에 자기 표부터 의심하는 자리가 여기다.**
- **못 맞추는 넷을 숨기지 않고 검사로 박았다.** 어떤 후보도 못 맞추는 4행(본문 뒤 긴 푸터 ·
  본문 앞 긴 사이드바 · 긴 내비 + 문단 안 `<script>` · 짧은 `<blockquote>` 뒤 긴 저작권 `<p>`)을
  `_UNMATCHED_SHAPES` 로 옮기고 **`assertNotEqual(want, today)`** 를 걸었다 — 오늘의 오답을
  계약으로 박은 것이라 **맞게 되면 검사가 빨개져** 다음 사람이 표를 갱신한다.
- **[R6-2] 증상 둘을 뿌리 하나로 닫았다.** `_tag` **필드 하나**가 «지금 열린 블록» 을
  흉내내고 있었다. `_open` **스택**으로 바꾸니 셋이 한자리에서 닫혔다 — ① `<title>` 은 부모가
  안 끊으므로 스택도 안 건드린다 ② 닫는 태그가 주인을 바깥으로 되돌린다 ③ **몰랐던 셋째**,
  같은 이름이 겹치면 **안쪽부터** 닫는다(`rindex`).
- **변이 일곱 — 여섯 즉사, 하나 생존 → 검사를 늘려 죽였다.** A 닫는 태그 pop 삭제 사망 ·
  B `_in_title` 가드 삭제 사망 · C `_open[-1]` → `[0]` 3건 사망 · **D `rindex` → `index` 생존** →
  중첩 `<div>` 단언을 더해 사망 · E 동점 키 삭제 9건 사망 · F `>` → `>=` 1건 사망 ·
  G 동점을 짧은 쪽으로 뒤집기 15건 사망. **D 가 이번 반복의 값이다** — 생존을 «천장» 으로
  적고 넘어갈 수 있었지만 그 입력이 실물 HTML 의 기본형(중첩 `<div>`)이었다.
  `PYTHONDONTWRITEBYTECODE=1` 을 붙여 돌렸다(묵은 `.pyc` 가 변이를 되돌려 보이게 한다).
- 결과: **단위 573건 전부 통과**(맨몸 단독, 13.5초) · `passage_eval` **정확도 100.0%(398/398) ·
  채택률 99.5% · p95 1.46ms**(예산 500ms, rc 0) · `.mutation-lock` **최종 0** · 의존성 0 ·
  스키마·마이그레이션 없음 · `docs/specs/`·`data/crawl.db` 무변경 · 강제 푸시 없음 · PR 없음.
- **러너 규율을 한 번 밟았다 — 재발 25 → 26회.** 변이 E 판정에 `| tail -5` 와 `2>/dev/null`
  을 붙였고, 판정 줄이 **stderr** 라 화면에 아무 판정도 안 나왔다. 즉시 맨몸·단독으로 다시
  돌려 `FAILED (failures=9)` 를 읽었다. **방아쇠는 26번째도 「출력이 길다」** 다 —
  크롤 로그가 화면을 덮는 것이 이 스위트의 상수고, 가리려는 손이 매번 판정 줄까지 가린다.
  처방(`unittest -b`)은 `digest.md ## 다음 계획 후보` 에 `[9]` 로 이미 등재돼 있다.
- 다음: **테스트 5.** 갈림길 6 은 설계서에 **열린 채로** 남는다 — 닫는 조건은 실물 크롤
  코퍼스다. 테스트·리뷰를 지나 **e2e 20종 전수로 보내 DONE 으로 닫는다.**
