---
signal: GREEN
phase: 탐색
step: 0/0
attempt: 0
iteration: 463
updated: 2026-09-08
ctx: 29
night_iterations: 3
night_red: 0
night_retries: 0
plan: null
---

## 탐색 결과 — 8순위 전수 + 컨셉 판정기 전수, 착수 가능한 근거 0 (반복 463)

**세 반복 연속 0이다** (461·462·463). 461·462 는 저장소 기록을, 463 은 **판정기를**
훑었다 — 「기록이 초록이라 말한다」와 「지금 돌려도 초록이다」는 다른 질문이라
후자를 실제로 돌렸다. **다섯 자 전부 초록이고 숫자가 기준선과 같거나 낫다:**

| 자 | 결과 | 기준선 대비 |
|---|---|---|
| 단위 전수 | **675 OK** rc 0 (16.9s) | 그대로 |
| `design_check.py` | rc 0 · 4축 · 대비 최저 3.56:1(`--focus`) | 그대로 |
| `quality_eval.py` | rc 0 · ko **20/20** · en **19/20** · 매치 평균 14.0 | 그대로 (미검출은 `loaf`←`loaves` 하나) |
| `passage_eval.py` | rc 0 · 정확도 **100.0%** · 채택 99.5% · p95 **1.67ms** | p95 1.62 → 1.67ms (예산 500 의 0.3%) |
| `perf_search.py` | rc 0 · p95 **8.71ms** | 9.11 → **8.71ms** (예산 300 의 2.9%) |

- 1순위 0 (전수 675 OK) · 2순위 없음(린터 없음) · 3순위 **0** (`src`·`tests`·`e2e` 전수
  검색의 유일한 히트 `tests/test_indexer.py:824` 는 HTML 주석 **픽스처 문자열**이다)
- 4순위 없음(`candidates.md` 부재) · 5순위 없음(보류 절 비어 있음) · 8순위 0(`gh issue list` 빈손)
- 6순위 열린 항목 전수: 여는 조건 미충족 또는 승인 게이트. `[7]` 비ASCII robots 는
  「`robots.py` 를 직접 부르는 새 호출처」가 조건인데 오늘 0곳 · `[7]` 숨은 텍스트는
  남은 축이 `_SKIP_TAGS`·`_INLINE_TAGS` 라 **재색인 = 승인 게이트** · 나머지는 `[4]`~`[6]`
- 7순위 반복 실패: 열린 셋 중 둘은 *"세 번째가 나면 규율이 아니라 계획이다"* 로
  스스로 조건을 적어 뒀고 오늘 세 번째가 안 났다

## 기록 결함 1건 — 착수하지 않고 넘긴다

`digest.md` 의 `## 반복 실패` 절 「회전을 돌리면서 그 아카이브의 `digest` 압축 줄을
안 남긴다」(5개)는
**처방이 이미 들어가 있는데 취소선이 안 그어졌다** — `tests/test_docs.py:768`
`ArchiveIndexTest` 가 `docs/history_[0-9]*.md` 전부를 `## 완료` 명부와 대조하고
오늘 전수 675 안에서 초록이다. **탐색이 이 줄에서 시간을 쓴다**(이번 반복이 실제로 썼다).

**고치지 않은 이유는 규칙이다** — 짧은 경로 **연속 3건**이면 멈추고 근거의 출처를 보는데
(461 `like-scan-verdict` · 462 `recrawl-blocker-remeasure` · 이번이 셋째), 셋 다 출처가
**「저장소가 자기에 대해 적어 둔 기록」** 으로 같다. 같은 출처 3연속은 탐색이 막혔다는
신호라 정지 쪽이다. 아침에 한 줄 취소선이면 끝난다.

## 사람 결정 대기 — 재크롤을 열지 말지 (착수 안 함)

스키마·마이그레이션 축은 **0** 이 됐지만(반복 462 실측), 남은 것은 사양이 숫자를 안 준
**정책 셋**이다: ① 실패 상태(4xx·5xx·`status 0`)도 다시 받나 ② 404·410 의 삭제 반영이
`docs` 행만인가 `pages` 행까지인가(후자는 데이터 삭제라 야간 금지) ③ 재크롤이 링크를
다시 추출해 프런티어를 넓히나(크롤 예산·간격 축). **야간은 넘겨짚지 않는다.**

## 사람 결정 대기 — 속도 제한을 지금 넣나 (착수 안 함)

`docs/specs/concept.md` 「사람이 정할 것」 1번. 사양이 **숫자는 줬다**(가정: IP 당 분당
60회) 하지만 **시점은 안 줬고** 절 머리가 *"루프가 혼자 못 여는 셋"* 이다. 지금 공개
API(`/search`·`/passages`)에 제한이 0인 것은 맞고, 컨셉이 남용 대응을 인증이 아니라
속도 제한으로 못 박아 뒀으니 **처방은 이미 정해져 있다** — 남은 것은 착수 시점뿐이다.

## 다음

**우물이 바닥이다.** 남은 근거는 전부 사람 판단이고(재크롤 정책 셋 · 재색인 승인
게이트 · 속도 제한 시점), 자는 다섯 개가 전부 초록이다. 하나를 열어 주면 이어간다.
