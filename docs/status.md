---
signal: GREEN
phase: 리뷰
step: 2/2
attempt: 0
iteration: 391
updated: 2026-09-06
ctx: 47
night_iterations: 190
night_red: 2
night_retries: 4
plan: spec-citation-address 계획 67 (리뷰 phase 완료 · 다음은 e2e phase)
---

## 현재 상태

**계획 67 `spec-citation-address` 의 리뷰를 닫았다 — 지적 4건 중 1건(8점)을 고쳤다.**
정적 판독으로 다섯 자리를 봤다(변이 0판 · 전수 1회 · 코드를 고쳐서 돌린 것이다).
전수 **627건 · `OK`** — 단언 무변(하한 상수 하나)이라 `README.md:104` 무접촉.

## 이번 스텝이 한 일

**[R67-1] 8점 · 고쳤다 · `tests/test_docs.py:275`.** 하한 `MIN_CHECKS = 5` 가 **값 축의
크기(5)와 정확히 같다** — 문구 추출기(`PHRASE`)만 죽는 날 대조가 7→5 로 줄어도
`>= 5` 라 **조용히 초록**이다. 하한이 막으라고 있는 바로 그 침묵인데(그 줄 주석이
「추출기가 깨지면 0건 대조 위에서 조용히 초록이 된다」고 적어 뒀다) 한 축의 죽음은
못 문다. 앞 스텝이 같은 주석의 「값 7」을 실측 **5** 로 고치면서 하한이 한 축에 딱
붙어 버린 자리다. **6 으로 올렸다** — 두 축 중 하나가 통째로 죽으면(문구 −2 → 5 · 값
−5 → 2) 이제 둘 다 하한에서 죽고, 인용 하나가 정당하게 빠지는 여유 1건은 남는다
(`MIN_HITS` 14 vs 실측 18 과 같은 모양이다).

**[R67-2] 5점 · 안 고쳤다.** `_has_number` 의 전방탐색이 `(?![0-9])` 라 **소수점을
안 막는다** — 후방탐색 `(?<![0-9.])` 과 비대칭이라 상수 `5` 가 사양의 `5.5` 앞자리에
붙을 수 있다. 오늘 실물은 0건이다: 사양의 소수 표기는 `1.5s`·`4.5:1` 둘뿐이고 값 대조가
붙는 상수 다섯(`TOP_N` 10 · `TARGET_RATE` 5.0 · `JS_BUDGET` 50·1024 · `MIN_CONTRAST` 4.5 ·
`MOBILE_WIDTH` 360) 중 그 앞자리와 겹치는 것이 없다. `(?![0-9.])` 로 좁히면 문장 끝
마침표 뒤의 수를 거짓 RED 로 만드는 **반대편 오탐**이 생겨 거래가 대등하지 않다.

**[R67-3] 4점 · 안 고쳤다.** 쉼표 낀 표기(`concept.md:30` 「1,000만 문서」)는 상수
`10000000` 과 문자열이 안 맞아 **거짓 RED** 다. 오늘 그 줄을 대는 상수 인용은 0건이고,
틀리는 방향이 **시끄러운 쪽**(초록이 아니라 빨강)이라 값이 낮다. 그 줄을 실제로 인용하는
계획이 그날 정규화 한 줄을 넣는 것이 싸다.

**[R67-4] 3점 · 지적 아님.** `_has_number` 를 `staticmethod` 로 `SpecCitationTest` 안에
둔 자리는 맞다 — 무상태이고 쓰는 곳이 그 클래스 하나뿐이라 모듈 함수로 빼면 이름만
넓어진다. 옆의 `CITE`·`PHRASE`·`CONST` 와 같은 층이다.

**범위 밖 확인 — 완료 기준을 훼손하지 않는다.** `tests/test_serve.py:918`
(`"JS 0KB 계약 위반 (concept.md:51)"`)이 의미 축에 안 물리는 것은 맞다(인용 뒤가
`)")` 라 「인용 뒤 따옴표」가 0건이고 `CONST` 도 안 문다). 그러나 계획서 4절 **C1~C7 은
전부 가드 자신의 변이**이고, 그 자리의 **주소 축은 물린다**(51행 실재·비어 있지 않음).
C4·C5 가 요구하는 문구 단언도 다른 자리에서 살아 있다. 「범위 밖 · `digest [6]` 등재됨」
으로 확인만 하고 닫는다. `digest [6]` 의 처방(테스트가 아니라 **소스 표기**를 바꿔
인용 뒤에 사양 문구 한 조각을 옮겨 적는다)은 다음 계획으로 열릴 만하다 — 고칠 자리가
11건으로 세어져 있고 검사기는 안 건드린다.

**범위**: `src/` 0줄 · `e2e/` 0줄 · `docs/specs/` 무변 · `data/crawl.db` 무변 · 재색인 0 ·
스키마 0 · 새 의존성 0(stdlib) · PR #7 무접촉(`gh` 0회) · 변이 0판 · 전수 1회 ·
`--no-verify`·`--force`·`--amend`·`rebase` 0회 · `main` 직접 커밋 0회.
바꾼 것은 `tests/test_docs.py` 하나(+3/-1) 와 문서(`status`·`history`·`metrics`).

## 다음

**e2e phase.** 이 계획의 산출물은 문서 인용을 무는 단위 가드 하나라 e2e 는
「사양을 한 칸 밀면 실제로 빨개지는가」를 실물 파일에서 재는 자리다.
