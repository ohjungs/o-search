---
signal: GREEN
phase: 리뷰
step: 1/1
attempt: 0
iteration: 385
updated: 2026-09-06
ctx: 38
night_iterations: 189
night_red: 2
night_retries: 4
plan: readme-shape-cover — 계획 66 (리뷰 1/1 완료 · 다음은 e2e phase)
---

## 현재 상태

**계획 66 스텝 1/1 을 리뷰했다 — 8점 이상 0건, 코드 0줄.**
이번 리뷰는 **정적 판독만** 했다. 직전 반복이 성한 트리에서 전수
`Ran 625 · OK · rc 0` 을 냈고 워킹트리가 깨끗하므로, 같은 판정을 다시 사기 위해
변이 판을 또 심는 것은 예산만 태운다(앞선 두 시도가 여기서 48분·39분 정지했다).
그래서 이 반복의 검증은 **README 실물에 정규식을 태워 읽은 결과**다 — 트리 무변경.

## 지적 목록 — 8점 이상 0건

| # | 점 | 무엇 | 처리 |
|---|---|---|---|
| 1 | 4 | `BAND_TABLE` 이 「제목 → 빈 줄 → 표」 배치와 콜론 없는 구분 줄(`\|---\|`)을 요구한다. 표 앞에 설명 한 줄을 넣거나 `\|:---\|` 로 정렬을 주면 `setUp` 이 빨개진다 | 안 고친다 — 메시지가 「표를 못 잘라냈다」라 오답으로 유도하지 않고, 잘라내기를 느슨히 하면 U3(제목 개명)를 놓친다 |
| 2 | 4 | 커버 판정이 «행마다 아홉 중 하나라도 걸리나»라 **일대일이 아니다**. 기존 행을 복제하거나 기존 문구를 담은 새 행은 통과한다 | 안 고친다 — 닫은 것은 「대조 밖 문구를 가진 행」이고 docstring 도 그렇게만 주장한다. 일대일은 밴드↔행 사상을 새로 들여야 해서 이 스텝의 값보다 비싸다 |
| 3 | 3 | `((?:\|.*\n)+)` 가 행마다 개행을 요구한다 — 표가 파일 끝에 개행 없이 놓이면 **마지막 행이 조용히 빠진다** | 안 고친다 — 표는 README 88~94행이고 뒤에 두 절이 더 있다. 오늘 잃는 것 0 |
| 4 | 3 | 이어받은 `path` 가 실패해도 뒤따르는 `〃` 행이 같은 경로로 연쇄 실패한다 | 안 고친다 — 소음일 뿐 방향이 옳고, subTest 라 첫 건에 진짜 원인이 적힌다 |
| 5 | 2 | `METER_CELL` 이 `$` 를 쓰면서 `MULTILINE` 이 없다. **줄 단위로 넣는 지금 호출부에서만** 옳다 | 안 고친다 — 호출부는 `splitlines()` 루프 하나뿐이다 |

## docstring 실물 대조 (정적) — 여섯 주장 전부 참

README 원문에 `BAND_TABLE`·`METER_CELL`·`QUALITY_BAND` 아홉 정규식을 그대로 태워 읽었다.

- 「README 에는 표가 셋」 → 구분 줄 `\|---\|---\|---\|` **3개**. 잘라낸 블록은 그중 합격선 표 하나.
- 「일곱 행」 → 잘라낸 데이터 행 **7**.
- 「아홉 수치 · 첫 행이 둘, 마지막 행이 둘」 → 행별로 걸린 밴드 인덱스가
  `[0,1] [2] [3] [4] [5] [6] [7,8]`. 아홉이 **빠짐없이 한 번씩**, 커버 안 되는 행 **0**,
  어느 행에도 안 걸리는 밴드 **0**.
- 「셋째 칸이 실재하지 않는 파일(`quality_evals.py`)」 → `e2e/quality_eval.py` 실재 ·
  `e2e/quality_evals.py` 부재. 변이가 골랐던 이름이 맞다.
- 표가 가리키는 측정기 일곱(이어받기 포함) **전부 `is_file()` 참**.
- `E2E_COUNT` 「21종」 → `e2e/*.py` **21**. `UNIT_COUNT` 625 는 직전 반복 전수 실측 그대로.

## `〃` 첫 행 판정 (정적)

테스트 phase 가 `[4]` 로 남긴 「이어받기 가지가 변이로 안 눌린다」는 **논리로는 닫혀 있다.**
유일한 `〃` 는 7행이고 바로 위 6행이 `e2e/design_check.py` 를 이름으로 적어 `path` 가 이미
차 있다. `〃` 가 **첫 행에 오면** `assertTrue(path, ...)` 가 먼저 죽으므로 「이어받을 값이
없는 채 조용히 통과」하는 눈먼 자리는 존재하지 않는다 — 없는 것은 그 가지의 **변이 실측**
하나뿐이고, 그 실측을 사는 값은 오늘 0 이다. `[4]` 는 digest 에 그대로 둔다.

## 이번 반복이 새로 안 것 — 정적으로 읽히는 것에 변이를 걸면 예산만 탄다

같은 리뷰 스텝에서 앞선 두 시도가 산출물 없이 48분·39분 정지했다. 둘 다 변이 판을 다시
심다가 못 나왔고, 되사려던 판정은 **직전 반복이 이미 초록으로 산 것**이었다. 이 반복은
변이를 0판으로 못박고 정규식을 실물에 태우는 판독으로 같은 다섯 축을 다 덮었다 —
도구 호출 열몇 회. `digest ## 반복 실패` 에 2회로 등재했다.

## 범위·집안일

`src/` 0줄 · `tests/` **0줄** · `e2e/` 0줄 · `README.md` **0줄** · `docs/specs/` 무변 ·
`data/crawl.db` 무변 · 재색인 0회 · 스키마 0 · 새 의존성 0 · PR #7 무접촉 · `gh` 0회 ·
변이 **0판** · 전수 **0회**(코드 무변경이라 재실행이 살 것이 없다. 문서 편집 뒤
네 동기 가드만 `-p test_docs.py` 로 확인했다). `history_current.md` 는 상한 아래라 회전 없음.
`digest.md` 는 한 줄 더하고 완료 항목 한 줄(계획 61 — 62·63 이 같은 축을 덮는다)을 뺐다.

## 다음

**e2e phase.** 계획 66 의 e2e 는 새 시나리오가 아니라 **표 편집이 21종 위에서 무해한지**만
본다.
