---
signal: GREEN
phase: e2e
step: 1/1
attempt: 0
iteration: 337
updated: 2026-09-05
ctx: 52
night_iterations: 158
night_red: 2
night_retries: 0
plan: passage-cost-axis 계획 57 (리뷰 완료 · findings 3건 전부 low · 프로세스 medium 1건)
---

# 현재 상태

**계획 57 리뷰 phase 를 닫았다 — 단언 셋이 각자의 변이를 실제로 잡는 것을 손으로 심어
확인했고, 계획이 세운 「축의 끝」 주장도 반증을 시도해 살아남았다.** 코드 findings 는
전부 **low**(전부 낡은 서술 · 동작 영향 0)이고, 대신 **저장소의 변이 검증 절차 자체에
medium 하나**가 나왔다.

## 백지 패스에서 잡은 것 — 3건 (전부 low, 신뢰도 85~90)

| # | 자리 | 무엇 |
|---|---|---|
| **[R57-1]** | `indexer.py:44` · `project.md:187` · `test_indexer.py:810` | **「0.95 ≥ 한가할 때 0.939~0.970」 은 부등호가 거짓이다.** 같은 주석이 두 번 선언한 «낮은 쪽으로 안 내린다» 를 자기가 어겼다. 오늘 재현해도 idle min-of-3 5회가 **0.917~0.955** 로 0.95 를 넘는 표본이 나온다. 영향은 ① 산술이 332.5 → **339.5ms**(67 → 68%)여야 한다는 것뿐이고 둘 다 500 안이라 **동작 0**. 처방: 세 자리 계수를 0.97 로 올리거나 문장을 «표본 중앙» 으로 정정 |
| **[R57-2]** | `tests/test_indexer.py:791~807` | **단언 위 17줄이 아직 옛 세계다.** 791 「사양 성능 5 예산 500ms 의 **1/3**」 이 바로 아래 815 `assertLessEqual(worst_ms, 500)` 과 **정면 모순**이고, 793~805 는 0.44 를 현재형(「…값이다」·「여전히 참인데」)으로 서술한다. 808 에 가서야 갈아 끼웠다고 밝힌다 — **이 계획이 죽이려는 실패 모드가 같은 함수 주석 안에 남았다** |
| **[R57-3]** | `docs/design_passage-cost-axis.md` | 설계가 코드보다 뒤처진 두 곳 — (a) 「② 가 ① 의 리터럴을 붙든다 · 0.95 가 낡으면 ② 가 먼저 빨개진다」 는 한도가 1.60 이라 **0.95~1.60 구간에서 거짓**이고 테스트의 `ponytail:` 줄이 이미 그 창을 인정한다 (b) 단언 블록에 **①-b(상수 동결)가 없다** — 테스트 phase 가 캡 50,000 변이의 초록을 보고 더한 것이라 설계가 그것을 모른다 |

**세 건 다 「살아 있는 코드 옆의 낡은 숫자」 한 종류다.** 고칠 자리가 `tests/`·`src/`·설계
문서라 **이번 스텝의 지시 범위 밖**이라 손대지 않고 넘긴다.

## 검증 — 변이를 직접 심어 「잡는지」까지 봤다

- **① 산술** — 리터럴 `0.95` → `1.50` 변이: `FAILED` (525.0 > 500). ①-b 는 초록이라 **①·①-b 는 서로 다른 변이를 잡는다**(「①-b 가 ① 을 삼킨다」 후보는 이것으로 반증돼 버렸다).
- **①-b 상수 동결** — `MAX_PASSAGE_HTML` 35,000 → 50,000 변이: `FAILED` (500000 > 350000). 계획 57 이 이 문을 세운 이유가 재현됐다.
- **② 계수 상한** — `extract_blocks` 앞에 1.0ms/1000자 지연(≈2배) 변이: `FAILED`. 셋 다 원복했고 워킹트리 무변.
- **「축의 끝」 반증 시도** — 캡을 채운 **26개 모양**을 같은 판에서 쟀다. 상위는 `<P>` 0.941 · `<p>` 0.919 · `<p>가` 0.880 · `<x>` 0.863 이고 속성(`<p a=1>` 0.526)·닫는 태그(`</p>` 0.413)·주석(`<!--` 0.002)은 전부 아래다 — **`<p>` 계열이 실제로 끝**이고 클레임이 살아남았다(2~3% 노이즈 안).
- **부하 여유 재현** — 코어 수(10)만큼 스핀 루프를 띄우고 min-of-3 을 5회: idle 0.917~0.955 · load **1.171~1.214**. 1.60 까지 부하 실측 대비 **1.32배**(설계는 1.36배로 적었다 — 오차 안).
- 전수 `Ran 605 tests · OK` · rc 0 · 13.7초 · 문서 가드 4종 `Ran 8 tests · OK` · rc 0.
- `data/crawl.db` sha256 앞뒤 동일(`85c96744…5bda18`) · 재색인 0 · 스키마 0 · 새 의존성 0 ·
  `src/`·`tests/`·`e2e/`·`README.md`·`docs/specs/` **무접촉** · PR 무접촉(`gh` 호출 0) ·
  원격 `loop/passage-cost-axis` = `e6f375c` (HEAD 와 일치).

## 버린 후보 (80점 미만)

- ② 의 1.60 이 이 기계 전용이라 느린 기계에서 오탐(60) — 저장소에 CI 가 없고 한 기계에서만 돈다.
- ①-b 가 곱만 봐서 «캡↓ × LIMIT↑» 조합이 통과(50) — 파싱 비용이 곱에 붙는 것이 맞고 주석이 그 이유를 적었다.
- ① 이 ①-b 에 삼켜진 죽은 단언(40) — 위 변이 실측이 반증했다.

## 다음 스텝 — e2e phase

계획 5절이 **새 e2e 파일을 명시적으로 배제**했다(프로세스 경계에서 재는 것이 없고,
`e2e/passage_eval.py` 가 이미 실물 p95 를 잰다). e2e phase 는 그 면제 판단을 확인하고
기존 e2e 를 돌리는 자리다.

## 사람 결정 대기

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1).
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 0 일 때만.
3. **반응형 360px 미검증** — 브라우저가 없어 저장소의 아무도 안 그려 봤다.
4. **3시간 자동 스냅샷 잡을 루프 작업 중에도 세울 것인가**(반복 328 사고). RED 중간을
   덮치면 깨진 상태가 원격에 올라간다. 반복 334 에서 RED 아닌 자리에서 실제로 일어났다.
5. **재파싱에 예산 얼마를 줄 것인가** — 캡 최악이 예산의 **67%** 라는 사실이 이제 세 자리에
   모두 적혀 있다. 캡·`PASSAGE_LIMIT` 를 내리는 것은 «긴 문서의 뒷부분 근거» 쪽 판단이다.
6. **[새] 변이 검증 절차를 고칠 것인가(medium)** — macOS 시스템 파이썬이
   `sys.pycache_prefix = ~/Library/Caches/com.apple.python` 를 써서 **저장소 안
   `__pycache__` 를 지워도 낡은 바이트코드가 남는다.** 크기가 같은 편집(`0.95`→`1.50`)을
   같은 초에 원복하면 mtime(초)+size 검사를 통과해 **원복한 뒤에도 변이 결과가 다시 나온다** —
   이번 리뷰에서 실제로 한 번 속았다(M3 이 원복된 트리에서 525.0 을 냈다). 이 저장소의
   **모든 변이 실측이 이 함정 위에 있다.** 처방은 변이 검사 앞에
   `rm -rf ~/Library/Caches/com.apple.python$PWD` 를 붙이거나 `PYTHONPYCACHEPREFIX` 를
   매번 새 임시 디렉터리로 주는 것이고, `digest.md` 후보로 올릴 자리다.
