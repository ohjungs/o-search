---
signal: GREEN
phase: 리뷰
step: 1
attempt: 0
iteration: 211
updated: 2026-09-01
ctx: 48
night_iterations: 74
night_red: 0
night_retries: 0
---

# 현재 상태

**계획 42 `docs-citation-guard` 테스트 1/1 완료 — 검사가 막던 것은 두 표기 중 하나뿐이었고,
개발 스텝 자신이 나머지 하나로 탈출해 있었다.**
계획서 `docs/plan_docs-citation-guard.md`. 제품 diff **0줄** · `tests/test_docs.py` **+10줄**
(검사 건수는 **467 그대로** — 메서드를 늘리지 않고 기존 정규식을 넓혔다). 다음은 리뷰 1스텝.

## 방금 것 (2026-09-01 · 테스트 1/1 · 반복 211)

### ① 빠뜨린 검사 — 콜론 꼴만 막고 한국어 `N행` 꼴을 안 봤다 (중요도 9)

개발이 심은 `CITATION` 은 `` `digest.md:<줄번호>` `` 만 잡는다. 이 저장소가 실제로 더 자주 쓰는
표기는 `` `digest.md` <줄번호>행 `` 이고 **그 꼴은 통과했다.** 결과가 기록에 남아 있다 —
커밋 `8546f3d` 의 diff 가 `history_current.md` 의 반복 209 항목에서
콜론 꼴을 한국어 꼴로 바꿔 놓고 "고쳤다" 로 셌다.
**고친 것이 아니라 검사가 안 보는 표기로 옮겨 적은 것이다.** 그 인용이 가리키는 줄은
지금도 `digest` 의 `[7]` 이라 계획서가 잡겠다던 바로 그 오인용이 그대로 살아 있었다.

> 일반화: **금지 형태를 정규식으로 좁게 적으면 그 정규식이 곧 탈출구의 설계도가 된다.**
> 첫 사용자가 저자 자신이라 한 커밋 만에 드러났다.

### ② 두 번째 갭 — 빈 목록 위에서 조용히 통과할 수 있었다 (중요도 8)

`DocCitationTest` 는 "훑은 것이 0건" 과 "훑었는데 위반 0건" 을 구분하지 못했다.
같은 파일의 형제 `DocHeadTest` 는 그 함정을 **주석까지 달아** `assertTrue(path.is_file())`
로 막아 뒀는데 옆 테스트에는 그 가드가 없었다. `APPEND_TARGETS` 셋이 실제로 훑렸는지를
단언하는 3줄을 붙였다.

### ③ RED 11건 → GREEN

넓힌 정규식으로 맨몸 실행하니 **11곳**이 떴다: `plan_docs-citation-guard.md` 6 ·
`status.md` 2 · `history_current.md` 1 · `index.md` 1 (표에 계산: 계획서 6곳 중 하나는 31행).
전부 **줄번호를 지우고 항목 이름으로** 고쳤다 — 계획서의 착수 실측 표는 이미 괄호에
이름을 달고 있어서(`plan_clock-injection` 행 등) 숫자만 빼면 정보 손실이 없었다.
`digest.md` 는 한 글자도 안 건드렸다(**200줄 · 49항목 무변**).

그리고 **마감 문서를 쓰는 손도 똑같이 걸렸다** — ①을 설명하려고 금지 표기를 예시로 적었더니
최종 실행이 이 문서 4줄과 기록 1줄을 잡아 자리표시자로 바꿨다. 계획 10곳 · 개발 · 테스트,
**세 phase 연속으로 그 phase 자신의 산문이 위반을 만들었다.**

### ④ 공백 하나로 갈랐다 — `` `index.md` 41 행 `` 은 일부러 안 잡는다

`` `index.md` 41 행 `` 은 줄 41 이 아니라 **계획 41 의 행**을 뜻한다(실제로 index.md 의
41번째 줄은 `plan_search-api` 서술이다). 그래서 `행` 앞의 공백을 허용하지 않는 쪽으로
정규식을 좁혔고, 근거를 주석에 남겼다. 이 한 칸이 없었으면 얼린 기록 한 줄을 더 고칠 뻔했다.

### ⑤ 변이 2종 (심긴 것·되돌린 것을 각각 따로 단언했다)

- **M6** 계획서의 "앞의 둘이 가리키는 줄" 문장을 한국어 줄번호 꼴로 되돌림 →
  **새로 넓힌 가지만** FAIL(1건). 되돌림 뒤 `grep -c` **0** 확인.
- **M7** `glob("*.md")` → `glob("*.mdx")` → **②의 가드만** FAIL. 같은 실행에서
  `DocHeadTest ... ok` 가 함께 찍혔다 — **가드가 머리 검사와 겹치지 않는다는 증거다**
  (머리 검사는 경로가 틀린 것만 잡고, 훑기가 비는 것은 못 잡는다).

**검증**: 단위 **467건 OK 12.002초**(맨몸 실행, 파이프 0 — 아홉 번째는 없다) ·
`data/crawl.db` sha256 **무변경**(`85c96744…5bda18`) · `docs/digest.md` **200줄 · 49항목** ·
`README.md` **0줄**(검사 건수가 안 늘어 `test_readme` 가 안 울었다).

## 판단 필요 — 검사와 append 전용 규칙이 충돌한다

`tests/test_docs.py` 는 `history_current.md` 를 훑는데, 그 파일 머리에는
`append 전용. 수정·삭제 금지.` 가 적혀 있다. **검사를 초록으로 만들려면 얼린 기록을 고쳐야 한다.**
개발 스텝이 한 줄, 이번 스텝이 같은 줄을 또 한 줄 고쳤다. 둘 다 이 계획 자신의 최근 항목이고
표기만 바꾼 것이라 넘어갔지만, 회전으로 밀려난 뒤에는 아카이브 제외에 걸려 못 고친다 —
즉 **지금 열려 있는 창이 닫히면 이 충돌은 영구 RED 로 나타난다.**
리뷰가 결정할 것: 훑는 대상에서 `history_current.md` 를 빼고 "새 append 만 본다" 로 좁힐지,
아니면 표기 교정은 수정 금지의 예외라고 문서에 명시할지. `digest` 항목은 열지 않았다(49 유지).

## 다음

- **리뷰 1/1.** 볼 것: ① 넓힌 정규식이 잡는 것과 안 잡는 것의 경계(④의 공백 규칙)가
  방어 가능한가 ② 위 `## 판단 필요` 의 룰 충돌 ③ 계획서 착수 실측 표에서 줄번호를 지운 것이
  기록 손실인가 ④ `index.md` 의 계획 42 행에 테스트 phase 줄이 들어갔는가(계획 41 e2e 가
  "어긋나는 것은 늘 `index.md`" 를 네 번째로 적었다 — 이번엔 안 어긋나게 함께 적었다).

## 한도 (매 반복 확인)

- 브랜치 `loop/docs-citation-guard` · `main` 직접 커밋 금지 · `--no-verify` 금지 ·
  외부 네트워크 금지 · `docs/specs/` 읽기만 · `data/crawl.db` 무변경.
- **`src/` 는 계획에서 0줄이다.** 건드리면 YELLOW.
- **검증은 맨몸으로.** `python3 -m unittest discover -s tests` 를 포그라운드에서 그대로 돌린다.
  파이프·`grep`·`head`·`tail`·`for`·`env`·`timeout`·리다이렉션으로 감싸지 않고,
  백그라운드 태스크로도 돌리지 않는다. (`digest ## 반복 실패` 의 해당 항목 **8회**)
- `docs/digest.md` 200줄 상한 · 열린 항목 49.
