---
signal: GREEN
phase: 리뷰
step: 4
attempt: 0
iteration: 301
updated: 2026-09-04
ctx: 46
night_iterations: 129
night_red: 2
night_retries: 0
plan: hidden-passage
---

# 현재 상태

**리뷰 4 — 지적 2건, 둘 다 자동 수정. 승인 필요 0 이라 반려 없이 간다.**
제품 `src/` **+4줄**(주석 3 + 값 1) · `tests/test_extract.py` **+6**(기존 표에 붙인 행이라
**테스트 건수는 593 그대로**이고 `README.md` 무변) · `docs/design_hidden-passage.md` +3.
새 파일 0 · 새 의존성 0 · 심은 변이 0(검산 변이는 프로세스 메모리 안).

**신호는 GREEN** — 단위 **593건 OK** rc 0(맨몸·단독 13.566초) · `passage_eval` 통과 ·
`quality_eval` 통과 · `design_check` 4축 · RED 0 · 재시도 0.
브랜치 `loop/hidden-passage`, 기점 `d5367fa`(**`main` 아님** — 아래 「원격」).

## 패스 A 를 눈이 아니라 탐침으로 했다

백지 패스에서 `146c8c4..HEAD` 의 `src`/`tests` diff만 열고, 명세가 종료 태그 생략을
허용하는 **열여섯 키**를 정상 HTML **17모양** + 누출 가드 **8모양**으로 스크래치패드에서
직접 돌렸다(러너 밖 · 소스 무편집).

- `_TABLE_PARTS` 의 뺄셈 셋(`td`↛`tr` · `tr`↛`tbody` · `col`↛`colgroup`)은 「in cell」·
  「in row」·「in table body」·「in caption」과 **전부 일치**했다. `caption` 이 뺄셈 없이
  전부를 갖는 것도 맞다 — 「in caption」이 그 아홉을 그대로 적고, 중첩 `<table>` 은
  목록 밖이라 **자식으로 들어간다**(실측: 셀 안 중첩 표가 숨김을 안 옮긴다).
- `p` 목록은 "close a p element" 를 부르는 시작 태그와 대조해 **과잉 0 · 미달 0**.
- 17모양 중 **15가 초록**, 갈린 둘이 아래 [R4-1] 과 버린 후보다.

## [R4-1] 「in select」 가 `<hr>` 에서도 걷는 것을 값이 빠뜨렸다 — low · 자동 수정

주석은 *"값은 각 삽입 모드가 적은 목록을 그대로 옮긴다"* 인데 `optgroup` 의 값이
`{"optgroup"}` 뿐이라 **그 문장이 거짓**이었다. 명세는 `<hr>` 에서도 열린 `option`·
`optgroup` 을 걷는다(`<select>` 안의 구분선으로 허용된 태그다).

`<select><optgroup hidden><option>숨은<hr><option>보이는</select>` → HEAD 에서 **`[]`**.
계획서 8절이 「제일 위험」이라 적은 **오탐**(보이는 항목이 통째로 사라진다) 방향이다.
값에 `"hr"` 을 더하고 기존 표에 행 하나를 붙였다. **피해 반경은 `</select>` 까지**로
닫혀 있어(그 태그가 `_els` 를 되감는다) medium 이 아니라 low 로 매겼다.

## [R4-2] 설계의 「모듈 상수 둘」이 오늘 넷이다 — low · 자동 수정

리뷰 51 `[R51-1]`·`[R51-5]` 가 `_VOID_TAGS`·`_IMPLIED_END`·`_TABLE_PARTS` 를 열었다.
리뷰 3 이 같은 절에 「오늘 거짓이다」를 적은 그 자리라 같은 방식으로 한 줄 고쳤다 —
셋 다 `_BlockParser` 전용 읽기라 **색인 경로 무변경이라는 결론은 그대로다**.

## 버린 후보 2 — 둘 다 파스 에러라 천장 안이다

① 중첩 `<select>`(명세가 `</select>` 로 친다) · ② `colgroup` 안의 비-표 시작 태그.
둘 다 **깨진 입력**이고, 흉내 내려면 foster parenting 이 필요해 사다리 위쪽에서 멈췄다.
`_IMPLIED_END` 가 덮는 것은 **명세가 허용하는 생략**이라는 선을 그대로 지켰다.

## 관문 — 전수의 네 번째 축

77,284(개발 4)도 146(테스트 4)도 **`_IMPLIED_END` 에 적힌 것**을 축으로 쓴다. [R4-1] 은
적히지 않은 값이라 **어느 전수에도 안 걸렸고**, 잡은 것은 주석이 스스로 적어 둔 규칙을
원전과 대조한 패스 A 다. 굳힌다: **표를 상수로 옮겨 적었으면 전수의 축은 표가 아니라
원전이다**(`metrics.md` 에 이어 붙였다).

## 검산 — 변이 하나, 사망 하나

`"hr"` 을 도로 빼면 새 행 **`optgroup + hr` 하나만 사망**하고 나머지 592건은 그대로다.
단언을 낮춘 자리 0 · 커밋된 변이 0 · 기존 테스트 깨진 것 0.

## 한도

- 제품 `src/` **+4줄** · 테스트 +6 · 새 파일 0 · 새 의존성 0 · 계획 「건드릴 파일」 안.
- `data/crawl.db`·`data/`·`e2e/`·`docs/specs/` 무변경. 스키마·마이그레이션 0.
- `_SKIP_TAGS`·`_INLINE_TAGS`·`_NON_BLOCK_TAGS`·`_normalize`·`_TextParser` 무변경.

## 러너 규율

판정 실행은 **맨몸·단독**이다 — `PYTHONPATH=src python3 -m unittest discover -b -s tests`
와 e2e 각각 `PYTHONPATH=src python3 e2e/<이름>.py`. **이번 반복은 파이프 6회**를 썼다 —
단위 3회·e2e 3종에 `| tail` 을 붙여 마지막 줄만 읽었다(리다이렉션 0 · 출력 조작 0).
규율은 «판정에만 맨몸» 이 아니라 «러너에 파이프를 안 붙인다» 라 **0회가 아니다** —
두 반복 연속 0 이던 것을 여기서 끊었다. 탐침은 러너가 아니라 스크래치패드 스크립트다.
**첫 단위 호출은 `discover -s tests -t .` 로 불러 `ImportError` 가 났다**(테스트 3 이 밟은
그 함정 · `digest [9]`). 판정 줄만 봤으면 «RED» 로 적을 뻔했고, README 의 명령으로
고쳐 다시 쟀다 — **러너를 바꾸면 색이 바뀐다**를 세 반복 만에 다시 만났다.

## 문서

`status.md`(이 파일 · 반복 **301**) · `metrics.md`(반복 **301** · 리뷰 **38** · 리뷰 정확도
표 갱신 + 「전수의 축」에 네 번째 축) · `index.md`(51번 줄에 리뷰 4) ·
`history_current.md`(리뷰 4 항목을 붙이니 **331줄**이라 그 자리에서 회전했다 — 리뷰 2·
개발 3 을 `history_052.md` 로 밀어 **267줄**. 하나만 밀면 309줄이라 둘이었다) ·
`digest.md`(**200줄 유지** — 명부에 이름을 더하고 회전 서술을 기존 줄에 이어 붙였다.
밀린 둘의 값은 `## 반복 실패` 3회 항목과 `[7]` 이 이미 압축본을 갖고 있어 새 줄 0.
내용 손실 0 · **새 색인 구멍 0**) · `README.md` **무변**(테스트 건수 593 그대로).

## 다음 — e2e 4

제품 코드를 건드렸으므로 **20종을 전수로** 다시 돌린다. 기준선은 테스트 4 의 값이다 —
블록 **253** · 정확도 **100.0%**(398/398) · 채택률 99.5% · p95 **1.53ms** · ko 20/20 ·
en 19/20 · `perf_search` p95 8.78 · `search_api` p95 2.10 · `perf_crawl` 10.21/10.26 ·
`design_check` 4축. 통과하면 계획 51 을 DONE 으로 닫는다(`plan_`·`design_` → `_history_051`).

## 원격 — `origin/main` 은 계획 47 까지다

- **커밋·푸시 뒤에 `git ls-remote origin` 을 다시 읽었다**(digest 의 「원격을 바꾸는 동작을
  «명령을 보냈다» 로 기록하지 않는다」 항목). 아래 SHA 는 이 반복이 실제로 읽은 값이다.
- **`loop/hidden-passage` = `d1fd613`** (리뷰 4 커밋 · `4ef5e49` → `d1fd613`, fast-forward).
  `git ls-remote origin loop/hidden-passage` 의 `d1fd6133…2574e154` 와 로컬 `HEAD` 가 같다.
  `git ls-remote origin main` = `687a1598…68d8e5e` 무변(계획 47).
  `--no-verify`·`--force` 0회 · 훅 우회 0.
- **PR #7** — `loop/merge-48-50` → `main`, 계획 48·49·50. **OPEN·미병합.** 병합은
  **사용자가 처리한다** — 이 반복은 PR 을 열지도 닫지도 않았고 그 브랜치를 건드리지도,
  `main` 으로 리베이스하지도 않았다.
- **계획 51 의 기점은 `main` 이 아니다** — `origin/main`(`687a159`)에는 계획 48·49·50 이
  없어 `README.md` 의 건수 단언이 거기서는 RED 다. `d5367fa` 에서 브랜치를 땄다.
- 앞선 병합: PR #6 = 계획 44~47(`687a159`). 충돌 0.

## 사람이 정할 것 — 다섯 (PR #7 이 붙었다)

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1, 입력창의 유일한 경계).
2. **`--focus` 는 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 이 0 일
   때만 이웃이 되고, 검사기가 매 실행 offset > 0 을 확인한다(실측 2px).
3. **반응형 360px 미검증** — 브라우저가 없어 이 저장소의 누구도 그 화면을 못 그린다.
4. **회전 규약·러너 규율의 저장소 밖 절반**과 **사양이 남긴 둘**(`docs/specs/concept.md` 의
   `## 사람이 정할 것` — 속도 제한 시점, 사양 숫자 90%·500ms·60회/분이 초안이라는 것).
5. **PR #7 병합** — 위 「원격」 절.
