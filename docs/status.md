---
signal: GREEN
phase: 리뷰
step: 1/1
attempt: 0
iteration: 342
updated: 2026-09-05
ctx: 64
night_iterations: 162
night_red: 2
night_retries: 0
plan: passage-cost-band 계획 58 (테스트 1/1 완료 · 다음은 리뷰)
---

# 현재 상태

**테스트 phase 를 돌았다 — 전수 605 OK · 갭 3건(전부 8점 미만) · 새 코드 0줄.**
`docs/status.md` 가 남긴 물음(「상수를 **내리는** 변경이 ② 를 느슨하게 하는가」)을
실측으로 닫았다: **그렇다, 그러나 사양은 안 깨진다.** 캡 20,000 이면 상한이 2.5 로
뛰어 **파서 2배 변이가 초록**이고(현행 캡 35,000 에서는 1.956 > 1.4286 으로 빨강),
그때도 최악은 20×1.956×10 = **391ms ≤ 500** 이라 계획의 목표(「② 초록 = 예산 안」)는
**어느 상수에서도 참**이다. 잃는 것은 사양 적합이 아니라 **회귀 감지력**이다 —
그날 함께 가야 할 것은 ① 의 리터럴 `1.00` 을 실측으로 올리는 일이다(갭 [6]).

**창이 실재했다는 것을 양쪽에서 봤다** — 계수를 1.50 으로 올리는 변이 M5 가
**착수 전에는 초록 3/3**(최악 529ms · 사양 밖인데 조용했다), **고친 뒤에는 빨강 5/5**
(스핀으로 고정한 1.457~1.477 · 최악 510~517ms). 상수 변이 M2(캡 10만)·M3
(`PASSAGE_LIMIT` 100)도 여전히 빨강이다.

**오탐은 반코어까지 샀다** — 5/10 코어 부하에서 이 테스트만 **20회 연속 초록**.
전코어(10/10)에서는 13/15 로 빨개지지만 **같은 판에서 옛 리터럴 1.60 도 2/15 로 깨진다** —
설계 2절이 「어떤 값으로도 못 사는 자리」로 판정하고 실패 메시지의 분간(«기계가 바쁨» 1.7
근처 대 «파서 회귀» 1.4~1.6)으로 받은 대가다. **완료 기준 4 를 문자 그대로는 못 맞춘다.**

**완료 기준 4 판정 — 조건부 충족(반복 342 가 독립 재현).** 한가할 때 **0/20** ·
반코어(5/10) 부하 **0/15** 로 오탐 0 이고, 전코어는 옛 리터럴 1.60 도 4/15 로 깨지니
**상한 탓이 아니라 기계가 바쁜 것**이다(반복 341 표). 「부하에서 안 흔들린다」를
값으로 살 수 없다는 설계 2절 판정을 재측이 그대로 확인했다 — 문자 그대로는 미달,
살 수 있는 구간(«반코어까지»)에서는 충족.

## 결정 — ② 의 상한을 그 자리에서 계산한다

```
② 예산 유도  min(3회 파싱)/캡kB ≤ 500 / (MAX_PASSAGE_HTML/1000 × PASSAGE_LIMIT)
             = 1.4286 ms/1000자   ← 리터럴 1.60 을 대체 · 창 폭 0
```

상한을 상수에서 유도하니 캡·건수를 올리면 상한이 **같이 내려가** 변이 M2·M3 가 ② 에서도
죽는다. ①(예산 산술 · 리터럴 1.00)과 ①-b(상수 동결)는 **그대로** 둔다 — ① 과 ② 사이는
아직 열려 있지만 그 구간은 전부 **≤500ms 라 사양 안**이다. 계획의 목표(「② 초록이면
최악이 예산 안이다」)가 여기서 참이 된다.

## 재서 갈랐다 — 부하 배수는 상수가 아니다

같은 입력(캡 35,000자 `<p>` 반복)·min-of-3·15판, 바쁜 코어 수만 바꿔서:

| 판 | 계수 최대 | 1.4286 초과 | 1.60 초과 |
|---|---|---|---|
| 한가할 때 · 2코어 바쁨 | 0.996 | 0/15 | 0/15 |
| 5코어(반) 바쁨 | 1.185 | 0/15 | 0/15 |
| 10코어(전) 바쁨 | **1.730** | 14/15 | **4/15** |

계획 57 이 잰 1.25배가 오늘 같은 방식에서 **1.79배**다. **그래서 「부하에서 안 흔들린다」는
어떤 값으로도 못 산다** — 전코어 부하에서는 현행 1.60 도 이미 빨개진다. 살 수 있는 것은
«반코어까지» 뿐이고 그 구간에서 두 값이 똑같이 초록이라 **창은 공짜로 닫힌다.**

기각한 안: **C** `time.process_time` 은 전코어 부하 1.612 로 벽시계 1.636 의 **6%만**
줄인다(경합이 대기가 아니라 진짜 CPU 시간이다) · **B** 리터럴 1.20 은 반코어 1.185 대비
여유 1.01배로 오탐 확정 · 곁가지 셋도 실측 기각(min-of-10/30 = 1.518/1.475 · 잣대 비율
정규화는 꼬리가 39~41% 로 절대값 18% 보다 나쁘다 · 흩어짐 `skip` 게이트는 한가 ≤3.2% 대
부하-조용 4.1% 로 분리가 얇고 가드가 조용히 꺼진다).

## 다음 스텝 — 리뷰 (스텝 1/1)

- **백지로 diff 를 본다** — 계획 58 이 만든 것은 `tests/test_indexer.py` 의 단언 한 줄과
  주석뿐이다(제품 `src/` 0줄). 리뷰가 볼 자리는 ② 의 유도식이 아니라 **주석이 적은
  천장이 오늘 실측과 맞는가**다 — 테스트 phase 가 주석의 「1.4~1.6 이면 파서 회귀」를
  변이로 확인했고(2배 → 1.956), 「① 과 ② 사이는 사양 안」도 산술로 참이다.
- **리뷰에 넘기는 갭 3건은 전부 8점 미만이라 `digest.md` 에 있다** — 고칠 것이 아니라
  판정할 것이다: ① 이 ①-b 의 부분집합이라는 것(단독으로 안 빨개진다) · 예산 `500` 이
  테스트 안 두 곳 리터럴이고 `docs/specs/concept.md:129` 와 안 묶여 있다는 것 ·
  캡을 내리면 감지력이 준다는 것.
- 변이는 전부 `PYTHONPYCACHEPREFIX=$(mktemp -d)` 와 함께 돌린다(`docs/project.md ## 명령`).
  **상수 변이는 파일을 고치지 말고 메모리에서 준다** — 반복 342 가 그렇게 했고
  아래 4번의 재발(스냅샷이 `src/` 의 변이를 커밋)이 구조적으로 불가능해진다.
- **회전은 이번 반복이 끝냈다** — append 로 306줄이 되어 상한이 그 자리에서 걸렸고,
  계획 57 의 반복 333~338 을 `history_060.md` 로 밀어 **113줄**이 됐다(`digest.md` 명부와
  57 완료 항목에 이름을 더했다). 「다음 반복의 집안일」로 미루지 않았다.
- 이미 한 것: 테스트 1/1 완료 — 전수 605 OK · 문서 가드 4종 초록 · 오탐 0/20·0/15 ·
  갭 3건 등재. 코드·문서 단언은 한 줄도 안 고쳤다(테스트 phase 는 찾고 돌리는 곳이다).

## 사람 결정 대기 (계획 58 과 무관하게 계속 열려 있다)

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1).
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 0 일 때만.
3. **반응형 360px 미검증** — 브라우저가 없어 저장소의 아무도 안 그려 봤다.
4. **3시간 자동 스냅샷 잡을 루프 작업 중에도 세울 것인가**(반복 328 사고). RED 중간을
   덮치면 깨진 상태가 원격에 올라간다. 반복 334 에서 RED 아닌 자리에서 실제로 일어났다.
   **반복 341 에서 또 일어났다** — 16:50 스냅샷(`a565d38`)이 M5 변이의 `sleep` 을 `src/` 에
   심은 채로 커밋했다. 다음 커밋이 되돌리지만 **히스토리에 일부러 심은 지연이 남는다.**
   **반복 342 가 증거를 확정했다 — 세 번째 재발이고, 잔여물은 0 이다.** 스냅샷의 내용은
   `src/websearch/extract.py` **+2줄 · `_m5.sleep(0.018)` 한 줄**이고(`git show a565d38`),
   지금 **HEAD 와 워킹트리의 `src/` 에는 인위적 지연이 0**이다 — 남은 `sleep` 아홉 자리는
   전부 `crawl.py` 의 **주입 지점**(`sleep=time.sleep` 파라미터·`Event.wait` 분기)이라
   설계 계약 2 그대로다. 되돌리기는 잘 됐고 **히스토리 재작성은 금지라 손대지 않는다.**
   재발이 328(RED 중) → 334(RED 아님) → 341(변이 중)로 **매번 다른 상태**에서 났다는 것이
   판단 재료다 — 「RED 일 때만 멈춘다」로는 341 을 못 막는다. 반복 342 는 변이를
   **파일이 아니라 메모리에서** 줘서 이 문을 스스로 닫았다(`sys.modules` 의 상수·함수를
   바꾸고 전수 후 워킹트리 청결 확인). 잡을 세우지 않기로 정하면 **그 방식을
   `rules/dev.md` 의 변이 절차로 올리는 것**이 값싼 대안이다.
5. **재파싱에 예산 얼마를 줄 것인가** — 캡 최악이 예산의 **70%** 라는 사실이 세 자리에
   모두 적혀 있다. 계획 58 은 이 몫을 **안 건드리고** 500ms 정각을 그대로 쓴다.
6. **계획 57(`loop/passage-cost-axis`)의 병합** — `main` 은 계획 56 까지고 열린 PR 은
   0건이다. 계획 58 이 그 브랜치 위에 쌓이므로 병합은 순서대로 하면 된다.
