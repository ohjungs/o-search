---
signal: YELLOW
phase: 계획
step: 0/0
attempt: 0
plan: null
iteration: 562
updated: 2026-09-13
mode: night
night_iterations: 1
night_red: 0
night_retries: 0
night_self_amendments: 0
ctx: 측정 불가 — 공유 파일이 **남의 살아 있는 세션 것**이다(게이트 ⑦ 여섯 번째). 54분 낡았다가
  **세션 중간에 신선해졌는데 값은 한 바이트도 안 바뀌었다** — 신선도 검사만으로는 못 잡는다
note: 열한 번째 빈손. 핀이 세 밤 연속 맞았다(첫 줄 빔 · 이슈 0 → 전수만 1회 763 OK). 잰 것은
  둘 — **⑧ 납작해진 줄이 「두 줄」이 아니라 09-13 넷 · 09-12 셋**(최근 8줄이 값 둘뿐 = 세션
  해상도 0) · **⑦ 소유를 전사본으로 처음 확인**(내 것은 `0ffb5a2f…`, 그 파일은 홈 슬러그의
  `402da0af…` 가 쓴다 — 처방의 「내 id 를 어떻게 아나」가 닫혔다)
---

# 현재 상태

계획 **96 `docs-delete-rowid` 완료** 이후 새 계획이 없다. **탐색 0건 — 열 세션 연속**이다
(552~555 는 여덟 출처를 네 번 따로 전수, 556~561 은 아래 핀).

## 빈손 대조 핀 — 여덟 출처를 다시 안 재도 되는 조건

기준 트리는 **`78b22fc`** (559~562 가 전수로 확인한 것: 763 OK · 코드 `TODO` 0 · 4순위 스캐너
0건 · 이슈 0 · 패치는 `userinfo-leak` 하나 · 활성 `plan_*.md` 은 보류분 하나). 1~7순위는
**트리의 함수**라 트리가 같으면 결과가 같다. **8순위(이슈)만 트리 밖이라 둘째 줄을 «항상» 돌린다.**

```bash
git diff --stat 78b22fc..HEAD -- src tests e2e scripts docs/digest.md docs/candidates.md docs/specs
gh issue list --state open --limit 20     # 트리 밖 — 항상 돌린다
```

**핀은 스냅샷이라 낡는다 — 전수를 새로 돌려 빔을 확인했으면 기준을 HEAD 로 옮긴다.**
**560·561·562 세 밤 연속 맞았다** — 첫 줄이 비어서 5·6·7순위를 안 열고 전수 한 번으로 끝냈다.
**4순위 칸은 「`candidates.md` 0건」이 아니다** (562 정정) — 그 파일은 이 저장소에 **존재한 적이
없고**, 0건을 내는 것은 스캐너다(`candidates-scan.js docs/digest.md` 실측 · 구조적 0 은 digest
후보 `[6]`). **없는 파일을 0으로 읽는 것과 돌려서 0을 받는 것은 다른 문장이다.**
**핀의 수명은 회전까지다.** 핀을 깨는 유일한 것이 `digest.md` 이고, 그것을 건드리는 유일한
것이 `history_current.md` 300줄 초과 회전이다(회전이 digest 에 명부·기록 한 줄씩 쓴다).
지금 **193/300** 이고 빈손 한 밤이 실측 **21~28줄**(559 +21 · 560 +28 · 561 +25)이니
**다음 회전까지 약 4밤**이다 — 그 밤은 핀이 오경보를 내므로 전수를 돌리고 기준을 HEAD 로 옮긴다.

**핀은 «코드»의 함수지 «환경»의 함수가 아니다** — 파이썬·OS 가 움직이면 같은 트리가 다른
결과를 낸다. 그래서 전수는 싸니(**763 OK** · 18.7s · Python 3.9.6) 밤마다 돌린다.

## 사람 결정 대기 — 하나를 열어야 이어진다

근거·재현·처방은 갖춰졌고 막는 것은 판단뿐이다. 밤에 새로 막힌 것은 **여기 먼저 적는다**
(야간 보고서가 이 절을 그대로 퍼 간다).

- **① 동시 색인이 만드는 중복 `docs` 행** — 반복 550 리뷰 보류. 고치는 자리는 `index_pages`
  의 트랜잭션 경계(`BEGIN IMMEDIATE`)이고 재구축 갈래의 `BEGIN`(`indexer.py:217`)과 중첩을
  함께 풀어야 한다. **자기 계획 크기**이고 레이스 컨디션이라 밤이 안 연다. 승인하면 바로 계획.
- **② `perf_crawl.py` 기준선 여유 0.5%**(digest 후보 8점) — ①재기준선 ②[열림] 대비 비율
  ③참고용 강등 **셋 중 택1**. 밤이 못 고르는 이유는 셋 다 「통과시키려고 시나리오를 낮추는
  것」과 겉모양이 같아서다.
- **③ `docs/patches/userinfo-leak-refuse-credentials.patch`** — URL 자격증명이 크롤·저장·렌더로
  새는 건. 적용 상태에서 초록 확인 후 뽑아 뒀다. **보안 경계**라 밤이 영구히 못 연다.
- **④ `~/.claude/settings.json` 미커밋** — 내 변경이 아니고(`model: opus → opus[1m]` + `allow`
  자리 정렬 · 권한 상향 아님을 diff 로 확인) 코어층 가드가 막았다. 승인하면 `--no-verify`.
- **⑤ `~/.claude/hooks/ojeong-guard.cjs`** — `RUNNER` 가 `^python3` 로 앞머리를 고정해 **환경변수로
  시작하는 이 저장소의 정식 테스트 명령을 검증으로 한 번도 안 센다.** 고칠 내용은
  `~/.claude/hooks/guard.test.js` 가 명세한다. 분류기가 그 파일 쓰기를 막아 내가 못 고친다 —
  나를 제약하는 가드를 내가 푸는 것은 권한 상향이라 우회하지 않는다.
- **⑥ `docs/digest.md` 211줄**(상한 200) — **561 이 산술로 확인: 승인으로도 안 풀린다.** 유일한
  레버(「오래된 완료 항목부터 지운다」)가 닿는 줄이 **넷**뿐이라 다 지워도 **207** 이고, 남는
  175줄은 규칙이 남기라 한 절과 **탐색이 입력으로 읽는** 후보 절·사람 대기열이다(상세는
  `history_current.md` 반복 561). `tests/test_docs.py:420-424` 가 **일부러 안 재는** 축이다 —
  처방이 삭제라 못을 박으면 야간이 스스로 못 푸는 RED 가 된다. **아침 처방**: 「지우게 해
  달라」가 아니라 「상한 200 이 이 파일 모양에 안 맞으니 레버나 숫자를 고쳐라」.
- **⑦ 정지 조건이 `session_id` 를 안 본다** (557 최초 · 558~561 · **562 가 여섯 번째로 재현하며
  두 가지를 새로 쟀다**) — `~/.claude/.context-state.json` 은 **모든 세션이 공유하는 파일 하나**고
  statusLine 이 매 렌더 덮어쓴다. 신선도 검사는 `updated_unix` 만 보고 누가 썼는지를 안 본다.
  **(ㄱ) 신선도 검사는 스스로 통과로 뒤집힌다.** 562 는 한 세션 안에서 두 상태를 다 봤다 —
  11:12 에 **54분 낡음**(탈락)이던 파일이 11:18 에 **신선**(통과)해졌는데 `ctx` 83 · 5h 30 ·
  7d 55 · `session_id` 가 **한 바이트도 안 바뀌었다.** 남의 세션이 죽어 값이 굳은 게 아니라
  **살아서 같은 값을 계속 덮고 있다.** 559·560·561 을 잡은 것이 신선도였던 건 **그 세션이
  잠깐 조용했던 덕**이고, 자는 그 우연 위에 서 있었다.
  **(ㄴ) 「내 것인가」는 실제로 알 수 있다.** 처방의 숨은 구멍이 「내 `session_id` 를 어떻게
  아나」였는데, `~/.claude/projects/<cwd 슬러그>/*.jsonl` 중 **지금 쓰이고 있는 것**이 이 세션의
  전사본이다(562: `0ffb5a2f…` · 11:19 갱신). `402da0af…` 는 **다른 슬러그**(`-Users-t2025-m0191`
  = 홈 디렉터리 세션) 아래서 11:17 에 갱신돼 **살아 있는 남의 세션임이 확인됐다** — 다섯 밤의
  「남의 것」 판정이 처음으로 증거를 얻었다. 천장: 같은 프로젝트에서 두 세션이 동시에 돌면
  「최신 전사본이 내 것」이 다시 추정이 된다.
  처방은 그대로(내 것이 아니면 **statusLine 이 꺼진 것과 똑같이** 취급)이고 이제 **구현
  가능**하다. 고칠 자리가 SKILL.md 의 «나를 멈추는 룰»이라 밤이 안 연다 — ⑤ 와 같은 이유다.
  **⑧ 의 `ctx` 칸은 이미 오염됐다** — 562 의 jsonl 줄이 싣는 83 은 **남의 세션 사용량**이다
  (스크립트가 그 파일을 직접 읽으므로). 측정 불가가 아니라 **남의 값을 내 밤에 적는** 것이다.
- **⑧ `metrics.jsonl` 의 `iterations` 가 밤마다 다른 것을 센다** (560 최초 실측). 스크립트는
  `status.md` 의 `night_iterations` 를 그대로 적는데(`metrics-record.js:48`), SKILL.md:226 의
  「야간 시작 시 0 으로 리셋」이 **세션 시작인지 그 밤의 첫 세션인지를 안 정한다.** 그래서
  08-24~08-29 세션들은 안 리셋해 **그 밤의 누적**을, 09-08 이후는 세션별을 적었다(배열은
  `metrics.md` 「평균 반복」 칸). **합이 626 인데 누적 반복은 559 다** — 넘을 수 없는 값을
  넘었으니 적어도 한쪽은 제가 말하는 것을 안 센다. 손목록은 **37줄인데 jsonl 은 55줄**이라
  그 목록으로 낸 「평균 반복 4.7」도 근거가 없다. **표의 용도가 「임계값을 고칠 근거」라
  이게 곧 그 근거의 품질이다.** 처방: 리셋 단위를 세션으로 못박고 줄에 세션 식별자를 넣는다
  — 빈손 밤에는 변하는 칸이 ctx 뿐이고 그 ctx 가 ⑦ 로 굳어 **세션들이 바이트까지 같은 줄로
  납작해진다.** 고칠 자리가 공유 스크립트·SKILL.md 라 ⑤·⑦ 과 같은 이유로 밤이 안 연다.
  **562 가 세 보니 「두 줄」이 아니었다** (`sort | uniq -c`) — 09-13 **4줄이 한 값**(그 밤 세션
  전부) · 09-12 **3줄**이라 **최근 8줄이 값 둘뿐**이고 빈손 구간의 세션 해상도는 **0** 이다.
  **새 위험은 「세다」가 아니라 「지우다」다** — 세션을 구별하는 유일한 성질이 **줄의 중복도**라
  `sort -u` 한 번에 세션 수가 소리 없이 줄고 복원할 수 없다.
  **같은 표의 「정지 사유」도 같이 깨져 있다** — jsonl 53줄이 **스키마 두 가지**(32줄은 `stop`
  칸이 없고 21줄만 싣는다)라 표가 아는 것은 세션의 **40%** 고, 아는 구간에서도 `계획 소진`
  표 3/jsonl 7 · `컨텍스트 상한` 표 6/jsonl 2 로 **4건이 두 칸 사이에서 뒤바뀌어 있다.**
  그 두 칸이 아침에 반대 처방을 낸다(「게이트를 열어라」 vs 「그냥 다시 불러라」).

병합은 손으로 조립하지 않는다 — `scripts/merge-to-main.sh` (전수가 초록일 때만 민다).

## 규모 축 (2026-09-13)

`pages` **52,172** · `docs` **40,347** · 2.48GB · 전수 **763 OK**.
필수 읽기 **599줄**/600 (`project.md` 249 + `status.md` 126 + `history_current.md` 224) —
회전분은 `history_083.md`(반복 532~551). `project.md` 는 제 예산 60 의 4배지만 「거의 안 바뀌는 사실」이라 안 건드렸다.

**다음 세션이 먼저 밟는 못은 회전이 아니라 읽기 상한이다** (562 실측). `history_current.md` 는
**224/300** 으로 회전까지 세 밤쯤인데 읽기 합계는 **599/600** 이라 **빈손 한 밤이면 넘는다**
(빈손은 history·status 를 함께 늘린다 — 562 가 +30·+24, 그래서 562 는 제 몫만큼 ⑥·⑧ 의 닫힌
서술을 줄였다). 다음 밤의 첫 일은 ① 300 전 조기 회전(이탈이니 사유를 적는다) 또는 ② 닫힌
서술 줄이기(선례 `3e8f82c` 721→448) 중 하나다. **회전은 digest 를 건드려 ⑥ 을 악화시키고 핀을
깨므로 ② 가 싸다.** **넘긴 채로 계속하는 것만 고르지 않는다** — 매 반복이 제 입력을 못 읽는다.
