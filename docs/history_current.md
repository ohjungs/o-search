# 기록 (현재)

### 반복 508~510 — 계획 88 `frontier-prime` · 받다 만 URL 을 되찾는다

- **계획 87 이 남긴 문제를 닫았다.** 프런티어가 메모리라 부모가 신선하면 자식이 영영
  큐에 못 들어와, 86 의 수정이 실물 3,807개에 **아무 일도 안 했다.**
- **새 표를 안 만들었다.** 87 은 정답을 프런티어 영속화로 적었지만, 관측된 문제에 한해선
  필요 없다 — **그 URL 들은 이미 `pages` 에 있고** 판정자(`is_fresh`)도 있다. 없던 것은
  그 판정을 **거꾸로 물어보는 길**뿐이라 `unfinished()` 가 **같은 조건을 부정해서** 쓴다
  (술어 두 벌이면 언젠가 갈린다).
- **실물: 시드 하나로 60장을 되찾았다** — 429 행 3,807 → **3,747**, 정확히 60 감소.
  시드는 신선해 스킵됐고 큐를 채운 것은 전부 `unfinished()` 다.
- **범위를 안 넓히는 것을 계약으로 못박았다** — `--same-site` 는 「여기서 나가지 마라」
  이고, 되찾기가 그걸 넘으면 **사용자가 막은 곳으로 요청이 나간다**(크롤 윤리 축).
  `add` 가 `_scope` 를 봐서 자동으로 걸리지만 **자동으로 걸린다는 게 계약이라는 뜻은
  아니다.**
- 상한 1,000(미완 10만을 다 넣으면 크롤이 아니라 재시도 배치다) · 오래된 순 ·
  시드보다 뒤. **아직 못 푸는 것**: `pages` 에 한 번도 안 들어간 발견 URL 은 여전히
  잃는다 — 진짜 영속화의 몫이다. 전수 **731 OK rc 0**.

### 반복 511~514 — 계획 89 `backoff-recovery` · 백오프가 되돌아온다

- **백오프가 한 방향 톱니였다.** 계획 84 가 429 벌점을 `set_delay` 로 걸었는데 그것은
  robots 를 지키려고 **일부러** 단조 증가로 만든 자리다 — 서버가 멀쩡해져도 영영 안
  돌아오고, 위키미디어처럼 순간 429 가 몰리면 **그 한 순간이 크롤 전체를 벌한다.**
- **벌점을 `_penalty` 로 분리**했다. `_delays` 는 robots 값이라 단조 증가, `_penalty` 는
  우리가 매긴 값이라 오르내린다. `interval()` 이 `max` 로 읽어 **회복이 예의를 깎을 수
  없는 구조**다. 손을 떼는 길(`MAX_DELAY` 초과 시 폐기)은 그대로 남는다.
- **실물이 누출을 잡았다.** 분리해 놓고도 첫 실행은 회복이 안 됐다(8.0초 고정) —
  `set_delay` 가 `interval()`(벌점 포함)을 읽어 **벌점이 robots 칸에 새어 굳었고**
  단조 증가라 `relieve` 가 못 내렸다. **내가 그 위험을 주석에 적어 놓고 그 자리를
  놓쳤다**(「한 칸에 섞으면 둘 중 하나가 반드시 틀린다」). **단위 5건은 전부 초록**
  이었다 — 하나는 벌점만, 하나는 robots 만 보고 **둘이 만나는 자리를 아무도 안 봤다.**
- 고친 뒤 실물: **2.0→4.0→8.0 → 4.0→2.0→1.0** 으로 하한에 안착. 전수 **737 OK rc 0**.
- **테스트 하나를 계약 쪽으로 옮겼다** — 84 의 「`interval()` 이 하한으로 읽힌다」는
  그때의 내부 사정이었고 분리하자 깨졌다. **깨진 쪽이 테스트다**: 재야 할 것은
  「더 안 간다」이고 `add` 가 거부하는지로 본다.

### 반복 515 — 계획 90 `live-plan-gap` · 버려진 계획을 아무도 안 물었다 (계획 phase)

- **읽던 문서 자신이 근거였다.** `status.md` frontmatter 는 `backoff-recovery 2/2` 인데
  본문은 계획 85 를 설명하며 「개발 3/3」을 시켰다. `iter-third-witness` 가 2/3 에서
  버려져 `index.md` 의 계획 행 68개 중 **유일한 「진행」**으로 4계획을 남았고, 전수 737건은 초록이었다.
- **문서 가드 여덟 벌이 전부 `status.plan` 이 가리키는 행만 본다** — 뒤에 남겨진 행은 시야 밖.
- 실물 정정: 그 행을 `보류(미완)` 로(완료라 안 적는다 — 스텝 3/3 은 실제로 안 했다),
  재개 근거를 `digest ## 보류` 에, `status.md` 본문 재작성.

### 반복 516 — 계획 90 개발 1/2 · 자를 세우고 이빨을 실물 다섯 트리로 쟀다

- `live_gap` + `LivePlanSyncTest`(실물) + `LiveGapTest`(갈래 7). `PLAN_ROW` 에 슬러그
  자리만 열어 재사용(`LIVE_ROW`) — 열 모양이 바뀌면 둘이 함께 움직인다.
- **이빨은 합성이 아니라 정정 전 커밋 다섯이 증명한다** — `f2e8e37`(버려진 그 커밋)부터
  `1b79a74`(89)까지 다섯 다 `iter-third-witness` 를 이름으로 신고한다.
- **거짓 RED 를 과거 전체로 쟀다**: 슬러그 관례 이후(`3f7d1f8..main`) 137커밋 중 빨강
  5건, 전부 그 결함. 오탐 0. 그 앞이 빨간 것은 `plan:` 이 갈래 이름이던 시절이라
  `step_gap` 도 같은 이유로 빨갛다.
- 내 테스트 하나가 틀려서 고쳤다 — 메시지는 대조 대상을 「지금 도는 것은 …」으로
  일부러 적는데 단언이 그 문구까지 금지했다. 신고 목록 쪽만 보게 좁혔다.
- 전수 **745 OK rc 0**. README 단위 건수 737 → 745.

### 반복 517 — 계획 90 개발 2/2 · 변이 3종이 각각 죽는다

- ① `진행` 필터 삭제 5건 · ② 대조를 자기비교로 3건(조용한 초록 방향) · ③ 판정 통째
  삭제 4건. 변이가 실제로 심어졌는지와 **앵커가 유일한지**를 먼저 단언했다.
- **③ 이 그 단언에 걸렸다** — `p = PLAN_SLUG.search(status_text)` 가 `step_gap` 과
  겹쳐 두 자리다. 안 세었으면 `step_gap` 을 변이시키고 `live_gap` 을 쟀다고 적을 뻔했다.
- `digest ## 반복 실패` 등재. 일반화: **「지금 도는 것」만 재는 자는 «도는 것이 바뀌는
  순간» 뒤에 남은 것을 구조적으로 못 본다** — 문서 가드 여덟 벌이 전부 그 모양이었다.
- 전수 **745 OK rc 0**.

- **앞 반복이 이 파일의 본문 세 절을 커밋에서 지웠던 것을 되돌렸다.** `status.md` 를
  `index("## 다음 스텝")` 으로 잘랐는데 그 머리 문구가 산문에 인용돼 있어 슬라이스가
  거기서 시작했다. 전수는 745 OK 였다 — 절 구성을 보는 자가 0개다. digest 등재.

### 반복 518 — 계획 90 테스트 2/2 · 넓어지는 변이 둘이 살아 있었다

- 갭 ⑥(커버리지): 형제 가드는 정규식을 못박는 `*PatternTest` 를 갖는데 `LIVE_ROW`·
  `LIVE_MARK` 에는 없었다. 심어 보니 **A `^` 앵커 삭제 0건 · C 상태 칸 `in` 비교 0건**
  으로 745건 전부 통과 — 둘 다 조용히 넓어지는 변이다.
- 닫은 것은 새 가드가 아니라 **픽스처 두 줄**이다(비고 칸이 행을 인용하는 꼴 · 「진행」을
  품는 `진행(재개)`). 재변이 4/4/1/4 로 전부 죽는다. 전수 **747 OK rc 0**, README 갱신.
- **C 는 내가 「여기서 그것을 잰다」고 docstring 에 적어 둔 자리였다** — `보류(미완)` 은
  「진행」을 안 품어 우연히 통과할 뿐이다. digest 등재: 주장은 참인 범위까지만 적는다.
- 8점 미만 등재: 열이 줄면 `LIVE_ROW` 가 행을 건너뛰고 `verdict_gap` 에 기대는데 그
  기댐을 재는 자가 0개다.

### 반복 519 — 계획 90 리뷰 · 백지 패스가 커밋 자신의 위반을 잡았다

- 패스 A 를 별도 세션에 넘겨 8건. **일곱을 실측 확인해 자동 수정, 하나는 등재.**
- **[R90-1] 이 무겁다** — docstring 이 「비고 칸이 다른 행을 인용하는 습관은 이 저장소에
  있다」고 단정했는데 실물 `docs/index.md` 에 그 꼴은 **0건**(백틱 안 파이프 34줄은
  있다). **바로 앞 반복이 digest 에 등재한 「주장은 참인 범위까지만 적는다」를 그 커밋
  자신이 어겼다.** 실재하는 것은 머리 문구 인용이고 표 행 인용은 예방이라고 고쳤다.
- [R90-2·3·4] `replace` 사슬로 조립한 픽스처를 **찍어 보니 마지막 칸이 안 닫힌 깨진
  행**이었고 「같은 행」이라던 둘은 꼬리 칸이 달랐다 — 리터럴 `PROMOTED` 로 낮춰 셋을
  한 번에 닫았다. [R90-5] 앵커 변이를 무는 것은 테스트가 아니라 `QUOTED` 한 줄임을
  분해 실측(테스트를 빼도 3건 죽음 · `QUOTED` 를 빼면 도로 산다). [R90-7] 은 계획 76
  `[R76-2]` 와 같은 자리(계획서가 README 동반 수정을 안 적었다).
- 판정 4/4/1/4 그대로, 전수 **747 OK rc 0**. 8점 미만 1건은 `strike_gap` 쪽 등재.

### 반복 520 — 계획 90 e2e · 사전결함 2건을 실측으로 갈라냈다

- **새 e2e 파일 0개** — 「없다」를 주장이 아니라 실측으로 세웠다: `git diff --stat be0ddd5 HEAD -- src/ e2e/` 가 **빈손**이라 HTTP·CLI·exit code·스키마 중
  무엇도 어제와 다르지 않다. 대신 실물 트리 복사본에 **사람이 낼 편집 일곱**을 걸었다 — E2·E3·E6 을 `live_gap` 이 물고 E1·E4·E5 에는 조용하다(양방향).
- **대조군 E0 이 8건 빨개서 한 번 되돌아갔다** — `docs/` 와 `tests/test_docs.py` 만 복사해 README·e2e 개수 가드가 없는 파일을 찾은 것이다. 대조군이 초록이 아니면 나머지 여섯 줄의
  차이를 못 읽으므로 추적 트리(`data/` 608MB 제외)로 넓혀 다시 쟀다.
- e2e 22종 중 **20종 rc 0**. `deadline_e2e`·`perf_crawl` 둘은 실패인데 **내 회귀가 아니다** — 처음엔 「동시 실행 부하 탓」으로 짚었다가 **부하를 없애고 단독으로 재니 그대로 실패해
  가설을 폐기했다.** 코드가 같은 과거 트리 셋(`be0ddd5`·`05705b9`·`24fc18d`)을 워크트리로 세워 재니 같은 띠였다: `perf_crawl` 8.88~9.11(기준선 9.0) · `deadline` 은 `be0ddd5` 에서도
  0.880s 로 갈렸다. 기준선을 내리는 것은 `rules/e2e.md` 7절이 금지하므로 **digest `[8]`·`[6]` 으로 등재하고 아침 판단에 올렸다.**
- 그래서 index 판정 칸에 맨 「통과」를 안 적었다 — `통과(사전결함 2건)`. 계획 87·88·89 는 세 줄 다 「통과」인데 그중 `be0ddd5` 가 오늘 여기서 빨간 판을 냈고, **그 세 줄의 근거는
  오늘 재지 않았다.** 전수 **747 OK rc 0** · `src/` 0줄 · 계획서 → `plan_history_069.md`.

### 반복 521 — 계획 탐색 · 근거는 있는데 밤이 못 간다 · 앞 반복의 결론을 뒤집었다

- 탐색 1~3순위를 값싼 것부터 직접 확인: 단위 **747 OK**(실패 0) · 린터 **없음**(`project.md`) ·
  `src/` 의 `TODO|FIXME|HACK` **0건**. 남은 근거는 오늘 밤 e2e 가 주운 실패 2종뿐이다.
- **그 2종을 계획으로 만들기 전에 전제를 쟀고, 전제가 틀렸다.** 앞 반복은 「계획 64 의 10.26 과
  오늘 8.9 사이 어딘가에서 코드가 깎았다」고 적었는데, **그 10.26 을 실제로 기록한 트리**
  (`cc3d01e`)를 세워 HEAD 와 **번갈아** 재니 [차단] 9.01·9.07 대 8.94·9.07 로 **같은 띠**였다.
  같은 분에 교차 측정했으니 기계 상태 차이도 아니다 — **어느 커밋의 회귀도 아니고 이 기계가
  ~12% 느리다.** [열림] 축이 같은 비율로 내려온 것(10.25 → 9.01~9.16)이 처음부터 그 모양이었다.
- 그래서 **이등분 탐색은 할 일이 아니다.** 남은 문제는 회귀가 아니라 **여유 0.5%** 이고,
  처방 셋(재기준선 · [열림] 대비 비율 · 참고용 강등)은 전부 「통과시키려고 낮춤」과 겉모양이
  같아 `rules/e2e.md` 7절에 걸린다. **무엇을 재기로 했는지는 사람이 정할 몫이라 보류한다.**
- 나머지 출처도 비었다 — 살아 있는 `[8]` 후보는 이미 몸에 밴 교훈이거나(변이 심김 단언 등)
  `## 보류`(승인 대기 · 재시도 금지)이거나 깨진 것이 아닌 제품 변경이다. **YELLOW 로 정지.**
- digest `[8]`·`[6]` 과 `docs/e2e/live-plan-gap/result.md` 3-1 을 실측대로 고쳐 적었다.

### 반복 522~523 — 짧은 경로 `mock-blindspot` · digest 를 상한 안으로 + 오늘의 교훈 등재

- **digest 가 223줄로 상한 200 을 넘었다.** 정리 기준은 계획 `digest-cap-verdict` 의
  선례를 따랐다 — **닫힌(취소선) 후보 11줄**(원본은 `index.md` 계획 행과
  `plan_history_*.md` 에 있다)과 **판단 없는 회전 기록 6줄**(명부가 파일 이름을 영구
  보관한다)을 걷어 **207줄**. 「취소선 규약」이 영구 보존을 뜻하지는 않는다 —
  닫힌 것은 닫힌 것이다.
- **정리가 숨은 결함을 드러냈다** — 회전 기록을 지우자 `ArchiveIndexTest` 가
  `history_078.md`·`history_079.md` 가 **명부에 없다**고 물었다. 회전 기록이 그것을
  가리고 있었던 것이다. 명부에 등재했다.
- **오늘의 교훈을 `[8]` 에 합쳐 등재했다**(줄을 안 늘린다). **하루에 네 번 났다 —
  전부 「로컬 목·작은 표본은 초록, 실물은 틀림」이다**: ① 저장 외삽 4.4배 ② 한 서버에
  초당 6건(같은 IP) ③ 계획 86 이 실물에서 아무 일도 안 함 ④ 벌점이 robots 칸에 누출.
  **②③④ 는 전부 «둘 이상이 만나는 자리»** 이고 단위 테스트는 구조적으로 한 번에 하나만
  본다. **처방은 규율이 아니라 절차다** — 바깥과 만나는 축을 고쳤으면 **실물 한 번을
  e2e 완료 기준에 넣는다.** 전수 **747 OK rc 0**.

### 반복 524~526 — 계획 90 `rejection-visible` · 자를 되살리고, 실물을 치던 테스트를 잡았다

- **계획 84 가 문제를 고치면서 자를 같이 없앴다.** 429 를 `pages` 에 안 박는 판단은
  옳은데, 그 순간 **이 축의 관측이 통째로 사라졌다** — 회수 크롤 뒤 「백오프가 얼마나
  일했나」를 재려니 DB 증거가 0건이고 크롤 로그 3줄이 전부였다. 어제 1만 크롤은
  `429 3,686건` 이라는 숫자가 있어서 **잴 수** 있었다. **안 박는 것과 안 세는 것은
  다르다** — 저장은 「이 URL 은 이렇다」는 주장이고, 세는 것은 「이번 실행에 이런 일이
  있었다」는 사실이다. 요약에 `거절 M건(429)` 을 넣었다(**0 은 안 찍는다**).
- **이 계획이 훨씬 큰 것을 잡았다 — 테스트가 실물 코퍼스를 쓰고 있었다.**
  `crawl.main` 은 DB 경로를 안 받아 기본값이 곧 사용자 코퍼스다. `main` 호출 테스트마다
  `data/crawl.db`(0.94GB·14,160행)를 열고, 계획 88 의 되찾기가 들어간 뒤로는 **미완 URL
  1,000개**를 읽어 왔다. **전수 20초 → 267초**가 그 자국이다.
- **느린 것보다 나쁜 것은 테스트가 사용자 데이터에 행을 넣는다는 사실이다.**
  `setUpModule` 에서 `DEFAULT_DB` 를 임시 경로로 씌워 **모듈 하나로** 막았다
  (개별 테스트에 맡기면 새 테스트가 생기는 날 빠진다). **267초 → 20.25초.**
- **왜 못 봤나**: `-b` 가 출력을 삼켰고, 계획 88 전에는 열기만 해서 느리지 않았다 —
  **성능 회귀가 결함을 드러낸 것**이지 성능이 문제였던 게 아니다. 전수 **749 OK rc 0**.

### 반복 527 — 계획 탐색 · 버려진 계획 85 를 재개했다

- **1~4순위가 전부 비었다** — 전수 749 OK rc 0 · 린터 없음 · `src/` 의 `TODO|FIXME|HACK`
  0건 · `candidates.md` 없음. 5순위 `digest ## 보류` 의 둘 중 하나가 갈 곳이었다.
- **보류 2건은 같은 무게가 아니다.** `userinfo-leak` 은 사람의 승인을 기다리는 보안
  패치라 밤이 못 댄다. `iter-third-witness` 는 **승인 대기가 아니라 그냥 안 끝난 것**이다 —
  계획 90 이 `진행`→`보류(미완)` 으로 정정했을 뿐 사람이 정할 것은 처음부터 없었다.
  남은 일은 변이 3종 + digest 한 줄이라 `src/` 무변이고 야간 금지 목록에 안 걸린다.
- **스텝 1·2 가 살아 있는 것을 실측으로 확인하고 재개했다** — `iter_gap` 이 셋째 인자를
  받아 `HIST_ITER` 의 **최댓값**을 물고(`tests/test_docs.py:147`) `IterGapTest` 갈래
  6개가 그것을 밟는다. 안 한 것은 **그 자에 이빨이 있는지 재는 일**뿐이다.
- **브랜치가 다섯 계획 뒤처져 `main` 에서 다시 땄다.** 옛 위치가 `main` 의 조상인 것을
  `git merge-base --is-ancestor` 로 확인하고 옮겼다 — 잃은 커밋이 없다.
- **덤으로 하나 쟀다 — `status.md` 본문이 다섯 반복 뒤처져 있었다.** frontmatter 는
  `526 · rejection-visible` 인데 본문은 계획 90 `live-plan-gap` 과 반복 515~521 을
  설명하고 있었다(`b671982`·`c70bf5b` 둘 다 `docs/status.md | 6 +++---` — frontmatter
  3줄만 고쳤다). **계획 90 의 `live_gap` 은 이것을 구조적으로 못 잡는다** — 그 자는
  표의 칸을 읽고 **산문을 안 읽기로 한 결정**이 docstring 에 적혀 있다(거짓 RED 의
  원천이라서다). 오늘은 파일을 통째로 다시 써서 닫고, **재는 자를 세울지는 digest
  후보로 등재해 사람에게 넘겼다** — 산문 매칭은 `verdict_gap`·`live_gap` 이 두 번
  일부러 피한 방향이라 밤이 혼자 뒤집을 것이 아니다.
- **statusLine 이 꺼져 있다** — `.context-state.json` 이 시작 시점에 이미 618초 묵었다
  (상한 600). 컨텍스트 게이지를 못 믿으니 이 밤은 **반복 상한에만 의존한다**.

### 반복 528 — 계획 85 개발 3/3 · 변이 4판으로 셋째 증인의 이빨을 쟀다

- **계획서가 적은 「변이 3종」의 ③ 이 두 가지로 읽혀 4판을 돌렸다.** 「범위 `?:` 제거」가
  문구 그대로(`?:` 만 떼어 캡처 그룹으로)와 그 취지(범위 처리를 통째로 제거)로 갈리는데
  **완전히 다른 자리를 친다.** 고르는 대신 둘 다 쟀다.
  ① 셋째 증인 블록 삭제 **2건** · ② `max` → `heads[-1]` **2건** ·
  ③a `?:` 만 제거 **4건**(errors 3 + fail 1) · ③b 범위 처리 통째 제거 **1건**.
- **③b 만 얇았고 그 1건이 `IterationPatternTest` 였다 — 판정 층은 조용했다.** 원인은
  픽스처다: `IterGapTest.HISTORY` 의 최댓값이 여느 머리에서 나와 범위를 잘못 읽어도
  `max` 가 안 움직였다. **실물이 조용한 이유는 더 얇았다** — 오늘 `history_current.md`
  의 최신 머리가 **마침** 범위가 아니었을 뿐이고 바로 앞 둘(`522~523`·`524~526`)은
  범위다. **이빨이 그날 기록의 표기에 달려 있으면 그것은 자가 아니라 우연이다.**
- **닫은 것은 새 테스트가 아니라 픽스처 한 줄**이다 — 최댓값이 범위 끝에서 나오게
  `### 반복 356` 을 `### 반복 350~356` 으로 바꿔 **③b 를 1 → 3건**으로 만들었다.
  계획 90 이 픽스처 두 줄로 배운 것과 같은 자리다. 단위 건수 무변이라 `README.md` 는
  안 건드렸다. 전수 **749 OK rc 0**.
- **digest 39행 등재 — 적어 둔 처방 자신이 틀렸다는 것이 이 항목의 값이다.** 39행은
  처방을 「마지막 `반복 NNN`」이라 적어 뒀는데 그날 실물의 마지막 항목은 490 이고
  최댓값이 494 였다. 일반화: **등재된 처방은 근거지 설계가 아니다 — 코드로 옮기기 전에
  실물에 한 번 대본다.**
- **변이 규율**: `.git` 없는 `/tmp` 사본에서만 심고(제자리 변이를 훅이 커밋한 5회의
  처방), 매 판 `PYTHONPYCACHEPREFIX` 를 새로 주고, 심긴 것을 먼저 단언하고, 매 판 뒤
  원복을 파일 비교로 확인했다. 러너는 **여덟 판 전부 맨몸**이다.

### 반복 529 — 계획 85 리뷰 · 미실측 한 문장을 재고 나서 승인했다

**리뷰는 이슈 없음이지만, 그 전에 직전 반복의 문장을 쟀다.** 개발 3/3 이 「최신 항목이
범위인 날이었으면 `IterationSyncTest` 가 물었을 것이다」를 안 재고 적어 뒀고, 계획 90 이
등재한 실패가 정확히 그것이다. 변이 ③b + 기록 최신 머리 `527~528` 을 함께 심으니
`IterationSyncTest` 가 「최대 527 ≠ `iteration` 528」로 물었다 — **반사실은 참이다.**
실물 코퍼스도 셌다: `### 반복` 머리 15개 중 **6개(40%)가 범위**라 「오늘은 마침
아니었을 뿐」이 수가 됐다. 실측을 주석으로 옮겼다(`tests/test_docs.py:1085`).
**장비 결함 하나를 정직하게 적는다** — 실패 7건 중 3건(`SpecCitationTest`)은 사본에
`e2e/`·`README.md` 를 안 넣어 난 것이고 발견이 아니다. 판정에 쓴 것은 네 건뿐이다.
리뷰는 [리뷰 예산] 항목대로 정적 판독으로 닫았다(코드 12줄·워킹트리 깨끗). 패스 A —
`HISTORY` 를 먹는 갈래 여섯 전부 최댓값 356 그대로라 기존 단언 무영향, `356` 리터럴을
기대하는 diff 밖 소비자 0. 패스 B — 계획 5절 넷 전부 지켰다. 전수 **749 OK · rc 0**.

### 반복 530 — 계획 85 e2e · 트리는 그대로 두고 자만 갈았다 · DONE

계획서가 지정한 증거를 **그날 아침 트리 통째로** 꺼냈다(`git archive 9947049`). 시드·명령·
트리가 같고 갈린 것은 `tests/test_docs.py` 하나뿐이다 — **E0(그날 자) `70 OK` · E1(오늘 자)
`failures=1`**「최대 494 ≠ `iteration` 490」. **E1 이 1건인 것도 값이다**: 오늘 자는 13건
많은데(70→83) 나머지는 그날 문서에 전부 조용했다 — 겨냥한 자리 하나만 물었다. 계획 2절의
「회전은 이 자를 안 깬다」도 쟀다(E2 최신 3항목만 남겨도 `OK`), 항목을 전부 지우면 하한과
**한 줄 신고**가 함께 운다(E3 — `max()` 트레이스백이 아니다). **부분 복사를 안 했다** —
낮에 `SpecCitationTest` 3건이 거짓으로 빨갰던 장비 결함이 있었고 계획 90 e2e 가 같은 자리를
등재했다. 새 e2e 파일 0(`src/`·`e2e/` diff 빈손 · `e2e/*.py` 22개 그대로). 전수 **749 OK**.
계획서 → `plan_history_070.md`, `index.md` 행 `완료 · 통과`.

### 반복 531 — 계획 탐색 · 여덟 출처 전수 실측 0건 → 정지(탐색 막힘)

단위 **749 OK** · e2e **22종 중 21종 `rc 0`** · `src/`·`e2e/` TODO **0** · 린터 없음 ·
이슈 없음 · 보류는 `userinfo-leak`(보안) 하나 · 후보 **84항목 전수 판독**(35개가 「여는
조건」 미충족, 나머지는 교훈·도달 불가·저가치) · 39행은 오늘 계획 85 가 닫았다.
`perf_crawl` 만 `rc 1`(**8.92/s** · 기준선 9.0)인데 후보 `[8]` 이 어제 등재하고 아침에
올린 그 건이라, 기준선을 내리는 것은 「통과시키려고 시나리오를 낮추는 것」이라 안 댔다 —
**오늘 값은 어제 판정에 점 하나를 더한 것**으로만 적는다. **빈손 옆에서 자 하나가 죽어
있는 것을 봤다**: `candidates-scan.js` 가 `## 재발` 을 찾는데 하네스 **자기 템플릿은
`## 반복 실패`** 이고, 앵커를 고쳐 다시 재도 **여전히 0건**이다(`severity`·`기제화` 토큰이
이 저장소 산문에 없다). `check.test.js` 30번이 초록인 것도 픽스처가 `## 재발` 을 써서다 —
**계획 85 가 오늘 배운 그 자리**라 후보 `[6]` 으로 등재하고 **밤이 안 넘겼다**(공유 도구 +
84항목 표기 계약이라 사람 몫).

### 반복 532~534 — 계획 91 `frontier-persist` · 크롤이 실행을 넘어 이어진다

- **계획 87 이 「정답」으로 적고 88 이 절반만 닫은 것을 마저 닫았다.** 88 은 `pages` 에
  **이미 있는** 미완 URL 을 되찾았지만 **큐에만 있다가 종료된 URL** 은 여전히 잃었다.
  컨셉 1단계(10만)는 한 번에 도는 크기가 아닌데 `--max` 로 끊긴 크롤이 다음 실행에
  그 지점을 못 이어받았다.
- **실물: 1회차 수집 3 · discovered 20 → 2회차 수집 5**(시드가 신선한데도 이어받았다).
  **고치기 전 2회차는 0장**이었다.
- **`pages` 에 안 박고 `discovered` 표를 따로 뒀다** — `pages` 는 「받아 본 것」이고
  발견은 수집이 아니다. 거기 넣으면 계획 84·86 이 429 에서 닫은 거짓(**「안 받은 것을
  받았다고 적는다」**)을 다시 여는 것이다. **지우는 자리는 안 만들었다**: 받고 나면
  `pages` 행이 생겨 `unfinished()` 가 알아서 뺀다(지우는 곳이 둘이면 한쪽만 고쳐진다).
- **`Frontier.add` 가 받아들인 것을 돌려주게 했다.** 처음엔 `u in frontier` 로 물으려
  했는데 그 연산자가 없었고 **만들지 않았다** — 부르는 쪽이 범위·중복을 다시 판정하면
  거르는 자리가 둘이 되고 새 호출부가 생기는 날 샌다. 덕분에 계획 88 의 「되찾기가
  `--same-site` 범위를 안 넓힌다」가 여기서도 **공짜로** 지켜진다.
- 전수 **753 OK rc 0**.

### 반복 535 — 짧은 경로 `verdict-head` · 자리 약속을 이름으로 바꿨다

- **어젯밤 「여덟 출처 전부 0건」은 절반이 틀렸다.** 6순위를 「84항목 전수 판독」으로
  닫았는데 `digest.md` 의 후보 절은 **둘**이고 「테스트 phase 갭, 8점 미만」 절을
  안 봤다 — 거기 착수 가능한 `[6]` 이 둘 있었다. 오늘 그중 하나로 돌았다.
- `verdict_gap` 은 「index.md 다섯째 칸이 e2e」라는 **자리 약속** 위에 서 있었고,
  행 수 대조가 잡는 것은 열이 **줄어든** 쪽뿐이었다. e2e **앞에** 열을 끼우면 개수가
  맞아 통과하고 엉뚱한 칸을 판정으로 읽어 **틀린 RED** 를 낸다. 자리 대신 **이름**을
  본다(`VERDICT_HEAD` — 머리의 다섯째가 `e2e` 가 아니거나 머리가 없으면 신고).
- **단언을 어디에 거느냐가 이 스텝의 전부였다.** 새 케이스의 행은 옛 갈래(날짜 칸)로도
  울어서 `assertIsNotNone` 만으로는 **다른 이유로 초록**이다 — `표 머리` 라는 낱말에
  걸어야 이 자를 잰다(`digest` 「부정 단언 하나가 다른 이유로 통과할 수 있다」의 적용).
  TDD RED 출력이 오귀속을 그대로 찍었다: ``e2e 칸에 판정이 아니라 날짜가 있다 … `2026-09-07` ``.
- 곁가지: `test_verification_counts_match_reality` 가 README 단위 수를 물어 753 → **755**.
  전수 **755 OK** · 제품 `src/` **0줄** · 계획서·설계서·e2e 문서 없음(짧은 경로).

### 반복 536~538 — 계획 92 `relink-backfill` · 닭-달걀을 네트워크 없이 푼다

- **코퍼스 14,105장인데 크롤이 0장을 받았다.** 429 행 0(야간이 전부 회수) · 30일 넘은
  행 0 · `discovered` 0 → 시드 12개가 전부 신선 → 팝에서 스킵 → 링크 추출 0 → 큐가
  빔. **계획 91 의 `discovered` 가 막게 돼 있는데 그 표가 비어 있었다** — 영속화가
  들어오기 **전에** 마지막 크롤이 끝났기 때문이다. 채우려면 크롤이 돌아야 하고 크롤은
  전부 신선해서 못 돈다 — **30일을 기다리는 것 말고 길이 없었다.**
- **링크는 이미 우리 손에 있다.** `pages.html` 원문에 `links.extract` 를 다시 돌리면
  된다 — **요청 0건**. 실측 200장 → 38,794개 · 20.8ms/장 · 전체 환산 293초.
- **`frontier.empty()` 로 물었더니 영영 안 돌았다** — 시드가 아직 큐에 있어 거짓이고,
  스킵은 그 뒤 팝에서 일어난다. 「되찾을 것이 없고 시드가 전부 신선하다」가 **한 건도
  안 나가는 상태**의 정확한 표현이다. **평소 비용 0**, 막혔을 때만 한 번.
- **전수 241초를 한 번 오해했다** — 계획 90 과 같은 부류(실물 코퍼스 접촉)를 의심했는데,
  모듈별 합계 39초 · 재실행 62초(CPU 11%)로 **첫 실행의 일회성**이었다. 숫자 하나로
  결론 내지 않고 갈라 잰 것이 값을 냈다. 전수 **758 OK rc 0**.

### 반복 539~540 — 계획 93 `default-db-binding` · 계획 90 의 수정이 실은 안 먹고 있었다

- **파이썬 기본 인자는 정의 시점에 묶인다.** 계획 90 이 `setUpModule` 로
  `crawl.DEFAULT_DB` 를 패치해 「테스트가 실물 코퍼스를 건드리는 것」을 막았다고 믿었는데,
  `def crawl(..., db_path=DEFAULT_DB)` 가 그 값을 이미 고정해 **모듈 속성을 바꿔도
  소용이 없었다.**
- **그때는 안 드러났다** — 실물이 전부 신선해 크롤이 아무것도 안 받았기 때문이다.
  **계획 92 의 링크 되살리기가 그것을 깨웠다**: 테스트 한 번에 실물 DB 로 **1,001행**이
  들어갔고, 그 전 실행에서는 **3,580개 도메인**으로 실제 요청이 나갔다.
  **느린 것보다 나쁘다 — 남의 서버로 요청이 나간 것이다.**
- 처방은 `db_path=None` 으로 두고 **함수 안에서** 해석하는 것. 확인: 전수
  **240초 → 20초**, 전수 전후 `pages` **21,168 → 21,168 무변**.
- **계획 90 은 증상(느려짐)을 고쳤지 원인을 못 고쳤다.** 그때 「전수 전후 행 수가
  같은가」를 한 번만 쟀으면 그날 잡혔다 — 이번에 그 단언을 테스트로 남겼다.
- 전수 **759 OK rc 0**.
