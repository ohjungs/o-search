---
signal: GREEN
phase: 리뷰
step: 1/1
attempt: 0
iteration: 411
updated: 2026-09-07
ctx: 74
night_iterations: 3
night_red: 0
night_retries: 0
plan: index-e2e-verdict
---

## 현재 상태

**계획 71 테스트 phase 완료 — 변이 6판을 팠고 다섯이 죽었다. 산 하나는 단언 하나로 닫았다.**
전수 **647 OK**. 다음 반복은 **리뷰 phase**.

## 이번 phase 가 산 것

전부 **저장소 밖 사본**(`mktemp -d`)에서 맨몸으로 돌렸다. 계획서 4절의 M3·M5·M6 에
**범위를 넓히는 축 둘**을 더했다 — 계획 70 테스트 phase 가 「넓어지는 변이가 조용히 산다」를
실측한 자리라 같은 곳을 다시 팠다.

| 판 | 심은 변이 | 결과 |
|---|---|---|
| V0 대조군 | 성한 사본 | `Ran 647` `OK` rc 0 |
| **M3 재발** | 다른 완료 행(`plan_digest-strike-sync`)의 e2e 칸을 `2026-09-01` 로 | **죽음** `failures=1` · 새 슬러그를 이름으로 댄다 |
| **M5 판정 무력화** | `verdict_gap` 몸통을 `return None` 으로 | **죽음** `failures=2` (`VerdictGapTest` 둘) |
| **M6 추출기 무력화** | 행 정규식이 아무 행도 안 물게 | **죽음** `failures=10` · 하한 못과 열 모양 신고가 함께 문다 |
| **넓힘 ①** | `완료` 행 가드 삭제 | **살아남았다 → 닫았다** (아래) |
| **넓힘 ②** | `fullmatch` → `search` (판정 **안의** 날짜까지) | **죽음** `failures=1` |

**살아남은 하나가 이 phase 의 값이다.** `완료` 가드를 지운 변이가 647건을 그대로 통과했다 —
합성 표의 진행 행 e2e 칸이 `—` 라 **날짜가 아니어서 어차피 안 물렸고**, 그래서
`test_running_row_is_not_bitten` 은 아무것도 재지 않고 있었다(`rules/test.md` 3절 ⑥
「기존 테스트가 옛 동작만 덮음」). **진행 행의 e2e 칸을 날짜로 바꾸는 단언 하나**로 닫았고,
같은 변이를 다시 심으니 죽는다. 판정 로직은 한 줄도 안 고쳤다 — 단언만 조였다.

## 검증 (맨몸 · 리다이렉션 0)

```
PYTHONDONTWRITEBYTECODE=1 PYTHONPYCACHEPREFIX=$(mktemp -d) PYTHONPATH=src python3 -m unittest discover -b -s tests
Ran 647 tests in 15.997s · OK · rc 0
```

린트·타입체크 **없음**(`docs/project.md`). `src/`·`e2e/` **0줄** · `docs/specs/` 무접촉 ·
`data/crawl.db` **무개봉** · 바깥 네트워크 0 · `main` 직접 커밋 0.

**갭 탐색 6종을 훑어 새로 연 것은 0개다** — 부정 경로(열 모양 붕괴)는 덮였고, 격리·불안정·
보안 축은 파일을 읽기만 하는 검사라 해당이 없다. 경계값 축의 **빈 칸·`미정`** 은 계획서
8절이 이미 **천장으로 등재**했고 여는 조건(날짜 아닌 비판정이 실물에 나타나는 날)이 안 찼다.

## 다음

**리뷰 phase** (`rules/review.md`) — 이 diff 를 그대로 머지해도 되는지 본다. 특히
① `VERDICT_ROW_HEAD` 로 행 머리를 따로 세는 3줄이 계획서에 없던 추가라는 것
② 하한 못 `45` 가 실물 48 에서 값 축에 안 붙어 있는지
③ 새 주석의 수치(647·실측 문구)가 실제와 맞는지.

**사람 몫으로 남긴 것 하나** — `digest.md` 는 **214줄로 상한 200 을 넘는다**. 지울 수 있는
완료 항목이 여덟 줄뿐이라 산술이 안 되고, **무인 모드는 파일·데이터를 삭제하지 않는다**.
무엇을 버릴지는 아침에 사람이 정한다 — 야간 보고서에 올린다.
