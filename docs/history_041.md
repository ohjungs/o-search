# 반복 기록 아카이브 — 계획 48 `passage-api` 의 개발 7 (반복 265)

<!-- history_current.md 에서 밀려난 원본. 수정·삭제 금지. -->

## 2026-09-02 17:20 | passage-api | 개발 7 | 시도1

- 한 일: **RED 2건을 닫았다 — 문자열로 마크업의 끝을 찾는 것을 그만두고 파서에게 물었다.**
  `src/websearch/extract.py` `_BlockParser.close()` 에 두 줄(`rawdata` 가 `<` 로 시작하면
  버린다) · `src/websearch/indexer.py` `passages()` 의 되감기 줄 삭제(-13줄).
  **코드 +2 / -1줄**(주석 포함 순 -11줄).
- **자리는 ㉠ 도 ㉡ 도 아니었다.** 테스트 4 가 «`extract.py` 냐 `passages()` 냐» 로 물었는데
  `extract.py` 안에 경로가 **둘**이다 — 색인은 `_TextParser`, 문단은 `_BlockParser` 로 간다
  (계획 48 이 처음부터 갈라 둔 자리다). `_BlockParser.close()` 는 ㉠ 의 비용(파싱 한 번)과
  ㉡ 의 범위(색인 무변경)를 동시에 갖는다. 출력으로 확인했다 — 같은 입력에서
  `extract_text` 는 수리 전과 글자 그대로 같고(`'김치찌개 <!-- TODO: <a href=x> 옛 링크'`)
  `extract_blocks` 만 `['김치찌개']` 를 낸다. 계획 48 의 «색인 경로는 새 코드를 안
  지나간다» 불변이 그대로다.
- **㉡ 을 죽인 것은 산수가 아니라 실측이다.** 두 번 파싱은 계수가 **1.88배**
  (0.311 → 0.585 ms/1,000자)라 값 가드가 `35 × 0.66 × 10 = 231ms > 125ms` 로 깨진다 —
  되살리려면 캡을 깎아야 하고 그건 계획 밖의 손잡이다.
- **되감기 줄을 지운 근거는 변이 Y1 이다.** 지워도 되나가 아니라 **지운 줄이 붙들던 것을
  새 줄이 물려받았나**가 물음이었고, Y1(꼬리를 안 버린다)이 네 단언을 한꺼번에 죽여
  답했다. 남겨 두면 손해만 난다 — `<p>a < b` 에서 `html.parser` 는 브라우저와 같은 판정으로
  `<` 를 데이터로 내는데 되감기는 «a» 만 남긴다(사양 기능 8 의 «원문에서 읽히는 텍스트»).
- **변이 둘을 심었다**(`.mutation-lock` 아래 · 원복은 초록이 아니라 `git diff -- src/` 로):
  **Y1 넓힘**(꼬리를 안 버린다) → RED **4건**(이번 RED 2 + 컷 안 태그 + 원문이 깨진 문서) ·
  **Y2 좁힘**(무조건 버린다) → RED 1건(잘린 엔티티 `<p>김치찌개 &am` 의 문단이 0개가 된다.
  테스트 4 가 세워 둔 초록 가드가 정확히 이것을 물었다).
- 결과: **단위 564건 전부 통과**(RED 2 → 0 · 기존 561 회귀 0). `e2e` 문단 경로 5종 rc 0 —
  `passage_eval` 정확도 **100.0%(398/398)** · 채택률 99.5% · p95 1.39ms(무변동) ·
  `quality_eval` ko 100% · en 95% · `search_api_e2e` p95 2.18ms · `indexer_e2e` · `design_check`.
  **20종 전수는 e2e phase 의 일이라 안 돌렸다.**
- **프로세스 밖 p95 재측 — 수리는 예산을 한 칸도 안 움직였다**(같은 탐침으로 수리 전/후 ·
  임시 DB · `data/crawl.db` 안 열었다): 위키 인라인 507.2 → 507.1·509.2ms · `<p>가</p>`
  반복 2825.5 → 2820.2·2845.9ms. `/search` 몫을 뺀 **문단 몫 123.2ms(예산의 25%)** 로
  개발 6 의 123.5ms 와 오차 안에서 같다. 총 p95 가 예산을 넘는 것은 **`/search` 가
  대부분**(386ms·2632ms)이고 합성 코퍼스에서만 나는 값이다 — 실물 64문서에서는 1.39ms.
  갈림길 5 옆에 «/search 몫» 축이 하나 더 있다는 기록만 남기고 안 건드렸다.
- 실측: 러너(판정) 호출 **6회 전부 맨몸·단독**(아홉 반복 연속). 탐침 5회는 판정이 아니라
  따로 셌다 — 그중 2회는 `stash`·`cd &&` 로 이었다. 변이 실행에 `python3 -B` 를 안 붙였다
  (판정은 갈리지 않았다 — 두 변이 모두 예고한 단언에서 정확히 죽었다).
  의존성 0(stdlib) · 스키마·마이그레이션 없음 · `docs/specs/` 무변경 · 단위 564건 유지라
  README 카운트 무변동 · 새 파일 0.
- 다음: **리뷰 4.** 먼저 볼 것은 **불변식 문구가 코드보다 넓다** — `extract_blocks` 의
  `" ".join(...) == extract_text(h)[1]` 이 이제 예외 둘(제어문자 블록 · EOF 의 안 닫힌
  마크업)인데 단언으로는 ①만 고정돼 있다. 열린 축 0.489 와 새 «/search 몫» 은 안 건드렸다.
