---
signal: GREEN
phase: 계획
step: 0/2
attempt: 0
iteration: 485
updated: 2026-09-09
ctx: 55
night_iterations: 1
night_red: 0
night_retries: 0
plan: userinfo-leak
---

## 다음 반복이 읽을 것 — 계획 83 `userinfo-leak` 을 열었다. 다음은 **설계**다

**야간 처분이 이미 정해져 있다 — 패치만 남긴다.** 보안 경계라
`SKILL.md` 자동 적용 기준(「보안 관련 — 줄 수 무관 항상 보류」)에 걸린다.
끝까지 구현·테스트하고 초록을 확인한 뒤 `docs/patches/` 로 뽑고 작업 트리를 되돌린다.
**적용된 채로 커밋하지 않는다.**

- **근거**: `digest` 후보 `[high]` 「URL 에 실린 자격증명이 `pages.url` PK 로 저장되고
  검색 결과에 렌더된다」(2026-08-27 반복 115 백지 리뷰). 오늘 실물로 재확인했다 —
  `normalize('http://user:pw@evil.test/p')` 가 **그대로** 나오고,
  `normalize('http://google.com@evil.test/x')` 도 그대로다(피싱 모양).
- **탐색 1~5순위는 0건이었다**: 전수 **709 OK** · `src`·`e2e`·`scripts` `TODO` 0 ·
  `docs/candidates.md` 없음 · `docs/patches/` 없음 · `digest ## 보류` 비어 있음.
- **「승인 대기」를 「손대지 마라」로 읽지 않았다.** `metrics.md ## 야간` 이 이 건을
  승인 대기 셋 중 하나로 세고 있는데, 하네스가 보안에 대해 정한 것은
  **적용 금지**지 착수 금지가 아니다(`SKILL.md` 「패치를 남기는 법」).
  밤 세 번이 「근거 0」으로 멈춘 자리가 여기였다.
- **설계가 필요하다** — 후보가 적어 둔 안이 둘(저장 열쇠에서만 뗀다 / 렌더에서 가린다)이고
  계획이 고른 것은 **셋째 안**(아예 안 받는다)이다. `design.md` 1절 「대안이 2개 이상 갈림」.
  컨셉 갈림길 1순위가 **크롤 윤리**라 「남의 자격증명으로 남의 서버에 인증 요청을
  보내지 않는다」가 저장·렌더보다 앞선다.
- **오늘 가릴 것은 없다**: 실물 `data/crawl.db` 400행 중 자격증명 URL **0건**.
  그래서 렌더 가리기는 「하지 않을 것」이고 여는 조건은 「옛 DB 에서 1건이라도 세어지는 날」.
