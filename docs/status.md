---
signal: GREEN
phase: 테스트
step: 0/1
attempt: 0
iteration: 334
updated: 2026-09-05
ctx: 52
night_iterations: 156
night_red: 2
night_retries: 0
plan: passage-cost-axis 계획 57 (개발 1/1 완료 · 다음은 테스트 phase)
---

# 현재 상태

**계획 57 개발 1/1 을 닫았다 — 재파싱 예산을 지키는 단언이 이제 오늘의 파싱 비용을
실제로 잰다.** 고친 파일은 `tests/test_indexer.py` 하나이고 제품 `src/` 는 0줄이다.

## 무엇이 바뀌었나 — 단언 둘

`test_cap_and_passage_limit_together_stay_inside_the_budget` 안에 설계가 고른 둘이
나란히 선다.

```
① 예산 산술   MAX_PASSAGE_HTML/1000 × 0.95 × PASSAGE_LIMIT ≤ 500   (333ms · 예산의 67%)
② 계수 상한   min(3회 파싱)/캡kB ≤ 1.60 ms/1000자                   (오늘 실측 0.948)
```

① 은 계수 `0.44`→`0.95` · 한도 `500/3`→`500` 이다. 1/3 배정치는 «모양 배수를 흡수하는
자리» 였는데 계수가 이제 캡 최악 모양의 실측이라 흡수할 배수가 없다. ② 는 캡을 채운
최악 모양(`<p>` × 11,666 · 34,998자)을 그 자리에서 파싱해 재고, 상한 1.60 은 부하
실측(min-of-3 1.176)의 **1.36배**이자 오늘 값(0.948)의 **1.69배**다.

## 검증 — 구멍부터 실측하고 변이 셋을 죽였다

변이는 **전부 저장소 밖 `src` 사본**에 심었다(제품 `src/` 는 한 글자도 안 고쳤다).

| 변이 | 옛 단언 | 새 단언 |
|---|---|---|
| M1 `extract_blocks` 머리에 `sleep(0.06)` (2.8배) | `Ran 1 test · OK · rc=0` | `2.975 ms/1000자 > 1.6` · `FAILED` · rc=1 |
| M2 `MAX_PASSAGE_HTML` 35,000→100,000 | — | `950ms > 500` · `FAILED` · rc=1 |
| M3 `PASSAGE_LIMIT` 10→20 | — | `665ms > 500` · `FAILED` · rc=1 |

**M1 의 옛 단언 칸이 이 계획의 근거다** — 파서가 2.8배 느려져도 안 빨개지는 것을
착수 전에 눈으로 봤다.

- 전수 `Ran 605 tests · OK` · rc 0 · 13.6초(② 가 더한 몫 0.1초). 새 테스트 메서드
  0개라 605 가 그대로이고 `README.md`·`tests/test_readme.py` 는 무접촉이다.
- 문서 가드 4종(`IterationSyncTest`·`ArchiveIndexTest`·`DocCitationTest`·
  `tests/test_readme.py`)을 실제로 돌려 판정 줄을 눈으로 봤다 — 전부 `OK` · rc 0.
- `src/` · `e2e/` · `README.md` · `docs/specs/` **무접촉** · `data/crawl.db` sha256
  앞뒤 동일(`85c96744…5bda18`) · 재색인 0 · 스키마 변경 0 · 새 의존성 0 ·
  `--no-verify`·`--force` 0 · `main` 직접 커밋 0.

## 남긴 천장

0.95~1.60 사이는 열려 있다 — 파서가 1.5배 느려지면 ② 는 초록인데 ① 은 예산 정각이다.
조이려면 부하 배수 1.25 를 먼저 줄여야 하고 그것은 스위트를 격리해 돌리는 별건이다.
그때 1.60 을 1.20 으로 내린다(`ponytail:` 주석으로 테스트에 못박았다).

## 커밋이 둘로 갈렸다 — 자동 스냅샷이 코드 변경을 먼저 집어갔다

3시간 자동 스냅샷 잡이 **13:50 에 `823aae8 자동 스냅샷 …` 으로 `tests/test_indexer.py`
한 파일을 먼저 커밋했다**(작업 중간이라 문서는 아직 없었다). 되돌리려면 `git reset` 이
필요한데 그것은 확인이 필요한 명령이고 우회하지 않기로 했으므로, **역사를 다시 쓰지 않고
문서 커밋을 그 위에 얹었다** — 이번 스텝은 커밋 둘이다(코드 `823aae8` + 문서 1개).
아래 「사람 결정 대기」 4번이 경고하던 일이 **RED 가 아닌 자리에서 실제로 일어난 첫 사례**다.
잃은 것은 없고(내용은 그대로) 값은 커밋 제목 하나다.

## 사람 결정 대기

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1).
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 0 일 때만.
3. **반응형 360px 미검증** — 브라우저가 없어 저장소의 아무도 안 그려 봤다.
4. **3시간 자동 스냅샷 잡을 루프 작업 중에도 세울 것인가**(반복 328 의 사고).
   RED 중간을 덮치면 깨진 상태가 원격에 올라간다. `.mutation-lock` 안이 있다.

(반복 332 의 5번 「PR #11 병합」은 **반복 333 이 처리해 닫혔다.**)

## 정지 사유

**GREEN.** 개발이 끝나 다음은 계획 57 테스트 phase 다 — 이 단언이 **못 잡는** 자리를
찾는다(위 「남긴 천장」이 첫 후보다). 새로 쓰는 곳이 아니라 빠뜨린 것을 찾고 전수를
돌리는 곳이다.
