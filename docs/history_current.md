# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**회전 명부는 `digest.md` 의 `## 완료` 절 «아카이브 명부» 줄이 정본이다.**
여기 있던 스물한 회전의 서술(233줄)은 그 줄과 내용이 겹쳤고, 검사가 강제하는 명부도
그쪽 하나뿐이라(`tests/test_docs.py` 의 `ArchiveIndexTest`) **개발 9(반복 269)가 이
자리에서 지웠다** — 회전으로는 300줄 상한을 못 맞추던 세 반복(309 → 372 → 418줄)의
원인이 이 명부였다. **지운 것은 머리말이지 반복 기록이 아니다** — 항목은 여전히
append 전용이고 수정·삭제 금지다. 각 회전의 사유는 `digest.md` 의 같은 줄에, 원문은
`history_<NNN>.md` 에 그대로 있다.

## 2026-09-06 20:10 | readme-shape-cover | 계획 0/1 | 시도0
- 한 일: **계획 66 `readme-shape-cover` 등재** — `docs/plan_readme-shape-cover.md` 신규 ·
  `docs/status.md`(signal GREEN · phase 개발 · step 0/1 · 반복 382 · plan 슬러그) ·
  `docs/index.md` 계획 행 1줄 · `docs/metrics.md` 누적 셋. 앞 반복이 `digest [7]` 에 ①④ 로
  남긴 **README 표 «구조» 축 두 구멍**을 한 계획으로 묶었다 — ① 표에 행을 끼워도 조용하다 ·
  ④ 셋째 칸(측정기 파일 이름)이 실재하는지 아무도 안 잰다.
- 결과: **착수 탐침 4판**(전수 1 · `test_readme.py` 3)으로 **두 구멍이 오늘도 살아 있는 것**을
  실물 파일 편집으로 확인했다 — 기준선 전수 `Ran 623 tests in 15.724s` `OK` rc 0 ·
  사본 무변이 M0 `Ran 6` `OK` rc 0(오탐 0) · U1(행 하나 추가) `Ran 6` `OK` rc 0 **생존** ·
  U2(`quality_eval`→`quality_evals` 오타) `Ran 6` `OK` rc 0 **생존**. 저장소 밖 사본에서
  돌렸고 워킹트리는 무변경, 매 판 `PYTHONPYCACHEPREFIX=$(mktemp -d)` 를 함께 줬다.
  **digest 의 처방 한 줄을 정정했다** — `[7]①` 이 적어 둔 「표 행 수를 `len(QUALITY_BAND)` 와
  대조」는 **7≠9 라 성립하지 않는다**(표는 일곱 행, 밴드는 아홉 — 첫 행과 마지막 행이 각각
  둘을 담는다). 상수 7 을 적는 안은 그것이야말로 거울이라, 방향을 **「각 행이 밴드 하나
  이상에 물리는가」**로 바꿔 적었다. 셋째 칸은 `(README.parent / 경로).is_file()` 이고
  `〃` 는 건너뛰지 않고 윗 행 경로를 이어받는다(건너뛰면 눈먼 자리가 생긴다).
  **기점을 `origin/main` 으로 다시 쟀다** — 사람이 `bff2580` 로 병합해 `QualityBandTest` 가
  이미 `main` 안에 있다(`git diff --stat origin/main HEAD -- tests/test_readme.py README.md`
  빈손). 계획 58 이 「기점은 아카이브 커밋」으로 판단했던 근거는 오늘 안 썼다.
  설계 생략 — 트리거 0(파일 2개 · 새 파일 0 · 인터페이스 0 · 되돌리기 쉬움) · 대안 무분기.
  범위 무접촉: 제품 `src/`·`e2e/` **0줄** · `README.md` 무변 · `docs/specs/` 읽기만 ·
  `data/crawl.db` 무변경 · 재색인 0 · 스키마 0 · 새 의존성 0 · PR #7 무접촉(`gh` 호출 0).
  러너 규율 위반 **0회**(누적 38 유지).
- 배운 것: **보류 항목의 «처방» 은 진단과 같은 신뢰도가 아니다 — 세 번째 사례다.**
  `digest [7]` 이 스스로 「기록된 답을 실행 전에 다시 재라」로 적어 둔 그 자리에서, 오늘
  틀린 것은 값도 진단도 아니고 **한 줄짜리 처방**이었다. 진단(「행이 늘어도 조용하다」)은
  실측으로 그대로 참인데, 같은 항목이 이어 적은 처방은 표 행 수(7)와 밴드 수(9)를 같은
  것으로 놓고 있었다 — **처방을 쓴 순간에는 둘이 같아 보였다.** 계획 phase 가 진단만
  다시 재고 처방을 그대로 받았다면 개발이 성립하지 않는 단언을 짜다 멈췄을 것이다.
- 다음: **개발 1/1** — `tests/test_readme.py` 에 `BandTableShapeTest`(`__main__` 가드 **앞**).
  전수가 623 → **625** 가 되므로 `README.md:104` 의 건수 한 줄을 같은 커밋에서 고친다.

## 2026-09-06 21:00 | readme-shape-cover | 개발 1/1 | 시도0
- 한 일: **계획 66 스텝 1/1 — 표 «모양» 축 단언 둘을 넣었다.** `tests/test_readme.py` 에
  모듈 수준 정규식 둘(`BAND_TABLE` 제목으로 표를 잘라낸다 · `METER_CELL` 셋째 칸의 백틱 경로)
  과 `BandTableShapeTest`(`QualityBandTest` 다음 · `__main__` 가드 **앞**) 를 붙였다 —
  `test_every_band_row_is_covered`(각 행이 `QUALITY_BAND` 정규식 하나 이상에 물리는가) ·
  `test_every_band_row_names_an_existing_meter`(셋째 칸 경로가 `(README.parent/경로).is_file()`
  이고 `〃` 는 건너뛰지 않고 **윗 행 값을 이어받는다**). `setUp` 이 표를 못 자르거나 행이
  0 이면 먼저 죽는다(빈손 위 조용한 통과 차단). `README.md:104` 건수 `623` → `625` 를
  **같은 커밋에서** 고쳤다. 제품 `src/` 0줄 · `e2e/` 0줄 · 고친 파일 둘(+47/-1).
- 결과: **TDD 빨간 줄을 눈으로 봤다** — 클래스만 넣고 돌린 전수가 `Ran 625` ·
  `FAILED (failures=1)` · rc 1 이고 죽은 것이 `test_verification_counts_match_reality`
  (`(623, 21) != (625, 21)`) 였다. 건수를 고친 뒤 **전수 `Ran 625 tests in 15.841s` · `OK` ·
  rc 0**(맨몸 · 리다이렉션 0회 · `PYTHONDONTWRITEBYTECODE=1 PYTHONPYCACHEPREFIX=$(mktemp -d)`).
  변이 **여덟 판**(저장소 밖 사본 · 각 판 `-p test_readme.py` `Ran 8`): M0 무변이 `OK` rc 0
  (**오탐 0**) · U1 행 추가 → `test_every_band_row_is_covered` **1건** · U2 셋째 칸을 없는
  파일로 → `test_every_band_row_names_an_existing_meter` **1건** · U3 제목 개명 → **둘 다**
  (2건) · M0b 실재하는 다른 파일로 개명(`perf_crawl.py`) `OK` rc 0(**정당한 편집을 안 막는다**)
  · R1 행 삭제 → 기존 `test_readme_bands_match_e2e_constants` 2건(**회귀 0**).
- 다음: **완료 기준 4번(U4)이 계획서에 적힌 대로는 성립하지 않았다.** 「`any(...)` 를 참으로
  고정하면 1건 이상 죽는다」인데, 판정을 무력화해도 **README 가 성한 트리에서는 죽을 것이
  없다** — 실측 `Ran 8 · OK · rc 0`. 그 항목이 재려던 것(판정이 실제로 판정하는가)은
  **U4′ = U1 + 판정 무력화**로 다시 쟀고 `Ran 8 · OK · rc 0` 으로 **생존**했다 — U1 에서
  죽은 것이 곁가지가 아니라 커버 단언 자신이었다는 귀속 증명이다. `digest [7]`(「기록된 것을
  실행 전에 다시 재라」)의 **네 번째** 사례이고 계획 65 의 `len(QUALITY_BAND)` 7≠9 와 같은 모양
  이다 — **진단은 옳았고 처방이 그때의 추정이었다.** 집안일: `history_current.md` 287줄 +
  이번 항목이 상한 300 을 넘어 여섯 항목(반복 376~381)을 `docs/history_067.md` 로 회전했고
  `digest.md` 아카이브 명부에 등재했다(계획 64 e2e 가 명부 누락으로 대조군이 빨개진 선례).
  다음 반복은 **테스트 phase** 다.

## 2026-09-06 22:00 | readme-shape-cover | 테스트 1/1 | 시도0
- 한 일: **계획 66 스텝 1/1 테스트 — 개발의 판정을 받아 적지 않고 전수 1판 + 변이 8판을
  처음부터 다시 쟀다**(`rules/test.md` 1절). 제품·테스트 코드 **0줄** — 고친 것은 문서
  다섯이다: `docs/status.md`(phase 테스트 · 반복 384 · 판 표와 갭 절) · `docs/plan_readme-shape-cover.md`
  4절 **완료 기준 4번을 U4′ 로 정정**(원문은 취소선으로 남겼다 — 왜 성립하지 않았는지가
  산출물이라 지우지 않는다) · `docs/digest.md` 두 줄(기존 `[8]` 에 **3회** 덧붙임 · 테스트
  phase 갭 `[4]` 신규) · `docs/history_current.md`(이 항목) · `docs/metrics.md` 누적.
- 결과: **완료 기준 8/8 통과**(4번은 U4′ 로 읽는다). 전수 **`Ran 625 tests in 15.851s` ·
  `OK` · rc 0**(맨몸 · 리다이렉션 0회 · `PYTHONDONTWRITEBYTECODE=1 PYTHONPYCACHEPREFIX=$(mktemp -d)`),
  문서 갱신 뒤 네 동기 가드 확인용으로 한 번 더 돌려 **2회**(예산 2회). 변이 여덟 판(저장소
  밖 사본 · 각 판 `-p test_readme.py` `Ran 8` · 매 판 원본 복원): M0 무변이 `OK` rc 0(오탐 0) ·
  U1 행 추가 → `test_every_band_row_is_covered` **1건** · U4′(U1+판정 `True` 고정) `OK` rc 0
  **생존 = 귀속 증명** · U2 셋째 칸을 없는 파일로 → `..._names_an_existing_meter` **1건** ·
  U3 제목 개명 → `setUp` 에서 **둘 다**(2건) · M0b 실재하는 `perf_crawl.py` 로 개명 `OK` rc 0
  (정당한 편집을 안 막는다) · R1 행 삭제 → 기존 `..._bands_match_e2e_constants` **1건**
  (개발이 잰 2건과 다른 것은 지운 행이 달라서다 — 첫 행만 수치를 둘 담는다). 갭 탐색 여섯
  카테고리에서 **8점 이상 0건**, `[4]` 하나(`〃` 이어받기 가지가 변이로 안 눌린다)를 digest 로.
- 다음: **한 판이 무효였고 그것이 이 반복의 값이다.** 첫 U1 이 `FAILED (failures=3)` 을
  냈는데 그대로 읽었으면 「새 단언이 세다」였다 — 실제로는 심는 파이썬이 `text[:end] + ROW`
  로 **README 뒤를 통째로 잘라** 반 토막을 냈고, 죽은 셋 중 둘은 새 단언과 무관하게 사라진
  `## 검증` 절 때문이었다. 심는 쪽에 `assert new != old` 는 **이미 있었다**: 그것은 «바뀌었나»
  만 재고 «의도한 것만 바뀌었나» 는 안 잰다. 러너 앞에 `diff -u` 한 줄을 붙이니 그 자리에서
  보였고, 고쳐 다시 재니 죽는 것은 목표 하나뿐(`failures=1`)이었다. `digest [8]` 의 **3회**
  로 등재했다 — 1회는 BSD sed 미적용(안 심었다), 2회는 revert 누락(안 지웠다), 3회는 **너무
  많이 심었다**. 다음 반복은 **리뷰 phase** 다.

## 2026-09-06 23:10 | readme-shape-cover | 리뷰 1/1 | 시도0
- 한 일: **계획 66 스텝 1/1 리뷰 — 변이 0판, 정적 판독만.** 같은 스텝에서 앞선 두 시도가
  산출물 없이 48분·39분 정지했고 둘 다 변이 판을 다시 심다가 못 나왔다. 되사려던 판정은
  직전 반복이 이미 산 것(성한 트리 전수 `Ran 625 · OK · rc 0` · 워킹트리 깨끗)이라 이번엔
  README 실물에 `BAND_TABLE`·`METER_CELL`·`QUALITY_BAND` 아홉 정규식을 태워 읽는 판독으로
  같은 축을 덮었다. 고친 것은 문서 넷: `docs/status.md`(리뷰 · 반복 385 · 지적 표) ·
  `docs/history_current.md`(이 항목) · `docs/metrics.md` 누적 · `docs/digest.md`(반복 실패에
  한 줄 등재 · 완료 항목 계획 61 한 줄 제거로 줄 수 유지).
- 결과: **지적 다섯, 8점 이상 0건** → 코드 0줄 · 전수 재실행 0회. [4] `BAND_TABLE` 이 제목
  직후 배치와 콜론 없는 구분 줄을 요구(정당한 표 편집에 빨개짐, 메시지는 명확) · [4] 커버가
  «행마다 하나라도 걸리나»라 일대일이 아니라 **행 복제·기존 문구 재사용은 통과** · [3] 행마다
  개행을 요구해 표가 파일 끝이면 마지막 행 누락 · [3] 이어받은 `path` 실패의 연쇄 · [2]
  `METER_CELL` 의 `$` 가 줄 단위 호출부에서만 옳다. docstring 여섯 주장은 실물과 **전부 일치**
  — 표 3개 중 합격선 표를 골랐고 데이터 행 7, 행별 밴드 인덱스가 `[0,1] [2] [3] [4] [5] [6]
  [7,8]` 로 아홉이 빠짐없이 한 번씩(커버 안 되는 행 0 · 안 걸리는 밴드 0), 측정기 일곱 전부
  실재, `quality_evals.py` 부재, `e2e/*.py` 21 = README 「21종」. 문서 편집 뒤 `-p test_docs.py`
  로 네 동기 가드만 확인.
- 다음: **테스트 phase 가 `[4]` 로 남긴 `〃` 가지는 논리로는 이미 닫혀 있다** — 유일한 `〃`
  행 위가 `e2e/design_check.py` 를 이름으로 적어 `path` 가 차 있고, `〃` 가 첫 행에 오면
  `assertTrue(path)` 가 먼저 죽는다. 없는 것은 그 가지의 **변이 실측** 하나뿐이라 항목은
  digest 에 그대로 둔다. 다음 반복은 **e2e phase** 다.

## 반복 386 — 2026-09-06 · e2e · 계획 66 `readme-shape-cover` 1/1 → **DONE**

**전수 `Ran 625 tests in 15.899s` · `OK` · rc 0(1회) · 대조군 3판 기대대로.**
산출물 `docs/e2e/readme-shape-cover/result.md`. 계획서는 `docs/plan_history_052.md` 로 아카이브.

**예산을 먼저 못박고 들어왔다.** 같은 계획의 리뷰 스텝에서 앞선 두 시도가 변이 실험에
들어가 산출물 없이 48분·39분 정지했다(`digest ## 반복 실패` 2회). 이번 반복은 **전수 1회 ·
변이 2판 · 도구 호출 25회 이내**로 상한을 정하고 그 안에서 기록·커밋까지 도달했다.

| 판 | 편집 | 판정 줄 | 물린 단언 |
|---|---|---|---|
| U0 | 손 안 댐 (성한 원본) | `Ran 8 tests in 0.157s` · `OK` · rc 0 | — (오탐 0) |
| U1 | 표에 여덟 번째 행 끼움 (셋째 칸은 실재 파일) | `FAILED (failures=1)` · rc 1 | `test_every_band_row_is_covered` |
| U2 | 셋째 칸 개명 오타 `quality_eval.py`→`quality_evals.py` | `FAILED (failures=1)` · rc 1 | `test_every_band_row_names_an_existing_meter` |

**착수 탐침(반복 382)에서 U1·U2 는 둘 다 `Ran 6 · OK · rc 0` 으로 생존했던 편집이다.**
계획 66 이 연 구멍이 정확히 그 둘이고, 오늘 각각 1건씩 **서로 다른 단언에서** 죽는다 —
한 단언이 둘을 다 덮어 라벨이 뭉개지는 모양이 아니다. U1 의 셋째 칸을 실재 파일로 둔 것이
그 분리를 산 값이다. 편집은 저장소 밖 `mktemp -d` 사본에서만 했고, 심기 전에 앵커 줄이
정확히 1개인지 먼저 단언했다(`digest [8]` BSD `sed` 거짓 초록 대응 — 오늘은 파이썬 줄 편집).
끝난 뒤 `git status --porcelain` 빈손.

**21종 개별 재실행은 0회로 정했고 그 근거를 실측했다.** `git diff --stat 3566374 HEAD --
src/ e2e/ docs/specs/ data/` 가 **빈손**이라 계획 65 e2e(반복 381)의 21/21 rc 0 이후 프로세스
밖에서 달라진 것이 없다. `ls e2e/*.py` 21 · `data/crawl.db` sha256 `85c96744…5bda18` 무변.
**재실행하지 않은 것을 「통과」로 적지 않았다** — `result.md` 0절이 그 자리의 증거다
(카파시 4번: 실행 없이 통과 기록 금지는 「안 돌렸다」를 숨기지 말라는 뜻이지, 값이 0 인
실행을 사라는 뜻이 아니다).

**이 반복이 새로 안 것 — 예산 상한을 phase 시작에 적는 것이 산출물을 산다.** 앞선 두
시도는 상한 없이 변이를 다시 심다가 못 나왔고, 되사려던 판정은 직전 반복이 이미 초록으로
산 것이었다. 이번엔 「전수 1회 · 변이 2판」을 먼저 적고 **어느 판이 무엇을 새로 사는지**를
정해 두니(U1·U2 는 생존→사망 뒤집기, U0 은 오탐 0) 두 판으로 끝났다. 계획 65 e2e 가 산
「정당한 편집은 안 막는다」 축(U4·U6)은 값 대조 쪽이라 오늘 다시 안 샀다.

**범위**: `src/` 0줄 · `tests/` 0줄 · `e2e/` 0줄 · `README.md` 0줄 · `docs/specs/` 무변 ·
재색인 0 · 스키마 0 · 새 의존성 0(stdlib) · PR #7 무접촉(`gh` **0회**) ·
`--no-verify`·`--force`·`--amend`·`rebase` 0회 · 러너 리다이렉션 0회.
`history_current.md` 상한 아래라 회전 없음. `digest.md` 는 한 줄 더하고 완료 항목 한 줄을 뺐다.

**다음**: 활성 계획 0. 다음 반복은 탐색(`rules/discover.md`)부터.

## 반복 387 — 계획 67 `spec-citation-address` 등재 (2026-09-06 · 계획 phase · 시도 0)

**탐색 8개 출처를 전수로 훑고 근거 하나를 확정했다.** 1순위 실패 테스트 0건(전수
`Ran 625 tests in 15.969s` `OK` rc 0) · 2순위 해당 없음(stdlib 전용) · 3순위 코드
`TODO`/`FIXME`/`HACK` 0건(`tests/test_indexer.py:759` 은 HTML 주석 fixture 문자열이다) ·
4순위 `docs/candidates.md` 없음 · 5순위 `## 판단 필요` 는 전부 승인 대기라 안 건드림 ·
6순위 후보에서 **여는 조건이 실제로 온 것은 `digest [7]③` 하나**였다 · 7순위 반복 실패는
처방이 이미 적용됨 · 8순위 `gh` 0회.

**digest 의 「오늘 셋 일치」를 다시 재니 절반만 참이었다.** `[7]③` 은 사양·README·상수
세 사본의 **값**이 같음을 확인하고 「잠복」으로 넘겼는데, 이번 반복이 **주소**를 쟀다:
`src`·`tests`·`e2e` 의 `concept.md:<N>` 인용 **18건 중 5건이 빈 줄(49행)** 을 가리키고,
**4건은 자기가 따옴표로 옮겨 적은 문구가 그 줄에 없다** — `JS_BUDGET`(`e2e/design_check.py:33`)과
「JS 0KB 계약」(`tests/test_serve.py:918`)의 `:50` 은 51행이 정답이고, `TOP_N`
(`e2e/quality_eval.py:34`)과 `tests/test_quality_eval.py:153` 의 `:22` 는 23행이 정답이다.
맞는 아홉(`:44` ×3 · `:53` ×2 · `:54` · `:59` ×2 · `:22-23`)도 함께 갈라 오탐 0 의
기준선으로 계획서 2절에 적었다 — 틀린 것과 맞는 것이 같은 파일에 섞여 있어 「전부 빨갛다」도
「전부 초록이다」도 배선 오류의 신호다(`digest [6]`).

**digest 의 처방을 정정했다.** `[5]`(예산 `500`)는 처방을 「지역 상수 `BUDGET_MS = 500`
으로 테스트 안을 먼저 묶는 두 줄」로 적어 뒀는데 **그 두 줄은 이 구멍을 안 막는다** —
값을 상수로 모아도 주소는 여전히 아무도 안 본다. 값 축은 계획서 5절로 미루고 주소 축을
먼저 열었다. `[7]③` 이 「사양 축은 사람이 고치는 읽기 전용 문서라 **별도 판단**」으로,
`[5]` 가 「사양-테스트 인용 가드는 **별건으로 판단한다**」로 각각 미뤄 둔 그 별건이다.

**변이는 0판 심었다.** 재려던 것이 「인용 주소가 맞나」라 파일을 읽으면 끝난다 — digest 의
정지 사례 2회(48분·39분, 산출물 0)가 「정적으로 읽으면 끝나는 것에는 변이를 걸지 마라」로
남긴 그 자리다. 전수는 기준선 한 판만 돌렸고 러너에 리다이렉션은 0회다.

**설계 트리거 판정 — 생략.** 새 모듈 0 · 공개 인터페이스 0 · 데이터 구조 0 · 되돌리기
쉬움 · 대안 갈림 없음. 「3개 이상 파일」만 걸리는데 5개 중 넷이 주석/문자열 한 줄 정정이라
설계가 고를 것이 없다. 다음 phase 는 개발이다.

**범위**: `src/` 0줄 · `tests/` 0줄 · `e2e/` 0줄 · `README.md` 0줄 · `docs/specs/` 무변 ·
`data/crawl.db` 무변 · 재색인 0 · 스키마 0 · 새 의존성 0(stdlib) · PR #7 무접촉(`gh` 0회) ·
`--no-verify`·`--force`·`--amend`·`rebase` 0회 · 변이 0판.
`digest.md`·`metrics.md` 무변(항목을 닫는 것은 e2e 몫이고, 탐색 막힘이 아니었다).

**다음**: 계획 67 개발 스텝 1/2 — 주소 축 가드를 먼저 심어 RED 5건을 받는다.

## 반복 388 — 계획 67 `spec-citation-address` 개발 1/2 (2026-09-06 · 개발 phase · 시도 0)

**주소 축 가드를 심었다.** `tests/test_docs.py` 에 `SpecCitationTest` 한 벌.
`src`·`tests`·`e2e` 의 `*.py` 를 훑어 `concept\.md:(\d+)(?:-(\d+))?` 를 전수 수집하고,
`subTest` 라벨을 **파일:줄**로 달아 ① 대상 줄이 사양 길이 안이고 ② 범위 **첫 줄이 빈 줄이
아닌지**를 단언한다. 수집 건수 하한 **≥14** 을 같은 메서드에 넣었다 — 정규식이 좁아지면
0건 수집 위에서 조용히 초록이 되는 자리(계획서 7절 위험 1 · 완료 기준 C7)다.

**RED 를 받고 갔다.** 가드만 심은 판이 **정확히 5건 RED** —
`tests/test_serve.py:914`(`:49-54`)·`:925`(`:49`)·`:955`(`:49`) ·
`e2e/design_check.py:2`(`:49-54`)·`:444`(`:49-54`). 계획서 2절 P1 이 정적 판독으로 예고한
다섯과 **파일·줄까지 같다**. 맞는 아홉(`:44` ×3 · `:53` ×2 · `:54` · `:59` ×2 · `:22-23`)은
한 건도 안 빨개졌다 — 오탐 0 이고, 「전부 빨갛다/전부 초록이다」라는 배선 오류 신호가
아니라는 뜻이다(`digest [6]`).

**인용 쪽 다섯 자리를 고쳤다.** `49` → `50` · `49-54` → `50-54`. 사양 49행은 빈 줄이고
디자인 축은 50~54행이라 인용이 통째로 한 칸 밀려 있었다. **`docs/specs/concept.md` 는
한 글자도 안 고쳤다**(계획서 5절 · `rules/discover.md` 0절). 정정은 스크립트로 하되
줄마다 **앵커가 정확히 1개인지 먼저 단언**하고 바꿨다(`digest [8]`).

**전수 626건 `OK` · rc 0.** 새 단언은 메서드 1개라 625 → **626**. 첫 전수는
`test_readme.py` 의 `test_verification_counts_match_reality` 가 `(625, 21) != (626, 21)`
로 잡았고 — 건수 못이 실제로 문다는 표본이다 — `README.md:104` 「단위 625건」을 626 으로
같은 커밋에서 고쳐 초록으로 갔다.

**변이는 0판이다.** 재려던 것이 「주소가 실재하나」라 파일을 읽으면 끝난다. digest 의 정지
사례 2회(48분·39분 · 산출물 0)가 남긴 「정적으로 읽으면 끝나는 것에 변이를 걸지 마라」를
그대로 지켰다. 전수는 **2판**(가드 단독 RED 1 · 최종 1), 러너에 리다이렉션 0회.

**범위**: `src/` 0줄 · `docs/specs/` 무변 · `data/crawl.db` 무변 · 재색인 0 · 스키마 0 ·
새 의존성 0(stdlib) · PR #7 무접촉(`gh` 0회) · `--no-verify`·`--force`·`--amend`·`rebase` 0회.
바꾼 것은 `tests/test_docs.py`(+43) · `tests/test_serve.py` 3줄 · `e2e/design_check.py` 2줄 ·
`README.md` 1줄.

**다음**: 스텝 2/2 — 인용문 축. 같은 수집기 위에 따옴표 문구가 대상 줄 범위 안에 있는지
단언하고 네 자리를 고친다(`:50` → `:51` ×2 · `:22` → `:23` ×2).

## 반복 389 — 계획 67 `spec-citation-address` 개발 2/2 (2026-09-06 · 개발 phase · 시도 0)

**스텝 2/2 인용문 축을 닫았다 — 개발 phase 끝.** 주소 축(반복 388)이 「그 줄이 있나」를
물었다면 이번은 「그 줄이 **그 항목**인가」다. 사양은 한 항목이 여러 줄이라 한 칸 밀린
주소가 빈 줄이 아니라 멀쩡한 이웃 항목에 착지한다 — 주소 축이 구조적으로 못 보는 자리다.

**가드.** 수집을 `_citations()` 로 빼서 두 테스트가 공유한다(반환에 줄 본문과 인용 끝
위치를 얹었다). 새 단언은 둘: 인용 **뒤**의 따옴표 문구가 대상 줄 범위 안에 실재하고,
인용을 주석으로 단 상수(`^\s*[A-Z_]+\s*=`)의 값 표기 중 하나가 그 범위 안에 있다.
`5.0` 은 사양에 `5문서` 로 적히니 `.0` 을 떼서 정규화한다. 대조 건수 **≥ 5** 하한도
단언한다 — `MIN_HITS` 와 같은 이유로, 추출기가 죽으면 0건 대조 위에서 초록이 된다.
따옴표를 인용 **앞**에서 안 보는 것이 설계의 전부다: `tests/test_serve.py:918` 처럼
인용을 통째로 품은 실패 메시지가 있어 앞을 보면 그 메시지를 사양 문구로 오인한다.

**RED 를 받고 갔다.** 가드만 심은 판이 **3자리 4건** — `e2e/design_check.py:33`(값
`50`·`1024` 가 사양 50행에 없다) · `e2e/quality_eval.py:34`(문구 「상위 10건」과 값 `10`
둘 다) · `tests/test_quality_eval.py:153`(문구 「80% 이상」). 계획서 2절 P2 가 정적
판독으로 예고한 넷과 **파일·줄까지 같다**. 맞는 인용(`:44` ×2 · `:53` · `:54` 등)은
한 건도 안 빨개졌다 — 오탐 0.

**인용 쪽 네 자리를 고쳤다.** `:50` → `:51` ×2(JS 50KB 는 51행, 50행은 「화면은 둘뿐」) ·
`:22` → `:23` ×2(합격선 문구는 23행, 22행은 질의 수). `tests/test_serve.py:918` 은 가드가
못 무는 자리지만 정적으로 틀린 것이 확인돼 같이 고쳤다 — 못이 없는 자리를 남기면
다음 사람이 그것을 근거로 삼는다. **`docs/specs/concept.md` 는 한 글자도 안 고쳤다.**
정정은 스크립트로 하되 줄마다 앵커가 정확히 1개인지 먼저 단언하고 바꿨다(`digest [8]`).

**변이 2판, 둘 다 죽었다.** C4 주소 되돌리기(`:51` → `:50`) rc 1 · C5 인용문만 바꾸기
(「상위 10건」 → 「상위 20건」, 주소는 그대로) rc 1 · 복구 후 `OK`. 계획서 완료 기준
C4·C5 를 그대로 밟았다. C6·C7 은 테스트 phase 몫이다. 정적으로 읽으면 끝나는 것에는
이번에도 변이를 안 걸었다(정지 사례 2회의 교훈).

**전수 627건 `OK` · rc 0.** 새 단언 1개라 626 → **627**. 첫 전수를
`test_verification_counts_match_reality` 가 `(626, 21) != (627, 21)` 로 잡았고 —
두 반복 연속으로 실제로 문 못이다 — `README.md:104` 을 627 로 고쳐 초록으로 갔다.
전수는 **2판**(최종 1 · README 정정 후 1) + 가드 단독 RED 1(전수 아님), 리다이렉션 0회.

**범위**: `src/` 0줄 · `docs/specs/` 무변 · `data/crawl.db` 무변 · 재색인 0 · 스키마 0 ·
새 의존성 0(stdlib) · PR #7 무접촉(`gh` 0회) · `--no-verify`·`--force`·`--amend`·`rebase` 0회.
바꾼 것은 `tests/test_docs.py`(+52/-11) · `e2e/design_check.py` · `e2e/quality_eval.py` ·
`tests/test_serve.py` · `tests/test_quality_eval.py` 각 1줄 · `README.md` 1줄.

**다음**: 테스트 phase — C6·C7 과 문구 축의 하한·「인용 뒤만 본다」 규칙을 변이로 잰다.
