---
signal: GREEN
phase: e2e
step: 1
attempt: 0
iteration: 307
updated: 2026-09-04
ctx: 58
night_iterations: 132
night_red: 2
night_retries: 0
plan: focus-ring-combinator
---

# 현재 상태

**계획 52 리뷰 1/1 완료.** 보고 **3건 · 전부 자동 수정 · 승인 필요 0 · 반려 0.**
값이 있는 쪽은 패스 A 가 낸 [R52-1] 이다 — **이 계획이 닫은 미탐이 괄호 한 겹
안에서는 그대로 산다**(`:is(a:focus-visible > .hint)` 통과, 실측). 고장이 아니라
**안 적힌 천장**이라 코드 주석 + `digest` 등재로 닫았다. 다음은 **e2e 1/1**.

## 보고 3건 (전부 low · 자동 수정)

**[R52-1] 패스 A — 괄호 안의 결합자는 아직 거짓 초록이다.** `_top_level` 은 깊이만
세고 안을 안 읽으므로 `:is(a:focus-visible > .hint)` · `:is(a:focus-visible .hint)` ·
`:where(a:focus-visible + .hint)` 셋 다 **통과한다**(리뷰가 직접 먹여 `('--focus',
'2px')` 를 읽었다). 링은 `.hint` 에 그려지므로 계획 44·49·52 가 겨눈 바로 그 고장이고
다른 것은 **괄호 한 겹 뒤에 있다는 것뿐**이다. 회귀는 아니다(변경 전에도 통과했다).
지적한 것은 **설계서 4절이 「천장은 ponytail 주석으로 코드에 남긴다」 고 적어 놓고
이 갈래를 세 천장 중 어디에도 안 적은 것**이다 — 커밋 제목이 「괄호 밖」이라고
정확히 적었어도, 코드만 읽는 다음 사람은 결합자 축이 닫혔다고 읽는다.
→ `e2e/design_check.py` `_top_level` 에 `ponytail:` 천장 5줄 · `digest` 에 [5] 등재.
**안 고친 이유**: 닫으려면 투명 의사클래스의 인자를 다시 갈라 재귀해야 하고 그것은
계획서 7절·설계서 6절의 «CSS 파서를 안 만든다» 밖이다(제품 CSS 에 괄호 0곳).

**[R52-2] 패스 A — docstring 의 계수가 틀렸다.** 오탐 표 docstring 이 「뒤 **여덟**은
계획 52 가 더했다」인데 실제로 더한 행은 **아홉**이다(★ 여섯 + 앞 compound + 꼬리
공백 + 줄바꿈 낀 쉼표). → `아홉` 으로 고쳤다. 개발 phase 가 설계서 25행/실제 24행을
스스로 적어 둔 것과 **같은 종류**이고, 이 저장소에서 주석의 수는 기록이라 센다.

**[R52-3] 패스 B — 계획서 완료 기준 5 가 4절과 모순이다.** 기준 5 는 「N건 OK
(N > 593)」인데 4절은 「두 표에 **행**을 더한다 · 새 클래스·파일 0」이라 `subTest`
행은 건수를 안 늘린다. 늘렸다면 `README.md` 의 `단위 593건` 과
`tests/test_readme.py` 의 건수 대조가 **같은 커밋에서 RED** 다 — 즉 «593 무변» 이
옳은 값이고 기준 문구가 틀렸다. e2e phase 가 이 기준으로 DONE 을 판정하므로
그 전에 닫는다. → 계획서 5절에 정정 주석(취소선 + 사유).

## 버린 후보 2건 (80점 미만)

- 결합자 집합에 `\r`·`\f` 가 없다 — CSS 공백이지만 `\n` 이 늘 함께 오고 저장소에
  0곳이라 도달 불가. `severity.md` 4절 억제.
- 문자열 지우기 순서 의존 — **테스트 phase 가 이미 `digest [4]` 로 등재**했다(중복).

## 백지 패스 수율 (여섯 번째 측정)

지적 **2건**(둘 다 패스 A) → **확증 2 · 기각 0**, 고친 줄 **9**(주석·docstring).
패스 B 는 **1건**([R52-3] 계획서 모순). 관찰이 또 맞았다 — **값은 구조·계약·기록에서
나왔고 성능·숫자 주장은 이번엔 아예 내지 않았다.** 새로 보이는 것: 이번 [R52-1] 은
«코드가 틀렸다» 가 아니라 **«문서가 약속한 천장이 코드에 없다»** 이고, 그것을 잡은
자는 스위트가 아니라 **리뷰가 직접 먹인 7개짜리 탐침**이다(593건은 이 자리에 눈이
멀어 있고, 멀 수밖에 없다 — 통과가 정답인지 오답인지가 표에 안 적혀 있었다).

## 재측 — 기준선 전부 무변 (자동 수정 3건 뒤)

- 단위 **`Ran 593 tests in 13.562s · OK`**(맨몸·단독) — 건수 무변.
- `PYTHONPATH=src python3 e2e/design_check.py` **rc 0** · `[3]` 축 **16행** ·
  라이트 `--focus on --bg-page` **3.56:1**.
- 이번 phase 가 고친 것은 **주석 5줄 · docstring 한 낱말 · 문서 2곳**이다.
  실행 경로 **0줄** · 제품 `src/` **0줄** · 단언을 낮춘 곳 **0**.
- e2e **전수 21종은 다음 phase 의 일**이다.

## 러너 규율 — 가린 것 0회, 섞은 것 1회

- 러너에 파이프·리다이렉션 **0회**. 누적 31회는 그대로다.
- **`design_check.py` 뒤에 `; echo "rc=$?"` 를 한 번 붙였다** — 가린 것은 없고
  (앞서 맨몸으로 한 번 돌려 전문을 읽었다) 판정 줄도 그대로 나왔지만, 형태가
  digest ⑭ 가 이름 붙인 **«러너를 다른 명령과 한 호출에 섞기»** 라 적어 둔다.
  가리지 않았으므로 31 은 안 올린다.

## 원격을 다시 읽은 값이다

- 브랜치 **`loop/focus-ring-combinator`**(기점 `20ee8d5`). 리뷰 커밋 뒤 푸시하고
  **원격을 다시 읽었다** — 값은 `history_current.md` 리뷰 항목에 적었다.
- 기점을 `main` 으로 안 잡았다 — `origin/main` 은 `687a159`(계획 47)이라
  `README.md` 의 `단위 593건`·`e2e 21종` 단언이 거기서는 RED 다.
- **PR #7**(`loop/merge-48-50` → `main`) **OPEN·미병합.** 병합은 사용자가 처리한다 —
  이 반복은 PR 을 열지도 닫지도, 그 브랜치를 건드리지도 않았다.
- `--no-verify`·`--force` 0회 · 훅 우회 0 · `data/crawl.db`·`docs/specs/` 무변경.

## 승인 대기

1. **`--line` 이 SC 1.4.11 대상인가**(라이트 1.34:1 · 다크 1.27:1).
2. **`--focus` 가 `--bg-button` 위에서 1.45:1**(다크 1.66:1) — `outline-offset` 0 일 때만.
3. **반응형 360px 미검증** — 브라우저가 없어 저장소의 누구도 그 화면을 못 그린다.
4. **회전 규약·러너 규율의 저장소 밖 절반**과 **사양이 남긴 둘**(`specs/concept.md` 의
   `## 사람이 정할 것`).
5. **PR #7 병합** · `loop/hidden-passage`·`loop/focus-ring-combinator` 의 PR 생성 여부.
