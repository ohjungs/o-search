# 아카이브 — 계획 46 `json-contract` 의 e2e 반복 (2026-09-02)

<!-- 회전으로 `history_current.md` 에서 밀려났다. 수정·삭제 금지.
     `digest.md` 의 `## 완료` 절에 1~2줄 요약이 있다. -->

## 2026-09-02 03:51 | json-contract | e2e 1/1 | 시도0

- 한 일: 리뷰가 지목한 세 자리를 **기존 `e2e/search_api_e2e.py` 에 더했다**
  (+20 −1줄 · 154줄). **새 `e2e/*.py` 0개 · 새 의존성 0 · 제품 `src/` 0줄.**
  계획 46 DONE.
- **「DB 없음 → 503」을 상태 코드 표에 한 줄로 못 올린 이유가 이 스텝의 내용이었다.**
  400·404·501 은 인자만 바꾸면 나는데 503 은 **파일을 치워야** 난다 — 서버는 요청마다
  DB 를 새로 여니 프로세스는 그대로다. 표 뒤에서 `os.rename` 으로 색인을 치우고 같은
  질의를 다시 보낸다: **503 · JSON · `version` 1 · 본문에 경로도 `Traceback` 도 없고**
  stderr 에 `색인 없음: FileNotFoundError(…)` 한 줄. 그리고 **되돌려 200 을 다시
  본다** — 안 보면 503 이 «치웠기 때문» 인지 알 수 없다(`digest ## 판단 필요` 의
  *"부정 단언 하나가 다른 이유로 통과할 수 있다"*). 버전은 **200 과 503 양쪽**에서 잰다.
- **대조군 변이 셋 전부 RED**(`.mutation-lock`, 시나리오 5 가 요구한 것):
  **E1** `_send(503,…)`→`_send(500,…)` = *"색인을 치웠는데 500 다"* · **E2** `_send` 의
  버전 삭제 = `KeyError: 'version'` · **E3(좁힘)** 503 호출부만 `{"version": None}` =
  *"503 이 버전을 안 갖는다"*. **E3 을 따로 심은 이유는 E2 가 200 단언에서 먼저 죽어
  503 쪽 버전 단언이 살았는지 못 재기 때문**이다(설계 6절 교훈 ② — 좁히는 변이).
  그 변이는 **리뷰가 지적한 `_send` 의 «payload 가 이기는 병합»을 그대로 이용해**
  만들었다 — 다섯 호출부 중 하나가 `"version"` 을 넘기는 날 무슨 일이 나는지가 이제
  실측으로 있다. 리뷰는 방어를 더하지 않기로 했고 이 반복도 안 더한다, **재기만 한다.**
- **시나리오 2(화면↔JSON 순서)는 e2e 로 안 겹쳤다.** 두 응답을 만드는 것이 같은
  프로세스 안의 `_page_hits` **한 벌**이라 프로세스 경계가 새로 재는 것이 없고,
  반복 239 의 `TestScreenMatchesJson` 이 이미 리스트로 1·2페이지 둘 다 비교한다 —
  리뷰가 드리프트에 내린 *"겹쳐 만들지 않는다"* 와 같은 판정이다. **프로세스 경계가
  진짜로 더 재는 것은 파일 상실뿐이었다.**
- **p95 비교를 스크립트에 안 박았다** — 설계 7절이 판정 ②를 *"눈으로, e2e 문서에
  숫자를 적어"* 로 정했다. 기준선은 규모·기계에 딸린 숫자라 스크립트에 박으면 다른
  기계에서 거짓 RED 가 된다(ponytail 1번).
- **판정 줄을 가린 호출 2회 — ⑭·⑮ 다(정직하게 적는다).** 러너 호출은 스크립트 기준
  **30회**: 판정을 눈으로 읽은 맨몸 28회(`search_api_e2e` 5 · 단위 5 · 나머지 e2e 18)
  + 가린 것 2회.
  파이프는 **`wc -l` 로 문서 줄 수를 세는 명령에 러너를 `&&` 로 이어 붙이면서**
  `2>&1 | tail -4` 를 딸려 보낸 것이고 **`Ran/OK` 가 실제로 잘렸다.** 즉시 맨몸으로
  재실행해 **495건 OK 12.209초**를 눈으로 읽었다 — 그것이 마감 실행이고 정정된 문서
  전부를 읽은 뒤의 것이다. **⑬ 과 결이 다르다**: ⑬ 은 «마감 실행», ⑭ 는 **«러너가 아닌
  명령에 러너를 얹은 것»** 이라 방아쇠가 파이프 자체가 아니라 *다른 목적의 명령에
  얹기*다(`wc -l` 출력을 줄이려던 `tail` 이 러너까지 덮었다). `digest` 의 그 항목에
  ⑭ 로 적고 처방 후보에 *"러너는 다른 명령과 한 호출에 섞지 않는다"* 를 더했다.
  **⑮ 는 파이프가 아니다** — 회전을 마치고 돌린 마감 실행에 잡음을 줄이려고 `2>/dev/null`
  을 붙였는데 **`unittest` 의 `Ran/OK` 가 바로 그 stderr 라** 판정이 통째로 사라졌다.
  ⑭ 를 적고 «이제 파이프는 안 쓴다» 로 옮겨 간 자리에서 **같은 사고가 다른 문법으로**
  났다. 즉시 완전 맨몸으로 재실행해 **495건 OK 12.268초**를 읽었고, ⑮ 를 기록한 문서 넷을
  고친 뒤 한 번 더 맨몸으로 돌려 **12.269초**를 읽었다 — 그것이 마감 실행이다.
  `digest` 항목 이름을 **«러너의 판정 줄을 가린다»** 로 넓히고 처방 후보에
  *"러너 호출에는 출력 조작을 아무것도 붙이지 않는다 — 재지향도 포함"* 을 더했다.
  변이 실행 셋의 `; echo rc=$?` 와 회귀 18종의 `&&` 는 아무것도 안 가렸다.
- 결과: 단위 **495건 OK**(맨몸 5회 — 12.193 · 12.209 · 12.211 · 12.268 · **12.269초**,
  마지막이 회전과 문서 정정까지 끝낸 마감 실행) · `e2e/*.py` **19종 전수 rc 0** · p95 **8.71ms**
  (예산 300ms 의 2.9% · **기준선 8.86ms 보다 낮다** — 판정 ② 통과) · 품질 ko **20/20** ·
  en **19/20** · 매치 평균 **14.0** · 처리량 [열림] **10.22/s** · [차단] **10.21/s** ·
  디자인 **4축** · `data/crawl.db` sha256 `85c96744…` **무변경** · 되돌림은 해시가 아니라
  **`git status`(`src/` 0건) + `python3 -B` 재실행 rc 0** 으로 확인 · 커밋된 변이 0 ·
  `.mutation-lock` 지웠다.
- 기록 갱신: `docs/e2e/json-contract/result.md`(새 파일) · `status.md`(반복 242 ·
  **DONE** · `plan: null`) · `metrics.md`(반복 242 · 계획 30→**31** · e2e phase 22→23) ·
  `index.md`(계획 46 행 진행→**완료** · 확장 분할 9번 완료) · 계획서·설계서를
  `plan_history_032.md`·`design_history_032.md` 로 아카이브. **README 카운트 가드는
  조용하다**(단위 495 · `e2e/*.py` 19개 그대로 — 파일을 안 늘리고 기존 것을 늘렸다).
  **회전 돌렸다** — 이 항목을 붙인 뒤 세니 **301줄**(상한 300)이라 그 자리에서 돌렸다:
  테스트·리뷰 두 반복 → `history_027.md`, **174줄**로 내려왔다. 아카이브 27개 ·
  `digest.md` **200줄 그대로**(명부에 이름을 더하고 회전 줄에 이어 붙였다) · 명부 구멍 0.
- 다음: **계획 phase (탐색).** 사양 확장 절에서 남은 것은 기능 7·8(근거 문단 API)이고
  그것이 계획 47 이다 — 사양 `## 사람이 정할 것` 2번이 정한 순서를 이 계획이 지켰다.
  **계획 47 이 `indexer.py` 를 열면 `digest.md` 의 `[4]` 한 항목을 같이 본다**
  (TOCTOU · 무토큰 질의가 DB 고장 판정을 건너뛰는 갈래). **없는 DB 는
  `os.path.exists` 가 먼저라 이번 계약은 어느 질의든 안 샌다** — 이번 e2e 가 다시 밟았다.
  PR 은 안 열었다(사람 결정).
