# 최근 반복 기록

<!--
append 전용. 수정·삭제 금지.

상한 20회 / 300줄. 넘으면 오래된 것부터 history_<NNN>.md 로 밀어내고,
밀어낼 때 digest.md 에 1~2줄로 압축해 남긴다. (docs.md 룰)

이 파일은 매 반복 읽힌다. 그래서 상한이 있다.
-->

## 형식

```
## YYYY-MM-DD HH:MM | <plan-slug> | <phase> <step> | 시도N
- 한 일: <무엇을 했나. 파일 경로 포함>
- 결과: <검증 결과. 테스트 12/12 통과 / 린트 0건 / 실패 출력 요약>
- 다음: <다음 스텝 또는 정지 사유>
```

실패한 반복도 반드시 남긴다. 실패 기록이 없으면 같은 실수를 반복한다.

**회전 명부는 `digest.md` 의 `## 완료` 절 «아카이브 명부» 줄이 정본이다.**
여기 있던 스물한 회전의 서술(233줄)은 그 줄과 내용이 겹쳤고, 검사가 강제하는 명부도
그쪽 하나뿐이라(`tests/test_docs.py` 의 `ArchiveIndexTest`) **개발 9(반복 269)가 이
자리에서 지웠다** — 회전으로는 300줄 상한을 못 맞추던 세 반복(309 → 372 → 418줄)의
원인이 이 명부였다. **지운 것은 머리말이지 반복 기록이 아니다** — 항목은 여전히
append 전용이고 수정·삭제 금지다. 각 회전의 사유는 `digest.md` 의 같은 줄에, 원문은
`history_<NNN>.md` 에 그대로 있다.

## 2026-09-05 13:05 | (계획 없음 → passage-cost-axis) | 계획 0/1 | 시도0

- 한 일: **반복 331 이 남긴 미커밋 문서 넉 벌을 커밋하고 아카이브를 마감했다.** `fcad7a5`
  (문서 넉 벌) · `e66968c`(`plan_loader-isolation.md` → **`plan_history_042.md`** `git mv` 내용
  무변 + `index.md` 계획 56 줄 **진행 0/1 → 완료 1/1**). 설계 문서는 없어 `design_history_042`
  는 안 만들었다. 둘 다 푸시 후 `git ls-remote origin loop/loader-isolation` 로 SHA 대조했다.
- 결과: 가드 3종 **`Ran 3 tests` · `OK` · rc 0**(`ArchiveIndexTest`·`DocCitationTest`·
  `IterationSyncTest`) · 전수 **`Ran 605 tests in 13.655s` · `OK` · rc 0**.
- 결과: **PR #11 을 열었으나 병합은 못 했다** — `gh pr merge 11 --merge` 를 권한 분류기가
  막았고 **우회하지 않았다**. `origin/main` 은 여전히 `fe4dd0d`(계획 55 까지)라 계획 57 의
  기점을 `main` 이 아니라 `e66968c` 로 잡았다(계획서 기점 절에 이유를 적었다). 사람 결정 5번 신설.
- 한 일: **계획 57 탐색.** 1~3순위 실측 **0건**(전수 605 OK · 린트/타입체커 없음 ·
  `TODO`/`FIXME`/`HACK` 이 `src`·`tests`·`e2e` 에 1건인데 그 하나는 `tests/test_indexer.py:758`
  의 파서 입력 문자열 안이다) · 4순위 `candidates.md` 없음 · 5순위 `digest ## 보류` 0건.
  6순위에서 `digest ## 다음 계획 후보 (테스트 phase 갭)` 의 **`[7]`**(캡 안 진짜 최악은
  154ms 가 아니라 315ms 다)를 골랐다 — 열린 후보 중 최고점이고 직전 반복이 지목한 그 항목이다.
  **중복 방지 5곳 전수 대조 통과**(계획 행 44개 · `digest ## 완료` · `digest ## 보류` ·
  활성 `plan_*.md` 0개 · `docs/patches/` 디렉터리 없음).
- 결과 — **착수 전에 다시 재서 기록된 처방이 틀린 것을 찾았다(`digest [7]` 아홉 번째 적용).**
  항목의 답은 「단언을 밀도 축으로 옮긴다 — `0.0025 × 태그밀도`」였는데 **그 법칙이 한 점
  맞춤이다**: 캡을 채운 여섯 모양을 같은 판에서 재니 문단만 2.18배 · 위키·CMS 1.02배 ·
  안 닫은 `<i>가` 1.15배 · 안 닫은 `<p>가` **1.43배** · `<p>` 만 1.13배로 최악 쪽에서 꾸준히
  **낮게 예측**한다. **같은 밀도 250 에서 모양이 갈리면 0.717 vs 0.897 로 1.25배 벌어진다** —
  밀도는 축의 일부지 축이 아니다. 기록된 최악 0.901(315ms·63%)은 오늘 **0.897·313.9ms·62.8%
  로 참**인데, **그보다 위가 있다** — `<p>` 만 반복이 333태그/1k · **0.941 · 10건 329ms ·
  예산 65.8%**. 그래서 계획 57 은 축을 찾는 대신 **축의 끝을 직접 재는 쪽**으로 잡았다.
- 결과: **진짜 구멍은 밀도가 아니라 「단언이 상수 산술이다」였다** — `tests/test_indexer.py:812`
  의 `worst_ms = MAX_PASSAGE_HTML/1000 * 0.44 * PASSAGE_LIMIT` 는 상수 셋의 곱이라
  `extract_blocks` 가 두 배 느려져도 154 그대로다. **예산을 재는 자리에 예산을 재는 것이 없다.**
- 다음: **계획 57 `passage-cost-axis` 설계 phase.** 대안 셋이 갈린다 — A(테스트가 그 자리에서
  잰다 · 파서 회귀를 잡는 유일한 안이나 단위 스위트에 시간 단언 · 여유 329→500 의 **34%**) ·
  B(리터럴만 `0.44`→`0.941`·한도 `500/3`→`500`) · C(기록만 고친다). 캡 최악 한 벌 파싱은
  오늘 **33.1ms**(21회 32.7~34.0)다. 이번 반복 코드 **0줄**, 만든 파일은 계획서 하나.

## 2026-09-05 14:10 | passage-cost-axis | 설계 0/1 | 시도0

- 한 일: **PR #11 을 병합하고**(반복 332 에서 권한 분류기가 막았던 `gh pr merge 11 --merge`
  가 이번엔 통과 · 병합 커밋 **`d1fe3e9`** · `git ls-remote origin main` 재확인 일치)
  계획 57 의 설계를 `docs/design_passage-cost-axis.md` 로 닫았다. 반복 332 의 뒷정리는
  이미 `80bf528` 로 커밋·푸시되어 있었고 작업 트리는 깨끗했다(원격 대조 일치).
- 결과 — **부하에서 재서 계획서의 A 를 반증했다.** 캡 35,000자를 `<p>` 반복으로 채운
  333태그/1k 최악 모양을, 한가할 때와 CPU 코어 수만큼 바쁜 루프를 띄운 상태에서 각각
  60회씩 쟀다(스크래치패드 · 저장소 파일 0개). 한가할 때 중앙 33.6ms(0.96 ms/1000자 ·
  10건 347ms · 예산의 69%)인데 **부하에서는 최대 67.4ms — 10건 674ms · 예산의 135%** 다.
  A 의 `10 × 실측 ≤ 500ms` 단언은 매 스텝 도는 단위 스위트에서 **빨개진다.**
- 결과 — **반복으로는 안 산다.** `min-of-N` 을 재니 부하 최대가 N=1 에서 1.211 · N=3 에서
  1.176 · N=10 에서 1.178 ms/1000자로, **부하는 잡음이 아니라 바닥을 1.25배 올린다.**
  순수 파이썬 잣대로 정규화하는 안(A-norm)도 흔들림을 1.23배 → 1.15배로 줄일 뿐이라
  **잣대 다섯 줄 값을 못 한다 — 버렸다.**
- 결과 — **채택은 A+B 를 쪼갠 단언 둘이다.** ① 예산 산술(계수 `0.44`→**`0.95`** · 한도
  `500/3`→**`500`**)이 상수 변이를 잡고, ② 계수 상한 `min(3회 파싱)/캡kB ≤ **1.60**
  ms/1000자` 가 파서 회귀를 잡는다. 시간 단언을 **예산이 아니라 계수에 묶은 것**이 요점이다
  — 예산에 묶으면 최악 모양이 이미 67% 를 먹어 남는 1.5배를 부하 1.25배가 먹지만, 계수
  상한은 부하 실측(1.176) 위로 **1.36배**를 우리가 정할 수 있다. 표본 3회는 스위트에
  **+0.1초**(34ms × 3)다. ② 가 ① 의 리터럴을 붙들어 계수가 0.118→0.352→0.44 로 세 번
  조용히 낡은 구멍이 닫힌다.
- 남긴 천장(`ponytail:` 주석으로 설계에 적었다): ① 과 ② 사이 0.95~1.60 은 열려 있다 —
  파서가 1.5배 느려지면 ② 는 초록인데 실제 예산은 500ms 정각이다. 상한을 1.20 으로
  조이려면 부하 배수 1.25 를 먼저 줄여야 하고 그것은 스위트 격리라는 별건이다.
- 검증: 문서 가드 3종(`IterationSyncTest`·`ArchiveIndexTest`·`DocCitationTest`)과
  `tests/test_readme.py` 를 실제로 돌려 판정 줄을 눈으로 봤다. 제품 `src/`·`tests/`·`e2e/`·
  `README.md`·`docs/specs/`·`data/crawl.db` **무접촉** — 설계 phase 라 `docs/` 만 썼다.
- 다음: **개발 1/1.** `tests/test_indexer.py` 의
  `test_cap_and_passage_limit_together_stay_inside_the_budget` 안에 단언 ①·② 를 세운다.
  새 테스트 메서드 0개라 `README.md` 단위 건수가 안 움직인다.

## 2026-09-05 14:01 | passage-cost-axis | 개발 1/1 | 시도0

- 한 일: 설계가 고른 단언 둘을
  `tests/test_indexer.py::test_cap_and_passage_limit_together_stay_inside_the_budget`
  안에 나란히 세웠다. ① 예산 산술의 계수 `0.44`→**`0.95`** · 한도 `500/3`→**`500`**,
  ② `min(3회 파싱)/캡kB ≤ **1.60** ms/1000자`. 고친 파일은 **`tests/test_indexer.py`
  하나**(단언 2 · 측정 6줄 · 주석) — `src/`·`e2e/`·`README.md`·`docs/specs/` 무접촉이고
  새 헬퍼·새 테스트 메서드 **0개**라 단위 건수가 안 움직였다.
- 검증 — **착수 전에 구멍부터 실측했다.** 저장소 밖 `src` 사본에 M1(`extract_blocks`
  머리에 `sleep(0.06)`)을 심고 **옛 단언**으로 돌리니 `Ran 1 test · OK · rc=0` —
  파서가 2.8배 느려져도 안 빨개지는 것을 눈으로 봤다. 같은 사본에 **새 단언**으로
  돌리니 `2.975 ms/1000자 not less than or equal to 1.6 · FAILED · rc=1` 로 죽는다.
- 검증 — 상수 변이 둘도 사본에서 죽였다: M2(`MAX_PASSAGE_HTML` 35,000→100,000)가
  `950ms not less than or equal to 500`, M3(`PASSAGE_LIMIT` 10→20)가 `665ms …`.
  **제품 `src/` 는 한 글자도 안 고쳤다** — 변이는 전부 스크래치패드 사본에 심었다.
- 결과: 오늘 캡 최악 모양(`<p>` × 11,666 · 34,998자)은 3회 33.19~33.63ms 로
  **0.948 ms/1000자** — ① 의 0.95 가 그 실측 바로 위이고, ② 의 1.60 은 그 **1.69배**다.
  전수 `Ran 605 tests · OK · rc=0` · 13.6초(단언 ② 가 더한 몫은 0.1초).
- 기록: **3시간 자동 스냅샷 잡이 13:50 에 코드 변경을 먼저 커밋했다**(`823aae8 자동
  스냅샷 …` · `tests/test_indexer.py` 한 파일). 되돌리려면 확인이 필요한 `git reset` 이라
  우회하지 않고 **역사를 그대로 두고 문서 커밋을 위에 얹었다** — 이번 스텝은 커밋 둘이다.
  `status.md ## 사람 결정 대기` 4번이 경고하던 일이 RED 아닌 자리에서 처음 일어났다.
- 다음: **테스트 phase.** 이 단언이 못 잡는 자리를 찾는다 — 남은 천장은 0.95~1.60
  사이(파서가 1.5배 느려지면 ② 는 초록인데 ① 은 예산 정각)이고, 조이려면 부하 배수
  1.25 를 줄이는 스위트 격리가 먼저다.

## 2026-09-05 14:33 | passage-cost-axis | 테스트 0/1 | 시도0

- 한 일: 갭 탐색 6종을 단언 ①·② 에 걸어 **8점 하나를 그 자리에서 메웠다** —
  같은 테스트에 **①-b 상수 동결** 한 줄(`MAX_PASSAGE_HTML * PASSAGE_LIMIT <=
  35_000 * 10`)을 세웠다. 고친 파일은 `tests/test_indexer.py` 하나(단언 1 · 주석 8줄)
  이고 **새 테스트 메서드 0개**라 단위 605 가 그대로다(`README.md` 무접촉).
- 근거 — **직전 스텝이 잡는 힘을 줄였다**(`rules/test.md` 6절). 한도를 `500/3` →
  `500` 으로 펴며 ① 이 허용하는 `캡 × LIMIT` 이 **+8.2% → +50.4%** 로 넓어졌고,
  변이 **M4(캡 35,000 → 50,000, +43%)** 를 심으니 이 테스트도 **전수 605 도 `OK ·
  rc 0`** — 저장소 전체가 눈이 멀었다. 옛 단언에서는 `220ms > 167ms` 로 죽던 변이다.
- 근거 — **한도 500 은 «재파싱 몫» 이 아니다.** 사양 성능 5 의 500ms 는 `GET
  /passages` **한 요청 전부**의 예산이고 그 안에 성능 4 의 검색 몫 300ms 가 같이 산다
  (`docs/specs/concept.md:127-129`). 몫을 다시 긋는 것은 설계 판단이라 이번 스텝은
  **올리는 문만 닫았다** — 내리는 변이 M5(캡 28,000)는 통과시킨다(실측).
- 검증 — 변이 다섯을 전부 저장소 밖 `src` 사본에서 돌렸다(제품 `src/` 무접촉):
  M1 `sleep(0.06)` → `2.914 ms/1000자 > 1.6` · M2 캡 100,000 → `950ms > 500` ·
  M3 `PASSAGE_LIMIT` 20 → `665ms > 500` · **M4 캡 50,000 → `500000 > 350000`(새
  단언)** 넷 다 FAILED rc=1 · M5 캡 28,000 → `OK`. 셋이 겹치지 않고 하나씩 잡는다.
- 결과: 전수 `Ran 605 tests · OK · rc 0` · **13.5초**(직전 13.6 — 새 단언은 산술이라 0초).
  문서 가드 4종 `Ran 8 tests · OK · rc 0` · `data/crawl.db` sha256 앞뒤 동일
  (`85c96744…5bda18`) · `src/`·`e2e/`·`docs/specs/` 무접촉 · 재색인 0 · 원격 `c09c545`.
- **못 메운 것 — 계획 4절 완료 기준 6 이 아직 거짓이다.** 세 기록 자리 중 **둘이
  낡았다**: `src/websearch/indexer.py:41~64` 주석과 `docs/project.md:185~192` 가 여전히
  «0.44 · 154ms · 예산의 31% · 배정치 1/3 은 모양 배수를 흡수하는 자리» 인데 테스트는
  «0.95 · 332.5ms · 67% · 흡수할 배수가 없다» 다 — **정면 모순**(중요도 7). 고칠 자리가
  `src/` 라 이번 스텝의 지시 범위 밖이라 손대지 않고 다음 스텝으로 넘긴다.
- 러너 규율 **위반 1회(누적 36)** — 문서 가드를 `2>&1 | tail -20` 에 넣어 rc 를 잃었고
  맨몸 재실행으로 `rc=0` 을 확인했다. 방아쇠는 「출력이 길 것 같다」.
- 다음: **개발(짧은 스텝).** 기록 자리 둘을 오늘 값으로 맞춰 완료 기준 6 을 참으로
  만든다. 그 다음이 리뷰 phase 다.

## 2026-09-05 15:20 | passage-cost-axis | 개발 1/1 | 시도0

- 한 일: **완료 기준 6 의 낡은 기록 자리 둘을 오늘 값으로 맞췄다** — `src/websearch/indexer.py`
  (**주석만** · 제품 동작 0줄)와 `docs/project.md ## 품질 기준`. 「0.44 · 154ms · 예산의 31% ·
  배정치 1/3 이 모양 배수를 흡수」 → 「0.95 · 332.5ms · **67%** · 흡수할 배수가 없다 + 상수 동결
  (`캡 × PASSAGE_LIMIT ≤ 35,000 × 10`) + 계수 상한 1.60」. `project.md` 의 「예산 25% 선에서
  막는다」도 같은 이유로 낡아 함께 고쳤다(같은 단언을 가리키는 문장이다).
- 결과: 전수 `Ran 605 tests · OK` · rc 0 · 13.8초(앞뒤 동일) · 문서 가드 4종 `Ran 8 tests · OK` ·
  rc 0 · 낡은 숫자 잔존 grep 0건(남은 `0.44`·`1/3` 은 «세 번 낡았다» 이력 서술뿐) ·
  `data/crawl.db` sha256 앞뒤 동일(`85c96744…5bda18`) · `tests/`·`e2e/`·`README.md`·`docs/specs/`
  무접촉 · 재색인 0 · 새 의존성 0.
- 회전: 300줄 상한에 닿아 계획 56 다섯 반복을 `history_059.md` 로 밀었다(digest 명부·회전 줄 갱신).
- **남은 구멍**: 세 기록 자리를 묶는 **기계 검사가 없다** — 이번에도 사람이 grep 으로 맞췄고
  `DocCitationTest` 는 줄번호 인용만 본다. 계수가 네 번째로 낡으면 같은 자리가 다시 갈린다.
- 다음: **리뷰 phase**(계획 57 스텝 1 은 이것으로 완료 기준 1~7 을 전부 닫았다).

## 2026-09-05 16:05 | passage-cost-axis | 리뷰 1/1 | 시도0

- 한 일: **백지 패스(diff 와 수정 파일만) → 대조 패스** 순으로 계획 57 의 diff 를 봤다.
  변이를 손으로 심어 단언 셋이 각자 무엇을 잡는지 실측했고, 계획이 세운 「축의 끝」
  주장은 **반증을 시도**했다 — 캡을 채운 26개 모양을 같은 판에서 재서 `<p>` 를 이기는
  것이 있는지 봤다.
- 결과(변이): ① 리터럴 `0.95`→`1.50` **FAILED**(525.0>500, ①-b 는 초록) · ①-b 캡
  35,000→50,000 **FAILED**(500000>350000) · ② `extract_blocks` 앞 1.0ms/1000자 지연
  **FAILED**. **①·①-b 는 서로 다른 변이를 잡는다**(「①-b 가 ① 을 삼킨 죽은 단언」
  후보는 이것으로 반증돼 버렸다). 전부 원복 · 워킹트리 무변.
- 결과(반증 실패 = 클레임 유지): 상위 모양이 `<P>` 0.941 · `<p>` 0.919 · `<p>가` 0.880 ·
  `<x>` 0.863 이고 속성(`<p a=1>` 0.526)·닫는 태그(`</p>` 0.413)·주석(`<!--` 0.002)은
  전부 아래 — **`<p>` 계열이 실제로 끝**이다. 부하 여유도 재현했다(코어 10개 스핀 루프에서
  min-of-3 최대 **1.214** → 1.60 까지 **1.32배**, 설계의 1.36배와 오차 안).
- findings **3건 · 전부 low**(신뢰도 85~90) — [R57-1] 「0.95 ≥ 0.939~0.970」 은 부등호가
  거짓이고 같은 주석이 두 번 선언한 «낮은 쪽으로 안 내린다» 를 자기가 어겼다(오늘 idle
  표본도 0.955 가 나온다. 바른 값은 0.97 → 339.5ms · 68%, 여전히 500 안이라 동작 0) ·
  [R57-2] 테스트 791~807 이 아직 옛 세계다 — 791 「예산 500ms 의 **1/3**」 이 815
  `assertLessEqual(worst_ms, 500)` 과 정면 모순이고 0.44 를 현재형으로 서술한다 ·
  [R57-3] 설계 문서가 코드보다 뒤처졌다(「② 가 ① 리터럴을 붙든다」 는 0.95~1.60 구간에서
  거짓 · 단언 블록에 ①-b 가 없다). **셋 다 「살아 있는 코드 옆의 낡은 숫자」 한 종류**이고
  고칠 자리가 `tests/`·`src/`·설계라 이번 스텝의 지시 범위 밖이라 넘긴다.
- **프로세스 medium 1건** — macOS 시스템 파이썬이 `sys.pycache_prefix`
  (`~/Library/Caches/com.apple.python`)를 써서 **저장소 안 `__pycache__` 를 지워도 낡은
  바이트코드가 남는다.** 크기가 같은 편집을 같은 초에 원복하면 mtime(초)+size 검사를
  통과해 **원복 뒤에도 변이 결과가 다시 나온다** — 이번에 실제로 속았다(M3 이 원복된
  트리에서 525.0). **이 저장소의 모든 변이 실측이 이 함정 위에 있다.** 처방:
  `rm -rf ~/Library/Caches/com.apple.python$PWD` 를 변이 검사 앞에 붙인다.
- 검증: 전수 `Ran 605 tests · OK` · rc 0 · 13.7초 · 문서 가드 4종 `Ran 8 tests · OK` · rc 0 ·
  `data/crawl.db` sha256 앞뒤 동일(`85c96744…5bda18`) · `src/`·`tests/`·`e2e/`·`README.md`·
  `docs/specs/` 무접촉 · PR 무접촉 · 원격 `loop/passage-cost-axis` = `e6f375c`(HEAD 일치).
- 다음: **e2e phase.** 계획 5절이 새 e2e 파일을 배제했으니 그 면제 판단을 확인하고
  기존 e2e 를 돌리는 자리다.

## 반복 338 — e2e (계획 57 `passage-cost-axis` 1/1) — **DONE**

- **e2e 21종 전수 `rc 0` · 기준선(계획 56) 회귀 0** — 정확도 100.0%(=) · `/passages` p95
  1.51ms(1.53) · `perf_search` p95 8.96ms(8.95) · ko 20/20 · en 19/20 · 크롤 10.23/s(10.30) ·
  디자인 4축 통과. **새 e2e 파일 면제는 유지** — 만진 것이 단언과 주석뿐이라 프로세스
  경계에서 달라질 동작이 0이고, 안쪽 상한은 `e2e/passage_eval.py` 가 이미 실물로 잰다.
- **[R57-1] 은 0.97 이 아니라 1.00 으로 갔다** — 오늘 idle min-of-3 두 벌이 0.931~0.974 라
  리뷰가 제안한 0.97 도 깨진다. 20표본 0.917~0.974 위로 올려 세 자리를 **1.00 · 350ms ·
  70%** 로 맞췄다(허용 `캡 × LIMIT` 은 +50.4% → +42.9% 로 **조여진다**).
  [R57-2] 낡은 «배정치 1/3» 주석 정정 · [R57-3] 설계 계약에 ①-b 추가 + 「② 가 ① 을
  붙든다」를 「1.60 위에서만」으로 정정.
- **변이 넷을 새 캐시 접두어로 다시 쟀다** — M1 `FAILED`(2.154>1.60) · M2 `FAILED`(1000>500) ·
  M3 `FAILED`(3500>500) · M4 캡 5만 `FAILED`(500000>350000 · ①-b 만 잡는다, ① 은 정확히
  500.0). 저장소 밖 사본 · 원복 `diff -r` 무변 · 사본 삭제. 함정은 `digest [7]` 등재하고
  처방을 `project.md` 「변이 검사」 줄에 넣었다(`PYTHONPYCACHEPREFIX=$(mktemp -d)`).
- 검증: 전수 `Ran 605 tests · OK` · rc 0 · 13.837초(착수 전 13.655 대비 +0.18) · 문서 가드
  `Ran 16 tests · OK` · rc 0 · `data/crawl.db` sha256 앞뒤 동일(`85c96744…5bda18`) ·
  `e2e/`·`README.md`·`docs/specs/` 무접촉 · 새 의존성 0 · 재색인 0.
- 완료 기준 **7/7 충족 → signal DONE.** 다음 반복은 아카이브와 새 계획 탐색이다.

## 2026-09-05 16:30 | (passage-cost-axis 마감 → passage-cost-band) | 계획 0/1 | 시도0

- 한 일: **계획 57 아카이브.** `plan_passage-cost-axis.md` → **`plan_history_043.md`** ·
  `design_passage-cost-axis.md` → **`design_history_043.md`**(`git mv` · 내용 무변경).
  `index.md` 의 계획 57 줄을 **진행 1/1 → 완료 1/1**(완료 기준 7/7 · 21종 rc 0 · 회귀 0)로
  닫고 아카이브 파일명을 적었다.
- 한 일: **계획 58 탐색.** 1~5순위 실측 **0건** — 전수 `Ran 605 tests in 15.832s` · `OK` ·
  rc 0 · 린터/타입체커 없음 · `TODO`/`FIXME`/`HACK` 이 `src`·`tests`·`e2e` 에 **1건**인데
  그것은 파서 입력 문자열 안이다 · `docs/candidates.md` 없음 · `docs/patches/` 없음 ·
  `digest ## 보류` 0건. 6순위에서 **더 높은 점수 넷이 전부 막혀 있음을 항목 본문으로
  확인**했다(`[9]` `/search` 몫 = 실물 분포 → 재색인 · `[8]` 숨은 텍스트 = `_SKIP_TAGS`
  공유 → 재색인 · `[7]` macOS pycache = 처방 이미 적용 + 여는 조건이 「옛 변이 결론」 ·
  `[7]` 재파싱 몫 배정 = `docs/specs/` 판단 → 사람 결정 5번). 골라 잡은 것은 `[5]`
  「계수 상한 1.60 과 예산 정각 1.4286 사이가 열려 있다」다.
- 한 일: **`docs/plan_passage-cost-band.md`(계획 58) 작성.** 스텝 1개 · 건드릴 파일
  `tests/test_indexer.py` 하나 · 제품 0줄. **phase 는 설계** — 대안 셋(A 예산에서 유도
  1.4286 · B 리터럴 1.20 · C 자를 `time.process_time` 으로 교체)이 갈려 `plan.md` 6-1 의
  「대안이 두 개 이상이면 설계」에 걸린다. `digest` 에서 닫힌 항목 둘(`[9]` 러너 잡음 =
  계획 50 · `[7]` 캡 안 최악 = 계획 57)에 취소선을 긋고 `[5]` 에 계획 58 을 표시했으며,
  `## 완료` 에 계획 57 줄을 더하고 상한 200줄에 맞춰 오래된 완료 넷을 지웠다.
- 결과: 문서 가드 3종 `Ran 3 tests` · `OK` · rc 0(`ArchiveIndexTest`·`DocCitationTest`·
  `IterationSyncTest`) · 전수 605 `OK` · rc 0 · `src/`·`tests/`·`e2e/`·`README.md`·
  `docs/specs/` **무접촉** · `data/crawl.db` 무변경 · 새 의존성 0.
- 결과: **기점을 `main` 으로 안 잡았다** — `git ls-remote origin main` = `d1fe3e9`(계획 56)이고
  `git show origin/main:tests/test_indexer.py` 에 `1.60` 이 **0건**이다. 고칠 단언 자체가
  `main` 에 없어 `loop/passage-cost-axis` 의 아카이브 커밋에서 `loop/passage-cost-band` 를 땄다.
- 다음: **설계 phase.** 세 안의 부하 여유 배수를 실측해 고르고, 착수 전에 「계수 1.50
  변이(M5)가 오늘은 초록이다」를 먼저 재서 기록한다.

## 반복 340 — 계획 58 `passage-cost-band` 설계 (2026-09-05)

- 한 일: **설계 phase.** `docs/design_passage-cost-band.md` — **A 채택**: ② 의 상한을
  리터럴 `1.60` 대신 `500 / (MAX_PASSAGE_HTML/1000 × PASSAGE_LIMIT)` = **1.4286** 으로
  그 자리에서 유도한다(창 폭 0 · 캡·건수를 올리면 상한이 같이 내려가 M2·M3 도 ② 에서 죽는다).
- 결과: **부하 배수가 상수가 아니라는 것이 갈랐다** — 같은 입력·min-of-3·15판에서
  한가 0.996 · 5코어 1.185 · **10코어 1.730**(계획 57 의 1.25배가 오늘 1.79배)이라
  **현행 1.60 도 전코어 부하에서 4/15 로 빨개진다.** 「부하에서 안 흔들린다」는 어떤
  값으로도 못 사고, 반코어까지는 1.4286 과 1.60 이 똑같이 초록이라 **창이 공짜로 닫힌다.**
- 결과: **C 를 실측으로 반증했다** — `time.process_time` 은 전코어 부하 1.612 로 벽시계
  1.636 의 6% 만 줄인다(경합이 «대기» 가 아니라 진짜 CPU 시간이다). B(1.20)는 반코어
  1.185 대비 여유 1.01배로 오탐 확정. 곁가지 셋도 기각 — min-of-10/30(1.518/1.475) ·
  잣대 비율 정규화(꼬리 39~41% 로 절대값 18% 보다 나쁘다) · 흩어짐 `skip` 게이트
  (한가 ≤3.2% 대 부하-조용 4.1% 로 분리가 얇고 가드가 조용히 꺼진다).
- 결과: 코드 0줄 · `src/`·`tests/`·`e2e/`·`README.md`·`docs/specs/` **무접촉** ·
  `data/crawl.db` 무변경 · 새 의존성 0 · 문서 가드 4종 초록.
- 다음: **개발 1/1.** 착수 전에 「계수 1.50 변이(M5)가 오늘은 초록이다」를 먼저 재서
  기록하고(완료 기준 2), ② 한 줄과 `ponytail:` 주석을 바꾼다. 실패 메시지에 잰 계수·
  최악 ms·한가할 때 기대 범위를 함께 찍어 「기계가 바쁨」과 「파서 회귀」를 가르게 한다.

## 반복 341 — 계획 58 `passage-cost-band` 개발 1/1 (2026-09-05)

- 한 일: **설계 A 구현.** `tests/test_indexer.py` ② 의 리터럴 `1.60` → `500 / (캡kB × 건수)`
  로 그 자리 유도(1.4286) · `ponytail:` 주석을 설계 3절 문장으로 교체 · 실패 메시지에
  **잰 계수 · 최악 ms · 한가할 때 기대 범위**를 함께 찍었다. `docs/project.md` 의 1.60 기록도 갱신.
- 결과: **착수 전 M5(계수 1.51 · 최악 529ms)가 초록 3/3** — 창이 실재했다. 구현 후 같은
  변이(스핀 고정 1.457~1.477 · 최악 510~517ms)가 **빨강 5/5**. M2(캡 10만 1000ms)·M3
  (`PASSAGE_LIMIT` 100 · 3500ms) 여전히 빨강. 전수 **605 OK** · 스위트 13.810 → 13.897s(+0.09s).
- 결과: **오탐 — 반코어 부하 20/20 초록.** 전코어(10/10)는 1.275~1.697 로 상한 초과 13/15
  인데 **옛 1.60 도 2/15 로 깨진다** — 설계가 이미 「값으로 못 사는 자리」로 판정하고
  메시지 분간으로 받은 대가다(완료 기준 4 를 문자 그대로는 못 맞춘다 · 설계 2절).
- 결과: `src/` 제품 **0줄** · 새 파일 0 · 새 의존성 0 · `data/crawl.db` 무변경 ·
  `docs/specs/`·`e2e/`·`README.md` 무접촉. 계수 1.00/350ms/70% 세 기록 자리 그대로.
- 다음: **테스트 phase.** 빠뜨린 자리를 찾고 전수를 돈다.
