---
signal: YELLOW
phase: 계획
step: 0/0
attempt: 0
iteration: 376
updated: 2026-09-06
ctx: 60
night_iterations: 187
night_red: 2
night_retries: 4
plan: null
---

## 현재 상태

**계획을 열지 않았다 — 탐색 막힘으로 YELLOW 정지다.** 8개 출처를 하나도 「해당 없음」으로
넘기지 않고 전부 실제로 돌렸는데 **착수 가능한 근거가 0건**이다. `rules/discover.md` 2절
(「구체적 근거를 못 찾으면 YELLOW 로 정지한다 · 개선할 만한 것을 상상해서 만들지 않는다」)과
`SKILL.md` 4-1(「짧은 경로 연속 3건이면 근거의 출처를 본다 — 셋이 같은 출처면 탐색이 막힌
것이니 정지」)이 이번엔 같은 곳을 가리킨다. 코드는 한 줄도 안 썼다.

## 출처별 실측 — 0건이 「안 봤다」가 아니라 「돌려서 0」이다

- **1순위 실패하는 테스트 — 0건.** 전수 맨몸 `Ran 622 tests in 15.867s` · `OK` · rc 0.
- **2순위 린터·타입체커 — 없다.** stdlib 전용 저장소라 설정된 도구가 0개다(가정이 아니라
  `pyproject.toml`·`setup.cfg`·`.flake8` 부재로 확인). 도구가 없다는 것이 이 출처의 값이다.
- **3순위 `TODO`/`FIXME`/`HACK` — 실행되는 코드에 0건.** `src/`·`tests/`·`e2e/` 전수 grep 이
  문 것은 `tests/test_indexer.py` 의 **fixture 문자열** 하나뿐이라 근거가 아니다(2절이 경고한
  「설명용 예시」다). `scripts/` 는 없다.
- **4순위 사용자 대기열 — 없다.** `docs/candidates.md`·`docs/patches/`·`scripts/` 전부 부재.
- **5순위 보류(승인 대기) — 0건.** `digest ## 보류` 의 본문은 통째로 HTML 주석이다.
- **6순위 다음 계획 후보 — 8점 이상 열린 것 0건.** `[9]`·`[8]` 셋은 전부 이미 소비됐거나
  일반화 교훈이고(`/passages` p95 는 계획 48 이, 「깨우기와 접기」는 34·35 가, 「측정 하네스가
  재지 않는 세계」는 `perf_crawl` 의 `BASELINE_BLOCKED` 가 닫았다), 유일하게 살아 있는
  `[8]` 숨은 텍스트의 뒷절반은 **여는 조건이 명시적으로 미도래**다 — `_INLINE_TAGS`·
  `_SKIP_TAGS` 가 색인 경로와 공유라 손대면 재색인이고, 그것이 승인 게이트다.
- **7순위 반복 실패 — 열린 처방 0건.** 살아 있는 항목 둘(「선례를 옮기면 전제는 안 따라온다」
  3회 · 「제자리 변이를 자동 스냅샷이 커밋한다」 5회)은 **규율이지 계획이 아니다** — 오늘도
  변이는 `.git` 없는 사본에서만 심었다.
- **8순위 판단 필요 — 전부 승인 대기.** `[8]` indexer 증분·`[high]` URL 자격증명 등은
  이름 그대로 사람의 판정을 기다리는 자리다. **후보가 남아 있는데 혼자 열 수 없다** — 이것이
  `metrics ## 정지 사유` 가 「계획 소진」과 갈라 세는 **「탐색 막힘」의 정의 그대로**다.

## 출처 편중을 정면으로 판정했다 — 여섯 번째는 정당하지 않다

계획 **60·61·62·63·64 다섯이 연속**으로 `tests/test_docs.py` 하나만 고쳤고 제품 `src/` 는
다섯 번 다 **0줄**이며, 근거도 다섯 번 다 `digest ## 다음 계획 후보 (테스트 phase 갭)`
같은 광맥에서 나왔다. `SKILL.md` 4-1 의 문턱은 **연속 3건**이다. 다섯은 그 문턱을 넘고도
두 번 더 간 것이고, 여섯째를 같은 자리에서 뽑는 것은 **탐색이 막힌 것을 생산성으로
착각하는 일**이다. 그래서 다른 출처를 먼저 뒤졌고, 위 8줄이 그 결과다.

## 착수 탐침 둘을 돌려 기록된 전제가 지금도 참인지 쟀다 — 둘 다 거짓이었다

**탐침 A — 「README 의 성능 수치가 실물과 어긋날 수 있다」는 거짓이다.** README 「잘하고 있나
재는 자」 표에는 실측치가 **한 개도 없다** — 담긴 일곱은 전부 **합격선**(80% · 90% · 0.3s ·
0.5s · 5장/s · JS 50KB · 4.5배/3배)이다. 낡을 수 있는 실측 숫자는 「단위 622건」·「e2e 21종」
둘뿐이고 그 둘은 `tests/test_readme.py` 의 `test_verification_counts_match_reality` 가
이미 실제 개수와 대조한다. **다만 이 탐침이 다른 것을 하나 남겼다** — 그 합격선 일곱은
`quality_eval`·`passage_eval`·`perf_search`·`perf_crawl`·`design_check` 의 상수를 손으로
옮겨 적은 **무가드 복제**다. 상수를 고치면 README 는 조용히 낡는다. 근거는 있지만
**이것이야말로 여섯 번째 문서 하네스**라 위 판정에 걸려 오늘 안 연다.

**탐침 B — 「품질 기준선 회귀는 사람 눈만 잡는다」는 거짓이다.** `.git` 없는 사본에서
`indexer._fts_query` 의 2-gram 열 둘(`title_ng`·`body_ng`)을 하나로 합치는 변이를 심었다.
`e2e/quality_eval.py` 는 ko 20/20 → **17/20(85%)** 로 떨어지고 답 셋이 1위에서 12~13위로
밀렸는데도 「합격선 80% — 통과」 · **rc 0** 으로 **조용했다**. 그런데 전수는 `Ran 622` ·
**`FAILED (failures=4)`** · rc 1 로 **울었다** — `tests/test_quality_eval.py` 의
`KO_BASELINE = 20`·`EN_BASELINE = 19` 가 이미 그 자리를 지키고 있었다. **합격선은 80 이지만
기준선은 20/20 이고, 둘을 가른 것이 오늘 계획 하나를 없앴다**(ponytail 1칸 — 있을 필요가
없는 것이다).

## 물어보는 것 — 셋 중 하나를 열어 주면 곧바로 계획이 선다

1. **여섯 번째 문서 하네스를 허용한다** — 탐침 A 가 남긴 README 합격선 일곱을 `e2e/*.py`
   상수와 묶는 가드(`tests/test_readme.py`, 짧은 경로 크기). 근거는 확보돼 있고 출처 편중만이
   막고 있다.
2. **`digest ## 판단 필요` 에서 하나 열어 준다** — 예: `[8]` indexer 증분이 갱신을 반영 안 함,
   `[high]` URL 자격증명이 `pages.url` 에 저장·렌더. 둘 다 제품 `src/` 축이다.
3. **`docs/specs/concept.md` 에 방향을 적어 준다** — 기능·경량·성능·디자인 4축 중 어디를
   밀지. 지금은 4축이 전부 합격선 위라 축 자신이 다음 일을 못 고른다.

**막힘이 실제로 이렇게 풀린 선례가 있다** — 2026-09-01 반복 222 에서 사람이 후보를 **하나만**
열어 주자 곧바로 계획 44 가 섰다.

## 검증

전수 **맨몸** `Ran 622 tests in 15.867s` · `OK` · **rc 0**(변이를 되돌린 실물 위에서).
변이는 저장소 밖 사본에서만 — `~/work/websearch` 의 `git status --short` 는 이 반복 내내
빈손이었고, 고친 파일은 `docs/` 셋뿐이다.
`data/crawl.db` 무변경 · 재색인 0회 · `docs/specs/` 읽기만 · 의존성 0.

## 다음

**사람이 답할 때까지 새 계획은 안 연다.** 답이 오면 그 지시가 곧 근거이므로
(`rules/discover.md` 0절) 탐색을 다시 돌리지 않고 바로 계획서를 쓴다.
답 없이 다음 반복을 돌리면 위 8줄이 그대로 반복될 뿐이다 — 그것이 「막힘」이 「소진」과
다른 점이다.

## 한도

- 병합은 사람 몫이다 — 계획 57~64 의 커밋이 `loop/passage-cost-band` 에 쌓여 있고
  `origin/main` 무접촉 · PR #7 무접촉(`gh pr` 호출 0).
- `--force`·`--amend`·`rebase` 없음. 스텝 하나 = 커밋 하나.
- 러너에 리다이렉션·파이프를 안 붙인다 — 이번 반복도 위반 **0회**(누적 38 유지).
- `PYTHONDONTWRITEBYTECODE=1` 과 `PYTHONPYCACHEPREFIX=$(mktemp -d)` 를 함께 준다.
- `docs/digest.md` 는 **200줄 정각**이라 이번엔 한 줄도 더하지 않았다 — 오늘 남길 것은
  전제 두 개의 **반증**이고, 그것을 적으려고 완료 항목을 지우면 손해다. 다음 탐색이
  읽는 자리(여기와 `history_current.md`)에 남긴다.
